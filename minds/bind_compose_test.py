"""bind_compose_test — a binding-mind: mode 'compose-test' → the 'pair' composition.
mode→composition binding as registry DATA (a minds/ row, NOT a suite.py mode-field — keeps suite.py
single-owner). Rebinding the mode to a different composition is a ROW edit here, never code."""
MIND = {
    "id": "bind_compose_test",
    "kind": "binding",
    "mode": "compose-test",
    "mind": "pair",
}
