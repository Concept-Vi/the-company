"""guides/using_corpus_pipeline.py — agent-authored guide (the narrative-guide face — guide://using_corpus_pipeline).
Authored through the agent surface (create_guide / the guide-author); validated by
import-in-a-temp-dir before it reached the live guides/ tree. A guide ROW: guide://using_corpus_pipeline
resolves to its declared narrative `content` (the how-to a learner reads for skill://corpus_pipeline)."""
GUIDE = {
    "id": "using_corpus_pipeline",
    "content": "### What it is\nThe corpus pipeline is a three-layer process that transforms a raw corpus of N units into discoverable, addressed, and queryable knowledge. It relies on the \"registry-is-truth\" principle, meaning all role IDs, projection IDs, and space IDs must be read from `cognition_info()` before use; fabricating an ID is a failure. The pipeline chains three distinct phases: Layer 1 captures and persists durable records, Layer 2 (handled by the engine) embeds these records into vector spaces, and Layer 3 reduces the data by clustering similar units and synthesizing findings. This process is grounded in the map-reduce composition, where a per-unit \"map\" operation feeds directly into a cross-unit \"reduce\" operation.\n\n### When to reach for it\nUse the corpus pipeline when you need to turn a large set of units into a structured, queryable knowledge base. Specifically, reach for it when:\n*   You need to persist durable corpus records with lineage (use `capture`), versus just generating intermediate outputs (use `run_items`).\n*   You need to discover groupings of similar units (use `run_reduce` with mode='cluster').\n*   You need to synthesize a single finding across multiple units (use `run_reduce` with mode='role').\n*   You are engaging in a discovery loop where you run an operation, look at the output, mark a pattern by creating a new lens, and re-run with that lens to find new insights.\n\n### How (the steps)\nFollow this ordered sequence to wire the pipeline correctly:\n\n1.  **Orient**: Call `cognition_info()` to retrieve valid `.roles`, `.projections`, and `.spaces`. Never invent an ID.\n2.  **Layer 1 (Capture)**: Run `capture(role=<describe-role>, units=[<N units>], project=<P>, session=<S>, round=<R>, projection=<lens>)`. This persists one durable record per OK unit. Ensure `project`, `session`, and `round` are provided; missing them causes a refusal.\n3.  **Layer 1b (Extract and Project)**: Feed the addresses from the capture step into `run_items(role=<extract-role>, items=[<addresses>])` to extract structured fields. Then, feed those addresses into `run_items(role=<project-role>, items=[<extract addresses>])` to re-describe them through a specific lens.\n4.  **Layer 2 (Embed)**: The engine automatically handles embedding for embeddable lenses. If a standalone vector is needed for an arbitrary input, use `run_role(role=<r>, op='embed', ensure=True)`, but note this does not populate the corpus space index.\n5.  **Layer 3 (Reduce)**:\n    *   **Cluster**: Run `run_reduce(addresses=[<addresses from L1/L1b>], mode='cluster', cluster_threshold=0.85)` to group similar units.\n    *   **Synthesize**: Run `run_reduce(addresses=[<clustered addresses>], mode='role', role=<reduce-role>)` to write one finding per group.\n    *   *Alternative*: Use `mode='rule'` for deterministic aggregation (e.g., count, concat, first) without a model.\n6.  **Read Results**: Use `corpus(op='list'|'find', ...)` to list records or `marks(by='target', target=<address>)` to read the gold-likelihood profile left on an address.\n\n### A worked example\n1.  **Orient**: Call `cognition_info()` and note `role_id='describe'`, `proj_id='principles'`, `space_id='vectors'`.\n2.  **Capture**: Run `capture(role='describe', units=[unit_a, unit_b], project='P', session='S', round=1, projection='principles')`. This returns `{captured: [{i:0, address: 'run://...'}, {i:1, address: 'run://...'}], ...}`.\n3.  **Extract/Project**:\n    *   Run `run_items(role='extract', items=['run://...'])` to get extracted addresses.\n    *   Run `run_items(role='project', items=[<extracted addresses>])` to get projected addresses.\n4.  **Reduce**:\n    *   Run `run_reduce(addresses=[<projected addresses>], mode='cluster', cluster_threshold=0.85)`. This groups units.\n    *   Run `run_reduce(addresses=[<clustered addresses>], mode='role', role='reduce_synth')` to synthesize findings for each cluster.\n5.  **Read**: Call `marks(by='target', target=<final_address>)` to see the findings.\n\n### Gotchas\n*   **Fabricated IDs**: Never invent a role, projection, or space ID. A fabricated ID causes a failure (rule 8). Always read IDs from `cognition_info()`.\n*   **Lineage Gates**: A `capture` record missing `project`, `session`, or `round` is refused and will fail loudly.\n*   **Missing Embedder**: If you attempt `mode='cluster'` or `mode='role'` without the embedder being resident, the operation fails loudly.\n*   **Map vs Reduce Confusion**: Do not use `run_reduce` for independent per-unit tasks; use `run_items` (MAP) for that. Only use `run_reduce` when combining across units.\n*   **Poison Units**: If a unit produces no record (skipped/failed), it will not appear in the final `addresses` list for the reduce step.\n*   **Marking Patterns**: There is no direct `write_mark` tool today. To mark a pattern discovered during a loop, you must encode it by creating a new projection lens via `create_projection` and re-running the capture with that lens.\n*   **Inversion Queries**: When using `find_relations`, ensure you are using the correct space keys (near/far) to find what is missing or generic relative to a known fingerprint.",
    "target": "skill://corpus_pipeline",
    "grounded_from": [
        "skill://corpus_pipeline",
        "skill://map_reduce_composition",
        "skill://patterned_visibility"
    ],
    "source_hash": "26b4978b8f7ec8b4",
    "label": "Using the corpus pipeline",
    "description": "How to run the 3-layer corpus pipeline, grounded in the corpus_pipeline + map_reduce + patterned_visibility skills."
}
