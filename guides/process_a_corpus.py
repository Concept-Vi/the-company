"""guides/process_a_corpus.py — agent-authored guide (the narrative-guide face — guide://process_a_corpus).
Authored through the agent surface (create_guide / the guide-author); validated by
import-in-a-temp-dir before it reached the live guides/ tree. A guide ROW: guide://process_a_corpus
resolves to its declared narrative `content` (the how-to a learner reads for skill://full_coverage_dragnet)."""
GUIDE = {
    "id": "process_a_corpus",
    "content": "PROCESS A CORPUS — START HERE.\n\nYou've been pointed here to process a WHOLE body of material (an email export + attachments, a\ntranscript set, a doc folder, a repo) into the Company: embedded, addressed, queryable, and\nself-organizing. You are NOT the first to do this shape of work — the pattern, the models, and the\nsharp edges are already captured. Read, then suggest + work it out WITH Tim (he directs by ideas +\nrecognition, never specs; surface options, don't wait for instructions).\n\nDO THIS, IN ORDER:\n1. Read context://session_laws FIRST. Non-negotiable rules, each paid for in a real failure. They\n   govern everything — especially fail-loud, external-reference completeness, truncation-is-lazy,\n   open-vocabulary, private-data-governance, capture-before-reduce.\n2. Read skill://full_coverage_dragnet — the generalized pipeline (derive the universe → structural\n   floor → lossless extract → open-vocab tag → embed → cluster→nucleate) + the hard disciplines.\n3. Read the three capability skills it leans on:\n   • skill://running_models — which model tier for which layer, fired through the engine.\n   • skill://reliable_structured_output — how to get valid JSON at all (cloud ignores `format`;\n     schema rides the prompt; open vocab; the gotchas that cost days).\n   • skill://corpus_and_coordinates — embed/store/query by meaning + address + relation.\n4. ORIENT against the LIVE system (registry-is-truth, never invent an id): cognition_info() for\n   roles/projections/spaces, capabilities() for tools/models/flows, flows(op='list') for existing\n   production lines to REUSE (repo_ingest, transcript_mine, pattern_cluster — don't rebuild),\n   `company status` / list_skills_contexts() for what's live.\n\nFOR A NEW SOURCE-TYPE (e.g. Zoho email + attachments):\n  • The pipeline ABOVE L0 is source-blind — identical machinery. Only the L0 STRUCTURAL extractor\n    is source-specific, and it is DETERMINISTIC code, not a model: email = RFC5322 via the stdlib\n    email parser (headers, parts, Message-ID, In-Reply-To/References); attachments = walk + type by\n    extension + hash. Do NOT have a model 'discover' structure a standard already defines.\n  • Native ids are addresses (Message-ID is globally unique — never synthesize one). Native edges\n    are a gift: In-Reply-To/References = a real reply-DAG; land them as edge rows, absorb-never-reject.\n  • PRIVATE DATA: email is Tim's private data — default LOCAL models only for content; cloud is\n    per-source opt-in by Tim. SURFACE this to him before any cloud model sees a body; don't decide it.\n  • A genuinely-unknown source-type = a model proposes its schema-row ONCE (nucleating a new\n    source-type, same lifecycle as a new kind), then deterministic thereafter.\n\nThe measure of done is the external-reference set-diff (context://session_laws), never a feeling.\nBring Tim the sample records from a measured gate before opening the throttle over everything.\n\nFLOOR: everything here composes READ/RUN/create tools — never resolve/approve/dispatch, never claude -p.",
    "target": "skill://full_coverage_dragnet",
    "grounded_from": [
        "skill://full_coverage_dragnet",
        "skill://running_models",
        "skill://reliable_structured_output",
        "skill://corpus_and_coordinates",
        "context://session_laws"
    ],
    "label": "Process a corpus — start here (point a fresh session at this)",
    "description": "The single entry point for a session tasked with processing a whole body of material (emails+attachments, transcripts, docs, a repo) into the Company. Orients, then sends you to the skills + laws in order. Built from the mesh/archaeology session, 2026-07."
}
