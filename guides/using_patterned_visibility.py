"""guides/using_patterned_visibility.py — agent-authored guide (the narrative-guide face — guide://using_patterned_visibility).
Authored through the agent surface (create_guide / the guide-author); validated by
import-in-a-temp-dir before it reached the live guides/ tree. A guide ROW: guide://using_patterned_visibility
resolves to its declared narrative `content` (the how-to a learner reads for skill://patterned_visibility)."""
GUIDE = {
    "id": "using_patterned_visibility",
    "content": "### What it is\nThe Patterned-Visibility Loop is a discovery process where insight is not a pre-scripted plan but a continuous cycle: run an action, look at the output, mark the pattern you see, and use that mark to reveal the next step. It relies on the `find_relations` tool (Inversion Query) to find items that share a principle in one space but differ in another, or to identify what is missing from a known pattern.\n\n### When to reach for it\nUse this loop when you need to discover non-obvious relations or missing elements that flat similarity searches cannot provide. Specifically, reach for it when:\n*   You have produced a corpus and need to compare records across different axes (e.g., principles vs. topics).\n*   You want to find what is \"missing\" or generic by comparing a known fingerprint against a space of genuine substance.\n*   You need to tune neighbor thresholds to refine which items are considered \"near\" or \"far.\"\n*   You have already run the 3-layer corpus pipeline and are ready to analyze the resulting vector spaces.\n\n### How (the steps)\n1.  **RUN**: Produce something to look at by running a capture or reduce operation.\n    *   Capture fresh data: `capture(role, units, project, session, round, projection=<lens>)`.\n    *   Map over items: `run_items(role, items)`.\n    *   Group similar items: `run_reduce(addresses, mode='cluster')`.\n2.  **LOOK / COMPARE**: Read the output and compare pieces side-by-side using read-only tools.\n    *   List all records: `list_corpus(project=P)`.\n    *   Filter by axis: `find_corpus(project=P, projection=<lens>, kind=, source_address=)`.\n    *   Read a specific record: `read_corpus_record(address)`.\n    *   Discover past runs: `find_runs(role=, op=, run_op=)`.\n    *   **Perform Inversion**: Run `find_relations(item, near_space, far_space)` to find items near in one space but far in another.\n    *   Check existing marks: `findings_for(address)`.\n3.  **MARK THE PATTERN**: Since there is no direct write-mark tool, encode the pattern by creating a new lens.\n    *   Create a projection: `create_projection(spec={id, level, produced_by, embeds, field?, enum?, desc, stage?})`.\n    *   The `desc` field describes what the unit claims; it does not judge truth.\n    *   If `embeds` is true, the engine populates the vector space for future `find_relations` queries.\n4.  **THAT REVEALS THE NEXT STEP**: Re-run the capture with the new lens.\n    *   Increment the round: `capture(..., round=<R+1>, projection=<the new lens>)`.\n    *   If the lens has embeddings, the vector space is updated.\n    *   Return to Step 2 to look at the new output, compare it to the prior round, and mark the next pattern.\n\n### A worked example\n1.  **Run**: Execute `capture(role='principles', units=['A', 'B'], project='P', round=1, projection='default')`.\n2.  **Look**: Run `find_relations(item='A', near_space='principles', far_space='topics', min_score=0.5)`. This returns items that share 'A''s principle but differ in topic.\n3.  **Mark**: Notice that item 'C' shares the principle but lacks the specific depth of 'A'. Create a new lens: `create_projection(spec={id='depth_lens', level=1, produced_by='model', embeds=true, desc='units with deep principle application'})`.\n4.  **Next Step**: Run `capture(role='principles', units=['A', 'B', 'C'], project='P', round=2, projection='depth_lens')`.\n5.  **Repeat**: Look at round 2 output, compare to round 1, and mark the next pattern.\n\n### Gotchas\n*   **No Silent Failures**: If `find_relations` returns an empty result, it is not because the query failed; it means no items satisfy the near/far condition.\n*   **Prerequisite Vectors**: The `item` must already have a persisted vector in both the `near_space` and `far_space`. If it does not, `find_relations` will raise an error. Run the capture+embed pass first.\n*   **Valid Space IDs**: Never invent a space ID. Always get space IDs from `cognition_info().spaces`. Using a made-up ID causes failure.\n*   **Thresholds**: Without setting `min_score`, the `far` space will contain everything, making the difference empty. Adjust `min_score` (default 0.5) and `k` to control the threshold.\n*   **Lens Description**: The `desc` in `create_projection` describes the unit's claim, not its truth value. Truth assessment happens later in a reduce pass.\n*   **No Direct Writing**: You cannot write a mark directly. You must encode it as a new lens to make it persistent and live.",
    "target": "skill://patterned_visibility",
    "grounded_from": [
        "skill://patterned_visibility",
        "skill://inversion_query"
    ],
    "source_hash": "01c4d52b6b24ef3d",
    "label": "Using patterned visibility",
    "description": "How the patterned-visibility discovery loop works."
}
