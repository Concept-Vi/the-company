"""edge_kinds/delegates_to.py — edge kind 'delegates_to' · face=knowledge.

Seen in: cvi.type_instance_edges. [needs_review: face auto-assigned; confirm the JOB] """
EDGE_KIND = {
    "id": 'delegates_to',
    "directed": True,
    "face": 'knowledge',
    "needs_review": True,
}
