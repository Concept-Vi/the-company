"""edge_kinds/generates.py — edge kind 'generates' · face=knowledge.

Seen in: cvi.type_instance_edges. [needs_review: face auto-assigned; confirm the JOB] """
EDGE_KIND = {
    "id": 'generates',
    "directed": True,
    "face": 'knowledge',
    "needs_review": True,
}
