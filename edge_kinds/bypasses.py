"""edge_kinds/bypasses.py — edge kind 'bypasses' · face=knowledge.

Seen in: cvi.type_instance_edges. [needs_review: face auto-assigned; confirm the JOB] """
EDGE_KIND = {
    "id": 'bypasses',
    "directed": True,
    "face": 'knowledge',
    "needs_review": True,
}
