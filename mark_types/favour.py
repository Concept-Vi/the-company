"""mark_types/favour.py — INTERACTION mark-type: favour (operator favour-score at an element).

The disposition the operator writes via a favour score on a rendered gallery element — the route-back of
`gallery:direction` → `territory_write` → `suite.mark`. `value` is the numeric score. direction `surface`
(operator input). An INTERACTION mark-type, beside the analysis types; additive, registry-is-truth.
id MUST equal the file stem (`favour`).
"""

MARK_TYPE = {
    "id": "favour",
    "value_shape": "score",
    "direction": "surface",
    "desc": "an operator favour score at an addressed element (value = a number) — the route-back of gallery:direction",
}
