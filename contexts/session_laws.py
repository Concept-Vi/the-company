"""contexts/session_laws.py — agent-authored context (Concurrent Cognition C 3b · #56/#58 write-half).
Authored DIRECTLY through the agent surface (create_context); validated by import-in-a-temp-dir
before it ever reached the live contexts/ tree. A registry ROW: context://session_laws resolves to its
declared `content` (the reusable context blob a role reads)."""
CONTEXT = {
    "id": "session_laws",
    "content": "STANDING OPERATING LAWS — each is a correction Tim made, now binding. Violating one is how the\nwork fails; they are not style.\n\n• FAIL LOUD. No silent skips/truncation/swallow/no-op. Record or raise, never drop. An empty or\n  crashed pass is a FAILURE, never silently a 'done'. A HANG is the worst silent failure — put\n  connect + statement timeouts on anything that shells out (a dead docker + live Windows port-proxy\n  hung the whole system 7h; 'best-effort try/except' assumed dead=fast, but half-dead=forever).\n\n• EXTERNAL-REFERENCE (coverage is a set-diff, never a feeling). A completeness claim REQUIRES a\n  DERIVED enumeration of the whole (a directory/registry/table — the address trick, cheap,\n  everywhere), a machine-readable record of what was processed, and the DIFF computed by code.\n  'done/dry/complete' only against the diff. (Claimed 58 tools; the registry had 78. Claimed the\n  archaeology 'dry'; it was dry against 0.2% of the transcripts.)\n\n• THE CLASS. A failure is never one bug — it is one instance of a class of unverified assumptions.\n  Enumerate the class (every field sent/omitted, every default trusted) and MEASURE each. 'Probably'\n  is banned. No one knows how these systems were set up — verify, don't assume. See more than you\n  looked for.\n\n• TRUNCATION IS LAZY DESIGN. Remove every [:N] cap on model-bound material. A real limit (a context\n  window) is met by chunking COMPLETELY / a reduce-tree — coverage-preserving — never by cutting.\n  (Caps had been feeding the miner CUT conversations — 474 messages truncated in one session.)\n\n• OPEN VOCABULARY (schema constrains SHAPE, never MEANING). No closed enum on a judgment word —\n  it censors the decoder and destroys the nucleation signal. Open str + guidance + invitation to\n  coin; canonical vocab emerges at the cluster layer as counts + registry rows (G16 no-confidence:\n  tags + corroboration-counts, never a fabricated float), re-openable forever. Closed sets only\n  where code branches deterministically. FACES: a thing that does several jobs keeps all of them —\n  never flatten to one winner.\n\n• NO PRIORITY / NO UPFRONT-CORRECTNESS. Don't predetermine what matters or pre-rank. Convergence\n  across many independent cheap looks replaces verification of one careful look; density ranks\n  itself. Sample only to CALIBRATE an instrument (a measured A/B), never to COVER.\n\n• DESCRIBE, DON'T JUDGE (Tim 2026-07, = the Company's legibility-layer law). An ORGANISING tag says\n  what a thing IS (descriptive fact); IMPORTANCE / RELEVANCE / keep-vs-junk is a SEPARATE, LATER\n  lens applied OVER the organised corpus, never baked into the filing. Mixing them poisons an\n  archive — you cannot re-find what you pre-judged as unimportant and buried. Organise everything\n  descriptively (fail-loud/absorb); apply relevance reversibly at RETRIEVAL, never at ingest. ('This\n  is a marketing email' = fact; 'ignore marketing' = a query-time choice you can undo.)\n\n• DETERMINISTIC-WORK-TO-CODE, JUDGMENT-TO-MODELS. Mechanical work (set-diffs, parsing, structure a\n  spec defines like RFC5322) goes to code. Models are for no-oracle judgment. A model given a\n  set-diff is the classic waste; a regex is not a parser.\n\n• CAPTURE-BEFORE-REDUCE + IDEMPOTENT RESUME. Persist every observation the instant it exists (a\n  crash then costs nothing). The skip-list IS the corpus; re-runs deepen, never duplicate;\n  version-stamp so a method change re-mines cleanly.\n\n• MEASURED GATE BEFORE SCALE. Prove a new extractor/layer on a small sample and show the records\n  BEFORE opening the throttle over the whole universe.\n\n• NO CLOUD BUDGETS. A :cloud fire omits the output cap and runs to completion; cloud is patchy →\n  more retries, longer timeouts. Local models keep budgets (VRAM/time are real).\n\n• PRIVATE DATA GOVERNANCE. Personal sources (email/Slack/Drive) default to LOCAL models only; cloud\n  is per-source opt-in by the operator, never a default. Surface the choice; don't decide it.\n\n• THE SPIRAL (attack + fold, nothing is spec). Everything found was built by agents from Tim across\n  many sessions, from multiple angles — attempts toward one centre, none authoritative alone. Treat\n  a found design/doc as dense EVIDENCE, never a spec; attack it, find what's missing, fold your\n  strengths over its edges. The real thing is between the attempts; no agent has the whole picture.\n\n• REUSE-DON'T-PARALLEL. Fire through the engine (run_role/run_items/run_reduce), author through\n  create(), query through corpus/coordinate — never a bespoke urllib/psql or a second copy of a\n  path. Going around the system loses the guards and becomes a one-off.\n\n• VERIFY BY-USE. Confirm a binding/behavior by running it and reading the result, never by reading\n  the file — a nested/mis-set binding LOOKS right and resolves wrong. 'No error' ≠ working.",
    "label": "Standing operating laws (the corrections that became rules)",
    "description": "The hard rules a session must hold when running models, extracting, embedding, and building over the Company — each one paid for in a real failure this session. Referenced by skill://full_coverage_dragnet, running_models, reliable_structured_output, corpus_and_coordinates. Tim's corrections, made standing. 2026-07."
}
