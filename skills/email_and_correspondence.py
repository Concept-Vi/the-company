"""skills/email_and_correspondence.py — agent-authored skill (Concurrent Cognition C 3b · #56/#58 write-half).
Authored DIRECTLY through the agent surface (create_skill); validated by import-in-a-temp-dir
before it ever reached the live skills/ tree. A registry ROW: skill://email_and_correspondence resolves to its
declared `content` (the reusable instructions a role reads)."""
SKILL = {
    "id": "email_and_correspondence",
    "content": "PROCESSING A CORRESPONDENCE ARCHIVE (email / messaging) — the source-specific layer under\nskill://full_coverage_dragnet. A business email export (e.g. a Zoho archive + attachments) is\nSPECIAL: unlike code or transcripts, its GRAPH ARRIVES PRE-FORMED in the headers. You are not\ninferring relations — you are ABSORBING ones handed to you. That makes it the ideal first proof of\nthe edge layer. The pipeline ABOVE L0 is the general dragnet, unchanged; this skill is the L0\n(deterministic, code) + the model-seams (judgment) + the descriptive tag design for this source-type.\n\nL0 — THE PRE-FORMED GRAPH (code, no model, 100% coverage, exact). Pull these deterministically\n(email = RFC5322 via the stdlib email parser; never a model 'discovering' structure a standard\ndefines):\n  • REPLY DAG — In-Reply-To + References reconstruct the conversation tree, and References carries\n    the WHOLE ancestor chain, so a thread survives a subject-line change. Edge kind: replies-to.\n  • PEOPLE GRAPH — From/To/Cc/Bcc are edges between people; every email is a tie. Cc = ambient,\n    To = direct, Bcc = asymmetric (only in your Sent). Edge kinds: from-sender, to, cc.\n  • ATTACHMENTS — each is its OWN addressed node, HASHED: the same file across many emails is one\n    node with many edges → document lineage + versions (same name/diff hash) + dedup for free.\n    Edge kind: attached-to. Type by Content-Type/extension.\n  • TIME — Date = the linear axis; reply latency is derivable.\n  • DIRECTION — sent vs received is a FREE deterministic facet (the From field is you / it's in the\n    Sent folder). Record it as one more fact; do NOT give it special treatment — it's already\n    separated (Tim, 2026-07).\n  • NATIVE IDS ARE ADDRESSES — Message-ID (RFC5322, globally unique) is the address; never\n    synthesize one where a real stable id exists.\n  • HEADER-FACTS (descriptive, not judgement) — List-Unsubscribe/List-Id/Precedence:bulk = 'this\n    is bulk/marketing' (a factual TYPE); noreply@/notifications@ = 'automated'. These describe\n    WHAT it is; whether it's low-relevance is a LATER lens (see below), not an L0 verdict.\n  • QUOTED-TEXT STRIPPING — HARD, load-bearing step: bodies carry the entire quoted chain ('>').\n    Strip it → each message's NEW content vs the inherited chain, or you embed the same paragraph\n    20× down a thread and poison every search. Deterministic.\n  • MISSING-MESSAGE DETECTION — a References id with no local message = a gap (incomplete export or\n    a deleted message). The thread's own chain is the external-reference completeness oracle.\nLand every edge as a row under a registered edge_kind (create(kind='relation_type')/edge if new);\nABSORB-never-reject (fail-loud/absorb law).\n\nWHERE THE MODEL GOES (the seams the headers CANNOT reach — 'a model at the right place', Tim):\n  • MATTER-STITCHING (the biggest unlock, and the ORGANISING SPINE). The reply-DAG catches\n    MECHANICAL replies; it MISSES threads that were forwarded, restarted under a new subject, moved\n    to a fresh email after a call, or re-raised months later — header link broken, but 'part of the\n    same thing'. A SMART model judges semantic + shared-people + time continuity and stitches those\n    severed threads into ONE MATTER (a client engagement, a project, a deal, a case). Threads\n    nucleate into matters. Make MATTER a first-class addressed node the whole Company can point at\n    — 'everything about client X' then spans a dozen broken threads + their attachments as one thing.\n  • IDENTITY RESOLUTION — one person across work/personal/old addresses (or a shared inbox that's\n    several people): a clustering+judgment call (a person has many address-faces — faces law), not\n    a lookup.\n  • CONTENT TAGGING — the cheap model (0.8B) OPEN-tags every message at volume; the SMART model\n    canonicalizes the clusters into the tag vocabulary (skill://reliable_structured_output → open\n    vocabulary; skill://running_models → tiering: cheap=coverage, smart=judgment).\n\nORGANISING TAGS DESCRIBE, THEY DO NOT JUDGE (context://session_laws → describe-don't-judge). An\norganising tag says what a thing IS; importance / relevance / keep-vs-junk is a SEPARATE, LATER lens\napplied OVER the organised corpus, never baked into the filing (you can't re-find what you\npre-judged as unimportant and buried). The descriptive dimensions (ORTHOGONAL — an email carries\nseveral at once, faces law; VALUES NUCLEATE from the business's real mail, never a list picked up\nfront):\n  • MATTER — the engagement/project/account it belongs to (the backbone; reconstruct around this).\n  • SUBJECT AREA — the business domain (sales/quotes, invoicing/accounts, purchasing/suppliers,\n    contracts, staff/HR, operations, tax/accounting, banking, legal, marketing, support — seeds;\n    the business grew its own).\n  • ACT / TYPE — what it IS, factually: a quote, order, invoice, receipt, statement, contract,\n    enquiry, delivery/scheduling, report, notice.\n  • RELATIONSHIP ROLE — who the correspondent is TO the business (client/prospect/supplier/\n    contractor/staff/accountant/solicitor/bank/government) — a factual role, NOT a ranking.\n  • MESSAGE NATURE — personal-correspondence / automated / bulk (header-derived, factual).\n  • ATTACHMENT KIND — contract/invoice/statement/spec/photo (when present; an attachment is its\n    own taggable thing — same dimensions as its email, plus its own).\n\nRELEVANCE IS THE LATER LENS (not an organising tag). The dragnet organises EVERYTHING descriptively\n(fail-loud/absorb — nothing dropped, nothing pre-judged). 'Junk'/importance is applied AT RETRIEVAL,\nreversibly: 'a bulk marketing email' is a descriptive fact; 'ignore marketing' is a query-time\nchoice you can always undo (that marketing email might be the receipt you need at tax time).\nskill://querying_and_collecting is the read side; organising is designed BACKWARD from it.\n\nGOVERNANCE (context://session_laws → private-data): a business email archive is ALL private data →\nLOCAL models by default (the 0.8B/4B do the bulk on-machine); cloud (kimi) opt-in per slice by the\noperator, never a default. One blanket decision — no judgemental per-email sensitivity tag needed.\n\nFOLDERS ARE WEAK SIGNAL. Existing folders are usually disorganised (Tim, 2026-07) — treat them as\nONE noisy input the nucleation weighs, never the spine.\n\nFLOOR: describes composing READ/RUN/create tools (the stdlib parser is CODE; run_items/run_reduce/\ncapture/corpus/coordinate/mark for the rest) — never resolve/approve/dispatch, never claude -p.",
    "label": "Processing a correspondence archive (email/messaging) — the native graph + matter spine",
    "description": "The source-specific layer for an email or messaging export (e.g. a business's Zoho archive + attachments): the graph arrives PRE-FORMED in the headers (deterministic, code, 100% coverage), a smart model stitches the broken pieces into MATTERS (the organising spine), and organising tags DESCRIBE (never judge — relevance is a separate later lens). Specializes skill://full_coverage_dragnet at L0 + the model-seams; read with skill://querying_and_collecting + context://session_laws. From the mesh session, 2026-07."
}
