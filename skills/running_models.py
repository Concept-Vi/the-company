"""skills/running_models.py — agent-authored skill (Concurrent Cognition C 3b · #56/#58 write-half).
Authored DIRECTLY through the agent surface (create_skill); validated by import-in-a-temp-dir
before it ever reached the live skills/ tree. A registry ROW: skill://running_models resolves to its
declared `content` (the reusable instructions a role reads)."""
SKILL = {
    "id": "running_models",
    "content": "RUNNING THE MODELS — pick the tier by ECONOMICS + job, and fire it through the engine (run_role/\nrun_items/run_reduce), NEVER bespoke urllib/psql (that goes around the system + loses the guards).\nOrient: models_for_role(role|requires=...) for what fits; capabilities(section='models') for the\nlive set; company status / `company up @<combo>` for what's resident.\n\nTHE TIERS (tier by economics, not by task — this is how coverage stops being rationed):\n  • CODE (no model) — deterministic absolutes (size/time/id/edges/set-diffs, regex, parsing). The\n    dragnet's L0. A model given mechanical work is the classic waste; a regex is not a parser but a\n    set-diff is not a model's job either. Deterministic-work-to-code.\n  • 0.8B / 2B local (chat-08b/chat-2b, vLLM) — AREA: bulk, ultra-concurrent, ~free. The coarse\n    open-vocab tag over EVERYTHING. Noisy-and-fine: the cluster layer absorbs noise; counts are the\n    confidence. Co-resident with the embedder (small footprint).\n  • 4B local (chat-4b AWQ worker / chat-4b-fp8 brain) — the rule-routed finer pass; also the\n    everyday interaction brain (fp8: thinking + tools). Fits beside embedder+reranker+voice.\n  • 9B local (chat-9b-fp8) — the quality brain, SOLO (fills most of the 16GB card; @quality-9b\n    with --evict).\n  • kimi-cloud (kimi-k2.7-code:cloud, ollama-served) — JUDGMENT: canonicalization, triangulation,\n    deep intent extraction, synthesis. Frontier; cheap enough for every-round use. NO output budget\n    (see below). Served via ollama at :11434.\n  • Fable/Claude (this session) — the whole-picture seat: ontology decisions, cross-space\n    reasoning, tending the loop. Scarce; spend only where the whole picture is genuinely needed.\n\nHOW TO FIRE (role-resolved, never pinned):\n  • run_role(role, utterance|inputs, model=?, max_tokens, temperature) — ONE fire. Omit model to\n    use the role's DECLARED binding (a role carries default_model TOP-LEVEL — the binding-trap:\n    a model nested in model_binding is silently unread → falls to the cfg brain). A role that\n    should run on kimi DECLARES default_model:'kimi-k2.7-code:cloud' + default_base_url the ollama\n    host (model+endpoint must match).\n  • run_items(role, items) — the MAP: fan ONE role over N units concurrently. The batch runs on the\n    role's bound model (there is no per-unit model). This is the dragnet's fan.\n  • run_reduce(mode='role'|'rule'|'cluster', ...) — the JOIN (synthesize / deterministic tally /\n    embed-cluster). run_draft / run_draft_items for a one-off inline role (no registry pollution).\n\nGPU BUDGET (16GB card): loadouts in ops/services.json are VRAM-budgeted sets (`company up @recall`\n= embed-pplx + rerank-jina; @interaction = 4b-fp8 brain + embedder + reranker + ear + voice). The\nresource manager checks fit. Local fans inherit the resident brain's slot budget; a CLOUD fan is\ncapped separately (COMPANY_CLOUD_CONCURRENCY, ~6 — the ollama subscription limit, not the local KV\nmath). Bring a local model up on-demand (no-boot-autostart law): `company up chat-08b`.\n\nLAWS (context://session_laws): NO OUTPUT BUDGET FOR CLOUD MODELS — a :cloud fire omits num_predict\nand runs to completion (a local-sized cap truncates cloud output mid-JSON). Cloud is patchy → more\nretries, longer timeouts. Local models keep budgets (VRAM/time are real there). Prefer local for\nprivate data (governance). Verify a binding BY-USE (resolve_role(id)['model']), never by reading the\nfile — a nested binding LOOKS right and resolves wrong.\n\nFLOOR: describes composing READ/RUN tools only — never resolve/approve/dispatch, never claude -p.",
    "label": "Running the models — local + cloud tiers through the Company",
    "description": "Which model for which job, and how to fire it through the cognition engine (never bespoke urllib). The tiers (0.8B/2B/4B/9B local vLLM + ollama-cloud kimi), loadouts + GPU budget, role-bound firing (run_role/run_items), no-cloud-budgets, and the deterministic-work-to-code / area-vs-judgment allocation. From the mesh session, 2026-07."
}
