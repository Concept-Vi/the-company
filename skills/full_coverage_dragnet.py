"""skills/full_coverage_dragnet.py — agent-authored skill (Concurrent Cognition C 3b · #56/#58 write-half).
Authored DIRECTLY through the agent surface (create_skill); validated by import-in-a-temp-dir
before it ever reached the live skills/ tree. A registry ROW: skill://full_coverage_dragnet resolves to its
declared `content` (the reusable instructions a role reads)."""
SKILL = {
    "id": "full_coverage_dragnet",
    "content": "FULL-COVERAGE EXTRACTION DRAGNET — turn a WHOLE body of material into embedded, addressed,\nqueryable, self-organizing knowledge, with COMPLETENESS you can prove. This is the generalized\nshape behind the transcript archaeology, the repo ingest, and any 'process all of X' task (e.g. a\nZoho email export + attachments). Read the three capability skills it leans on: skill://running_models\n(which model for which layer), skill://reliable_structured_output (getting valid JSON at all),\nskill://corpus_and_coordinates (embed/store/query). And context://session_laws (the standing rules —\nviolating them is how this fails).\n\nORIENT FIRST (registry-is-truth — never invent an id): cognition_info() for roles/projections/spaces;\nlist_skills_contexts() for skills; capabilities() for tools/models/flows. Pass only ids that appear.\n\nTHE SHAPE — layers, cheap→dear, each attaching to ADDRESSES (never a flat report):\n\n0. DERIVE THE UNIVERSE (code, no model). Enumerate the WHOLE set from an external reference — a\n   directory glob, a registry, a table — NEVER a curated 'the ones that matter' list (that is\n   sampling; it produced a false 'done' at 0.2% coverage once). The enumeration is your completeness\n   oracle: 'done' = a full pass over it with zero new + zero failures, computed by set-diff, never\n   asserted by feel (context://session_laws → external-reference).\n\n1. STRUCTURAL FLOOR (code, no model). For each item pull the deterministic absolutes — size, time,\n   native id, native edges (email In-Reply-To/References = a reply DAG; a folder tree; a thread) —\n   and give each thing an ADDRESS. Deterministic work goes to CODE, never a model (a model set-diff\n   is the classic waste). File extension = type, name = id, path = nesting, mtime/native-date = the\n   time axis. Attach via skill://corpus_and_coordinates.\n\n2. LOSSLESS EXTRACT. Read the FULL content — never truncate (context://session_laws → truncation:\n   any [:N] cap on model-bound material is lazy design; a real context-window limit is met by\n   chunking COMPLETELY / a reduce-tree, never cutting). Frame each unit for the extractor role.\n\n3. TAG / EXTRACT (a model). Fan ONE extraction role over every unit with run_items (the MAP;\n   REUSES the engine — never hand-loop). The role's schema constrains SHAPE, never MEANING: use\n   OPEN vocabulary for any judgment field (no closed enum on a kind/verdict/state word — novel\n   words are the nucleation signal; see skill://reliable_structured_output). Model tier per\n   skill://running_models: the coarse universal tag = the 0.8B (free, ultra-concurrent); the\n   finer/intent extract = kimi-cloud (cheap judgment). CAPTURE each output as a durable record\n   keyed to the item's address (capture / corpus — project·session·round REQUIRED).\n\n4. EMBED (local embedder). Every record embeds on write into a corpus SPACE, so it retrieves by\n   MEANING and clusters. One geometry per space (skill://corpus_and_coordinates → layers).\n\n5. CLUSTER → NUCLEATE (the resolve step). run_reduce(mode='cluster') groups near records; a strong\n   model (kimi) NAMES/canonicalizes each cluster into a type; outliers escalate for review. The\n   taxonomy EMERGES from the material — you never predefine it. Canonical vocab lives as COUNTS +\n   addressed registry rows (G16 no-confidence), re-openable forever, never a decode constraint.\n   Record the FACES of a thing that legitimately does several jobs — never flatten to one.\n\nHARD DISCIPLINES (each paid for in real failures — context://session_laws):\n  • CAPTURE-BEFORE-REDUCE: persist every observation the moment it exists; a crash then costs zero.\n  • IDEMPOTENT RESUME: the skip-list IS the corpus (query what's already captured per item);\n    re-runs deepen, never duplicate. Version-stamp records so a method change re-mines cleanly.\n  • BOUNDED + RESUMABLE: run in time-boxed batches that compose to full coverage; a giant set is\n    many batches, not one heroic call.\n  • FAIL LOUD: an empty/crashed pass is a FAILURE, never silently a 'dry' completion. A hang is the\n    worst silent failure — set connect + statement timeouts on anything shelling out (a dead\n    docker/proxy hung a whole system for 7h once).\n  • MEASURED GATE BEFORE SCALE: prove the extractor on a small sample (A/B a couple of models) and\n    show the records BEFORE opening the throttle over the whole universe.\n  • PRIVATE DATA (email/Slack/personal): default LOCAL models only; cloud (kimi) is per-source\n    opt-in by the operator, never a default. Attachments/bodies are a governance axis — surface it\n    to the operator, do not decide it.\n\nEXISTING FLOORS TO REUSE (flows(op='list') — don't rebuild): repo_ingest (files→repo space),\ntranscript_mine (dialogue→history space), pattern_cluster (the cluster-and-name reduce),\ndrift_radar, floor_walk. A new source-type (email/attachments) = the SAME machinery above L0; only\nthe L0 structural extractor is source-specific (email = RFC5322 via stdlib; deterministic, not a\nmodel guessing structure the RFC defines).\n\nFLOOR: this skill DESCRIBES composing READ/RUN tools (run_items/run_reduce/capture/corpus/coordinate/\ncreate). It never instructs resolve/approve/dispatch and never launches claude -p — declarative content.",
    "label": "Full-coverage extraction dragnet (any corpus → addressed, queryable knowledge)",
    "description": "The generalized pipeline for processing a whole body of material (transcripts, emails+attachments, a repo, a doc set) into embedded, addressed, nucleating knowledge — with proven completeness. Derive the universe → extract LOSSLESS → open-vocab tag → embed → cluster → nucleate → capture-before-reduce → idempotent resume. Entry skill; leans on skill://running_models, skill://reliable_structured_output, skill://corpus_and_coordinates, and context://session_laws. Built from the mesh/archaeology session, 2026-07."
}
