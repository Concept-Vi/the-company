"""skills/corpus_and_coordinates.py — agent-authored skill (Concurrent Cognition C 3b · #56/#58 write-half).
Authored DIRECTLY through the agent surface (create_skill); validated by import-in-a-temp-dir
before it ever reached the live skills/ tree. A registry ROW: skill://corpus_and_coordinates resolves to its
declared `content` (the reusable instructions a role reads)."""
SKILL = {
    "id": "corpus_and_coordinates",
    "content": "CORPUS + COORDINATES — the substrate side: store knowledge ADDRESSED + EMBEDDED so it retrieves\nby MEANING, by ADDRESS, and by RELATION, and so structure can nucleate over it. Orient:\ncognition_info().spaces (embeddable lens ids), layers() (which embedder-layer each space carries),\nembeddings(op='spaces') (live status). registry-is-truth: never invent a space/projection id.\n\nADDRESSES (contracts/address.py — the coordinate spine). Every thing gets ONE address, any scheme:\ncode:// (a file/symbol), exchange://<sid>/<seq> (a moment in a transcript), ui://, run://, cas://,\nskill://, guide://, decision://, board://, mesh://, and more — the scheme set is ADDITIVE (adding\none is documented + purely additive; a per-source scheme like email://<acct>/<message-id> follows\nthe same precedent). Native ids are addresses: a file path, an email Message-ID (RFC5322), a Slack\nts — never synthesize an id where a real stable one exists. extension=type, name=id, path=nesting.\n\nSTORE (embed on write):\n  • capture(role, units, project, session, round, projection?) — fan a describe/extract role over\n    N units and PERSIST one durable record each (REUSES run_items). project·session·round REQUIRED\n    (the lineage gate; a record missing them is refused). Each record embeds into its space.\n  • corpus(op='query'|'read'|'list'|'find', ...) — read/query the corpus; op='read' address=<id>\n    returns one record; op='query' space=... text=... k=... ranks by cosine (the right lens per\n    space auto-routes: code/symbol=nomic-3584, else pplx-2560).\n  • A SPACE is one embedded projection (history, repo, design_intent, extractions, mesh, code,\n    symbol, operators, ...). ONE geometry per space — don't mix embedder layers within a cluster.\n  • embeddings(op='build'|'pyramid'|'route') — (re)build a space's vectors / its scale-pyramid\n    rungs / read the lens routing. The scale-pyramid (coarse→fine centroids) already exists.\n\nQUERY — THE THREE AS ONE (this is the instrument, not three filing systems):\n  • coordinate(op='query', spec={filter{path_under,node_type,ext,changed_after}, semantic{text,\n    space,emb}, lexical{text}, graph{anchor,kinds,direction,depth≤3}, count, limit}) — ONE call\n    fusing ADDRESS-filter + MEANING + substring + RELATION-walk; meta.plan echoes every leg's count\n    so under-recall is never silent. THIS is 'start here, widen by meaning, walk these edges'.\n  • project(binding, space, emb, dim, rung, center, at, pole_a/pole_b) — the radial instrument:\n    binding='semantic' (meaning radius) | 'by_nucleation' (where new types are born — the keystone)\n    | 'by_separator' (lean between two poles); dim = MRL meaning-zoom; rung = scale zoom (units→\n    theme centroids); center = relative origin (attention IS origin-selection); at = time scrubber.\n  • find_relations(item, near_space, far_space) — the inversion: near in A, far in B ('same\n    principle, different subject'). fail-loud if item has no vector in either.\n  • scope(op='resolve'|'symbol', address) — the see↔code join (ui:// ↔ code symbols), ledger-derived.\n\nTHE EXCHANGE:// ↔ CODE:// SPINE: conversation caused everything (no human hand). A design-intent\nrecord's connects_to are CANDIDATE mentions — the cross-linker must NORMALIZE + VERIFY them against\nthe real code:// space before writing edges (kimi decorates names with schemes not verbatim in the\ntext). Native edges (email reply-DAG, folder tree, thread) are a GIFT — land them as real edge rows\nunder registered edge_kinds, ABSORB-never-reject.\n\nTHE LEDGER (supabase, :15432): content in the fs_store (CAS), embeddings + the address tree in the\nledger (ledger.entry/edge/embedding/path/tool_call/cluster_member/assertion — the pyramid's tables\nare pre-formed). If Suite init reports cap rows = 0 or a call hangs, check `docker ps` FIRST (a dead\nengine + live Windows port-proxy hangs psql forever — connect+statement timeouts guard it).\n\nFLOOR: describes composing READ/RUN tools — never resolve/approve/dispatch, never claude -p.",
    "label": "Corpus + coordinates — embed, address, and query by meaning + graph + axis at once",
    "description": "How to store knowledge so it retrieves by MEANING, by ADDRESS, and by RELATION — together. capture/corpus (embed on write), the spaces + layers (the multi-layer model), coordinate (the fused one-call query), project (the instrument: nucleation/scale/time/relative lenses), find_relations (the inversion), scope (see↔code), and the address grammar. From the mesh session, 2026-07."
}
