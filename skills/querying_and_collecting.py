"""skills/querying_and_collecting.py — agent-authored skill (Concurrent Cognition C 3b · #56/#58 write-half).
Authored DIRECTLY through the agent surface (create_skill); validated by import-in-a-temp-dir
before it ever reached the live skills/ tree. A registry ROW: skill://querying_and_collecting resolves to its
declared `content` (the reusable instructions a role reads)."""
SKILL = {
    "id": "querying_and_collecting",
    "content": "QUERYING, TAGGING + COLLECTING — the READ side. Ingesting a corpus is half the job; the point is\nan agent later FINDING + COLLECTING the right things for Tim out of a lot of junk, and Tim TAGGING\nas he goes. Design the write side BACKWARD from this: what you tag at ingest is exactly what queries\ncan filter on later — an unrecorded facet is an unaskable question. Pair with skill://full_coverage_dragnet\n(the write side) and skill://corpus_and_coordinates (the tools).\n\nTHE OPERATOR LOOP THIS SERVES:\n  agent runs a query → cheap recall + rerank → a SMART model judges the shortlist → presents a\n  COLLECTION → Tim leaves SIGNALS (favour / a note / a correction) → those signals re-canonicalize\n  the emergent tags + refine the query → SAVE the refined query (query://) → WATCH it (new matches\n  surface to the board over time). Retrieval is a conversation with Tim, not a one-shot; the tag\n  vocabulary it filters on is GROWN from the material, never a list picked in advance.\n\nTWO TAG LAYERS — DO NOT CONFUSE THEM (this is the correction Tim made 2026-07):\n  A. DETERMINISTIC facets (code, predefined-because-STRUCTURAL): sender, recipients, thread-id,\n     date, has-attachment, attachment-type, PII/sensitivity. These are pulled by code (RFC5322),\n     are a real closed set (the structure defines them), and become the exact-match coordinate\n     filters. Fine to fix up front — they are structure, not judgment.\n  B. MEANING tags (topic, kind, junk-vs-keep, what-this-is) — THESE ARE NOT PREDEFINED. Do NOT hand\n     a model a fixed list to pick from, and do NOT invent mark_types like 'junk'/'keep' up front —\n     that is the closed-enum trap in the tag layer. They NUCLEATE (skill://full_coverage_dragnet\n     step 5, the same process as the write side): cheap models put OPEN tags on everything → the\n     tags embed + cluster → a SMARTER model names/canonicalizes each cluster into a canonical tag →\n     outliers escalate → the emergent canonical tags (counts + registry rows, re-openable) ARE the\n     query filters. 'Junk' is not a checkbox — it is a low-value CLUSTER that nucleates, confirmed\n     by what Tim marks. The vocabulary you filter on is the one the material grew, not one you wrote.\n\n1. THE SMARTER MODELS DO THE JUDGMENT — on both sides (the tiering, skill://running_models):\n   • WRITE/tag: the 0.8B open-tags at volume (cheap, no list); the SMART model (kimi) canonicalizes\n     the clusters into the tag vocabulary and adjudicates the junk clusters. Cheap = coverage,\n     smart = judgment. Never ask the cheap model to decide the taxonomy; never ask the smart model\n     to tag every unit.\n   • READ/collect: finding is ALSO tiered — cheap RECALL (embedding cosine) → rerank → then a SMART\n     model reads the top candidates and JUDGES 'is this what Tim actually meant?' before presenting.\n     In a sea of junk, cosine+rerank is recall; a kimi/Fable relevance-judge is precision. An agent\n     collecting FOR Tim runs that judge over the shortlist, not raw cosine. Escalate the genuinely\n     ambiguous to the operator, never guess.\n\n2. JUNK: TAG IT, NEVER DROP IT (fail-loud/absorb). Never delete or skip. Junk is a nucleated\n   low-value cluster (layer B) + whatever Tim marks — not a predefined label. Queries EXCLUDE it by\n   filtering out those emergent tags; the junk-set stays queryable + correctable; nothing vanishes.\n   Tim marking a false-positive RE-TAGS it and feeds the next canonicalization (the smart model\n   re-clusters with his correction as signal).\n\n3. OPERATOR MARKS — the thin human-SIGNAL layer (not the primary vocabulary):\n   mark(target=<address>, mark_type=<id>) is how Tim + agents leave a SIGNAL on a thing —\n   favour (this matters), comment (a note), reaction. Keep this set SMALL and open; its job is to\n   FEED the nucleation (Tim's favour = a positive example; his correction = training signal), NOT\n   to be the tag taxonomy. marks(by='type', mark_type='favour') = 'everything Tim favoured';\n   marks(by='target', addr) = the signal-thread on one thing. Agents read these back to refine +\n   re-canonicalize. Tim comments as operator://tim via the board comment wire.\n\n4. FIND (coordinate — the fused query, the primary find tool):\n   coordinate(op='query', spec={ semantic:{text, space, emb}?, lexical:{text}?,\n     filter:{path_under, node_type, ext, changed_after, ...}?, graph:{anchor, kinds, direction,\n     depth≤3}?, count:{by}?, limit }) — ONE call: 'about X (meaning) ∧ mentioning Y (substring) ∧\n     from sender Z / after date T (filter) ∧ in this thread (graph)'. meta.plan echoes every leg's\n     count so under-recall is never silent. count:{by:<facet>} = 'how many per sender/kind' (collect\n     as a histogram). For code/symbol spaces pass a precomputed vector, not text.\n   • PRECISION over junk: the semantic leg ranks by cosine (recall); run the RERANK stage (@recall\n     jina, skill://corpus_and_coordinates) over the top candidates for precision, and threshold on\n     its score to cut low-relevance. Full pool, not a truncated head (truncation law).\n   • find_relations(item, near_space, far_space) = 'more like this' / the inversion ('same topic,\n     different sender' or 'what's MISSING'). project(binding='by_nucleation'|'semantic', center=...) \n     = explore the space around a point when the query isn't yet a sharp filter.\n\n5. COLLECT + MAKE IT STANDING:\n   • coordinate(op='save', qid, label, spec) → registers the question as query://<id> (VALIDATED by\n     running once — a query that can't run is never saved). Now the collection is an addressable\n     thing an agent (or Tim) re-runs by name: coordinate(op='run', qid) / op='saved' to list them.\n   • WATCH it: the watch_query job (jobs) re-runs a saved query, diffs the result-set vs last time,\n     and SURFACES new matches to the board — a standing collection that fills itself as new material\n     lands (the 'collect things for me over time' Tim wants). Arm it via the jobs door (operator arms).\n   • A one-off collection = the coordinate result set; hand it to Tim as a board view / a channel /\n     a captured set he can tag.\n\nDESIGN NOTES (fold these back into the ingest plan):\n  • Every query Tim will want is a facet the ingest must have tagged. Enumerate the likely questions\n    FIRST ('emails from X about Y with an attachment, not junk'), derive the ingest facets from them.\n  • Tags are open-vocabulary + counts (G16), re-openable; a thing can carry many (faces law) — an\n    email is junk-to-one-query and keep-to-another; don't force one label.\n  • Tim's tags are TRUTH that feeds back: a false-positive he marks junk should tune the sieve; a\n    thing he favours is a positive example. The mark layer is the operator-correction channel.\n\nFLOOR: describes composing READ/RUN tools (coordinate/corpus/find_relations/marks/mark/jobs) —\nnever resolve/approve/dispatch, never claude -p. (mark + save_query WRITE durable tags/queries, not\noperator gate-actions.)",
    "label": "Querying, tagging + collecting — the read side (find things for Tim in a sea of junk)",
    "description": "The retrieval half of the dragnet: how what you tag at INGEST becomes what queries filter on LATER; operator/agent TAGGING via marks; the fused find (coordinate: semantic ∧ lexical ∧ filter ∧ graph) + rerank for precision; SAVED queries (query://) + WATCH (standing collections that surface new matches to the board); and the junk strategy (tag-don't-drop, operator-correctable). Design ingest BACKWARD from these queries. From the mesh session, 2026-07."
}
