"""skills/reliable_structured_output.py — agent-authored skill (Concurrent Cognition C 3b · #56/#58 write-half).
Authored DIRECTLY through the agent surface (create_skill); validated by import-in-a-temp-dir
before it ever reached the live skills/ tree. A registry ROW: skill://reliable_structured_output resolves to its
declared `content` (the reusable instructions a role reads)."""
SKILL = {
    "id": "reliable_structured_output",
    "content": "RELIABLE STRUCTURED OUTPUT — getting valid JSON from a model, automatically, at ANY size. These\nare ROOT-CAUSED (wire-verified + source-confirmed), not folklore. The engine (run_role/run_items)\nnow does most of this for you; this skill is WHY, so you don't re-break it.\n\nTHE CLASS (look for the class, not the instance): 'structured output' is a decoder-level grammar\nconstraint — EXCEPT where the serving stack doesn't actually apply it. The failure is never 'the\nmodel is dumb'; it is a specific unverified assumption at a serving seam. Enumerate + MEASURE the\nseam (no 'probably').\n\n1. OLLAMA-CLOUD IGNORES `format`. A :cloud model proxies to ollama.com BEFORE format/think shaping\n   runs — the JSON-schema constraint never reaches the decoder (docs: 'Cloud currently does not\n   support structured outputs'; wire-verified: full schema sent, markdown came back, 4/4). So for a\n   :cloud model the ONLY constraint is the PROMPT. The engine now AUTO-injects the schema into the\n   system prompt as a hard OUTPUT CONTRACT on every :cloud schema fire; client-side parse/validate/\n   retry is the guarantee. Don't 'fix' cloud JSON by trusting `format` — it does nothing there.\n   (A misread that cost days: 'unparseable JSON: char 0' was MARKDOWN (a '#' heading), never empty —\n   measure VALIDITY, not length.)\n2. LOCAL vLLM / ollama-local: the grammar constraint IS real (guided decode / GBNF). Trust it; the\n   engine requests it (response_format json_schema).\n3. SCHEMA ⊥ THINKING. A grammar-constrained fire cannot emit reasoning tokens — a thinking-ON\n   schema call degenerates to empty/invalid. So a schema fire DEFAULTS to no-think unless a role/\n   caller explicitly asks to think. A role that must REASON should emit free text (no output_schema)\n   and reason there. (Do NOT sprinkle think=false as a bandaid — the default already handles it.)\n4. NO OUTPUT BUDGET FOR CLOUD. Omit num_predict on :cloud — a local-sized cap truncates the answer\n   mid-JSON (then the retry net burns). num_predict=-1 is a hard 400 on ollama; just omit it.\n5. SIZE-INDEPENDENCE. Prompt size does NOT affect a correctly-configured structured call. If big\n   prompts fail and small ones pass, the cause is elsewhere (concurrency beyond the subscription;\n   context truncation; a stale bandaid) — measure the truth table (format × think × budget × size),\n   don't guess.\n\nSCHEMA CONSTRAINS SHAPE, NEVER MEANING (the open-vocabulary law — the deepest one):\n  • A closed enum on a JUDGMENT field (a kind/verdict/state/label word) is predetermination — and\n    under constrained decoding it CENSORS the decoder (the model literally cannot say what it saw;\n    the retry-burns were the ENUM failing, not the model). Use an OPEN str field with the common\n    values named as GUIDANCE + an explicit invitation to coin. Novel words are the nucleation\n    signal — they pile up, cluster, get named downstream (skill://full_coverage_dragnet step 5).\n  • A closed enum is legitimate ONLY where CODE branches deterministically on the value (an ordinal\n    routing dial) — and even then keep the raw word.\n  • LENIENT-IN, STRICT-OUT on SHAPE: a pydantic field_validator(mode='before') that coerces a\n    shape near-miss (a bare string where an object was wanted → {claim: str}) so you never lose an\n    observation to a shape mismatch. Coerce SHAPE; never coerce MEANING onto your list.\n  • FACES: when the model emits a richer shape than your schema (it attached a source to every\n    claim), UPGRADE the schema toward the model — don't punish it.\n\nRETRIES + FAIL-LOUD: the fabric retries on transport/empty/unparseable/schema-mismatch with jittered\nbackoff; cloud gets more attempts. A deterministic near-miss at temp 0 reproduces identically every\nretry — the engine jitters temperature up after a validation miss so the retry actually explores.\nExhausted retries RAISE (never a silent empty). Required fields (no default) make a hollow {} FAIL\nvalidation instead of passing as success — use them for fields a valid answer MUST carry.\n\nAUTHORING A ROLE (create(kind='role')): output_fields is a list of {name,type,values?,description?};\nfield_types() for the type set (str/int/bool/list[str]/enum/object/list[object]). Prefer open str +\nguidance over enum for judgment fields. Bind the model TOP-LEVEL (default_model) — see\nskill://running_models. Verify BY-USE (run_role once, read the output), never by reading the file.\n\nFLOOR: describes composing READ/RUN + create tools — never resolve/approve/dispatch, never claude -p.",
    "label": "Reliable structured output from any model (the hard-won JSON lesson)",
    "description": "How to get valid, schema-conforming JSON out of local vLLM AND ollama-cloud models, automatically, at any prompt size — the root-caused lessons: cloud ignores `format` (schema rides the prompt), schema ⊥ thinking, open-vocabulary (shape not meaning), lenient-in coercion, retries + fail-loud. From the mesh session's wire-verified investigation, 2026-07."
}
