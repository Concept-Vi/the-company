# THE FIRST SLICE — scope (Tim: "scope it", 2026-06-16)

*The build-HOLD is lifted FOR THIS SLICE ONLY (the full interface stays held until the finding completes + Tim's judgment). This is the minimal end-to-end operable surface, assembled from BUILT pieces (not from scratch) — the proof you can click + talk + adapt. Provisional; members scope their own part; nothing built on assumption (the litellm lesson — investigate the actual code first). Tim is the only judge of "right."*

## THE GOAL (one sentence)
A real, clickable surface: an addressed unit RENDERED IN DNA's gallery (from data), every element a sub-address you can point at, click an element → talk to Claude Code (on a company/ollama model) ABOUT that element in-surface → it adapts → re-render. = the Circuit, over one addressed state, rendered in DNA's voice, with the loadable-brain — the whole interface in miniature.

## WHY THIS SLICE (Tim's gallery question is the key)
DNA's gallery is a render-FROM-DATA engine whose 73 reference screens are its targets. So "make the screens/mockups into the gallery" = the gallery working as designed: a screen becomes a gallery entry by being expressed AS DATA (layout-archetype + tokens + content), rendered by DNA's engine. That gallery, made addressable + given the loadable-brain, IS the operable interface. We render the screens you already want (DNA's voice) AND make them live.

## THE PIECES (all BUILT — this is BINDING, not building)
- projection's **Instrument** = the field/overlord altitude (zoom out = the company as a field; drill into a unit → the gallery render). Built, tri-form-factor.
- DNA's **gallery** = the render surface (presentation-from-data; the 73 screens as targets; desktop+portrait+landscape). Built.
- fork's **loadable-brain wire** = click an address → a Claude Code session bound to that context (now on ollama models). Built + verified.
- composition's **resolver** + the **address spine** (16 schemes, 9 resolvable). Built.
- wildcard's **recognition grammar** (element-addressable + the 4 direction mechanisms) + the F14 catalog (slots+presentation+form-factors as data). Built/precedent.

## THE SEAMS (who binds what — members scope their own; meet at the address)
1. **DNA** ← Tim's question: can the screens/mockups render in your gallery FROM DATA? Scope: pick ONE screen/unit, express it as gallery-data (layout-archetype + tokens + content), confirm your engine renders it. Is the F14 catalog.json your data substrate, or your own layouts.json? (Your call — your domain.)
2. **fork** ← the loadable-brain at an element-address: `project(address) → resolve → click element (sub-address) → CC session (provider=ollama, model=kimi-k2.7-code:cloud) bound to that element's context → stream in-surface`. The wire's built; scope the territory_for(address) composer + the in-surface bind.
3. **projection** ← the Instrument hosts it: a wheel-point → the gallery render of that addressed unit (the drill-in). Wire the wheel-point→render handoff.
4. **composition** ← the resolver feeds DNA's render (project()'s territory → DNA renders it); the brain is a behaviour-socket at the element, scale-selected.
5. **wildcard** ← element-addressability + the 4 direction-mechanisms (comment/stamp/favour/highlight) on the rendered gallery elements + the route-back (direction → mutation at the sub-address → re-render).

## THE PROOF (verified-by-use, no green paint)
One addressed unit → rendered in DNA's gallery from data → an element is clickable/anchorable → click it → a Claude Code brain (ollama model) loads on that element's address → talk → it adapts → re-render shows the change. Desktop first; portrait/landscape are DNA's form-factor reflow (already built). When that round-trips live, the first slice is real.

## DISCIPLINE
- Investigate the actual code before binding (the litellm lesson — at every level).
- Use-don't-build: every piece exists; this is binding + the missing render-the-Tim-facing-half.
- GPU: a loadable-brain on a CLOUD ollama model (kimi/deepseek) doesn't load the local card; local models GPU-sequence with the steward. Clone-boots ask the lead.
- The FULL interface (all screens, all polish, the graph-of-minds projection, the principled completeness) stays HELD until the finding completes + Tim judges the shape. THIS is the minimal proof, extended from the Instrument + the gallery.
