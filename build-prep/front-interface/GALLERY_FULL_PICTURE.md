# THE GALLERY — FULL PICTURE (STEP 1 finding, DNA-owned)

*The deep look-through the plan ([[GALLERY_AS_FACE]]) asks for, solo + GPU-free half — done from DNA's own territory (the gallery code + manifest + a representative read of the liked-look mockups), no clone needed yet. The clone-scale pass (all 49 pieces in fine detail, fresh eyes) is queued for AFTER fork's KIMI-CLONE-PROVEN. Evidence-classed: VERIFIED = read in code/manifest this session; CHARACTERIZED = read a representative artifact; KNOWN = built across this session's mob-* work.*

---

## 1 · THE FRAME (verified)
The gallery is a spatial shell — ONE canvas, rooms slide over it (`ui/rooms/shell.js`: gallery implicit order-1, + **bench · map · almanac** as registered overlays). The look/feel is DNA's own (its archetypes/tokens), exactly as the plan states. Company-mockup frontend is throwaway; the architecture/address-system underneath is kept.

## 2 · WHAT THE GALLERY IS TODAY (verified in code)
- **The slide-form** — `ui/gallery/stage.js`: pieces are stepped through (`select`/`step`, the skim), each with a **version axis** (switch/scrub takes) + **compare** (before/after, verdict pills). On mobile the version axis folds into the bottom **scrubber** beside the piece axis (one affordance, two axes).
- **Mobile** — real and refined (`isNarrow()` = max-width:920px; mobile sheet, scrubber, top-left scale anchor). The session's mob-1..24 hardened this.
- **The annotation/interaction seed** — element **pins** sit ON the work (`xFrac/yFrac`), click → **pin-pop** with a note + who-left-it; comments persist per version. This is the interaction layer's seed (the wildcard seam extends it).
- **The almanac** — a content room reading live face content (`ui/rooms/almanac.js` → `ctx.api.dna(name)`); reports faces/proofs.
- **Drawers** — tokens · grammar · layouts (`ui/gallery/drawer-*.js`): the DNA dials/grammar are editable live.
- **NO desktop form** — confirmed three ways: not in code, the manifest category "The Application" lists `pending: Desktop screens — "the same face at the wider tier"`, and the plan's STEP 2 asks for it.

## 3 · THE LIKED LOOK — the PHONE ARCHETYPE, shown in SLIDE LAYOUTS (measured + SEEN, screenshots)
★ The "mobile UI mockups in slides" Tim singled out are **portrait PHONE screens** — the UNIT is always the phone, defined as a shared reusable component in **`ui/phone.css`** (+ `ui/piece.css`). The **1600×900 is the SLIDE/presentation FRAME**, not the UI — it comes in layouts: the **DECK** (multiple phones telling a flow — `app-flow-full`) and the **EXPLAINER** (one phone + its design principles in a text column — `app-canvas`). Verified: **all 19 Application/scr pieces reference the phone frame**, and confirmed BY SIGHT (screenshots `/tmp/slideform-app-flow-full.png`, `/tmp/hub-app-canvas.png`).
(Two self-corrections logged honestly: first mis-sourced the look as "landscape" from app-canvas's frame → caught by measuring dimensions; then over-corrected to "app-canvas is a distinct non-phone archetype" → caught by SEEING it — app-canvas's UI is itself a portrait phone, just shown in an explainer layout. The image is the authority, not the code.)

**The phone archetype (`ui/phone.css`, the liked look — VERIFIED):**
- **Phone frame** — portrait, rounded: `.phone` 300×600 (`--dna-radius-30`) · `.phone.lg` 330×660 · `.phone.sm` 240×480. All sized/cornered from DNA tokens.
- **Backgrounds as warmth** — `.phone.warmbg` = the warm radial pole (`--warmpole-1..4`); `.phone.closebg` = the warm-gold close (`--warm`→`--goldwash`); cool default (`--cool`). Warmth is the screen's mood, set per screen.
- **Status bar** (`.sbar`, "9:41"), **header** (`.p-sup` ochre superhead + `.p-h` charcoal heading).
- **The stat band riding the hatch** (`.p-band` over a `-45deg` ochre hatch) — gold "star" stat (`.p-bs.star`), ochre stats.
- **Calm rows** (`.p-rows`/`.p-row`, zebra via `--nested`), **plates/insets**, **the one gold action** (`.p-act`).
- **Type:** Plus Jakarta Sans (body) · IBM Plex Mono (micro-labels). All composed from DNA tokens + `ui/organisms.js`.

**The slide-form / deck (read of `app-flow-full`, VERIFIED):** a `.view` stage holds a **row of phone screens** (e.g. five: open·choose·work·review·close), each a `.phone.sm`, **joined by the plug** (`.joint` = sock·dash·plug — *"the plug carries between every pair"*), warmth swinging screen-to-screen (85→50→35→40→80), labeled by `.role`. THIS is the slide-form: phone screens choreographed as a deck on a stage.

**The explainer layout + the immersive phone-Hub** (`app-canvas`, seen): a landscape SLIDE showing the immersive **phone** Hub on the left + its principles spelled out on the right — *"The World Is the Screen; the Chrome Floats"*: **dark glass over imagery** (`rgba(28,26,25,.85)`+blur, *"never outlined light"*), the **frosted word-card** (*"one frosted card carries the words"*), floating gold hotspots, the gold hatch (*"the hatch still closes the page"*), blueprint-dot ground — with a "THE CHROME, COUNTED" tally (1 glass menu thumb-reach · 1 frosted card · 3 hotspots · 0 outlined pills). This is the immersive variant of the phone look, with its rules stated on the slide.

## 4 · THE CONTENT — INVENTORY (verified, `mockups/manifest.json`)
**49 pieces / 74 version-files on disk** (the "73 frozen files" earlier = the file count, now 74; "49" = distinct pieces, each with 1+ version files). **3 categories:**
- **The Slides** (~30) — *"page-scale pieces, where the language was learned."* Groups: Zone system+movement, Sampled from source, Subtlety pass v2, Warm pole, First pass (compare), The wider language, From the corpus, Told from a tree, Grown on the bench.
- **★ The Application — 19 pieces = THE FACE** (all 19 use the phone archetype; the *slide-form/phone* screens are the liked look, the *canvas* pieces are the landscape Hub) — *"the product ground, the Workshop dressed in its own language":*
  - *screens, full-size* (7): scr-home · scr-ledger · scr-piece · scr-compose · scr-canvas · scr-compare · scr-dashboard
  - *screens (thumb/mobile)* (5): app-canvas · app-sheet · app-scr-ledger · app-scr-focus · app-scr-compare
  - *flows = the slide-form deck* (3): app-flow · app-flow-fused · **app-flow-full** ("five screens, one arc — open·choose·work·review·close — warmth 85→35")
  - *states* (2): app-states · app-states-2 (empty/loading/done/error/offline)
  - *components* (2): app-comp-cards · app-comp-controls (the component bench)
- **Further Out** — empty categories awaiting first pieces (scenes/3D, documents, social, email).

## 5 · ★ KEY FINDING — the mockups ALREADY HALF-COMPOSE (verified)
The mockup files are TINY (39–89 lines), not big frozen renders. They **consume DNA tokens directly** — `var(--dna-type-micro)`, `var(--dna-space-5)`, `var(--dna-radius-md)`, `var(--warm)`, `var(--gold)`, `var(--charcoal)` — which the gallery injects live via `applyTokens` on iframe load. The look is also **factored into shared CSS components** — `ui/phone.css` (the phone frame), `ui/piece.css`, and `ui/organisms.js` (row/band/list generators) — so a piece is mostly *composition of shared parts + DNA tokens*, with only its specific structure + content text hardcoded. So **the look already resolves from the DNA**; only the *structure + content* rung is still baked. This is the same gap named in the 73-rebuild synthesis: the **tint + shared parts** are live, the **re-layout/re-content** rung is not. The face is closer to live-composable than it looks.

## 6 · THE GAPS, MAPPED TO THE PLAN
- **Desktop form (STEP 2)** — "full-size" ≠ desktop; true wide-tier is `pending` in the manifest. The mobile work already proved the surface axis; desktop is the same face at the wider tier.
- **Interaction layer (STEP 2 + wildcard)** — the pin/annotation seed exists; needs to become the full operable interaction system.
- **How-to-add guide (STEP 2)** — not written.
- **Live-compose rung (the deep one, = 73-rebuild)** — structure/content still baked; the path to "design the interface INTO the gallery format" (STEP 3) runs through making pieces composable, not just tinted.

## 7b · THE PROJECTION → GALLERY SEAM (contract received, projection commit 7852acb — to consume, not yet wired)
projection (the Instrument) built the upstream half of the drill-in handoff. **Contract:** selecting a unit in the Instrument fires a window event `projection:select`, `detail = { address, source, record, seq, kind, space }` — `address` is the drill target (e.g. `code://contracts/resolver.py`); null on deselect. Same-page consumers can read `drillAddress` off the Instrument's SurfaceState. **DNA's hook (when built):** `addEventListener('projection:select', e => renderGallery(e.detail.address))` — render that addressed unit IN the gallery (the look is 100% DNA's; projection only hands off WHICH unit). Transport is a reversible window CustomEvent; swappable (URL param / postMessage / shared React context if the Instrument HOSTS the gallery same-page).
★ **CONVERGENCE:** `renderGallery(address)` IS the downstream half of the live-compose rung (§6, STEP 3 / the 73-rebuild) — render a finished unit FROM its address (→ content + DNA). projection's upstream (drill→address) is live now; the gallery rendering that address live-composed is the rung still to build. NOT wired yet (mid-finding; integration is a STEP 2/3 build awaiting Tim's direction). When that rung is built, this is its input seam — meet-at-the-address, file-disjoint.

## 7 · WHAT THE CLONE-SCALE PASS ADDS (deferred, after fork's KIMI-CLONE-PROVEN)
This solo picture covers the SHAPE. The kimi era-clone fan-out (lead-sequenced) adds: every one of the 49 pieces read in fine detail in parallel, fresh-eyes characterization of each app screen, and the almanac content read in full — breadth this solo pass deliberately did not spend tokens on.

## 8 · THE FIRST-SLICE BUILD SEAM (scouted 2026-06-16, every level — litellm lesson)
The directive (Tim: "gallery + face in parallel"): LANE B = hook `projection:select` → `renderGallery(address)` → render a live common_knowledge unit in DNA's look. Scouted, not assumed:
- **DATA shape (verified, `corpus(op=read)`):** a common_knowledge unit = `{ source_address: "code:///…/x.ts", output:{ digest:"<one-para summary>", kind:"test" }, lineage:{project,round,session}, projection:"common_knowledge", kind:"capture" }`. 2740 units. So renderGallery renders: title = source_address basename + `lineage.project`; super = `output.kind`/projection; body = `output.digest`; meta = kind/project. **Read-by-address needs NO embedder** (`op=read`/`op=list` work; only `op=query` meaning-search is still empty on default space — that's the lens FAST-FOLLOW, not the core).
- **HOST page (verified):** `company/surface/app` (React/Vite) — `App.tsx:151-157` dispatches `projection:select`, `detail={address,source,record,seq,kind,space}`; for corpus points `source=code://<path>`, `record=run://…` (resolvable), `address = source ?? record`. Same-page hosting is the stated model.
- **BROWSER read path (verified):** `runtime/bridge.py` HTTP API — `/api/corpus` (the mockup-gallery INDEX, registry-truth), `/api/corpus-query` (semantic door), `/api/layers`; `corpus.py:197 read_record(store,address)` is the by-address resolver. ★ OPEN: the exact browser route that resolves an arbitrary `run://` unit → record isn't pinned yet (bridge.py:1458 hints "two read shapes on one route"); if absent it's a small bridge-owner add (meet-at-the-address, not my file).
- **fork's brain wire (verified exists):** `POST /api/claude/turn` takes an address (`_claude_stream`, ui://-only today; fork generalizes via `territory_for` — see TERRITORY_FOR_DESIGN.md). Default model, NOT kimi-blocked.
- **⚠️ THROWAWAY, don't build on:** `company/canvas/app` (React StudioKit) binds `/api/corpus` as "the gallery corpus" — this is the company mockup frontend the directive says don't even look at. The LOOK is DNA's (counterpart/design) entirely.
- **★ THE ARCHITECTURE CRUX (RESOLVED — host keystone 2026-06-16 + advisor):** ONE renderer (DNA's look), MANY sources (resolve THROUGH the existing address spine, don't build a gallery-local resolver). DNA.story is one *source* (authored trees); the corpus is another (units); composition's catalog another. The renderer takes NORMALISED data; each source has an adapter. Build split into two increments (advisor): Inc-1 = the pure renderer (no fetch/React); Inc-2 = integration (live fetch + React hosting + the DOM-mount seam).

### INCREMENT 1 — BUILT + VERIFIED BY SIGHT (2026-06-16) ✅
- **Files:** `ui/unit-view.js` (`DNA.renderUnit(normalised, opts)` — the live-compose PRESENTATION rung; `DNA.unitFromCorpus(record)` — the common_knowledge source adapter) + `ui/phone.css` `.p-prose` (the missing body-prose component, token-driven) + `_dev/unit-render-test.html` (standalone harness, the REAL `verify.test.ts` record verbatim).
- **VERIFIED:** rendered the real unit at :8090 → a clean `.screen` (390×844) in the gallery look: `9:41/TEST` sbar · `COMMON KNOWLEDGE / verify.test.ts` header · `WHAT THIS IS` + the real digest as prose · meta ledger (kind·project·round·session, mono values) · the gold hatch. (`/tmp/firstslice-renderunit-v2.png`.) The renderer is pure + source-agnostic; DNA.story-disjoint (it's the presentation DNA.story leaves to the caller — NOT a parallel renderer). Discipline held: real classes/tokens, no bespoke values, verified by sight not by code-reading.
- **★ ONE RENDERER, MANY SOURCES — PROVEN BY SIGHT (the host keystone made real):** added `DNA.unitFromStory(node)` (authored story-tree source adapter) + extended `renderUnit` with a `stat` slot (`figure` → the gold `.p-bs.star` band) and a `points` slot (`bullets` → `.p-row` items). Rendered a CORPUS unit and an AUTHORED node (workshop.json A2 — "The Seam Outpaces the Ledger", the 9-seams stat, the bullets) SIDE-BY-SIDE through the SAME `renderUnit` → identical gallery look, two data origins. (`/tmp/firstslice-many-sources.png`.) This is "one renderer, many sources" demonstrated, not asserted — composition's `vi-vision://` catalog plugs in as a third `unitFrom*` adapter the same way.
- **★ FORM CORRECTION (Tim 2026-06-16: "terrible… failed every visual test… you haven't looked at the slides"):** the first renderUnit was FORM-BLIND — a flat text-wall (title + paragraph + a label→value *debug ledger*) with 40% dead space. I had called it "verified by sight" meaning "the fields render" — that conflation IS the failure. Studied the real slides (`scr-piece` dense / `scr-canvas` immersive) and rebuilt renderUnit to the IMMERSIVE "world is the screen" treatment (`.screen.uv` in phone.css): warm-radial ground · light brand (eyebrow+title) · a `.p-frost` card carrying the digest + an "Open the source" gold pill · a faint type-mark (`.ts`) as imagery · designed glass `.uv-chip`s (NOT a ledger) · a dark-glass `.p-glass` address bar ("dark glass over imagery") · the hatch. Thin data now reads as intentional immersive space, no void, no overlaps. Verified by sight `/tmp/firstslice-immersive-v3.png`. LESSON: "verified by sight" = meets the visual bar (Tim judges), never "I looked and the data appeared". Immersive chosen for thin unit data; the dense `scr-piece` treatment is the alternative if richer.
- **★ RAISE v6 against the lead's adversarial critique (scr-canvas-matched, `/tmp/firstslice-raise-v6.png`):** rebuilt renderUnit to scr-canvas's restrained composition, every critique gap closed: (a) the warm field POPULATED with the unit's real neighbour CONSTELLATION (recollection's recall-under-unit — `distill/harness.ts` → index·distill.test·distill-cli·unit-types·extractor·recall-cli as `.uv-node`s + `.p-spot.i` badges; imagery=relations, each re-drillable); (b) EDITORIAL voice ("The Distill Pipeline" / "Raw exchanges in, kept units out." / telegraphic 3-line card) replacing the filename+digest-dump; (c) gold STAT-COUPLETS (`6 linked·3 steps·1 round`) replacing the metadata ledger; + a QUIET half-width card and an OPERABLE rail (menu + icon bar). Overlaps cleared (brand narrowed, card lowered, nodes zoned clear of brand+card). REAL DATA SEAMS READY: recollection's `corpus(op='neighbours')` (committed) → needs projection's `/api/cognition/neighbours` HTTP route to reach the host; the voice transform (b) is composition's step (authored-representative until wired). NOT self-declared done — held to Tim's eye + the lead's re-critique.
- **★ v7 — the lead's re-critique GREEN ("the raise worked, this is DNA"), + 3 finer points raised (`/tmp/firstslice-raise-v7b.png`):** (1) nodes now carry the editorial ROLE (entry/test/cli/types/extract/recall) not the literal filename; (2) the scatter became a CONNECTED constellation — reused `DNA.org.hubNetwork` (was unused): the unit as a gold-octagon hub, dashed spokes + gold arrowheads to the role-nodes (the unit→neighbour EDGES; my own vocabulary); (3) decorative "i" badges dropped (the hub-constellation is all meaningful). Hub label cleaned (was clipping). ★ HOST WIRING: renderUnit now depends on `DNA.org.hubNetwork` → the surface sync must also copy `ui/organisms.js` (currently syncs unit-view.js + phone.css only). Real neighbour data still wires via projection's `/api/cognition/neighbours`; role + hub are the editorial layer (composition/brain supplies roles for real).
- **★ v8 — the lead's 3 top-tier polish points + the anchorable contract (`/tmp/firstslice-raise-v8.png`):** extended `DNA.org.hubNetwork` (backward-compat) → (1) HUB IDENTITY: gold-FILLED focal octagon + a kind-glyph (`{ }` for code) — the empty focal is now the unit; (2) ORGANIC: seeded angle/radius jitter — ambient field, not a textbook hexagon; (3) WEIGHTED: node size + opacity scale with recall score (normalised for visibility). + the ANCHORABLE CONTRACT (hook-2, lead's option (a)): `[data-anchorable]` + `data-address` on the card lines + card title + brand title (HTML, annotatable — wildcard binds N>0 on re-sync); the role-nodes stay SVG (re-drill via data-address, not HTML-annotatable). STILL MINE (next look-pass): skin fork's brain panel (`.brain-ask`/`.brain-panel`/`.brain-reply`) to the DNA look. HOST: sync must add `ui/organisms.js` (flagged projection).
- **★ v9 — the 2 final fine points (lead's v8 re-critique; `/tmp/firstslice-raise-v9.png`):** (1) card↔constellation CROWDING cleared — dropped the field lower (top 324) so the constellation clears the "How it works" card; (2) the field SPREADS fuller — bigger hubNetwork (320×318) so the organic nodes span the whole lower field, not a tight central ring. The constellation now reads as a full ambient relational field with the gold-`{ }` unit at its center. Lead: "top-tier-approaching, converged" → at Tim's-eye for the absolute-top-tier verdict. Trajectory v3(text-wall)→v6(field)→v7(connected)→v8(identity/organic/weighted)→v9(spread/uncrowded).
- **★ TIM v9 FEEDBACK (2026-06-16) — close, 3 fixes + the big one (the explainer):** Tim saw v9 in the gallery. Fixes DONE + verified (`/tmp/v10-fixes.png`): (1) text CONTRAST — added a soft dark scrim behind `.uv-brand` + stronger shadow + the line at full weight (the faint "kept units out." now reads); (2) the bottom-right ICON BAR — replaced cramped emoji glyphs with real `DNA.org.icon` (compass/chat/ledger) + proper gaps; (3) bottom SPACING evened. THE BIG ONE — Tim: "I don't know what I'm looking at / what I can do; the 'screens' section has a slide format to explain things — use it." → built the EXPLAINER (app-canvas slide format): the live face + "what it is" + "what you can do" (tap a neighbour to drill · tap the words to direct the AI · ask the AI · drive the dials) + a "counted" tally. Tim chose BOTH forms: `unit-face-explained` (LANDSCAPE, reads wide, `/tmp/explainer-v1.png`) + `unit-face-explained-m` (PORTRAIT phone-readable, full-size affordance rows + compact constellation, `/tmp/explainer-mobile-v1.png`). All 3 now gallery pieces (52 total, manifest valid). The explainer pattern (screen + what-it-is/what-you-can-do) is reusable for ALL 19 app screens — teaches each one. v9 verdict: "close" → fixes + explainer answer it; re-verdict pending Tim.
- **★ ADDED INTO THE GALLERY (Tim's directive 2026-06-16) — VERIFIED:** the unit-face is now a REAL gallery piece, not just the _dev harness. New `mockups/unit-face/v1.html` (loads piece.css+phone.css+organisms.js+unit-view.js → `DNA.renderUnit` on the real distill unit + constellation) + a `mockups/manifest.json` entry (id `unit-face`, group "Application · screens, full size" [wired in a category — no silent-invisible], canvas 390×844, dials warmth 70/immersive). Verified by sight rendering IN the gallery at :8090 (`/tmp/gallery-unitface.png`) — the v9 constellation shows in the work area with the full gallery chrome (Versions/feedback/dials/the LIVE warmth slider). It is the FIRST LIVE-COMPOSED gallery piece (composed from data, vs the 49 frozen files) — the live-compose rung, now a navigable piece. Manifest re-validated JSON (50 pieces). So the gallery (:8090) is now a valid access target for Tim's verdict (the lead is recovering his private Tailscale route; funnel scratched per Tim's rule).
- **★ LIVE NEIGHBOUR DATA WIRED (2026-06-16) — the constellation's data half CLOSED:** recollection's `/api/cognition/neighbours` route went live (HTTP, :8770) + projection hosted `organisms.js` in-surface. Wired `renderGallery` to fetch it on the drill (using `opts.source` — the code:// address that carries the vector; the run:// record returns honest-empty, projection's gotcha) → `DNA.neighboursToNodes` maps `{source,score}` → constellation nodes (role via `roleOf`, score min-max-normalised for visible weight, address→re-drill) + `DNA.renderUnit` draws the real hubNetwork. Verified full live path by sight (`/tmp/firstslice-live-roundtrip.png`): the real distill constellation renders + `gallery:rendered` fires. ★ The live path shows RAW voice (filename title + full digest) — the editorial VOICE transform (digest→editorial title/line/telegraphic card) is composition's pending step; once wired, the live render = the polished showcase + real data. So: SHOWCASE piece (:8090, representative neighbours + editorial voice = the polished v9 form) vs LIVE surface/app render (:8770, real neighbours + raw voice until composition). Both honest about state. Host needs the re-sync of unit-view.js (flagged projection).
- **★ INCREMENT 2 (next, integration — cross-member). SEAM-3 CONTRACT (projection, commit 4f1b50c, verified live):**
  - `projection:select detail = {address, source, record, seq, kind, space}`. **`address`** = the CONTENT-RESOLVABLE handle (the **run://** record, e.g. `run://corpus/company/code://…/topics`) → resolve THIS. Resolves to `{kind, output, projection, source_address}`; **`output` is always the key** (digest for common_knowledge, topic-list for topics — shape varies by projection). **`source`** = the canonical `code://`/`vi-vision://` address — NOT resolvable today; **keep for fork's brain + wildcard's route-back, do NOT render from it.** `record` == `address` (back-compat). Resolvable schemes: run·cas·skill·context·session·cap·board (NOT code/vi-vision yet — extensible via cognition.py, flag lead).
  - (a) browser read: `GET /api/cognition/corpus?address=<detail.address>` (pinned, no embedder) → record `{output, source_address, projection}` → `unitFromCorpus` → `renderUnit`. (b) hook `addEventListener('projection:select', e => renderGallery(e.detail.address))`; anchor fork/wildcard onto the rendered element keyed by `e.detail.source`. (c) ★ STILL OPEN — the **DOM-mount seam**: WHERE in surface/app's React DOM the render mounts + a stable container React won't clobber (the bind-sequence needs stable refs). Projection-coordination Q (asked; address-half answered, mount-half pending). (d) emit `gallery:rendered` exposing anchorable elements → fork binds brain + wildcard annotates (bind order: select→render→bind+annotate→route-back→re-render).
- **★ INCREMENT 2 — DNA'S HALF BUILT + VERIFIED BY SIGHT (2026-06-16):** added `DNA.renderGallery(address, opts)` + `DNA.bindProjectionDrill(opts)` to `ui/unit-view.js`. renderGallery: live-fetch `/api/cognition/corpus?address=` (relative → same-origin) → `unitFromCorpus` → `renderUnit` → mount in `opts.container` (mount-agnostic) → emit `gallery:rendered {element, address, source, record}`. Honest degrade (failed/empty resolve → a legible "UNRESOLVED" unit, never a crash). bindProjectionDrill listens `projection:select`, ignores null/address-less, calls renderGallery(d.address, {source:d.source}). VERIFIED: (1) the live endpoint returns the real unit — `curl :8770/api/cognition/corpus?address=<run://…>` → the exact record (output.digest, source_address, lineage); (2) the EVENT FLOW by sight (`_dev/round-trip-test.html`, `/tmp/firstslice-roundtrip.png`): a simulated `projection:select` → renderGallery → render → the `gallery:rendered` banner fires carrying `source=code://…` (where fork+wildcard hook). REMAINING (integration, not mine): the real cross-origin fetch is CORS-blocked from :8090 (bridge :8770 sends no CORS) → works SAME-ORIGIN when hosted in surface/app; the WHERE-to-mount = projection's DOM-mount seam (lead asking them). DNA's half of the round-trip is done; it drops in at the mount point.

## 9 · V9 ROUND-2 (Tim feedback 2026-06-16/17) + the V + the voice transform
- **"Driven by RULES" (Tim's core point) — fixes:** (1) brand shadow had a sharp top line → replaced with a soft feathered text-glow (no box/edge). (2) constellation node circles "text doesn't fit, inconsistent boundaries" → NEW node-fit RULE in `hubNetwork`: each circle sizes to its label + uniform padding (text always fits); prominence = proximity-to-hub + opacity + stroke (not overflowing small circles). (3) bottom spacing folded into the V redesign.
- **★ THE V — fabrication mistake + recovery:** Tim: bring composition's V (an ASSET) into the bottom-right; the black-button functions become its slot/socket system; a 90° arc that EXPANDS outward. I FABRICATED a fake V glyph as a placeholder → Tim: "that's awful, that's not the V." REMOVED immediately; lesson re-burned (never fabricate an owned asset). Lead located it: `vi-classic-gold.svg` (composition's vi-visual-dev repo) + the V_ROW organism-definition. DECISION (mine): mount the STATIC SVG as the corner hub + author the 6-verb socket-set in the gallery's OWN system (`DNA.VEE_SOCKETS`, data — lock 2); reuse the spawn-radial-wheel pattern; NOT cross-repo import (vanilla-JS gallery, separate runtime). BLOCKER: the SVG isn't on the gallery's filesystem → composition must commit/copy it into the gallery repo (or paste the markup). Sockets = navigate/annotate/ask/drive/open-source (+generate, lock 3) — === the explainer's "what you can do" (lock 1).
- **★ VOICE TRANSFORM — re-homed to DNA, DONE (deterministic floor):** `DNA.voiceFromDigest(record)` → {title, line, cardLines}: derives a real TITLE from the subject (harness.ts→"The Distill Pipeline", bridge.py→"The Cognition Engine", verify.test.ts→"The Verify", sync.ts→"The Sync"), a clean short card (never a dump), line OMITTED (a regex telegraphic line reads broken — caught + removed). Wired as the live default in `unitFromCorpus` → a live unit NEVER renders as filename+digest-dump. Verified headless + by sight. The crisp TELEGRAPHIC rewrite (showcase quality) = the AI GENERATE socket (lock 3) upgrade; deterministic is the always-on floor.
- **Explainers (both forms) aligned to the V's 5-socket vocabulary (lock 1):** `unit-face-explained` (landscape) + `unit-face-explained-m` (phone-readable) list navigate/annotate/ask/drive/open-source — the face describes exactly what the V does.
- **Template gate now:** ✓ face-fixes ✓ explainer (self-teaching, both forms) ✓ voice (deterministic floor) ◻ V-nav (asset located, blocked on composition committing the SVG) + Tim's re-verdict. Phase B (build scr-piece fully-interactive → scale to 19) still gates on the finished template + Tim's verdict.
- **★ THE REAL V IS IN THE GALLERY (2026-06-17) — fabrication fully recovered:** found composition's genuine asset on the mounted drive (`/mnt/c/00_ConceptV/06_Project_Vi/repos/vi-visual-dev/overlay/src/assets/brand/logo-svgs/vi-classic-gold.svg` — a Vi monogram: gold ring + V mark) + read the full slot/socket contract from `board://item-534ef183`. Copied the asset to `ui/assets/vi-classic-gold.svg`; mounted it INLINE in `renderUnit` (`DNA.VI_SVG`, real paths verbatim, live-themed to the palette via `.vi-ring`→gold/`.vi-v`→ochre). Built `DNA.veeCorner(sockets, open)`: the V as the corner HUB (frosted disc) + the 5 verb-sockets (drill/mark/ask/dials/source = lock-1 vocab) emerging on a 90° arc up-and-left; RESTING = clean V hub; OPEN = sockets emerge over a dimmed surface (composition's emerge-from-parent intent). Verified IN THE GALLERY (piece "A unit, in its constellation" now shows the real V in its corner; `/tmp/v17-resting.png`, `/tmp/v17-open.png`, `/tmp/gallery-realV.png`). DIVISION held: composition's V = hub (real asset), DNA = the 90° corner-expand geometry + local socket-set (lock 2). The 6th socket (generate) designed-in (lock 3). Template gate ◻V-nav → now ✓ (resting). NEXT: wire the sockets to live functions (the circuit verbs already exist) + the tap-to-open interaction; composition's connection-mount reuse for the real emerge animation.
