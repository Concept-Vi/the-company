# INTERFACE EXCAVATION — what's ALREADY been described (answering Tim's "did you find all the UI descriptions?")

*Tim flagged the lead "doesn't even know if it found all the descriptions about the UI from its own session history." So this is a deliberate excavation (lead-directed scout, 2026-06-16) across: build-prep design dirs · design/ system+mockups · the memory index · AND the lead's own raw transcript (bda8ce28) via semantic recall. Provisional; built-claims are claimed-not-verified per the law.*

## ★ THE GAP TIM WORRIED ABOUT — CONFIRMED REAL
There is an EARLIER interface conception in the lead's OWN June 11–13 history that the current front-interface convergence does NOT absorb — the **SESSION-FABRIC "FACE" (the company as a GRAPH OF MINDS)**:
- "THE SURFACE (the Face) = a canvas, a GRAPH OF MINDS" — each mind a timeline, connected by accumulating edges, grouped into channels (transcript 2026-06-11).
- TWO views of ONE fabric: a resting **graph-of-minds** (orchestration) + a live **lanes/timeline** view when sessions are talking (messages pop up live, openable).
- **Drill in / drill out** — zoom from the graph INTO one mind (a true terminal into that session) and back out.
- **The CLI surfaced NATIVELY** — not an embedded terminal, but the CLI's capabilities (rewind, /mcp, slash commands) reachable in the surface.
- **Selection = Forager grammar; a saved selection = a channel** (circle a set of the fleet → it becomes a channel).
- **The presence dial — all 8 modes** (walkthrough = you follow it; watch-and-react = it follows you).
- "The Face (Loop 2)" — the clicking/viewing/voicing screen, flagged "not built yet."
→ This is a genuinely DISTINCT conception (the company as a graph of MINDS/SESSIONS) vs the projection-wheel (the corpus as a FIELD). ★ DECISION FOR TIM: does it FOLD IN (graph-of-minds = just another projection, frame=sessions instead of corpus-units) or is it a SEPARATE MODE? (Lead's lean: it folds in — it's the same addressed state projected with sessions as the units — but Tim's call.)

## WHAT THE INTERFACE SHOULD BE (principles already recorded — file paths)
- OVERLORD surface; the BOOTSTRAP; NOT a screen but the rendered PROJECTION of one addressed state (front-interface/INTERFACE_BRIEF + BUILD_PLAN).
- RENDER-FOR-COGNITION — project into visual/spatial/temporal/relational shape; "his brain is the algorithm"; tread-marks/density/gaps/dead-ends (memory: feedback-render-for-cognition; build-prep/concurrent-cognition/06-rendering.md).
- THE INTERFACE AUTHORS, not read-only — everything is a variable (axes/lenses/data/frames); load/embed/override/store/filter are first-class; safety = git-revert (memory: feedback-instrument-authors-not-readonly, Tim-direct 2026-06-15).
- RECOGNITION-OVER-GENERATION; navigable landscape, never a query box (universal-projection/RESEARCH-SYNTHESIS).
- The existing UIs are NOT the target; the SUBSTRATE under them is (memory: feedback-company-ui-disposable, Tim-direct).
- FORM = the WHOLE SCREEN as Tim sees it, on real device (memory: feedback-whole-screen-verification).
- Click INDICATES + CONSENTS, does not execute; governance rides the address (design/CONNECTION-CONTRACT.md).

## SPECIFIC SURFACES / MODES ALREADY DESCRIBED
- **The Instrument** (projection's radial wheel) = project()→resolve() as a navigable FIELD; zoomed-out = whole company as one field (overlord altitude); OPTICS cluster (lens·layer·resolution·representation·centre), 8 lenses, paper aesthetic, live SSE; native tri-form-factor (desktop 1440×900 + portrait 390×844 + landscape 844×390, discrete classify() layouts) — CLAIMED BUILT + critic-passed (instrument-surface/, universal-projection/). [claimed-not-verified]
- **The wildcard MODES** (slide/graph/document/team) — see INTERFACE_BRIEF (wildcard's section): slide=recognition surface (the only one really built + the one Tim uses), graph=topology/axes-in-space (8 placeholder templates), document=sections-as-persistent-sub-address-channels (Tim's Feb design; ~60%), team (barely wired).
- **The DNA gallery** — hybrid presentation+UI, AI-in-each-part, content-from-data-files (pending DNA's own surfacing).
- **The design-system blueprint** — J1–J9 journeys, ~40 views, 20 rendered HTML mockups (canvas/inspector/inbox-3-lane/build-review/replay/walkthrough/presence-dial/twin/fleet/studio/scenario-player…) at design/blueprint + design/mockups (most `planned`, 4 `quality-passed`). NOTE: per feedback-company-ui-disposable these are OPEN input, NOT binding/benchmark.

## BUILT vs DESIGNED vs IDEA (downgraded per the verification law — claimed, not re-verified)
- CLAIMED-BUILT (per commit/session records, not independently re-run): the Operable Interface (28 commits, merged — cockpit regions, consent model, altitude-transformation-layer, corpus-true registry); the overnight surfaces (builder panel = Claude Code embedded in canvas, greeting, forager v1); the projection Instrument + tri-form-factor.
- DESIGNED-ONLY: the front-interface convergence; the ~40 design-system views; the cognition authoring UI ("no front-end exists yet"); live-intent-resolution + guided-review surfaces.
- IDEA-stage: the wildcard modes (graph/document/team), the DNA gallery, the Session-Fabric graph-of-minds Face.

## COVERAGE HONESTY (Tim wanted this stated)
- DONE: build-prep design dirs · design/ + mockups · memory index (9 files) · the LEAD's raw transcript (5,108 turns, via recall) — confirmed most high-signal interface talk IS in INTERFACE_BRIEF, surfaced the Session-Fabric Face that ISN'T.
- NOT DONE: the OTHER members' raw transcripts (projection f609942f is 195MB; DNA/composition/wildcard/recollection) — their interface principles live in THEIR histories (Tim said so); only their channel CONTRIBUTIONS are captured, not a recall over each member's own session. ← the next excavation if Tim wants full coverage (recollection's per-member index, when it lands, makes this systematic).
- One contradiction RECONCILED (was: BUILD_PLAN "active build" vs common-knowledge "parked/scout-first"): Tim's 2026-06-16 message resolves it — the interface IS the bootstrap, build it now (even if redone) BECAUSE it's what enables the understanding + the overlord seat. Not contradictory: build the bootstrap interface now; deep-understanding continues in parallel and informs the final form.
