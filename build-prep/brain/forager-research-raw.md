# Forager research — raw lane findings (workflow wf_c199b8c6, 2026-06-10)

## LANE: facets

SUMMARY: CORPUS facet coverage for a faceted canvas, from /home/tim/company (read-only). The corpus = 2067 deduped records (2080 corpus.record events in .data/store/events.jsonl): history=1420 (mined exchange:// units), repo=644 (code:// digests), +3 stragglers; vectors: history=1420, repo=644, legacy default=78. Queryable TODAY without content fetches (the index row, corpus.py:178-193): projection/lens, record_kind, source_address (and its scheme → a free conversation-vs-code facet), lineage{session, round, project}, capture-ts. find_corpus filters project/kind/projection/source_address (corpus.py:230-242); query_corpus(text, *, space=None, k=8) (suite.py:10256) gives per-space semantic retrieve through the MCP corpus tool (mcp_face/tools/corpus.py:16). Spaces are enumerated from the DECLARED registry — 5 embeddable lenses (repo, history, topics, principles, worldview; suite.py:9906) — but only repo+history are populated; topics/principles/worldview have ZERO records/vectors. Verdict on Tim's five facets: PROJECT = queryable but degenerate (2079/2080 records say 'company'; lineage.session names capture campaigns, not Tim-projects → source-project needs derivation from transcripts). TIME PERIOD = needs derivation (event ts is capture time, all 2026-06-09/10; true source time ts_source exists on only 173/1420 history records, content-only, written by a miner version no longer in the tree — current g23_mine.py extracts the exchange ts at :112 but drops it at :130, so a deterministic no-model backfill from the transcripts is possible and cheap). TOPIC = declared space, zero data; needs a topics capture run (machinery ready); interim approximation = pattern_tag clusters (flows/pattern_cluster.py → .build/g13/clusters.json). PRINCIPLE = declared space, zero data; needs a principles capture run; find_relations (suite.py:10283) is already built and fail-louds until it's populated. LANGUAGE/VOCABULARY = does not exist at all — named in design as 'the coined-vocab projection' (ai_tics.py:6) but no projections/ file declares it; needs a file-drop registry row + capture run. Also: no count/aggregate API exists anywhere (facet counts = client-side over op='list'), and ts_source/pattern_tag live only in cas content, not on the index event — the main structural gap between the store and a faceted surface.

- The corpus index row — the ONLY fields facetable without per-record content fetches
  WHERE: /home/tim/company/runtime/corpus.py:178-193 (event row), :208-227 (list_corpus), :230-242 (find_corpus)
  DETAIL: Every corpus record is indexed by a `corpus.record` event carrying exactly: seq, ts (capture time), cas, address, source_address, record_kind, model, projection, lineage{session, round, project}, and source_hash (when supplied — 629 of 2080 events). list_corpus is a read-time projection over events_since(-1) deduped by address (latest seq wins); find_corpus filters ONLY by project / kind / projection / source_address (corpus.py:230-242). Real data: 2080 events → 2067 deduped records.
  IMPLICATION: A faceted canvas gets project, projection (lens), record_kind, source_address, capture-ts, session, round for free from the index. Any other facet (source time, pattern_tag, topic words) requires reading each record's cas content — a 2k-record scan today, fine once, but it is NOT an indexed field.

- query_corpus signature + the MCP face
  WHERE: /home/tim/company/runtime/suite.py:10256; /home/tim/company/mcp_face/tools/corpus.py:16-18
  DETAIL: Suite.query_corpus(self, text: str, *, space: str | None = None, k: int = 8) -> dict — embeds text via _embed_consult_query (BGE-M3 @ :8001), ranks via store/vector_index.query_index(store, qvec, k, space=, with_note=True); returns {query, space, ranked:[{id, score}], note}; embedder down → honest empty with note. MCP tool: corpus(op: Literal['query','list','find','read'], project='', kind='', projection='', source_address='', address='', text='', space='', k=8, detail='concise', limit=50). detail='detailed' on op='query' inlines each hit's record content (suite.find_corpus(source_address=hit) → read).
  IMPLICATION: Semantic per-space retrieval is fully wired through the MCP face today; a canvas can call corpus(op='query', space=...) and corpus(op='find', ...) directly. There is no count/aggregate op — facet counts must be computed client-side from op='list' detail='concise' (2067 rows).

- Spaces: 5 declared embeddable, only 2 populated; enumeration is registry-based not data-based
  WHERE: /home/tim/company/projections/ (8 files); /home/tim/company/runtime/suite.py:9906; /home/tim/company/.data/store/vectors/ (2142 entries)
  DETAIL: Declared lenses (projections/<id>.py): what(embeds:F), claimed_status(F), lineage(code,F), repo(T), topics(T), principles(T), worldview(T), history(T) → 5 embeddable spaces. cognition_info enumerates spaces as [p.id for p in projection_registry.embeddable()] (suite.py:9906) — the DECLARED set, including empty ones. Actual vectors per space (counted from every vectors/*.json `space` field): history=1420, repo=644, None/default(legacy consult code-symbol index)=78; topics=0, principles=0, worldview=0. Records per projection (deduped events): history=1420, repo=644, what=1, None(retired)=2.
  IMPLICATION: A canvas asking cognition_info().spaces will be told topics/principles/worldview exist — querying them returns the honest 'index EMPTY' note (vector_index.py:166-173). There is no per-space count API; counts need store.index_addresses(space=...) or a vectors-dir scan.

- FACET: project — queryable TODAY but degenerate in real data
  WHERE: /home/tim/company/runtime/corpus.py:73,107-113 (lineage gate), :220 (project filter); events.jsonl aggregate
  DETAIL: lineage{session, round, project} is REQUIRED at write (fail-loud gate, corpus.py:97-119) and rides on every index event, so find_corpus(project=...) works structurally. BUT real values: project='company' on 2079/2080 events, 'cognition-engine' on 1. The discriminating lineage axis is session (g23-mine=1419, ingest-full=392, exocortex-core=130, full-repo=62, ingest-flow=60, ...) — which names the CAPTURE CAMPAIGN, not what Tim means by project (Vi / OpenClaw / vault / etc.).
  IMPLICATION: The 'project' facet exists as a field but carries one value — it cannot partition the canvas today. Tim's notion of source-project needs DERIVATION: for the 1419 exchange:// records, from the transcript's cwd/content (the transcripts live at /home/tim/.claude/projects/-home-tim, g23_mine.py:29); for code:// records it is trivially 'company'.

- FACET: time period — capture-ts useless, source-ts exists on only 173/1420 history records and is content-only
  WHERE: /home/tim/company/runtime/corpus.py:160-164; cas content scan; /home/tim/company/build-prep/cognition-self-improvement/g23_mine.py:112,130
  DETAIL: Index-event ts is the WRITE time (deliberately excluded from cas content for resume-safety, corpus.py:160-164) — and the whole corpus was captured 2026-06-09→06-10, so ts cannot partition by period. A `ts_source` field (the source exchange's original timestamp, range 2026-05-25→2026-06-08) exists ONLY inside the cas content of 173/1420 history records (seq 2483-2655, the earliest g23-mine batches) — written via write_record's **extra seam by a miner version no longer in the tree (grep finds NO .py writing ts_source today; current g23_mine.py:112 carries e['ts'] into the mining unit but :130 drops it from the persisted record). It is NOT on the index event (only source_hash rides up, corpus.py:191).
  IMPLICATION: Time-period faceting needs derivation — but it is CHEAP and deterministic: exchange://<sid>/<i> maps back to the transcript file + index via the same extract_exchanges walk (no model, no re-mine), so a backfill pass can stamp ts_source onto all 1420 history records (re-write same address; latest-seq-wins supersedes) and/or onto the index event. Re-adding ts_source to g23_mine's record dict (one line at :130) stops the gap growing.

- FACET: topic — declared space, ZERO data; pattern_tag is the nearest live approximation
  WHERE: /home/tim/company/projections/topics.py (embeds:true, field:array); vectors scan (topics=0); history output keys
  DETAIL: The topics lens is fully declared ('the subjects/areas this file covers', embeds:true → would become vec://<item>#space=topics) but no capture run has ever produced a topics record: 0 records, 0 vectors. What exists today: (a) semantic query in repo/history spaces (topic-LIKE retrieval, not a facet), and (b) `pattern_tag` — a coined per-exchange tag on all 1419 history outputs ({decision, rationale, tim_correction, my_error, bug_fix, needs_tim, frustration, pattern_tag}) — content-only, already clustered by flows/pattern_cluster.py into named weighted groups at .build/g13/clusters.json.
  IMPLICATION: A true topic facet needs a capture run with projection='topics' over the sources — the machinery (capture_corpus + embed_corpus_to_spaces) exists and the space auto-appears; no code change. Interim: the pattern_cluster output is a ready-made tag-cluster facet for the history half of the corpus.

- FACET: principle — declared space, ZERO data; the inversion-finder is already built waiting on it
  WHERE: /home/tim/company/projections/principles.py (embeds:true); /home/tim/company/runtime/suite.py:10283-10345 (find_relations)
  DETAIL: principles is the M3-corroboration space ('high recurrence across sessions in principle-space = a corroboration mark' — the very reason lineage is gated at write, corpus.py:8-15). It is declared embeddable but has 0 records and 0 vectors. find_relations(item, near_space, far_space, k=10, min_score=0.5) — the 'same principle, different subject' inversion — is implemented and fail-louds if the item has no persisted vector in BOTH named spaces (suite.py:10313-10323), which today is every item for principles.
  IMPLICATION: Principle faceting needs a capture run with projection='principles' (machinery ready, registry ready). Until then any canvas affordance built on find_relations or principle-space clustering will fail-loud by design.

- FACET: language/vocabulary — does not exist; named in design (the 'coined-vocab projection') but never declared
  WHERE: /home/tim/company/runtime/ai_tics.py:4-9 (references 'the coined-vocab projection'); /home/tim/company/projections/ (no such file)
  DETAIL: No projection for vocabulary/language exists among the 8 declared lenses. runtime/ai_tics.py's fingerprint pass (M4) is specified to match its tic catalogue 'against the coined-vocab projection' — i.e. a coined-vocabulary lens is a named design dependency — but no projections/<id>.py declares it, so it has 0 schema, 0 records, 0 vectors. The nearest live signal is again history.pattern_tag (coined tags like 'vpn-off-for-public-downloads').
  IMPLICATION: This facet needs BOTH a new registry row (drop projections/coined_vocab.py — zero code change, the registry is file-discovered per projections.py:11-21) AND a capture run. It is the furthest-away facet, but it unblocks the already-built ai_tics subtraction pass as a side effect.

- The per-space vector substrate the canvas would rank/cluster over
  WHERE: /home/tim/company/store/fs_store.py:830-839 (space_address), :841-877 (put_vector), :896-945 (index_addresses/index_corpus); /home/tim/company/store/vector_index.py:142 (query_index)
  DETAIL: Spaced vectors are keyed vec://<source>#space=<proj> with `space` and `source` as explicit fields (portable field-match, fs_store.py:824-828 — 'a SQL backend implements WHERE space = X'). query_index(store, query_vector, *, k=5, with_note=False, space=None): space=None ranks ONLY the default/unspaced 78 legacy entries; a named space returns SOURCE item ids; FsStore.ALL_SPACES ranks everything (cross-dim cosine crash risk noted at fs_store.py:889-892). Per-space query returns the bare item address which round-trips through corpus op='read'.
  IMPLICATION: Per-space k-NN, per-space enumeration, and item round-trip are all real today for repo+history. Note the default-space trap: a canvas passing space='' / None silently queries the 78-entry legacy consult index, not the corpus.

- Source-address scheme split — the corpus is two corpora wearing one substrate
  WHERE: events.jsonl aggregate; /home/tim/company/runtime/suite.py:10051 (ingest_paths), build-prep/cognition-self-improvement/g23_mine.py
  DETAIL: source_address schemes: exchange://<sid>/<i> = 1419 (mined conversation exchanges, history space), code://<path> = 659 (repo file digests, repo space; output={digest, kind}), run:// = 1, deferred:// = 1. capture_corpus(records, *, project, session, round='1', embed_fn=None) (suite.py:10152) is the one capture+embed seam both faces share; ingest_paths(roots/paths, project='company', session='ingest', round='1', projection='repo', max_files=50, force=False) (suite.py:10051) is the incremental repo path with content-hash refresh (G25).
  IMPLICATION: The canvas's facet semantics differ by half: time/project/vocabulary derivations are exchange-side problems (transcripts are the ground truth to backfill from); repo-side records already have a stable code:// id + content-hash staleness story. A 'source kind' facet (conversation vs code) is queryable TODAY from the address scheme prefix.

## LANE: retrieval

SUMMARY: Retrieval seams in /home/tim/company, mapped with file:line evidence. THREE Suite methods: query_corpus(text, *, space=None, k=8) at runtime/suite.py:10256 (embeds via BGE-M3 then ranks the persisted index; honest-empty {ranked:[],note} when :8001 is down; returns ranked [{id,score}]); find_corpus(*, project/kind/projection/source_address) at suite.py:10247 → runtime/corpus.py:230 (event-log read-time projection, newest-first rows with full lineage, no Suite-level limit); find_relations(item, *, near_space, far_space, k=10, min_score=0.5) at suite.py:10274 (near∩¬far inversion anchored on persisted per-space vectors — zero embedder dependency, raises on a missing anchor). EMBEDDER PATH: suite._embed_consult_query:4306 → fabric/transport.py:261 openai_embeddings_transport → fabric/client.py:148 complete_embeddings (count+dim guards, 3 retries); config fabric/config.py:26-37 = http://localhost:8001/v1, BAAI/bge-m3, 1024-dim, 180s timeout. Residency = ops/cli/capabilities.py:400 ensure_resident('embed-bge' — services.json:274, port 8001, 2.5GB, vllm-embed.service) with port-serving wait and the G14 swap-approval dict — but the query path does NOT call it; it degrades. HTTP TODAY (runtime/bridge.py): GET /api/cognition/corpus (:905 — read-one/find/list), GET /api/cognition/find_relations (:897), POST /api/cognition/corpus (:1870 — capture+embed write); /api/corpus (:667) is the MOCKUP gallery, a verified name-collision. MCP: the consolidated corpus(op='query'|'list'|'find'|'read') tool (mcp_face/tools/corpus.py:16 — detailed mode inlines record content; read round-trips code:// ids) + flat find_relations (server.py:918) + ingest (tools/ingest.py:15, space='repo', 50 files/call). CALLABLE AS-IS for a whittle-search canvas: metadata whittling, record read-back, and the inversion-finder, all over existing GETs. THE ONE GAP: semantic query has NO HTTP route — query_corpus is MCP-only (zero hits in bridge.py); a new GET (e.g. /api/cognition/query) delegating to SUITE.query_corpus, added to the BRIDGE_ROUTES drift-table (bridge.py:45), mirroring the MCP tool's contract (space/k/detailed-enrichment), is the single new seam — optionally fronted by ensure_resident('embed-bge') or by surfacing the honest empty-note with a load action. Live index: 2,142 vectors (repo=644, history=1,420, rest=default consult space); spaces topics/principles/worldview are registered-embeddable but unpopulated. Ranking is a full-space pure-Python cosine scan per query (nodes/retrieve.py:41) — fast at this scale, debounce rather than per-keystroke embed; k is effectively uncapped at runtime.

- Suite.query_corpus — the semantic text-search seam (NOT exposed on the bridge)
  WHERE: /home/tim/company/runtime/suite.py:10256-10272
  DETAIL: Signature: query_corpus(self, text: str, *, space: str | None = None, k: int = 8) -> dict. Flow: _embed_consult_query(text) embeds via BGE-M3 @ :8001 → store/vector_index.query_index(store, qvec, k=k, space=space, with_note=True). Returns {query, space, ranked:[{id,score}], note}. space=None ranks the DEFAULT/unspaced index (the code://+ui:// consult symbols), NOT 'repo' — the ingested-file corpus lives in space='repo'. Degrade-with-warning: embedder down → {ranked:[], note:'embed endpoint unreachable — no semantic query'} (honest empty, never a zero-vector). No k ceiling enforced anywhere in the runtime path (nodes/retrieve.py:44-48 does scored[:k]; the CONFIG 'max':100 at nodes/retrieve.py:27 is canvas-schema metadata only, not enforced in run()).
  IMPLICATION: This is the heart of a whittle-search UI but it is MCP-only today — grep proves zero query_corpus references in runtime/bridge.py. An HTTP canvas needs ONE new GET route (e.g. /api/cognition/query?text=&space=&k=) delegating to SUITE.query_corpus, mirroring the existing find_relations route at bridge.py:897-904, plus an entry in the BRIDGE_ROUTES table (bridge.py:45-75 — a drift test fails loud if the table and handlers diverge).

- Suite.find_corpus / list_corpus / read_corpus_record — the filter-whittle axes (fully exposed over HTTP already)
  WHERE: /home/tim/company/runtime/suite.py:10232-10254 → /home/tim/company/runtime/corpus.py:208-242
  DETAIL: find_corpus(*, project=None, kind=None, projection=None, source_address=None) -> list[dict]; rows are corpus.record index events {address, cas, source_address, record_kind, model, projection, lineage:{session,round,project}, seq, ts}, newest-first, NO limit parameter at the Suite level (the MCP tool layers limit=50 on top). Mechanism: read-time projection over store.events_since(-1) filtered to kind=='corpus.record', dedup-on-read latest-seq-wins (corpus.py:216-227) — a full event-log scan on every call, then in-Python list filters (corpus.py:235-242). read_corpus_record(address) = head→get_content, honest None (suite.py:10232-10237).
  IMPLICATION: A fast iterative whittle UI can call this as-is via GET /api/cognition/corpus (?address= reads one; any of project/kind/projection/source_address filters; bare → list) — no new route needed. The metadata axes (project · kind · projection · source_address) work with the embedder DOWN. Watch the cost shape: every keystroke-refilter re-scans the whole event log; for rapid iteration the UI should fetch once and whittle client-side, or a new route should add limit/since paging (Suite has none).

- Suite.find_relations — the cross-space inversion query (exposed on BOTH faces, no embedder dependency)
  WHERE: /home/tim/company/runtime/suite.py:10274-10333; HTTP: /home/tim/company/runtime/bridge.py:897-904; MCP: /home/tim/company/mcp_face/server.py:917-942
  DETAIL: Signature: find_relations(self, item: str, *, near_space: str, far_space: str, k: int = 10, min_score: float = 0.5) -> dict. Anchored on the item's OWN persisted per-space vectors (store.get_vector(store.space_address(item, space)) — suite.py:10310-10313), so NO :8001 round-trip at query time. Computes near = thresholded k-NN in near_space, far = same in far_space, relations = near.ids − far.ids − {item}. Returns {item, near_space, far_space, min_score, relations:[ids], near:[{id,score}], far:[{id,score}]} — near/far carry the WHY rows for rendering. FAIL-LOUD: ValueError if the item lacks a persisted vector in EITHER space (suite.py:10314-10323). Bridge GET /api/cognition/find_relations takes item/near_space/far_space[&k&min_score]; a missing axis → KeyError → 400 (bridge.py:932-933 catch).
  IMPLICATION: Callable as-is from a canvas for relation edges/pivots. Two UI obligations: (1) only offer it on items already embedded in both chosen spaces (else the 400 surfaces — catch and render it, the error text is teaching-grade); (2) spaces must come from the live registry (cognition_info().spaces — suite.py:9906 derives them from projection_registry.embeddable()), never hardcoded.

- Embedder transport path — BGE-M3 @ :8001, OpenAI /v1/embeddings shape, dim-guarded
  WHERE: /home/tim/company/runtime/suite.py:4306-4322 → /home/tim/company/fabric/transport.py:261-283 → /home/tim/company/fabric/client.py:148-190; config: /home/tim/company/fabric/config.py:26-37
  DETAIL: Suite._embed_consult_query(query) is the ONE query-side embed seam every retrieval call reuses (query_corpus at suite.py:10268, consult at 4351). It builds transport.openai_embeddings_transport(base_url=fcfg.DEFAULT_EMBED_URL) and calls client.complete_embeddings(t, [query], model=fcfg.DEFAULT_EMBED_MODEL, dim=fcfg.DEFAULT_EMBED_DIM). Config (env-overridable): COMPANY_EMBED_URL default http://localhost:8001/v1 · COMPANY_EMBED_MODEL default BAAI/bge-m3 · COMPANY_EMBED_DIM default 1024 · timeout COMPANY_FABRIC_TIMEOUT=180s. complete_embeddings guards: transport error / empty / count-mismatch / per-vector dim-mismatch, 3 retries with backoff, then raises FabricError — never partial. _embed_consult_query catches ALL exceptions → emits a durable warning event → returns None (the degrade signal).
  IMPLICATION: A search UI gets dim-consistent cosine for free (the index was built with the same model/dim — a mismatch raises in retrieve._cosine, nodes/retrieve.py:32-35). Per query cost = ONE :8001 round-trip + O(N) pure-Python cosine over the space (2,142 vector files live at ~/company/.data/store/vectors — fine at this scale, but every query is a full rescan with no caching; an iterative whittle UI should debounce keystrokes rather than embed per character).

- Embedder residency — ensure_resident('embed-bge') exists but is NOT wired into the query path
  WHERE: /home/tim/company/ops/cli/capabilities.py:400-510 (ensure_resident), :381-397 (_wait_until_serving); service: /home/tim/company/ops/services.json:274-297
  DETAIL: Service key 'embed-bge': port 8001, user-unit vllm-embed.service, vram_mb 2500, health /v1/models, --runner pooling. ensure_resident(model_or_service, *, evict=False, reg=None, wait=True, timeout=420) is the gated launch/select/evict actuator: idempotent already-resident no-op; fit-check via gpu.check_fit; G14 swap-approval ASK dict ({swap_needed:True, would_load, would_evict, free_gb, needed_gb, approve}) when eviction is needed and not pre-authorized; blocks until the port actually serves (so the very next complete_embeddings hits a ready endpoint — the docstring at :384-386 names exactly this). Suite.query_corpus does NOT call it — grep shows zero ensure_resident hits in runtime/suite.py; the query path's only embedder behaviour is degrade-to-honest-empty.
  IMPLICATION: The canvas has a choice the code already supports: (a) surface the honest 'embed endpoint unreachable' note and offer a load action (the CLI path: company ensure embed-bge / company up --wait), or (b) a new query route wires ensure_resident('embed-bge', wait=True) ahead of SUITE.query_corpus — at 2.5GB the bge embedder usually fits without eviction, but the route must handle the swap_needed dict (render the approval ask, never silently evict). Nothing auto-starts at boot per the ops model, so first-query-of-session WILL hit the down case unless handled.

- Bridge /api surface for search today — three corpus-adjacent routes, one name-collision trap, no semantic-query route
  WHERE: /home/tim/company/runtime/bridge.py:45-75 (BRIDGE_ROUTES), :667-671 (/api/corpus), :897-904 (find_relations GET), :905-919 (cognition/corpus GET), :1870-1903 (cognition/corpus POST)
  DETAIL: GET /api/cognition/corpus — ?address= → {record} (read-one); any of project/kind/projection/source_address → {records} via find_corpus; bare → {records} via list_corpus. GET /api/cognition/find_relations — ?item=&near_space=&far_space=[&k=&min_score=] → the inversion dict. POST /api/cognition/corpus — the WRITE half: capture+embed-on-write via Suite.capture_corpus (single-record or {records:[…],project,session,round} batch; lineage gate → CorpusError → 400; non-embeddable projection raises). TRAP: /api/corpus (bridge.py:667-671) is the MOCKUP GALLERY index — a verified name-collision called out in the route comment at :906; the cognition corpus is /api/cognition/corpus. There is NO /api route that performs an embed-and-rank semantic search: BRIDGE_ROUTES lists no /api/cognition/query, /api/search, or similar, and query_corpus never appears in bridge.py. Errors on all GETs fail loud as 400 {error: 'Type: msg'} (bridge.py:932-933).
  IMPLICATION: As-is for a canvas over plain fetch(): metadata whittling (find_corpus axes), record read-back, and the inversion-finder. The ONE missing piece for whittle-search is the semantic route — a ~5-line addition following the find_relations pattern (delegate to SUITE.query_corpus, add to BRIDGE_ROUTES). If the canvas also wants the MCP corpus tool's detail='detailed' content-inlining (hit → record content in one call), that enrichment loop (mcp_face/tools/corpus.py:57-65: find_corpus(source_address=hit.id) → read_corpus_record) should be hoisted into the new route or into Suite.query_corpus rather than re-implemented FE-side.

- MCP corpus tool — the consolidated 4-op retrieval face (the only place query is exposed today)
  WHERE: /home/tim/company/mcp_face/tools/corpus.py:14-107; registered note at /home/tim/company/mcp_face/server.py:910-913; ingest sibling: /home/tim/company/mcp_face/tools/ingest.py:13-41
  DETAIL: corpus(op: Literal['query','list','find','read'], project='', kind='', projection='', source_address='', address='', text='', space='', k=8, detail='concise', limit=50). op='query': requires text; optional space (e.g. 'repo'), k; detail='detailed' inlines each hit's record content + record_address (the P5 one-call answer); every hit id is op='read'-able. op='list'/'find': limit=50 cap, concise shape = {source_address, projection, seq, address} per row. op='read': normalizes code:// absolute/'./' paths to the canonical repo-relative id (P8, :84-90), round-trips source ids via find_corpus newest-first (N1, :96-100), and returns a teaching error naming ingest(paths=[...]) when nothing is found (:102-106). find_relations stays a separate flat MCP tool (server.py:918). ingest(roots, paths, project='company', session='ingest', round='1', space='repo', max_files=50, force=False) populates the repo space, incremental path-keyed skip, self-teaching `remaining` hint.
  IMPLICATION: An agent-driven whittle loop works END-TO-END through MCP today: ingest → corpus(op='query', space='repo') → corpus(op='read') → find_relations → mark. The MCP face is therefore the reference semantics for the new HTTP route (same params, same detailed-enrichment, same teaching errors) — per the MCP-is-top-priority law, the canvas route should mirror, not fork, this tool's contract.

- Index mechanics + live population — what a query actually ranks over
  WHERE: /home/tim/company/store/vector_index.py:142-172 (query_index), :64-139 (build_index); /home/tim/company/store/fs_store.py:830-941 (space_address/put_vector/get_vector/index_corpus, ALL_SPACES at :893); nodes/retrieve.py:41-48; live data: /home/tim/company/.data/store/vectors/
  DETAIL: query_index(store, query_vector, *, k=5, with_note=False, space=None): loads the ENTIRE space corpus via store.index_corpus(space=) into memory, ranks via nodes/retrieve.run (pure-Python cosine, dim-mismatch raises ValueError, zero-vector raises ZeroDivisionError — both fail-loud). space=None = default/unspaced only (spaced entries never leak in); named space = within-space k-NN returning SOURCE ids; FsStore.ALL_SPACES = everything. with_note=True distinguishes 'index EMPTY (embedder was down at build / space unpopulated)' from 'populated, no match'. Space keys are vec://<item>#space=<projection> (fs_store.space_address:830). LIVE population (counted): 2,142 vector files total — space=repo 644, space=history 1,420, remainder = the default consult index over code://+ui:// symbols. Embeddable projections (embeds:true in /home/tim/company/projections/): repo, topics, principles, worldview, history — topics/principles/worldview registered but currently unpopulated (a query there returns the honest empty-note, and find_relations anchored there raises).
  IMPLICATION: The whittle UI's space picker must bind cognition_info().spaces (live registry) and should expect three population states per space: populated (repo/history), registered-but-empty (topics/principles/worldview — render the note, offer capture/ingest), and default (the symbol-level consult index — useful for 'ask the code' as opposed to 'ask the digests'). Linear-scan cosine at 2k vectors is millisecond-cheap; no pagination/cursor exists on the ranked list, so 'show more' = re-query with larger k (cheap, same scan).

## LANE: canvas

SUMMARY: Read-only research of /home/tim/company/canvas/app (tldraw 3.15.6 confirmed in node_modules). The app has exactly one custom shape (NodeShapeUtil, registered at App.tsx:364) whose pattern — TLBaseShape props schema with T.* validators, Rectangle2d geometry, HTMLContainer body with useValue-based semantic zoom (zoom > 0.5), SVG indicator — is the direct template for a forager circle; Circle2d is exported by the installed version for a true circular hit-area. Programmatic creation follows loadGraph's update/create/prune (NodeShape.tsx:194-222) with createShapeId-namespaced ids ('n-' prefix); a circle loader with its own prefix and type filter coexists untouched because the prune, the fitGraph/jitter/paintStuck consumers, the inspector's getOnlySelectedShape read, and the /api/move store-listener are ALL fenced to s.type === 'node' (audited line-by-line). Multi-select is native tldraw and already exercised: getSelectedShapes().filter-by-type (N-aware in deleteSelected/chat-focus), variadic editor.select(...ids)+zoomToSelection, reactive selection via useValue. Edges (NodeShape.tsx:243-278) is the copyable screen-space overlay (getCamera-subscribed useValue → getShapePageBounds → pageToScreen → pointer-events:none SVG) if circles need link lines. The ONE risk seam is persistenceKey discipline (App.tsx:363-373): the IndexedDB snapshot is validated per-record on load and a schema mismatch white-screens — ship circles with a v3→v4 bump, keep the util permanently registered, and note circles persist across reloads unless actively pruned. Minimal path: 1 new ForagerShape.tsx + shapeUtils array edit + persistenceKey bump + a loadResults sibling — zero edits to existing graph-loader code. Runtime behavior of the new shape is Inferred (patterns observed, nothing executed in this pass).

- Custom shape registration pattern (the template a forager circle must mirror)
  WHERE: /home/tim/company/canvas/app/src/NodeShape.tsx:16-43 and /home/tim/company/canvas/app/src/App.tsx:364
  DETAIL: Observed: NodeShape is `TLBaseShape<'node', {...}>` (NodeShape.tsx:16-19); the util class declares `static override type = 'node'` (line 31), a `static override props` map of T.* validators (lines 32-35), `getDefaultProps()` (36-38), `getGeometry()` returning `new Rectangle2d({width, height, isFilled: true})` (39-41), `canResize = () => false` (42), `hideRotateHandle = () => true` (43), an HTML body via `component()` returning `<HTMLContainer>` (129-179), and an SVG `indicator()` (181-183). Registration is one line: `<Tldraw shapeUtils={[NodeShapeUtil]} ...>` (App.tsx:364). For a circle: `Circle2d` is exported by the installed tldraw 3.15.6 (declared at @tldraw/editor dist-esm/index.d.mts:552 and re-exported in tldraw's own index.d.mts; tldraw/package.json version confirmed 3.15.6) — so geometry can be a true circle hit-area, with `indicator()` returning an SVG <circle>. Interactive elements INSIDE a shape use `stopEventPropagation` + pointer-capture so tldraw doesn't read them as drag/select (NodeShape.tsx:106-113); a circle's BODY must NOT stop propagation, or shift-click/marquee multi-select would break — only inner buttons should.
  IMPLICATION: A second shape is a sibling file (e.g. ForagerShape.tsx) + one array edit at App.tsx:364 (`shapeUtils={[NodeShapeUtil, ForagerShapeUtil]}`). Nothing else registers shapes.

- Semantic zoom inside a shape component
  WHERE: /home/tim/company/canvas/app/src/NodeShape.tsx:46-49
  DETAIL: Observed: `const zoom = useValue('zoom', () => editor.getZoomLevel(), [editor])` then `const expanded = zoom > 0.5` gates the detail body (lines 150, 166). useEditor/useValue are usable inside ShapeUtil.component because it renders under <Tldraw>. Companion pattern: shape components cannot reach Hud React state, so registry/hook reads go through the module-scope external store (registryStore.ts:1-17 explains why; imperative getters at 83-92; controller installs hooks via setConnect/setForceRun at useAppController.ts:512-514, store side registryStore.ts:52-63).
  IMPLICATION: A forager circle gets semantic zoom (dot at far zoom, title+score card when zoom > threshold) by copying two lines; if a circle needs a click-action hook (e.g. 'promote result into graph'), install it the setConnect/setForceRun way — never via React context.

- Programmatic shape creation — loadGraph's update/create/prune, and why it CANNOT touch a second shape type
  WHERE: /home/tim/company/canvas/app/src/NodeShape.tsx:21, 194-222, 224-238
  DETAIL: Observed: ids are namespaced via `shapeIdFor = (nodeId) => createShapeId('n-' + nodeId)` (line 21). loadGraph does (a) `editor.updateShape` if `editor.getShape(id)` exists (208-209), (b) `editor.createShape` at backend position otherwise (211), (c) PRUNE — but the orphan filter is `s.type === 'node' && !live.has(props.nodeId)` (215-216), so any non-'node' shape is invisible to the prune. The `wasEmpty` zoomToFit guard also filters to type 'node' (196). refresh() only updates ids it derives from g.nodes via shapeIdFor (226-235).
  IMPLICATION: Coexistence is safe BY CONSTRUCTION at the loader: search-result circles created with a distinct id prefix (e.g. createShapeId('sr-' + resultId) — distinct prefix avoids any collision in the one shape-id space) will never be pruned, moved, or updated by loadGraph/refresh. The minimal creation path is a sibling `loadResults(editor, results)` doing the same update/create/prune but filtered to its own type — zero edits inside loadGraph.

- The drag write-back store listener is already type-fenced — circles are inert to /api/move
  WHERE: /home/tim/company/canvas/app/src/useAppController.ts:616-629 and /home/tim/company/canvas/app/src/NodeShape.tsx:192-193
  DETAIL: Observed: `editor.store.listen((entry) => { ... if (!to || to.typeName !== 'shape' || to.type !== 'node') continue ... })` with options `{ source: 'user', scope: 'document' }` (628). Two fences: (1) programmatic createShape/updateShape calls are not source:'user', so loadResults-style creation emits no move calls (the documented feedback-loop trap, NodeShape.tsx:192-193); (2) the `to.type !== 'node'` continue means even USER drags of a circle never POST /api/move.
  IMPLICATION: No backend wiring is required for circles to exist and be draggable. If circle positions should persist later, that is a deliberate new branch in this listener, not a default behavior.

- tldraw 3.15.6 selection APIs already exercised in this codebase — multi-select is native and the read idiom is established
  WHERE: /home/tim/company/canvas/app/src/useAppController.ts:765, 804, 1173, 1633, 1891-1894, 754, 1261, 1764
  DETAIL: Observed usages: `editor.getSelectedShapes().filter(s => s.type === 'node') as NodeShape[]` — multi-aware reads in wireSelected (765, requires exactly 2), deleteSelected (804, handles N), chat focus shipping (1173, maps N selected nodeIds), portalSelected (1633). Reactive single-selection: `useValue('sel', () => { const s = editor.getOnlySelectedShape(); return s && s.type === 'node' ? s.props : null }, [editor])` (1891-1894) drives the Inspector + freshness/versions effects (1930, 1958). Programmatic selection: `editor.select(sid)` single (754, 1764) and `editor.select(...canvasIds)` variadic multi (1261), each followed by `editor.zoomToSelection({animation})`. Shift-click and marquee multi-select are tldraw built-ins — deleteSelected already depends on N-selection working with zero custom gesture code. Inferred (not executed): the same gestures apply to any registered shape type, since selection is shape-type-agnostic in tldraw; I have not run a build to verify circles specifically.
  IMPLICATION: Multi-select of forager circles costs nothing: `editor.getSelectedShapes().filter(s => s.type === 'forager')` for batch actions (mirror deleteSelected at 803-813), `useValue` for a reactive selected-circles list, `editor.select(...ids)` to select a result set programmatically. The Inspector's `s.type === 'node'` check (1893) means a selected circle yields `selected = null` — no crash, just no node inspector; a circle-specific panel is an additive branch.

- Every existing shape consumer is type-filtered — the full no-breakage audit
  WHERE: /home/tim/company/canvas/app/src/useAppController.ts:712 (fitGraph), 745 (addNode jitter), 2001-2002 (paintStuck), 1173 (chat focus), 1892-1893 (inspector); /home/tim/company/canvas/app/src/NodeShape.tsx:196, 215 (loader)
  DETAIL: Observed: all seven `getCurrentPageShapes()`/selection consumers in the app either `.filter(s => s.type === 'node')` or early-return on `s.type !== 'node'` (paintStuck 2002). The Edges overlay (NodeShape.tsx:243-278) only looks up ids derived from `shapeIdFor(e.from/e.to)` against backend edges (256-259), so it never touches foreign shapes. Edges is the screen-space pattern to copy if circles need connector lines: a useValue block that touches `editor.getCamera()` to subscribe (246), computes `editor.getShapePageBounds(id)` (258-259) and `editor.pageToScreen({x,y})` (264-265), rendered as a pointer-events:none SVG at zIndex 200 (270).
  IMPLICATION: There is no global 'all shapes are nodes' assumption anywhere — the second shape type drops in without touching any existing consumer. A 'result → node' link overlay is a copy of Edges keyed by the circle id-prefix instead of getOINFO port math.

- persistenceKey discipline — the one real risk seam for a second shape type
  WHERE: /home/tim/company/canvas/app/src/App.tsx:363-373
  DETAIL: Observed: `persistenceKey="company-canvas-v3"` (372) with the in-repo law (365-371): tldraw persists the store to IndexedDB under this key and VALIDATES every record on load; a snapshot written by an older props schema fails with a ValidationError in TldrawEditorBeforeLoading and white-screens the canvas — the v2→v3 bump happened exactly because `error` was added to node props. Backend is layout-authoritative for nodes (reflects-never-owns), so a key bump only resets the local camera. Inferred (not executed): ADDING a new shape type is an additive schema change tldraw tolerates on old snapshots (they contain no forager records), but once circles have been persisted under the key, later changing the circle props schema — or shipping a build without ForagerShapeUtil registered — would hit the same ValidationError white-screen; I have not verified these load paths by execution.
  IMPLICATION: Ship the circle with a persistenceKey bump v3→v4 (cheap, camera-only loss, follows the documented discipline) and treat the circle props schema under the same BUMP-ON-CHANGE comment. Also note search-result circles WILL persist across reloads via IndexedDB unless the feature actively deletes them (e.g. prune-on-new-search via the loadResults prune, or delete-all-foragers on boot) — ephemerality must be built, it is not the default.

- Minimal path to the forager circle (synthesis)
  WHERE: /home/tim/company/canvas/app/src/ (new ForagerShape.tsx) + App.tsx:364,372
  DETAIL: (1) New ForagerShape.tsx mirroring NodeShape.tsx: `TLBaseShape<'forager', {w,h,resultId,query,title,score,...}>`, static props with T.* validators, Circle2d geometry (export confirmed in installed 3.15.6), HTMLContainer round card with `useValue` zoom gating, no stopEventPropagation on the body (preserves marquee/shift multi-select), indicator = SVG circle. (2) Register at App.tsx:364 and bump persistenceKey at 372. (3) `loadResults(editor, results)` sibling of loadGraph with id prefix 'sr-', update/create/prune filtered to type 'forager'. (4) Batch actions read `editor.getSelectedShapes().filter(s => s.type === 'forager')`, mirroring deleteSelected (useAppController.ts:803-813); programmatic select via `editor.select(...ids)` + zoomToSelection (1261 pattern). (5) Any circle→controller action hook installs via the registryStore setter pattern (registryStore.ts:52-63). All statuses here: Observed for the patterns and fence lines cited; Inferred for runtime behavior of the new shape (nothing was executed — this is a read-only research pass).
  IMPLICATION: Total touch surface: 1 new file + 2 lines in App.tsx + 1 new loader function. Zero edits to NodeShape.tsx, loadGraph, refresh, the store listener, Edges, or any region — coexistence holds because every existing consumer is already type-fenced.

## LANE: handoff

SUMMARY: Generalizing the builder-panel context from one address to a selection set is almost entirely a bridge-side block-builder change. Evidence: (1) run_turn already takes context_block as an opaque string prefixed onto EVERY turn's prompt (ui_claude_session.py:80-83) — set semantics need no session-layer change. (2) Mid-conversation context updates are supported by construction: one `claude -p` subprocess per turn with --resume carrying history (lines 57-69, 84); the briefing is first-turn-only but the context block re-folds fresh each turn, so a changed selection just changes the next prefix — note the blocks compound in the resumed session's history, so per-item stanzas should stay small. (3) The current handler (bridge.py:1083-1126) takes singular `address`, folds only what_this_is + how_to_use at 600 chars/leg (~1.3 KB/address, blast_radius deliberately excluded), with a per-address try/except degrade already shaped right for per-item folding in a set. (4) Hard ceiling: the prompt is a single argv arg to claude -p (~128 KiB Linux MAX_ARG_STRLEN — background OS knowledge); repo conventions (600/leg, 4000 result cap, 6000 WALK_MAX_CHARS) put the sensible operating range at single-digit KB, i.e. tens of addresses, never raw file bodies (the builder reads files itself and holds the company MCP). (5) address_help (suite.py:2871-2962) is the generic per-item resolver with legs_present/notes for honest gaps; malformed addresses raise and the bridge guard absorbs that per item. (6) Corpus records fold as {source_address (≤200 chars by the G18 gate, corpus.py:140-146), output} — e.g. repo_digest's {digest, kind} 1-2-sentence payloads keyed code://<path>; query_corpus → ranked addresses → read_corpus_record hydrates the set. (7) FE is single-select today (indicated: string|null, useAppController.ts:150; ClaudeChat.tsx:43 sends singular address; one chip at 94-100) — widening means addresses[], a chip list, and leaving the build-this/intent-at seam single-locus unless the wire is separately widened.

- Context fold-in point is a single opaque string prefixed onto EVERY turn's prompt
  WHERE: /home/tim/company/runtime/ui_claude_session.py:72-83
  DETAIL: Observed: run_turn(prompt, *, session_id, context_block, should_stop) builds `full_prompt = f"[Operator context — what Tim is pointing at]\n{context_block}\n\n{prompt}"` (lines 80-81) whenever context_block is truthy — this is independent of session_id. context_block is already an arbitrary string with no shape assumptions, so generalizing from one address to a selection set requires ZERO change to run_turn's signature: the bridge just builds a longer block (one stanza per selected address).
  IMPLICATION: The generalization seam is entirely in the bridge's block-builder (bridge.py:1115-1126), not in the session layer. A list of {address, content} folds as concatenated stanzas in that one string.

- Mid-conversation context updates ARE supported by construction — one subprocess per turn, --resume carries history, context re-prefixed fresh each turn
  WHERE: /home/tim/company/runtime/ui_claude_session.py:13-14, 57-69, 82-84
  DETAIL: Observed: each turn spawns `claude -p <full_prompt> --output-format stream-json ...` (lines 57-64, Popen at 84); `--resume <session_id>` is appended when a session exists (65-66); the PANEL_BRIEFING system-append rides ONLY on the session-opening turn (`system_append=None if session_id else PANEL_BRIEFING`, line 83), while context_block folds into the prompt on EVERY turn including resumed ones (80-81). Module docstring states the model: 'one subprocess per turn; --resume carries the conversation; Claude Code owns the session state, we hold only the id' (13-15). So changing the selection between turns simply changes the next turn's prefix — the session sees a sequence of differently-prefixed prompts. Inferred (not executed here): each prefixed prompt persists into the resumed session's history, so context-block bytes COMPOUND across turns in the session's token budget — an argument for small per-item stanzas (digests/addresses, not raw bodies) and for not re-sending unchanged selections verbatim.
  IMPLICATION: Selection-set semantics per turn need no new mechanism: the bridge accepts `addresses: [...]` in the body, builds the stanza block per turn, done. The cost model is cumulative-across-turns, not per-turn-only.

- Current /api/claude/turn handler takes ONE address and folds only TWO of the three help legs, each truncated to 600 chars
  WHERE: /home/tim/company/runtime/bridge.py:1073-1135 (route dispatch at 1484)
  DETAIL: Observed: body is {prompt, session_id?, address?} (1083-1085, singular `address`). Lines 1116-1126: `h = SUITE.address_help(address)`, then `bits = [f"Address: {address}"]` plus a loop `for leg in ("what_this_is", "how_to_use")` with `json.dumps(v, default=str)[:600]` per leg — note the how_to_change/blast_radius leg is deliberately NOT folded into the panel context. Per-address context today is therefore ≤ ~1.3 KB. The whole address_help call is wrapped in try/except → on failure ctx becomes the honest string `f"Address: {address} (help bundle unavailable: {type(e).__name__})"` (1125-1126).
  IMPLICATION: The per-item degrade pattern for a selection set already exists: keep the try/except PER ADDRESS so one bad address yields one 'unavailable' stanza instead of killing the whole block. The 600-char-per-leg cap is the existing repo precedent for per-item context budget; N selected addresses ≈ N × ~1.3 KB.

- Hard transport ceiling: the prompt (context block included) rides as a single argv argument to claude -p
  WHERE: /home/tim/company/runtime/ui_claude_session.py:58 (cmd list), :84 (Popen, no stdin)
  DETAIL: Observed: `cmd = [CLAUDE_BIN, "-p", prompt, ...]` — the full prompt is one argv string; nothing is piped via stdin. Background OS knowledge (not repo-observed, not executed): Linux MAX_ARG_STRLEN caps a single argv string at 128 KiB, with ~2 MB total argv+env (E2BIG). Repo-observed sizing precedents that sit far below that: 600 chars/leg in the bridge fold (bridge.py:1123), result event capped at 4000 chars (ui_claude_session.py:114), WALK_MAX_CHARS=6000 per file slice fed to the digest role (corpus.py:251), tool-detail lines capped at 160 (ui_claude_session.py:112).
  IMPLICATION: A selection-set block has a hard ~128 KiB ceiling via the current -p transport, but the sensible operating range per the repo's own conventions is single-digit KB — tens of addresses at ~1.3 KB each fits comfortably; raw file bodies do not belong in the block (the builder can read files itself; it holds the company MCP per lines 46-64). If a set ever needed >128 KiB, the transport itself would have to change (stdin) — flag, don't assume.

- Suite.address_help bundle shape — the per-address contribution and its degrade contract
  WHERE: /home/tim/company/runtime/suite.py:2871-2962
  DETAIL: Observed: returns {address, what_this_is, how_to_change:{scope, blast_radius, note}, how_to_use, legs_present:{...}}. Leg 1 what_this_is = registry title / 'represents <feature-id>' / '(unregistered)' via _describe_ui_address (2902; format at 2860-2869). Leg 2 how_to_change = resolve_scope + blast_radius, guarded so internal failure becomes a legible note (2907-2918). Leg 3 how_to_use = authored howto or None (2921). legs_present makes the degrade explicit per leg (2943-2955). A MALFORMED address raises at the S0 grammar gate (parse_ui_address, 2899) — that raise is what the bridge's per-address try/except absorbs. Docstring explicitly says 'GENERIC over any address... no per-element branch' (2896-2897). A presentation pref is applied last (2962) so the bundle reflects learned per-address shaping.
  IMPLICATION: For a selection set, address_help is already the per-item resolver: call it once per address, fold what_this_is + how_to_use per stanza (the bridge's existing leg choice), and let legs_present/notes carry honest gaps. blast_radius is large (co_reference/structural/semantic edge lists, 2934-2936) — its current exclusion from the panel fold is the right default for multi-address blocks too.

- What a corpus record can contribute: get_content returns the full record dict; `output` is the content payload, `source_address` is a guaranteed-short key
  WHERE: /home/tim/company/runtime/corpus.py:122-205, /home/tim/company/store/fs_store.py:200-201
  DETAIL: Observed: read path is head → get_content (corpus.read_record, 197-205); fs_store.get_content is `json.loads(...read_bytes())` — arbitrary JSON (200-201). The CAS record shape is {source_address, output, kind, model, projection, lineage, **extra} (corpus.py:165-173). The G18 boundary guard FORCES source_address ≤ 200 chars with no newlines ('content goes in output; source_address names WHAT the record describes', 140-146) — so the address half of an {address, content} pair is short by contract. Example output shape: repo_digest records carry {digest: '1-2 sentence summary', kind: 'code|doc|config|test|data'} (/home/tim/company/roles/repo_digest.py:8-25), keyed `code://<path>` by ingest_paths (suite.py:10051-10059). Retrieval to BUILD a selection set exists: query_corpus(text, space, k) → {query, space, ranked:[{address, score, ...}]} (suite.py:10256-10272), then read_corpus_record(address) hydrates each (10232-10237, honest None if never written).
  IMPLICATION: A selection-set item sourced from the corpus folds naturally as `Address: <source_address>` + the record's `output` (a digest is 1-2 sentences — ideal stanza size). The {address, content} list shape maps directly onto (record.source_address, record.output); ui:// items map onto (address, address_help legs). One stanza grammar covers both.

- Frontend selection state is SINGLE-valued today — string | null, mirrored by a ref; the turn body sends it as singular `address`
  WHERE: /home/tim/company/canvas/app/src/useAppController.ts:150-151, /home/tim/company/canvas/app/src/regions/ClaudeChat.tsx:43, 94-100, 106-118
  DETAIL: Observed: `const [indicated, setIndicated] = useState<string | null>(null)` with `indicatedRef` for non-stale reads (useAppController.ts:150-151); indicate() clears the prior `.ui-indicated` DOM class before painting the new one (822-848) — single-selection is enforced at the DOM-paint level too. ClaudeChat sends `address: indicated || undefined` (ClaudeChat.tsx:43), renders ONE chip (94-100), and the 'build this' exit requires `indicated && lastCc` (106-118) — the build-intent mints AT the single indicated locus via mintBuildIntent → /api/intent-at.
  IMPLICATION: Generalizing to a set touches three FE points: (1) the controller's indicated state (string|null → a set, or an additive selection list alongside the primary locus), (2) the body field (address → addresses[]), (3) the chip row (one chip → a chip list with per-chip clear). The build-this seam stays single-locus unless the wire's intent-at is also widened — that is a separate, gated seam (bridge.py docstring 1077: panel is OPERATOR FACE ONLY, plan-mode; mutations exit only through the WireRequest gate).

- Recommended fold grammar for a list of {address, content} — extend the bridge's existing stanza pattern, not a new format
  WHERE: /home/tim/company/runtime/bridge.py:1119-1124 (the stanza precedent), /home/tim/company/runtime/ui_claude_session.py:80 (the header precedent)
  DETAIL: Inferred from the observed precedents (I have not executed a multi-address turn): the existing single-address block is already 'Address: <addr>' + 'leg: <json≤600>' lines. The natural set generalization is one such stanza per item, blank-line separated, under the existing '[Operator context — what Tim is pointing at]' header — with the header wording widened for plurality. Per-item budget = the existing 600-char-per-leg cap; per-block budget = a count cap (the repo's bounded-batch precedent: ingest_paths max_files=50, suite.py:10053) rather than silent truncation of the set (no-silent-failure floor: if the selection exceeds the cap, say so in the block or fail loud, never drop items silently).
  IMPLICATION: Bridge change is localized: `addresses = b.get("addresses") or ([b.get("address")] if b.get("address") else [])` keeps the singular field backward-compatible, then loop the existing guarded stanza-builder per address. ui_claude_session.py and ClaudeChat's stream-reading loop need no change at all.