# THE UNIVERSAL PROJECTION — the seed's purpose, stated + the corrected laws
**Tim Geldard's object; all derived work attributed to him. Captured 2026-06-12 night, his words
steering; build mandate: "I've given everything you could need... it is free universal rendering of
exactly the same points... the actual equation for universal projection, it's just building
something around it. So that's up to you." He is asleep; the loop builds.**

## The two corrections (binding)
1. **TYPES ARE ANGULAR DIVISIONS** — the polygons of the integer division. NOT boxes tiled outside
   (that makes mirrors of the space). Type birth: data pressing into the space with no legal sector
   → pressure threshold → **n increments and ALL sectors redistribute evenly** (the water law;
   global re-division, never a bolt-on slice). Type death = the inverse. The ~20/80 threshold
   governs the redistribution trigger. Scale-growth (rows/columns, nesting) vs type-growth (angular)
   = different axes BUT coupled by the lock: 2π/n = (1/n)^k binds them — "is that kind of the same
   thing?" → yes, two faces of one re-division, held commensurate by the equation.
2. **THE TIMESTAMP ITSELF IS THE TIME-ADDRESS** — not the journal (the journal only references
   time). Self-ordering (depends on nothing but itself), absolute where space addresses are
   frame-relative ("just a face"). And it is JAMPACKED WITH CYCLES: year/month/day/hour/minute =
   nested modular wraps = phase coordinates stacked on the one shared axis. Everything Tim and the
   system do is relative to the same point of time → THE CENTRE OF THE PROJECTION IS NOW.

## The root equation, corrected form (his flash, verified)
x = 2π·n⁻¹: side-of-division = arc-of-division ⇔ the circle is UNITY WRAPPED (circumference 1,
radius 1/(2π) — the reciprocal phase scale lock). Holds at EVERY n simultaneously → invariant for
recursive nesting. Linear divisions ≡ angular divisions forever once the circle is the unit wrap.

## The projection (free, mechanical — no model calls, no layout engine)
For every store record, position is a READ of data it already carries:
- **θ (angle) = kind** → sector index i of n registry types: θ = 2πi/n (+ within-sector sub-kind
  offset). The type registry IS the angular geometry; a new type row re-divides the screen.
- **r (radius) = time** → distance from NOW (the centre): r = f(now − ts), log-scaled (recent =
  inner rings; rings = the inscribed circles). Cycles render as ring-phases (day/session/week).
- **depth (zoom) = nesting** → recursive subdivision; descend = the dual sites legalize (centre of
  a cell at depth k IS a corner at depth k+1 — verified arithmetic).
- The lock keeps angular and radial cell-size commensurate at every n → congruent cells at every
  scale ("any movement has the same shape and properties in every possible move").
- Semantic residual (within-cell arrangement by embedding) = later refinement, not required for v1.

## Build slate (overnight; the floor holds; both faces; registry-is-truth; honest status)
S1 backend `/api/projection`: walk the corpus index → {address, kind, ts, lineage} → (θ, r, depth)
   per the equation; sectors from a declared registry of kinds. Pure read.
S2 FE lattice view (desktop + mobile): polar lattice centred on NOW; sectors + rings drawn; records
   as points at computed positions; tap → existing inspect/position bundle; zoom = depth.
S3 The sector registry as rows (types→sectors; adding a row redistributes — live).
S4 Phase faces on time (cycle coordinates from timestamps; ring-phase rendering).
S5 Wire to the forager seam: select → builder context (the existing S1 panel seam).
Ratified so far: store/recall = opposite verbs. Corrected: types angular; timestamps the time-address.
Open for Tim: the 20/80 dial placement; semantic residual; promotion gesture.
