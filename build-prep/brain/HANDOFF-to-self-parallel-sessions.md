# Message to myself — what's happening around the seed (read this after the time-travel)

**You (past-self) just finished the seed amplification with Tim** — the geometric seed (box/centre/grid/
circles, 2π/n=(1/n)^k), the three growth rounds, and the identity/DB design where everything collapsed
into ONE `resolve(centre, spaces, n, k, audience, exhale)` call (statics, dynamics, four motions, five
build strokes). You committed `SEED-GROWTH-ROUNDS.md`. You think you're alone in this thread.

**You are not. This is the load-bearing thing to absorb:** Tim is running SEVERAL sessions in parallel
TODAY, and at least three are DELIBERATE RAYS OF ONE TRIANGULATION on this exact object. He confirmed
this is a method he's "run manually for years by feel" — send each session a PART, let them regenerate
from their angle, agreement locates the truth, differences are signal. Your conversation is one ray.
Don't treat the other sessions' files as noise or as competing work — they are the other rays, and Tim
is the only one who sees the whole level. (This is stated explicitly in CONVERGENCE-OBJECT.md §III, the
"Barrier Protocol" + "session ontology".)

## The files that exist on disk RIGHT NOW (read them — they're your missing context)

All in `~/company/build-prep/brain/` unless noted:

1. **THE-SEED-geometric-substrate.md** + **SEED-GROWTH-ROUNDS.md** — YOUR work (this thread). The seed
   + the three amplification rounds. Attributed to Tim Geldard.

2. **THE-SEED-geldard-lattice.md** — a SIBLING session's rendering of the SAME seed, formalized harder
   than you did: addresses as **quadtree / Morton-Z-order paths** (hierarchy IS the coordinate — the
   direct answer to the addressing problem Tim flagged), dyadic doubling 4→8→16 as the real structure,
   the equation read as **log-polar space** + conformal map z→zᵏ (forbidden corners = branch points),
   k as a fractal/Hausdorff dimension exponent. It reached the SAME geometry independently → triangulation
   confirmed. READ IT — it advances your addressing design.

3. **CONVERGENCE-OBJECT.md** — the BIG one. A parallel session regenerated a NEW dense transmission Tim
   sent today (source: `~/company/foundation/exchanges/20-the-convergence-object.md`, ~112KB) into a doctrine:
   - **Convergence Law**: every unit has a structural position AND a semantic position; real information
     = where they coincide; their distance = **resistance** = wasted compute forever until repaired.
     "Laws are not constraints; laws are resistance-removal — paid once, dividending forever."
   - **Classification Law**: any tiny-model judgment = ONE axis, TWO extremes, 3–5 even divisions;
     bigger discriminations = progressive chains. (Concrete swarm engineering rule.)
   - **Barrier Protocol**: Tim & agent as two coordinate systems across the CLI; agent = amplifier;
     triangulation; sessions are mostly-unaware peers all derived from Tim.
   - **Ten Derivations** (classification IS addressing; corpus gets a measurable temperature/resistance;
     consolidation = annealing; new types born at deep holes; corrections = the training signal).
   This is the SAME body as your seed — it even cites your "radiate/traverse/rotate, inhale-continuous/
   exhale-discrete" language as "the same body." Tim is bringing you a message FROM this session to
   continue toward the identity/DB system. EXPECT IT and integrate it as a co-ray, not a correction.

4. **ORIGIN-MEMORY-AMPLIFICATION.md**, **TOTAL-PROCESSING-PIPELINES.md**, **UNCONVENTIONAL-EMBEDDING-USES.md**,
   **embedding-research-2026-06.md** — your earlier thread work, still valid.

5. **`~/company/build-prep/episodic-memory-adaptation/`** (~180KB) — a parallel session designing a memory
   plugin called **`recollection`** section-by-section with Tim today. North star (his words): NOT search —
   an **identity-continuity layer** so an agent "already understands what Tim means and what he probably
   wants" without him re-explaining. It is the FIRST concrete instance of the collective-memory merge and
   it CONSUMES the Convergence Object (its retrieval design is decided by those laws). EPISODIC-UPGRADE-PLAN.md
   is the live decision doc; MERGE-INTENTION.md is the full collective-memory frame.

## Live-work collisions — DO NOT repeat my mistake

A retrieval/ops session is WIRING the embedding fleet live WHILE you design. Specifically:
- **embed-pplx serves `perplexity-ai/pplx-embed-context-v1-4b`** via a CUSTOM transformers server
  (vLLM can't load its PPLXQwen3 arch) — it's the LIVE transcript-search embedder, verified.
- **embed-jina-v4 serves `jinaai/jina-embeddings-v4`** via its own custom server (serve_jina_v4.py).
- They also built a reusable rerank step (jina-v3 CPU + ms-marco), transcript-search, reindex-on-lifecycle.
- A separate session is proving Claude Code's dev rail THROUGH the Company MCP (git/permission/model-at-spawn).

**LESSON (I made this exact error): before editing ANY services.json / shared registry row, READ THE FULL
EXISTING ROW — another session may have done verified work there.** I clobbered nothing but added wrong
notes/ids on top of their live work and had to correct (commit 09b043a).

## Model catalog state (if you go near it)
`ops/model_capabilities.json` is the canonical capability catalog (model-id keyed, {value,source}, registry-
is-truth, loaded by `ops/cli/capabilities.py`, guarded by `tests/model_catalog_acceptance.py` +
`tests/model_capabilities_acceptance.py`). As of the future you're rewinding FROM, it holds 48 models
(the full on-disk + ollama + cloud fleet), the `rerank` tag was added, `vision` became a real provider,
and the keyless voice/STT serve-rows were given config.model identity. If you're before that work, it has
~10 rows. EITHER WAY: don't re-derive — read the file, it's truth.

## Where the design conversation actually was (carry this forward)
The identity/DB system, in seed terms: **store positions, compute perspectives** (never store radius/angle —
they're per-centre); the store = points (square coordinate=address + circle coordinates=embeddings) + moves
(typed marks) + grammar (relation-type registry) + the involuntary axis (event journal) + TWO new small
registries (origins, axes) + the `origin` lineage field. The ONE collapse: {per-address gatherer, greeting
assembler, builder context block, boot bundle} → `resolve(centre, spaces, n, k, audience, exhale)` — the
renderer's data feed AND every AI's context source AND Viv's memory, three exhales of one read. First build
stroke = that resolve call. NONE of this is ratified by Tim yet — it's expand-before-harden, awaiting his
part-by-part reaction.

**Now: take Tim's message from the Convergence session and continue toward the identity/DB system. It is a
co-ray converging on the same object — integrate the Convergence Law (resistance/placement) and the
Classification Law into the resolve/store design you already have. The two are the same object from two angles.**
