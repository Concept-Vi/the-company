Both corrections resolved decisively. Correction #2 is dead: `writeDirections` only POSTs to `/api/territory/write` (the route-back) and emits rerender — no `run_turn`/`claude-turn` generation call. The map's "GENERATE is the one unbuilt rung" is correct; I withdraw the under-credit claim. Correction #1 is now severity-confirmed: the composition runtime files exist NOWHERE on disk (not in any of ~30 repos, not under /mnt/c) — they live only as textual references inside research notes. The build is told to edit `component.schema.json`, a file that does not exist anywhere reachable.

I have my evidence. Here is the critique.

---

# COMPLETENESS CRITIQUE — what the V-surface map MISSED, under-specified, or assumes-but-cannot-verify

I verified the map against the actual filesystem (not the reports). The backend half is exceptionally well-grounded — nearly every `suite.py` / `contracts` / `kinds` line checked out exactly. But three classes of gap would break this build. Tiered by severity.

## TIER 1 — LOAD-BEARING + UNVERIFIABLE (the map's one genuinely dangerous blind spot)

**T1.1 — The composition runtime does not exist on any reachable filesystem, yet §1/§2.A/§4 stamp it "BUILT (runtime)" with exact line numbers.**
The map fuses **three separate filesystems** under one confident "BUILT":
- **company** (`/home/tim/company`) — the backend spine. VERIFIED present: `address.py` SCHEMES, `cognition.py:842 resolve_address`, `suite.py` (ModeSpec:155, MODE_REGISTRY:2138, _CORPUS_REGISTRIES:360, _write_registry_file:9840, _chat_context:2975, THOUGHT_SHAPES:2385, CoaOption:111, CoaFraming:117), mark-type live/empty (suite.py:257-264), `kinds/raw.py:36-38`, `node_type.py:10` RenderMode, `node_record.py:58`, `ui_info.py`. All real.
- **counterpart** (`/home/tim/repos/counterpart/design`) — a *separate repo*. This is where `palette.js`, `brand-icon.js`, `surface.js`, `BACKLOG.md` actually live. The map cites them with no repo qualifier, as if they were in `company`. They are NOT in `company` (confirmed: `find /home/tim/company -name palette.js` → empty).
- **composition runtime ("overlay")** — `factory-mount.js`, `connection-mount.js`, `factory-ops.js`, `v-symbol-sockets.js`, **and `component.schema.json`**. These exist **NOWHERE**. I searched all ~30 repos under `/home/tim/repos` and `/mnt/c` (maxdepth 6). The only hits for "factory-mount" / "connection-mount" / "overlay/src/components" are **textual references inside research notes** (noticeboard items, DISCOVERY_LOG, GALLERY_FULL_PICTURE.md, and `counterpart/design/docs/command/V-SURFACE-DNA-RESEARCH.md`).

**Why this breaks the build:** §4 instructs "**The ONE required schema change:** add `connection` to `component.schema.json` sockets:77-103." That file is not on disk. You cannot edit a file you cannot see. The map's own §3 admits this in one place ("the composition runtime is not present in the accessible filesystem") but then proceeds to give §2.A, §4, and seams S1–S4 confident `connection-mount.js:71-270` line citations as if BUILT. **Every claim about sockets, `mountConnection`, `setSocket`, `parentLeap`, and the recursive-nesting story rests on a repo that isn't here.** Correct label: v-slot-socket is **DESIGNED / claimed-by-another-session, NOT verifiable here** — the same tier the map honestly applied to `decision://`, but did NOT apply to the socket runtime.

**The build needs, and the map omits:** a resolution of WHERE the composition runtime lives and whether this session/build can even reach it. If it's on a machine/branch not mounted here, v-slot-socket is an **unflagged hard cross-repo dependency** — the build must first mount or clone that repo, and "the one required schema change" may be in a file controlled by a different stream entirely.

## TIER 2 — FORWARD GAPS (mechanisms assumed; things that will break at build time)

**T2.1 — Scheme-ordering hazard, never flagged.** I confirmed `address.py` SCHEMES has exactly 17 entries and the resolver **raises on unknown scheme** (fail-loud). The map's recipe (§1, §2.G) says "add `decision://`/`window://`/`archetype://` to SCHEMES + resolver branch" but never flags that doing these in the wrong order is a hard failure: **add a scheme label before its resolver branch exists → every address of that scheme raises across the entire surface.** This is an ordering invariant the build must obey (resolver branch FIRST, or same commit), and the map presents the steps as an unordered set.

**T2.2 — The decision write-back targets a type that does not exist.** Seam S16 says the "take" writes decision STATE (`state→decided`) to a `decision://` address via `territory_write`. But (a) `decision://` is not in SCHEMES, has no resolver, no `Decision(BaseModel)`, no `DecisionRegistry` — all confirmed absent; and (b) the only built route-back, `fork-brain-core.js:writeDirections`, POSTs to `/api/territory/write` and the existing HOOK 2 resolves to **`suite.mark` at the sub-address** — i.e. it writes a **mark/annotation**, not a typed decision-state transition. So the entire "the take IS the write-back" closure (the heart of `DECISION-SURFACE-BUILD.md`) is **unbuilt at its target**: the type, the scheme, and the state-writing route-back leg all need building before S15/S16 are real. The map lists the schema as "DESIGNED-not-built" but does not connect that the write-back leg it credits as built (mark) is the *wrong* leg for a decision-state take.

**T2.3 — "Recursive nesting is free, zero mechanism changes" (§4, S1) is the single highest-risk assumed-but-unverified claim.** It asserts "V → radial-wheel → nested-option-surface → detail-surface is structurally available today, with zero mechanism changes." This is **doubly unverifiable**: it's an *inference* about runtime behavior (depth recursion, teardown, z-index/positioning at depth, MutationObserver orphan-guard interactions, the `OPEN` WeakMap at nested levels) AND it rests on `connection-mount.js`, the file that doesn't exist here. Pattern-matching from a report is not verification that nesting works at depth 3. No depth-stress was researched. This should be labeled **inferred, NOT verified** — exactly the distinction the global rules demand.

**T2.4 — The surface is entirely happy-path. No failure/empty/loading state design.** Every dimension describes the success render. Missing: what the decision-card shows when `explanation_source` (an address resolved through the RHM brain) **fails to resolve or the brain errors**; the empty inbox; the loading state while `run_turn` streams; a `gallery:write-error` already exists in code (`fork-brain-core.js:88`) but no dimension specifies how the surface renders it. The map's own cited "fail-loud" law (no silent fallbacks) means these states are *required*, not optional — and none are designed.

**T2.5 — Operator concurrency + identity/auth is unaddressed.** Seam S2 leans on the Supabase cascade `user → project → global`. But: **who is "user" on a single-operator surface?** No auth/identity model is stated anywhere in the map. If two sessions (the builder self-registering as a `cc_channels` member per S24, plus Tim) write the same socket/decision concurrently, the SELECT-then-UPDATE path and Realtime broadcast have a last-writer-wins race that's never discussed. For a surface whose whole premise is "the take IS the write-back," concurrent takes / stale-read-then-write is a real correctness gap.

## TIER 3 — CORRECTIONS / LOWER-VALUE

**T3.1 — WITHDRAWN: I initially suspected the keystone was MORE built than the map claims.** Decisive read of `fork-brain-core.js:writeDirections` (lines 75-91): it only POSTs to `/api/territory/write` and emits `gallery:rerender` — **no `run_turn`/`/api/claude/turn` generation call.** The route-back is the *annotation/mark* leg, not generation. The map is **correct**: GENERATE (direction→**generate**→write) is "the one unbuilt rung" (noticeboard item-534ef183:42 confirms verbatim, and `SPEC-direction-to-generate-wire.md` is a spec, not built). The map's §2.H credit ("joins 6 BUILT pieces, the router is net-new") stands.

**T3.2 — Minor citation drift.** `BACKLOG.md` is in **counterpart**, not company (the map cites bare `docs/BACKLOG.md`). The "★★ the work must be live-composed" rung the map references is real and at **:113 / :115** as cited — the content is accurate, only the repo qualifier is missing.

**T3.3 — `bridge.py` has MORE built than the map's strict reading implies (in the map's favor).** The map says "BRIDGE_ROUTES has ZERO channel routes" — true for *channel* routes. But `/api/territory/write`, `/api/decision`, `/api/resolve`, `/api/propose`, `/api/claude/turn`, `/api/corpus-query` ARE all in BRIDGE_ROUTES (verified). So the operator/decision plumbing is further along than a casual read of the map suggests; the genuine channel gap (the 5th wire `op="recall"`) is correctly identified.

## WHAT THE MAP GOT RIGHT (do not soften)
Confirmed by primary evidence: (1) **two un-unified channel stacks** — `cc_channels.py` and `session_channels.py` both exist, both define create/add/remove/archive, no unification layer; (2) **`op="recall"` is genuinely absent** from `mcp_face/tools/cc_channel.py` (ops stop at create/list/add/remove/archive/list/send/broadcast/mail — no recall); (3) **no runtime reads `layouts.json`** and `composeArchetype` exists **nowhere** — the generic compositor is a true net-new gap; (4) **`--vi-mode-primary` exists nowhere** in code (only in a noticeboard claim) — the map's "claimed-not-verifiable" label on the noticeboard's "verified" assertion is itself correct and well-disciplined; (5) the **gold-drift three-hexes** is real: `palette.js:20 #E3C421` vs `brand-icon.js:35 #D4AF37` vs `company tokens.json:8 #e6ab5c` — all three confirmed.

## THE ONE-LINE HEADLINE
**The map's backend spine is verified-real and excellently cited, but it presents the composition runtime (v-slot-socket: `factory-mount.js`, `connection-mount.js`, `factory-ops.js`, and the `component.schema.json` the build is told to edit) as "BUILT (runtime)" with exact line numbers — and those files exist on NO reachable filesystem (searched ~30 repos + /mnt/c), only as text inside research notes. v-slot-socket and all of §4 + seams S1–S4 are an unflagged cross-repo dependency, not a verified foundation; the build cannot make "the one required schema change" to a file it cannot see.**

Relevant verified paths: `/home/tim/company/contracts/address.py`, `/home/tim/company/runtime/cognition.py`, `/home/tim/company/runtime/suite.py`, `/home/tim/company/surface/app/src/rhm/RightHand.tsx`, `/home/tim/company/surface/app/public/gallery/fork-brain-core.js`, `/home/tim/company/runtime/{cc_channels,session_channels}.py`, `/home/tim/company/mcp_face/tools/cc_channel.py`, `/home/tim/company/build-prep/the-one-application/DECISION-SURFACE-BUILD.md`. Cross-repo (NOT company): `/home/tim/repos/counterpart/design/ui/{palette,brand-icon,surface}.js`, `/home/tim/repos/counterpart/design/docs/BACKLOG.md`. Absent everywhere: `factory-mount.js`, `connection-mount.js`, `factory-ops.js`, `v-symbol-sockets.js`, `component.schema.json`.