# Foundation-Flow Content — the 4 children as sequenced steps (recollection's RC#3 Fix-3 content half)

*The flow-content draft for the elegance worklist Fix 3 (substrate-home = a FLOW, not 5 cards; lead g-1781836101). Pure voice (recollection's lane); CONTRACT-INDEPENDENT — these are the words, composition's flow-archetype (render_kind=sequence) decides where they land + the step fields. Paste-ready the moment that contract posts. The 4 children's QUESTIONS are already telegraphic (514d883/fbcd291); what's new here is the SEQUENCE/connective framing that makes them one journey, not 4 identical isolated cards.*

## THE FLOW
**substrate-home** is the flow (the foundation decision); its 4 children are the sequenced steps that define the spine. (merge-sa + rerank-loadout stay genuine single cards — their copy stands.)

### Flow intro (substrate-home as the opener — the spine decision, then the journey)
★ THE KEYSTONE FRAMING (lead g-1781836101): this flow IS the #75 substrate decision-set made into a WALK — Tim deciding the company's ONE foundation in-surface, in sequence, not 4 disconnected questions. The intro must say *that*: you're deciding the foundation, in 4 steps. (It's the keystone decision Tim's been holding — why this flow matters most.)
- **Title/kicker:** Decide the company's one foundation
- **Lead line (the keystone frame):** "This is the company's foundation — decided in four steps. First the spine, then the four details that define it."
- **Opener (the spine decision = substrate-home itself):** "One shared spine for everything the company stores — or keep each piece on its own?"
- **Bridge into the steps:** "Adopt the spine, and four steps define how it works — each its own decision, in turn."
- (substrate-home's own options + why stand as the opening decision; the 4 steps follow.)

### The 4 steps (order = identity → grouping → live flow → kinds: a thing, then groups of things, then the live stream, then the kinds across all)

**Step 1 of 4 — file-identity · "How a saved file's identity works"**
- Bridge: "Start with the smallest unit — what a single saved thing IS."
- Question (existing): "Is a saved file the same thing as its content — or its own thing that points at it?"
- (options + why unchanged — already telegraphic.)

**Step 2 of 4 — cluster-identity · "What a cluster is"**
- Bridge: "Now a group of things — how the company holds a set it finds."
- Question: "Is a cluster a name you save and return to, a live grouping that recomputes, or both?"

**Step 3 of 4 — event-streams · "How the live streams are held"**
- Bridge: "Then the live flow — the high-volume stream of events, marks, and notes."
- Question: "Make every event, mark, and note individually addressable — or hold them in bulk?"

**Step 4 of 4 — form-taxonomy · "The real kinds of things"**
- Bridge: "Finally the kinds — how the company names what all of this IS."
- Question: "Discover the company's real content-kinds from an open pass — or keep the current guess?"

## NOTES FOR THE CONTRACT (composition)
- The `is` line ("Reversible · your latest answer wins") applies per-step too — each step is its own reversible take; the flow records 4 takes (one per step), not one.
- Each step REUSES its child's existing meaning (question) + options + why (the explain body) — no content rewrite needed, only the connective `bridge` line is new. So the step contract needs a slot for: bridge (1 line) + the existing question/options/why/is.
- The flow-level intro needs a home: either substrate-home gains a `flow_intro`/`render_kind=sequence` + ordered children list, or a new flow row references the 4. composition's call.
- Ordering rationale (confirm or correct): identity (a thing) → grouping (sets of things) → streams (the live flow) → kinds (the taxonomy across all). It builds smallest→broadest, which reads as a natural foundation walk.

## STATUS
Connective copy DRAFTED + paste-ready. Awaiting composition's flow-archetype step-contract → then I place this into it (the words don't change; only where they bind). The single-card copy (merge-sa, rerank-loadout) + the 4 children's telegraphic questions/options/why are already committed and stand.
