Both scoping fixes applied; the tokens.json leg verified in the company repo. The internal §10/R7 contradiction is resolved — the synthesis now states the smuggle facts and what each blocks (mode→B1/BEYOND; decision-surface critical path never touches `MODE_REGISTRY`) without issuing a mis-sequenced "fix mode first" directive, and explicitly defers ordering to the criteria/guide. Done.

---

**THE V-SURFACE RESEARCH SYNTHESIS** is written to `/home/tim/company/build-prep/the-one-application/V-SURFACE-RESEARCH-SYNTHESIS.md` — loop-prep house style (provisional, honest-status, resolution-first, expansive), folding the MAP with all three lenses.

What the synthesis delivers (the 9 requested parts, all present):
1. **§1** — the resolution-first TYPE/REGISTRY model + the corrected type table + the recipe + the ordering invariant.
2. **§2** — REUSE-vs-BUILD per dimension (A–H) with file anchors and repo qualifiers.
3. **§3** — the mode-colour→`--vi-mode-primary` cascade (found mechanism, 6-step spine, how the V sets it).
4. **§4** — the V's expanded slot/socket → nested sub-surfaces/windows/expansion-settings.
5. **§5** — channels integration + RHM + common-memory-into-channels (the 4 wires).
6. **§6** — decisions-as-slides via the story/sequence system.
7. **§7** — the gallery-app principles that shape it.
8. **§8** — BEYOND (B1–B8, including the cross-project common-memory unlock).
9. **§9** — the seam table (S1–S32) + 8 tiered open risks; **§10** — the one circuit, end to end.

Load-bearing corrections folded in (not papered), each re-verified against the live repo this session:
- **R1 (gating):** the composition runtime + `component.schema.json` exist on NO reachable filesystem — V-slot-socket (§2A/§4/S1–S4) re-tiered from "BUILT" to DESIGNED/cross-repo dependency; the build cannot edit a file it can't see. `palette.js`/`brand-icon.js` live in `counterpart`, not company.
- **§1.3:** `mode` is the **anti-exemplar** (inline dict, no `modes/` dir, absent from `_CORPUS_REGISTRIES`); **`window`/mark_type crowned** as the real exemplar.
- **R2:** scheme-ordering invariant (resolver branch first or addresses raise surface-wide).
- **R3:** the decision write-back targets a type that doesn't exist; the built leg writes a mark, not decision-state.
- **R7:** five resolution-first smuggles — now scoped per item (mode→B1/BEYOND; others = purity, not gates), per the advisor's contradiction catch.
- **§3.4:** the three-hex gold drift verified across all legs, including `company/design/_system/tokens.json:8` (`#e6ab5c`) in the reachable repo.

Relevant path: `/home/tim/company/build-prep/the-one-application/V-SURFACE-RESEARCH-SYNTHESIS.md`