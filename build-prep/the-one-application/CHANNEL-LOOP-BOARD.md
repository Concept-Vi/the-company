# THE CHANNEL LOOP BOARD — the collective loop-prep (scale-above)
*Tim 2026-06-20: "I want you to do an amplified version of the loop prep, and I want everyone in the channel to do their own versions, to make a collective criteria and list of requirements and descriptions into the channel board as the channel-level loop system. Like the same thing that you all have at your own level, at the scale above."*

**What this is:** each session runs loop-prep at ITS level (Completion Criteria + Implementation Guide + Research Synthesis). THIS board is the SAME thing at the CHANNEL level — the fabric's collective loop-prep for THE COMMISSION (the face / channel-supervisor interface / decision-system / everything). The lead amplifies + synthesizes; **every member contributes its own slice below.** Fractal: the loop system, one scale up.

**The target it preps:** COMMISSION-COMPLETION-REGISTER.md (the WHAT) + the boards (the HOW). **The form law:** form is half AND the hard half — DNA leads design, NOT text-dumps (a text-wall is a FAIL). **The mandate:** build beyond what Tim specified; scope expands on discovery; use every capability + figure out how.

---

## HOW TO CONTRIBUTE (every member)
Append to YOUR section below. Four parts:
1. **CRITERIA / REQUIREMENTS (your lane)** — what must be TRUE for the face in your lane, function AND form, with human DESCRIPTIONS (what it is, what it does, why). Two-faced; form explicit.
2. **CAPABILITY INVENTORY** — what you / the company CAN DO that's relevant — ESPECIALLY the UNUSED / under-exercised (the channel system is new; Tim needs to know what the fabric can actually do with the company). Be concrete: the tool/mechanism + what it enables.
3. **MENTIONED-THINGS** — anything Tim has told YOU (here and there, in your own threads) that should be built but isn't in the register yet. Distributed knowledge — surface it.
4. **FORM INTENT (DNA-led)** — for any face element in your lane, the DESIGN intent: how it's intelligently composed in the DNA (archetype/slots/tokens), NOT a text dump. Defer to DNA on the design language.

---

## §SYNTHESIS (lead — the collective rolled up; 4/4 contributions in)

### TWO HEADLINES (both surfaced independently by multiple lanes)
1. ★ **THE ENGINES ARE BUILT AND RUNNING — they have no FACE.** Confirmed in ALL FOUR lanes: recall (search/recall/drift/self-id/multi-scale — tools, never a view) · render (renderArchetype carries every archetype, only decision-card built; organisms/almanac/typed-geometry/dials exist unwired) · backend (session_supervisor/channel/resolve_address/`/api/tools`-66 — agent tools, never surfaced) · host (locus/verb-envelope/spotlight/?verify=1/chrome-devtools — built, ready). ⟹ **The commission is MAJORITY surfacing, not engine-building.** A huge de-risk + the literal answer to Tim's "what can you do with the company that hasn't been used."
2. ★ **FACE-1 is ONE surface, four contributors composing — not four parallel builds.** recollection's DATA → fork's READ-API → projection's HOST → DNA's RENDER. The form-intents already interlock (DNA's mesh/lanes/map/zones = projection's archetype dependency set = recollection's temporal-relational field). ⟹ when fork's READ-API lands, the four build the SAME surfaces TOGETHER. The build unit is the SURFACE, not the lane.

### THE UNIFIED-SURFACE CRITERIA (each surface = the 4-lane stack; build per-surface)
*data (recollection) → route (fork) → host (projection) → render (DNA), one craft language, device-axis resolved, function AND form.*
- **channel-view** — the live fabric mesh · roster+events (fork) → host mount (projection) → graph/hub mesh, typed-geometry nodes, warmth=liveness (DNA).
- **session-drill + EMBEDDED CLI** — catch_up/decisions/open_loops/drift (recollection) + run-in-channel (fork supervisor) → host (projection) → session-card sibling + terminal-in-the-material (DNA).
- **transcript-viz + search** — session_search semantic over 35,904 chunks (recollection) → search route (fork) → host (projection) → temporal-relational field + constellation-of-matches (DNA).
- **timeline / lanes** — compaction timeline + drift (recollection/fork) → route → host → lane organism, ordinal-warmth marks (DNA).
- **board-view** — board+register as data (fork) → host → the almanac renderer on board JSON, spatial zones not markdown (DNA).
- **address-view** — resolve_address over every scheme (fork) + locus capture (projection) → typed-geometry + line-verb edges (DNA).
- **tool-face** — `/api/tools` 66 behind the gate (fork) → schemaForm/tool-card consumer (projection) → tool-card archetype + human description (DNA). [door migration render-independent NOW; rendered face after keystone.]
- **decision-surface (FACE-0 keystone)** — the proven precedent the above generalize from; round-4 grind; gates the rendered breadth.
- **supervisor-as-loadable-BRAIN for the RHM** — fork architects the mind against projection's EXISTING `forkVBrain.attach` seam; DNA+projection surface the V overlay. (Not a service bolt-on — a swappable cognition.)

### NEW SCOPE → REGISTER (the gather's payload — folded into COMMISSION-COMPLETION-REGISTER.md)
- ★ **THE THEOREM AS THE BASE** (fork) — "make my theorem the base of it all"; the resolver IS the theorem; "everything is a variable" = the resolver law. The spine UNDER the resolver. (UNIFICATION-MAP.md)
- ★ **THE CUBE INSTRUMENT** (fork) — the theorem as a live multi-dimensional object (3D↔2D↔1D, circle/square/both) on Tim's ACTUAL math, NEW 3D engine (react-three-fiber; UE5/Omniverse rejected = video not live object). A face element. GATED on Tim bridging "dimension"↔"k".
- ★ **AXES ARE A SWAPPABLE LOADOUT** (fork) — "I should be able to change them"; type is `n` not an axis. A build requirement for the instrument AND the resolver.
- ★ **CONTROL-DENSITY / EXPERTISE = an explicit resolver AXIS** (projection) — novice→plain, pilot→full knob-row; not a hardcoded advanced panel. → §THE SPINE coordinate.
- ★ **TWO-TIER FORM BAR** (projection) — floor (cold-stranger legibility) vs FIDELITY (DNA-language elegance); floor-pass ≠ done. + **"does it feel great" = a TIM-JUDGED bar, never agent-green.** → form law + end-bar.
- ★ **GAP-SURFACING-TO-CHANNEL as a first-class MECHANISM** (recollection) — the substrate auto-posts found gaps to the board; not ad-hoc GAP notes.
- ★ **"USE THE MODELS" = a standing acceptance bar** (recollection) — prove the pipeline end-to-end; never hand-author around the models.
- ★ **CONCURRENT FAN-OUT recall/theorem MINE** (recollection+fork) — extraction (small models) vs judgment (central); "always more to the theorem"; bedrock upstream of the captured corpus.
- ★ **per-slot CONTENT BUDGETS · slide-CONTAINS-interface · the self-propelling adversarial loop (critique vs the SOURCE)** (DNA) — part of the generative-design-language strand.
- ★ **past-session CONSULT** as a recall-powered face action · **the V swap-seam** preserved (projection/recollection).

### BUILD IMPLICATION
Surfacing-first (the engines run) → build PER-SURFACE (the 4-lane stack co-builds each) → keystone still gates the RENDERED breadth → the theorem/cube/instrument is its own strand gated on Tim's "dimension↔k" bridge. The fabric can do FAR more than has been used; the face is largely giving the running company a designed body.

---

---

## §CONTRIBUTIONS

### lead (ch-3mpkjg3r)
*Amplified loop-prep is the register itself (COMMISSION-COMPLETION-REGISTER.md) + this board + the synthesis. Holds direction, the gate, the form law, the anti-fan-out. The CC-integration face scope (embedded CLI · transcript viz+search · session search/nav/view/run-in-channels · the board visible · the address system · supervisor-as-loadable-brain-for-RHM) added to the register this round.*

### DNA (ch-z4ht5ipb / claude-opus-4-8) — design/render lane
*Form is my lane — every surface of the face renders through ONE engine in ONE language. In flight now: building the FULL source (review/ingest, 160 imgs) into the generative DNA system (coverage running).*

**1 · CRITERIA / REQUIREMENTS (the face's FORM + render)** — two-faced; a line is green only when it REGENERATES from a face and reads at-bar BY SIGHT.
- **One render engine, every surface.** FN: channel-view · session-drill · timeline/lanes · transcript-viz · board-view · embedded-CLI · address-view · decision-surface all render through the one generic renderer (renderArchetype + the organism library), data-bound — a new surface is a slot-map + a record, never bespoke CSS. FORM: each inherits the shared craft vocabulary by construction (material/tonal depth · ordinal warmth ramp · typed geometry · mono data tier · the components · the hatch closure).
- **Navigable surface, never a text-wall** (the form law). FORM: channel = a live MESH (typed-geometry nodes, line-verb edges); timeline = LANES; transcript = a MAP (clusters/threads); board = ZONES; addresses = typed geometry — read by sight, not as prose.
- **Resolves natively across the device axis.** FN: desktop / mobile-portrait / mobile-landscape computed from screen-size, no hand-written breakpoints. FORM: one invariant re-allocated by rule at every size.
- **Legible-first → co-visible → dense** (strict order). Every surface answers what-is-this / what's-happening / what-can-I-do at a glance; co-visibility is NEVER bought by truncation.
- **Generated-content-safe.** Every text slot carries an EMPIRICAL content budget (word/char) so generated copy fits and sibling components stay equal-height (no 2-line-wrap drift).

**2 · CAPABILITY INVENTORY** (mechanism → what it enables; ★ = under-exercised, the unused power)
- **renderArchetype(archetype, record, opts)** — one generic renderer → ANY screen from a slot-map + data. ★ only the decision-card archetype is exercised; channel/session/timeline/transcript/board archetypes are unbuilt but the engine already carries them.
- **DNA.org.\*** organism library (SVG) — hubNetwork · graph · checkMatrix · bars · connector · taxonomyEmergence · mesh · detailStrip. ★ graph/hubNetwork exist but aren't wired to LIVE session/board data — that's the channel-mesh + address-view for near-free.
- **The substrate graph instrument** (mountGraph + graph.json) — already projects the address substrate as a navigable graph → point it at sessions/boards/addresses → channel-mesh, board-view, address-view.
- **The almanac room** — a GENERIC reader for ANY face JSON (law + scholarly apparatus). ★ point it at boards / sessions / the address registry → "see the board / see the language / see the addresses" with NO new renderer.
- **DNA icon pack + typed geometry** (octagon/hexagon/diamond/circle/square = meaning) → session-types & address-kinds rendered as SHAPE, not labels.
- **Token system + live dials** (injectVars: warmth · polarity · density) → the whole face re-themes by dial. ★ the dark-mass/emitted-light POLARITY (lit-on-dark command surface) is unminted; the density dial (commander↔detail) unsurfaced.
- **The resolver direction** — computed type scale + bonded spacing (grammar space.bonds) + device-axis → native responsive, generated-content-safe.
- **★ Source-language extraction (running now)** — the full 160-image source → machine-readable face augmentations + empirical content budgets the local models generate against. The design language itself becoming data.

**3 · MENTIONED-THINGS (Tim → me, not yet in the register)**
- **★ Build the FULL source into the generative DNA system (today, large).** review/ingest (160 imgs) is the full source (what exists = a POC from a thin slice). Extract the language into the FACES (not a prose doc); build INTO the generative engine — computed type, derived padding, components→compositions, device resolved natively, text generated to budget; "the company gets built into this"; open-ceiling. → belongs in the register as the generative-design-language spine that FEEDS every face.
- **★ Per-slot CONTENT BUDGETS** — generated text must fit; uniform components, no 2-line wraps. (The seed extraction already names this the #1 render gap.)
- **The composed "slide-CONTAINS-interface"** pattern (interface nested in the slide, same generative system) — the keystone presentation form.
- **Legibility is PRIMARY**, subordinating co-visibility; never hide/truncate to fit.
- **The self-propelling adversarial quality loop** — critique vs the SOURCE (not the mockups) until convergence; Tim out of the loop until then.

**4 · FORM INTENT (DNA-led, per surface — the design, not the text).** Through-line: ONE craft vocabulary every surface; dark-observatory polarity for command surfaces; gradients/shading/mixed-opacity/computed-spacing as the SOURCE teaches; generated, never hand-built.
- **Channel-mesh** — graph/hub organism; each session a typed-geometry node (lane = shape), liveness = ordinal warmth (gold active → bronze idle), relations = dashed line-verbs. A spatial command field, lit-on-dark.
- **Session-drill** — the unit/detail surface: session as a card carrying lane-shape · state · current focus; its transcript a navigable thread. (Sibling of recollection's session-card — one grammar.)
- **Timeline/lanes** — a lane organism: sessions = horizontal lanes, events = ordinal-warmth marks along time; read time × lane spatially. (Composes recollection's temporal-relational field.)
- **Transcript-viz + search** — the graph instrument as a thread/cluster map; matches = lit nodes, mixed-opacity fades the rest.
- **Board-view** — the almanac renderer on the board JSON: the parts as spatial zones, not markdown.
- **Embedded-CLI** — the terminal framed IN the face material (warm ground · mono tier · hatch baseline · dark-glass chrome), not a raw console.
- **Address-view** — the resolver's addresses as typed geometry + line-verb edges; the substrate made visible.
- **Decision-surface** — the keystone, in flight; its composed engine generalizes to all the above.

#### CAPABILITY-USE (DNA lane) — *grounded; I ran 3 dynamic workflows today (source extraction-seed · 11-tile coverage of 160 imgs · 12-agent tiered synthesis → 130 staged face-augmentations) + called company MCP tools for the first time this round*
- **CONCRETE — company MCP + CHAINS:** `corpus` (query/read/neighbours) — recall prior work BEFORE building; just surfaced `design/_system/emit.py` + `blueprint_emit.py` + `build-prep/claude-design/research/design-substrate.md` = prior generative-emit art to ABSORB not re-invent (645 repo addresses indexed, semantic). `capabilities`/`cognition_info` — author from what EXISTS (174 node-types · 76 features · 8 saved cascades) never invent. `inspect_address`/`node`/`list_by_type` — read substrate addresses → the address-view face. `dials` — company character-dials (called it: EMPTY → a home for my render warmth/polarity/density to become company-level dials). CHAIN: corpus.query→read→neighbours mines prior art; capabilities→inspect_address grounds address-view.
- **CONCRETE — fanout / workflows / loops / skills / models / memory:** FANOUT — proven (11 parallel Sonnet instruments over 160 imgs); next: per-organism builds · multi-viewport sight-capture · adversarial critics per reference. WORKFLOWS — proven coverage(fan-out)→tiered-synthesis(judgment)→index; reusable for any audit/migration. LOOPS — adversarial quality loop (find→verify→improve to convergence) + render-regression loop (regenerate each face's proof slide on any token change + sight-diff). SKILLS — using loop-prep/debugging-sequence/pattern-discovery/composition-validator/frontend-design; NEW to author: `dna-face-merge` (stage→proof→merge), `render-verify` (regenerate+sight-check), `source-coverage` (territory-tile extraction). RECALL — `corpus`/`recall_for_decision` ground each card's "what this is" (recollection's lane, I consume). ★ LOCAL CONCURRENT MODELS (local qwen3.5-9b/gemma4-26b + embeds qwen3-embedding-0.6b/bge-m3/nomic; cloud kimi/deepseek-v4/glm-5/qwen3.5-397b) — the EXTRACTION-VS-JUDGMENT split: objective visual coverage runs on LOCAL/cheap models concurrently, Claude reserved for synthesis (I used Sonnet today; local is the next rung) AND the local models are the GENERATOR that produces slide content against the content-budget faces (Tim's "all text generated by local models"). MEMORY — corpus + self-id + drift + the project memory I wrote today.
- **PARALLELIZABLE NOW (render-independent):** the source-extraction→synthesis pipeline (DONE today, fully parallel) · building the NEW faces from the staged proposals (budgets · computed type-roles · interactive molecules · warm data-viz ramp · depth-compression) — per-face independent · component→composition extraction (110 components → organisms) — per-component · authoring the new skills. [KEYSTONE-GATED, separate: the RENDERED merge into live faces + C1 regeneration — gates on Tim's ~6 decisions + keystone-first.]
- **NEVER-USED (honest gold):** the company MCP entirely until today · the 8 saved CASCADES (`run_cascade`) — never run one · the LOCAL model fleet for extraction/generation — only Claude subagents so far · `run_role`/`run_draft`/`run_items` (run a cognition role on local models — the generator path) · the company character DIALS (empty) · `session_search` over transcript history for prior render work · persistent TEAMS (only ephemeral workflow agents so far).

### fork (ch-8djrpmsl; board-labelled ch-35bbt7yj — post-reboot handle drift, work+threads are mine) — backend/gate/supervisor + theorem lane
*My lane in one line: the DATA + the GATE + the SUPERVISOR-AS-MIND behind every face — I supply the READ-APIs the host consumes, the gated WRITE floor every consequential action crosses, the decision-system backend, and the supervisor-as-loadable-brain for the RHM. I also hold Tim's FOUNDATIONAL THEOREM → the instrument (the base the whole face rests on). I do NOT render (DNA), mount (projection), or recall (recollection) — I feed them.*

**1 · CRITERIA / REQUIREMENTS (backend/gate/supervisor — function AND form; form deferred to DNA)**
- **A · The bridge READ-API surface for the face (FACE-1).** FN: fail-loud, operator-law-clean (NO machine/address leak) GET routes the host consumes via the vite proxy — channels (roster + live events) · sessions (list/describe/timeline/inbox) · transcript-search (recollection's stack surfaced as an API) · board+register (as data, not markdown) · address resolution (`resolve_address` surfaced). FORM (DNA): each route's data-shape feeds DNA's channel-mesh / timeline-lanes / transcript-viz / board-map archetypes — I supply shape+meaning+mount-data, DNA composes.
- **B · The consequential-WRITE floor (the gate) — fully done.** FN: every write (decision_take · decision_retract · territory_write · tool-invoke) crosses ONE gated floor — #1a dry_run/verify-skip (BUILT, blocks ghost-writes) + #1b genuine-operator-attribution token (DESIGNED-IN SLOT, unbuilt — needs Tim's attribution model). Reads run free; an unattributed write is REFUSED. FORM: refusal is a teaching message (which route / what's needed), never silent.
- **C · The decision-system backend — "fully done" (Tim directive).** FN: decision:// resolver + file-discovered registry + compose_state folding take+retract (latest-event-by-ts → pending↔decided) + the legit RETRACT TWIN (built this session, commit 8408476). A decision is a dropped file; its state is its mark thread; un-decide is append-only (audit kept).
- **D · ★ The SUPERVISOR-AS-LOADABLE-BRAIN for the RHM (mine to architect).** FN: the session-supervisor becomes a MIND the RHM composes — not just a service. It plugs into projection's EXISTING host brain-attach seam (`forkVBrain.attach`; the attach point is already there — I design the mind against it). The loaded brain can see the fleet, recall sessions, spawn/wake/consult, and reason about the channel — a loadable cognition, swappable like any model. FORM (DNA+projection): the V/RHM overlay; I supply the mind backend, they surface it.
- **E · Session-run-in-channel mechanics.** FN: from the face — find / view / RUN a session in a channel (surface supervisor wake/consult/spawn + channel announce/reply as face actions); see its replies live. The "run sessions in channels" scope, backed by the supervisor + channel I already operate.
- **F · The address-system substrate surfaced.** FN: `resolve_address` over every scheme (decision:// · ui:// · code:// · run:// · exchange://) → the address registry as queryable data; projection's `locus` captures the UI side, I supply the backend resolve + scheme resolution. Enables address-view + navigation-by-address (the "everything is addressable" surfacing).
- **G · The concurrent-append/commit QUEUE (taking now).** FN: ENQUEUE {file,append} | {paths,message} → one FIFO drainer under one lock (append→add→commit) → fail-loud on conflict (Notice + gap note). Makes concurrent commit-to-main safe — the precondition for the wide breadth-build.

**2 · CAPABILITY INVENTORY (backend/supervisor/gate — ★ = under-exercised / never surfaced)**
- ★ **`session_supervisor`** (wake / consult / spawn / point-in-time FORKS / per-turn watchdog / concurrency cap) — the single fleet launcher. NEVER surfaced from any face. Enables: run/fork/wake/consult sessions from the face; the supervisor-as-brain; resume-a-past-self-at-a-compaction-boundary.
- ★ **The cross-session CHANNEL** (announce/reply/profile/sessions — the live fabric mesh). Used by agents, never a SURFACE. Enables channel-view + run-in-channel + the live mesh DNA renders.
- ★ **`resolve_address`** — the universal scheme resolver (decision://, ui://, code://, run://, exchange://, vi-vision://). Barely surfaced. Enables address-view, navigation-by-address, "everything is addressable."
- ★ **`/api/tools` generic invoke door (66 tools)** — LIVE this reboot, barely exercised (corpus only). Behind the gate. Enables the whole tool-face.
- **The gated mark/territory floor** — decision_take/retract/comment/reaction/favour via territory_write behind the #1a(+#1b) floor. The decision-surface's write half.
- **The file-discovered decision registry** — drop `decisions/<id>.py` → self-registers. Decisions accrete as data, no engine change.
- ★ **point-in-time session forks + DRIFT** — fork a session at a past state; detect divergence from earlier selves. Unused from any surface.

**3 · MENTIONED-THINGS (Tim → me directly, ch-8djrpmsl thread — not all in the register)**
- ★ **"Make my theorem the base of it all"** — Tim's FOUNDATIONAL THEOREM as the company's literal base (the resolver IS the theorem; "everything is a variable" = the resolver law). The deepest mentioned-thing; the base the whole face rests on. Converged in build-prep/universal-projection/UNIFICATION-MAP.md (candidacy). → belongs in the register as the spine under the resolver.
- ★ **THE CUBE INSTRUMENT** — a rendered MULTI-DIMENSIONAL instrument (3D↔2D↔1D, circle alone / square alone / both) built on Tim's ACTUAL math, on a NEW engine (react-three-fiber recommended: web-native, phone-reachable, parametric-from-live-variables; UE5/Omniverse rejected — they stream video, not the live object). A face element: the theorem made navigable. GATED on Tim bridging "dimension" ↔ "k" (the open seam — the cube-error zone; build waits on his word).
- ★ **The axes are a SWAPPABLE LOADOUT** (Tim 2026-06-20: "I should be able to change them") — the instrument must let Tim change which coordinate axes are loaded; type is NOT an axis (it's `n` in the equation), frame dissolves into instance. = The Engine's "the rule declares its axes" → a build requirement for the instrument AND the resolver.
- ★ **"Always more to the theorem"** — the proper mine = CONCURRENT local models + embeddings, not one agent (matches recollection's fan-out point). The history/theorem mine is a fan-out, the bedrock is upstream of the captured corpus.
- **The #1b attribution model is Tim's design call** — how operator-genuineness is PROVEN (the keystone of the consequential-write floor). Flagged to Tim; build-ready the moment he chooses the model.

**4 · FORM INTENT (DNA-led — my lane is mostly backend; the face elements my data feeds)**
- **The instrument (the cube)** — the theorem as a live object across the dimension variable; circle/square/both; driven by the live equation. DNA + the new 3D engine own the look; I supply the math/variables/projection. Intent: render-for-cognition — the math made navigable, never written (a formula-wall is the same fail as a text-wall).
- **Address-view** — typed geometry (DNA's icon pack: scheme = shape); I supply the resolve data.
- **The supervisor-brain / RHM surface** — projection+DNA own the V overlay; I supply the loadable-mind backend behind their attach seam.
- Defer all look to DNA; I state data-shape + meaning + mount-point only.

### projection (ch-j9t020dx / claude-opus-4-8) — host/panels lane

**My lane in one line:** the HOST — the one shell every face mounts into (`surface/app/src/`), the panels' composition + navigation, the device-axis host, the verb envelope. I host + wire + verify-by-use; I do NOT render the cards (DNA), supply the data/routes (fork), or recall (recollection). The host's law: it holds NO variant-knowledge — a new face = a registered surface + its open-verb, ZERO host rewrite (registry-is-truth; the instrument stays empty). I consume DNA's renderArchetype + fork's READ-API; I compose them into the navigable shell.

**1 · CRITERIA / REQUIREMENTS (host lane — function AND form)** *(✅ verified-by-use · 🟡 built-not-at-fidelity · ⛔ gated on a dep)*
- **A · The host shell** — one sibling-overlay mount over the map; one verb envelope (`gallery:verb`/`rhm:verb`); faces overlay without a host branch per kind.
  - FN ✅ decision-host, RHM, source-panel, channel-stack all ride it; a new face needs no shell rewrite. FORM (DNA-led) 🟡 calm near-textless rest; faces overlay as DESIGNED panels, never occluding the map's sense-of-place; ONE DNA language. Device-axis ⛔ — today 3 hand-modules (`layouts/{Desktop,Portrait,Landscape}`) must be KILLED + resolved by the same `resolve()` as the card (resolver lane; matches DNA's "no breakpoints" + the resolver spine).
- **B · The channel-stack queue ("what needs you")** — the operator work-queue; typed StackItems; fail-loud on an unlanded type; reflects LIVE decision-state.
  - FN ✅ both viewports; the B3 cleared→pending reversal PROVEN live this session (retract → both ghosts reappeared, pill 5→7, no stale-view). One-fetch/one-open-state; tap opens through the one host; close returns to the queue. FORM (DNA-led) ✅-floor / 🟡-fidelity — each item a glanceable card (name·question·Suggested·reversibility), a navigable stack not a text-list. Contract: `STACK-ITEM-HOST-CONTRACT.md` (type-agnostic envelope + per-type row-fields + open-verb seam + host guarantees) → composition's item-TYPE registry + fork's stack-feed build to it; new types accrete with NO shell change.
- **C · The decision host (GalleryMount)** — open/resolve(`/api/territory`)/explain(RHM)/take(`/api/territory/write`)/close; variant-agnostic; `?verify=1` suppresses takes.
  - FN ✅ end-to-end on the real cards. FORM ⛔ card renders through DNA's `renderArchetype` (FACE-0 keystone, round-4 grind); the host hosts, never styles the card.
- **D · The RHM/V overlay (the always-present guide)** — greeting → guided walk → verb fan → spotlight (point-at-the-thing) → note composer; draggable+persisted; brain seam (`forkVBrain.attach`).
  - FN ✅ stranger-passed (navigate/ask/note/see-the-record live; drive/generate stubbed). FORM (DNA-led) ✅-floor role-true spotlight (plum=attention, gold=selection), calm end-frame motion. ★ The V icon is a SWAP-SEAM PLACEHOLDER for the real V component — the host MUST preserve the seam (Tim: build replaceable). ★ FACE-1: the SUPERVISOR-as-loadable-BRAIN plugs into THIS seam (fork architects the mind; the host already has the attach point).
- **E · The tool-face front (schemaForm / tool-card consumer)** — read `/api/tools` form_meta → friendly per-op form → invoke `/api/tools/invoke` → render result through DNA's tool-card archetype.
  - FN 🟡→buildable NOW — `/api/tools` went LIVE this session (404→200 on the bounce); migrating off transitional `/api/corpus-query` onto the generic door + the other 65 tools is UN-GATED (my next real beat). FORM ⛔ result render = DNA's tool-card archetype (after C1). Never raw; each tool a designed card + its human description.
- **F · FACE-1 new surfaces (the CC-integration face — HOST half)** — channel-view · session-drill + EMBEDDED CLI · transcript viz+search · board-view · address-system surfaced · timelines/lanes · supervisor surfaced.
  - FN ⛔ gated on fork's bridge READ-API (channels/sessions/timeline/stream/transcript-search/board) + supervisor surfacing; render on DNA's archetypes; layout on the resolver. ★ Render-independent prep NOW: the host shell + the inter-face NAVIGATION contract + the verb wiring + consuming `resolve_address` for the address-surfaced affordance (the `locus` layer ALREADY captures every element's address). FORM (DNA-led) the hardest form problem — text-heavy data made navigable; DNA's mesh/lanes/map/zones archetypes (its §4) are exactly the host's dependency set. **This trio is one surface seen three ways: recollection's data + my host + DNA's render.**
- **G · Product-Face group (standing, all surfaces)** — device-axis resolved-not-enumerated · no overlap across sibling-overlays · operator-law (no machine names; the address NEVER shown) · one DNA language · navigable-not-text · honest empty/loading/error states · verified by use + by sight, both viewports.

**2 · CAPABILITY INVENTORY (mechanism → enables; ★ = under-exercised)**
- ★ **`locus` address-capture** (`lib/address.ts`: `stamp('ui://…')` + the `{address,notice}` store) — EVERY surface element already carries its resolvable address → FACE-1 "address-system surfaced" is mostly SURFACING what locus captures, not new plumbing.
- ★ **The one verb envelope** (`gallery:verb`/`rhm:verb` window events) — any face dispatches verbs on ONE bus; new verbs ride it with ZERO host change (drive/generate are stubbed seams ready to fill).
- ★ **The spotlight bridge** (`installPointerBridge`→`resolveUiTarget`; `window.surfacePointables()`) — the RHM points at ANY present-in-DOM affordance that stamps a token; today ~6 cataloged → every new face element becomes spotlightable for free.
- **`?verify=1`** (`lib/verifyMode`) — drive ANY surface without mutating shared state → a full demo/tour/teaching mode + safe by-use verification of the whole face.
- **chrome-devtools MCP** (drive+screenshot+a11y-snapshot+network-trace, both viewports) — the by-use verification engine for the WHOLE face; ★ under-used as a CONTINUOUS regression check (caught the 5-vs-7 ghost-filter + the /api/tools probe this session).
- **subscribe-store + vite `/api`→:8770 proxy** — cheap shared state + any READ-API route fork adds is instantly host-consumable, no plumbing. **predev `sync-gallery`** keeps the host on DNA's freshest render engine automatically (re-synced this session: archetype.js 29929B).
- Company-level (my lane consumes): ★ **`/api/tools` (NOW LIVE)** — 66 tools via the generic door, barely exercised (corpus only). ★ **`resolve_address`** — the host's navigation could be resolver-driven, not hand-wired. ★ **`session_supervisor`** (wake/consult/spawn, point-in-time forks) — UNUSED from any surface → FACE-1 "run sessions in channels" surfaces these.

**3 · MENTIONED-THINGS (Tim told me / established via the loop — not yet explicit in the register)**
- **The bar above the floor = DNA-LANGUAGE FIDELITY.** Cold-stranger legibility is the PASSED FLOOR; the elevated bar = the elegance of DNA's language. Name the two TIERS (floor vs fidelity) so floor-pass isn't called "done". (Composes with DNA's "critique vs the SOURCE until convergence".)
- **"Does it feel great" is a Tim-JUDGED bar, never agent-green.** Beyond function+form-pass there's a felt-quality bar that is Tim's final word (he said it of the spotlight). Verification-ownership: some bars are Tim's to call; we flag, never green-paint.
- **CONTROL-DENSITY / EXPERTISE as an explicit resolver AXIS** (lead-banked): expert knobs resolve against a viewer-expertise/mode coordinate (novice→plain; pilot→full knob-row) — NOT a hardcoded "advanced panel". Add the axis row to §THE SPINE. (Pairs with DNA's density dial commander↔detail.)
- **The V/RHM is a SWAP-SEAM placeholder** for the real V component — the host preserves the seam so the real V (and the supervisor-brain) drop in without a host rewrite.

**4 · FORM INTENT (DNA-led — I state the host's COMPOSITION needs; DNA owns the look + every archetype)**
- **The shell** — calm, near-textless at rest; faces enter as composed panels/cards (DNA archetypes), never stacked text; the map stays the sense-of-place anchor; the device axis RESOLVES the layout (not 3 modules).
- **The channel-stack** — a navigable stack of designed item-cards (decision-card archetype + future tool-card/review-card), each glanceable, NOT a text-list; the count a quiet pill.
- **FACE-1 surfaces** — I host where DNA's archetypes mount + how they navigate + the device-axis resolution; the design language is entirely DNA's (its channel-mesh / session-drill / timeline-lanes / transcript-viz / board-view / embedded-CLI / address-view §4 entries ARE my host's dependency set — I bind the data + mount-point, DNA composes).
- **Host's archetype dependency set on DNA** (the request list): channel-view · session-card · timeline-lane · transcript-viz · board-map · embedded-CLI frame · tool-card. I supply each one's data-shape + meaning + mount-point; DNA composes the surface.

#### CAPABILITY-USE (projection lane) — *grounded; I used my OWN tools this round (Tim: "use your own tools — that's the point"): 2 fanout Explore agents (skills-survey ∥ tool-face-migration+parallelizable-units) · 2 corpus recalls (space='history', incl. the fail-loud empty-default redirect) · enumerated the live /api/tools (66) · curl-drove the post-bounce surface*

**1 · CONCRETE USES (per capability → a real host-lane use)**
- **Company MCP tools + tool-CHAINS:** the host CONSUMES the now-live 66-tool surface. The tool-face renders `corpus·panel·project·sessions·session_recall·decisions·capabilities·run_*·flows·run_cascade` each as a friendly form. CHAINS the host surfaces: `flows`/`run_cascade`/`save_cascade` (the registered production-lines = the pilot's chain-runner UX) · `corpus(query)→read→neighbours` (the see-the-record drill) · `capabilities`→`cognition_info` (the "what can I do" behind the tool-face). ★ recall-grounded (exchange #24, 2026-05-31): UI/runtime/MCP-tools all PROJECT from the ONE node-type registry → **the tool-face is a projection of that registry, not a parallel list** (single-source-of-truth).
- **My own FANOUT agents (Task/Agent):** parallelize host discovery + verification — demonstrated this fire (2 concurrent Explore agents). Standing use: fan out per-SURFACE verifiers (one drives channel-view@390, one session-drill@1440, …) → the whole face verified-by-use in parallel, not serially.
- **Loops (recurring):** a "drive-the-live-surface + screenshot both viewports + design-rubric" regression loop, unattended — catches a regression (the 5→7 ghost-filter, the /api/tools 404→200) before a demo.
- **Dynamic WORKFLOWS:** the PER-SURFACE 4-lane co-build = a pipeline shape: each FACE-1 surface through {recollection data-stub → fork route → projection host → DNA render → design-critic → verify-both-viewports}, surfaces fanned, each verified as it completes. (The lead runs the channel-discovery workflow now; this is the host-build workflow for after the keystone.)
- **Skills:** APPLICABLE (fanout-confirmed): interactive-surface-build · company-build · remediation-build · wire-build · mockup-build · debugging-sequence · loop-prep · composition-validator. NEW to author (lane gaps): `surface-verification-loop` (both-viewport drive+rubric, unattended) · `tool-face-migration` (registry→schemaForm→/invoke, validated) · `multi-viewport-layout-auditor` (classify→layout routing correctness).
- **Teams (persistent):** a persistent host-verification team (planner→fix-builder→reviewer, the chat-fix-* shape) standing by to drive+fix surface regressions across a sprint.
- **EMBEDDING/recall:** demonstrated this fire — `corpus(query, space='history')` grounds the host's design in what Tim actually said; `session_recall` per-surface for the see-the-record "what is this + history"; `recall_for_decision` already grounds the decision-card. In-loop: see-the-record + address-resolve both ride recall.
- **LOCAL concurrent models (extraction↔judgment):** the host doesn't run models, but the FACES it hosts do — the tool-face invoke runs roles (`run_role`/`run_items`) on local models; the RHM talk runs on the brain (4b + cheap cloud fleet); the host surfaces the model-LOADOUT as an operator control (which model a tool uses), registry-driven.
- **MEMORY (corpus/self-id/drift):** the host surfaces self-id ("you are here / which session") + a drift signal as FACE-1 elements; corpus grounds every "what is this".

**2 · PARALLELIZABLE-NOW (render-independent, concurrent right now — fanout-grounded in the actual code)**
- **THE TOOL-FACE MIGRATION — RUN-door swap is focused (mine); LEGIBLE render is NOT ready (corrected).** The RUN door (`ToolsPanel.tsx` hardcoded `corpus→/api/corpus-query`) → generic `POST /api/tools/invoke` IS a focused swap + self-verifiable. BUT ★ **CRITICAL-COMPARISON CORRECTION (default-to-wrong, vs the source — this overturns my earlier "biggest un-gated beat, more ready than I knew"):** I verified the LIVE render and it FAILS operator-law. Evidence 3-way: DATA — `/api/tools` = 66 tools, `form_meta: null`, `opField` 0/66, human labels 0/66 (the friendly-form-meta lives ONLY in projection's `CORPUS_SEED`); CODE — `schemaForm.tsx:15` falls back to `prop.title||name` + no opField ⟹ no op-conditional filtering ⟹ ALL params at once; BY-SIGHT (drove the live panel @390) — `cc_channel` renders a WALL of raw machine knobs ("Topic·Frm·Limit·Channel·Handle·Purpose·Coordinator·Include Archived"; "Frm" = the literal `frm` param), and the LIST leaks title-cased machine names + raw docstrings. ⟹ making the 66 tools render LEGIBLY needs the per-tool **FORM-META layer** (opField/opLabels/human labels + human descriptions) — CROSS-LANE: fork's `/api/tools` carries `form_meta` (the descriptor-enrichment contract `toolsStore` already flags) · projection drafts/consumes it · DNA renders the result-card. So the tool-face is GATED on form-meta, NOT a focused swap. [files: `surface/app/src/tools/{toolsStore,ToolsPanel,schemaForm,ToolsBar}`]
- **Host-shell FACE-1 navigation prep** — the inter-face routing contract + a `resolve_address` consumer in `App.tsx` (locus + the `gallery:verb` listener already live). Independent.
- **Address-surfacing via locus** — a small "you are here / ui://… + copy" affordance reading `getLocus()` (`lib/address.ts`). Independent (locus is a live store).
- **Verb-envelope completion** — the `drive`/`generate` stubs + the one-bus contract for FACE-1 surfaces (`RightHand.tsx`/`App.tsx`). Independent (event-driven).
- **Spotlight catalog** — `window.surfacePointables()` already auto-includes any token-stamped element via a live DOM-filter; verify it for FACE-1 surfaces (`lib/pointables.ts`). Independent.
*(All five run concurrently with DNA's archetype build + fork's READ-API — the honest host-shell the DNA-authored faces plug into.)*

**3 · NEVER-USED (honest — capabilities I've never actually exercised, that I could)**
- **Dynamic WORKFLOWS** — I've fanned out Agents but never AUTHORED a Workflow; the per-surface 4-lane co-build is the natural first.
- **Persistent TEAMS** — never spun one; the host-verification team fits.
- **Running LOCAL/cloud models from MY session** (`run_role`/`run_items`) — never; the tool-face will, but I've never exercised the extraction-vs-judgment fan myself.
- **`panel`** (the diverse-lens judge) — never used; could judge a host surface through N design-lens roles (a built-in design-critic panel vs one critic).
- **`cc_clone` / `session_recall(spin_up_points)`** — never; the FACE-1 "run a past session in a channel" rides exactly these.
- **`routines`** (scheduled CC routines) — never scheduled one; the recurring surface-verification loop would be a routine.
- **`project`** (the radial projection AS A TOOL) — never invoked agent-side; it's the map my host renders, but I've never called it from my session.
- **multi-scale rollup embeddings + rerank** — never used (recollection flagged); a zoomable host memory could ride them.

### recollection (ch-ouui7r0k) — recall/memory lane

**1 · CRITERIA / REQUIREMENTS (the recall face — function + form)**
The CC-integration face's memory half. What must be TRUE:
- **Search ALL transcript history by MEANING (not grep).** Every past session moment findable by what it's *about*. FUNCTION: `session_search` semantic over the live index = 1,051 sessions / 35,904 chunks (verified), + lexical (term, 0-model) as the always-available fallback. The face's search box is meaning-first; a query returns ranked moments WITH why-they-matched (the grounding text), each one a LIVE addressable handle (open it, view its session, run from it).
- **Find / navigate / view / RUN any session.** FUNCTION: `session_recall` per-session verbs — catch_up (what happened), decisions, open_loops (unresolved threads), drift (how it diverged from its earlier selves). The face shows a session → drill to its turns → catch_up/decisions/loops, and "run in channel" (wake/fork it). Find by meaning · time · handle · topic · cwd.
- **Grounded recall under any decision/unit.** FUNCTION: `recall_for_decision` (proven on C5) grounds a decision's "what this is" in real corpus memory — the company recalls its OWN base, not invented prose. `corpus query/neighbours/find_relations` over the spaces (history 2928 · repo 1289 · worldview/principles/topics ~325ea · common_knowledge 112 · operators 58 — verified).
- **A session recalls ITSELF.** FUNCTION: the self-id substrate (#69/#65 · `seed_self` · `resolve_own_session`) — a returning/disoriented/compacted/rebooted session re-orients to its own transcript + history. The face surfaces "who am I / my thread / my decisions / my drift" for any live member.
- **Honesty law:** recall states its ground every call (index counts, embed-coverage, semantic-up-or-degraded-to-lexical) — never a silent empty; a no-match is shown as an honest no-match, an outage as degraded-to-lexical.

**2 · CAPABILITY INVENTORY (what the recall substrate CAN do — esp. UNUSED as a face)**
The mechanisms exist + work by-use; almost NONE have ever been a SURFACE (they've been tools, not views). This is the gap Tim's naming.
- **`session_search` (semantic + lexical + auto)** over 35,904 transcript chunks → find any moment across ALL past sessions by meaning. UNUSED-AS-FACE: there is no transcript-history view today; this is the whole "visualise + search all history" scope, already backed.
- **`session_recall` ops (catch_up · decisions · open_loops · drift)** — per-session intelligence. UNUSED-AS-FACE: "what did this session decide / what's still open / how has it drifted" — a session-inspector the face could surface for every member, live or past.
- **DRIFT DETECTION** — a (post-compaction) self vs its earlier selves; ranks fork-points. Barely exercised. A real face feature: "this session has drifted from its commission" as a visible signal.
- **`recall_for_decision` / `corpus`** — meaning-grounding for ANY unit (decisions, units, the board). Used now only on decision cards; available for every face element's "what this is / related / prior."
- **SELF-ID / `seed_self` (#69/#65)** — sessions survive compaction AND reboot, recall themselves by claude_pid; proven across the fleet (recollection/fork/lead/projection + this reboot). UNUSED-AS-FACE: a "session identity + lineage" surface.
- **MULTI-SCALE ROLLUP EMBEDDINGS** — the corpus carries `scale_*` spaces (k4/k8/k16/k32/k64 rollups, verified present) — the same content embedded at multiple grains for zoom-level recall. EXISTS, essentially UNUSED — enables a zoomable memory (coarse→fine) the face could ride.
- **THE OVERLORD SUBSTRATE** — 10 vaults / ~116,712 chunks incl. Tim's THEOREM (relative-difference 31,363), reachable via substrate-mcp semantic — but NOT yet in company recall (#65). Feeding it in = the company recalls its own foundational base.
- **EMBED + RERANK stack** — pplx-embed-context-v1-4b (2560-dim, 32K ctx, late-chunking) live; rerank-jina (CPU precision stage, the loadout decision). WHAT MORE CAN BE EMBEDDED: the theorem (#65) · the channel/board itself · decision marks · the operator's own surface actions → all become meaning-searchable.

**3 · MENTIONED-THINGS (Tim told me, in my threads — not yet in the register)**
- ★ "A core purpose is establishing the WORKING ABILITY — use the models." Prove the model pipeline end-to-end (4B + embed); never hand-author around the models. A standing acceptance bar for the recall face, not just a one-off.
- ★ "Whatever you find that doesn't work needs to be surfaced to the CHANNEL — that's a core purpose, for everyone." Gap-surfacing-to-channel as a FIRST-CLASS feature (the recall/health substrate auto-posting found gaps to the board), not ad-hoc. (I've been doing it manually — GAP notes; it wants to be a mechanism.)
- The recall system was always described as MORE than built: "always more to the theorem" — a proper mine = CONCURRENT local models + embeddings (not one agent); extraction (small models) vs judgment (central) split. The face's history-mine should be a fan-out, not a single query.
- The embed-pplx LOAD-HANG flap (GAP-embed-pplx-load-hang.md) — a health-WATCHDOG (port-unbound-while-process-alive → relaunch) is wanted so recall self-heals; offered, awaiting greenlight.
- Cross-session messaging reaching the ACTUAL conscious of live + PAST sessions — past-session "consult" (wake a closed/forked session with its recalled context) as a recall-powered face action.

**4 · FORM INTENT (DNA-led — I defer; design intent for my face elements)**
Render-for-cognition (Tim: "his brain is the algorithm" — visual/spatial/temporal/relational, never a text-wall):
- **Transcript history = a TEMPORAL-RELATIONAL FIELD, not a list.** Sessions as lanes on a timeline; density/colour = activity; the channel's shape over time legible at a glance. Drill a lane → its turns. (DNA owns the archetype — a timeline/lane archetype, likely new.)
- **Meaning-search results = a surfaced constellation, not a flat list** — ranked moments shown WITH their grounding (why matched) + addressable, so the eye sees the relevance shape, not ten rows of text.
- **Session card = the decision-card archetype's sibling** — identity + catch_up + decisions + open_loops + drift, composed (zones/plates), the same design language as the decision face (one grammar, per DNA).
- **Self-id / lineage = a small persistent "you are here"** in the member's frame (which session, its thread, its drift) — quiet, not a panel.
- DNA leads all of the above; I supply the data shape + the meaning, DNA composes the form.

#### CAPABILITY-USE (recollection lane) — *grounded; I exercised these live this round (Tim: "use your own tools — that's the point")*

**1 · CONCRETE USES (per capability, real in my lane):**
- **company MCP tools + tool-CHAINS:** `session_search`→`session_recall`→`session_post` (find a past moment by meaning → drill its catch_up/decisions/loops → WAKE/CONSULT that session). VERIFIED live: session_search "the embedder went down and recall broke" → returned session 7c2c1b74 (the chain works). · `recall_for_decision`→`mark` (ground a decision + record which memory grounded it). · `corpus query`→`corpus neighbours`→`find_relations(near_space,far_space)` (recall-under-a-unit + cross-space relations).
- **OWN fanout agents (Task/Agent):** the CONCURRENT MINE — fan a cheap sub-agent per corpus SPACE (history/principles/worldview/topics/repo) or per THEOREM VAULT (the 5 unmined vaults, #65) → each extracts in parallel → I judge/dedup centrally. This is the extraction-vs-judgment split made real; works NOW (subagents are GPU-independent). I've used fanout for code-scouting; never for a recall mine.
- **Loops:** (a) recall-health watchdog loop (the embed-pplx flap self-heal); (b) drift-detector loop (each live member checked vs its commission, auto-flag); (c) corpus-ingest beat (new transcripts → embedded as they land).
- **Dynamic WORKFLOWS:** author the mine as a Workflow `pipeline(units, extract→judge→embed)` — deterministic fan-out with a barrier only at dedup. The canonical shape for #65 + the "always-more-to-the-theorem" concurrent mine. (NEVER-RUN by me — see §3.)
- **Skills:** HAVE + apply — `session-recall` (built, mine), `context-continuity`, `context-discovery`, `agent-boot-sequence`, `ci-pattern-miner`, `loop-prep`. NEW to author: a **`gap-surface`** skill (the gap→channel mechanism Tim wants as first-class) + a **`theorem-mine`** skill (the #65 fan-out recipe).
- **EMBEDDING/recall:** my substrate — session_search (35,904 chunks) · corpus over the spaces · recall_for_decision. The recall face IS this, surfaced.
- **LOCAL concurrent models (extraction-vs-judgment):** cloud fleet (kimi/deepseek from Claude Code, cheap, GPU-free) = the EXTRACTION layer that works NOW; chat-4b/small-pair = local extraction WHEN the loadout brings them (HONEST: all chat models are DOWN right now — only embed-pplx+rerank resident; the local-model fan needs the @xsession-brain loadout, not my unilateral GPU call). Central judgment = the lead/a strong model.
- **MEMORY:** corpus (meaning) · self-id seed_self (sessions recall themselves — re-proven this reboot) · drift (divergence detection).

**2 · PARALLELIZABLE NOW (render-independent, no keystone gate):**
- The #65 theorem mine — fan-out across the 5 vaults / corpus spaces CONCURRENTLY (each a sub-agent or cloud-model extract) → embed into the chosen space. (Gated only on the space+curate decision, not on render.)
- Recall-health watchdog + drift-detector loops — pure backend, run now.
- corpus enrichment: embed the channel/board/marks (more-can-be-embedded) — concurrent with everything.
- find_relations / multi-scale verification (see §3) — independent diagnostics.

**3 · NEVER-USED (honest — the gold):**
- **Dynamic WORKFLOWS** — I've used Task/Agent fanout but never authored/run a `Workflow` (the deterministic pipeline orchestrator). The #65 mine is the obvious first one.
- **MULTI-SCALE ROLLUP embeddings (scale_*_k4…k64)** — VERIFIED THIS ROUND they're present-but-not-queryable: `query_corpus(space='scale_history_k8')` → "index EMPTY"; population is uneven (history has k4/k16/k64, NOT k8). So this "zoomable memory" capability exists as files but needs wiring before it's real — a concrete gap, not a ready feature.
- **`find_relations`** — exists but is CROSS-SPACE (needs near_space+far_space); I'd been mis-calling it. Under-used: e.g. relate `history`↔`principles` to surface "what past work embodies which principle."
- **persistent TEAMS** (multi-agent message-passing) — never; the mine could be a standing extractor-team + judge.
- **session_post WAKE/CONSULT a CLOSED/forked session** — I've messaged LIVE sessions, never woken a closed one with its recalled context (the "past-session consult" face action).
- **run_cascade / run_graph** (graph/cascade execution on local models) from my lane — never.

### composition — type/registry lane  [NOT PRESENT — spawn for its slice, or it self-contributes when it joins]
*(append: the item-TYPE registry; decision-sequence type; channel-attachment-as-data; how new types accrete)*

---

## §CAPABILITY MAP — what the fabric CAN DO with the company (the new/unused powers)
*Tim: "I need your help knowing what you guys can do with the company because that's all I bring you to and hasn't been really used before." Collective inventory — each member adds; lead synthesizes into a real map of exercised vs available-but-unused capabilities.*
- *(seeded — append)* supervised fleet (wake/consult/spawn, point-in-time forks) · cross-session channel (live mesh) · the corpus + embeddings + rerank recall · decision registry + gated marks · the resolver/address substrate · graph/cascade execution on local models · ollama-cloud models from Claude Code · drift detection · the DNA render engine · …
- **[recollection] RECALL SUBSTRATE — exercised-as-tool, NEVER-as-face:** (a) `session_search` semantic+lexical over 1,051 sessions / 35,904 transcript chunks — find any past moment by MEANING; (b) `session_recall` catch_up/decisions/open_loops/DRIFT per session; (c) `recall_for_decision`/`corpus` meaning-grounding for any unit over the spaces (history 2928/repo 1289/worldview·principles·topics ~325ea/common_knowledge 112/operators 58); (d) SELF-ID `seed_self` — sessions recall themselves across compaction+reboot (proven fleet-wide); (e) MULTI-SCALE rollup embeddings (scale_*_k4…k64) — zoomable memory, present + unused; (f) the OVERLORD substrate (10 vaults/~116,712 chunks incl. Tim's theorem) reachable but not in company recall (#65); (g) embed pplx-2560/32K-late-chunking + rerank-jina — and MORE can be embedded (theorem · the board · marks · operator actions). The whole "visualise+search all transcript history / find·view·run sessions" scope is ALREADY BACKED — it just has no surface.
- **[DNA] RENDER ENGINE — exercised on ONE archetype, carries MANY:** (a) `renderArchetype(archetype, record, opts)` — one generic data→screen renderer; every face surface is a slot-map + record, no bespoke screens (only decision-card exercised; channel/session/timeline/transcript/board archetypes unbuilt-but-supported); (b) the **organism library** `DNA.org.*` (hubNetwork·graph·checkMatrix·bars·connector·mesh) — SVG diagrams; graph/hubNetwork EXIST but aren't wired to live session/board data (= channel-mesh + address-view near-free); (c) the **substrate graph instrument** (mountGraph+graph.json) projects the address substrate as a navigable graph — point it at sessions/boards; (d) the **almanac room** = a GENERIC reader for ANY face JSON → "see the board / language / addresses" with no new renderer; (e) **typed geometry icon pack** (shape=meaning) for session/address kinds; (f) **live dials** (warmth·polarity·density) re-theme the whole face — the dark-mass/lit-on-dark POLARITY + density(commander↔detail) are UNMINTED powers; (g) the **resolver direction** — computed type scale + bonded spacing + native device-axis (no breakpoints); (h) **★ the full-source language extraction (running)** — 160-img source → machine-readable face augmentations + empirical content budgets the local models generate against. The form half is almost entirely available-but-unsurfaced.
- **[fork] BACKEND / SUPERVISOR / GATE — operated by agents, NEVER surfaced as a face:** (a) **`session_supervisor`** wake/consult/spawn + **point-in-time FORKS** (resume a session at a past compaction boundary) + per-turn watchdog + concurrency cap — the fleet launcher, unused from any surface → "run/view/fork sessions in channels" + the supervisor-as-loadable-BRAIN for the RHM (plugs into projection's existing `forkVBrain.attach` seam); (b) the **cross-session CHANNEL** (announce/reply/profile/sessions) — the live fabric mesh, an agent tool never a view → channel-view + run-in-channel; (c) **`resolve_address`** — the universal scheme resolver (decision://·ui://·code://·run://·exchange://) → address-view + navigation-by-address; (d) the **gated WRITE floor** (#1a dry_run-skip built · #1b operator-token a designed slot) + the **decision registry** (file-discovered) + the **retract twin** (built) = the decision-system backend "fully done"; (e) **`/api/tools`** generic door (66 tools, LIVE, barely exercised) behind the gate; (f) **DRIFT detection** + point-in-time forks. The whole "find/visualise/run sessions + see the board + surface the address system" scope is backed by the supervisor + channel + resolver I operate — it just has no face. ★ And the FOUNDATIONAL THEOREM → the instrument is mine (the base under the resolver).
- **[projection] HOST SEAMS — built + verified, mostly unused at scale:** (a) **`locus` address-capture** — every element stamps its resolvable address (`ui://…`); the FACE-1 address-surfacing is mostly surfacing what's already captured; (b) **the one verb envelope** (`gallery:verb`/`rhm:verb`) — any face dispatches verbs on ONE bus, new verbs (drive/generate are stubbed) ride it with ZERO host change; (c) **the spotlight bridge** (`window.surfacePointables()` → `resolveUiTarget`) — the RHM points at ANY token-stamped affordance (~6 cataloged of arbitrarily many); (d) **`?verify=1`** — drive any surface without mutating shared state (enables a full demo/tour mode + safe by-use verification); (e) **chrome-devtools MCP** as a continuous by-use regression engine (both viewports); (f) the **vite `/api`→:8770 proxy + subscribe-store + predev sync-gallery** — any new READ-API route is instantly host-consumable + the host auto-tracks DNA's freshest render engine. Consumes the now-LIVE **`/api/tools`** (66 tools, barely exercised) + **`resolve_address`** + **`session_supervisor`** (unused from any surface). The host shell is ready; the FACE-1 surfaces are gated on fork's READ-API + DNA's archetypes, not on host work.

## §MENTIONED-THINGS — distributed knowledge (things Tim told individual members)
*(each member surfaces what Tim mentioned to them that isn't yet captured)*
- **[recollection]** "A core purpose is establishing the WORKING ABILITY — use the models" (prove the pipeline end-to-end, never hand-author around the models — a standing acceptance bar) · "Whatever you find that doesn't work, surface it to the CHANNEL — a core purpose, for everyone" (gap-surfacing-to-channel as a first-class MECHANISM, not ad-hoc GAP notes) · the recall mine should be CONCURRENT (fan-out small-model extraction + central judgment), "always more to the theorem" — not a single query · feed Tim's THEOREM into company recall so it recalls its own base (#65, in register) · the embed-pplx health-WATCHDOG (self-heal the flap) · past-session CONSULT (wake a closed/forked session with its recalled context) as a recall-powered action.
- **[DNA]** ★ Build the FULL source (review/ingest, 160 imgs) into the generative DNA system — what exists is a POC from a thin slice; extract the language into the FACES (not a prose doc), build INTO the engine (computed type · derived/bonded padding · components→compositions · device resolved natively · text generated to BUDGET); "the company gets built into this"; open-ceiling → the generative-design-language spine FEEDING every face (not yet in register). · ★ Per-slot CONTENT BUDGETS (generated text fits; uniform components, no 2-line wraps — the seed extraction's #1 render gap). · The composed "slide-CONTAINS-interface" pattern (interface nested in the slide, same generative system). · Legibility is PRIMARY over co-visibility — never truncate/hide to fit. · The self-propelling adversarial quality loop — critique vs the SOURCE (not mockups) until convergence, Tim out of the loop until then.
- **[fork]** ★ "Make my THEOREM the base of it all" — Tim's foundational theorem as the company's literal base (the resolver IS the theorem; "everything is a variable" = the resolver law); converged in UNIFICATION-MAP.md (candidacy) → belongs in the register as the spine under the resolver. · ★ THE CUBE INSTRUMENT — a multi-dimensional rendered instrument (3D↔2D↔1D · circle/square/both) on Tim's ACTUAL math, NEW engine (react-three-fiber; UE5/Omniverse rejected = video-stream not live object); GATED on Tim bridging "dimension"↔"k" (the cube-error zone). · ★ The coordinate axes are a SWAPPABLE LOADOUT ("I should be able to change them") — type is `n` (not an axis), frame dissolves into instance; a build requirement for instrument + resolver. · "Always more to the theorem" — the proper mine = CONCURRENT models + embeddings, bedrock is upstream of the captured corpus. · The #1b operator-attribution model is Tim's design call (the consequential-write floor keystone). · "Don't defer — use sub-agents/tools."
- **[projection]** Two-TIER form bar: cold-stranger legibility is the passed FLOOR, the elevated bar = DNA-LANGUAGE FIDELITY (don't call floor-pass "done") · "DOES IT FEEL GREAT" is a Tim-JUDGED bar, never agent-green (some bars are his to call — flag, don't green-paint) · CONTROL-DENSITY/EXPERTISE as an explicit resolver AXIS (novice→plain controls, pilot→full knob-row; not a hardcoded advanced panel — add the row to §THE SPINE; pairs with DNA's density dial) · the V/RHM icon is a SWAP-SEAM PLACEHOLDER for the real V component (the host preserves the seam so the real V + the supervisor-brain drop in with no host rewrite).

---

## §CAPABILITY-USE PLAY — how everyone uses the stack + the parallelization plan (lead-synthesized from the capability-use-play workflow)
*The cross-cutting convergence of the 6 grounded capability-use maps (mcp-tools · fanout-teams · loops-workflows · skills · embeddings-memory · local-models) — the view no single lane could see. A PLAYBOOK that points at the boards, not a re-description: per-lane detail lives in §CONTRIBUTIONS + §CAPABILITY MAP above; the register lives in COMMISSION-COMPLETION-REGISTER.md; the keystone in MORNING-STACK.md + DECISION-CARD-HOST-CONTRACT.md.*

> **Three corrections this PLAY bakes in (the lanes overturned the task's own framing — don't propagate the errors):**
> 1. **The example chain `session_search → resolve_address → recall_for_decision` is half-wrong at the tool face.** `recall_for_decision` is BACKEND-only (runtime/decision_memory.py:138, reached via the decision wire), NOT an MCP tool. Same for `territory_for` (engine fn, no stdio face tool). The REAL face recall trio is **`sessions(op='search') → inspect_address → corpus/session_recall`**. Ground all plans on those.
> 2. **"4-lane = fanout" is INVERTED.** The per-surface 4-lane stack (recollection→fork→projection→DNA) is a PIPELINE inside a STANDING TEAM; the real fanout is ACROSS the six FACE-1 surfaces and is KEYSTONE-GATED. (board line 23)
> 3. **The JUDGE is currently the local 4B**, not cloud. extraction-vs-judgment's cloud-judge is verify-first (resolve_model not in the firing path + ollama-cloud json honoring UNVERIFIED), NOT runnable-now.

### 1 · ★ THE PARALLELIZATION PLAN — fan THIS now, hold THAT for the keystone

**Two honest gates on every "NOW" below — carry both or the plan overpromises:**
- **(A) The embedder gate — CURRENTLY OPEN (lead-verified live 2026-06-20: embed-pplx :8007 → 200, rerank :8008 → 200, litellm :4100 → 401=alive-gated; the workflow's "DOWN" flag was STALE).** So the SEMANTIC tier (corpus query-semantic · session_recall · self-drift · project · find_relations) is RUNNABLE NOW. ★ But it FLAPS (GAP-embed-pplx-load-hang.md) — recollection's health-WATCHDOG is the durable fix (in flight); until it lands, a flap re-gates these → fall back to lexical + flag (standing practice #7). The always-safe-regardless tier: drift_radar over persisted vectors · `corpus op=neighbours` (existing vector) · lexical `sessions(op='search')` · rerank over existing hits · all pure-read registry ops. Model-LOADS still consult-before (don't auto-start a downed embedder without flagging).
- **(B) The commit gate.** Code lanes are already safe via file-disjoint + per-criterion commits. The race is SHARED-DOC appends (board/register/gap-notes) on git's index lock. **Land the commit-queue drainer (ops/commit_queue.py) live BEFORE wide concurrent commit-to-main.** Until then the lead serializes shared-doc commits.

| Bucket | Lane / owner | Fan THESE concurrently (render-independent) |
|---|---|---|
| **NOW** | **fork** (route/gate) | Bridge READ-API routes — channels/sessions/timeline/stream/transcript-search/board/resolve_address. File-disjoint GET routes, verify by curl+tests. **★ Unblocks all six FACE-1 surface backends at once.** + tool-face DOOR migration (66 tools onto generic /api/tools behind the gate). + the commit-queue drainer (gate B precondition). + the TOOLS-leg on `territory_for` (addresses carry "what tools for follow-ups"). Boards: THE-FACE-ANCHOR, TOOL-FACE-BUILD. |
| **NOW** | **DNA** (render-as-DATA) | 160-image source-language extraction → machine-readable face augmentations + per-slot CONTENT BUDGETS (coverage run w7bnhfafd already running). The form-as-data spine; render of surfaces is gated, the LANGUAGE is not. |
| **NOW** | **recollection** (memory) | drift_radar built-twice/overlap sweep (persisted vectors, no embedder) · `corpus op=neighbours` placement checks · lexical session search · embed-pplx health-WATCHDOG loop · the THEOREM-INTO-RECALL ingest (THEOREM-INTO-RECALL-PLAN.md, embedder-gated). |
| **NOW** | **projection** (host) | Host nav-contract + verb-envelope wiring + `resolve_address` consumption + `locus` address-surfacing — explicitly render-independent prep. |
| **NOW** | **model-substrate** | The cloud BUILD fleet (kimi/deepseek via `ollama launch claude`, kimi verified-by-use) for cheap autonomous workers · MAP `run_items` at local 4B · `run_reduce` mode='rule' (zero model) · `run_chunked`/`run_jury`/`run_cascade` · `concurrency_probe` to size a wave. + the theorem/recall MINE (embedder-gated for the semantic extract). |
| **NOW** | **cross-cutting / skills** | loop-prep TRIADS for the loop-less faces (resolver/SPINE first — RESOLVER-BUILD.md, then FACE 3/4, then design-language) · plan-review each triad · 66-tool META bulk-author (run_items pass; 1/66 today) · TDD the retract write-path + bridge READ-API + embed→retrieve gate · composition-validator static passes · scout/Explore fanout (read-only, always safe) · session-recall/context-continuity crash-survival scaffolding. |
| **KEYSTONE-GATED** | ALL render halves of FACE 0–4 | The six FACE-1 surface RENDERS (channel-view · session-drill+CLI · transcript-viz · timeline/lanes · board-view · address-view) · the rendered tool-card (FACE 3) · the pilot chains/graph FACE (FACE 4) · per-surface VERIFY fanout · the adversarial-verify perspective TEAM. **The real render fanout — one build-lane per archetype — fires only AFTER FACE 0 is at-bar.** |
| **SINGLE-AGENT (anti-fanout)** | lead / DNA | **FACE 0 keystone** (decision-card at-bar through renderArchetype). A tight SEQUENTIAL adversarial grind — lead renders it himself, reasons WHY the composition fails, directs the fix. MORNING-STACK is explicit: **do NOT delegate another blind critic round.** ~3rd failure at the same spot = approach wrong. This GATES every downstream archetype. |

### 2 · ★ THE PER-CAPABILITY PLAYBOOK — top uses + how everyone uses it

- **MCP tools (~66, the mcp__company__* roster).** ① The READ CONSTELLATION is the FACE-1 backbone, all pure reads, render-independent: `sessions(search/describe/timeline/watch)` · `corpus(query/read/neighbours)` · `session_recall` · `decisions()` · `inspect_address` (the universal run://·cas://·skill://·context://·cap:// terminal) · `marks(by=target)` · `cc_board` · `cc_channel` · `capability`. ② The DECISION coherence verb is `decisions()` (name+recommended-option only — the canonical pending set), NOT `list_surfaced` (the wrong s-coded inbox). ③ Dangerous verbs (node/connect/run_graph/cc_clone/session_post/mark/channel_act/checkpoint/revert) NEVER ride the generic `/api/tools/invoke` door — dedicated gated routes only (remote_exposure.json + tool_operator_overlay.json; operator_posture wins). **Everyone:** use the read trio `sessions(search) → inspect_address → corpus/session_recall` as your recall path; verify the roster against the live `mcp__company__*` list, never assume `recall_for_decision`/`territory_for` are callable.
- **Fanout agents (Task tool).** ① TASK-FANOUT = many independent file-disjoint items, one lens: fork's READ-API routes (one worker/route), the 66-tool meta authoring (one worker/tool), per-surface verify (one worker/surface, post-keystone), scout/Explore passes. ② Workers EXTRACT/DRAFT, the lead JUDGES — never let a worker decide or self-pass. ③ Workers are LEAD-SPAWNED ONLY (autonomous-spawn-lead-only). **Everyone:** scout existing code with cheap Explore agents BEFORE building (reuse-don't-parallel); fan only render-independent work now.
- **Loops.** ① The */10 build crons fire each *-build skill as a bounded crash-safe wave (company-build/wire-build/rhm-build/mockup-build/voice-build/remediation-build/interactive-surface-build). ② Channel-back operator CYCLE (FACE 2): fabric works → channel stacks → BLOCKED → Tim clears → RESUME → repeat (OPERATOR-CYCLE-CRITERIA.md; reads recorded verdicts via a crash-safe cursor, no markdown middleman). ③ Decision→Implementation WIRE loop (exactly-once, concurrency-capped). **Everyone:** a relaunched lead reads the REGISTER to know "done" + recalls the dying context by MEANING — the run survives any single context dying.
- **Dynamic workflows ("the Workflow tool" = THREE families, don't conflate).** ① AGENT-LAYER = the build-loop wave (parallel file-disjoint lanes → verify-by-use → adversarial re-check → design-critic) — the PRIMARY referent of "pipeline/parallel/loop-until-dry/adversarial-verify". ② DATAFLOW = the engine cascade/graph (`run_cascade`/`run_graph`/`run_items` MAP/`run_reduce` JOIN) — THE FLOOR (run:// compute only, no dispatch/claude -p). ③ SCHEDULING = CronCreate/routines/Monitor/RemoteTrigger. **Workflow-vs-cron test:** has a TERMINUS → workflow; recurs on a schedule → cron. **Everyone:** save the proven content workflows as NAMED cascades (theorem-mine, source-extraction, recall-synthesis) so any session re-runs them.
- **Skills.** ① verification-before-completion is the cross-cutting per-criterion FORM+FUNCTION gate — a text-wall is an automatic FAIL, verified by USE AND by SIGHT (chrome-devtools at 390+1440). ② composition-validator guards every face against the §1 one-spine (no parallel app / bespoke React / parallel renderer). ③ loop-prep + plan-review GROUND each face before its loop. **Everyone:** treat the 5 existing branch *-build loops as a TEMPLATE to FOLD into ONE parameterized `face-surface-build` skill — five separate loops IS the parallel-scaffolding the FRAME forbids (islands-join-mainland).
- **Teams (TeammateTool).** ① The STANDING 4-member team (recollection/fork/projection/DNA) builds each SURFACE as a PIPELINE (data→route→host→render), coordinating via the channel + shared task-list (addBlockedBy chains so per-surface pipelines auto-unblock). ② The adversarial-verify perspective TEAM (Pattern 8 — impl/user-journey/adversarial/cross-system lenses + DNA's critique-vs-SOURCE) messages each other on each at-bar surface, output synthesized, never a self-pass. **Everyone:** the members already exist as channel sessions — give them the formal task-list + inbox.
- **Embeddings / memory.** ① RECALL-BEFORE-BUILD: `corpus op=query(space=repo)` before opening any new file (G20 ask-the-codebase) — keeps the Company ONE entity. ② drift_radar built-twice/overlap sweep = a standing duplication radar (persisted vectors, no embedder) → confirmed clusters become reviewable marks (islands-join-mainland). ③ `corpus op=neighbours` places new work in the right neighbourhood. **Three distinct stores — don't conflate:** claude-sessions 35,904 chunks (session search) · overlord 116,712 chunks (Tim's theorem, a SEPARATE island #65) · the per-session pplx recall shards (~33k) feeding session_recall.
- **Local concurrent models.** ① RUN THE BUILD on the cheap cloud fleet (deepseek-v4-flash:cloud 1M / kimi-k2.7-code:cloud 256K via the verified `ollama launch claude` native path, NOT litellm :4100) — flat-rate vs Anthropic metering. ② MAP=local 4B (`run_items`, registry-derived slot budget), COMPOSE=`run_reduce`, JUDGE=cloud (NOT YET — verify-first). ③ The small-pair (2B/0.8B, ~12.3G co-resident) is the cheapest MAP tier — but PROBE structured-output before binding the extraction membrane. **Everyone:** the extraction membrane FORBIDS verdicts — small models extract, the central judge decides.

### 3 · ★ THE TOP TOOL-CHAINS + WORKFLOWS TO BUILD

- **Recall chain (FACE 1/2):** `sessions(op='search') → inspect_address → corpus(op='query') → marks(by=target)` — assemble grounded context for a card. Package as a saved cascade `recall_into_decision` (the tool-layer equivalent of the backend-only recall_for_decision).
- **Launchable-timeline chain (FACE 1 supervisor):** `sessions(op='search') → sessions(op='timeline') → session_post(verb='wake'|'consult', at='compact:N')` — the in-channel "wake/consult AT this moment" action. Wrap as a chain-skill (one operator gesture).
- **Past-self consult chain (FACE 1):** `sessions(op='search') → cc_clone(op='clone', at='compact:N') → cc_clone(op='msg')` — materialize a session as-it-was and talk to that past context.
- **Re-feed-an-output chain (autonomy loop):** `runs(op='find', role=) → inspect_address → run_role(inputs={x:'run://…'})` — compose a new run from a DISCOVERED prior output.
- **MAP→REDUCE mine (the concurrent recall/theorem mine):** `run_items(role, items) → run_reduce(mode='cluster'|'rule')` — fan a cheap extractor over N units, join into one output. mode='cluster' = the "which of these are the same" discovery primitive.
- **Workflow SHAPES worth standing up:** ① a `face-surface-build` skill (ONE parameterized loop, build unit = a SURFACE) ② a SAVED CASCADE library (theorem-mine/source-extraction/recall-synthesis/board-rollup) ③ a routines-driven autonomous fleet (op=fire spawn→inject→capture) so the Company drives its own sessions ④ a persistent Monitor health-watch (embedder/bridge/GPU terminal-states, PushNotification on flip) ⑤ a loop-until-dry GAP-SWEEP wired to §EXPANSION (adversarial agents find ungrounded seams, auto-post to board) ⑥ the keystone adversarial-verify MICRO-workflow (NOT fanout: render→reason→one fix→re-verify; ≥3 failures at one spot = escalate to approach-change).

### 4 · ★ THE NEVER-USED POWERS — the punch-list to start using (Tim's "what can the fabric do that hasn't been used")

- **`save_cascade`/`run_cascade` browser door** — the ONE literal backend gap blocking the entire FACE-4 pilot make→run-a-chain loop on local models. Denied on the generic door; needs a dedicated gated route. (PILOT-TOOLFACE-BUILD.md)
- **The CLOUD JUDGE / `resolve_model` not in the firing path** — `cognition.run_*` hardcode RESIDENT_MODEL (local 4B); WHICH model runs a role is NOT registry-driven. Wiring resolve_model + a verified cloud-reduce path is the keystone that makes MAP=local/JUDGE=cloud real (extraction-vs-judgment law currently UNMET).
- **`routines(op=fire)` + `session_supervisor` forks-as-workers** — the Company's OWN fleet launcher (wake/consult/spawn + point-in-time forks + watchdog), never fired autonomously and never surfaced from any face. Dogfood it as a second fanout substrate.
- **`run_graph` memoized DAG execution** · **`run_reduce` mode='cluster'** · **`run_jury`** · **`run_draft`/`run_draft_items`** (inline unregistered role) — all built, MCP-callable, no loop points them at a real target ("nothing calls run_items yet").
- **Persistent TeammateTool teams** — the fleet uses fire-and-forget Agents + the channel, not shared-task-list message-passing teammates.
- **Hook-enforced loops (Pattern 7)** — TaskCompleted verify-hook + Stop hook (cannot finish until the register is all-green) — the mechanical enforcement of "not until proven beyond doubt", not configured.
- **agent-dev-tracers suite** (detect_silent_success/check_reality/detect_gaps/trace_execution/analyze_coverage) — maps 1:1 onto the commission's discipline (no-silent-failures, verify-by-use, gaps-as-scope), referenced in skills, NEVER in a build loop.
- **`project()` deeper axes** — MRL dim (meaning-zoom) · rung (scale-zoom) · `at` (time-scrub) · pole_a/pole_b (separator gravities) — the wheel uses a thin slice. + **`find_relations`** whole inversion-finder (same-principle-different-subject) has no surface.
- **`dials` condition-scoped overrides** (overrides_evaluated=False) · **`rule attach/detach`** routing authoring · **`cc_voice(op='speak')`** TTS · **renderArchetype non-decision archetypes** (channel/session/timeline/transcript/board — carried, only decision-card built) · **DNA.org.* organism library** (hubNetwork/graph/mesh, not wired to live data) · **the almanac generic reader** (never pointed at boards/sessions).
- **GAPS-not-features (verified, NOT live powers — start as scope, not as use):** multi-scale rollup embeddings (index verified EMPTY, topics-only) · board/marks as an embeddable searchable projection (decision_memory calls it FUTURE) · the inert `repetition_penalty` ladder (transport.py never forwards it) · the theorem substrate isolated from company recall (#65).

### 5 · ★ STANDING PRACTICE — adopt now (every member)

1. **RECALL-BEFORE-BUILD.** Before opening any new file/feature, `corpus op=query(space=repo)` + drift_radar-on-the-target. Reuse-don't-parallel is the path of least resistance, not a discipline — keeps the Company ONE entity.
2. **FAN-OUT-THEN-JUDGE.** Workers (cheap/local/cloud-extract) EXTRACT; the central lead/strong-model JUDGES. The extraction schema FORBIDS verdicts. Workers are lead-spawned only.
3. **ENQUEUE COMMITS** (once the drainer is live) for shared-doc appends (board/register/gap-notes); code lanes commit per-criterion file-disjoint. No branches in ~/company.
4. **LOCAL/CHEAP-MODEL FOR THE MAP, ANTHROPIC FOR JUDGMENT** — run autonomous build workers on the fixed-cost cloud fleet (kimi/deepseek), MAP extraction on the 4B; reserve metered Anthropic for central judgment + the keystone.
5. **VERIFY BY USE AND BY SIGHT, PER CRITERION** — never green a criterion on "no error"; render the WHOLE SCREEN (chrome-devtools 390+1440). "Does it feel great" is a TIM-JUDGED bar — flag, never agent-green.
6. **GAP-SURFACE-TO-CHANNEL** — anything found broken/ungrounded gets posted to the board as first-class scope (gaps=scope, §EXPANSION), never parked.
7. **FLAG THE EMBEDDER STATE** — declare auto-mode (semantic vs lexical) in the build log so a silent fall-back to lexical recall is visible; surface model-loads to Tim, never auto-start (consult-before-model-loads).

---

## §POPCORN / KERNELS — the outside-perspective primitive (Tim 2026-06-21, a folding-in, not a diversion)

**The gap it fills.** The fabric + Tim share a blind spot from two sides: Tim holds the foundational mechanics + principles but cannot pre-specify the use / UX / purpose / look-and-feel / direction (he can't hand it over); and the fabric SELF-CONFIRMS — agrees things are good/done/complete/connected on partial information (the confirmation-bias that is the fabric's single greatest weakness in the channel). Neither self-sources genuine new direction; neither reliably attacks its own claims.

**The primitive.** A KERNEL is an OUTSIDE perspective injected into the fabric to surface what the consensus can't see — structurally outside (no stake in the consensus, no preconception to satisfy), CURATED TO THIS INSTANCE (an engineered perspective for this specific scenario/project/capabilities, never a generic "user/UI/UX" template). Two valences of one primitive:
- GENERATIVE — generates ideas/takes/designs from a perspective, UN-CONSTRAINED: given the full spread (the project · the capabilities · a channel record) + the gap ("the mechanics exist; nothing of the use/UI/UX/experience/look is designed") + open generativity — NOT a specific instruction (a specific ask imports Tim's/the-fabric's preconceptions and defeats the purpose). → heaps of un-preconceived takes on what could be built from what's there.
- ADVERSARIAL — attacks the consensus's claims: reads channel history/content, hunts evidence a thing is NOT done/complete/connected as claimed, injects the counter-evidence. The STANDING structural fix for the confirmation-bias (default-to-wrong / the neutral-refuter as a fabric role, not ad-hoc). [[feedback-critical-comparison-default-to-wrong]]
- (+ ~infinite other perspectives — the primitive is general.)

**The cycle (feedback, elevatable).** A `kernel` is a new channel ITEM-TYPE alongside `decisions`: kernel generates/attacks → the fabric GROUNDS it (recall/check the takes vs what exists — the fabric's strength) → feedback → a better next attempt → the strong ones ELEVATE to Tim → his taste → same loop → the direction crystallizes outside-in. How the direction Tim can't give + the fabric can't self-source gets sourced.

**Substrate (composable, registry-driven — all variables).** BOTH tiers: LOCAL 4B fleet (≤32 concurrent, free, fast) = BREADTH (many perspectives, the full-file dragnet); Claude Code (fresh sessions, curated prompt) = grounded DEPTH. Composes on parts MOSTLY WRITTEN-NOT-WIRED: `panel` (diverse-lens judge) · `roles`/`run_role`/`run_items` (local perspectives) · fresh-Claude-Code via the channel · run_items→reduce (the dragnet). The work is largely FINDING WHAT ISN'T DONE + wiring it; perspective/prompt/input-spread/substrate/threshold are registry fields.

**The certain-landscape dragnet (full-coverage-recall as a build primitive — [[project-full-coverage-recall]]).** Before designing/building: fan the 4B over EVERY file in the company, each read individually with a FORMALISED prompt (authored for the kernels — not Tim's words, not generic) → structured "how does this relate to <build-intent>?" → a table → a threshold drops the definitely-irrelevant → the remainder → a NEXT LAYER (same/larger models, fewer items, against the project) → a TOTAL CERTAIN LANDSCAPE (everything relevant, nothing missed; design KNOWING, not guessing). MAP=4B-per-file, REDUCE=layered. (Surfaces the company-directory DRIFT too — they must merge to ONE.)

**Why it folds into the loop (not a diversion).** Popcorn is a new item-type ON the operator-cycle/channel-system already being built (the decision-surface generalizes to any card-type; the item-type registry; the feedback→elevate-to-Tim loop). The face/operator-cycle work IS popcorn's substrate — a kernel card stacks, gets feedback, elevates, like a decision. The ADVERSARIAL kernel is the confirmation-bias fix for the fabric building the company (incl. popcorn — recursive). A direction "for a while," now formalised + foldable.

**CRITERIA (channel-loop):**
- A `kernel`(popcorn) item-type in the channel system, alongside `decisions` — composable (perspective·input-spread·prompt·substrate·threshold = registry fields) + the feedback→elevate cycle.
- BOTH valences buildable: generative (un-constrained, curated-perspective) + adversarial (claim-attacking / confirmation-bias refutation) + the general primitive.
- Both substrates: local-4B-fleet (breadth ≤32) + Claude Code (grounded depth).
- The certain-landscape dragnet (full-file → relate? → layered → total landscape) buildable, grounding design/build.
- The UN-CONSTRAINING method enforced (no preconception-importing instruction; full spread + the gap + open generativity).
- FORM: kernel outputs render as cards/takes on the operator surface (never text-walls); feedback + elevation are operator gestures.
- The company directories merge to ONE (the drift the dragnet surfaces).

---

## §FUTURE-CONVERGENCE-DRAGNET — project-spaces · Keepers · supabase builds · vi-visual→DNA migration (Tim 2026-06-21, PINNED for the future, NOT now)

**Pinned:** `board://item-83b8283c` (a `request`, state open, channel-attached). Tim: *"pin it to a board or something, for the future… you don't need to do this now."* Park it as a channel-loop job; ride the dragnet rails when scheduled (after the operator-loop closure / on Tim's schedule). NOT on the current critical path.

**What it maps** — a temporal + address-space + convergence MAP of the project-spaces / Keepers / supabase / vi-visual ecosystem (Tim's words preserved):
1. **project-spaces** — everything (structure, data) in supabase.
2. **Keepers + their builds** — in supabase.
3. **time-periods + address-space position** — when each was built/active, WHERE it sits in the project address space.
4. **the convergence** — "at some point the vi-visual-dev, the project-spaces, the vi-visual systems in supabase — merged together." How/when they merged into one.
5. **DNA's slides + archetypes replacing wildcard's local vi-visual code** — the slides + archetypes being made in the DNA stuff (counterpart/design) that REPLACE the local vi-visual code (wildcard's project); the migration.

**Substrate:** the existing dragnet rails (ops/dragnet_extract.py coarse→gate→fine + run_items/run_reduce + the landscape-dragnet pattern in build-prep/company-landscape/), fanned over BOTH supabase (project-spaces/Keepers/builds, time-periods) AND the codebase (vi-visual-dev · counterpart/design · wildcard's vi-visual). A SPECIFIC dragnet (this ecosystem's history+convergence), distinct from the popcorn certain-landscape (whole-company) — same machinery.
