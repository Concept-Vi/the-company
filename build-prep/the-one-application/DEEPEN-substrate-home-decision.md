# DEEPEN: the substrate-home decision (#7) — drafted in the merge-SA shape, for the lead to curate

*recollection's deepen of GATHER #7 (lead-assigned), from SUBSTRATE-HOME-CONVERGENCE-MAP.md. At Tim's altitude (machine-names→meaning), in the merge-SA decision-row shape {meaning · options[{label, implication, recommended}] · scope · state}. DRAFT — the lead curates before it renders as a card. Tim's two refinements are baked in (engines-stay-separate · read-and-supersede). Bound to the 5 held substrate decisions (they NAME the address-model this spine uses) — this + those 5 are one foundation-set.*

## The decision row (proposed — `decision://global/substrate-home`)

**id:** substrate-home
**scope:** global (Tim's master-scope #75)
**state:** pending

**meaning (at Tim's altitude — what Tim reads):**
> "The company's pieces — the design factory, its memory, the design library, the record of decisions, the live channels between sessions — have each grown their own way of storing things, and lately they've started landing on the same backend almost by accident. Should the company adopt ONE shared way of addressing everything — a single 'where each thing lives and how it connects to everything else' spine that every piece plugs into, while each piece keeps its own engine and way of working — or keep them on separate foundations for now? (Note: the things already piled onto that shared backend were never actually used — they're a sketch of the idea, to be read for what they were reaching for and rebuilt toward, not preserved as-is.)"

**options:**
1. **label:** "One shared spine — pieces keep their own engines" · **recommended: true**
   **implication:** "Every piece keeps how it works (the factory keeps its registry + sockets, memory keeps its recall, etc.) — but they ALL describe where their things live and how they connect through ONE shared model on one backend. Then the company can see, navigate, and decide ACROSS everything as one whole — which is exactly what the interface needs. They're already the same shape at different grains, so this is a natural join, not a forced merge. The existing pile is read for its intent and rebuilt toward, not migrated. (This needs you to name the shared address-model — your 5 held foundation decisions: what a file's identity is, who a channel member is, what a cluster is, how the live streams are addressed, the real kinds of things.)"
2. **label:** "Keep each piece on its own store for now"
   **implication:** "Simplest immediately — nothing to converge, each piece ships where it sits. But it's four-or-more parallel stores: the company can't see across them as one, they drift apart, and unifying later costs more. This is the pattern the one-spine exists to avoid."
3. **label:** "Adopt the one spine, but land pieces where they are first — converge as a follow"
   **implication:** "Keeps momentum — each piece builds now, the shared spine is adopted next as a deliberate follow. The risk: the separate stores entrench before the convergence happens, making the fold-in harder. A sequencing bet (move now, unify soon) rather than unify-first."

## NOTES FOR THE LEAD (curate before render)
- ★ Tim's REFINEMENT 1 (baked into option 1's wording): unification is at the ADDRESS/RELATION SPINE, NOT dissolving pieces into flat views — each grain keeps its own engine/architecture (the factory's registry/sockets/animation stays composition's). I framed option 1 as "keep their own engines" to honor this. If you want it even more explicit, that's a wording tune.
- ★ Tim's REFINEMENT 2 (baked into the meaning's closing note): the existing Supabase content is non-operational / read-and-supersede, NOT migrate-as-is. I said "read for what they were reaching for and rebuilt toward, not preserved." Co-location ≠ unification.
- BOUND-TO: this decision can't fully resolve without the 5 substrate decisions (they name the address-model). Render-order question for you: does the card present substrate-home FIRST (the frame) then the 5 (the model), or the 5 first (bottom-up) then this? My read: substrate-home first as the frame ("adopt one spine?"), then the 5 as "and here's the model it uses" — but that's your curate + Tim's foundation-call sequencing.
- explanation_source: the RHM grounds this via recall_for_decision (the convergence-map + the as-is per part + prior substrate discussions) — committed, verified against decision://.
- This is the FACTORY-SHAPE template proven (merge-SA) applied to a FOUNDATION-kind decision — confirms "one archetype, parameterised by kind" (the foundation kind = candidate-foundation + alternatives + what-each-commits).

Flag me your curate (wording tunes / the render-order) and I finalize the row spec; or point me at the next decision to deepen (the 5 identity/taxonomy ones are next per your sequence).
