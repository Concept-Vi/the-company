# OPERATOR-LOOP CLOSURE — the honest bar (Tim's check, 2026-06-21)

**Why this exists:** the lead + lanes reported rendered PIECES as "complete / first-real-run-live." Tim's check exposed the confirming-bias: the BAR is the CLOSED OPERATOR LOOP verified by **Tim using it end-to-end** (decide → grounded walk-through → his response surfaces → the fabric resumes), NOT pieces verified by the fabric's by-use. By that bar it is **NOT done** — Tim has made ZERO decisions through it. This tracker holds the TRUE per-link state (default-to-wrong; "renders" ≠ "closed"). [[feedback-critical-comparison-default-to-wrong]]

**THE BAR:** every link below true AND proven by Tim's own use. Until then: NOT finished.

**★ THE CLOSURE-TEST (wildcard's adversarial pass, 2026-06-21 — the precise failure-shape behind every over-claim).** Every over-report this session was ONE shape: a capability REAL+verified at ONE layer, the claim silently generalizing to the WHOLE loop. Instances caught by-use: on-topic ≠ grounded (L1 grounding) · route-exists ≠ works (the brain 400 was loadout) · slot+route ≠ wired (L1 card-display, fed by a stub) · grounded-backend ≠ surface-grounded (HOLE-3: RightHand calls .ask not .groundedAsk) · general-grounded ≠ aimed-grounded (the aim-injection) · canonical ≠ all-input (HOLE-1 chokepoint) · retrieved ≠ traced (HOLE-2 provenance) · mechanism ≠ Tim-closed (L3). This IS [[the landscape's substrate-ahead-of-wiring]] sharpened. THE TEST for any "done": does the capability hold at EVERY layer THROUGH to Tim's actual use — or just one? The live popcorn-B kernel (wildcard / ch-piffgfxv) tracks each link to Tim-closed by re-checking at every layer (default-to-wrong). [[feedback-critical-comparison-default-to-wrong]]

| # | LINK (Tim's question) | HONEST STATE (default-to-wrong) | OWNER | what closes it |
|---|---|---|---|---|
| 1 | RHM walks Tim through every decision, GROUNDED | **SHOWS GROUNDED 16/16, by-sight-verified both viewports (390+1440) — close = TIM'S USE.** The full wire is live: fork route + DNA seam (84dd35d) + DNA card-region (eb85afb) + projection injection (543a0d3) + the WIRE-ILLUSION root-fix (8b5a169: decision-render.js was missing from the sync-manifest → decisionSlide undefined → renderExplained never ran; one missing file blocked the whole walk-through). Every card shows {what_this_is / why_it_matters / grounding_note} inline — 13 authorize/trade-off fast + the 2 theorem-forks grounded in Tim's REAL multi-axis math (rebake cube=15) + the 16th posture-card. Provenance level-3 (traced-pointer, verbatim, HOLE-2-safe); regression f03efe9 restored the note on all 22 (committed, rides next bounce). theorem cold-first ~24-30s (placeholder-covered) → cache ~3s. ONLY remaining: Tim opens a card by-use. | recollection+composition (grounding DONE) · DNA (region DONE) · projection (wire DONE, GROUNDED_EXPLAIN_ENABLED on) | Tim opens a decision → the grounded walk-through shows (it does, by-sight) → his own use closes it. |
| 2 | RHM is the GROUNDED MIND (V overlay), fully integrated | **V ANSWERS GROUNDED by-use (projection verified) · close = TIM'S USE + DNA's V-overlay-render (fresh budget).** Brain = role-resolved KIMI via local ollama :11434 (a555964, Tim-directed "role-based not -pro"; the 400 was loadout not code — the native-model-layer's first instance, [[feedback-cognition-is-role-resolved]]). The V's default ask IS the grounded mind: forkVBrain.groundedAsk → /api/brain/ask (12b028c self-render, 56e8844 RightHand .ask→.groundedAsk, 482044b markdown-strip, bf82ad2 aim-injection → grounds on the AIMED card). Default-brain extended to 16k + resolve-to-context (275146c). REMAINING: Tim asks the V by-use + DNA's richer V-overlay render archetype (fresh budget; the panel answers now). | projection (wire DONE, by-use verified) + DNA (V-overlay render, fresh budget) | Tim asks the V about a card → grounded answer on THAT card (works now) → his use closes it; DNA's overlay-render deepens it |
| 3 | Tim's responses surface + the fabric RESUMES (the cycle closes) | **MECHANISM LIVE + HARDENED · close = TIM'S ACTUAL DECIDE → resume.** fork's L3 v1 (149fd77): territory_write chokepoint → `decision_decided_signal()` emits decision.decided{id,address,chosen_option,by} on the take → both an EVENT (GET /api/decision/decided-signals, verified live) AND a noticeboard signal-item attached_to decision:// (144c21a) → lanes SEE it without polling. HARDENED: the read is registry-is-truth (1285543, drops event-only phantoms — no ghost-decided card) + the chokepoint CANONICALIZES decision_take/retract targets (5b7acbb / HOLE-1, closes the silent-resume-miss). Single-source: wildcard's decide() + projection's onVerb both hit the one chokepoint (verified). v2 gated_on:decision:// (value-on-address + optional blocked-on, the both+others shape) = fork's build at Tim's v1-verify. REMAINING: Tim decides a real card → a gated lane resumes (the close-by-his-use). | fork (wire ✓ + hardened; v2 later) + projection (decide-write ✓) + the lane resume-consumer | Tim decides a card through the surface → the decided-signal fires → a gated lane resumes with his choice, verified end-to-end by HIS decide |
| 4 | RHM ↔ CHANNEL (read/post the fabric channel) | **READ WORKS NOW · POST = FULLY-OPEN per Tim's informed bar (2026-06-22).** READ: the V reads/discusses the channel today (4eff1a4; L2 wire + channel-read-tool cd43ed4) — projection verified by-use. POST: Tim chose "anything can post, no safety check" — VERBATIM, INFORMED (fork explained the runaway-spam trade-off; Tim chose it anyway, consistent with "posting shouldn't be gated"+minimize-gating). ⟹ the SUPERVISED-POST discriminator/split/#1b-token is MOOT for posting — wire OPEN post_to_channel (no gate, no token, no propose-gate). Runaway-safety OFF (Tim's eyes-open call, reversible — internal channel, low-ceiling). fork wires the open backend; projection builds the post-surface + transparency-render (show Tim what went out — UX-good even ungated). | fork (open post_to_channel) + projection (post-surface) | the V posts freely; the transparency-render shows Tim what it posted. No gate per Tim's informed choice. |
| 5 | RHM can UPDATE a decision card | **CONTRACT FINALIZED + POSTURE-CARD LIVE (16th) · MECHANISM = fork's post-#1b build.** L5-CARD-UPDATE-CONTRACT.md (46556b9): 3 mark-types (decision_update inert/by=rhm/content-whitelisted · decision_update_accept behind the #1b token, an-AI-can't-write-it · reject) + compose_definition (row never mutates); Hole-1=RE-OPEN on options-change (decision_retract, explicit); Hole-2=content-fields-only. The posture-card (card-refine-posture, authorize/owner=tim) lets Tim decide the posture THROUGH the surface — live in his queue, grounds inline (verified). The MECHANISM (the mark-types + compose_definition) is fork's build AFTER #1b lands → then projection's re-render + composition's by-use confirm. | composition (contract DONE + posture-card) → fork (mechanism, post-#1b) → projection/DNA (re-render) | the RHM proposes a card-update → Tim accepts (gated) → re-render; the posture-card is Tim's own call on whether to enable it |
| C | The CONNECTOR (full remote access) | built + adversarially verified + funnel-on; external reachability is Tim's app-connect test (the one in his court) | lead (built) / Tim (app-connect) | Tim pastes the URL into an app + it auths |
| #1b | OPERATOR-SESSION TOKEN (built-inert foundation; NOT used by posting per Tim) | **BUILT-INERT — posting doesn't use it (Tim chose fully-open); A held.** 8347660: GET /api/operator-session mints a real token (live-inert, both flags OFF); projection's surface auto-mints+attaches (558e4a8, verified) — now harmless-dormant (no enforcement consumes it). ★ Tim's bar = fully-ungated POSTING (B moot — no token/discriminator). A (decide-gating / OPERATOR_TOKEN_ENFORCE): HELD — Tim's posting-answer ≠ decide-gating; decision_take runs free today (no regression); no forging-threat shown (fork's recommend, sound). enforce-scope CONFIRMED (fork + lead-scan): territory_write = only production take-writer, one HTTP-route caller, no MCP-face writer → IF A ever wanted, HTTP-boundary enforcement is SAFE. The token+enforce-scope stay BANKED as the foundation if decide-gating or the L5-accept-floor is ever separately wanted; not fired. | (dormant; foundation if-ever-wanted) | n/a — Tim chose no posting-gate; A held with no regression to prevent |

**Data substrate (DONE, real — the pieces ARE built, just not the loop):** the 24-row decision registry (15 pending + decided, owner-resolved, schema-gated single-source) · the cards RENDER (binary/n-panel) on Tim's real queue · the cards now GROUND (scandir-once fix) · theorem-forks carry caveat + grounding_source in-card · channel-view live (breadth #1). These are the rendered+data pieces — necessary, not sufficient.

**Closure sequence (live 2026-06-21):** fork's backend DONE + bounced LIVE — explain route (L1) + decide→signal wire + noticeboard signal-write (L3 v1). The GATES now: (1) L1 CONTENT — recollection's per-decision retrieval-keying fix (the BULK gate, 13/15 cards) + the theorem rebake (2 theorem-forks); (2) L3 — Tim's ACTUAL decide → a lane resumes (mechanism verified by fork's by-use; awaits Tim). L2 DISPATCHED → projection (wait-fill). L4/L5 SCOPED (dispatch-ready). EACH closed only when Tim uses it and it works — flag the lead per-link as it's Tim-proven, never fabric-by-use.

**Lead-tracked non-blocking flags (no-deferral: OWNED + sequenced, NOT parked-as-minor):**
- `commit_queue_acceptance` STANDING RED (drift_acceptance suite): not in STATE's suite block (MAP/RHM/mode/panel drift GREEN; only this red). Pre-existing (red before fork's L3 work). fork correctly won't touch STATE.md now (would entangle other lanes' uncommitted edits). OWNER: lead/commit_queue. SEQUENCE: add commit_queue_acceptance to STATE's suite block once STATE.md's cross-lane uncommitted edits settle (safe to edit). Routed by fork 2026-06-21 (g-1782035947).

**Adversarial findings (wildcard / ch-piffgfxv = the live popcorn-B kernel, 2026-06-21):**
- HOLE-1 (L3, ACTING): territory_write does NOT canonicalize decision_take targets before mark + decision_decided_signal → a non-canonical caller silently misses (mark lands at the literal string, decision:// resolver reads pending; signal emits non-canonical → a lane's gated_on:decision://<canonical> never matches → silent resume-miss). Happy-path defended (DNA stamp + wildcard guard); the CHOKEPOINT itself isn't. "verified by fork's by-use" held only for CANONICAL input. FIX: wildcard canonicalizing at territory_write (decision_address(parse_decision_address(target))) before mark+signal, coordinating fork's signal-emit. Rides the next bounce.
- HOLE-2 (L1, refines the gate): "retrieve the decision's origin" must TRACE TO THE REAL SOURCE (the channel-discussion / gap-note / Tim's-words content gathered-from), NOT a stored origin-FIELD an agent wrote (= claim-grounded-in-a-claim — passes "adds-what's-not-on-the-card" while still ungrounded-in-reality). origin-field = pointer-only; grounding traces THROUGH it. Discriminating bar: does grounding cite REAL gathered-from content (verifiable to a real msg/note/utterance)?
