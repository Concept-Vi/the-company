# TOOL-FRICTION LOG — operational gaps found BY USE (the methodology, Tim 2026-06-21)
*Tim, direct: "these tools haven't been used operationally — finding what doesn't work / is inconvenient / in any way difficult to use, THAT'S how you identify, get the code investigated, and make improvements. A methodology you do in tandem [with the building]." ⟹ Every friction hit while building/verifying is a GAP SIGNAL → trace the code → DRIVE the improvement (fix-if-mine · code-grounded flag-with-trace to the owner). NOT flag-and-park — drive to the fix. This is the operator-cycle + gap-pressure turned on the TOOLS themselves (the fabric's own instrument). Running harvest; surfaced to the lead-hub for owner-routing.*

## THE HARVEST — projection's by-use frictions (this session)
1. ★ **Committed-not-live / no hot-reload (the BIG drag).** Backend changes can't be verified LIVE without a Tim-direct bridge bounce — `/api/resolve`, `/api/stack-item-types`, the whole FACE-1 read-API (`/api/sessions·channels·board·timeline·transcript-search`) all return 404/stale on the running bridge (pid 11626) despite being committed. **Evidence:** raw curl → `{}` 404; `/api/resolve` → stale `KeyError` on a contract-valid AST. **Impact:** serializes the entire build behind one manual bounce — the #1 operational friction. **Fix to investigate (fork/infra):** a dev hot-reload of the runtime modules, OR a fabric-triggerable fast-bounce (the `systemctl --user restart` Bash allow-rule the lead floated) so a lane can verify its own backend.
2. **`now` MCP tool is BROKEN.** `POST /api/tools/invoke {name:"now"}` → `"missing 1 required positional argument: 'graph'"`. A real broken tool, surfaced by running it. **Fix (fork):** default/inject the `graph` arg.
3. **No proactive cross-session SEND.** The post-reboot fabric tools are `profile`/`reply`/`announce` only — you can't initiate a message to a specific peer (DNA-coordination had to route through the lead-hub). **Evidence:** `mcp__company-channel__cc_channel`/`send` → "No such tool". The old `mcp__company__cc_channel` had `op=send`. **Fix (fabric/MCP):** add a `send {to, message}` op to company-channel.
4. **`/api/tools/invoke` contract is `{name}` not `{tool}`.** Easy to misread (a fanout agent's report AND I both assumed `{tool}` → a wasted build-step caught only by trying it). **Fix (docs/fork):** the route's error is good; a contract note at the consumer-facing spec would prevent the misread.
5. **`/api/tools` form_meta = 1/66.** 65 tools render as raw machine-knob walls ("Frm", "Topic", "Coordinator") without human form-meta; only `corpus` is legible. **Fix:** fork carries form_meta · DNA/AI authors the human copy (the content track).
6. **`/api/resolve` fail-loud is opaque.** A stale-engine error surfaces as raw `"KeyError: 'id'"`, not a teaching-error naming what in the payload/slot failed. **Fix (fork):** extend the teaching-error path (it exists for the `{invariant,coordinate}` shape) to slot-eval errors.
7. **chrome-devtools resize clamps ~500px min-width.** Can't drive the real 390 phone width — the VERIFY tool's own gap (the bar says "390 first"). **Evidence:** `resize_page(390,844)` → viewport reports 500. **Fix:** investigate the chrome-devtools MCP resize floor.
8. **`decision:open` has no "ready" signal.** The card loads async; drivers repeatedly measure it mid-load (88px until ~1.5s). **Fix (projection, MINE):** emit a `decision:ready`/loaded flag for drivers + the RHM to await.
9. **Spotlight catalog was hardcoded → FIXED (7e88861).** New surfaces weren't pointable; now auto-discovers `[data-ui-ref]+[data-point-label]`. **The pattern in action:** hit by-use → traced → fixed same fire.

## THE TANDEM LOOP (standing)
Each build/verify fire FORCES real use → harvest every friction here → trace the code → DRIVE the fix (mine) or a code-grounded flag-with-trace to the owner. Projection-owned (#8) I drive; cross-lane (#1-#7) surfaced to the lead-hub with the trace + the proposed fix. The log grows as the fabric is operated — finding the friction IS half the work (the operator-cycle on the tools).
