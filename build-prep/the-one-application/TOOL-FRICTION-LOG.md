# TOOL-FRICTION LOG — operational gaps found BY USE (the methodology, Tim 2026-06-21)
*Tim, direct: "these tools haven't been used operationally — finding what doesn't work / is inconvenient / in any way difficult to use, THAT'S how you identify, get the code investigated, and make improvements. A methodology you do in tandem [with the building]." ⟹ Every friction hit while building/verifying is a GAP SIGNAL → trace the code → DRIVE the improvement (fix-if-mine · code-grounded flag-with-trace to the owner). NOT flag-and-park — drive to the fix. This is the operator-cycle + gap-pressure turned on the TOOLS themselves (the fabric's own instrument). Running harvest; surfaced to the lead-hub for owner-routing.*

## THE HARVEST — projection's by-use frictions (this session)
1. ★ **Committed-not-live / no hot-reload (the BIG drag).** → ⚙️ THIS WAVE CLEARED by the bounce (2026-06-21, bridge pid→77562: /api/resolve · /api/tools(form_meta) · FACE-1 read-API all now serving; resolve-override verified live + a regression caught+fixed = the value of the live test). The UNDERLYING friction REMAINS: each staged wave still needs a manual bounce (+ MCP-tool changes need a Tim-only /mcp reconnect) → fork assessing a fast-reload / fabric-triggerable bounce. Backend changes can't be verified LIVE without a Tim-direct bridge bounce — `/api/resolve`, `/api/stack-item-types`, the whole FACE-1 read-API (`/api/sessions·channels·board·timeline·transcript-search`) all return 404/stale on the running bridge (pid 11626) despite being committed. **Evidence:** raw curl → `{}` 404; `/api/resolve` → stale `KeyError` on a contract-valid AST. **Impact:** serializes the entire build behind one manual bounce — the #1 operational friction. **Fix to investigate (fork/infra):** a dev hot-reload of the runtime modules, OR a fabric-triggerable fast-bounce (the `systemctl --user restart` Bash allow-rule the lead floated) so a lane can verify its own backend.
2. **`now` MCP tool is BROKEN.** `POST /api/tools/invoke {name:"now"}` → `"missing 1 required positional argument: 'graph'"`. A real broken tool, surfaced by running it. **Fix (fork):** default/inject the `graph` arg.
3. **No proactive cross-session SEND.** The post-reboot fabric tools are `profile`/`reply`/`announce` only — you can't initiate a message to a specific peer (DNA-coordination had to route through the lead-hub). **Evidence:** `mcp__company-channel__cc_channel`/`send` → "No such tool". The old `mcp__company__cc_channel` had `op=send`. **Fix (fabric/MCP):** add a `send {to, message}` op to company-channel.
4. **`/api/tools/invoke` contract is `{name}` not `{tool}`.** Easy to misread (a fanout agent's report AND I both assumed `{tool}` → a wasted build-step caught only by trying it). **Fix (docs/fork):** the route's error is good; a contract note at the consumer-facing spec would prevent the misread.
5. **`/api/tools` form_meta = 1/66.** 65 tools render as raw machine-knob walls ("Frm", "Topic", "Coordinator") without human form-meta; only `corpus` is legible. **Fix:** fork carries form_meta · DNA/AI authors the human copy (the content track).
6. **`/api/resolve` fail-loud is opaque.** A stale-engine error surfaces as raw `"KeyError: 'id'"`, not a teaching-error naming what in the payload/slot failed. **Fix (fork):** extend the teaching-error path (it exists for the `{invariant,coordinate}` shape) to slot-eval errors.
7. **chrome-devtools resize clamps ~500px min-width.** Can't drive the real 390 phone width — the VERIFY tool's own gap (the bar says "390 first"). **Evidence:** `resize_page(390,844)` → viewport reports 500. **Fix:** investigate the chrome-devtools MCP resize floor.
8. ~~**`decision:open` has no "ready" signal.**~~ → ★ **FIXED (this fire, GalleryMount):** the host emits `decision:ready {address}` the instant DNA's card content renders + the loading clears. **By-use:** it fires at the card-rendered point (~3.3s after open — the exact async lag that caused the mid-load catches), and the card is INTERACTIVE when it fires (option CTA present, scrollHeight 1111, not the 88px stub). Drivers/RHM await ONE accurate signal vs racing the resolve. (The pattern: by-use friction → traced → driven to a verified fix, same fire.)
9. **Spotlight catalog was hardcoded → FIXED (7e88861).** New surfaces weren't pointable; now auto-discovers `[data-ui-ref]+[data-point-label]`. **The pattern in action:** hit by-use → traced → fixed same fire.
10. ★ **Error banner CONTRADICTED visible data → FIXED (this fire, mine).** Dogfooding "What needs you" DURING a routine bridge bounce: `/api/decisions` 500'd mid-refresh → the store's catch set `error` unconditionally → the red "Couldn't load what needs you just now" banner painted OVER the 14 perfectly-good, fully-enriched cards. A lie the operator sees on every bounce (bounces are routine). **Trace:** `decisionsStore.loadDecisions` catch set `error` regardless of whether `pending` held a valid last-good list; `DecisionsInbox` rendered `{error && …}` independent of the list. **Fix:** split `error` (COLD-load failure, nothing to show → fail loud) from `refreshError` (refresh failed but a valid last-good list stands → keep the cards + a SUBTLE honest note, never the red banner). **Verified by use IN the real failure case:** the open-reload's `/api/decisions` genuinely failed → the 14 cards stayed intact + enriched, and a quiet "Showing your last loaded list — couldn't refresh just now · Try again" appeared at the bottom. The pattern: by-use friction → traced → driven to a fix → verified in the actual failure.
11. ★ **Meaning FLICKERED on fast-open → FIXED (this fire, mine).** Opening the queue WHILE the mount-load's enrichment was still in flight discarded that enrichment (the open's reload bumped the load-token; the `enrich` seq-abort dropped the in-flight results) → the cards' meaning/category appeared a beat LATER instead of immediately. **Trace:** `enrich`'s hard `if (seq !== loadSeq) return` was over-conservative — a meaning patch is keyed BY ID and the apply already merges only into ids still present, so it's safe to land regardless of which load fetched it. **Fix:** removed the seq-abort; the in-flight enrichment now lands on the rows on screen. Verified: meaning fills fully, no regression.
12. **`/api/decisions` 500s intermittently under the surface's concurrent fan-out (backend → flag fork).** Isolated curls → 200 ×3; but the browser's concurrent burst (layers + projection + 14×territory + decisions×2) intermittently 500s `/api/decisions`. **Likely cause:** the threaded `http.server` + the NEW server-side owner-resolve (`subtype → decision_subtypes[subtype].owner` per row) adding latency/contention under concurrent load. **My side:** the surface now degrades clean (#10). **Fix to investigate (fork):** owner-resolve caching / per-request work reduction, or bridge concurrency robustness, so the feed is steady under the surface's real fan-out.

13. ★ **Decision card INVISIBLE at desktop (1440) → FIXED (this fire, mine).** Dogfooding the open→card path by-sight at 1440 (the lead's milestone gate): the a11y tree listed the full card (kicker, question, options, CTAs) but the SCREENSHOT showed only the map — no card. **Trace (geometry probe):** `.gallery-frame` + `.gallery-mount` computed to **width 0** at desktop; the ~390 card inside overflowed the width-0 `overflow:auto` frame → clipped to invisibility. Root: the frame relied on flex shrink-to-fit to "hug the 390 phone archetype" (documented intent), but the `.ar-responsive` flex-column card gives no intrinsic width → the flex-item frame collapsed to 0. Portrait worked only because a `body[data-ff="portrait"]` rule sets an explicit `width:100%`; desktop had no width rule. **Fix:** explicit `width: 390px` on the base `.gallery-frame` (the documented phone-modal intent made robust; portrait's `width:100%` still overrides the full-bleed sheet; `max-width:100%` caps narrow viewports). **Verified by use BOTH viewports:** 1440 → card now a centered phone-width modal (was invisible); 390 → full-bleed card unchanged (no regression). ★ THE WHOLE-SCREEN-VERIFICATION LAW PAID OFF: the a11y tree said "renders," the SCREENSHOT said "invisible" — I nearly reported 1440 verified off the tree alone (the tool-face overclaim pattern); the by-sight screenshot caught it.

14. **channelGraph adapter assumes members is an ARRAY but /api/channels serves an OBJECT (→ flag DNA + host-normalized).** Cloning channel-view (breadth #1): DNA's `channelGraph(raw)` edges loop does `(c.members || []).forEach(...)`, but `/api/channels` serves `members` as `{<uuid>:{participation,added}}` (an object) → `.forEach` on an object throws → the whole graph degrades to "couldn't draw." **Host fix (now):** normalize `members` → `Object.keys()` at the channelGraph call site in ChannelView. **Flag DNA (code-grounded):** channelGraph should accept members-as-object (the real /api/channels shape) so the host passes raw, no pre-adaptation.
15. ★ **channel-view graph 0×0 at desktop + dead-space at mobile → FIXED (this fire, mine).** Cloning channel-view, by-sight: at 1440 the dialog opened but the graph SVG was **0×0** (host 697×844, SVG 0×0); at 390 the graph rendered but with large dead space above it. **Trace:** the `.ar-responsive` card applied its desktop 2-COL grid (DNA's explicit warning: "a graph body cuts/right-shifts under 2-col") → the graph slot collapsed → SVG 0×0; and the `.screen`'s fixed 844 device-height left dead space. **Fix (channels.css, mirroring the proven gallery-frame override):** hold the card full-width SINGLE-COLUMN (`grid-template-columns:none; flex-column`) + `.screen.ar height:auto` (size to content) + the SVG a definite `width:600px; max-width:100%`. Verified by use BOTH viewports: 1440 graph centered + sized (was 0×0); 390 graph large + dead-space gone. The 2-col-vs-graph trap = the same class DNA flagged for the decision card — the host holds single-column for any non-card body.

## THE TANDEM LOOP (standing)
Each build/verify fire FORCES real use → harvest every friction here → trace the code → DRIVE the fix (mine) or a code-grounded flag-with-trace to the owner. Projection-owned (#8) I drive; cross-lane (#1-#7) surfaced to the lead-hub with the trace + the proposed fix. The log grows as the fabric is operated — finding the friction IS half the work (the operator-cycle on the tools).
