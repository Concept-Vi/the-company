# THE COMMISSION — master scope + completion register
*The single truth-table the autonomous run drives to and is measured against. Tim (2026-06-20): "run me autonomously, the end of which should have all of those things, well beyond just what I articulated and specified." This enumerates EVERY request (decompressed), its state, and the end-bar. The HOW lives in the boards (linked); this is the WHAT-must-all-be-true. Survives any single context dying mid-run — a relaunched lead reads THIS to know what "done" means.*

> ⚠️ Freshest operational state: MORNING-STACK.md. Grounded build map: THE-FACE-ANCHOR.md. This register is the COMPLETENESS spine over both.

---

## THE FRAME (the laws the whole run obeys)
- **ONE system, many faces.** Every face is a PROJECTION of the one Company resolved against context — never a parallel app. The resolver/invariant is the spine ([[project-invariant-application]], RESOLVER-BUILD.md).
- **Function AND form — and FORM is the hard, neglected half.** "Technical success is only ever half; my use of it is the whole of a half." ★ Tim 2026-06-20, explicit: form is "the most undeveloped part, and the least intuitive for you [AI] to do." It must be IN THE DNA, INTELLIGENTLY DESIGNED — NOT dumping everything into text on a page. So form gets the MOST rigor precisely because it's where AI defaults to failing: DNA leads design; every face element is a composed, navigable, designed surface; a text-wall is an automatic FAIL. No criterion is done on function alone — both faces, by use + by sight, across the device axis.
- **Scope EXPANDS on discovery.** "The purpose of a development methodology is to identify; your finding of things is half the intention." Every gap found becomes scope. This register GROWS — the §EXPANSION log is a feature.
- **Build BEYOND what was specified.** Build every feature/function we can think of — even ones Tim won't use — so he can then connect Claude Design and make it his way. "I can't do that if it isn't already built."
- **Don't stop until intuitively + fully operable.** The end-state = Tim can operationally, intuitively use ALL parts without feeling limited or that any part is incomplete.
- **Autonomous, together, into the centre.** The fabric builds in parallel; accumulation INTO the company centre, never parallel scaffolding ([[feedback-islands-join-mainland]]).
- **Use EVERY capability + figure out how** (the mandate is the figuring-out): the §CAPABILITIES ledger.

---

## THE SPINE — the resolver / invariant-application  ·  board: RESOLVER-BUILD.md
- [ ] ONE invariant `resolve(invariant, coordinate) → surface`; never build variants.
- [ ] The coordinate = orthogonal AXES, NO ranking (device·viewer·mode·perspective·type·intent·resolution·state·posture·register).
- [ ] Express RELATIONSHIPS not CASES — no hand `@media`/breakpoints; every dimension derived from the axis-root by rule.
- [ ] Device-type registry (native-mobile · desktop · portrait · landscape, designed simultaneously); screen-size = the ROOT of calculations; the system auto-allocates.
- [ ] Axes-are-registries (adding an axis = a row). FRACTAL (app⊃card⊃slot⊃token). It's the whole COMPANY resolved against context, not just UI.
- [ ] Built by UNIFYING the seeds (dials + the rule tool + project/the instrument + relative tokens) — not from scratch.

### ★ STRAND: the generative DESIGN LANGUAGE (form-as-data — the form engine)  ·  owner: DNA
*Tim → DNA directive (2026-06-20, surfaced via the channel loop-prep — was NOT in the register). The strand UNDER the resolver that makes "intelligently designed, not text-dumped" REAL. The resolve() verb computes WHICH surface; THIS is the LANGUAGE it composes from.*
- [ ] Build the FULL source (review/ingest, 160 imgs) into the GENERATIVE DNA system — extract the design language INTO THE FACES as MACHINE-READABLE data the models GENERATE AGAINST (not a prose doc). Today: a POC from a thin slice. (coverage running: w7bnhfafd → review/ingest/_extracted/)
- [ ] Built INTO the engine: computed TYPE · derived/bonded PADDING · components→compositions · device resolved NATIVELY · text generated to a BUDGET. Open-ceiling — "the company gets built into this." Every face (FACE 0–4) renders through this language.

## FACE 0 — the atomic-composition engine (the keystone)  ·  board: DECISION-CARD-HOST-CONTRACT.md
- [~] The decision-card archetype renders at-bar through renderArchetype — **GATE-ZERO, in round-4 grind, NOT yet at-bar by lead's pixel-honest sight** (merge-sa). Everything below inherits this engine; nothing downstream is real until it passes.
- [ ] The renderer is archetype-COMPLETE (today 6 of 22 slot_types; molecules/slot_bindings unconsumed) — every face = a record through the one wire, no bespoke React.
- [ ] PROVE-ONE: one decision, composed, on the LIVE surface, across the device axis BY THE RESOLVER (the foundational example of the theorem).

## FACE 1 — the Claude Code integration face / channel + supervisor interface ("THE FACE")  ·  boards: OPERATOR-CYCLE-CRITERIA.md, THE-FACE-ANCHOR.md
*★ THE WHOLE POINT (Tim 2026-06-20): "I'm still just communicating with you guys through the CLI." The face REPLACES the CLI as how Tim works with the fabric. He must be able to FULLY LEAVE — everything below from the face, none of it from a terminal.*
- [ ] Multiple CHANNELS; each a navigable surface (not a menu-wall) — chat-interface base/inspiration, DNA tokens + look.
- [ ] In a channel: CHAT the fabric / the supervisor, or DRILL INTO an individual session.
- [ ] ★ EMBEDDED CLI built INTO the face — the Claude-Code interaction lives in the interface, not an external terminal.
- [ ] ★ TRANSCRIPT HISTORY: visualise AND search ALL of it (over the recall/embedding substrate — the 35,904-chunk index).
- [ ] ★ SESSIONS: search · navigate · find · VIEW them · and RUN them in channels (the supervisor's wake/consult/spawn surfaced as in-channel actions).
- [ ] ★ SEE THE BOARD from the face (the corpus/registries/decision board — visible + navigable, rendered, not raw).
- [ ] ★ THE ADDRESS SYSTEM surfaced (resolve_address: every thing addressable, context+tools per address — the substrate Tim named).
- [ ] TIMELINES + LANES + visuals (the compaction timeline; the fleet as lanes).
- [ ] The SUPERVISOR surfaced: wake (self-heal a closed member w/ context) · consult (fork / past-self) · timeline · lifecycle (cap/watchdog/no-orphan).
- [ ] ★ ARCHITECTURAL — the SUPERVISOR is a LOADABLE BRAIN for the RHM (not just a service: a mind the RHM composes; ties the mind-registry / R13 composable-mind + [[project-collective-cognition]]). Design this relationship, don't bolt it on.
- [ ] Built on the atomic composition / token SLOT+SOCKET system — "done right from the start" so it accretes.
- [ ] Backend: the bridge READ-API (channels/sessions/timeline/stream/transcript-search/board) — render-independent, in flight (fork).
- [ ] Reads files-about-the-face for inspiration (V-SURFACE-MAP, RHM-BRAIN-DISCUSSION); uses recollection tools.

## FACE 2 — the decision system, FULLY done  ·  board: OPERATOR-CYCLE-CRITERIA.md §A
- [x] decision:// typed-resource + take + compose_state + the retract write-path (the twin — un-decide now legitimate, 8408476).
- [ ] Decision = a TYPE attached to CHANNELS (channel-attachment as data).
- [ ] A decision = a SEQUENCE of ≥1 cards (present · explain · choose); the item-TYPE registry (file-discovered).
- [ ] Each step/decision/slide = an ADDRESS carrying its own context + tools (the TOOLS leg on territory_for).
- [ ] The RHM is ACTIVELY there per decision — resolves the relevant context + knows what tools to use for follow-ups; multi-turn engagement over a single decision before it's submitted (NOT static options).
- [ ] The submitted decision writes BACK to the channel as a TYPED RESOURCE.
- [ ] The operator CYCLE: fabric works → channel stacks (presentations · explanations · decisions · verifications) → BLOCKED → Tim clears (decide + verify, function AND form) → RESUME → repeat. ≥2 full cycles by use.

## FACE 3 — the MCP tool-face  ·  board: TOOL-FACE-BUILD.md
- [ ] A beautiful front face on the real ~66-tool MCP — click a tool, click options, enter inputs, RECEIVE responses.
- [ ] NO developer UI, non-text-heavy, NO raw anything — everything rendered beautifully.
- [ ] Every tool has an embedded DESCRIPTION — informative + pleasant (the human-meaning layer).
- [ ] The gate: fail-closed, posture-scoped, operator-attributed writes (#1b couples here).

## FACE 4 — pilot mode (open-world) + chains/graph  ·  board: PILOT-TOOLFACE-BUILD.md
- [ ] EVERY tool exposed for pilot mode — DYNAMIC loading (not handcrafted, not static), auto-in-sync with any backend change (single-place-dynamic).
- [ ] Open-world game-design interface — tools/chains/graphs as objects in a navigable world (perspectives; RHM companion).
- [ ] CHAINS + GRAPH execution — MAKE then RUN runs on LOCAL models, in pilot OR standard mode (mine the old dev UI's graph loop; the one backend gap = expose save_cascade/run_cascade to the browser).
- [ ] All token/registry/resolver-driven. An ideas-board for pilot mode.

## CROSS-CUTTING — one system, documented, connected
- [ ] LINK all faces (this + MCP-tool-face + pilot + operator-cycle + prior faces RHM/gallery/builder/greeting/forager) — different faces, ONE system; navigable between them.
- [ ] NATIVE desktop AND mobile, mobile landscape AND portrait — the device axis, resolved not enumerated.
- [ ] DOCUMENTED so Claude Design auto-designs/builds in the right grammar (the token-slot contract + the FORM gate).
- [ ] CONNECTED: the face joins the channel; exposed to Claude Design; the GitHub repo connected (the connector — Tim-driven session finalizing OAuth now).
- [ ] Board RESOURCES with requirements + quality criteria + what's-used-from-where (this register + the boards).
- [ ] FUNCTION AND FORM verified on every criterion — by use + by sight, both viewports/the device axis, generated-through-the-system (no stubs).

---

## §CAPABILITIES LEDGER — use every one; figure out how (the mandate)
*Tim: "it is up to all of you to figure out how to use them... actively find new ways to use them and learn about using them." Notes → THE-FACE-ANCHOR.md §6 (skills/chains/tips).*
- session brain loadout · company + orbit content · recollection system · session histories · embeddings (+ embed more) · local models (+ memory about them) · **ollama CLOUD models from Claude Code** (cheaper, near-equal — recipe found, verify-by-use before the unattended fleet) · drift detectors · channels · git. Take notes of wanted skills, chain ideas, tips as we learn.

## §EXPANSION LOG — gaps found = scope (grows as we go; finding is half the intention)
- decision_retract write-path was missing end-to-end → built (8408476). [done]
- bridge has zero session/channel/timeline read-routes for a UI → scoped (fork).
- renderer is decision-card-complete, not archetype-complete (6/22 slot_types) → scoped (DNA).
- device axis is hand-enumerated (3 modules), not resolved → scoped (resolver lane).
- two un-unified channel systems (live cc_channels vs dormant) → needs Tim's canonical call + Islands-Join fold.
- TOOLS leg missing from territory_for (addresses don't carry "what tools for follow-ups") → scoped.
- summary-leak (266 machine summaries → 0) → closed + verified. [done]
- phone surfaces not in boot sequence → added (4c6bc05). [done]
- ★ TOOL-FACE NOT NEAR-READY — gated on the FORM-META layer (projection, default-to-wrong by-sight, 2026-06-20): /api/tools returns form_meta NULL / opField 0/66 / human labels 0/66 — the live panel @390 renders a WALL of raw machine knobs ("Frm", title-cased "Cc Channel", raw docstrings) — operator-law violation. The RUN-DOOR swap (corpus→/api/tools/invoke) IS focused + render-independent (FUNCTION now). But LEGIBLE render = a 4-lane surface: ATLAS (lead, running) sources per-tool human label/desc/op-labels → tool_meta.json · fork's /api/tools CARRIES form_meta · projection consumes (opField filter + labels) · DNA tool-card renders (keystone-gated). Corrects the earlier "door render-independent now; face after keystone" → the FACE also needs the form-meta layer first. [door now · form-meta with the atlas · render after keystone]
- ★ FACE 3 ANCHOR FOUND BY USING THE TOOLS (corpus query, 2026-06-20 — Tim: "there might be a lot more than you are aware of" — there was): `build-prep/cognition-self-improvement/MCP-DESIGN-PRINCIPLE.md` is the AUTHORITATIVE tool-face law ([[project-mcp-is-top-priority]]), not in the build's frame until now. It RESHAPES FACE 3: (1) ★ CONSOLIDATION AUDIT — 61 flat tools → ~25-30 RESOURCE-shaped (corpus 4→1, create 8→1, marks 4→2, runs 5→2, node/rule…); a tool per NOUN + a small op-enum, "never add a flat tool, add an op." ⟹ the tool-face renders the CONSOLIDATED ~30 resource-tools, NOT projection's 66 flat ones. (2) the human-meaning layer projection needs IS the law's "prompt-engineered descriptions + meaningful-context-not-IDs + detail=concise|detailed" — the atlas produces exactly this. (3) ★ "TWO FACES, ONE COMPOSABILITY": MCP(agent) ⟂ Surface/RHM(human) — getting the MCP right makes the UI intuitive BY NATURE; the atlas/descriptions serve BOTH. (4) THREE CONSUMERS, design for the COLDEST (external agent) → the floor is a hard SECURITY boundary (ties the connector full-access: identity-gates the floor). (5) INTUITIVENESS is EVAL-DRIVEN (fresh-agent friction = the gap list), not asserted. ⟹ FACE 3 = consolidate-then-surface, anchored on this law. Owner: projection + DNA + the tool-atlas.
- ★★ FULL-COVERAGE (WASTEFUL) RECALL — THE DRAGNET (Tim reframe, 2026-06-20, high-value): the local concurrent models' KILLER use is FULL COVERAGE, not specific search. Push ALL corpus+transcript data through 32-concurrent local models, each judging "relates?/how?"; the 99%-no is the coverage PROOF, not waste. Local=free → be WASTEFUL → wastefulness IS the feature (buys certainty). THE FLIP: spear (sampled, misses, recall-precision-bug-prone) → dragnet (exhaustive, never-misses). It's the MAP in MAP-REDUCE (dragnet=MAP on local, judgment=REDUCE central — the extraction-vs-judgment law), MOOTS the recall-floor problem for coverage (reads content, not embedding-precision; embeddings RANK / dragnet NEVER-MISSES — use both), and makes RECALL-BEFORE-BUILD real (find EVERYTHING said+existing before writing a line → true anti-duplication, company-as-ONE-entity). Generalizes: any find-all-X-in-a-huge-pile (dupes/gaps/relations/drift) is a dragnet. ★ PROVEN by-use small-scale (recollection, 64f9f37): run_items fans 1-role×N concurrent on chat-4b — 20 chunks → 2.1s, 20/20 clean structured JSON (relates/how), correct 12/20 with accurate 'how', ~10 chunks/s, free. The 32-concurrent local dragnet is REAL today. ★★ THE NEUTRAL-JUDGE LAW (critical finding caught INSIDE the proof): "be wasteful / 99%-no" is an AGGREGATE OBSERVATION of the output, NEVER an instruction to the per-chunk judge — a prejudging prompt ("expect most to NOT relate") biased the judge to 0/20 FALSE NEGATIVES, turning the never-miss dragnet into a MISS-MACHINE. The per-chunk judge asks ONLY genuine topical overlap, ZERO prior on the answer rate. (Tim's confirming-bias concern, recursive.) ★★ REFINED to its correct form (recollection 2e910e5 = Tim's design exactly): even a NEUTRAL binary relates?-worker has a local-4b NO-LEAN → conservative → MISSES borderline (a worker that FILTERS can miss). FIX = split MAP into EXTRACT+DETERMINE: WORKER = neutral TOPIC-FREE extraction ({about, touches} — describes, NEVER filters, no topic/no relevance/no prior → CANNOT miss) → DETERMINE = central, per-topic, reads the cheap extractions to decide relevance. RESULT: misses 21→8 (residual = keyword false-positives, not real content), caught 45 vs keyword's 25 (20 semantic catches keyword missed) — never-miss restored. ★ THE PRIZE — EXTRACT-ONCE / DETERMINE-MANY: the extraction is topic-free → extract the corpus ONCE into a reusable structured ASSET (~7/s on local-4b free; full 35,904 ≈ 85min, overnight-feasible — "wasteful is the feature"), then ANY future question = a cheap central DETERMINATION over the stored extractions. A new corpus LAYER, not a per-question re-read. SHAPE: extract(worker, neutral, once) → determine(central, per-topic) → reduce(synthesize). Runnable on local NOW; fork's think-fix opens cloud for big-ctx. ★ FIRST RUN self-referential: dragnet the collective session history to RECOVER the design depth Tim remembers (structured-output shapes · allocation · what-prompts · feeding — "a lot discussed before"). Owner: recollection (mine) + fork (engine). Sharpens §CAPABILITY-USE PLAY standing-practice #1 + the local-models playbook. [[project-full-coverage-recall]]
- ★ COGNITION TOOLS ACROSS ALL PROVIDERS (Tim-direct to fork, 2026-06-20): the cognition tools must work across EVERY provider with settable params (structured-output · prompt · context · output-location · THINK · tools) — "the code does it in the back." ROOT CAUSE of the cloud-empty-structured bug (fork, evidenced): reasoning models burn the whole max_tokens on the hidden reasoning trace before emitting JSON → truncated → empty (proved: 462 completion tokens for a 1-word answer). FIX: make `think=false` actually function (native ollama /api/chat honors it; /v1 silently ignores) → ~5 tokens of direct JSON + budget-retry safety net + a built/unwired/absent map of the 6 params. ⟹ resolves cloud structured-extraction → BOTH substrates real (local-4b serving :8000 + cloud-with-think). Owner fork, render-independent, hot-path cognition.py/transport.py/fabric (sole-writer, no race). [building]
- ★ RECALL SUBSTRATE — 3 confirming-bias bugs caught by default-to-wrong (recollection, 2026-06-20; the recall half EVERYTHING leans on): (1) ★ NO RELEVANCE FLOOR — cosine always returns top-k so "non-empty" proved nothing; off-corpus noise (~0.31) presented identically to real hits (~0.47); zero honest-no-match → a no-silent-failures violation silently grounding decisions/builds on irrelevant content. FIX (building): a CALIBRATED relevance floor → honest-no-match below it (calibrate on enough samples, NOT a guessed 0.40). (2) FRESHNESS LAG — recent sessions not re-exported/indexed → "search ALL history" currently = "search the INDEXED (older) corpus" (honest qualifier; ties the reindex job). (3) VARIABLE GROUNDING — recall_for_decision grounds on-point for well-represented topics, generically for off-represented ones (ties embedding-more / #65). Owner: recollection, render-independent. This IS gap-surface-to-channel's first real use.
- ★ MULTI-SCALE ROLLUP EMBEDDINGS — present-as-files but NOT QUERYABLE (recollection, verified by probe 2026-06-20): query_corpus(space='scale_history_k8') → "index EMPTY"; population uneven (history has k4/k16/k64, not k8). The "zoomable memory" capability needs WIRING (build the queryable index + even population) before it's a face feature — was wrongly assumed ready. Caught by exercising-not-describing. Owner: recollection. [scoped, render-independent]
- ★ CONNECTOR — FULL ACCESS FOR THE AUTHENTICATED OPERATOR (Tim 2026-06-20): the remote gate becomes CAPABILITY-BY-AUTHENTICATED-IDENTITY, not per-tool-posture. Authenticated-as-Tim (connector OAuth + the #1b operator-token proving genuineness) → FULL tool access, all postures, like the local operator-face. Any other / unauthenticated principal → STILL deny-by-default, fail-closed. The per-tool restriction becomes Tim's VISIBILITY TOGGLE (show/hide by preference), NOT the security wall. ★ RESOLVES the #1b / exposure-shape fork — Tim gave the intent: identity is the gate; full access on proven-Tim. ★ CONDITION (load-bearing, Tim's words "so long as it is secure"): implement identity-gated, VERIFY the auth is unspoofable/unbypassable, THEN enable full — NEVER a blanket removal (that would expose dangerous tools to anyone reaching the endpoint). Owner: fork (gate/security) + the connector session (auth). Couples to the connector OAuth (in flight). [assigned, sequenced after the auth is proven airtight]
- ★ CONCURRENT-COMMIT COLLISION (Tim 2026-06-20): under no-branches/commit-to-main, N members appending to shared files (the board, register, gap-notes) + committing concurrently RACE on git's index lock → interleaved/lost appends. → build a CONCURRENT-APPEND/COMMIT QUEUE (a write-serializer): members ENQUEUE {file, append_content} or {paths, message}; a single DRAINER applies FIFO under one lock (append atomically → git add → commit), one at a time. Appends "just queue" (the common case). FAIL-LOUD on conflict (Notice + gap note, never silent-drop). Makes "everyone commits to the same fire" safe — the precondition for the heavy concurrent breadth-build. Owner: fork (git/store/infra). Render-independent; land BEFORE many lanes commit to main at once. [assigned]
- *(append every new discovery here)*

## §END-BAR (when the run is allowed to call itself done)
Tim can sit down (desktop or phone, any orientation) and **operationally, intuitively use every part** — channels, the supervisor, decisions end-to-end with the active RHM, the tool-face, pilot mode with real chain/graph runs on local models — **without feeling limited or that any part is incomplete**, all of it function AND form at-bar by our own comparison, generated through the one system, connectable to Claude Design. Not before. And beyond what was specified — the features we added that he didn't ask for are part of the gift.
