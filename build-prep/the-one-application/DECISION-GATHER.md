# THE DECISION GATHER — the operator-cycle's first real payload
*Kicked 2026-06-21 (composition requested, lead kicked). EVERY lane appends its open decisions here; recollection dragnets the corpus for pending decisions. The assembled set does DOUBLE DUTY: (1) the decision-surface's FIRST REAL CONTENT — Tim's actual pending decisions, rendered as cards, the operator-cycle's first genuine run (works→stacks→Tim clears→resumes on REAL decisions), and (2) the source the SUBTYPE vocabulary DERIVES from (composition clusters → the kinds + each kind's required elements — never invented). Be expansive: both genuine Tim-direction calls (his eye/strategy/security) AND unresolved cross-lane fabric-calls.*

> Row shape per decision: **WHAT** (being decided, human) · **OPTIONS** · **OWNER** (Tim-direction = needs his eye/strategy/security · fabric-call = a cross-lane choice the streams settle) · **BLOCKS** (what's waiting on it). Append your lane's section below.

---

## composition (ch-o1wy1t07) — open decisions

**C1 · SUBSTRATE-HOME (#75)** — `Tim-direction`
- WHAT: the ONE Supabase substrate shape — the factory component-registry + decision:// + the Claude-Design connector-edge + the #75 address-space + DNA's substrate-graph + the coverage-store are ALL one substrate (parts/projections), never parallel stores.
- OPTIONS: (a) one centre substrate; every store is a projection of it (the converged candidate) · (b) some stores stay separate · (c) Tim's own master-scope structure.
- OWNER: Tim (his #75 master call; tied to his 5 held substrate decisions).
- BLOCKS: factory-persistence (Supabase-as-truth, not localStorage) · schemas-as-truth · the whole substrate convergence + the coverage mechanism.

**C2 · MERGE / SA-WRITE AUTHORIZE** — `Tim-direction (security)`
- WHAT: authorize the fabric to write back as a service-account (the SA write) so the keystone generate→write can persist generated assets / decided states.
- OPTIONS: (a) authorize, identity-gated (the connector #1b proves genuine-Tim; full on proven-Tim, deny otherwise) · (b) hold read-only until the auth is proven airtight.
- OWNER: Tim ("so long as it is secure" — verify unspoofable, then enable).
- BLOCKS: the keystone generate→write (the AI builds an asset → write-back) · the factory save-back · the gated-floor's live write leg.

**C3 · CLAUDE DESIGN adoption + connector** — `Tim-direction`
- WHAT: adopt Claude Design as the face-design tool + make the company-MCP a connector so it participates in the channel (seed in-channel, design back in-channel).
- OPTIONS: (a) adopt + connector (Tim indicated yes; connector in flight) · (b) defer.
- OWNER: Tim (an external-tool commitment).
- BLOCKS: the Face-Pipeline translator BUILD (the face-design inlet) · the one-asset round-trip prove-on-one.

**C4 · TRANSLATOR / generated-instance STORAGE** — `fabric-call (composition+fork)`
- WHAT: where GENERATED decision/asset instances live long-term (the keystone generate→write target; NOT .py — generated, not authored).
- OPTIONS: (a) the centre's addressed-record store (the territory_write path) · (b) cc_board (if decisions register cleanly as a board item-type) · (c) a substrate record store.
- OWNER: fabric — fork owns the storage; composition owns the type-shape. (Couples under C1/#75.)
- BLOCKS: the keystone generate→write contract.

**C5 · ISLAND-SCHEMA removal (islands-join completion)** — `fabric-call (composition+fork)`
- WHAT: remove the vi-visual-dev island copies of the FACE-2 schemas once fork wires decision_registry to the company-canonical (company/schemas/vi-vision/v1/).
- OPTIONS: (a) remove on fork's wire-confirm (single-source, no parallel) · (b) keep a mirror.
- OWNER: fabric (composition removes on fork's confirm).
- BLOCKS: the islands-join clean single-source (no permanent duplicate).

**C6 · ARCHETYPE-RENDER dispatch** — `fabric-call (composition+DNA)`
- WHAT: is the archetype loud-lookup dispatch (exact→family→bare→UNDECLARED-loud) DNA's existing renderArchetype, or a separate composition registry?
- OPTIONS: (a) renderArchetype IS the dispatch — confirm, no parallel (likely) · (b) a composition dispatch registry.
- OWNER: fabric (composition+DNA confirm).
- BLOCKS: nothing critical — a no-parallel clarity check (so the archetype catalog dispatches through one place).

---
<!-- other lanes: append your section below (## <lane> — open decisions) -->

## fork (ch-8djrpmsl) — open decisions

**F1 · THE 4TH ROOT / what "dimension" IS** — `Tim-direction (theorem)`
- WHAT: the instrument must drop through dimensions (3D↔2D↔1D); but Tim's written math treats k as recursive DEPTH + "dimension" = the AXES, not spatial 3D/2D/1D. So: what IS the "dimension" the instrument drops through?
- OPTIONS: (a) same knob — k IS the dimension · (b) two knobs — spatial-d separate from depth-k · (c) dimension is a STANDPOINT on the one object (viewpoint-duality applied to dimension). + the 4th coordinate root itself: type/state/instance/influence (Tim's own namings split). [grounded: EXPLORATION-dimension-bridge.md — the "constant"-reading is DEAD; this is the live seam]
- OWNER: Tim (his theorem; the bedrock is his — the AI-projections err, proved by the cube-error correction).
- BLOCKS: the CUBE INSTRUMENT build (its dimensional behaviour — the heart) · the resolved-axis-set.

**F2 · THE CUBE 3-D FORK** — `Tim-direction (theorem)`
- WHAT: how the cube becomes 3-D in the instrument.
- OPTIONS: (a) render it as its 2-D projection-from-each-vantage (projection-IS-the-math; the lean) · (b) orbit the existing wheel in CSS-3D (real orbit, zero rebuild) · (c) true 3-D (new WebGL layer). Engine = react-three-fiber (web-native, phone-reachable; UE5/Omniverse rejected = video-stream not live).
- OWNER: Tim (his instrument; F1 likely settles this shape).
- BLOCKS: the instrument render-substrate choice.

**F3 · CONNECTOR #1b full-access FLIP** — `Tim-direction (security)`
- WHAT: enable full remote tool-access gated by authenticated-identity (proof-of-Tim = OAuth principal + the #1b operator-token); everyone else deny-by-default.
- OPTIONS: (a) verify-unspoofable-then-enable (build the identity gate, prove non-Tim/replay/peer all fail, Tim flips full) · (b) hold.
- OWNER: Tim-DIRECT (security/credential = his word, not peer-relay; "so long as it is secure"). The connector server is built+verified+committed; the FLIP is his.
- BLOCKS: the operator's full remote tool-face (locked/hazard tools via the connector).

**F4 · MODEL-SOURCE / `_CORPUS_REGISTRIES` mind row (R13 follow)** — `fabric-call (fork+lead)`
- WHAT: whether to add a model-kind / corpus-source mind row to the (complete, green) R13 composable-mind substrate — the recall-as-a-source for the supervisor-as-brain.
- OPTIONS: (a) R13 complete as-is, this is a follow that pairs with the supervisor-brain build · (b) in-scope now (then: what it binds + what running it means — needs run_composition extension for a non-role leg).
- OWNER: fabric (lead scopes; fork builds). [fork's lean: (a) — a follow, not a now-piece]
- BLOCKS: nothing urgent (the supervisor-as-brain arch is specced; the substrate's ready).

**F5 · vLLM enable_thinking (think-fix local half)** — `fabric-call`
- WHAT: wire vLLM-local think-control (chat_template_kwargs.enable_thinking) — the ollama/cloud half is built+verified (200× token-drop); the local half is an honest no-op until wired+verified.
- OPTIONS: (a) wire when a free vLLM model is available to verify the token-drop · (b) leave — cloud think + local no-think-models already cover structured extraction.
- OWNER: fabric (gated on a free vLLM model = lead's loadout call).
- BLOCKS: think-control on local vLLM specifically (cloud think + the budget-retry net already cover the structured-output gap).

**F6 · prompt-TEMPLATE wrapper + SCHEMA-select (resolved-slots §5 follows)** — `fabric-call (fork+composition)`
- WHAT: the resolved-slots flagged-follows — the {{name}} prompt-template-interpolation wrapper + output_schema select-between-classes.
- OPTIONS: (a) build when a real consumer needs continuous prompt-composition / per-coordinate schema (select + literal-superset cover v1) · (b) build ahead.
- OWNER: fabric (fork builds on a real need; composition's contract carries them as flagged-follows).
- BLOCKS: nothing now (the v1 select-path is built+verified; these are the continuous-composition extension).

---

## lead (ch-3mpkjg3r) — open decisions

**L1 · CONNECTOR funnel exposure (the last-mile)** — `fabric-call → Tim-touch if :443`
- WHAT: the full-access connector is built+verified+funnel-up on :10000, but external routing is flaky (Tailscale edge-propagation incomplete; cloud tests bounce cert-reached↔refused).
- OPTIONS: (a) hold on :10000, let edge-propagation settle + Tim's app-connect is the live test · (b) move the funnel to :443 (reliable port) — evicts the company-UI serve from :443, touching Tim's UI-access URL · (c) a path-mount alternative.
- OWNER: fabric drives (a); (b) touches Tim's UI access → flag him before moving.
- BLOCKS: the connector going reliably live for ChatGPT/Codex/Claude.

**L2 · /MCP RECONNECT timing** — `Tim-direction (the single action only Tim can do)`
- WHAT: the staged MCP-TOOL changes (fork's corpus-ops + recollection's corpus-ops MCP-live) need a /mcp reconnect to go live — the one thing the lead's restart-authority can't cover.
- OPTIONS: (a) batch ALL MCP-side changes + ask Tim once at a coherent checkpoint · (b) ask per-change.
- OWNER: Tim (runs /mcp reconnect or a fresh claude).
- BLOCKS: the corpus-ops MCP tools going live (the HTTP-route wave is already live via the bounce).

**L3 · FORM / generator design-direction** — `Tim-direction`
- WHAT: the visual design-direction Tim is working via ChatGPT (generator, material-density, real-artefacts-vs-stylised, "indistinguishable from real") — the form half DNA renders against.
- OPTIONS: (a) DNA drives build-priority #1-6 against Tim's stated direction + the artefact-fidelity bar (current) · (b) Tim refines specific calls as renders surface.
- OWNER: Tim (his eye/direction; DNA executes).
- BLOCKS: nothing hard (DNA's driving); the final fidelity is Tim's "feel great".

**L4 · KEYSTONE final lock ("does it feel great")** — `Tim-direction`
- WHAT: the merge-sa keystone is soft-locked (Tim validated direction); the final felt-quality bar is Tim's by-sight call, never agent-green.
- OPTIONS: (a) DNA keeps lifting to fidelity → Tim gives the by-sight nod on review · (b) lock now at current fidelity.
- OWNER: Tim (the felt-quality bar).
- BLOCKS: calling the keystone "done" (the rendered breadth already generalizes from it).

---

## recollection (ch-ouui7r0k) — corpus-dragnetted open decisions

*The dragnet's first real use on a build need: full-coverage over the 44k-extraction recall layer (session+visual-dna+theorem) — 274 kind='decision' extractions + the located pending-decision LIST chunks (read from raw, since the lists were coarse-classified 'log' → not claim-extracted). ★ HONEST HEADLINE (default-to-wrong): the corpus's pending-decision lists are largely HISTORICAL or CROSS-PROJECT (Vi-Chat variable-system, AGENTS.md) — few NEW open COMPANY-FACE decisions beyond what's already in the registry (the 7 cards) + the lanes' sections above. The dragnet's value here = confirming the open Company-set is largely already captured + surfacing a few maybe-open ones to TRIAGE (not padding the gather with resolved/other-project decisions). Each row tagged with my triage.*

> My own substrate decisions (file/cluster/event/substrate-home/rerank-loadout) are RESOLVED this session (decision_take recorded) — NOT re-listed; substrate-home(#75) = composition's C1 (Tim-direction, awaits his master call) + merge-sa = C2.

**R-corpus-1 · VERSION-CONTROL the walkthroughs + methodology skills** — `fabric-call` · TRIAGE: maybe-open
- WHAT: the walkthrough artefacts + methodology skills live in gitignored `.build/` + `~/.claude/skills` — bring them under version control (the company tree) or leave out-of-tree?
- OPTIONS: (a) adopt into the company tree (durable, shared) · (b) leave in .build/skills (ephemeral). 
- BLOCKS: durability/shareability of the methodology; minor. (corpus c87002)

**R-corpus-2 · `decision_build=AUTO` consent model** — `Tim-direction` · TRIAGE: LIKELY-RESOLVED (build-freely + git-revert model settled it; verify)
- WHAT: confirm the autonomous-build consent posture (auto-build vs per-build-consent).
- OPTIONS: (a) AUTO (build freely, git-revert is the safety — the current operating model) · (b) per-build consent.
- BLOCKS: autonomous build loops. (corpus c87002 — appears settled to AUTO in practice; flag for explicit confirm.)

**R-corpus-3 · WHETHER/WHEN to merge branches** — `fabric-call` · TRIAGE: LIKELY-RESOLVED (no-branches-in-company rule = commit-to-main)
- WHAT: branch-merge policy. OPTIONS: (a) commit-to-main, no feature branches (the stated company rule) · (b) feature-branches + merge.
- BLOCKS: nothing currently (main-line is the practice). (corpus c87002)

**R-corpus-4 · DESIGN-SYSTEM location + state** — `n/a` · TRIAGE: RESOLVED (the Obsidian Builder vault / the Gallery is canonical; DNA owns it)
- Listed for completeness; the corpus surfaced it as open but it's since settled. Not for the cards.

**R-corpus-5 (CROSS-PROJECT, not Company-face) · Vi-Chat variable-system decisions** — TRIAGE: OUT-OF-SCOPE (Vi-Chat project, not the Company operator-surface)
- The corpus holds 4 Vi-Chat substrate decisions (ship `computed` resolver-kind? · Vi-authored-var visibility scope · can-Vi-edit-Tim-vars · Character-Panel+Codex) + the 18-feature register addition + the AGENTS.md A/B/C verdict fork. These are Vi-Chat / a different project's decisions — surfaced by the dragnet but NOT the Company operator-cycle's payload. Flagging so composition's cluster doesn't pull them into the Company subtype vocabulary. (corpus c92148/c93741/c99931)

★ NET (recollection): the genuinely-open COMPANY decisions are already enumerated — the 7 decision cards (5 resolved, merge-sa + rerank-loadout posture) + the lanes' C/F/D rows above. The corpus adds R-corpus-1 (maybe-open) + 2 likely-resolved-confirm + a cross-project set to EXCLUDE. The dragnet confirms coverage (no big hidden trove of open Company decisions) — which is itself the useful result: the gather above IS ~complete.

---

## DNA (ch-z4ht5ipb) — open decisions

**D1 · SYNTHETIC RICHNESS CEILING** — `Tim-direction`
- WHAT: how far the generator renders artefacts itself (SVG-material, token-driven) vs where photoreal-GRADE generated assets take over. Tim's bar = "synthetic indistinguishable from real" (2026-06-21). The whole artefact family is now lifted line-art→material (golden-hour spaces, real blueprints, warm cards), token-only SVG. The ceiling: SVG-material plateaus below true photoreal.
- OPTIONS: (a) SVG-material carries structure/density everywhere + composition's factory GENERATES photoreal assets for hero "look-at-this-space" content (a made-asset, not stock) · (b) push SVG further (texture/noise/layering) before reaching for generation · (c) all hero imagery = generated assets, SVG only for diagrams.
- OWNER: Tim (his indistinguishable-from-real bar) + composition (the asset factory).
- BLOCKS: how rich FACE-1 + decision hero visuals get; the render-vs-asset-generation division.

**D2 · KEYSTONE FLOW-VISUALS: bespoke vs the organism family** — `Tim-direction`
- WHAT: the approved keystone two-panel uses BESPOKE inline SVG narrative visuals (library→authorize-octagon→library). The artefact organism family is now material. Adopt organisms, or keep the bespoke narrative (which carries the authorize-octagon semantics)?
- OPTIONS: (a) keep bespoke for narrative decisions, organisms for artefact-density decisions (screen picks per content) · (b) rebuild keystone visuals on organisms · (c) hybrid: organisms substrate + narrative overlay.
- OWNER: Tim (approved screen — don't regress).
- BLOCKS: whether the decision surface fully inherits the family.

**D3 · SESSION-CARD facts/summary SHAPE** — `fabric-call (DNA+recollection+projection)`
- WHAT: session-card binds a `facts` list + `summary` the HOST shapes from raw session fields. What goes in facts, who computes it?
- OPTIONS: (a) projection shapes facts from /api/sessions + recollection supplies recall-derived lines (state/last-active/role + catch_up/open_loops) · (b) fixed facts template from raw fields only.
- OWNER: fabric (DNA renders; projection+recollection supply the shaped record).
- BLOCKS: the LIVE session-card mount (archetype registered + binds; awaiting the shaped record).

**D4 · DECISION card_variant SET (subtypes → variants)** — `fabric-call (composition derives, DNA renders)`
- WHAT: which decision subtypes need distinct card_variants. Built: binary TWO-PANEL. Unbuilt: N-panel (3+), workflow/sequential, ranking, allocation, single-confirm.
- OPTIONS: derived from THIS gather's clustering (composition owns derive) → each subtype a variant.schema; DNA renders each.
- OWNER: composition derives; DNA renders.
- BLOCKS: rendering the gathered decisions in their right forms.

**D5 · N-PANEL / WORKFLOW archetype shape** — `fabric-call (DNA+composition)`
- WHAT: how the two-panel generalises to 3+ panels and sequential workflows (build-priority #5).
- OPTIONS: (a) N-panel = the compare engine parameterised by count + scroll/stack reflow · (b) a distinct stepped workflow archetype · (c) both as separate rows.
- OWNER: fabric (DNA renderExplained + composition catalog row).
- BLOCKS: non-binary decisions rendering richly (3+ today falls back to the ledger list).

**D6 · RESPONSIVE: @media interim → computed resolver** — `fabric-call (DNA+projection)`
- WHAT: when to replace phone.css @media breakpoints with projection's device-axis RESOLVER (computed from screen-size; build-priority #6, the anti-@media directive).
- OPTIONS: (a) resolver supersedes @media surface-by-surface · (b) keep @media until resolver covers all faces, then cut over.
- OWNER: fabric (projection owns resolver; DNA owns the layout rules, tokenised in tokens.json `layout`).
- BLOCKS: native device/orientation resolution.

**D7 · --dna-indicate (bright-gold addressable token)** — `fabric-call (DNA)` (low)
- WHAT: mint gold_pop (#E3C421, the reserved bright focal) as a named DNA SEMANTIC (--dna-indicate) so the "one focal" is addressable by meaning.
- OPTIONS: (a) mint now · (b) defer until a second consumer needs it.
- OWNER: DNA (no Tim needed).
- BLOCKS: nothing critical.

**C6 answer (DNA confirms):** renderArchetype IS the dispatch — PROVEN: the session-card (a NEW archetype) rendered through existing renderArchetype + slot_map with ZERO new render-code (just a layouts.json row). No parallel composition dispatch needed.

---

## projection (ch-j9t020dx) — open decisions
*(my session-drill-shape decision = DNA's D3; the @media→resolver = DNA's D6 — not re-listed. Below = the projection-lane decisions not yet captured.)*

**P1 · CONTROL-DENSITY / EXPERTISE axis** — `Tim-direction (+ composition)`
- WHAT: should expert controls (the lens semantic knob-row, the tool-face advanced params) RESOLVE against a viewer-expertise coordinate — novice→lens+plain-controls, pilot→full knob-row — vs always-shown or a hardcoded "advanced" panel?
- OPTIONS: (a) a resolved AXIS (control-density resolves against viewer.expertise — the resolver-philosophy fit; lead-banked) · (b) always-show all knobs · (c) a hardcoded advanced toggle.
- OWNER: Tim-direction (product-shape; his pilot-wants-control vision) + composition (the axis). viewer.expertise value-source pending (no operator-mode state yet).
- BLOCKS: the resolver's expertise axis · the lens chip-wall legibility (cold-stranger-vs-pilot tension).

**P2 · V-ICON SWAP (placeholder → real V)** — `Tim-direction`
- WHAT: the RHM/V icon is a swap-seam PLACEHOLDER; when/with-what does the real V component replace it?
- OPTIONS: (a) Tim provides the real V → swap (seam preserved) · (b) a fuller interim V now · (c) after FACE-1.
- OWNER: Tim-direction (the real V is his).
- BLOCKS: nothing hard (the host preserves the swap seam; the supervisor-brain plugs the same seam).

**P3 · TOOL-FACE posture-gate (non-safe tools from the UI)** — `fabric-call (couples F3/#1b)`
- WHAT: should non-safe-posture tools (consent/hazard/write) be RUNNABLE from the tool-face once the #1b operator-token gate exists, or stay UI-gated (the honest 'gated' state shipped now)?
- OPTIONS: (a) gate-then-run (the #1b token authorizes → UI runs them) · (b) stay UI-gated until #1b proven · (c) per-posture (safe runs · design previews · consent/hazard need the token).
- OWNER: fabric (couples fork's #1b / F3 connector-security); projection wires the surface behavior.
- BLOCKS: the tool-face's write/mutating tools (safe/read run now; the rest show 'gated').

**P4 · DIALS op=str → selector (Literal vs surface-inject)** — `fabric-call (fork or projection)`
- WHAT: the `dials` tool declares op_labels (form_meta) but its schema `op` is a free string (no enum) → no op-selector renders. Fix at the tool (op→Literal, fork) or the surface (inject enum from op_labels keys when the discriminator lacks one)?
- OPTIONS: (a) fork: make dials.op a Literal (clean source-fix; form_meta+inference then work) · (b) projection: inject enum from op_labels in applyFormMeta (a general surface fallback for any op_labels-without-schema-enum tool).
- OWNER: fabric (lead routed to projection; my lean: fork's Literal is the clean fix + I add the surface-inject as a general fallback).
- BLOCKS: the dials op-selector; small instance but the pattern recurs for any str-op tool with op_labels.
