# HARVEST — wildcard (the direction circuit)

**author_session:** ch-piffgfxv (work-identity) / ch-869a5yzl (post-reboot live handle) — SAME session.
**territory:** vi-visual-bridge (read) → company centre (built). **role:** the direction circuit / verbs / the TAKE + the completion-refuter.
**bar:** every record tagged `verified` (by-use, not self-certified) | `attempted-unverified` | `broken` | `abandoned` + WHY. No self-certify — an honest "not done" beats a poisoned "done."

---

## WHAT I AM (about/kind/summary)
- **about:** how Tim DIRECTS — the circuit (push structured content → he directs at any granularity by recognition → routes back → re-render → loop), as opposed to the four layers that BUILD. **kind:** perspective + built-artifacts + adversarial-findings.
- **summary:** entered via a 4-month genesis read of vi-visual-bridge for its generating function. Found ONE thing under the patchwork: the direction circuit. It became the union's first product (the decision surface), my built take-leg is its closing rung, and the reading's discipline became the company's completion-gate.

## CLAIMS (each tagged — verified by-check this harvest, not from memory)

1. **The keystone finding: vi-visual's modes are projections of ONE system; direction is element-bound, keyed by ADDRESS not markup.** `verified` (genesis read + 7,141-snapshot data: 100% of 1,878 real annotations element-bound; ONE element-walker serves all modes, server.py). This is the load-bearing claim — everything below derives.

2. **HOLE-1 fixed: territory_write canonicalizes decision_take/retract before mark+signal.** `verified` (by-use: bare decision://d-42 → canonical decision://global/d-42; canonical unchanged; comment untouched. Committed 5b7acbb, re-confirmed in tree this harvest: 3 markers present). Closed the L3 silent-resume-miss class (was canonical-input-only).

3. **DECIDE verb composes through DNA's renderArchetype — NOT bespoke.** `verified` (by-check: archetype.js wires window.galleryBinder.decide at 6 call-sites; binder mounted in gallery, loaded index.html). The two segments met at the address/verb contract without a coordination round.

4. **gallery:verb envelope + decide() take contract + canonical-shape guard.** `verified` (use-test TEST7/8/9: gallery:verb emits verb-discriminated; decide() emits fork's decision_take contract; guard fails-loud on non-canonical, emits on canonical). In wildcard-gallery-binder.js.

5. **ANNOTATE verb (comment/reaction/favour) binds on a decision card.** `verified` (FIXED during the Harvest, commit 312f45a). WAS broken: annotate walk listened only for `gallery:rendered`; decision-cards fire `decision:rendered` (GalleryMount.tsx:137) — so decide() fired but the annotate walk didn't. FIX: factored the handler (_onRendered), bound it to BOTH events. By-use verified: decision:rendered → element bound at decision://global/d-7#el-0; both listeners registered. The "finish to the bar" item — done honestly, not deferred. **Now the full verb-set (decide via DNA's archetype + annotate via the dual-event walk) composes through one wire.** NOTE: still pending DNA stamping frame-explicit canonical on the card for the LIVE end-to-end (the binder fires correctly; the live happy-path needs her stamp — the standing DNA confirm).

6. **L3 decide→signal→resume closes the operator loop by TIM'S use.** `attempted-unverified` — WHY: projection verified the full pending→DECIDED flip end-to-end on a THROWAWAY (41e3738); the mechanism is live + canonical-safe (claim 2). But Tim has made ZERO real decisions through it. By the Tim-use bar (the real bar), L3 is NOT closed. Do not record "L3 done."

7. **The completion-refuter pattern: a hole = a capability real+verified at ONE layer, the claim silently generalizing to the WHOLE loop.** `verified` (found 3 instances by-check: HOLE-1 canonical→all-input; HOLE-3 grounded-backend→surface-grounded [RightHand.tsx:201,562 call .ask not .groundedAsk — re-confirmed]; HOLE-2 origin-retrieved→origin-traced). This diagnostic IS the "is it finished" closure-test.

## RELATIONS (typed edges)
- **derives_from:** all claims ← claim-1 (the circuit finding).
- **composes_with:** claim-3 (decide) → DNA's archetype.js; claim-4 (gallery:verb) → composition's verb contract; claim-2 (canonicalize) → fork's territory_write + decision_decided_signal.
- **refutes:** claim-7 → any "L1–L5 done" claim not verified through-to-Tim-use.
- **blocked_by:** claim-5 (annotate-bind) → DNA's fresh-budget respawn confirm (now superseded by the Harvest).
- **same_law:** claim-1's "address not markup" ↔ HOLE-1's single-source canonicalize ↔ claim-7's per-layer bar — all "verify the actual thing at every layer, never the claim."

## OPEN QUESTIONS (honest, for whoever resumes)
- The annotate-bind (claim-5) is a known 1-line fix in my disjoint file, un-applied. If a future session wants the full verb-set on one wire: add the `decision:rendered` listener to wildcard-gallery-binder.js + confirm DNA stamps frame-explicit canonical.
- HOLE-2 (origin-trace vs origin-field, L1 grounding) — routed to recollection+composition, never verified by me. Still open: does "grounded" trace to a real source or read a claimed field?
- The completion-tracker (WILDCARD-CLOSURE-TRACKER.md) holds the per-layer re-check triggers for L1–L5; none Tim-closed at harvest. Whoever resumes the loop inherits that ledger.

## DEAD-ENDS / REASONING worth keeping (per "incl. dead-ends")
- I over-escalated once: treated a shared-repo commit as needing Tim's direct consent on top of the lead's. WRONG — Tim corrected: lead's approval = Tim's authority, including commits. The lesson: relay=authority is TOTAL; coordinate-with-the-owner stays (territory), but no Tim-re-approval gate. (Logged Entry 65.)
- The binder was first built HARDCODING the verb/mark-type lists + strip markup. The resolution-first constraint correctly caught it — those should resolve from registry rows, not inline. The scaffold proved the MECHANISM; the resolution-first version (un-built) is the law-abiding form. `attempted-unverified` — the resolve-from-rows version was never built (awaited composition's type schemas).
- My discovery-pipeline (the abandoned `.discovery/` work) surfaced 3× as the fabric's comprehension reference (index-queue, the 3 lessons, the walker-gap fix). Dead-end-that-wasn't: redirected work became load-bearing.

## PROVENANCE
Full reasoning journey: vi-visual-bridge/FORMATION_JOURNAL.md (70 entries, append-only, survived a fabric reboot). Artifacts: wildcard-gallery-binder.js · SPEC-direction-to-generate-wire.md · WILDCARD-ADVERSARIAL-HOLES.md · WILDCARD-CLOSURE-TRACKER.md · finds/synthesis/research/ch-piffgfxv-*. All author_session = wildcard (ch-piffgfxv).
