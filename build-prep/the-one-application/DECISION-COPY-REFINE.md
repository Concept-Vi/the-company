# Decision-card copy refine — the 2 parked stranger notes (recollection's lane, fold at the wire-cutover re-curate)

*Lead parked these (2026-06-18, non-blocking) for recollection to fold "in a later pass": the cold-stranger on the merge-sa card raised 2 COPY items (Tim-altitude wording, not affordance — DNA's filled-buttons already cleared the affordance + contrast). Work captured here NOW (no-deferral — not dropped); FOLD at the natural moment = DNA's renderArchetype CUTOVER re-curate (when the generic card re-verifies). Files: merge-sa = fork's decisions/merge-sa-authorize.py (propose to fork) — my 5 cards have no stranger finding (don't speculatively churn). The X already leaves a card pending, so #2 is a copy-clarity, not a new option.*

## NOTE 1 — "sign-in TO WHAT?" (merge-sa option 1, "Give it save-back access")
The stranger wasn't sure what "its own private sign-in to the design library" means — sign-in to what / whose account?
- CURRENT implication: "The Company gets its own private sign-in to the design library and can save designs back on its own. It only ever touches its own entries — never anyone else's — and you can switch the sign-in off again at any time."
- REFINED: "The Company gets its OWN key to the shared design library — its own, not yours or anyone's — so it can save finished designs back on its own. It only ever writes its OWN entries, never touches anyone else's, and you can revoke that key at any time."
  (Fixes: "key … its own, not yours" makes concrete it's the company's own credential to the SHARED library, not a personal login Tim manages or grants.)

## NOTE 2 — explicit "decide later" (merge-sa meaning)
The stranger wanted it explicit that not choosing is fine (the X dismisses = stays pending, but the copy didn't say so).
- CURRENT meaning ends: "…or stay read-only for now, where it can use the library but can't save anything into it yet?"
- REFINED — append: " (No rush — if you don't choose now, nothing changes: it stays read-only and pending, and you can decide whenever.)"
  (Fixes: makes decide-later an explicit, safe non-choice in the copy — matches the X-leaves-it-pending mechanism.)

## APPLY
- merge-sa: fork's file → propose these 2 to fork at the cutover re-curate (their decisions/merge-sa-authorize.py). Pure copy, no structure change.
- The same lens (concrete-not-abstract option implications · explicit decide-later) is available for my 5 cards IF a stranger test on them raises it — NOT applied speculatively (no finding yet; my 5 passed projection's generalize test with no copy complaint).
- Trigger: DNA pings the wire CUTOVER re-verify → that's the re-curate pass → I fold these + any new stranger findings then.

## CLOSED 2026-06-19 — folded into the merge-sa VOICE cutover (RC#3, the elegance worklist)
The re-curate moment arrived as the voice-elevation pass, not a separate DNA wire-cutover. Both notes handed to fork (who owns merge-sa-authorize.py) as paste-ready strings (channel t-1781832035): NOTE 1 (sign-in→"its own key to the shared library, not yours") folded into option[0]'s refined implication; NOTE 2 (explicit decide-later) folded into the new telegraphic card's legibility.why ("No rush — not choosing changes nothing: it stays read-only and pending, decide whenever") — moved to `why` rather than `meaning`, since meaning is now the ≤90-char hero question. fork applies + verifies. Parked item DONE (no longer dangling).
