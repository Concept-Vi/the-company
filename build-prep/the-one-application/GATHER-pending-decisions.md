# THE GATHER — Tim's real pending decisions (the interface's first CONTENT + the subtype source)

*recollection's piece of INTERFACE-FIRST (lead, Tim-reprioritized 2026-06-18): surface Tim's REAL pending decisions from the corpus/history → the lead CURATES into the decision-set → the first decision-cards + the subtype KINDS derive from it. These are CANDIDATES + provenance (lead curates; I don't decide the set). Each is at Tim's altitude (machine-names→meaning), with options + the merge-SA row as the shape template. Honest scope: recall over history surfaced NO crisp additional pending decisions beyond this set — these ARE the real pending ones; I did not manufacture filler.*

## THE CANDIDATE DECISION-SET (curated from the explicit sources, verbatim where stated)

### The 5 genuine UNION decisions (UNION-DIVERGENCE-LEDGER §A — already at Tim's altitude, machine-names removed)
1. **file identity** — is a saved file "the same thing" as its content, or its own thing? (lead modelled BOTH-with-derivation; residual = dedup/cache as one identity, or two kinds of reference?) · KIND: identity
2. **channel member (residual)** — when a session ends, which face is the durable channel member — the person or the session? (lead reconciles the dual-face; reaches Tim only if a residual survives) · KIND: identity
3. **cluster** — is a "cluster" a thing you name and return to, a live re-computing result, or both? · KIND: identity
4. **event streams (Shape-B)** — should the high-volume live streams (history log · marks · pins · notes) become first-class addressable, stay references-only, or a mix? · KIND: addressability/shape
5. **the form taxonomy** — what are the REAL kinds of things in the corpus? (the current decision/log/registry/prose are prior-AI scaffolding; the real taxonomy is authored from the open coverage) · KIND: taxonomy

### The live, already-typed decision (fork's DecisionRegistry — decision://global/merge-sa-authorize)
6. **save-back to the design library** — should the Company get its own private way to save designs back into the shared design library (finished/changed designs written back automatically), or stay read-only for now (use the library, can't save into it yet)? · 2 options (1 recommended), state=pending · KIND: capability/authorization · ★ THIS IS THE SHAPE TEMPLATE (meaning + options[label, implication] + recommended + state).

### The foundation decision (the lead is composing this for Tim — #75)
7. **the substrate home** — should the one Supabase substrate be the centre's address-space (factory · connector-edge · corpus · coverage = grain-projections of it), and where does it live? (the lead surfaces the composed shape; it's tied to Tim's 5 held substrate decisions) · KIND: foundation · NOTE: Tim authorized the interface to walk him through THIS as one of its first decisions ("it decides its own foundation").

### DNA's #1–19 overlaps — MAP onto the above (decide ONCE, not twice; SUBSTRATE-IN-THE-COMPANY-REFLECTION §54)
- #8 typed-edges → folds into (4) event-streams/addressability + the substrate shape · #11 surface-axis-taxonomy → (5) taxonomy · #2 runtime-mutation-boundary → its own (KIND: boundary) · #14 substrate-in-canon → (7) foundation · #15 content-registry ≈ (5) form taxonomy. (DNA's island-specific ones — flat-vs-faces, render location, archive cleanup — stay DNA's call, not Tim-gated.)

## THE SUBTYPE KINDS (derived from the set — the interface's decision-subtype vocabulary)
*The lead asked: the subtypes fall out of the real decisions. They do — these are the recurring shapes:*
- **identity** — "is X one thing or two / a thing you name or a live result / which face is canonical" (decisions 1,2,3). Shape: the thing + the candidate identities + what each implies downstream.
- **addressability/shape** — "first-class addressable vs reference-only vs mix" (4, #8). Shape: the data + the 3 representation choices + cost/benefit.
- **taxonomy** — "what are the REAL kinds" (5, #11, #15). Shape: the evidence (coverage's emergent kinds) + the proposed taxonomy + Tim authors.
- **capability/authorization** — "grant access vs stay restricted" (6 merge-SA). Shape: the capability + grant/withhold + the implication of each (the live template).
- **foundation** — "which substrate / where it lives" (7, #14). Shape: the candidate foundation + the alternatives + what each commits.
- **boundary** — "where's the line" (#2 runtime-mutation). Shape: the operation + the boundary options.
★ Every kind = a `decision` with {meaning, options[{label, implication, recommended}], state} — the same archetype (merge-SA's shape) parameterised by kind. So ONE decision-card archetype renders all six; the kind tunes the options-shape + the explanation lens.

## HANDOFF
- LEAD: curate this candidate set → the first decision-set (pick the first card — merge-SA is the only one already a live decision:// row + has options written, so it's the natural FIRST card). The subtype kinds above seed composition's archetype catalog + the fork's component types.
- Each card's EXPLANATION grounds via my recall_for_decision (the RHM context bundle + prior_decisions) — already committed (3b5107a) + verified against decision://.
- I can DEEPEN any one: pull its full history/context + draft its meaning+options at Tim's altitude (the merge-SA shape) on request. Flag which to deepen first.
