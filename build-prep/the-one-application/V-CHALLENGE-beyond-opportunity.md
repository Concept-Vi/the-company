The advisor confirms: the task line is BEYOND/OPPORTUNITY, the MAP is reference material, and §6 already satisfies "maximal/generative" — grounded, four unlocks covered. My job is to return the BEYOND answer as a standalone, grounded deliverable, plus the one candidate gap (cross-project common memory) anchored to an existing primitive. I won't re-dump §1-§5/§7. Here is the deliverable.

---

# BEYOND THE SPEC — What the Resolution-First Engine Makes POSSIBLE

**The unlock in one line:** because every named thing is a *type that resolves through one dispatcher* (`resolve_address` `cognition.py:842`, 16 schemes) and the surface grows by *adding registry rows, not screen-code* (`_write_registry_file` `suite.py:9840`), the engine Tim asked for is not a decision surface — it is a **universal surface generator**. The decision surface is merely its *first content*. Everything below is reachable by adding rows to registries that already exist, wiring seams that are already half-built, or retargeting a circuit that already runs. Each "possible" names the built primitive that makes it possible — nothing speculative.

---

## B1 — The surface edits and grows ITSELF (self-build closure)

- **MAKE retargeted at `mode://<id>` → Tim adds a MODE by speaking to the RHM.** The interface builds its own modes through the SAME circuit it renders with — one `MODE_REGISTRY` row (`suite.py:2138`) via `_write_registry_file`, zero screen-code. *Primitive:* MODE_REGISTRY is already the most complete type-with-schema in the system (`ModeSpec` `suite.py:155`); the keystone generate→write→re-render wire already exists (`implement.py` + `suite.dispatch_decision`, proven `tests/wire_commit_acceptance.py`).
- **Decisions are "type row zero."** Once `decision://` proves the chain address→type→archetype→render→take→write-back, the SAME chain renders code-units, DNA-tokens, factory-archetypes, session-minds. "Whole interface by adding type rows" becomes literally true for EVERY domain. The decision surface is the TEMPLATE that proves the engine, not the endpoint.
- **Nucleation → axis-birth → learning.** `bindings/by_nucleation.py` is BUILT — items pile out when they don't fit a registry's declared axes (20/80 water-law). When a `born` event fires, prompt Tim to RATIFY a new axis → gate-ratified row → new angular geometry the next render uses. **Tim's FOUR ROOT AXES get formalized by forbidden-zone pressure, not manual declaration.** Surface: a Nucleation Lens inside the V's "Make."
- **Decision-kinds far beyond "build."** The `implement.py` wire (AUTO/CONFIRM/SURFACE/LOCKED postures, exactly-once `decision.dispatch`, git-checkpoint, `build_result_review`) generalizes with zero new mechanism to: config-change, role-change, model-swap, schedule-change, annotation-policy-change. **The V's "Drive" verb = governed propose→confirm→act for ANY system parameter.** One circuit, every change-kind. (Satisfies "other decision-kinds.")

## B2 — One resolve, MANY surfaces (every future view = one binding/template row)

Each is a single `bindings/<id>.py` row over the existing `runtime/projection.py:project` engine. None requires a new dispatcher.
- **Planning Surface** — `center=future://goal/<id>`, `radius = semantic distance to goal`. *"The distance between present and goal IS the plan."*
- **Pollution-Hunt Surface** — `center = AI-pole address`; every unit's lean reveals its AI-contamination distance. A corpus cleaner AND a real-time grader at once.
- **Graph-of-Minds Surface** — sessions-as-units, channel-membership as angle, last-activity as radius (D1; near-unanimous "fold in"). A `bindings/by_session_graph.py` row.
- **Remembering Surface** — `project(center=cas://<past-commit>, at=ISO-past)`: the field AS IT WAS. The `?at=` time scrubber already exists; CAS already holds every prior state.
- **Parallax / Triangulation Surface** — `bindings/parallax.py`: two poles from any registry, every unit by its lean between them = multi-perspective reasoning made spatial. The `?pole_a=`/`?pole_b=` separator math exists.
- **Strain Surface** — `by_separator` with pole_a = where-it-IS-filed, pole_b = where-it-MEANS-to-be: the corpus's structural-vs-semantic divergence at a glance. **Gap-pressure made visible.**
- **No-modes by construction** — `resolve(address × DNA-frame × arrangement-template) → surface`. A `templates/` registry (mirrors `bindings/`) makes any address renderable as slide / graph / document / dashboard by passing a different template — not a mode flip. (Satisfies "other surfaces the same engine resolves.")

## B3 — Bidirectional address / accumulation AS memory

- **256 pre-aimed conversations already in the corpus.** The snapshot holds 256 real unanswered element-bound "Why?" questions, each an `exchange://` address with no brain loaded yet. Each is a pre-seeded, pre-aimed conversation: load a brain, the conversation resumes. **Annotation-as-conversation-seed** — every annotated element becomes an `exchange://` address; *direction IS a brain handle.*
- **Option accumulation as a corpus.** Every `option://` Tim ever encountered accumulates; the RHM resolves PAST options as context for NEW decisions. Options don't die when a decision is made — they become reusable primitives. The corpus IS the institutional memory of what Tim considered.
- **Decision slides as corpus entries.** `kinds/raw.py:36` already declares the `decision` kind; every rendered slide ingests back as a `corpus.record` carrying its verdict mark; the RHM recalls "decisions made about X" and resurfaces the slide sequence. Introspective data building applied to decisions.
- **Cross-channel memory diffusion.** Channel posts captured as `corpus.record` (`channel://` scheme) via the `post_to_channel` `:468` hook → `corpus(op=query)` searches ACROSS all channels. **The common company memory IS the cross-channel search surface.** (Satisfies "common-memory unlock.")

## B4 — Narrative / sequence machinery applied to DECISIONS

- **Decision sequences as traversable walks** — `reading_axes`: DESCENT drills to evidence, TRAVERSE steps the decision queue; the 3 invariants mean any slice is a valid artifact.
- **A `decision` arc type in `sequence.json`** (data, not code: `argue(1)+show(1-3)+prove(0-1)+plan(1)+close(1)`); `measure` applied to decisions = **partial approval** (approve options 1+2, defer 3 — multiple `ResolveSurfaced` against one id).
- **The V corner as a decision-action rail** — drill=descend to evidence, mark=approve, ask=more context, source=trace to the surfacing node. No new verbs; `DNA.veeCorner` `unit-view.js:131` already places the 5 verb-sockets on every unit.
- **Progressive wizard via `generation_ladder`** — the 8 stages ARE "Intent→Proposal→Approval→Execution applied to design"; each stage a Vi-Chat wizard step; converges with `plan-wizard-redesign.md`. (Satisfies "stories unlock.")

## B5 — DNA / composition completion (the compositor unlocks the corpus)

- **Complete dial closure** — warmth re-tints, density re-spaces, register swaps eligible archetypes, polarity inverts grounds — all WITHOUT re-rendering the DOM, just re-injecting CSS vars via `DNA.injectVars`.
- **Scale-tier multiplier at compose time** — `grammar.scale_tiers` (poster ×2.0 … component ×0.5): the SAME archetype record renders at any scale. **"Any part is a whole" becomes mechanically provable.**
- **The narrative walk as the compositor's content source** — `DNA.story.cut(tree,tier)` → archetype names → `composeArchetype` → injectors → live-themed DOM. No bespoke render per story.
- **The Company token bridge as a polarity proof** — the Company dark theme as a second *identity* (same engine, different `warmth_anchors`) proves "derive-never-place" at the identity level.
- **The 6 null organisms** (process_steps, stage_cards, branch_tree, time_tree, porthole_row, updown_duo — confirmed `generator:null`) — each closes a story-shape the narrative walk can already request but cannot yet render.

## B6 — Type-system / spine deepening

- **The polarity / signed-axis face** — `{magnitude,sign}` depth tokens, an `inverts-to` symmetric edge, a magnitude-preservation invariant. The equal-opposite law arrived 3× independently; the type system is ready to carry it.
- **Field-field edges** — the type system has registries / types / 17 edges but NOT YET field↔field. Adding them makes the typed graph queryable at *field resolution* ("fields with fields and so on").
- **Bond-count tuning** — `grammar.scales.space.bonds.levels` `{0.5,1,2,3,5,11}` are seeded candidates; Tim retunes once, every gap of that bond type re-resolves estate-wide. The authoring step that turns the spacing *mirror* into the real vocabulary.
- **Colour as resolved meaning-field** — a pending high-stakes decision's `state`/`scope` resolves to a colour-token tinting the operator's WHOLE visual field. Colour as resolved signal, not decoration. (Rides the `--vi-mode-primary` cascade — see the V-expansion unlocks.)
- **The window as a TIME axis** — `window://` resolving `run://<turn>` parameterized by `turn_id` → a time-axis slider over decision history. CAS already holds every prior state.

## B7 — The V as a two-way dial (V-expansions/modes)

- **The V becomes a live drive, not just a pointer.** "Drive" moves a warmth/density/register dial *inside* the V → `DNA.injectVars` re-tints the whole app, frame/grammar constant. V-as-navigation+ask+annotate AND V-as-live-drive = ONE object, two roles — *"an overlord surface = sees AND drives."*
- **Recursive sub-surface nesting is already latent.** `mountConnection` renders children via the universal `renderDefinition()` `connection-mount.js:150`; a child is a full registry organism with its own sockets. **V → radial-wheel → nested-option-surface → detail-surface is structurally available TODAY, zero mechanism changes** — the composition law running recursively.
- **Expansion mode as a separately-addressable type** — `socket.connection.expansion: 'resting'|'radial'|'window'|'sub-surface'` resolved through the user→project→global Supabase cascade: change HOW a panel opens independent of WHAT opens, per scope, without touching the global V. `parentLeap` is the embryo of this discriminant.
- **Expansion resolved FROM decision-state** — a `decided` decision auto-expands its artifact; a `pending` one collapses to a summary. Expansion becomes a *function of meaning*, not a UI toggle.

---

## The candidate that exceeds even §6 — cross-PROJECT common memory

One unlock sits past the Company's own boundary and deserves naming on its own. B3's diffusion stops at cross-*channel*; the resolver makes cross-*project* the next ring.

**Possible:** the SAME `resolve_address` scheme table (`address.py:129`) + the corpus (`exchange://` identity, embed `:8007` / rerank `:8008`) become **one address space spanning Vi Chat, OpenClaw, and the ~8 pre-Company vaults** — not federated lookups, one resolver. A `project://` scheme already sits in SCHEMES; promoting it from a label to a resolvable scope makes "what did I decide about X" answerable across *every* surface Tim works in, because they all become rows the one dispatcher resolves. *Primitive that grounds it:* Tim's own standing frame — *"the Company IS the common memory for ALL projects/agents; context resolution spans them"* (Common Memory Temporal) — plus the fact that `corpus.record` lineage already carries `project` as a first-class field. The unlock is not new infrastructure; it is **declaring the existing project field a resolvable axis** and letting the one resolver cross the boundary the schemes already anticipate.

---

**The one circuit, end to end** — every BEYOND item above is a node already on this loop or a row added to a registry the loop already reads:

```
Principal → aim (the V, at an address)
  → resolve_address (cognition.py:842 — one resolver, 16 schemes)
  → type + registry row (decision·option·mode·window·sub-surface — all ROWS, not code)
  → archetype:// → composeArchetype + DNA.injectVars(--vi-mode-primary) + DNA.story.cut
  → V hosts it (socket.connection.expansion → mountConnection → renderDefinition, RECURSIVE)
  → take (verdict mark / MAKE verb)
  → dispatch_decision wire (git-checkpoint) → territory_write(kind=generated)
  → corpus.record (channel:// + decision kind → common memory, project axis → cross-project)
  → recall_for_decision feeds the next aim
```

The spec asked for a decision surface. The engine, by construction, makes a self-growing, self-remembering, multi-surface, cross-project instrument — and reaches all of it by adding rows. The relationships are the content; every node is a type that resolves; nothing above is new architecture, only the engine run further.