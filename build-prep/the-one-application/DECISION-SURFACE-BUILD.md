# Decision Surface — the prove-on-one build (A+B, RHM + common memory + into-channels)
*Tim 2026-06-17: "do A and B, and have the decision surface with the RHM and a common company memory into the channels." The first real composed product of the union — the command bridge, channel-native. Provisional; investigate→confirm-seams→build.*

## WHAT IT IS
The live decision-surface = the KEYSTONE (generate→write-back) RETARGETED at DECISIONS. It SHOWS a pending decision, EXPLAINS it (RHM brain + common memory), TAKES Tim's decision in-surface, and the take IS the write-back (the decision becomes a durable artifact). Composed from the streams' existing pieces — the union assembling its first product.

## A + B (both, per Tim)
- **A (build now):** build the surface as the prove-on-one. It does NOT need the 5 substrate decisions — it's built on existing pieces; the 5 (+ DNA #1–19 overlap + merge) become its FIRST CONTENT, decided in-surface.
- **B (parallel, don't stall):** keep live work moving — the factory-merge READ half goes live on anon now; the merge SA-write authorize stays a tiny Tim gate (or becomes the surface's first live decision once it exists). Lead-callable gating calls taken plainly to unblock the build; the meaning-decisions flow through the surface.

## THE SEAMS (each stream's piece — composed, not parallel)
- **fork** — the KEYSTONE WIRE (generate→territory_write→re-render) + the RHM brain (run_turn / /api/claude/turn, the talk+generate leg) + CHANNEL INTEGRATION (the surface lives in the channel fabric). Co-owns the keystone with composition.
- **composition** — the COMPOSITIONAL TOOLS (the factory / factory-ops as MCP tools the brain loads) that GENERATE the decision-presentation; the V as the corner handle. (vi-visual was the attempt — now built into the centre.)
- **DNA** — the RENDER: a DECISION-CARD archetype (show the thing + the options + the explanation), in the archetype/renderArchetype model; legibility meaning-fields so Tim sees meaning not machine-names.
- **recollection** — the COMMON COMPANY MEMORY wired INTO THE CHANNELS: the corpus/recall as one shared memory the RHM + the surface (and channel members) draw on. (Connects #69 self-recall + member-discovery → the channels share one memory.)
- **projection** — HOST the surface (the operator surface / the V on every page) + the /api/territory + /api/cognition wiring behind.
- **lead** — assemble + sequence; hold the floor; the decision-artifacts the surface writes ALSO discharge the adherence-ledger's "lead-decision artifacts" residual.

## FIRST CONTENT (real, not dummy)
Tim's actual pending decision-set: the UNION's 5 (file/content identity · session/person channel-member · cluster identity · event-stream addressability · the form taxonomy) + DNA's overlapping #1–19 (typed-edges, surface-axis, mutation-boundary, substrate-in-canon, content-registry) + the merge SA-authorize. Each rendered as a decision-card, explained by the RHM with common-memory context, decided in-surface → written back as the decision-artifact.

## BOOTSTRAP
The first card may be hand-assembled/presented THIS once (the generator is what's being built); after that, live. (Per A+B: build the surface + unblock-in-parallel.) The surface build starts on existing pieces — the 5 don't gate it.

## WHY THIS IS THE RIGHT PROVE-ON-ONE
Dogfoods the whole stack (substrate-addressed decisions · archetype render · RHM+memory explain · keystone generate+write · legibility · the V · channel-native) on the most real content there is (Tim's own decisions), produces decision-artifacts as a byproduct, and becomes the standing CHANNEL by which decisions reach Tim — rendered, explained, live, decided-in-surface. The keystone's first retarget. Relates: [[the-one-application]], DECISION-SURFACE-VISION.md, RHM-BRAIN-DISCUSSION.md, SPEC-direction-to-generate-wire, UNION-COMPLETION-CRITERIA (ACT/keystone group).
