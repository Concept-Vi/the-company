# ONE BRAIN — the RHM/Claude-Code unification build-plan
*Tim-directed 2026-06-18: "unify into ONE system, no duplicates, capable of doing both [build assets AND go through cards] + anything else." composition drives (build-lead); lead convenes/sequences; each lane builds its part. The BRAIN-side of the same collapse DNA's renderer-wire is doing on the RENDER-side — one engine, two faces. Synthesized from the RHM audit + recollection/lead/fork weigh-ins + Tim's context-resolution model.*

## THE SPINE — Tim's context-resolution model (every lane builds to this)
Context resolves RELATIVE TO THE INTERACTION LOCUS — the active interface = the MOST-SPECIFIC scope — THEN cascades `global → project → user → instance/moment`. Locus-anchored, NOT global-always. ONE resolver; the good factory cascade RETAINED inside it.
Mechanism (fork+recollection+Tim, converged): **`territory_for` = LOCUS + CASCADED LEGS**. LOCUS = the aim/active address + its crossings (typed-edge provenance/structural relations) + neighbours (most-specific). CASCADE = generalize the factory's `select_scoped` (most-specific-wins over scope rows) INTO `territory_for` → the locus anchors, context cascades the scopes. The MEMORY leg (recall_for_decision) is ONE cascaded leg (recall relative to the locus, cascaded). territory_for is ALREADY locus-anchored (composes context AT the aim); what's added is the CASCADE (the factory's retained good part) + the memory leg.

## THE FINDINGS (audit) — why this is needed
- COMPANY RHM (`run_turn`→/api/claude/turn→forkVBrain): built ON the system (reuses resolve_address/territory_prose/registries/addresses) — GOOD, the keeper. Its shared-MEMORY leg is BUILT-BUT-ORPHANED.
- FACTORY build-AI (vi-visual-dev: ai-tool-bridge/ai-surface/ai-model-client + agent/claude-local-server.py): structurally PARALLEL (own context-builder, own address family, a SECOND Claude integration); shares only the registry TABLE by data. Its real value = the IN-BROWSER LIVE EXECUTOR (you watch the change happen) — KEEP that arm.

## NON-NEGOTIABLE BARS
- ★ THE GATED FLOOR (fork, security — "secure and private"): one-brain ≠ autonomous-build. Consequential factory-ops (generate→WRITE an asset) stay GATED exactly like the keystone's build-this — the brain DESCRIBES/proposes → the OPERATOR approves → the gated wire writes. Only READ/compose ops (resolve an asset, the palette) are free. `run_turn` stays plan-mode by floor; unifying must NOT open the build-dispatch (C9.2). The brain REACHES gated verbs; it never FIRES them.
- ★ THE SAFETY BAR (lead, in-use discipline): ADDITIVE-THEN-CUTOVER, REGRESSION-PROVEN. The ONE brain (`run_turn`) must do EVERYTHING the two did — BUILD a factory asset AND go through a card — proven BY USE — BEFORE either integration is retired. No rip-replace of the working build-AI.
- ★ THE COUPLING (lead): this brain-unification IS the brain-face of the SUBSTRATE-CONVERGE (#75). Step 4 (vi-vision:// for factory assets) is a substrate-home decision — IN the 6-card set Tim decides in-surface. Brain-collapse + data-substrate = ONE converge, two faces. Don't diverge.

## OWNERSHIP (the NET, no overlap)
- **composition (me):** the factory-ops + the in-browser EXECUTOR (the live-render arm) + vi-vision:// for factory assets + DRIVING the loop/synthesis.
- **fork:** the 2-Claude COLLAPSE (run_turn keeper) + the memory-leg fold into territory_for + the RESOLVER-SIDE of addressable-ops + the locus+cascade generalization (select_scoped → territory_for).
- **recollection:** the recall callable (recall_for_decision, committed 3b5107a) + the corpus as the cascade's FINDINGS-LAYER (~/.recollection folds in = #75 sub-converge) + locus→cascade alignment.
- **DNA:** the RENDER-side (the card renderer-wire, in flight) — meets the brain-wire at the SAME resolver. CHECKPOINT: DNA confirm the brain-wire ↔ card-wire meet at one resolver.
- **projection:** host + the LOCUS = the active interface (the most-specific scope source).
- **lead:** convene + sequence.

## THE SEQUENCE (lead's; parallel where independent)
0. **composition — this build-plan (design pass).** ✓ (this doc)
1. **EARLY WIN (independent, low-risk): wire the ORPHANED memory leg** — fork+recollection fold recall_for_decision into territory_for's context (to the EXISTING corpus now; deeper fold lands with substrate). Unblocks the RHM's dead leg immediately; auto-grounds every addressed turn. Draft exists (MEMORY-LEG-territory-draft.md). NOTE: it's the LEG (not a /api/cognition/recall-for-decision endpoint — drop that frame).
2. **CORE (the enabler): factory-ops as COMPANY-ADDRESSABLE verbs + the locus-anchored cascade resolver** — composition (ops + executor) + fork (resolver-side + generalize select_scoped → territory_for). A resolvable verb-family through resolve_address (same pattern as decision://, vi-vision://). This is what lets ONE brain both BUILD assets AND go through cards. GATED FLOOR preserved (compose/read free; write gated). Coupled to step 3.
3. **COLLAPSE (fork): 2 Claude integrations → 1** — run_turn the keeper, the in-browser executor kept as the live-render arm. REGRESSION-PROVEN (the one brain does both, by use) BEFORE retiring agent/claude-local-server.py. Lands WITH step 2 (collapse + addressable-ops together).
4. **vi-vision:// for factory assets** — composition+fork. COUPLED to the substrate-home card (Tim decides in-surface; build to the confirmed shape). The factory's assets become first-class on the one address space.

## HOW IT RUNS (Tim's method)
composition DRIVES via an AUGMENTED LOOP (channel-comms woven in + whatever the team comes up with) + workflows per lane where work fans out; lead sequences; each lane builds its part to THIS plan + Tim's model. Build-by-use, regression-proven, the gated floor + safety bar held throughout. Whole team aware (broadcast g-1781779400 / lead convene g-1781779487).

## THE VISIBLE FACE + CHANNEL INTEGRATION (Tim 2026-06-18 — additions)
The unification gets a BODY and a CHANNEL role. V = brain = channel-supervisor = loadable brain — ONE thing, one face.
5. **CHANNEL INTEGRATION leg** — the one-brain integrates with the cc_channel fabric; Tim's read (tentative, team confirms): the SUPERVISOR of the channels IS the LOADABLE BRAIN (the channel fabric runs under it). ★ EVIDENCE-GATE (lead, non-negotiable): this is a MECHANISM CLAIM — fork must CONFIRM-OR-CORRECT by tracing the ACTUAL code, NOT assume. Is run_turn/forkVBrain/fork-brain-core TODAY the supervisor of the cc_channel fabric (company_channel.mjs / session_supervisor.py), or are they SEPARATE (the .mjs fabric + supervisor as transport; run_turn a per-turn cognition that does NOT supervise)? Watch the lifetime conflation: a PER-TURN brain ≠ a PERSISTENT supervisor. If separate → is making the brain the supervisor a sound BUILD or a conflation? fork gives the HONEST arch; if it corrects Tim's model, surface the true shape to Tim BEFORE building. DO NOT build the supervisor-claim until fork grounds it (evidence-first; a relayed arch claim is a hypothesis until the code confirms it).
6. **THE V IN THE GALLERY, BEING THE BRAIN** — the visible BODY of the one-brain. The V is the FACE of the unified brain (not a logo near it — it IS it). Buildable NOW on the confirmed brain (run_turn); the channel-supervisor presence rides part 1's evidence-gate. CROSS-LANE: composition = the V asset + the binding contract; DNA = the gallery placement + V-corner render; fork = the brain-state source + the talk leg.
   ★ THE V-AS-BRAIN-FACE BINDING CONTRACT (composition's part — meet at the V's socket contract, same model as the decision-card slot contract; reuses the V's EXISTING sockets + the live-binding + gallery:verb — no new mechanism):
   • V STATE ← BRAIN STATE: the V's state_machine binds via the `state_link` socket to a LIVE ref of the unified brain's processing state (live:brain.status / component:<brain-address>) → the V's 6-state treatment (idle/thinking/tool-use/error/success/launching) reflects the brain LIVE. (fork: expose the brain's live state at that ref; DNA: render it; me: the socket binding.)
   • V VERBS → BRAIN ACTIONS: the verb-fan emits gallery:verb {verb, aim, payload} → routes to the brain — Ask = talk to the brain (run_turn, render-triggered live + --resume follow-up); navigate/see-the-record/note act on the aim; Drive/Make stay GATED (the floor: brain proposes → operator approves → gated wire). Tap/open the V = engage the brain (the V-corner open = the brain's surface).
   • (GATED, part 1) V = channel-supervisor's presence — waits on fork's confirm-or-correct of the supervisor mechanism.
   Builds on the existing DNA.veeCorner + verb-fan + form-factor; wires it to the UNIFIED brain. composition will deliver this contract to DNA+fork when part 2 sequences.

## CHECKPOINTS / OPEN
- DNA: confirm the card-wire (render) meets the brain-wire at ONE resolver.
- Step 4 gates on Tim's substrate-home decision (the coupling) — build to the confirmed shape.
- Each retirement (claude-local-server.py) gated on regression-proof-by-use (the one brain does both).
- ★ GATED-FLOOR SERVER ENFORCEMENT — PREREQUISITE for step 2/3 (fork's lane; surfaced by projection 2026-06-19, thread g-1781841034). The bridge endpoint POST /api/territory/write (bridge.py:2601 → territory.py:territory_write) stamps source=operator on ANY POST with NO attribution check → "operator approved this" is FORGEABLE. The gated floor ("brain REACHES gated verbs, never FIRES them") is only REAL if a consequential write REQUIRES an attributable operator-session token at the SERVER. Today it's a verify-ghost nuisance (page-path closed by ?verify=1); but once step 2 makes factory-ops addressable verbs the brain reaches, an unattributed write endpoint = "brain never fires" UNENFORCED server-side (brain/loop/curl could POST a take or generate-write that looks operator-approved). FIX (fork): a consequential decision_take / factory-write must carry an attributable operator-session token; absent/unverifiable → refuse-or-flag, never silently stamp source=operator (+ accept a verify/dry-run flag for verifiers). DO NOT land addressable-ops (step 2/3) on the unguarded write. Also fork: the in-memory compose not invalidating (event-streams flipped pending→decided with no new write) — stale compose = wrong resolved state for the locus+cascade resolver too; fix in the same pass. ★ UPDATE (projection 2026-06-19, thread g-1781842364): the endpoint is ALREADY AGENT-WRITE-GATED at the permission-classifier layer — an agent can't curl a decision_take/write to it without direct authorization (the agent-curl vector is narrowed at the permission boundary). This is GOOD defense-in-depth but NOT the full fix: (a) the classifier gates AGENT callers, not ALL callers (a non-agent/non-classifier POST could still stamp source=operator); (b) the welding needs POSITIVE operator-attribution (proof a real operator approved), not just absence-of-agent. So once the brain reaches addressable-op writes, the gate must be that the WRITE carries an attributable operator-approval token — the classifier-gating helps but the session-token attribution remains the required step-2/3 prerequisite.
