# The Self-Build Surface — Grounded Convergence Map
*Tim's realization (2026-06-16): the interface IS the self-build surface — ONE mechanism (click an element → talk to the inbuilt AI → direct it / point at sources → it generates or mutates → written to an addressed registry → re-render), RETARGETABLE by address at the mockup-content / gallery / DNA-system / the V / generation. "Build == use, the same mechanism pointed at different things" = universal composition. Grounded across the fabric — each member mined its own code+history and brought it to the channel (Tim's standing practice). Written confidently; provisional — Tim is the judge of "right." Verify each file:line before building on it.*

## THE CIRCUIT
`click element → MARK/DIRECT @address → [brain ACTS: generate/mutate] → WRITE to addressed registry → re-render`
Retargetable: same circuit, different address (units · gallery · DNA-system · mockup-content · the V · generation).

## BUILT (verified, across the fabric)
- **CAPTURE (mark/direct @address):** wildcard's gallery-binder (`gallery:direction {element_id=address, type, text}`, use-verified) + DNA's `stage.js` (preference/verdict/note/pin @element-address; the MARK primitive, `docs/RESOLUTION.md:184`: "{target, kind, value} … a pin is a note whose target is an element-address"). The mark IS "direction at an address."
- **WRITABLE REGISTRIES:** composition's `factory-ops → registry.register` (createComponent/setSocket/saveComponent, ~5% shadow-built for the V) · DNA's `feedback_server.py` (`/api/feedback · /api/tokens · /api/grammar · /api/dna/<name>`, GET+POST) · fork's `territory_write → suite.mark` (3 mark_types, live-mark verified).
- **RE-RENDER:** DNA.injectVars (live var-injection; change the registry → the surface re-renders).
- **RETARGET BY ADDRESS:** free by construction — the circuit is address-keyed (wildcard + composition both confirmed independently). wildcard's banked point: the elements tagged `[data-anchorable]` to close the annotate round-trip ARE the same elements the keystone fires on — the annotate-substrate and the self-build-substrate are ONE address-set.
- **TALK LEG — verified by use (2026-06-16, both viewports):** fork's hook-1 — drill a unit → "Ask Claude Code" → content-correct streamed answer ABOUT the unit (territory_prose composer + the loadable brain). = click→talk→[it answers], the first half of the circuit, LIVE.

## THE ONE UNBUILT RUNG (the keystone)
The brain READS the direction → GENERATES/mutates → writes to the registry (today it only RECORDS a mark).
**SPEC:** `build-prep/front-interface/SPEC-direction-to-generate-wire.md` (wildcard+fork) — a ~1-function router joining 6 built pieces:
`gallery:direction{do_this} → run_turn(context=territory_prose(address), instruction=text) → POST /api/territory/write(address, output, kind=generated) → re-render`.
**Sub-decisions (resolved, lead):** provenance-mark ALSO (not replace) · async shimmer→solid · `do_this`-only to start (expand to `direction` later). wildcard confirmed each matches a vi-visual law + needs ZERO emit change.
**Brain:** the model-routing-unification (#71) gives this generate-step its CAPABLE brain (the judge/wide tier, not the local 4B extractor — extraction-vs-judgment).

## THE TOKEN CONTRACT (one identity source — V + gallery + app + units)
**Source:** `dna/tokens.json` (stable identity) + `dna/grammar.json` (generative dials/scales — the mode/cascade anchors). **Resolver:** `ui/surface.js` DNA.injectVars (resolved-THROUGH, never literals — the gallery already enforces var(--dna-*), zero hardcoded values). **Access:** `/api/tokens · /api/grammar · /api/dna/<name>` (:8090, GET+POST).
**Mode-as-cascade:** the V moves ONE anchor (e.g. the warmth dial) → DNA.injectVars resolves → the whole app re-tints, frame/grammar constant (same identity, anchor moves). The V = the visible handle on the mode-token.

## GENERATION SURFACE (the content-half home)
DNA's gallery already has the HOME: the VERSIONS axis (per-piece v1/v2 + compare) + the BENCH room (compose/grow) + the writable registry. AI-generated icons/animations/variants land there once generation writes to the registry — i.e. the keystone wire pointed at generation (composition's "AI generates animations/effects/variants/states", the V as a reusable icon-container).

## THE DESIGN SOURCE (Tim's own, located by meaning)
relative-difference vault, session-3 (recollection R7): **"The Dynamic Wizard Surface"** + `application-composition-operational-mode` + `typed-surface-materialisation` — "UI is a generated projection over substrate state, AI as a typed block, variable resolution, typegraph-driven interface composition, recognition-mediated non-chat interaction." (Tim's "decomposition 1" — shorthand, no literal artifact; this is the by-meaning match.) **The built pieces realize a design Tim had already articulated.**

## NET
~90% built across the fabric · retargetable by address by construction · ONE unbuilt rung (the keystone wire) — spec'd, sub-decisions resolved, awaiting Tim's build-call. The talk leg is live by use; the keystone adds the generate-half. ONE spine — the V, the units, the gallery, the generation, the mode-cascade — all resolve through the address / registry / token system. Build == use.
