# THE COMPANY — WHOLE-TERRAIN LANDSCAPE (synthesis)

*Tim directed (2026-06-21): not a named query — "everything there is to find, by area; list the directory and allocate." 12 read-only agents swept all 49 top-level dirs → one AREA-*.md each (the durable detail). This file is the central SYNTHESIS (projection's judgment over the 12 extractions). The popcorn certain-landscape sweep (POPCORN-BUILD §C).*

## THE ONE PICTURE
The Company is a **typed compositional dataflow + cognition engine**, wired by a **fractal registry system**, surfaced through **one operator instrument** + **one MCP face**, coordinated by a **session fabric**, dressed by a **design system (DNA)**, with a **voice subsystem** and a **memory substrate**.
```
        registries (the typed vocabulary — ONE file-discovered mechanism, ~20 vocabularies)
              │  nodes·roles·projections·bindings·axes·mark_types·item_types·
              │  decision_subtypes·relation_types·forms·checks·dials·minds·…
              ▼
   runtime/ ENGINE ── cognition (run_role/items/reduce) · scheduler · resolver(4th primitive) · rule-engine
        │              ~130 /api routes · governance posture · decision stack · universal projection
        ├── surface/ THE INSTRUMENT (React, :5174) — wheel/grid/separator/nucleation · faces (decisions·sessions·channels·tools·RHM) · DNA-rendered cards
        ├── mcp_face/ THE TOOL FACE (66+ tools, op-selector design) + contracts/ (the C1–C8 spine) + schemas/ (vi-vision canonical)
        ├── session fabric — supervisor · channels · clones · recall · scan · lineage (cross-session coordination)
        ├── design/ DNA — tokens · archetypes · blueprint(J-surfaces) · mockups (synced into the surface gallery)
        ├── voice/ — 5 TTS engines + 3 STT ears · VRAM co-residence · DORMANT-by-default activation
        └── memory — channel-memory · operator_memory · introspection(mirror-registry) · the store (ext4, CAS+refs+vectors+marks)
```
Scale (real, not skeleton): ~130 API routes · 66+ MCP tools · 16 node types · 11 projections · 9 bindings · 7 axes · ~20 registries · 24 decisions · 97 role files · 5 TTS engines · 234 test files.

## THE AREAS (one line each → the durable map)
1. **runtime** (AREA-runtime.md) — the engine: bridge.py (3370 lines, ~130 routes), cognition.py (2778), suite (one-brain-two-faces), scheduler, resolver, rules, governance, decision stack, session fabric organs, projection, brain_router, implement-wire.
2. **surface** (AREA-surface.md) — the instrument: App orchestrator, 3 form-factor layouts, the faces, the subscribe-stores, the synced DNA gallery, the event+/api wiring map, 12 named gaps.
3. **registries** (AREA-registries.md) — the typed vocabulary: all 24 decisions (+ explanation_source coverage), 4 decision_subtypes, 11 mark_types, 6 item_types, 4 stack_item_types, 6 relation_types, 53 kinds, forms, checks, board_edges + the root state files.
4. **interface** (AREA-interface.md) — mcp_face (66+ tools + the two-door access control), contracts (C1–C8 + capability/platform mirror-registry), ui-contract (31 resource files, machine-checked), schemas/vi-vision (the 14 canonical archetypes), store.
5. **canvas** (AREA-canvas.md) — the map-computation: 16 node types, 11 projection lenses, 9 bindings, 7 axes, 8 flows, 2 dials.
6. **cognition** (AREA-cognition.md) — 97 roles (incl explain_role), 4 minds, 6 generation_policies, lifters, 7 ai_tics.
7. **design** (AREA-design.md) — DNA tokens, the J-surface blueprint, 23 mockups, the _system machinery, orphan-routes.json (built-but-unwired catalogue).
8. **memory** (AREA-memory.md) — channel-memory vault, operator_memory (28+ rows about working with Tim), introspection mirror-registry, the live channel/clone state.
9. **ops** (AREA-ops.md) — the `company` CLI, services.json, systemd units/timers, model/GPU/VRAM management, platforms, skills, the run/deploy substrate.
10. **voice** (AREA-voice.md) — the STT→cognition→TTS pipeline, 5 engines (1 blocked), VRAM co-residence, venvs, personas.
11. **tests** (AREA-tests.md) — 234 files, *_acceptance.py pattern, the suite-health gate, the coverage map + gaps.
12. **build-prep** (AREA-buildprep.md) — the 461-file PLANNING/INTENT/LOOP substrate: every commission, contract, anchor, loop-doc, the SYSTEM-GAPS backlog.

## ★ THE DOMINANT CROSS-CUTTING PATTERN (the judgment that reframes everything)
**The Company is built SUBSTRATE-AHEAD-OF-WIRING, everywhere.** Capability is laid in; the consumer / the live-wiring / the content trails. This is deliberate (seams-wired-ahead, replaceable-by-design) — but it is EXACTLY why the confirmation-bias kept recurring: an agent sees the built substrate and reports "done"; the trailing edge (wiring/content/use) is the actual unfinished part. The landscape makes the trailing edge visible at scale. The recurring instances (≥15, one shape):
- **orphan-routes.json** — built `/api` routes with no FE caller (the whole role/rule AUTHORING surface = to_build_ui; /api/knobs, /api/run-stats = to_wire).
- **dials** (anticipation, stability) — declared, **zero consumers**.
- **viewer/expertise axis** — wired, **value_source PENDING** (no operator-mode state) → control-density can't resolve.
- **common_knowledge space** — declared, **EMPTY** (publish pipeline hasn't run).
- **stack_item_types** — 3 of 4 (explanation/presentation/verify-request) declared, **not rendered**.
- **model_routing.py** — Phase 1; **NOTHING wired through it** (+ silent-floor-to-4B hazard).
- **mcp_face PHASE 2** — 37 operator tools **deny-by-default** until attribution lands.
- **activation loop** — built, **DORMANT** (env gate).
- **generation_policies ladder** — exists; **run_role reading it = separate pass, not built**.
- **remote.py** (the Claude Design connector) — **skeleton** (dispatch pending).
- **platform InvocationKind** — 4 of 5 adapters **unbuilt** (rest/graphql/mcp/library).
- **explanation_source** — **16 of 24 decisions MISSING** (the grounding gate-3 — NOT special; this same trailing-edge shape).
- **/api/brain/ask** — transport **400 bug** (the RHM grounded-mind, L2 — down now).
- **voice cosyvoice** — engine **blocked** (deps).
- **session_recall/lens/lineage** — built, **no acceptance tests**.
⟹ The leverage frame: the remaining work is overwhelmingly **wire / light / fill the trailing edge of what's already built** — NOT build more substrate. This landscape is the map of exactly where that edge is.

## STALE / DRIFT (docs lying about reality — fix-on-sight candidates)
- **README.md says "🦴 Skeleton only. Nothing implemented."** — the single most stale artifact; the system is large + live. (registries area)
- **STATE.md** dated 2026-06-07 — needs a refresh for everything since.
- **operator_memory AGENTS.md** lists 7 rows; 28+ exist; **missing the 2026-06-21 directives** (lead-drives-everything, minimize-gating, friction-is-gap-sensor).
- **voice ENGINE_PORTS hardcoded in bridge.py** — drifts vs services.json (Mirror-Registry-Law violation, F-FIX-11).
- **contracts/tools.py TOOLS dict (25 verbs)** — stale vs the live 66+ tools.
- **ops:** litellm :4100 down post-reboot (cloud-models path down); BOOT/WINDOWS-BOOT docs claim auto-start that's FALSE (company.target disabled); **main branch 1339 commits ahead of origin** (huge uncommitted set).
- **Worktree staleness risks:** concurrent-cognition (~/company-cognition), episodic-memory (~/episodic-memory) — may not be current main.

### CODE-LEVEL DRIFT (found in the deep sweeps — additional anti-confirmation payload)
- **NAME COLLISION: `projection.py` (the instrument) vs `projections.py` (the corpus LENS registry)** — and `tests/projections_acceptance.py` tests the WRONG one. A real trap for anyone editing either.
- **`channel://` and `cluster://` are USED but NOT in the SCHEMES tuple** (contracts/address.py) — addresses that resolve by luck, not by contract.
- **`_CORPUS_REGISTRIES` 3-way drift** — 6 rows actual / tests assert 4 / create.py `_DRIFT_HOME` missing `mind`.
- **2,793 UNREACHABLE vectors** in vectors.bge-backup-20260615/ (+ the recall layer is 43,991 vectors, DONE+TRUSTED per DRAGNET-RESULTS.md).
- **`lifter_registry.py` rename (2026-06-18)** — old `from runtime.lifters import…` is silently broken if any caller still uses it.
- **`vi_vision.py` duplicate principal/JWT code** — two parallel copies until the supabase_principal.py migration runs.
- **form_meta 1/66** — 65 tools still render as raw machine-knob walls (the tool-face content track).

### BUILD-PREP NAVIGATION (461 files — which prep areas are LIVE vs SUPERSEDED)
- **LIVE / freshest:** the-one-application/ (MORNING-STACK · COMMISSION-COMPLETION-REGISTER · THE-FACE-ANCHOR · RESOLVER-BUILD · TOOL-FRICTION-LOG · POPCORN-BUILD) · universal-projection/ (OPERATOR-SURFACE-LOOP = the living spec) · cognition-self-improvement/ (SYSTEM-GAPS · MCP-DESIGN-PRINCIPLE) · unify-laws/ (four root axes) · company-landscape/ (this sweep).
- **SUPERSEDED:** instrument-surface/ (→ universal-projection) · coordination/ (→ channel system) · cognition-engine/ (→ concurrent-cognition) · episodic-memory-adaptation/ (→ dragnet recall, per [[feedback-no-episodic-memory]] — but BEAT1's forms/lifters R9 gap-fix is still relevant).
- **UNCERTAIN/INCOMPLETE:** concurrent-cognition/ (BUILD-READY but merge-to-main uncertain — swarm fires listening/voice only) · self-serve-core/ (missing Implementation Guide) · trigger-system/ (design COMPLETE, build NOT started — the biggest pending build; its crux: recognisers are Python callables the create() gate refuses → move to RULE_OPS data).
- **AUTHORITATIVE CORRECTIONS (read these, not just the triad):** concurrent-cognition/review/R1-FOLD.md + R2-FOLD.md.

## SYSTEMIC GATE-3 FEED (what the dragnet systematizes for the backfill)
The grounding backfill (explanation_source) now has its concrete list from the registries sweep: **MISSING on 16/24** — build-consent-posture, card-visuals-source, connector-full-access, control-density, core-shape, cube-3d, dimension-meaning, figure-gold-value, line-language, lock-card-look, opacity-meaning, real-v-symbol, reconnect-tools, steer-visual-direction, substrate-spine, visual-fidelity. (control-density + steer-visual-direction origins already found by projection's fallback; the rest are the systematic backlog.)

## THE ELEGANT SPINE (what is genuinely coherent + done)
- **ONE file-discovered registry mechanism** across ~20 vocabularies — add a kind = drop a file, zero engine code. This is the architectural spine and it holds.
- **ONE renderer** (DNA.renderArchetype) for every card-type; **ONE brain-turn engine** (forkBrainCore); **ONE cognition engine** (run_role) shared by bridge + MCP + swarm; **ONE resolver** (the 4th primitive). The unifications are real.
- The **fail-loud / no-silent-fallback** discipline is pervasive and consistent (degrade-clean, teaching-errors, fsync-durable store).
- The **operator-law** (never show machine names; translate to human meaning) is enforced at the render boundary across the surface.
