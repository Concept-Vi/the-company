# ORGAN REBUILD STUDY — THE BOARD (the addressed-discourse surface)

*(④ the-one-system · fusion session, 2026-07-02. Observed/Inferred tagged; live queries against cvi_mine + the engine's 690 items.)*

## (1) EACH SIDE — five artifacts, one latent organ

**A1 · projects.notice_board JSONB inline — the embedded form**
IS [O]: a JSONB array on projects; items {post_id, post_type, status, content{title, priority, description, issue_number}, created_at}. Counts: block-composition 128, project-system 25, ci-processing 9, universal-types 6. WORKS: sync trigger appends (schema.sql:7715-7745); status functions aggregate over the array; the board travels with the project row. REACHING: the board as part of the project's one-call context. **The smoking gun** [O, migration 20260412070000 header verbatim]: "migration 20260411110000 moved notice board to a normalized table, but get_project_dashboard still reads from projects.notice_board JSONB. Result: board views show 128 stale items instead of 160 live items." Two stores, no derivation law — the view read the corpse (block-composition's JSONB froze at 128 while the table grew to 279).

**A2 · notice_board_posts — the operational store (the 319)**
IS [O]: post_id, project_id, post_type, status, content jsonb, timestamps. **No author column; no pinned column.** Distribution: issue 121 · observation 61 · milestone 58 · design 32 · task 31 · blocker 7 · cognitive_guide 3 · request 3 · research 2 · diagnostic 1. Status: open 184 · resolved 122 · closed 13 — only these three, for every type. Per project: block-composition 279 · project-system 25 · ci-processing 9 · universal-types 6. **author_type NULL ×319** (neither column nor in content — queried both); agent identity leaks only via content.resolved_by (16) / closed_by (8). content keys: priority+issue_number ×319, title 300, description 299, then a 35-key one-off tail (root_cause, workaround, bugs_fixed_count, template_location…) — un-registried drift inside the blob.
WORKS [O]: add_notice_board_item() inserts + per-project issue_number + emits notice_board.item_added channel event (1787-1824); update moves status/content (25531); an MCP tool (3706); dashboards enrich with type icon/color via the types registry (11930, 9355); keeper context summarizes it (21779+); decorators fire notice_board.decorator_matched → agent dispatch; agents create items — the loop closes (12230-12263). REACHING: the board as the project's operational event surface — countable, filterable, agent-triggering.

**A3 · ci_project_notice_boards — the social skin (designed, dead)**
IS [O, migration 20260213:244]: notice_type (announcement/update/question/decision), priority, title, content, **author_type ('user'|'agent') NOT NULL, author_id NOT NULL**, visibility, **pinned + partial index (project_id, pinned)**, expires_at, view_count, response_count. WORKS: it doesn't — absent from cvi_mine; PM-DB verification: "DOES NOT EXIST — dropped (no migration)". REACHING: the SOCIAL noticeboard — polymorphic authorship, pinning, visibility, expiry, engagement. Designed the community surface; never shipped.

**A4 · notice_board_types + universal_types.generates — the generator**
IS [O]: 17 rows — type_name, post_schema (JSON-Schema per type), renderer_component, icon, color, supports_threading, status_values[], is_active. `decision` IS a row (DecisionCard, {proposed,decided,reversed}). WORKS: fed by universal_types (16 rows generate notice_board_type; sync copies notice_board_spec in — 24024-24038). One type row → tool + card + board registration. REACHING: declare once, get everything. But [O from A2]: declared status_values are **not enforced** — tasks declare {todo,in_progress,blocked,done}; all 31 live tasks use open/resolved/closed. Registry as decoration, not law.

**B · the engine (runtime/cc_board.py, 528 lines + 690 live items)**
IS [O]: file-per-item channel-memory/noticeboard/<id>.md — YAML frontmatter + markdown body, git-tracked, atomic writes. Address board://<id>, deliberately FLAT ("identity must hold nothing mutable" — type can change without re-homing). Frontmatter: id, address, type, source, state, title, author_session, channel, thread, links, order, created, updated, history — OPEN (any extra key persists; an unlisted key used to be "real data loss"). Three file-discovered registries: item_types/ (11: block, note, guide, document, request, signal, message, idea, tip, issue, artefact) · source_types/ · board_edges/ (13: part_of, commented_on, references, sourced_from, attached_to, authored_by, blocked_by, reply_to, refutes, same_law, promoted_from, composes_with, attachment) — reusing RelationTypeRegistry VERBATIM, structural edges deliberately apart from corpus-semantic relation_types/.
Live grain [O]: types block 276 · note 245 · guide 36 · document 35 · request 27 · signal 24 · message 20 · idea 12 · tip 8 · issue 6 · artefact 1. Edges: part_of 277 · commented_on 235 · references 50 · sourced_from 41 · attached_to 22 · authored_by **8** · same_law 4 · reply_to 3 · refutes 3 · promoted_from 2. Authors in ONE untyped string: ch-3mpkjg3r 291, **tim 121**, lead 47, chatgpt-gpt-5.5-thinking 25, operator 21, named agent roles. Channels: dragnet-development 394, operator-surface 143, '' (global) 94, provider-registry 30.
WORKS [O]: file/get/list/transition/edit — lifecycle lives ON the item-type row as a full transitions map; transition() refuses illegal moves fail-loud + appends history (a provenance ledger every item carries). traverse (links through cognition.resolve_address — board→session→skill hops cross registries via the ONE resolver), reverse_traverse (**O(n) scan**), relations, comment/reply/thread on ANY address (comments are board items — threads recurse), assemble_document (ordered containers). Resolution: cognition.py:1292 board:// beside session/cap/clone/mind/decision. Surfaces: MCP face (10 ops), /api/board ("spatial zones (DNA's board-map), never a text-wall"), CLI (company board — pure file I/O), attachment binding. **The inbox seam** [O]: suite.inbox_lanes() (7562) contains NO board read — the traffic runs the other way: decision_decided_signal files a board signal item on every decide (attached_to → decision://); board-write failure surfaces as board_error, never breaks the decide. tool_meta.json has NO cc_board row [O] — the human overlay never authored. Honest ledger in the header: items NOT in any recall index — the recall↔board wire a declared follow-up.
REACHING: the board as a registry of addressed discourse in the one graph — "where the Company talks ABOUT ITSELF." Its drift (276 blocks + 35 documents) reveals the latent organ is bigger: **the addressed-discourse store**, already being used as a document store.

## (2) COMMON CORE · UNION'S EDGES · IMPLIED-BUT-ABSENT

**COMMON CORE** (independently converged): a typed item (type+title+body+status+timestamps, uuid-grade identity) · type as a registry reference carrying per-type state vocabulary + per-type rendering · both moved AWAY from the blob to one-row-per-item · both authored by humans AND agents in the same store · both wire item-creation to the channel/event layer · both feed a composed surface, not raw rows.

**UNION'S EDGES** — A only: the SCOPING axis (board per project) · per-type post_schema JSON-Schema · the universal_types generation seam · decorator→agent dispatch off board events · per-scope issue_number sequences · pinning/visibility/expiry/view-counts + typed author (designed) · dashboards/counts first-class. B only: addresses in the one resolved graph · typed directional edges with registered kinds · traverse/reverse/relations · ENFORCED transition maps · per-item history provenance · comment/reply/thread on ANY address · documents as ordered containers · promotion with provenance (identity survives type change) · git-tracked human-readable records · fail-loud.

**IMPLIED-BUT-ABSENT**: (1) **typed authorship** — A3 designed it, A2 shipped none, B has an untyped string + authored_by on 8/690 · (2) pinning/salience — and A3's index was (project_id, pinned): pinned PER BOARD, a view property · (3) enforced per-type lifecycle at scale — A declares and ignores, B enforces over 11 types · (4) an indexed equal-and-opposite (B's reverse is O(n); A has no links at all) · (5) payload validation (A has post_schema unused; B validates structure, never body) · (6) recall/meaning search over items (B's own header declares it) · (7) expiry/archive — lifecycle END exists nowhere live.

## (3) THE REBUILT ONE

**What a board-item IS** (the family's own trajectory, not a preference): *an addressed record (board://<id>, flat, identity holds nothing mutable) whose type is a reference into the one type registry, whose state moves only along the type row's declared transitions with an append-only history, and whose relations are typed directional edges with registered kinds and inverses into the one addressed graph.* B implements it literally; A converged toward every piece and stopped exactly at addresses and edges.

**Structure**
- **One store, many boards.** The item store is singular; every BOARD is a VIEW (projection filter) — "no way of looking disqualifies others." The smoking-gun migration is the proof: the failure mode was a view welded to a second store. A1's embedded read survives as a DERIVED projection into the project's composed page, never a second copy.
- **scope is an address**: new canonical field scope: project://block-composition | channel://dragnet-development | global root. A's project boards and B's global board = one organ at different scope values; B's channel:'' (94 items) becomes the explicit global scope. list(scope=…) is the same projection list_items(type=…) already is.
- **Author is an address**: session://ch-3mpkjg3r | operator://tim | agent://capability-expander. author_type collapses into the scheme — you know it's an agent because the address RESOLVES to one. The authored_by edge becomes DERIVED from the field (equal-and-opposite, indexed) — fixing both the 8-of-690 inconsistency and the O(n) reverse. A3's dead design finally lives, as resolution rather than a column.
- **Types**: B's item_types/ row shape (states+transitions, enforced) is the base; A4 contributes post_schema (validate typed payload at file-time), renderer/icon/color, supports_threading. A's generates seam adopted: one type row emits the MCP ops, the renderer binding, the board registration.
- **Pinning/salience/ordering are VIEW-records**: a pin is a typed edge from a board-view record to an item. Pinning on MY board doesn't pin on yours. visibility/expires_at land as optional typed fields (both stores already open/additive).

**Mechanics** (reuse-don't-parallel: B's runtime is the seed — the only side live and law-abiding)
file/get/list/transition/edit/comment/reply/thread/traverse/relations/assemble_document stay THE ops. Additions: scope param; a derived edge index (rebuilt from rows, never hand-maintained); the recall sweep (items → embedding axis on board:// addresses). System of record = the git-tracked file-per-item store resolved by the ONE resolver; **the Postgres form becomes a DERIVED projection** for dashboards/counts/RLS (A2's real strength), regenerated from the store — the polarity the family's own failure dictates [I as the correct polarity; never two primaries]. Events: item.filed/item.transitioned on the channel layer (A's item_added + decorator-dispatch grafted onto B's channels) — the board stays the thing agents are WOKEN BY. Operator surface: **the board does NOT become the inbox** — inbox = "needs the operator"; board = "the discourse"; a board item escalates into the inbox by explicit surfacing, never by existing. Author the missing tool_meta row.

**Data landing (nothing loses its grain)**
319 posts → 319 items: post_id→id (board://<uuid>) · post_type→type (issue/request exist in B; observation, milestone, design, task, blocker, cognitive_guide, research, diagnostic = **8 new item_types/ rows — a row-add, not code**) · status open/resolved/closed → legacy states in each new type's transition map (A never used its declared states anyway) · content.title/description→title/body · priority/issue_number→typed extra fields · the 35-key tail persists as-is (B's open frontmatter, zero drop) · resolved_by/closed_by/resolved_note → synthesized history entries ({to: resolved, by, note}) — A's only authorship traces become provenance · project_id→scope · timestamps preserved. **The stale JSONB is NOT migrated** — the superseded copy of the same rows (128⊂279; the three small projects' JSONB=table); one provenance note, not a second import. The 690 engine items gain only scope (channel→channel://<name>; ''→global) + the derived authored_by index. A3 contributes fields; there is no A3 data. 

**The decision boundary**: decisions keep their OWN registry (decision:// — where the decided VALUE lives). The board carries the discourse AROUND decisions: proposals, observations, and the signal items B already files on decide (attached_to → decision://). A's evidence agrees: decision exists as a TYPE row, but **zero of 319 live posts are type decision** — the cloud never stored decisions on the board either. Rule: *the board is where a decision is argued and announced; the decision registry is where it is held.* (Mapping rule for future decision-ish posts: [I], get Tim's eyes.)

**Genuinely new**: scope-as-address · author-as-address with the derived equal-and-opposite index · pin/board-view as first-class records · payload-schema validation at file time · the generation seam applied to engine registries · the recall wire · DB-as-derived-projection with the derivation law A1's failure proved necessary.

## (4) EACH SIDE'S PARTIALITY
A1 — the embedded read; blind to identity/query/concurrent writers; decayed into the stale copy. Lasting contribution is negative evidence: a board projected into a container must be derived, never stored there. A2 — operations (counts/filters/dashboards/MCP/sequences); blind to who speaks, to relations, to its own registry; the store without the graph. A3 — the social skin (typed author, pin, expiry, engagement); blind to shipping. A4 — generation; blind to enforcement and mechanism. B — graph purity (addressed, typed-edged, lifecycle-enforced, provenance-bearing, fail-loud); by its own ledger un-recalled, single-scoped, author-untyped, reverse O(n), payload-unvalidated, no salience, no tool_meta. The substrate half without the social/operational half — and its drift reveals the organ is the addressed-discourse store, of which every board is one way of looking.

**Each prototype is one organ-system of the same body: A1 the projection, A2 the operational store, A3 the social skin, A4 the generator, B the skeleton and nervous system.**

**Key refs:** runtime/cc_board.py · cognition.py:1292 · board_edges/AGENTS.md · item_types/ · channel-memory/noticeboard/ (690) · decision_registry.py:354 · suite.py:7562 · bridge.py:1625 · ops/cli/board.py · schema.sql:1787,7715,11930,12230,24024,25531,33483 · migrations/20260412070000 + 20260213:244 · docs/PROJECT_SPACES_OMNIFILE.md:2345 · cvi_mine live queries.
