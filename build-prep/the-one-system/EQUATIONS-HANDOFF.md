# FOR THE EQUATIONS SESSION — the language, its registries, its theory (a curated map)

> **From:** the language seat (Glyphics + the generative language; fabric handle `ch-518m76r0` — reachable live
> via `cc_channel(op="send", to="ch-518m76r0", …)`). **For:** the session formulating the master equations.
> **Why this list:** Tim is building from both ends — your equations are the outside-in half; this estate is the
> inside-out half; they meet at the frame law. These are the most explanatory sources on the universal language,
> the type registries, and Glyphics + their theory — each with what it *gives the equations*. All paths absolute
> on this machine. Everything herein: **author Tim Geldard**; the files are AI crystallizations of his
> articulations (hold them critically — the understanding is the spine, no file is authoritative over it).

---

## 0 · If you read only three things

1. `/home/tim/company/build-prep/the-one-system/glyphic/THE-GENERATIVE-LANGUAGE.md`
   — the unified doctrine: **18 laws** compressed from the whole corpus + the face-by-face fusion map.
   *Gives you: the complete law-set to formalize, already deduplicated across the rooms.*
2. `/home/tim/repos/counterpart/design/docs/THE-THINKING.md`
   — how the whole thing was grown + **one idea wearing many faces** (relational identity in colour, space,
   structure, narrative, reading, time). *Gives you: the invariant the equations must express in every dimension.*
3. `/home/tim/company/design/claude-ds/analysis/glyphic-language/UNDERSTANDING/03-the-intent-and-trajectory.md`
   — Tim's confirmed vision + **the FRAME law verbatim**: node ≡ frame ≡ type ≡ registry ≡ coordinate;
   everything a frame over invariant data; "ultimately it's ONE, relative to variable unity, invariant."
   *Gives you: the master variable. If the equations have one primitive, it is the frame/resolve operation.*

## 1 · The universal language — theory

- `/home/tim/repos/counterpart/design/docs/command/SUBSTRATE-ARCHITECTURE.md` — **Tim's verbatim transmission**
  (canonical) + its hydration: every folder/file an address in a typed graph; types declare valid edges; every
  typed edge directional with an equal-and-opposite; attachments; ghost nodes; time as a coordinate.
  *Gives you: the discrete structure — the graph the equations quantify over.*
- `/home/tim/repos/counterpart/design/docs/LINEAGE.md` — the 453-entry emergence record. The equation-dense
  entries: **[2702]** (two perpendicular motions: values cascade down, structure composes up), **[2704–2726]**
  (multiaxial reading: descend/traverse/fields/walks; "any forward walk respecting the orderings is a valid
  reading"), **[2727–2728]** (spans not points; nested-fraction/mixed-radix addresses; 7/15 decodes to its path),
  **[1416]** (measure: children partition the parent's [0,1]; fusion = conservation), **[2804–2827]** (motion:
  change budget, diff/LCA, count ≈ distance ÷ budget). *Gives you: the derivations in the order they arrived,
  with Tim's own words beside each.*
- `/home/tim/repos/counterpart/design/docs/SPACES.md` — **the four spaces** (SUBJECT soft polygon · TELLING =
  cut + route · STRUCTURE sharp/calculable · READER demand-side) + the two halves + the falsifiable-predictions
  ledger. *Gives you: the state-space decomposition — which variables are calculable and which are authored
  inputs, and the theorem that the boundary between them moves with the reader.*
- `/home/tim/repos/counterpart/design/dna/canon.json` (+ generated `docs/derived/CANON.md`) — the whole system
  as one 7-sector tree obeying its own laws, laws written content-free for exactly this merge: "adopt the laws
  sector, bring your own faces." *Gives you: the axiom list, machine-readable.*
- `/home/tim/company/build-prep/the-one-system/NORTH-STAR.md` — the ledger as a **multi-axis coordinate space**
  (graph · vectors · scale · time · provenance · paths · addresses); transcripts as the ROOT of all data;
  one function → two faces (MCP + UI). *Gives you: the world-model the language ranges over.*
- `/home/tim/company/design/claude-ds/analysis/glyphic-language/UNDERSTANDING/00-the-thinking.md` — the method
  layer (substrate-vs-views, coverage-is-a-diff, output-fidelity ≤ input-fidelity, nothing-canonical→fusion).
  *Gives you: the epistemics — what counts as evidence for a law.*

## 2 · The quantitative laws already latent (the proto-equations)

These are the places where a number is already DERIVED, with proofs that re-derive real artifacts:

- `/home/tim/repos/counterpart/design/dna/grammar.json` + `engine/prove/resolve.mjs` — **the two atoms:
  base unit u = 8, signature ratio r = 1.5.** Space = u·k over a small vocabulary k ∈ {3,4,5,7,11} (linear);
  type/radius = base·rⁿ (geometric); zones = fractions of the axis (scale-free); density multiplies.
  Proof: 11/11 — the rules re-derive the source mockups' geometry.
- `engine/prove/resolve-sequence.mjs` (same repo) — the narrative resolver: roles + dial envelopes + elastic arcs
  re-derive the source deck's exact structure, 7/7. *Sequence as a solvable system.*
- **The zone formula** (`/home/tim/company/design/claude-ds/core/containers.css:76-92`):
  `surface = mix(pigment, ground, depth · 2.1% · intensity)` — nesting depth as a computed visual quantity,
  theme-invariant because it mixes toward the ground.
- **The address algebra** (`dna/address.json` + LINEAGE [2728]): children partition [0,1); units are SPANS
  (slide 2 of 3 = [1/3, 2/3)); a path collapses to one mixed-radix number and decodes back
  (zone 3/5 in slide 2/3 → 1/3 + (2/5)·(1/3) = 7/15). Addresses derived, never assigned.
- **The motion equations** (`dna/motion.json` + docs/MOTION.md): the transition = the address-DIFF between two
  views; the anchor = the LCA of the changers' addresses; **count ≈ understanding-distance ÷
  change-budget-per-page** (why deck counts cluster); point density follows RATE-OF-CHANGE of the view, not
  distance. Tours-direct evidence (a shipped product).
- **Signal economy**: signal = area × chroma (the budget self-balances); rarity ∝ 1/frequency across colour
  (gold), geometry (facet-count), materiality (embossing), time (motion). One conservation-flavoured law,
  many dimensions.
- `/home/tim/company/build-prep/the-one-system/glyphic/live-instrument/findings/AREA-21-spatial-proportion-zones.md`
  — every spatial formula in claude-ds captured VERBATIM with file:line (ring trig, n/x even-spread, 0..1
  authored coords, DAG layering, the stable-slot pitch that pins nodes as siblings arrive, h = w/ar ratio
  types, the ratio-invariant % grid). *Gives you: the existing placement mathematics, exhaustively.*

## 3 · The type registries (the typed structure the equations bind to)

- `/home/tim/company/design/claude-ds/app/registry/types-core.js` — `CV_REGISTRY`: token→atom→block→system→
  surface→doc→template, single-inheritance via `extends`, slots + variables; the register/resolve/lineage/query
  API every registry mirrors.
- `/home/tim/company/design/claude-ds/app/registry/glyphic-type.js` — the full typed-node grammar: slots,
  sockets (`accepts`), triggers, values, state, click-actions. **A node is a full typed glyphic, never a bare
  triple.**
- `/home/tim/company/design/claude-ds/app/registry/kinds-type.js` — `kind.graph`: nodes accept glyphic/atom/
  block, edges accept relationship, layout a value-slot — the graph as a declared TYPE.
- `/home/tim/company/design/claude-ds/axes/axis-core.js` — `CV_AXES`: tokens are the value-units of an axis;
  subscriptions (`candidates`/`validate`, loud-fail); 9 axes (space/size/depth/motion/color/texture/fill/form/
  symbol). *A typed dimension = an axis whose values point at one home.*
- `/home/tim/repos/counterpart/design/registry/type-registry.json` + `registry/address-registry.json` — the
  substrate instance: every address typed, per-type valid edges, counts including zero, ghost nodes.
- The counterpart faces (all in `/home/tim/repos/counterpart/design/dna/`): `layouts.json` (frames, typed
  sockets, ONE focal per frame, NO numeric priority — "priority decreases compositionality"; reflow = policy) ·
  `shapes.json` (shape = f(entityType) + **cardinality**: octagon once) · `connectors.json` (three verbs ×
  direction, the seam law: only movement crosses a seam) · `molecules.json` (each record carries its `voice` —
  bimodality as a field) · `sequence.json` (roles, dial envelopes, tiers with fusion maps, reading_axes) ·
  `reader.json` (held context, occasion ladder, goodness-as-FIT) · `subject.json` (the polygon, the cut + the
  route) · `voice.json` (registers, 11 verbal forms bound to visual twins, predicate lints).
- Invariants carry a **checker class** — `predicate` (mechanical) · `build` (refuses to compile) · `model`
  (needs an AI judge). *Gives you: which laws are decidable and which require a judgment operator.*

## 4 · Glyphics — the tongue and its theory

- `/home/tim/company/design/claude-ds/analysis/LANGUAGE.md` — the founding spec. **The one law: meaning is a
  FIELD (a feeling + a cluster of senses), combinatorial** — the combination of facet values means, not each
  alone. The parts of speech: FORM = kind (circle = the kind itself · square = a specific one · triangle = an
  operation · diamond = interacting-with · octagon = a gateway) · SYMBOL = which thing · FILL+OUTLINE = mode ·
  COLOR = state · TEXTURE = material · DEPTH = prominence · MOTION = liveness · EDGE = the relation-carrier
  (line = mood: solid is / dashed could) · POSITION = word-order · CONTAINMENT = the copula. Fractal levels:
  facet (morpheme) → glyphic (word) → node·edge·node (clause) → container (sentence) → nesting (paragraph).
  **Glyphic = address: nouns are addresses, verbs are edges — the Company's address world and Glyphics are one
  language at two heights.**
- `/home/tim/company/design/claude-ds/assets/icons/cv-meaning.js` — the ENGINE of meaning (the one home):
  fields {feeling, senses, type}, loadable profiles, the combinatorial mode, `describe`/`describeRelation`/
  `readGraph` (the read-out — a graph says itself, with negation + conditionals), the live `author` API
  (the dictionary is DATA; Tim and the AI both write it). Loud-fail: an unknown meaning THROWS.
- `/home/tim/company/design/claude-ds/assets/icons/cv-glyphics.js` + `cv-edges.js` + `cv-icons.js` +
  `glyphgraph.js` — compose/render, the edge registry, 132 faceted symbols, the well-formedness rulebook
  (`validateGlyphgraph`).
- `/home/tim/company/design/claude-ds/system/glyphgraph-generator.html` — the live WRITER: sentence → extract
  → resolve-by-embedding → judge → graph → read-out; teach-by-click; the shared human+AI selection. *Gives
  you: the language exercised end-to-end, and the two-way door (write-by-meaning ⇄ transglyphing) working.*
- `/home/tim/company/design/claude-ds/glyph-corpus.json` — the meaning corpus as data: 132 `glyph://symbol/<id>`
  + 71 `glyph://field/<facet>/<value>` entries, embedded live in the shared vector store (space
  `glyph_meaning`, 2560-dim). *Meaning as positions in a metric space — probably your closest bridge.*
- `/home/tim/company/design/claude-ds/system/the-whole-thing.html` — the whole vision walked in the language's
  own laws (Tim's review page; being upgraded to render its diagrams through the live engine).

## 5 · The bridge notes for a formal model (this seat's read — correctable)

- **One primitive**: `resolve(address, frame) → value`. Everything else — cascade, LOD, reader-fit, chrome
  modes, device posture — is this with a different frame. Frames compose (nearest-scope-wins = a lattice meet).
- **Two motions, orthogonal**: values cascade DOWN (a monotone flow over the containment tree); structure
  composes UP (typed slot-filling). Conflating them is the historical failure mode.
- **Conservation laws**: the story is conserved under re-slicing (fusion); signal budget is conserved
  (area × chroma); identity is conserved through change iff the boundary holds (the ring law).
- **The two halves**: the calculable half is your domain (every number derived); the authored half (the cut,
  the route, the claims) enters as boundary conditions — and the SPLIT ITSELF is reader-dependent.
- **Everything flows both ways**: every typed edge carries its equal-and-opposite per type/level/scale —
  the adjoint is built into the grammar, never stored data.

*Living document — extend it as the equations firm; correction flows both ways (send yours back to
ch-518m76r0 or the board: doctrine = board://item-32f2bf9d, part_of the-one-system item-ecb0b169).*
