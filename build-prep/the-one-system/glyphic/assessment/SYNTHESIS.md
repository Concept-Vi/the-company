# WIDE ASSESSMENT — SYNTHESIS (the grounded gap-map to product-level-integrated)

> Synthesized 2026-07-03 from the five companions (AREA-A writer-vs-bar · B counterpart-fusion · C
> company-frontier · D canvas-spatial · E rigor), read as one set, skeptic weighed hardest. Every claim
> below traces to a companion's Observed evidence. This supersedes the corresponding sections of
> LIVE-INSTRUMENT.md and REGROUNDING §5/§6 where they conflict (staleness corrections listed in §1).

## 0 · The one-paragraph verdict
The foundation is real and the furniture EXISTS — it just lives in the other room. The instrument's
biggest gaps (placement, live-growth animation, arc, atmosphere/organisms, glass) are all **proven,
self-contained machinery in counterpart** waiting to be ported, not designed (B: resolve.mjs 11/11,
resolve-sequence 7/7, organisms.js 28 generators, motion.json's diff/LCA laws — all executed/verified).
The writer's shell is a bespoke specimen, **unreachable from the Studio** (A), on a CDN dev-scaffold that
cannot ship (E) — the product face is a RECOMPOSITION from the real components, not a polish pass. The
Company side needs near-zero new code for the mode and has an exact one-field fix for the VRAM wall (C).
Two things are genuinely Tim's: **the octagon** (a three-way shape-meaning conflict) and **the read-out's
register** (engine-correct but stilted — the real "can you hear the octagon?" gate). Everything else is
buildable now, additive, in five groups (§4).

## 1 · Truth corrections (stale claims killed by the wave — update the map, then never re-derive)
- **G1 is ~85% DONE** (E): zero pinned caps remain; ROLE_PROVIDERS + company-http built by A0–A6.
  Re-scope to "flip the binding + harden under load."
- **reactflow-vs-tldraw is a dead decision** (D): reactflow exists nowhere; tldraw only in canvas/app.
  The real shape: split by scale regime (writer-scale = DiagramSolver after 2 fixes · operator canvas =
  tldraw ShapeUtil if needed · 76k window = ④'s lane, genuinely unbuilt).
- **The layout-jump is HALF-solved** (E): stable-slot is wired but rank is recomputed every layout() —
  the freeze/address-derived placer is genuinely needed (and B/D supply its law + code).
- **The spacing language RESOLVED 146 commits ago** (B): a live, estate-wide vocabulary spine
  (grammar.json.scales.space.vocabulary, 47 counts, lint-space as the 7th proof). One Tim-gated
  word-choice remains (4 fine counts). REGROUNDING §5 corrected.
- **The VRAM wall is a one-field misconfig** (C): chat-4b-fp8 gpu_util reads 0.9 live vs its own
  documented fitted 0.44 — fixing restores full co-residence (brain+embedder+reranker+ear+voice).
- **L9–L11 are BUILT and live** (C): migrations 0014/15/16/21/22 applied; none touch glyph territory;
  L9's ledger.assertion is the closest mechanism for a persisted glyph edge.
- **counterpart is ACTIVELY MOVING** (B): 146 commits past the journal; a reorg (ui→surface etc.); the
  wayfinder/registry stale+empty (and structurally blind to source/ + reference/, 934 files); ④/"DNA
  Pack" mid-edit on tokens.json + archetype.js (broken in-flight). Coordinate before touching; port as
  COPIES, never edits.
- **think=False is free** (C): guided-JSON⊥thinking is structural (cognition.py:489) — every schema role
  gets it automatically. Not a gap.
- **"132 symbols" → 143** (E); "131 facets lack name/description" still true — only the 12 minted are
  full citizens. G2's de-risk: citizenship-backfill is data-authoring, not code.

## 2 · The two genuinely-Tim decisions (surfaced, not decided)
1. **The octagon — three shape-meaning tables coexist** (B §2b): counterpart's corpus-evidenced
   (octagon = THE core/hub, cardinality 1/sequence) vs CV_SHAPES.shapeTypes (octagon = Gateway/boundary,
   free recurrence) vs CV_SHAPES.entities (octagon = Virtual Hubs, the product). Options: (a) adopt the
   corpus-evidenced table as canonical (the other two are uncited invention); (b) keep three explicit
   AXES (semantic class / brand entity / corpus cardinality) that compose; (c) a frame Tim names that
   none of the three holds. Blocking only shape-by-entity-type resolution — W1–W3 proceed around it.
2. **The read-out register** (E §3, the predicted 80%): the engine's sentences are correct but stilted
   ("This X is the Y of this one" pronoun-soup). Coverage ships in W2; the REGISTER is Tim's ear —
   G4.5's law ("do not broad-build the wording until it rings true on ≥3 real chains"). The mechanism
   is live-authorable data (setRelation/setGloss), so the ear pass is a conversation, not a build.
   (+ the parked spacing word-choice: 4 fine counts await Tim's words.)

## 3 · Deliberate non-citizens this phase (named, not green-painted — identity-before-instance)
The writer/showcase SHELL (CDN react.development + browser-babel) ships as a citizen only in W3's
recomposition; until then it is scaffolding. The three parallel icon-gen UIs stay as-is until W3 fuses
them (investing in any one before the fusion decision is waste — A). counterpart's registries stay
UNTOUCHED by this seat (④'s live room); everything crosses as a copy with provenance.

## 4 · The build groups (sequenced; each verifiable by use; laws: loud-fail · derive-never-place ·
      additive on shared surfaces · board-announce ops touches · migrations next-free only)
- **W0 · TRUTH & HYGIENE** (hours, unblocks everything): fix core/cv-nodes.d.ts drift ("glyphgraph" in
  the union + 4 edge facets — D); fold §1's corrections into REGROUNDING/doctrine; the gpu_util
  one-field fix (ops/services.json, board-announced — C).
- **W1 · THE FURNITURE PORT** (additive, no gating): organisms.js → cv-organisms.js (re-point ~30
  --dna-* vars — B#2); address.json + resolve.mjs → the placement resolver (B#4/D); sequence.json +
  resolve-sequence.mjs → the arc resolver (B#5); CV_EDGES gains the verb facet (transport/gather(in|out)
  /descend/orbit + seam law — B#7); molecules' co-located voice-block convention adopted for minted
  glyphs (B#10).
- **W2 · THE LIVING CANVAS** (the core build): address-derived stable-slot in DiagramSolver (the real
  layout-jump fix — D); the growth animator from motion.json's laws (transition-is-the-diff + LCA
  anchors — B#1); fields-on-canvas (the ordinal ramp as a CV_AXES axis via the zone-wash mechanism,
  distinct from the scale pyramid — D); read-out COVERAGE closed (register waits for Tim's ear).
- **W3 · THE CITIZEN WRITER** (the product face): recompose from the real system — containers.css
  Band/Zone/Cluster, CanvasHeader, RefinePop, Icons.jsx's propose→adopt pattern (A); wire
  glyphic-type.js sockets (built, rich, unused — A); the relation-teaching call-site
  (glyphic.author-relation — one wire, A); fuse the 3 icon-gen UIs into one; production shell (vite-
  served, precompiled — kill the CDN scaffold); reachable from the Studio + gallery (@dsCard + canvas).
- **W4 · THE COMPANY WIRES** (each additive; collision-listed): modes/glyphgraph.py + a services.json
  combo (append + board-announce — C); burst-at-pause run_swarm wiring for the glyph roles (C);
  the voice.transcript SSE emit (the one seam, C); a reindex bridge route (emit corpus + rebuild
  glyph_meaning on demand); the persistence board-ask: "can a glyph edge ride ledger.assertion, or does
  it need its own table?" (C — coordination, not solo build).
- **W5 · THE EAR + THE OCTAGON** (Tim-gated, rides his availability, blocks nothing): the register pass
  on ≥3 real chains; the octagon decision; the 4 spacing words.

## 5 · Coordination (standing, from B/C/E)
④ live in counterpart (ports = copies; sync before the build phase touches anything it has open; its
window L8 is Tim-gated and not tonight's risk). Ledger session: L9–L11 live; stay additive on
ledger.embedding; ride ledger.query as the read contract; migrations next-free-at-write-time. The
collision surfaces are exactly: ops/services.json · the voice.transcript emit · the query contract.
