# Resolved-Context — Research Synthesis (proven vs open)

> The ground truth beneath the build. Full detail: RESOLVED-CONTEXT-A-mechanics (verified experiments),
> -B-architecture (the design), -C-directions (Tim's complete mandate + corrections). This is the condensed map.

## PROVEN (ran it, this machine)
- UserPromptSubmit stdout → model context (ZEBRA-77 experiment). SessionStart source∈{startup,resume,clear,compact};
  matcher:compact = post-compaction re-inject. Stop hook can block+feedback. Hook configs live-reload.
- Bridge: embed (pplx-2560 @:8007) + run_role (schema-validated, ~0.7s) + corpus-query, same-origin/localhost.
- Local Supabase: PG17.6 :15432 + pgvector + Realtime container; ledger.* resident (embedding session's store).
- Roles are file-discovered drops (eval_classify shape). Transcript = parentUuid tree (6315 nodes/560 leaves seen).
- Headless labs: `claude -p` + project-scoped settings + --output-format json (session_id) — the safe test bed.
- Board lifecycles: registry-declared transitions refused loud (draft→open bounced; the pattern to copy).

## GUARDED / CONSTRAINTS (respect, don't fight)
- In-place edit of a live session's jsonl = harness-blocked ("Session Transcript Tampering") → recomposition is
  boundary re-injection (CLI) or SDK-owned messages (full control; subscription-auth works headless).
- PreCompact is observe-only (cannot steer the summarizer) → use SessionStart:compact.
- Tim's global ~/.claude/settings.json: NOT touched overnight (labs only; global wiring with Tim).
- Auto-mode classifier can deny odd shell shapes — keep lab scripts plain.

## DECIDED (Tim, binding)
- Backend first; UI together tomorrow. All-wanted mandate on the full C-doc thread-spread.
- Correction 1: frame/node SCALE-RELATIVE → one recursive unit, address = containment path, no privileged level.
- Correction 2: anti-loss = the STATE AXIS — typed lifecycles + data-layer triggers (pg_notify/Realtime), never
  bespoke feature code.
- Store = LOCAL Supabase beside ledger.* (one store, coordinate naming with the embedding session).
- Tim's standout-tagged threads (salience, not exclusivity): many-faces · conversation-is-a-glyphgraph ·
  both-directions · LOD-on-speech · voices-as-identity+tunable · every-output-surface.

## OPEN (not blocking tonight; flagged)
- Embedding-match recall (v1) vs tag/recency recall (v0) — v0 tonight, embed-space next (rides ledger.embedding
  conventions; possibly a ctx space like glyph_meaning).
- SDK-hosted composition (full prune/replace) — designed, not built; after the CLI faces prove out.
- The form registry + per-destination rendering; member-registry contracts/cascade; identity unification
  (ch-* vs UUID vs agent://) — all in C-doc, sequenced after the substrate exists.
- Realtime (websocket) delivery to a future UI — pg_notify proven core first; Realtime publication when the UI lands.
- How far verdicts backfill old sessions (mine the existing corpus) vs forward-only — Tim call, later.

## The one-breath frame
Conversation becomes bounded, typed, addressed units on a containment axis with a state axis; judges + rules fill
a ledger over them; hooks resolve context in and validate form out; chains/forks give branches dedicated minds;
the UI (tomorrow, with Tim) is just this substrate made clickable. Two axes + an address space; the rest is
consequences — resolve-from-source applied to working memory, speech, and structure at once.
