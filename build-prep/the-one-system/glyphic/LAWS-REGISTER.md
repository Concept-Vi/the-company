# LAWS REGISTER — Tim's principles, found operating, named (provisional; grows)

> Tim (2026-07-09): "you have permission and encouragement to expand the concepts and principles to
> laws of mine you find." Each entry: the law, where it was FOUND OPERATING (not invented), status.
> Status: CANDIDATE (awaiting Tim's recognition) · RATIFIED (Tim's word, recovered) · ADOPTED (in build).
> Register: provisional — Tim recognises or rejects; wording tunes live. Evidence = wave-one-math/ + census/.

## Recovered RATIFIED law (Tim's own, found recorded — outranks everything below)
- **The movement canon** (counterpart dna/motion.json, Tim-ratified): anchor-is-the-LCA ·
  transition-is-the-diff · change-budget · boundary-carries-identity · arrival-relative-to-path ·
  anticipatory-verb · spatial-persistence · expert-lane. IMPORT WHOLESALE as the movement-axis spec.

## The identity family
1. **Identity is a minted id that RESOLVES to a current address; the address is never the stored key.**
   (CANDIDATE — rigor A2 proved raw addresses change on insert; the Company resolver + project://'s
   DB-generated address already work id-first. Sharpens "identity = addressability": addressABLE, not
   address-keyed.)
2. **The ancestry above the change is stable; below the LCA is re-derived position.** (CANDIDATE —
   rigor A2's tested refinement of "addresses come with their ancestry so always works.")
3. **Types are registered; instances must be too — registration extends to selections, groups, live
   things.** (CANDIDATE — canvases D5: every Type resolves via registry; no live section/selection ever
   becomes a row. mind://'s composition kind + the RG1→RG9 chain are the working precedents.)
4. **A declared inverse must resolve to a registered id — fail loud at registration.** (CANDIDATE —
   company-spine: relation_types/precedes declares inverse "follows"; no such registration exists.)

## The math family
5. **A lens changes the ratio, never the currency.** (CANDIDATE — discovered under pressure in the
   System Map's childValues; the unit-mismatch bug is the measured counter-example.)
6. **Freeze the inputs of a derivation, never its output.** (CANDIDATE — math-priors D2: slotFor
   freezes {index,capacity}, position always re-derives; the one principled exception to derived-never-stored.)
7. **Positions, counts and order are pure ratios; per-unit visual magnitudes remain authored tokens.**
   (CANDIDATE — rigor B3: the wash's 2.1% step is authored; the DEPTH driving it is derived.)
8. **A constant may buy breathing room between ratio-derived cells, never move which ratio produced
   the boundary.** (CANDIDATE — math-priors D4, the sunburst GAP.)
9. **1D partition and 2D packing are different citizens — register the sibling, never force-fit.**
   (CANDIDATE — math-priors: squarify is greedy area-packing, no closed form.)
10. **One fencepost law, declared once** (cells-of-n vs points-across-n-1 vs closed-loop-n).
    (CANDIDATE — three conventions currently mixed silently in one solver.)
11. **Derive, never place — a value a person had to type for a specific case is a defect.**
    (CANDIDATE for verbatim adoption — counterpart docs/RESOLUTION.md; the sharpest litmus form.)
12. **CASCADE and COMPOSE are different mechanisms: containment nests (nearest wins); other axes
    compose (multiply, none wins).** (CANDIDATE — counterpart RESOLUTION.md; guards §4 from conflation.)

## The relation family
13. **Relations come in THREE buckets: derived (containment+sequence, from the address math) ·
    authored provenance (who/what made this) · semantic (declared meaning fields).** (CANDIDATE —
    company-spine; bucket 1 is ALREADY Company law: "containment is derived, never stored.")
14. **The tree is the spine; edges are the web** — a hierarchy gives exactly one relational dimension
    free; every other relation is a typed, stored edge. (CANDIDATE — three independent sites: navgraph's
    tested law, LEDGER-SPEC §1, the anchor.)
15. **Structural first, semantic second, never merged, degrading honestly** (absent, never fabricated;
    a loud zero, never a silent gap). (CANDIDATE — X11's sibling edges + mechanisms.json's layer law.)

## The operation family
16. **Attribute operations map over member ids; structural operations require a minted frame — the LCA
    is the safety boundary, never the operation's target.** (CANDIDATE — rigor A3, proven by running
    lcaAll on a non-contiguous pick.)
17. **Speak through fields, never coordinates** — a raw k-of-n reaching a sentence is the octagon
    oracle one layer down. (CANDIDATE — rigor A5; cv-arc's role-named beats are the precedent.)
18. **Bounded traversal with a capped signal, never a precomputed closure.** (CANDIDATE — codeedges'
    DEPTH pattern; applies to group-cascade/reach queries.)

## The hygiene family
19. **Order is array position until an equation says otherwise — and array-position order is the
    defect class.** (CANDIDATE — the single most-repeated violation found: diffs by index, ids freezing
    creation position, three drag-reorder reimplementations.)
20. **A registered list and a hand-typed list look identical in a diff; only one degrades gracefully.**
    (CANDIDATE — canvases D4: CommandPalette holds both side by side; the hand-typed one is stale today.)
21. **Self-description drift needs a CHECK, not a charter sentence.** (CANDIDATE — design-machinery D5:
    the folder that states never-drift is measurably drifted; a Layer-0 mechanism enforces it.)
22. **Fixed kind→property tables are a recurring CLASS, not a retired instance** — standing grep, not
    one-time fix. (CANDIDATE — three more live instances found post-correction, one teaching the
    retired octagon table to an AI in a prompt.)
