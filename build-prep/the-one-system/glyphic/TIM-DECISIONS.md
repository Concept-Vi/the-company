# TIM-DECISIONS — the standing Tim-visible queue (glyphic)

> The one place every call that belongs to Tim's eye/ear/taste is CARRIED until he answers — never a
> gate (each has a DEFAULT the build proceeds on; his answer re-aims, live). Sources: the census
> (census/AREA-*.md), the advisor audit, glyphic-system.html §7 (his own recorded open questions).
> Updated in place as calls land. Nothing here blocks; everything here is findable.

## From the corrections + the census (defaults set, build proceeding)

1. **'face' / 'documents' — keep as verb-paired relations, or retire?**
   'face' has a thin recorded basis (LANGUAGE.md:48 — the page-face concept); 'documents' appears in NO
   Tim material and NO live graph (census/AREA-tim-canon §D1). R1 gave both law-shape (directed+inverse).
   **DEFAULT: 'face' stays (recorded basis, live consumers); 'documents' RETIRES** unless re-authored
   through this door. → best judged BY SIGHT in the live render when R3's canvas is up.

2. **The motion-verb axis (transport / gather / descend / orbit)** — removed as an inert table (nothing
   consumed it). Does a MOTION axis for edges return through the proper doors (meaning fields + Types
   with inverses)? **DEFAULT: not rebuilt until a real consumer needs it.**

3. **Inverse wordings are noun-phrases, not verbs** ("the container of", "faced by"). Your edge law says
   directional VERBS; the system's own prior-era decision made feelings noun-phrases so the read-out
   composes ("is" + "part of"), and LANGUAGE.md:47 itself frames edge as "the relation-carrier, not a
   'verb'". **DEFAULT: keep noun-phrase feelings (they compose under the mood word); tune wordings live
   through the author API as you hear them.** This is an ear call — made during generation, not before.

4. **Two form-vocabularies, never reconciled** (glyphic-system.html §7 asked you; unanswered):
   (a) LANGUAGE.md's sentence-words (gateway/system/feature/…) — now authorable field data (R2);
   (b) CV_SHAPES' type-class labels (Entity/Action/Object/Decision/Feature/System/Specialised/Gateway).
   They overlap but differ. **DEFAULT: both become field-shaped; (a) speaks in the read-out, (b) stays
   the type-class labels; the relationship recorded, neither frozen.**

5. **The 12 language symbols — depiction-only, or read-out participants?** Nothing today reads their
   description prose; symbols are your own intrinsic exception ("a house is always a house").
   **DEFAULT: depiction-only** — description becomes a search caption; glosses stay. If you want them to
   SPEAK (participate in transglyphing), that's new wiring — say so and it's built.

6. **Left-anchored vs centred rows (placement)** — the never-surfaced taste call, now CROSS-SURFACE
   (System Map rows also left-hug). Under the relative law, centred is what the math derives
   ((k−0.5)/n of the row). **Presented to your EYE in R3's live render — both, side by side.
   DEFAULT/RECOMMENDED: centred-by-derivation.**

7. **R3's blast radius** — the relative placement law lands on the GLYPHGRAPH case first. The
   pre-language layouts (hub/pipeline/tree/stack/…) are relationship-readings that should eventually
   become span projections under the same law. **DEFAULT: glyphgraph first, the rest as a named tail —
   not a silent same-pass rewrite.**

8. **The charter's wider charges** (they widen scope, so they walk through you before adoption):
   the provision/product bar ("frames without furniture" — organisms, atmosphere, evidence density,
   zones on the canvas) as explicit build items · two-wayness as a standing invariant · the 16-face
   convergence worklist · the narrative-arc organ (cv-arc is its unconsumed engine half).
   **DEFAULT: adopted into PHASE REMAINING as listed in ROADMAP — say stop/reshape if wrong.**

## Your own recorded open questions (glyphic-system.html §7, 2026-06 — still unanswered)
9. The v1 facet set (which facets ship first).
10. Form-axis ends: confirm 0 = no ring and circle = ∞; are 1-/2-side steps wanted? (n+1 is yours;
    circle=∞ was the AI's proposal awaiting your confirm.)
11. Do the 8 shape-type-classes match how you think about your entities? (= item 4 above.)
12. The value variable: which real value-sets first (status / entity-type / lifecycle)?
13. Taxonomy depth for symbols.

> Facet NAMES remain open by your own note ("rename freely"). All wording everywhere = starter,
> correctable live through the author API while generating — that is the standing gate (A5).
