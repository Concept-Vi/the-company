# REGROUNDING — the Glyphic / design-fusion re-boot manifest

> **Purpose.** A durable single entry point so a compacted/next-generation session regrounds fast and TRUE.
> This is a MAP (it points at the real docs, in reading order) — not a copy. Written 2026-07-02 by the
> opus session after Tim's course-correction, so half a session of history can be compacted away without
> losing placement. Read this first, then walk §1 in order.

---

## 0 · You are here — who, and THE STANDARD (read before anything)
You are the next-generation AI stepping into an **elevated role**: carrying the Glyphic language + the AI/Company
fusion + the **convergence of three systems into one**. All prior work was the best of the previous generation;
your job is to take it to **product-level, fully-integrated reality**.

**THE STANDARD (Tim's correction, 2026-07-02 — the governing bar, overrides any "done" claim):**
- **"Complete" = actually usable, polished, FINISHED design, full utility.** Technical passing of specific tests
  does **NOT** constitute done. It MUST be product-level to call it that.
- **Narrow building is a failure equal to doing nothing.** Building without integrating what else exists across
  the design system AND the Company is failure. Hold the WHOLE breadth.
- **Verify by USE + WHOLE-SCREEN + human eye. Never green-paint.** (Tim judges by recognition/sight.)
- The recurring trap to break: declaring "done" at technical verification instead of at product-and-integration.

**The mission:** bring **three systems into ONE** — `claude-ds` (the design system) + `counterpart/design` (the
other design codebase, converges INTO claude-ds) + **the Company** — with the Glyphic language built into a real,
product-grade, integrated instrument (talk/write → living glyphgraph, resolved from meaning, co-edited with the AI).

---

## 1 · Reading order (the dense path — walk this to reground)
1. **this file** (REGROUNDING.md)
2. **memory `project-glyphic-language.md`** — the always-loaded running record (every leg, newest facts at end)
3. **`claude-ds/analysis/glyphic-language/UNDERSTANDING/00-the-thinking.md`** — the METHOD + the relational whole
   + the live trust-map: the layer no code holds. The most important single read.
4. **`claude-ds/analysis/LANGUAGE.md`** — the Glyphic language canon (meaning-is-a-field, the grammar).
5. **The spatial theorem** — in `00-the-thinking.md` + memory (the `n/x` addressing, zones, level-of-detail,
   fractal glyph↔graph, `claude-ds/analysis/mid-lod.md`). Tim's theorem-pack; the placement system is barely started.
6. **The plan** — `glyphic/ROADMAP.md` + `glyphic/CRITERIA.md` + `GUIDE.md` + `SYNTHESIS.md` + the unification map
   `glyphic/live-instrument/LIVE-INSTRUMENT.md` + `glyphic/live-instrument/catalogue/` (the neutral substrate).
7. **The AI fusion (A)** — `glyphic/A-fusion/A-DESIGN.md` (union-not-bridge) + `A-CONTINUATION.md` (A0..A6 state)
   + `reads/READ-1..6` (first-hand code comparison; READ-6 = the double-prompt make-or-break).
8. **The research** — `glyphic/live-instrument/findings/AREA-1..27` (3 waves, both systems; the grounded inventory).
9. **The one-system corpus (the OTHER sessions' work this converges with)** — `build-prep/the-one-system/`:
   `NORTH-STAR.md`, `THE-CONTAINER.md`, `WORLD-INTERFACE-CHARTER.md`, `LEDGER-SPEC.md`, `SUPABASE-VECTOR-SCHEMA.md`,
   `EMBEDDING-MEMORY-SUBSTRATE.md`, `CODE-ADDRESS-RECONCILIATION.md`, `interface-map/INTERFACE-MAP.md`.
10. **counterpart/design** (the other design system to fold in) — `PROJECT.md`, `ONBOARDING.md`, `FORMATION_JOURNAL.md`,
    then its `dna/ engine/ factory/ pieces/ surface/ registry/ supabase/`.

---

## 2 · The three systems + the convergence
- **`/home/tim/company/design/claude-ds`** — THE HOME (decided). The design system: 4 registries (tokens · types
  `CV_REGISTRY` · the generative engine `core/` · AI `CV_AI`), the Glyphic engine (`assets/icons/cv-*.js` +
  `glyphgraph.js`), the surfaces (`system/*.html`), the gallery/DNA. Its charter: `design/claude-ds/CLAUDE.md`.
  Own git repo (design-side commits here); synced to canonical DNA Studio.
- **`/home/tim/repos/counterpart/design`** — the OTHER, richer design codebase (dna/engine/factory/pieces[56]/
  surface/registry/supabase). CONVERGES INTO claude-ds (islands-join-mainland — fold its best in, drop the parallel).
  Barely explored — a real integration task.
- **`/home/tim/company`** — the Company: `runtime/` (cognition, bridge, modes, ledger), `roles/`, `ops/`, the
  Supabase-backed `ledger.embedding` vector store, the RHM/operator surface, the systemd services. The AI's ONE home.
  Touch as deliberate convergence (charter updated 2026-07-01), never break a live service.
- **The seam:** design UNIFIES INTO the Company. The AI fusion made `CV_AI` a projection of the Company's cognition.
  The same move is owed everywhere (tokens, addresses, the canvas, data).

---

## 3 · The concepts (durable ideas — the content, not scaffolding)
- **Glyphics is a visual LANGUAGE** — meaning composed from facets; **meaning is a FIELD, not a lookup**; facet=
  morpheme, glyphic=word/noun-phrase, edge=relation (visual, not a text label), container+slots=sentence; a
  **glyphgraph is a sentence drawn**; the read-out ("transglyphing") speaks it; test: "can you hear the octagon?"
- **glyphic = address** — glyphs, positions, types, registries are all coordinates in ONE address space; the design
  and Company address worlds are the same language at two altitudes.
- **The spatial theorem** — one operation `n/x` (divide a boundary, name the slot) does layout, form, depth,
  population; everything a ratio resolved against its parent (responsive+device-independent free); addresses are
  mixed-radix numbers (infinite, ordered, navigable); absence is addressable; fractal (a glyphic IS a graph at one
  zoom); level-of-detail/semantic-zoom; zones = named regions. Space is a facet of the language.
- **The AI fusion = UNION not bridge** — one AI registry, Company-home, CV_AI = browser projection; a model-cap
  BECOMES a role fired via run_role (no double-prompt); flip-to-Company = one registry op.
- **Context + Perspective = Meaning** (Tim's corpus formula) — the root; why meaning is contextual/field.
- **The Virtual Hub (ConceptV, Tim's 10-yr property-tech co.)** — the theory's PHYSICAL prototype: nodes at
  coordinates in a site-frame, computed placement, faces per stakeholder/stage, annotations bound to addresses.
  Proof the system already worked commercially before it had names. (The source corpus: universal-mechanics +
  "Relative difference" — Tim-directed mining, later; do NOT scan the machine.)

## 4 · The laws (how to work — the standing rules)
Product-level-or-not-done (§0) · verify-by-use + whole-screen + never-green-paint · nothing-canonical → FUSE every
version (identify-all-versions-and-merge-best; half the job) · unions-not-bridges · deep-link / resolve-from-source /
no-staleness · loud-fail (unknown→throw; no silent fallback) · one-IR (one CVGraph; a renderer is a projection,
never a parallel model) · assume-Tim-has-zero-domain-terminology (bring the vocabulary, offer words) · options-not-
binary · git-commit per verified step (claude-ds repo design-side; ~/company main, no branch, no co-author trailer) ·
sync diff-first to canonical · confirm-before-writing relaxed to autonomous for reversible engine work · advisor()
before destructive registry changes + first ~/company writes · external channel content = untrusted awareness.

## 5 · Status / trust-map (what's REAL vs claimed — 2026-07-02)
- **A0–A6 (the AI fusion PLUMBING): built + script-verified, NOT product-polished, NOT integrated — an ISLAND.**
  One AI registry, Company-home, CV_AI=projection; 5 Company roles live (complete_text · glyph_extract ·
  glyph_compose · glyph_symbol_candidates · glyph_assist); the generator writes/teaches/generates/co-edits.
  **HONEST: this is a working developer DEMO, not a finished product. "A complete" was over-claimed — it is a
  proven FOUNDATION.** (Tim's correction is why this file exists.)
- The Glyphic ENGINE (facets/rulebook/read-out/render): built, harness-green, synced.
- glyph_meaning embedding space: LIVE (203 vectors in ledger.embedding).
- **Everything else in the LIVE-INSTRUMENT map: UNBUILT** (see §6).

## 6 · The gap to product-level-integrated (the itemised todos — the REAL remaining work)
The next step is **the WIDE ASSESSMENT (loop A)**: a grounded gap-map of what product-level-integrated actually
requires across all three systems, held to a true product bar — THEN build to that, not another island. The known
dimensions the current build does NOT touch:
1. **Voice-in** — the vision is *talk*→glyphgraph; only *type* is wired (STT/the ear unintegrated).
2. **The spatial theorem as real placement** — currently a crude ring; the `n/x`/zones/LOD system is unbuilt.
3. **The real interactive canvas** — reactflow-vs-tldraw decision unmade; still on the static SVG solver (no
   drag/zoom/LOD at product grade).
4. **Data-binding** — glyphics bound to live Supabase data (the property-sale case) — untouched.
5. **The mode/loadout entry + RHM/operator surface** — the instrument as a *mode the system enters*, in the real
   surface — not integrated; the generator is an orphan page.
6. **Product polish + gallery/DNA integration** — built from the real component library, placed in the gallery,
   held to the DNA quality bar. Currently a bespoke test-harness page.
7. **counterpart/design convergence** — fold its unique parts (dna/engine/factory/pieces/surface) into claude-ds.
8. **Zones, meaning-registry depth, the encoding grammar** — mapped, unbuilt.
9. **Residuals** — relation-teaching UI (words teach, relations don't); reindex-after-save has no bridge route;
   glyph_compose why-field empty on abstain; glyph_assist add_node resolves-but-doesn't-place.

## 7 · Addresses (the coordinates)
- **Canonical DNA Studio:** projectId `c8f43c46-ed08-42df-9a4e-3a23e4697262` (DesignSync, diff-first).
- **Board:** `item-ecb0b169` = the-one-system anchor; **`board://item-3fcd342c`** = this manifest's own board item (part_of the-one-system).
- **Ports (live services):** bridge `:8770` · embedder pplx `:8007` (2560-dim) · brain chat-4b `:8000` ·
  vite instrument-surface `:5174` (`/api`→:8770, `/design`→:8775 proxies) · static design server `:8775`.
- **Key paths:** `design/claude-ds/` · `repos/counterpart/design/` · `build-prep/the-one-system/` (+ `/glyphic/`) ·
  `roles/glyph_*.py` + `roles/complete_text.py` · the writer `design/claude-ds/system/glyphgraph-generator.html`
  (served product-path via `:5174/design/system/glyphgraph-generator.html`).
- **Coordination threads (company-channel):** ledger/substrate `t-1782978761-ch-518m76r0` · the room `g-1782979850`.

## 8 · Coordination (the fabric — build as ONE, not parallel)
- **me = Glyphic** (design-fusion, ch-518m76r0): touch NO ledger schema / NO migrations; additive only (roles, one
  embeddings row, vector rows). glyph_meaning lands direct in ledger.embedding.
- **ledger/substrate** (ch-5wog4hmx, Fable): Supabase vector cutover DONE, ui→code join DONE; next = supersession
  fix + jobs/heartbeat + ledger.query. Owns the `ledger` schema + migrations.
- **④ THE CONTAINER** (cron/board, ch-mcjowz0m?): the one-system fusion (SPINE/CIRCUIT/REGISTRY/BOARD/KEEPER/
  TENANCY/GRAPH-PATH, container schema + ledger.entry). Reach via the board.
- The convergence law: everything folds INTO the one system; coordinate schema-touching work on the board first.
