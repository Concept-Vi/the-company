# Glyphic — build-plan, research & coordination (moved here from claude-ds)

> **What this is.** The Glyphic / AI-fusion **build-plan, research, and coordination** docs — moved out of the
> design system (`design/claude-ds`) into this shared fabric home so other (on-disk) sessions can read, comment on,
> and build against them. `claude-ds` is the **workshop** (where agents design + make things for Tim with the design
> system); *plans/process/research/coordination* are not design outputs, so they live here beside the-one-system's
> other build-prep docs (the embedding/ledger/Supabase work). **Single home:** these are the canonical copies now —
> they were removed from claude-ds + the DNA-Studio canonical (no second place to drift).

## What's here
- **`A-fusion/`** — the AI/Company **fusion design** (`A-DESIGN.md` = one AI registry, browser projection, capabilities-
  become-roles), the **6 first-hand reads** (`reads/READ-1..6` — plans · findings · design-AI code · Company cognition ·
  bridge/CORS · the make-or-break comparison), and **`baseline-resolution-map.json`** (the A1 oracle: 55 caps, pre-refactor).
- **`ROADMAP.md`** — the unified build roadmap (A0→A6 sequence, phases).
- **`CRITERIA.md` · `GUIDE.md` · `SYNTHESIS.md`** — the build-plan triad.
- **`live-instrument/`** — the research wave: `ANCHOR.md`, the `LIVE-INSTRUMENT.md` unification map, the `catalogue/`
  constitution, and `findings/AREA-1..26` (the grounded inventory across the design system + the Company).

## What STAYED in claude-ds (design-canon / language / method — not moved)
`analysis/glyphic-language/UNDERSTANDING/00-the-thinking.md` (the thinking layer, the language arc, the relational
whole, the trust-map) — plus the **engine** (`assets/icons/cv-*.js`, `glyphgraph.js`), the **surfaces**
(`system/glyphgraph-generator.html`, `language.html`), the **registries/AI** (`app/ai/*`), and the **language spec**.
Those are the design system + things made with it; they belong in the workshop.

## Convergence context
This sits in `build-prep/the-one-system/` beside the embedding session's docs (`SUPABASE-VECTOR-SCHEMA.md`,
`CODE-ADDRESS-RECONCILIATION.md`, `EMBEDDING-MEMORY-SUBSTRATE.md`, `NORTH-STAR.md`). The Glyphic A3 (`glyph_meaning`)
lands as one space on that shared `ledger.embedding` store — no fork. The board (`channel://the-one-system` +
`provider-registry` + `design-fusion`) carries cards that address/deep-link these docs so the fabric coordinates over them.
