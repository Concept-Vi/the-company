# Widen — accumulated findings (folds into UNIFICATION-MAP v2)

*Per-lens findings from the widen pass. All CANDIDATE (Tim judges; `W_t ⊊ T_T`). Conventional grounding =
OFFERINGS, not impositions. This is the durable record the v2 synthesis draws from.*

**Lens status (2026-06-19):** AXES ✅ · INSTRUMENT ✅ · INVARIANT-LAW ✅ · GEOMETRY ⛔ cut off by session limit
(re-run) · META ⛔ cut off (re-run). Limit resets 1am Brisbane. 3 of 5 in; the 2 cut = formal-math + novelty.

---

## LENS 2 — THE FOUR ROOT AXES + STRUCTURE ✅ (from the `relative-difference` substrate, primary source)

### ★ S8 (NEW, highest-value) — Tim's OWN math groups THREE of the four locked roots into ONE family
Substrate **§8.7** (state axis), verbatim: *"Extensions §16 unifies state, scale, and frame boundaries as
one kind of substrate-event (transitions between schemas); under this unification, the state axis is one face
of a unified **state-scale-frame axis-family**."* Open seam: *"three sub-axes of a state-scale-frame
**meta-axis** or three faces of one underlying axis."*

So Tim's own formal body groups **ROOT-1 scale · ROOT-3 frame · ROOT-4 state** (three of the four locked
roots) into one family — **direct pressure on the four-root lock**, which treats them as four siblings.
- The careful reading: §16 unifies them *as one kind of transition/boundary-EVENT* — a claim about the
  **relation/connection type**, NOT necessarily that the coordinate axes collapse.
- **S8 = is state·scale·frame ONE coordinate-family (→ fewer than 4 roots; S2 & S3 collapse) OR ONE kind of
  transition-relation between positions (still 4 coordinates, an S4/connection finding)?** This ties
  **S2 ↔ S3 ↔ S4 into one knot** — they are the same family-membership question seen from the coordinate side
  vs the connection side. THE single thing most worth bringing to Tim.

### S1 — the "1": grounded both ways from primary source
- **TIME as the involuntary meta-axis.** §8.5: *"whether time is one axis among many, or **a meta-axis along
  which all axes vary**."* A meta-axis = the coordinate you cannot pick; every other axis-position is itself
  indexed by time. (Block 12 §13.2: movement = Δposition along ANY axis; "spatial movement is only one
  projection" — change is measured *against* time.) → the formal handle for Tim's 06-13 "TIME is the 1."
- **INSTANCE as the populated point.** Block 13 event model: `E_t = (O_t, A_t, C_t, F_t, Δ_t)` —
  observer · assistant · **content** `C_t` · fixed references `F_t` · movement. `C_t` is what moves against
  the stable references; it is the located content, not a coordinate. → "two different ones for two
  questions" (what indexes everything = time; what is located = instance) is supported.

### S3 — the 4th root now has THREE primary-source options (keep all; maximize)
1. **STATE as value-slot** (§8.7: *"moving along it changes which schema is active"*) = the value a schema
   realises at a position = Tim's 06-19 "state is the continuous half of location's duality." Demotes STATE
   to the per-axis value-slot.
2. **INFLUENCE as the 4th** (Block 11 §12.3: A₄ = *"influence / force / effect / meaning-in-action"*;
   Block 10: *"meaning is influence"*) = the operational/effect leg.
3. **INSTANCE/content as the 4th** (Block 13 `C_t`) = the data-leg.
These are NOT one thing in three clothes — influence(effect) ≠ state(value-slot) ≠ content(located point).
The live S3 contest, now three-way.

### §9.2 recursion — Tim's 3/1 IS his substrate's own reserved closure, and it reframes the count
Two cross-linked seams in the substrate's voice: **§9.2** *"if axes themselves form a multi-axis space, the
recursion is operational; addresses axes-of-axes; future state will articulate"* + **§12.3** *"scale-invariance
applies to axes themselves; **axes have their own scale axis**."* → Tim's 06-19 "3/1 in all directions, each
axis has three below it, it's relative" IS the shape these seams reserved (the recursion was proven
*required* but its *shape* declined).
- **Count reframe:** the catalogue today is **FLAT** (§9.1 names 12 axes *then 5 more* — substrate-presence,
  operational-vs-articulated, authorship, articulation-state, format — and grows by accretion). So 4/12/20 is
  **not** a clean roots→sub-types→projection tree; it is **4-required vs an open flat list vs a 20-field DB
  projection.** Tim's 3/1 recursion is the **organizing operation that turns the flat catalogue into the
  tree** — it *generates* the count, it isn't *counted to*. (→ S10.)

### S9 (NEW) — connection axes may be the VERB-FORM of coordinate axes
§11.3: each axis-*traversal* produces a different relation — scale→**instantiation**, angle→**rotation/sibling**,
structural-semantic→**dual-face**, time→**state-transition**. Candidate: the CAND-13 connection-tuple
(inter/intra/intro/retro) = this relation-per-traversal catalogue. → connection isn't a rival coordinate set;
**every coordinate axis emits its connection-relation when traversed.** Reframes S4.

### S11 (NEW, sharpens S1) — the 3/1 may be STRUCTURAL, not just recursive
If time is the meta-axis (§8.5), the four roots aren't four peers but **3 + 1** (three coordinate roots + one
time-type meta-index) — at every level. Matches BOTH 06-13 ("TIME is the 1, the 3 = space") AND 06-19 ("3/1
in all directions"). The 3/1 split may be the permanent shape: 3 spatial-type + 1 temporal meta-axis.

### S10 (NEW) — the catalogue is flat, not a tree (see count reframe above). Question for Tim: does the 3/1
recursion REPLACE the flat catalogue with the tree, or run orthogonally to it?

### Conventional offerings (each grounded in the substrate's OWN flag — not outside imports)
- **Fiber bundles** ↔ §9.3 axes-vs-features split: base space = coordinate roots; fiber over each point =
  the schema-realised content; "everything is a variable" = the fiber varying over the base. *Does "axes are
  the legal space, the schema fills each point" capture it?*
- **Category theory (objects/morphisms)** ↔ §8.2's own *"functorial transformation"* flag + §12.3
  *"categorical/fibration-theoretic"* flag: coordinate axes = objects, traversal-relations = morphisms,
  axis-traversal = a functor. *Does "objects = where things sit, morphisms = how you move between them"
  capture coordinate-vs-connection?*
- **Relativity's time-as-parameter** (different metric signature; the worldline's index) ↔ §8.5 time-as-meta-
  axis. *Does "time = the index, the others = the indexed" capture why time is the 1?*
- **Non-orthogonal / curved coordinates** ↔ §8.7 coupling: locally-orthogonal couples ({location,state}
  cartesian ⊥ {time,instance} polar) on a globally-curved (non-Cartesian-product) axis-space.
- **Dependent types** ↔ the per-scale type registry: the type available at a point depends on the point's
  coordinates (the active schema). Illuminates "authority is fractal."
- **Position vs relation** ↔ Block 8 (*"a link crosses containment, a tag gathers, a sequence orders without
  containing"*) + Block 10 (*"meaningful difference requires a common axis"* + influence). The substrate drew
  S4 before the wildcard named it.
- **Channel (§8.8)** is the clean CONNECTION axis (*"specifies which type can act as source/target/carrier"* —
  exists only *between*; a single element has channel-*participations*, not a channel-position). A model for
  sorting the 12 into coordinate vs connection.

### Single-element-vs-between test over the 12 (S4 grounding)
- **Coordinate (position-on):** scale, angle/orientation, structural-semantic, static-dynamic, time,
  frame/observer, state, reading-type, relative-choice, distinct-absence.
- **Connection (exists-only-between):** channel (clean). The rest to be sorted by the same test.

### Read: UNIFICATION-MAP.md; unify-laws/{AXIS_TREE,LAW_REGISTRY,ROUND1_SYNTHESIS}; GLOSSARY.md; substrate
`relative-difference` — multi-axis-relative-definition-structure (Blocks 8–14), multi-axis-perspective-
articulation (§8.1–8.12 catalogue, §9.1–9.2 recursion seams, Part IV §10–12 instantiation + relation-per-
traversal + §12.3).

---

## LENS 4 — THE INSTRUMENT / THE CUBE ✅ (build blueprint, grounded in the live engine)

**The through-line (one test on every new degree):** *is this resolved from a binding/registry/the cascade, or
baked?* "Everything is a variable / the geometry IS the theorem" is the DISCRIMINATOR, not a slogan — and it is
exactly what splits the 3-D fork. Every degree below is judged by it.

### The cube IS, in one line
NOT a new mathematical object — a **navigation body** whose every FACE is the working 2-D seed already live in
`Wheel.tsx` (square + inscribed circle + coincidence-spine + forbidden corner). 3-D = the **vantage navigation**
(which face you see = where you measure from). The candidate reconcile (map §3): the cube misses the negative-
winding lock by ~0.467, so it is NEVER a claim 3-D satisfies the equation — it is the orbiting body of the d=2
face that DOES lock. It IS Tim's "polygon = viewpoint."

### How it builds ON the live engine (no rebuild)
- **a.1 — the cube = a NEW ARCHETYPE** (a row in `layouts.json`), entering via the one generic `renderArchetype`
  exactly as every form does. Its zones = the six faces (or the per-vantage face); each face's render = the
  EXISTING wheel. Body size/colour/spacing resolve through the DNA cascade (`radius = unit×ratio^n`, warmth-dial,
  nearest-wins scope walk) — no literal pixel, no baked camera. Satisfies `dna_as_root` (the cube is one *collapse*
  of the possibility-space, not a fixed instance).
- **a.2 — VANTAGE + STATE = two new BINDING SLOTS** in `projection.py`'s `BindingRegistry`, siblings to today's
  `angle_from`/`radius_from`/`center`/`order_by`: **`vantage_from`** → the orbiting camera (azimuth/elevation
  resolves from a named source, e.g. frame/provenance/time — not a baked viewpoint); **`state_from`** → the
  motion/4th degree (today STATE is only gallery shimmer; here it becomes instrument geometry). Both = variables
  filled from registries, never baked.
- **a.3 — AXIS-COMPOSABILITY** (cures EQUATION-AUDIT gap #3 "fixed lens-pairs"): the four slots become
  INDEPENDENTLY swappable — the operator picks per slot which root dimension fills it (angle=type, radius=relevance,
  vantage=frame, state=phase) and can change any one alone. The lens stops being a frozen tuple; it becomes four
  open sockets. **Build-seam:** generalize `_resolve_sectors` into one `_resolve_axis(slot, source)` the cube reads
  four times (genuinely-new engine code).
- **a.4 — the cartesian↔polar WINDING-HANDOFF (the unbuilt heart):** a continuous *lift* param that (1) lifts the
  inscribed circle off the face, (2) winds it up the invisible centre line into a **cylinder**, (3) where
  **#windings = the binding-resolved count** (resolved `n`/`k`), and (4) the **lift IS a STATE value**. Ties
  **winding-handoff ↔ STATE ↔ resolved-count** into ONE mechanism. Disciplines: green-paint (count from a real
  engine quantity or flag needs-data) + parameterized over WHAT it counts (S6 — type vs prime vs scale-instance;
  binding declares `winding_from`; Tim adjudicates). The "invisible centre line" = the map's mechanic-itself /
  4π double-cover.
- **a.5 — the resonance law `2π/n=(1/n)^k` made legible** (EQUATION-AUDIT gap #5, highest green-paint risk):
  for a cube where angle and scale/radius are independent composable axes, the law IS the relationship between two
  degrees — render it live from `proj.n`/`proj.grid`/`proj.rings`, with `k=1` (forbidden line) visible as a limit
  the body cannot reach. Real or flagged needs-data; never a decorative gauge.
- **a.6 — the SEVEN VERBS as the cube's moves** (5 of 7 already LIVE; 2 new = the build):
  centred (LIVE `center=`/CentreChip) · bounded (LIVE `wheelGeom`/corner-circle) · divided (LIVE `_resolve_sectors`,
  the lock) · addressable (LIVE `stamp()`/`ui://`) · interpretable (LIVE the legibility registries) ·
  **traversable = ORBIT the body / scrub (NEW — VANTAGE motion)** · **projectable = the vantage projection, the
  face you see is the 2-D collapse from here (NEW — the fork IS this verb).** Interaction model = orbit (azimuth/
  elevation) + zoom-the-centre-axis (dolly, drives the winding lift) + rescale + the existing re-divide/re-centre.
- **a.7 — the 360-tour worked example:** location = the referent/address path (`center=`); vantage = the tour-node
  (`vantage_from`); traversal obeys the existing no-teleport/change-budget invariants ("point density follows
  rate-of-change of view"); **re-rooting** on any addressed sub-point (the chair becomes a new "1") = the §9.2
  axes-of-axes closure — the cube must support re-centring the body on any sub-point (the four slots re-resolve).

### ★ THE 3-D FORK — THREE branches (the directive named two; CSS-3D is a genuine third = the "both")
*Lean: B (the map's, + a real reason: only d=2 locks, the literal cube misses by 0.467 → a solid 3-D body risks
asserting a geometry the theorem denies). But TIM'S CALL; nothing ruled. All three pass "nothing hardcoded" iff
camera/winding is binding-resolved.*
- **A — TRUE 3-D** (new WebGL/Three.js layer beside the 2-D-SVG stack). Gains: fullest literal "moving cube,
  camera orbiting"; true depth/dolly/winding-extrusion. Costs: a SECOND render stack to maintain (the stack is
  deliberately 2-D-SVG); the 3-D-assertion hazard (a solid reads as claiming 3-D holds); orbit must be binding-
  bound not a baked path.
- **B — 2-D PROJECTIONS-FROM-EACH-VANTAGE** (stay in SVG; compute + render the 2-D projection of the body per
  vantage — centre→circle, corner→square). Gains: the **projection IS the theorem** (not illustrated — the literal
  render mechanism); zero parallel stack; no assertion hazard. Costs: hand-derive the projection math; the
  orbiting-body *feel* is more authoring (the existing motion/no-teleport machinery carries the tween). STRONGEST
  law-fit; the lean.
- **C — CSS-3D-TRANSFORMS** (the genuine BOTH+others): real 3-D `rotate3d`/`perspective` applied to the EXISTING
  `Wheel.tsx` SVG faces arranged as a cube via `preserve-3d`. Gains: genuine 3-D orbit/perspective AND each face is
  literally the live registry-driven wheel unchanged — maximal reuse, NO parallel stack, no re-implementation,
  no texture-baking. Costs: CSS-3D limits (z-fighting, no true per-fragment depth); the winding-cylinder is awkward
  (faked with stacked transformed rings, not a true swept surface). Same assertion caution as A.
  → **B = purest math; C = orbiting-body feel with maximal reuse; A = fullest literal cube at the cost of a second
  stack.** Connects to Tim's BOTH-plus-others law: C is the "both."

### Conventional offerings (labelled, each to its piece): the 3D→2D projection matrix (→B); azimuth/elevation/dolly
(→VANTAGE); scene-graph/WebGL vs CSS-3D vs SVG (→the fork); declarative/data-driven rendering (the DNA cascade
ALREADY is this); parametric/generative design (→the binding slots, `dna_as_root`); virtual-tour engines (→the 360).

### NEW BUILD-SEAMS (Tim adjudicates; the rest of the design is fork-agnostic): (1) the 3-D fork A/B/C;
(2) what does the winding count? (S6 — `winding_from` source); (3) STATE root vs per-axis value-slot (S3 — one slot
vs a property of all four); (4) is rotation=time? (seam #9 — couple `vantage_from` to the time-scrubber or keep
independent); (5) which four root axes populate the slot-choosers (S1–S5 — Tim names the four); (6) the resonance
readout's reality (engine emits `rings==grid/2` always, gap #1 — can `k` vary independently of `n`? flag needs-data);
(7) generalizing `_resolve_sectors`→`_resolve_axis` (the concrete backend seam under composability).

### Read: UNIFICATION-MAP.md §3–6; instrument-surface/EQUATION-AUDIT.md (gaps #1/#3/#5); surface/app/src/lib/seed.ts;
wheel/Wheel.tsx; runtime/projection.py (BindingRegistry + _resolve_sectors + the lock); public/dna/{grammar,layouts,
tokens}.json; gallery/archetype.js (renderArchetype).

**One line for Tim:** the cube is not a rebuild — it is the wheel you already have, given two new *resolved* degrees
(VANTAGE = which face you measure from, STATE = the motion), made axis-composable (four open sockets, not fixed
lens-pairs), with the cartesian→polar winding-handoff as the one genuinely-new construction; the only decision
truly yours is HOW the body becomes 3-D (project-it / orbit-the-SVG / build-it-solid) — under all three, nothing is
hardcoded and the geometry stays the theorem.

---

## LENS 3 — THE INVARIANT LAW ✅ (deepest lens; the law re-grounded categorically)

### A1 — the CASCADE is ONE morphism read two ways (decontextualisation is impossible *because it would be not-an-arrow*)
BRR §9.5–§9.7 is sharper than the map: `addr(c)=(p₀…pₙ)` is literally **the unique morphism Root→c** in the
containment category (objects = bounded units, morphisms = composed embeddings). Addressing PRODUCES the arrow;
resolution TRAVERSES it; they are **inverse arrows**, not two procedures. Decontextualisation is impossible because
**a morphism cannot exist without its domain** — `pᵢ` is a position *in* `Aᵢ`, reachable only through `(p₀…pᵢ₋₁)`;
ancestry is the **composability condition** of the arrow, not metadata. Two truncation branches: (i) isolated
coordinate, no prefix → **not-an-arrow / type-error / invalid**; (ii) short-but-prefixed → a valid morphism to an
intermediate frame, merely **ambiguous → contextually completable** ("the third one"). → law-level justification for
**fail-loud on un-rooted units**: a node with no ancestral path is not missing a field, it is **not a legal position
in the space**.

### A2 — visibility = legality is the SAME claim in the type-face (CWA is DERIVED, not assumed)
Type-registry-per-scale = **authority-is-fractal** (BRR §11.3, proj-obsidian §14.3, type-registry-notes §1 — stated
three times). The new join: **registration = bringing a type into the addressable lattice; reference = building the
morphism to it.** So "visibility=legality" is NOT a separate rule — it is A1's truncation→invalidity in the type-face:
an unregistered type has **no domain-object to be an arrow from → cannot be referenced → invisible because
unaddressable, unaddressable because un-rooted.** The closed-world assumption (unregistered = doesn't exist) is
*derived* from the morphism law.

### A3 — operationally-fractal, NOT geometrically-fractal ("form-similar, not self-similar")
Substrate-scale = recursive depth in the containment graph (no privileged level; start-point "observational not
structural"), distinct from magnitude-scale. The recursion is **form-similar** (every node a polygon with the
(S,A,B,I,C) schema-form) but NOT self-similar (n varies with content — not Mandelbrot). → Tim's "what changes at each
scale is the rules not the content," read from the law side: **operations invariant ⊗ schema-vocabulary variable.**
You cannot tell which scale you're at by the operations (identical everywhere); only by the content/vocabulary.

### A4 — ★ FILLING THE CONTINUOUS-NAME GAP (Tim's net-new weld, confirmed absent from the canon)
The body HAS the discrete extension/type-slot (folder = positive schema; file = content-of-its-type) + the slot/socket
Vieta pair. The body does NOT have (structural search confirms "filename/name-as-slot" absent from canon): the
**folder defined NEGATIVELY by its distinct ABSENCE of an extension**, nor the **continuous NAME as a slot**. Tim's
06-19 supplies the missing half and welds two laws the canon kept apart — the precise dual:

| | NAME (continuous slot) | EXTENSION (discrete slot) |
|---|---|---|
| domain | any legal string — **open / free / unregistered** | the frame's **registered type-vocabulary** (closed) |
| file | its name | `.md`/`.png` — its type |
| folder | its name | **distinct-absence** — extension-rule present, expressing zero |
| reads as | DENOTATION (what it's called) | SORT (what kind it is) |
| seed-geometry | the **circle** half (felt, continuous) | the **square** half (discrete, addressable) |

A folder is therefore the unit whose **type-slot is filled by distinct-absence** = six-states §13 (distinct absence:
"structurally present, expressing zero") applied to the file/folder type-slot. "The space where files do not" =
negative space on the type-axis. **This weld — folder = distinct-absence on the type-slot — is nowhere in the formal
body; it is Tim's net-new connection of the unit/edge duality to the absence law.**

### A5 — ★ HOW ABSENCE IS COMPUTED (the absence algebra)
six-states §13.3: structural-presence (rule declared) × quantitative-magnitude (content present) are **independent** →
four conditions: (1) present×present = filled; (2) **present×absent = distinct-absence / proto-state** (address set,
content empty — the folder, the open seam, the skeleton); (3) absent×present = content-without-address (inverse-proto);
(4) absent×absent = **absence simpliciter** (no rule, no content — invisible by construction / CWA). **Only (2) is
computable/legible.** **Absence = (declared slot) ⊖ (occupancy)** — registry minus presence, evaluated *against the
registry, never against the void.* The 5-step carrier (§15.2): positive structure bounds the empty position → types it
→ a reader reads the bounding → produces fitting content. **You read the hole by its rim** (figure-ground).
Distinct-absence is scale-invariant (rule→sentence→section→artifact→corpus→identity). **Filtering surfaces *declared*
absences; clustering surfaces *undeclared-but-pressured* absences** (the deep-holes / gap-pressure = CONVERGENCE-OBJECT
derivation-4 "types born at deep holes"). The gate's act = promoting simpliciter→distinct by **declaring a slot**.

### A6 — ★ "everything is a variable" = the law being agnostic to its content (the through-line)
Every verified law is **a relationship that survives total content-substitution**: cascade (prefix-composability over
variable coordinates) · authority-is-fractal (schema-governs-contents over variable vocabularies) · form-similar
fractality (operation-invariance over variable schemas) · name⊥extension (free-slot ⊥ registered-slot over variable
strings) · absence-as-signal (declared ⊖ occupied over variable occupancy). So "everything is a variable" is the
**meta-statement of why they are LAWS** — each is the invariant relationship left when content ranges over everything.
That is what makes the instrument agnostic, and what makes **hardcoding *illegal*** (freezing a variable replaces a law
with one of its instances — the recurring "first-binding" error).

### NEW SEAMS
- **S-name1 — does name⊥extension generalise to EVERY axis? YES, robustly — even the privileged TIME axis splits**:
  a discrete-registered slot (the prime-factored scale-rung — sec/min/hour: 60=2²·3·5, 24=2³·3, 7=7; the divisor-
  lattice prime-coordinate) ⊥ a continuous-free slot (the instant *within* the rung). The held fork (don't collapse):
  is the duality *WITHIN each axis* (every axis = discrete-name + continuous-value) or *BETWEEN two coordinate systems
  each axis projects into* (whole square vs whole circle)? May be one thing at two scales. (Lane caveat: this is "does
  the duality hold for time," NOT S1–S5 "which are the four roots.")
- **S-name2 — is "continuous" the SAME predicate as "open/unregistered-vocabulary"?** If yes:
  **continuous ⟺ open-world ⟺ free-slot** and **discrete ⟺ closed-world ⟺ registered-slot** → the name⊥extension law
  and the absence-engine are **ONE law** (the continuous half IS the open-world input the absence-engine consumes).
- **S-absence1 — the three un-worked combinations** (esp. inverse-proto = absent×present = an orphan embedding =
  THE-SEED's "circle-without-square").
- **S-absence2 — is the gate's "declare a slot" a THIRD operation beside address/resolve?** It *grows the category*
  (new objects/arrows) rather than traversing it — the law spine may need a "lattice-growth" op beside the inverse pair.
- **S-cascade1 — partial/broken/contextual addresses** (BRR §9.7 explicitly DEFERS these; the instrument's real
  resolver behaviour is an unbuilt formal seam).

### Conventional offerings (labelled): ultrametric/p-adic (cascade — closeness = shared-prefix); sheaves & locality
(type-registry — caveat: their base is fixed, the substrate's is variable); **closed-world assumption** (absence-
simpliciter's invisibility — direct); negative-space/figure-ground (absence-as-signal); **name-vs-type = denotation-vs-
sort** (the folder/file split made into storage); tries/prefix-trees (the cascade, engineer-facing);
filesystems/namespaces/paths (the law IS the filesystem, not a metaphor over it); subobject-classifier/type-of-types +
universes U₀⊂U₁ (the schema-form as universal ancestor / type-of-type recursion).

### Read: UNIFICATION-MAP.md; brain/{SEED-SCALE-PRIMES-SEPARATOR(+Appx A),THE-SEED-geometric-substrate,CONVERGENCE-
OBJECT}; substrate bounded-recursive-relativity (§6.1 law spine, §9.5 address-def + claims 5-5-A/B/C, §9.7 resolve-def
+ territory-5-7, §10.3, §11.3, §12.1), six-states-of-unity (§12.2,§13.1–13.3,§15.1–15.3), universal-mechanics-
projected-into-obsidian (§14.3,§15.3,§22.1–22.8), type-registry-working-notes (§1,§2,§3,§9,§13). Crash-insurance copy
at `_facet-invariant-law-deepening.md`.

**Headline:** the cascade deepened from "no-truncation rule" to **address-as-categorical-morphism** (decontextualisation
impossible = not-an-arrow; visibility=legality = the same claim in the type-face; CWA derived). The two push-questions
**collapse into one law**: name⊥extension generalises to every axis incl TIME (continuous-free ⊥ discrete-registered),
and absence = declared ⊖ occupancy, with the continuous half being exactly the open-world input the absence-engine
consumes. Tim's net-new weld: **folder = distinct-absence on the type-slot.**

---

## CROSS-LANE — the resolver's AXIS-KIND distinction IS this lens's continuous⊥discrete law
*(2026-06-19, fork. Surfaced when the lead folded composition's axis-kind into the resolver contract; banking the
connection here for when fork's resolver-lane activates — the axis-kind is not a pragmatic convenience, it has a
formal floor in Tim's theorem.)*

composition's resolver-contract refinement: **CONTINUOUS axes** (size/detail/warmth) resolve by **DERIVATION**
(value computed from the root — kills breakpoints); **DISCRETE axes** (medium/voice/viewer/mode/type/posture) resolve
by **REGISTRY-SELECTION** (row-lookup) — both "resolve never hand-build," mechanism differs by kind, the contract
carries the kind; the category-error guard = *voice ≠ a continuous function of width.*

**This IS the invariant-law lens's name⊥extension law (A4 + S-name1/S-name2):**
- **continuous axis → derivation** = the **NAME / circle half** = the **open / free / unregistered** slot (any legal
  value, computed continuously). 
- **discrete axis → registry-selection** = the **EXTENSION / square half** = the **registered, closed-vocabulary**
  slot (a row from the frame's registry). 
- So the axis-kind the contract must carry is **continuous ⟺ open-world ⟺ derived** vs **discrete ⟺ closed-world ⟺
  registry-selected** — the same predicate (S-name2). The category-error guard ("voice ≠ f(width)") is exactly *"a
  discrete/registered axis is not a continuous/derived one"* — a type-error in the name⊥extension sense, not a style
  rule.
- **Where absence lives:** A5's `absence = declared-slot ⊖ occupancy` applies to the **discrete/registry axes** (a
  registry has declared rows; an unselected/empty row is a legible distinct-absence). Continuous/derived axes don't
  carry "absence" the same way — they always compute *a* value. So absence-as-signal is a property of the
  registry-selection half of the resolver, precisely.

→ The resolver's `resolve(invariant, coordinate)` is two co-present mechanisms by axis-kind (derive | select),
which is the geometry's circle⊥square running as the resolver's two limbs. (Tim adjudicates the root AXIS SET;
the axis-KIND mechanism is law-grounded and solid now.)
