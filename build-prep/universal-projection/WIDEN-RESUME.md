# Widen/Debate — resume note (paused on session limit)

**Status (2026-06-19):** PAUSED. The widen orchestration was cut off by a session/usage limit
(resets 1am Australia/Brisbane). No findings were produced — the five widen agents' returned
"results" are just the limit notice. Nothing lost: the map is durable.

**Durable so far:** `build-prep/universal-projection/UNIFICATION-MAP.md` (commit `0c62ac6`) — v1 of the
unified theorem, every seam marked. Tim approved the direction and asked to WIDEN + STRETCH + DEBATE it
(multiple perspectives, content, research, debate) as real prep BEFORE any build.

**The plan (Tim-authorised):** WIDEN → DEBATE the seams → STRETCH into map v2 → loop-prep the build → build.
Quality over speed; Tim is the only judge; conventional grounding is OFFERINGS not impositions (his
reject-frameworks rule); nothing built until prep is real.

**NEXT ACTION after reset — re-run the five WIDEN lenses** (they did real reading but never synthesized;
re-run gets full findings). Each: deepen one facet + bring new content/connections + read the unread formal
math + bring conventional grounding as offerings + surface new seams. The five lenses:
1. GEOMETRY + EQUATION + THE FORMAL MATH — read the `relative-difference` vault's unread formal body
   (function-of-relative-definition(+mathematical-extensions), cross-scale-winding-mechanics,
   mechanics-of-parametric-n-dimensional-geometry, the 189-chunk reciprocal-power-phase-scale package);
   deepen the seed `2π/n=(1/n)^k`, k=1 forbidden line, (n,k)↔(1/n,2−k), square-uniqueness/cube=counter-
   example, the 4π double-cover/windings/cylinder, slot/socket=Vieta roots, primes/nucleation. Push: the
   cube-as-2D-projection-from-vantage; the cartesian↔polar winding-handoff transform.
2. THE FOUR ROOT AXES + STRUCTURE — unify-laws/{AXIS_TREE,LAW_REGISTRY,ROUND1_SYNTHESIS} + substrate's
   multi-axis-relative-definition-structure (Block 11) + multi-axis-perspective-articulation (§9.2 axes-of-
   axes). Seams S1 (the "1": time vs instance), S2 (scale: root vs recursion), S3 (4th root), S4 (coordinate
   vs connection). Tim's 3/1-from-any-point = closing §9.2.
3. THE INVARIANT LAW — brain/{SEED-SCALE-PRIMES-SEPARATOR,THE-SEED-geometric-substrate,CONVERGENCE-OBJECT}
   + substrate's bounded-recursive-relativity §6.1 + six-states-of-unity + type-registry-working-notes.
   Cascade/type-registry/unit-edge(continuous-name⊥discrete-extension, the new fill)/absence-as-signal/
   everything-is-a-variable. Push: does name⊥extension generalize to every axis? how is absence computed?
4. THE INSTRUMENT / THE CUBE — read surface/app/src/{lib/seed.ts,wheel/Wheel.tsx}, runtime/projection.py,
   public/dna/*, gallery/archetype.js, instrument-surface/EQUATION-AUDIT.md. Stretch: cube as new archetype
   on the live engine; vantage→camera, state→motion, the winding-handoff/cylinder, resonance-law-rendered,
   axis-composability, seven-verbs-as-moves, the 360-tour example. ★ THE 3-D FORK laid out both ways
   (true-3D WebGL/CSS-3D vs 2D-projection-from-vantage).
5. THE WHOLE / META — brain/CONVERGENCE-OBJECT + the-one-application/THE-ONE-SYSTEM + substrate's
   function-of-relative-definition (seven verbs). Coherence/through-line; the honest novelty map
   (novel vs re-clothing of known math — Tim invited this); falsification risks. The corpus-is-code-only gap.

**THEN:** I synthesize the five into map v2 (widened), shape + run the DEBATE (adversarial panels on the
seams — strongest case both ways, NO verdicts; Tim judges), stretch into the final pre-build map, then
loop-prep (Completion Criteria · Implementation Guide · Research Synthesis — both faces, FORM+FUNCTION).
