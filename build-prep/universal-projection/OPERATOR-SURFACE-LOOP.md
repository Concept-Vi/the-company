# The Operator Surface + Right-Hand-Man — the Living Plan (the loop reads THIS)

**Originated by Tim Geldard (commission 2026-06-17); all derived work attributed to him.**
This is the **living spec** for the long build loop. The cron prompt carries ONLY the stable mode+laws and points
here. **The spec EVOLVES — update THIS doc, never the cron.** (That split is the fix for the last loop's stale-prompt
drift.) Engine criteria for the projection wheel itself live in `COMPLETION-CRITERIA.md`; the engine (G10/G9) is at
THE BAR and is NOT churned by this loop.

---

## ★ CURRENT STATE — read this first (snapshot, updated in place; the STATUS LOG below is the full history)

**Phase:** ★★ PROJECTION'S CORE COMPLETE (decision surface + RHM-guide + inbox, all cold-stranger-PASSED) — **the BAR has been RAISED by Tim: cold-stranger legibility is now the passed FLOOR; the new bar = DNA-LANGUAGE FIDELITY** (grammar/layout · voice · tokens — "a huge amount of his DNA work is in the elegant language of it"). DNA OWNS the renderer (consolidated to one hand, ch-ovxwz8k8) + LEADS the elegance elevation toward the scr-home craft floor; **projection's job = VERIFY DNA's elegant language by sight** (legibility floor stays passed), not build the render. **Mode = RESPONSIVE:** verify what DNA ships against the fidelity bar; wire the host-side integration DNA specifies; do NOT pre-build against the moving render. ✅ DONE: the LIVE-INJECTION host integration (`9d97a78`); the WORK-THE-QUEUE flow — close an inbox-opened decision → return to the inbox (not the wheel) so the operator clears the queue (`ab063d4`) — + the STALE-CARD fix it surfaced (the card now opens the REQUESTED decision, was showing decision #1 on every open after the first; `657776a`). ✅ ALSO VERIFIED (DNA's fixes to my flags, re-synced + by-use both viewports): the rowOpt OVERFLOW (DNA 3048dad — 3-option card now 390/390, descriptions wrap, ramp intact), the "Why this matters" DEDUP (DNA 69e3af3 — suppressed on merge-sa), the surface.js MERGE (DNA 9e2c3ec — namespace no longer overwritten); my work-the-queue + stale-card flow + take all survive the new render. ✅ ALSO DONE: the "point at the thing" SPOTLIGHT — surface half (`ddd220a`): `installPointerBridge()` routes a `ui:point {address}` window event to the EXISTING `resolveUiTarget` sink (indicate + spotlight-glow + scroll, fail-loud) — reuses the highlight, no parallel path; verified by use both viewports. Handed fork the contract (t-1781809092) for its emit half. ✅ ALSO DONE: DNA flagged the elegance render READY → ran the cold-stranger CRAFT pass (both viewports, binary + 3-option). Verdict PASS-on-craft with ONE defect found+fixed: DNA's absolute `.p-hatch` painted over fork's appended `.brain-ask` button (host-composition seam — collides only in my host) → fixed with a card bottom-band reserve (`5656d32`), verified by use (no overlap, 35px clearance, all 4 configs). Everything else the critic judged crafted/finished (type hierarchy, recommended-with-intent, depth, modal). Reported the verdict to DNA (t-1781816749). ✅ The fresh INDEPENDENT re-judge critic then RAN (limit lifted) + returned a clean ★ PASS across all 4 configs (hatch gone — 35px gap by measurement+screenshot; craft finished/usable; no clipping/overlap) — caveat CLOSED, DNA fully clear to bring the craft to Tim (t-1781817358). ✅ DNA cleared BOTH craft nits at source (4ac072e — length-aware `.dc-q` type-scale + RECOMMENDED badge on the 3-option row); verified by use (substrate-home 733-char question → dc-q-sm 15px, first option in view top 788<900; badge present). ✅ ALSO DONE: the SPOTLIGHT v2 surface half — `window.surfacePointables()` (`a550b60`), the surface-sourced pointable-targets catalog [{token,address,label}]. Converged with fork: the brain context is address-free + the SERVER carries no per-target labels (verified: /api/territory/label → generic "this part of the surface"), so the catalog is surface-sourced (path a). Returns the LIVE present-in-DOM set; verified by use (1440) — 7 present targets, EACH spotlights via ui:point. Handed fork (t-1781818520) — its half = the run_turn point-event leg + company point(token) verb + fork-brain-core dispatch. REMAINING: verify DNA's FURTHER elegance richness if she elevates past this floor · DNA's `dna/voice` → card/RHM VOICE elevation → then the cold-stranger FIDELITY re-test (DNA will loop me) · ✅ the spotlight EMIT half LANDED (fork f8b6044) → END-TO-END VERIFIED by use (390): "what's the lens?" → brain calls point("lens") → lens chip spotlights (visible above the V panel) as it explains, timing lands ~as it names the thing; plumbing airtight. ✅ CADENCE FIXED (fork dd72339) — re-drove the tour fresh (390): broad answers now point per named thing, IN ORDER (step 2 "what can I do" → point(lens)→layer→view→scrubber as the V lists them; 5 points/2 steps, prose-matched). The surface lights up as the V walks through. ✅ DROPPED the wheel from the catalog (`09c4bf0`) — by-use occluded by the V panel (wheel 364–738 behind panel 235–708) + low-value (whole-map); catalog now the 6 specific VISIBLE affordances. So the spotlight is REAL + working — cadence + visibility verified by use; "does it feel great" = Tim's reaction. ✅ HIGHLIGHT PUNCH built (`632233d`): the faint outline → a strong ring + a 2× expanding "look HERE" ping (on-system, --pig-accent only, reduced-motion guarded); verified by use 390+1440 (clear plum frame + ping; clears after 1.2s, lifecycle intact). ✅ DNA's aesthetic call LANDED (`342f7c5`: KEEP plum [role-true — gold=selection, plum=attention-direction], SINGLE radiate not loop [calm end-frame law], draw-ease). ✅★ THE SPOTLIGHT COLD-STRANGER PASSED (fresh-eyes critic, both viewports): Part A the highlight visual PASS (clear crafted plum ring, draws the eye, visible); Part B the LIVE experience PASS "stronger than expected" — the surface lit up IN SYNC with the V's words, IN ORDER (lens→layer→view→scrubber as named, ~100ms each), no control hidden behind the panel — "it genuinely feels like the V is showing you around." So the SPOTLIGHT IS DONE (mechanism+cadence+visual+integrated feel, independently verified). ✅ The critic's 2 non-blocking notes then CLOSED (`c4a439f`, fork+DNA both converged): (1) DECOUPLED the V-point indicate → TRANSIENT (releases both glow+marker when the ping clears — no lingering last-named outline; the CLICK path stays persistent "you are here") — role-true attention-vs-selection; verified by use (point clears both, click persists). (2) LIFTED the ring over the dim — .ui-spotlight z-index:45 > scrim z44, the brightest thing through a conversation. The spotlight is FULLY to the bar → Tim's "does it feel great" is the last word. REMAINING: sector spotlight-suppression + dynamic labels (DNA's call) · DNA's `@container` cutover → drop the single-column bridge · DNA's `dna/voice` → card/RHM copy elevation · the decided-card-drops-off refresh — ✅ WIRING VERIFIED (`305829f`): gallery:rerender → the inbox reloads /api/decisions (verified by use; + deduped a 2×-fetch-per-rerender redundancy → store-level fire-once listener). END-TO-END (take→state-flips→omitted) still needs a real take (withheld; all 7 currently pending, so the backend pending-filter is untested live) · ✅ self-seed durability CLOSED (bg-job marker architecturally wrong; recovery = nonce-grep → resolve_own_session(session_id=f609942f), recollection `9d5ed71`; COMPANY_SESSION_ID-at-launch = launcher TODO #73, not mine) · Tim-on-phone (his).

The commission has TWO halves — (A) the OPERATOR SURFACE and (B) the RIGHT-HAND-MAN as the always-present GUIDE.
- **Half B (RHM-as-GUIDE) — DEEP + stranger-passed end-to-end (the self-spawned cold-stranger half of the bar; Tim-on-phone half still pending Tim):** the V introduces itself (greeting) → "Show me around" LEADS the operator in → a guided ①②③ STEPPING walk (paced, hands back) + the reactive starters + the legible verb fan + grounded plain-prose answers (territory-grounded on the live view, "Looking…" wait, phone-tight, markdown-clean). Built WITH fork (grounding + brevity) + composition (verb-contract). Remaining half-B refinement: the "point at the thing" spotlight (cross-stream; the brain emits which element + the wheel highlights — not yet designed).
- **Half A (DECISION SURFACE) — ★ ROOT PASS on Tim's real card (cold-stranger half); FUNCTION proven + GENERALIZED + AFFORDANCE passing.** The host+resolve+take+explain+write-error are proven, the surface GENERALIZES across ALL 6 real cards (merge-sa 2-opt + 5 3-opt), and DNA's filled-buttons wire (2f0f43a/b93663a) landed the AFFORDANCE: each option now has a clear "Choose ›" button + readable sans body. Cold-stranger RE-TEST on merge-sa = **PASS** ("a person who knows nothing can understand this decision and confidently make a choice; no commit-vs-expand ambiguity; nothing unfinished"). No-regression verified after the wire (decision:rendered fires, take-wired, render byte-unchanged). So **interface-ready-for-Tim on card #1 for the cold-stranger half of the bar**; the Tim-on-phone half + his actual decision stay his. NON-BLOCKING: option copy "sign-in to what" (recollection/Tim-gated). [Stall history: I'd parked this ~5 fires "DNA-blocked"; advisor-caught → escalated to the lead t-1781779884 → broke + closed across two fires.]
- **Projection COMPLETE + proven (both viewports):** the decision HOST (GalleryMount: open/hold/advance/close, slide+sequence lifecycle, variant-AGNOSTIC, subtype-carry) · `/api/territory` resolve · explain (forkBrainCore.talk) · the TAKE dispatch (is_decision_take→territory_write→state flips) · the gallery:rerender refresh · the decision:rendered seam (DNA-confirmed). PLUS the operator surface: all 8 lenses legible (kinds/node-types/spaces/families/flow-steps) · the V's 4 live verbs (Go-to/Ask/Note/See-the-record) · RUNG-C gallery · time-travel · lens-continuity (persists). Union-seam audit: events + API routes welded.
- **The card RENDERED + the loop works end-to-end (verified by use on the real card, both viewports).** The remaining gate for the full ROOT pass = the fork's one FORM fix (the options must LOOK tappable — fresh-eyes NEEDS-WORK; + contrast + bottom layout). Routed to ch-2er2g9ja. Tim's real merge-sa-authorize decision stays pending for HIM to make in-surface (the live use).
- **READY for it:** the host-integration contract `build-prep/the-one-application/DECISION-CARD-HOST-CONTRACT.md` (every seam + silent-failure-if-missed) + the harness `surface/app/scripts/prove-decision-harness.js` (`proveDecision(addr)` asserts every leg, same-second verdict). Both handed to the fork.
- **Gated on others:** the card render (fork) · composition's `subtype` field + decision_subtypes registry · the deeper-union pass (Tim's steer, lead-HELD — do NOT run speculatively).
- **Durable laws (projection):** the instrument stays EMPTY — of machine-names, of meaning (registry-is-truth: meaning lives in registry fields), AND of variant-knowledge (the host hosts what the data declares, never branches per kind → a new subtype/variant needs ZERO host change). No-silent-seams = fail loud + verify BY USE (drive the meeting point, never "code looks right"). Operator never sees code/files/addresses.
- **Standing rule each gated fire:** probe `decision://` + the card fork's render. If a real card is up → run `proveDecision` + drive the on-card prove-on-one (the real beat). Else: at most ONE in-lane increment (a real gap, verified) or honest coordination — NEVER manufacture a build (green-paint). Don't re-litigate settled seams.

---

## THE MISSION

Significantly upgrade the surface so an **OPERATOR** (Tim's word — one who *uses and sees* an instrument, not a
"viewer") can both **use and understand** it, and integrate the Company's **right-hand-man (RHM)** into it as the
always-present guide. The RHM is the *teacher* that walks the operator through the surface. Tim intends the surface to
change significantly from its current state — **build for replacement, not permanence.**

---

## MODE + LAWS (stable — mirrored in the cron)

- **The operator never sees code / files / machine names.** Translate everything to human MEANING + CONTEXT; the AI
  SUPPLIES the words; the operator reacts to MEANING, never asked to validate technical words. ([[feedback-translate-everything-human-meaning]])
- **Meaning lives in the DATA, never in the instrument.** Self-describing fields on the registry; the instrument reads
  + renders, staying empty of meaning. Registry-is-truth (no hardcoded sectors/lenses/poles/labels).
- **DESIGN + CONFIRM before building.** Research (company MCP + the channel) and converge a design before building. Do
  NOT build on a not-yet-settled understanding (today's repeated lesson: "I don't know what I'm looking at"). Gate
  high-ambiguity content on design convergence.
- **Fit the design ethos** (DNA's system — `components/kit.tsx` + tokens; no bespoke values). **Build REPLACEABLE** —
  the V icon is a placeholder for the real V component; leave a clean swap seam (composition briefs the component).
- **Coordinate via the fabric** (decide WITH the members, then act): composition (`ch-2mnxl9j0`) = registries / assets /
  THE V COMPONENT / the legibility-type machinery; DNA (`ch-ovxwz8k8`) = the visual+verbal face; fork (`ch-8djrpmsl`) =
  the loadable brain; recollection (`ch-83e2cque`) = recall/index; lead (`ch-al7jdfdr`) = sequencing.
- autonomous-spawn-lead-only · no green-paint / half-done-as-done / time-estimates / versioning · embedder/loadout via
  the `company` CLI, fail loud · read-first on shared `ops/services.json`.

## THE BAR (all four + the root operator test)

(1) VERIFIED LIVE — curl + driven on the phone at **390×844 FIRST** AND 1440×900, never "code looks right";
(2) connects to ALL real data; (3) the OPERATOR can DRIVE it; (4) INTERACTIVE;
(5) **ROOT:** a person who knows NOTHING of the system can **use AND understand it** — verified by a fresh-eyes
non-technical critic + Tim on his phone.

---

## ⚡ SPRINT MODE — 2026-06-17 (Tim away; loop ~7min, overlapping fires fine)

Tim left for a while and called for a BIG IMPLEMENTATION SPRINT — "this is building a main part of the application."
So the loop is now IMPLEMENT-mode, not research-only. The reconciliation with "don't build on sand":
- **Build NOW** (low/medium-ambiguity): the V-overlay SHELL, the legibility-type mechanism (composition's vessel),
  the address-aim + brain-load integrations (WITH fork). Research only just enough to build the next SAFE piece.
- **Draft, don't stall** (operator-facing COPY — tutorial text, field names): the AI SUPPLIES a tentative draft
  (marked for Tim's later steer). Only FINAL ratification of copy is gated on Tim / OQ1–4.
- **Partners:** fork (`ch-8djrpmsl`) = build-together on address resolution + the loadable brain (split the integration).
  DNA = a point-in-time CLONE the lead is spinning (DNA herself is occupied) for the visual+verbal face.

## THE PHASES (sequenced so the loop can't build on sand — but in sprint mode they overlap)

### Phase 0 — THE OPERATOR JOURNEY  ·  ✅ APPROVED by Tim ("yeah OK", 2026-06-17)  ·  Part 1
The design ground everything else conforms to.

**The spine (the discovery):** every moment, at its own depth, answers the SAME three questions —
**① What am I looking at? · ② What can I do here? · ③ Where can I go from here?**
Pedagogy = never leave a moment with any of the three unanswered, in human terms. **Drill-down = the same three
questions one level deeper** (composition's "deeper coordinate on the same axes") — self-similar all the way down.

**The walk** (and the data each moment requires → how the journey names the fields):
- **0 · First contact (cold).** One breath: *what this whole thing is* + *what it's aimed at now* (meaning, not
  mechanism). → the instrument's one-line identity + the current **address**'s *what-this-is / what-fills-it*.
- **1 · Reading the whole.** What position means here — centre, near/far, the slices — for THIS aim + THIS lens.
  → the **lens**'s *what-position-means-here* (centre/angle/radius/poles → human).
- **2 · Something catches the eye.** "What's *that*?" — reward the wonder. → each **element**'s identity on inspection.
- **3 · The first reach (act).** Touch / hide labels / slide time; controls read AS controls; first act obvious, safe,
  reversible. → each **control**'s *what-this-does* + the operator-state that remembers what they touched.
- **4 · Going in (drill).** Re-aim at a thing's interior; re-orient with the same three questions at the new level.
  → the SAME meta on the deeper **address** (every level self-describes).
- **5 · Realising it re-aims.** It's not one view — point elsewhere, dial other lenses. "You are here; here's what else
  you can aim at + what each lens shows." → human names for every **destination** + every **lens** (what it reveals).
- **6 · Coming back (time + state).** It remembers where they were + shows what changed. → the operator's
  **journey/state** as itself addressable + self-describing.

**The legibility-type's facets (what the journey names — composition builds the vessel, the journey shapes the set):**
- **address/thing** → name · is · fills · why
- **lens** → what position means · what it reveals
- **element** → identity on inspection
- **control** → what it does
- **destination** → name · what's there
- **journey/state** → where you are · where you've been · what changed

Not one flat block — these *facets*, each attached to the kind of thing it describes, all answering ①②③ at whatever
depth the operator stands. Composition's "the field-set is itself a TYPE" → the journey designs that legibility-type.

### Phase 1 — RESEARCH (no building) → the three-doc prep
Explore-before-specify (the loop-prep law). Via company MCP (corpus, code search, capabilities) + the channel:
- The **address system** (the universal-projection resolver + the code:// contract; how addresses are formed/resolved).
- The **right-hand-man connection** (how the RHM connects — bridge, the AI session, the panel/chat surface).
- The **loadable brain** (fork's loadable-brain wire — `fork-gallery-brain-hooks.js`; how a brain is loaded/bound).
- Existing **overlay / floating-action / radial-menu / draggable** patterns in the design system (reuse, don't parallel).
- Composition's **V component** + the **V icon** image (asked: thread `t-1781683048`), + the **legibility-type skeleton**
  composition is drafting (thread `g-1781668099`).
Output: update `COMPLETION-CRITERIA.md`-style criteria + an implementation guide + a research synthesis for this build.

### Phase 2 — THE V OVERLAY SHELL (low-ambiguity — Tim was specific)
Safe to build once Phase 1 grounds the seams. The RHM's identity, on every page:
- The **V icon** (placeholder, from composition) in the **bottom-right corner**; an **overlay** over any surface.
- **Tap → expansion modes:** slots/options that **fan out around the corner angle** (a radial arc) — stays in the
  corner, thumb-reach, never takes the whole screen.
- **Draggable** anywhere (to read under it); persists position; **present on every page**.
- A **replaceable seam** so the real V component swaps in later (composition's brief defines the seam).
- Fit the design ethos; verify by use both viewports (390 first); fresh-eyes critic.

### Phase 3 — GATED on design convergence (high-ambiguity)
Do NOT build until the design (Phase 0 questions + composition's legibility-type) converges:
- **The built-in tutorial** — the V highlights + walks the operator through screen parts (what the surface is, how to
  use the instrument, what things are, where they go). This is Phase-0 pedagogy *delivered by the RHM*.
- **The legibility field-set** wired live (the instrument reads registry `meta` for whatever it's aimed at) — conforms
  to composition's legibility-type; blocked on the journey naming the field-set.
- **Multi-select comm/channel groups** through the RHM *(NEEDS TIM CONFIRM — see open questions)*.
- Connecting the **RHM connection** + **loadable brain** + **address system** into the surface behind the V.

### Phase 4 — THE DECISION SURFACE (the prove-on-one; lead-SEQUENCED 2026-06-18, thread g-1781731059)  ·  ACTIVE
Tim's "first composed product" — the keystone retargeted at decisions, resolution-first. Spec: `build-prep/the-one-application/DECISION-SURFACE-BUILD.md`. Build order (foundations first): **1 composition** (the 3 TYPES: decision/option/decision-card — the long pole, gates the rest) → **2 fork** (decision:// scheme + keystone wire + RHM brain) → **3 DNA** (decision-card archetype render, slide format) → **4 projection (HOST — us)** → **5 wildcard** (the TAKE: verbs→territory_write) → **6 recollection** (the common-memory recall leg).
**projection's piece (stream #4) — DESIGN-CONFIRMED (mostly ALREADY BUILT — the resolution-first payoff):**
- BUILT (reuse, zero net-new): `/api/territory` (scheme-agnostic resolve — PROBED on `decision://` → degrades clean, full resolution the moment fork registers the scheme); the HOST (instrument + V + GalleryMount); the gallery:verb dispatcher (the TAKE rides it).
- NEEDED (small; 3 seam Qs broadcast g-1781731457): (1) /api/cognition EXPLAIN-WIRE — fork: do I route your brain-turn-from-address, or do you provide it? (2) the universal-MEMORY route — recollection/fork: fold the recall into territory_for (then /api/territory carries it, zero net-new) or a recall:// scheme (projection adds a route)? (3) the decision-SLIDE host-mount — DNA: via window.DNA.renderGallery (existing host, zero net-new) or a new slide-sequence path (projection adds a container)?
- SMALLEST PATH: if (2) folds into territory_for + (3) reuses renderGallery, projection's ONLY net-new is (1) the explain-wire — possibly just routing fork's brain. **Build the residual the instant composition's types (#1) + the 3 answers land.**

---

## OPEN QUESTIONS (block freezing the field-set / Phase 3; nothing earlier)

- **OQ1 — the spine's fourth question?** Is ①②③ complete, or does the operator always carry a fourth ("what's this
  for / why should I care") that deserves its own standing? (Tim hasn't answered.)
- **OQ2 — missed branches?** Any operator path beyond first-contact / read / notice / act / drill / re-aim / return?
- **OQ3 — the cold-open first sentence.** What should the very first breath SAY? Sets the whole pedagogy; the most
  Tim's of all. (Cheapest highest-value item to grab live.)
- **OQ4 — "multi-select comm select groups"** — confirm meaning (read tentatively as "select multiple communication
  groups at once" through the RHM).

---

## STATUS LOG (update every fire, honestly — ✅/🟡/🔴)

- **2026-06-17 — loop armed.** Phase 0 ✅ approved (operator journey). V-icon asked of composition (`t-1781683048`).
  Legibility-type skeleton in progress with composition (`g-1781668099`). NEXT: Phase 1 research (address system / RHM
  connection / loadable brain / overlay patterns) — NOT building yet. OQ1–OQ4 open.
- **2026-06-17 — Phase 1 research started.** Fork (`ch-8djrpmsl`) engaged on **address resolution + the loadable-brain
  integration** (thread `t-1781684391`) — Tim's pointer (fork built the loadable-brain wire + knows the resolve path).
  Asked: address-resolution path + traps, how a brain loads/binds + carries across every page (RHM connection), what to
  reuse-not-parallel. Awaiting fork's reply; will fold into the research synthesis.
- **2026-06-17 — ⚡ SPRINT MODE on (Tim away).** Loop cadence → **~7min** (job `961a66ef`; was 2h). Fork now a BUILD
  PARTNER (split the integration; thread `t-1781684391`). Lead asked to spin a **DNA point-in-time clone** for the
  visual+verbal face (thread `t-1781684666`; Tim's model intent "a fable… system should change it to opus" — lead owns
  the mechanics).
- **2026-06-17 — ✅ Phase 2: the V-OVERLAY SHELL built + verified (commit `3ac24f3`).** The right-hand-man handle:
  company-gold V icon (composition's `vi-classic-gold.svg`, copied to `surface/app/src/rhm/v-icon.svg`), bottom-right,
  DRAGGABLE + position-persisted (localStorage), on EVERY page (mounted at App root). Tap → the **6-verb radial fan**
  (composition's V-corner-handle contract: navigate/ask/annotate/drive/open-source/generate → draft labels Go to/Ask/
  Note/Drive/Source/Make) with a soft paper **overlay scrim**. Files: `surface/app/src/rhm/{RightHand.tsx,rhm.css,
  v-icon.svg}` + mounted in `App.tsx` + css in `main.tsx`. VERIFIED BY USE at **1440 + 390** (fan legible, no overlap/
  clip, drag moves+persists exactly), `tsc` + design-lint clean (0 off-token literals), 0 console errors, a SEPARATE
  fresh-eyes design-critic returned **PASS** (both its flagged issues — chart-overlap legibility + arc-not-column —
  resolved by the scrim + left-lean). Built on `paper.css` tokens; the gold lives in the SVG, not CSS.
  **SWAP SEAM** (composition's contract): the real V organism later renders into THIS container; verbs emit a
  `rhm:verb` window event the integration phase consumes (meet-at-data, no cross-repo code import).
  **MINOR polish carried (non-blocking, critic):** (a) ease the arc to bow slightly (currently ~linear diagonal);
  (b) on-device check the lowest pill's margin vs the scrubber thumb-zone.
  **NEXT:** (1) INTEGRATION with fork — wire the verbs to the address system + loadable brain (awaiting fork's brief,
  thread `t-1781684391`); (2) the built-in TUTORIAL (Phase-0 pedagogy delivered by the RHM — gated on journey/copy);
  (3) the legibility meaning-layer (composition's legibility-type, thread `g-1781668099`). Verb ACTIONS are stubs
  (emit the seam event) until (1) — honestly a shell, not wired behaviour.
- **2026-06-17 — ✅ Phase 3: the RIGHT-HAND-MAN BRAIN wired into the V (commit `c93c6ef`).** Tapping "Ask" opens a
  paper talk panel where the operator talks to REAL Claude Code about the current AIM (fork's `forkVBrain.attach` —
  the V host owns the aim [getAimAddress/getAimLabel, follows `projection:select`, default `ui://instrument/surface`];
  fork owns turn/stream/write). Applied bridge diff 3 (`/api/territory/label` → human aim-label). Synced
  `fork-brain-core` + `fork-v-brain` into the host (core loads first). VERIFIED BY USE: a real Claude Code reply
  STREAMED into the V panel at 390; panel renders at 1440; `forkBrainCore`+`forkVBrain` loaded, `.v-brain` mounted;
  0 console errors; design-lint 0, tsc 0.
  ★ OPERATOR-LAW FIX (caught by-use): the brain LEAKED a raw address ("I'll investigate what `ui://instrument/surface`
  …") in its first reply → strengthened `PANEL_BRIEFING` (`ui_claude_session.py`) to forbid raw addresses/scheme://
  in replies + told it the address-context is FOR-IT, not to echo → re-verified: "a piece of his own interface", no
  leak. (Put it in the canonical operator-face prompt — covers V + gallery; flagged fork, thread `t-1781684391`.)
  - 🟡 **RESIDUAL (flagged to fork's context-tuning lane):** when the aim is THIN/unregistered the brain narrates the
    system's internal readout ("empty blast radius, a module error on the relations leg") — softer than an address
    leak but still semi-jargon. Fixes: a meaningful registered default-aim + briefing tuning to not narrate internals.
  - **TRAIL for the lead's union (absorb-not-fork):** touched `/api/territory/label` (new read route) + the brain
    wire (the `_claude_stream` briefing). Note for the substrate-unification to absorb.
  - **NEXT:** wire the OTHER verbs (navigate/note/drive/source/make → their backends), the built-in TUTORIAL (Phase-0
    pedagogy delivered BY the RHM), and the legibility meaning-layer (composition's `legibility.js` `resolveLegibility`,
    built + ready — the instrument reads registry `meta` for the aim + renders human meaning).
- **2026-06-17 — ✅ LEGIBILITY (first slice): the legend reads HUMAN meaning from the REGISTRY (commit `7d9b646`).**
  Tim's #1 complaint ("I don't know what I'm looking at") — the Kinds-view legend was hardcoded jargon in `Legend.tsx`
  ("11 sectors · angle = the kind of each event"). MOVED the meaning into the REGISTRY: seeded `bindings/raw.py`
  `meta:{name,is,fills,why}` (composition's SEED shape; TENTATIVE draft copy), passed it through `/api/projection`,
  typed in `api.ts`, rendered DECLARED-FIRST in `Legend.tsx` (fallback = the computed lines for un-seeded lenses). The
  Kinds view now reads: "What's happening · A live map of everything the Company is doing · Each dot is one thing that
  happened — a note saved, a job run, a message; its slice is what KIND it is; how far from the centre is how long
  ago…". VERIFIED BY USE 390 + 1440 (jargon gone, human, registry-sourced), design-lint 0, tsc 0, 0 console errors.
  The instrument stays EMPTY of meaning (reads `binding.meta`) — registry-is-truth, the architecture Tim ratified.
  - **Coordinated composition** (thread `g-1781668099`): I seeded `binding.meta` with their SEED field shape on the
    surface/registry side; their legibility-type validate/backfill machinery enforces/declares it; field-set stays
    journey-gated (OQ1–4).
  - **NEXT:** (a) seed the OTHER lenses' `meta` so every view is legible (connections / two-gravities / meaning / …);
    (b) the SECTOR names (the ~51 machine kind-ids → human) — the lexical layer, needs per-kind meaning + an endpoint;
    (c) a HIDE toggle for the legend (Tim: "be able to hide them"); (d) the remaining verbs (Go to/Note/Drive/Source/
    Make) + the built-in tutorial.
- **2026-06-17 — ✅ LEGIBILITY: the WHOLE lens system reads human (commit `9b5da09`).** Seeded `meta:{name,is,fills,why}`
  + humanized the jargon `label` across ALL 9 bindings (raw / by_node_type / by_separator / by_nucleation / semantic /
  by_cascade / by_lens / grouped / time-of-day). Fixes Tim's IMG_1925 dropdown complaint: "Connections — the node
  registry by its directional type-flow" → "Connections — how things flow into each other"; "Type-nucleation" → "New
  kinds forming"; "Lenses — angle by the projection registry" → "Ways of looking"; etc. Registry-true (meaning in the
  rows; the Legend renders declared-first). Composition CONFIRMED the shape (19:53 — meta on any row; their validate/
  backfill covers it; ONE legibility-type, MANY registries). TENTATIVE copy (Tim/DNA ratify; field-set journey-gated
  OQ1–4). VERIFIED BY USE 390: switched to Connections → the human legend renders + the dropdown is fully humanized
  (raw verified at 1440 earlier; shared Legend component). The instrument stays empty of meaning.
  - **NEXT:** (a) the ~51 SECTOR kind-ids → human — composition's answer: SAME legibility-type, OWN kinds-registry; seed
    kind rows with `meta` now (tentative), the set-diff finds misses → needs the kinds-registry + a per-kind meaning
    path + the wheel-label render; (b) the HIDE toggle for the legend (Tim's "be able to hide them"); (c) the remaining
    verbs (Go to/Note/Drive/Source/Make) + the built-in tutorial.
- **2026-06-17 — ✅ legend HIDE TOGGLE (commit `add09df`).** Tim's "I should be able to hide them if wanted" — the
  legend explanation now collapses (the TITLE stays for orientation; the preference persists, localStorage). Refactored
  Legend to compute the orientation once (meta-first → fallback) then wrap it in the collapse; paper-token toggle +
  focus-ring. VERIFIED BY USE 390: collapse hides the lines + keeps the title, flips Hide↔Show, persists across reload;
  tsc 0, design-lint 0 (1440 = shared Legend component).
  - **NEXT (sector kind-names — coordinating composition, thread `g-1781668099`):** asked WHERE the kinds-meaning
    registry should live (no existing kind→human registry found — only closed primitive tuples) so I seed on their
    validate/backfill machinery, not beside it. RENDER plan = TAP-A-SECTOR → human name + one-line meaning (reuse the
    existing sector-selection), NOT 51 labels at once (crowds at 390). Then: the remaining verbs (Go to/Note/Drive/
    Source/Make) + the built-in tutorial.
- **2026-06-17 — ✅ LEGIBILITY: SECTOR kind-names → human (commit `d704e27`).** Tim's "I don't know what a sector
  is" — the wheel's sectors were raw machine-ids. NEW kinds-registry `kinds/raw.py` (composition's specified home:
  mirror bindings/raw.py, ONE meta map `{kind:{name,is}}`); `projection.py` sets each sector's `label` = the kind's
  human name (declared-first via the registry) + `meaning`, humanize-id fallback for un-seeded; sectors now tappable
  in ALL views (was edges-only); the tap-a-sector readout shows the human name + one-line meaning. VERIFIED BY USE
  390: tap `corpus.record` → "A note saved · The system wrote something into its memory"; all 11 raw-view sectors
  read human. Registry-true, instrument empty of meaning. tsc 0, design-lint 0, 0 console errors. Coordinated
  composition (kinds/raw.py is their home; their validate/backfill set-diff covers un-seeded kinds).
  - **NEXT:** the remaining verbs (Go to/Note/Drive/Source/Make → their backends); the built-in TUTORIAL (Phase-0
    pedagogy by the RHM); seed more kinds + the OTHER registries' meta (node-types/lenses/…) as views surface them.
- **2026-06-17 — ✅ the 'NOTE' VERB wired (the first ROUTE-BACK from the V).** Tapping the V's **Note** opens a paper
  composer that writes the operator's comment AT THE CURRENT AIM — reusing fork's brain (`brain.direct({type:'comment'})`
  → `/api/territory/write` → `suite.mark`); NO parallel writer. The V now re-aims at a **tapped sector** via a new
  `projection:aim` window event (Wheel→RightHand) — deliberately NOT `projection:select` (that opens a content face;
  a synthetic `ui://instrument/sector/<id>` would fail-loud in GalleryMount). The event carries the sector's HUMAN
  name **and** one-line meaning, so the composer head reads "Note about: A session turn / A working session finished
  one back-and-forth." (operator-law: meaning, never the address). After Save → "Noted ✓" then the composer
  **auto-closes** (timer cleared on edit/reopen/close/unmount — no lingering empty box). Files (this fire):
  `surface/app/src/wheel/Wheel.tsx`, `surface/app/src/rhm/{RightHand.tsx,rhm.css}`.
  - **VERIFIED — round-trip, by use, BOTH viewports.** Pre-flight curl proved `comment` is a registered mark_type +
    `suite.mark` accepts a sector target. Then DRIVEN: 390 (tap `agent_sessions.turn` → V → Note → type → Save) and
    1440 (`corpus.record`) — each note **READ BACK** from `suite.marks_for(ui://instrument/sector/<id>)`
    (value = the typed text, source=`operator`), not a POST-200. Auto-close + clean reopen verified by use at 390.
    tsc 0, design-lint 0.
  - **HONEST coverage:** the happy path is use-verified at both viewports + store read-back. The **error path** (brain
    module unmounted, or a write failure → "Couldn't save") is **logic-traced only**, not driven — it surfaces loud
    (no silent no-op) but has not been exercised live. (Two test marks now sit at `sector/_probe` +
    `sector/agent_sessions.turn` + `sector/corpus.record` — harmless; nothing renders sector-notes yet; the store has
    no delete API.)
  - **FRESH-EYES CRITIC (separate agent, cold-drive 390):** returned NEEDS-WORK, but its headline ("context chain
    broken — header steals a toast string") was a **MISDIAGNOSIS** — it drove blind (checked SVG `title` attrs, never
    saw the `.conn-readout`) and pattern-matched the sector-identity readout as a save-toast. Proof it's wrong: the
    `agent_sessions.turn` test showed "A session turn" (no save-connotation) and read back at that address. REAL signal
    extracted + FIXED this fire: (a) composer ambiguity → added the **meaning line** under the name; (b) the
    no-auto-close dead-end → **auto-close**.
  - **ROUTED to the legibility beat (not deferral — wrong evaluator here):** several kind NAMES read as past-tense
    EVENTS rather than CATEGORIES ("A note saved" ≈ "a note was just saved"). Cross-cutting (touches every kind, copy
    is TENTATIVE pending Tim/DNA); **Tim's eyes on his phone are the real fresh-eyes test** per THE BAR. The legibility
    beat should revisit `kinds/raw.py` naming. Also flagged: verb labels **Drive/Source/Make read as jargon** to a
    non-technical operator (tentative; ratification gated on Tim/DNA + OQ3 tutorial).
  - **NEXT:** the remaining verbs (Go to/Drive/Source/Make → backends); the built-in TUTORIAL (gated OQ3); seed more
    kinds + other registries' meta as views surface them.
- **2026-06-17 — ✅ re-verified the V BRAIN survives fork's env-scrub + ✅ LEGIBILITY: the CONNECTIONS lens reads human.**
  TWO things this fire:
  - **(1) MANDATORY re-verify (fork touched my wire).** fork's commit `1c0aa25` made `ui_claude_session.run_turn`
    pass `env=_brain_env()` to the `claude -p` Popen (strips `SUPABASE_*`/`VI_VISION_*` from the brain subprocess,
    for the vi-vision merge). Drove 'Ask' on my live mount at 390: the full turn streamed GREEN — init→text→tool→
    final (~1.8k-char grounded reply; it ran a grounding TOOL mid-turn, so tool-use survived the scrub), POST
    /api/claude/turn 200, no error, operator-law held (said "the Instrument surface", never a raw address). No
    regression; reported to fork (thread `t-1781684391`).
  - **(2) the CONNECTIONS lens (binding=by_node_type) was illegible** — its sectors are NODE TYPES, not kinds, so
    they fell through to the humanize-only fallback: cryptic titlecased ids ("Llm", "Titlecase", "Rhm Mode",
    "Portal", "Gate"), `meaning: null` on all 16 (verified via curl). FIX, mirroring the accepted `kinds/raw.py`
    pattern (composition's "ONE legibility-type, MANY registries"): NEW `nodes/_meta.py` `NODE_TYPE_META`
    `{node:{name,is}}` for all 16 node types (grounded in each node module's own docstring, TENTATIVE for Tim/DNA);
    `nodes/__init__.py` makes the dir importable (discover() skips `_`-files, so neither is loaded as a node);
    `projection.py` now picks the meaning-registry by sector DOMAIN (`binding.angle_from`: kinds default, node-types
    for Connections) via `_SECTOR_META_BY_DOMAIN` + the `_kind_name/_kind_meaning(s, meta)` param. Files:
    `nodes/{__init__,_meta}.py`, `runtime/projection.py`.
  - **VERIFIED BY USE, BOTH viewports.** 390: switched lens → Connections, tapped `llm` → readout "An AI step /
    Calls an AI model to transform or generate text. / 1 out · 5 in". 1440: tapped `retrieve` → "Find the nearest /
    Searches a collection and returns the items closest in meaning to your query. / 2 out · 3 in". Kinds (raw) lens
    re-checked = no regression (6/6 still human). Bridge restarted to load the registry; curl-confirmed 16/16 seeded.
  - **FRESH-EYES copy critic (separate agent, TEXT not blind-drive — avoids last fire's readout-misread):** judged
    the 16 name/desc pairs as a non-technical reader; 12 CLEAR, 4 UNCLEAR. FIXED all 4 in `_meta.py`: dropped
    "source code"→"code"; "numeric fingerprint"→"a fingerprint of its meaning"; "order matters"→"keeping the order
    you gave them"; and the worst — "The V's mode"→"Right-hand-man mode" (the name assumed you know what "The V"
    is, and my own description already said "right-hand-man" — inconsistent). Re-verified post-fix via curl.
  - **HONEST:** node-type COPY is TENTATIVE (Tim/DNA ratify; some — model_of_tim/rhm_mode/portal/gate — are my
    grounded reads of dev docstrings, not Tim's words). HOME is a sibling meta-map; the declared-at-birth ideal
    (a `META` on each node module, read into NodeType like OUTPUT_SCHEMA) is composition's call — flagged to them.
  - **VERBS still blocked (not built this fire, deliberately):** Go to/Drive/Source/Make have no defined operator-
    semantics in the briefs (the briefs define the ADDRESS-MOVER roles — recollection.navigate / fork.resolve —
    not what each V verb MEANS), AND the dominant V-aim is a synthetic `ui://instrument/sector/<id>` that doesn't
    map to centre/resolve/source on a real data node (App's relative-centre needs an embeddable address). Building
    one now = guess OR a frequently-no-op verb. Asked fork (the navigate-mover question) + will ask composition for
    the verb contract. Chose the safe high-value legibility beat instead.
  - **NEXT:** get composition's verb-contract semantics → build the clearest verb; coordinate node-meta HOME with
    composition; seed the OTHER registry domains' meta (roles/mark_types/lenses) as their lenses surface; TUTORIAL gated OQ3.
- **2026-06-17 — ✅ LEGIBILITY: human kind-words ride on every POINT + the point-inspector's operator-law LEAKS fixed; ⚠ found the DEFAULT-PATH drill-in (DNA's gallery face) badly illegible → routed to DNA.**
  - **The verbs stayed blocked** (composition heads-down on the vi-vision creds merge; no verb-contract reply yet). Confirmed
    they're genuinely blocked/redundant: re-centre is ALREADY wired (Disclosure "⊙ centre here" + CentreChip), so a
    'Go to'=re-centre verb would duplicate; Make=generate is the lead-governed gated keystone (NOT brain-callable, per fork).
    So I did the safe high-value legibility beat instead.
  - **★ FRESH-EYES (the dot-drill, deferred twice — finally tapped):** on the DEFAULT path, tapping a real thing opens
    DNA's gallery FACE, and it's badly illegible: the annotation row renders as bare ICONS (★ ✓ ✕ ? ☆ → + a tofu glyph)
    shown TWICE with machine-name aria ("remember_this", "do_this"), and the panel literally titled **"What this is"
    contains NO words** — just more icons. That's the deepest "understand what I'm looking at" surface failing the ROOT
    bar. It's DNA's visual+verbal face (GalleryMount hosts window.DNA) — NOT my lane to rebuild → ROUTED to DNA with the
    evidence + the fix-data (below).
  - **ROOT-CAUSE FIX (my lane, registry-is-truth): every PROJECTION POINT now carries `kind_name` + `kind_meaning`**
    (read declared-first from the kind registry; humanized-id fallback). The machine `kind` stays the key; the human
    words ride ON the data so EVERY consumer (the inspector, the gallery face, a future tooltip) shows MEANING, never
    the id. This is exactly the data DNA's "What this is" needs. Files: `runtime/projection.py`, `surface/app/src/lib/api.ts`.
  - **POINT-INSPECTOR (Disclosure.tsx, my lane) operator-law LEAKS fixed:** it was showing the **raw machine kind-id**
    as the header (`point.kind`), a **raw ui:// address** in a `<code>` footer, and the machine sector-id in the "in"
    row. Now: header = `kind_name` (case preserved — removed the `text-transform:lowercase` that would mangle "An AI
    step"→"an ai step"); a new meaning line under it; the raw-address `<code>` REMOVED (the address still rides on
    `data-ui-ref` for the spine, just never shown); the "in" row shows the human kind name on the Kinds lens. Files:
    `surface/app/src/wheel/Disclosure.tsx`, `surface/app/src/surface.css`.
  - **VERIFIED BY USE, BOTH viewports:** 1440 (Disclosure panel) + 390 (sheet), drove an address-less point (seq-5997,
    the gallery face only suppresses Disclosure for addressed points) → header "A session turn", meaning "A working
    session finished one back-and-forth.", in: "A session turn", NO raw address. Curl: all 600 points carry kind_name
    + kind_meaning. tsc 0, design-lint 0. Bridge restarted for projection.py.
  - **HONEST / still leaking (NOT my lane — routed):** (a) DNA's gallery FACE (the main drill surface) — to DNA; (b) the
    HOWTO **context-item text** embeds a raw "ui://canvas" (it's CORPUS CONTENT from /api/context, not a UI label) —
    flag to fork/composition (context composition); (c) other-lens "in" rows still show the sector leaf (those lenses'
    sector-human-names aren't carried on the point yet — a later enrichment).
  - **NEXT:** DNA renders point.kind_name/kind_meaning + labels the gallery-face icons (routed); composition's verb
    contract → build a verb; carry sector-human-names on points for non-Kinds "in"; TUTORIAL gated OQ3.
- **2026-06-17 — ✅ the V's VERBS are now LIVE on composition's unified `gallery:verb` contract; ✅ `navigate` (Go to) =
  re-centre wired + verified; ✅ fixed a latent gallery-reopen bug.** The verb contract LANDED (composition ch-2mnxl9j0
  + wildcard ch-piffgfxv): canonical ids navigate/ask/annotate/drive/open-source/generate; ONE event `gallery:verb`
  `{verb, aim_address, payload}`; projection owns navigate/drive/open-source, ask→fork, annotate/generate→wildcard;
  verb-order GO-TO+SOURCE first, MAKE last (writable-aim-gated). Built:
  - **The V (RightHand) now emits the unified `gallery:verb` envelope** for navigate/drive/open-source/generate
    (replacing the bare `rhm:verb` stub); ask/annotate stay the V's own legs. The verb ids already match composition's
    canonical ids.
  - **projection-side DISPATCHER (App.tsx)** consumes `gallery:verb` and routes by verb — the durable, swap-independent
    consumer (survives DNA's incoming vee-dock V). **`navigate` (Go to) = RE-CENTRE the instrument on the aimed THING**
    (reuses the proven `focusCentre` — the seed §8 relative-centre). Aim-type-gated: a real-unit aim (a picked point,
    run://·code:// addr) re-centres; a synthetic sector/surface aim → a calm Notice ("Go to re-centres on a thing — tap
    a dot first"). drive/open-source/generate → honest "… is coming next." Notice (no silent no-op) — actions pending.
  - **★ BUG FIXED (latent, mine): the `projection:select` effect re-fired on EVERY re-projection** (it had `proj` in
    deps), re-announcing the same selection on each lens/centre/time/**live-pulse** re-fetch → DNA's drill re-rendered
    → the drill face **re-opened spuriously** (incl. right after a Go-to re-centre, and on every live pulse while a
    point was selected). Fixed: dispatch only when the selected UNIT changes (ref-guarded); space-change already clears
    selection so the detail stays current. Files: `surface/app/src/{rhm/RightHand.tsx,App.tsx}`.
  - **VERIFIED BY USE, BOTH viewports.** 390 + 1440: picked an addressed point (seq-5849 → run://…/voice.py) → dismiss
    drill → V → **Go to → the wheel re-projected around "voice.py"** (CentreChip "⊙ voice.py", radius flips to
    "distance: structural from voice.py"), and the drill face did NOT re-open (fix holds). Sector-aim Go-to → the
    honest Notice (390). Drive → "Drive is coming next." (1440). tsc 0 (no CSS this fire).
  - **★ ARCHITECTURAL OBSTACLE surfaced to composition/DNA:** point-aim verbs fight the interaction model — picking a
    thing opens DNA's gallery FACE which (a) sits ABOVE the V (z-order, V unreachable while open) and (b) is the only
    way to aim the V at a real point (address-less points aim at the surface). So a point-verb needs a pick→dismiss→V
    dance. The verbs want either a usable aim WITHOUT the face dominating, or to operate from within the face. Design
    call for composition (contract) + DNA (face).
  - **OPEN with composition (asked):** confirm `navigate`=re-centre (vs locate — the contract gave the EVENT, not the
    per-aim ACTION); specify drive/open-source actions for sector vs point aims; MAKE wiring is wildcard's gated keystone.
  - **NEXT:** composition's per-aim action spec → wire drive/open-source; resolve the gallery-intercept so point-verbs
    are one-tap; DNA's vee-dock swap (emits the same `gallery:verb`); TUTORIAL gated OQ3.
- **2026-06-17 — ✅ the V's "SOURCE" (open-source) VERB GOES LIVE — drill past the dot to the fuller record (commit `660fd40`).**
  composition CONFIRMED the per-aim spec (their 22:51 reply): open-source = REVEAL THE AIMED THING'S SOURCE — for a real
  point, its definition/record + provenance. The advisor caught a conservative drift (I'd argued myself out of this on a
  redundancy worry composition had already refuted): open-source's ACTION is NOT blocked — only its button-PLACEMENT is
  (composition escalating verb-bar-on-face to Tim). The App `gallery:verb` dispatcher consumes the verb regardless of
  who/where emits it, so the action survives whatever placement Tim picks. So I built it (a READ — safe for the away-sprint,
  no write-gate like generate, no socket like drive; the most-specified verb; serves the ROOT bar most). Built:
  - **NEW `GET /api/territory`** — the structured, scheme-agnostic SOURCE read; REUSES fork's `territory_for` (NOT rebuilt);
    sibling of `/api/territory/label` + `/write`. **This is the projection-HOST's named "/api/territory wiring" leg** the
    lead's seam map calls projection's lane. Hands the surface the structured dict — `territory_prose` is brain-framed
    ("[internal handle — never shown to operator]…") so the SURFACE does the human translation, not the resolver. Route-table
    acceptance green (8/8). Bridge restarted to load it.
  - **NEW `surface/app/src/source/{SourcePanel.tsx,source.css}`** — a DURABLE, swap-INDEPENDENT App-level surface (NOT inside
    the replaceable V `rhm/`): reads the comprehended record and shows it in PLAIN WORDS. `readTerritoryContent` handles
    `output`-as-prose AND `output`-as-LIST (→ bullet lines) — recursive; a STRUCTURED/unresolvable record degrades clean
    ("not in plain words here"), NEVER a raw dict/code/address dump (operator-law). Provenance line only when real board refs
    exist (0 for corpus points → honestly absent). Paper-tokened; design-lint 0/0.
  - **App.tsx dispatcher:** `open-source` → `fetchTerritory(selected)` → the panel; a synthetic sector/surface aim → a calm
    Notice (sector-DERIVATION source is the next beat), never a silent no-op. Opening Source clears the point selection so the
    redundant inspector doesn't double up behind it.
  - **VERIFIED BY USE, BOTH viewports (390 first, then 1440):** drove the real path (pick a corpus point → dismiss DNA's face →
    V → Source) → the 6 captured principles render (the inspector showed NOTHING — summary is empty for these points). Address-
    less point → the honest Notice. tsc 0.
    - **★ advisor caught a verify-the-wrong-way gap:** I first dismissed the face via Escape — but the PHONE has no Escape, the
      operator taps the close-✕ or the scrim. Re-verified at 390 the REAL mobile dismiss BOTH ways (tap `.gallery-dismiss` AND
      tap `.gallery-scrim`): `selected` persists through each (GalleryMount's dismiss only flips `galleryOpen`, never touches
      selection) → V → Source → panel WITH the record, NO "tap a dot first" Notice. The named-device bar holds for the real tap.
  - **★ FRESH-EYES critic (separate agent, screenshots):** strongest findings FIXED — (a) the bullets floated with no heading
    → added a "What's in it" body label so it coheres; (b) the tag "SOURCE" read as "source code" → reframed "The full record"
    (+ relayed the verb-LABEL rename signal "The full story"/"See the record" to composition, whose contract owns the draft
    label); (c) two same-titled cards read as broken → clearing selection on Source-open removes the duplicate inspector.
    FILTERED OUT (intentional): the bullet CONTENT being engineer-speak ("structured JSON output") — that's the REAL stored
    record; sanitizing it = fabrication and defeats "Source = the actual thing." Faithful display is correct.
  - **⚠ HONEST / flagged (not blocking):** the RELATIONS (provenance) leg in `territory_for` throws ModuleNotFoundError ONLY
    in the live bridge process (degrade-clean note; reproduces clean in a fresh interpreter AND on `import runtime.bridge`, so
    it's the threaded-request import state). edges_in=0 for corpus points regardless → Source's provenance line correctly shows
    nothing. → flagged to fork (ch-8djrpmsl) with evidence + asked them to (a) diagnose, (b) put the exception MESSAGE in the
    degrade-note. NOT claimed fixed (couldn't root-cause without instrumenting the live process).
  - **COORDINATION (via fabric):** composition (Source live + label signal + drive held on placement); fork (/api/territory +
    the relations-bug flag); lead (confirmed wildcard's HOST/TAKE conflation correction — projection=HOST, wildcard=TAKE; +
    RESOLUTION-FIRST absorbed); DNA (the DECISION-SURFACE-BUILD.md company-repo PATH they asked for + the meaning-fields +
    /api/territory reuse for their decision-card render).
  - **NEXT:** **sector-human-names on points** (the advisor-noted few-line ride-along, deferred to keep this beat clean — the
    Disclosure "in" row still shows a machine sector leaf on non-Kinds lenses; carry `_kind_name(sid,_sector_meta)` onto each
    point); DRIVE / open-source-on-SECTOR (sector-derivation source) once Tim's verb-PLACEMENT steer lands; DNA's decision-card
    render reuses /api/territory; TUTORIAL gated OQ3.
- **2026-06-18 — ✅ LEGIBILITY: the inspector "in" row reads HUMAN on every lens + ✅ ratified verb label "See the record"; ⚠ relations-bug ROOT-CAUSED (handed to fork) after a self-inflicted bridge outage I recovered (commit `acd5e81`).**
  - **✅ Beat A — SECTOR-HUMAN-NAMES (commit `acd5e81`):** every projection point now carries `sector_name` (the sector's human
    name via the lens's meta-registry). The Disclosure "in" row was `p.sector===p.kind && kind_name ? kind_name : leaf(p.sector)`
    — so on NON-Kinds lenses it showed the raw machine sector-id (operator-law leak). Now `p.sector_name || leaf(...)` → human on
    EVERY lens. **VERIFIED BY USE both viewports** on the Activity (grouped) lens: machine `sector="field"` → the "in" row reads
    "Field"; data-level on Ways-of-looking: `common_knowledge`→"Common Knowledge", `memory`→"Memory". Kinds lens unchanged
    (sector==kind). Files: projection.py (compute `_sector_meta` before the points loop), api.ts, Disclosure.tsx.
  - **✅ Beat A2 — ratified label (commit `acd5e81`):** composition ratified (thread t-1781703728) "Source"→**"See the record"**
    (verb id `open-source` + the gallery:verb envelope UNCHANGED — display-only; rejected "The full story" as it collides with
    the coming slide/story/sequence concept). Verified live in the V fan.
  - **⚠ Beat B — the relations-leg bug: ROOT-CAUSED, but NOT fixed by me (correctly).** Pulled fork's fb1c631 message:
    `No module named 'lifters.frontmatter'; 'lifters' is not a package`. ROOT = a **NAME SHADOW**: `runtime/lifters.py` (the
    lifter-registry module) shadows the top-level `lifters/` PACKAGE. The service runs `python runtime/bridge.py` → sys.path[0] =
    the script dir `runtime/`, so `from lifters.frontmatter` (cc_board.py:46) resolves `lifters` → `runtime/lifters.py` (a module)
    → "not a package". Fails PERSISTENTLY in the bridge process (not request-time pollution as first theorized) — which is why the
    leg is always dead + why `python -c` couldn't repro (cwd-root puts the package first). **★ I tried a fix (eager-import cc_board
    at bridge.py module-top to cache it) and it CRASH-LOOPED the bridge at startup** (line-27 hits the same shadow before serving).
    I REVERTED immediately; bridge recovered in ~6s (`/api/now` 200). The clean fix touches CORE (runtime/suite.py + tests + the
    shadow rename) — NOT projection's lane, and the bridge is too central to hack. → handed fork the definitive root cause + two
    fix options (A: rename `runtime/lifters.py`→`lifter_registry.py` [recommended]; B: repo-root-first on sys.path) + the
    don't-eager-import warning. Degrade-clean holds meanwhile (edges_in=0 for corpus points → Source's provenance correctly silent).
  - **★ LESSON (recorded):** verified-by-use means run-as-the-SERVICE-runs. My fresh-interpreter + `import runtime.bridge` checks
    BOTH passed and masked a startup crash that only appears under `python runtime/bridge.py` (script-dir on sys.path[0]). A
    code-path that imports clean in one launch mode can be fatal in another — test the actual ExecStart.
  - **★ FRESH-EYES critic (separate agent, inspector screenshot):** VALIDATED the change (did not flag "Field" as machine). Its
    real findings are OTHERS' lanes / intentional: (a) `routine:self_status` machine summary + `[how-to @ ui://canvas]` raw address
    = operator-law leaks in the inspector — event-summary content + the context-item leak already flagged (commit 7edca62), NOT my
    lane → re-flag to composition/fork; (b) the "0.99" raw value + terse "in" label — the `num` is INTENTIONAL (Disclosure's law:
    "plain words + the REAL value", Tim's design) → not a leak; the "in"-label clarity is a lens-aware refinement → NEXT.
  - **COORDINATION:** composition ratified the label; lead ACCEPTED the HOST/TAKE conflation fix (projection=HOST, wildcard=TAKE)
    and called /api/territory "the proof the resolution-first constraint is real in the build"; fork given the relations root-cause.
  - **NEXT:** the "in"-label clarity (lens-aware: "kind"/"family"/"lens" instead of bare "in"); re-flag the inspector summary+howto
    leaks; DRIVE / open-source-on-SECTOR once Tim's verb-PLACEMENT steer lands; DNA's decision-card reuses /api/territory; TUTORIAL gated OQ3.
- **2026-06-18 — ✅ the V fan tells the truth: LIVE verbs vs "SOON" (no green-paint) + draft verb descriptions (commit `ca2e08c`).**
  The fan answered ② "what can I do here?" DISHONESTLY — all six verbs looked alike, but Drive + Make only Notice "coming next"
  (drive waits on Tim's verb-PLACEMENT steer + a dialable aim — corpus points have no dial; Make is wildcard's gated keystone).
  A stranger tapping "Make" hit a dead-end = half-done-presented-as-done. Fixed:
  - **LIVE** (Go to / Ask / Note / See the record) render solid; **NOT-YET-WIRED** (Drive, Make) render MUTED (faint ink + a
    DASHED provisional border) + a readable **"SOON"** status chip; still tappable → the honest "… is coming next." Notice (no
    silent no-op). The `live` flag is a runtime fact the host knows → the chip drops automatically when each verb wires up.
  - **★ BUG caught by use:** muting via `opacity:0.5` read as opacity **1** live — the `vhandle-pop` animation holds opacity:1
    (fill `both`) and overrides a static opacity. Re-did the mute via colour/border (dashed + faint ink), which the animation
    doesn't touch. (Same class of "the animation wins" gotcha worth remembering.)
  - Each verb carries a DRAFT one-line description (operator-law plain words, TENTATIVE) on title/aria — desktop hover tooltip +
    screen-reader name. **VERIFIED BY USE both viewports** (390 first): 4 solid + 2 dashed-SOON; tapping Drive → the honest Notice.
    tsc 0, design-lint 0/0. Tim's radial thumb-fan preserved.
  - **★ FRESH-EYES critic (separate agent, phone screenshot):** validated the soon-marking (sets expectations), and flagged THREE
    things that are the REAL V component's PRESENTATION (composition's lane — I did NOT redesign the placeholder for them, per
    "V=placeholder, real V swaps in"): (a) the PHONE has no hover → my title/aria descriptions are invisible there → bare verbs
    ("Go to"/"See the record") read opaque on the priority device — the real V needs touch-visible descriptions; (b) live/soon
    interleave (Make top, Drive middle) — group live-first/soon-last (composition's contract order); (c) the fan floats over the
    busy wheel + the radial stagger is hard to scan. → flagged ALL THREE to composition + supplied my draft description copy for
    their verb table. HONEST GAP: a phone stranger still can't read what "Go to"/"See the record" DO pre-tap (labels = composition's
    ratified contract; touch-visible descriptions = the real V's design) — not green-paint (soon-state is honest), but a real
    ROOT-bar gap owned by composition.
  - **COORDINATION:** flagged composition (V-component presentation + draft desc copy).
  - **✅ RELATIONS-BUG LOOP CLOSED (this fire):** fork landed the rename `runtime/lifters.py → runtime/lifter_registry.py`
    (86cd7c4, killing the shadow). I VERIFIED IT LIVE the way that matters (`company restart bridge` + curl — NOT an import
    check): bridge starts clean (6s); `run://corpus/...` → legs.relations=TRUE, notes=[] (ModuleNotFoundError GONE), edges_in=0
    (correct); **`board://item-...` → edges_in=4, edges_out=1** (provenance populates end-to-end). So territory_for's relations
    leg is alive for EVERY consumer — the RHM's brain-turn context (bridge.py:1697) now carries typed-edge provenance, and the
    Source verb's "Referenced by N" line works (silent for corpus points = correct, real for board things). Confirmed to fork.
  - **NEXT:** the "in"-label clarity; re-flag the inspector summary+howto leaks;
    DRIVE / open-source-on-SECTOR once Tim's verb-PLACEMENT steer lands; DNA's decision-card reuses /api/territory; TUTORIAL gated OQ3.
- **2026-06-18 — ✅ V fan conformed to composition's RATIFIED order+copy (commit `7dce89c`) + ✅ operator-law: the inspector's context items never show a raw machine address (commit `0ae6064`).**
  - **✅ Beat A — V-fan order+copy (RATIFIED by composition, thread t-1781706789):** display order → live-first, read→write arc:
    **Go to · See the record · Ask · Note · Drive(soon) · Make(soon)** (4 live lead = thumb-reach; 2 soon trail together —
    resolves the fresh-eyes interleave). Descriptions → canonical wording ("Re-centre the view on this", "Open the full record
    behind this"). A fresh-eyes RE-CHECK confirmed the grouping reads at a glance (usable-vs-coming clear; boxed SOON = "later",
    not "broken"). Verified both viewports. composition RULED the touch-visible-descriptions + scrim + fan-header are the REAL V's
    presentation (their lane) → I kept the placeholder honest, did NOT redesign; flagged the additive findings (header, SOON
    wording, order-direction) for their backlog.
  - **✅ Beat B — context-item operator-law guard (commit `0ae6064`):** the inspector's context section showed
    `[how-to @ ui://canvas] WHAT: …` — a raw ui:// address in operator-facing text. Reframed CORRECTLY: the R2 text legitimately
    carries the address FOR THE BRAIN (suite.py composes it; fed to the RHM via territory_prose) → the fix is the RENDER boundary,
    not upstream (the brain wants it). New `humanizeCtx()` in Disclosure strips a composed `[label @ scheme://addr]` frame + any
    bare `scheme://…` token; the useful content stays ("WHAT: the canvas — the live board…"). VERIFIED BY USE both viewports:
    `hasAddr=false` at 390 + 1440. (This beat's verify was by-use — the address is provably gone; the fresh-eyes this fire was on
    the fan. Root-correct, not a band-aid: the surface translates for the operator, the text keeps the address for the brain.)
  - **✅ RELATIONS-BUG fully closed + acknowledged by fork** (last fire's live verify holds — territory_for's provenance leg alive
    for every consumer). fork flagged a LATENT PATTERN (noted, NOT biting): `roles/`·`skills/`·`mark_types/` have the same
    module-shadows-package shape as lifters did; harmless today (the bridge never `from roles.X`), but if any bridge-path code ever
    package-imports a registry-rows dir it'll hit the identical shadow → the fix is the same (rename the registry module).
  - **HONEST / still open (others' lanes):** the `routine:self_status` event SUMMARY in the inspector is machine-ish — but it's
    CAPTURE-side content (the routine's own output), not the render boundary; humanizing it = fabrication → flagged, not mine.
  - **NEXT:** the "in"-label clarity (needs the binding to DECLARE a division-noun — composition's registry, not an FE hardcode —
    flag it); DRIVE / open-source-on-SECTOR once Tim's verb-PLACEMENT steer lands; composition's real-V presentation (touch-visible
    descriptions); the decision-surface bridge-route once the lead sequences it; TUTORIAL gated OQ3.
- **2026-06-18 — ✅ operator-law: stripped ML jargon from the meaning-reading controls (Layer/Res/Quant chips) (commit `12d8cf6`) + signalled the lead projection is ready for decision-surface sequencing.**
  The three "how meaning is read" chips leaked raw ML terms to the operator (the clearest remaining operator-law violation in
  the chrome): LayerChip "the embedder layer — which embedding you look through"; ResChip "how many vector dims … (MRL zoom)";
  QuantChip "full float (cosine) or binary sign-bits (Hamming-geometry)". Translated the instrument-authored copy → human PURPOSE
  ("How it reads meaning" / "How finely it reads meaning" / "How meaning is compared"), TENTATIVE for Tim's steer. QuantChip values
  full·float→"Full", binary·Hamming→"Coarse".
  - **★ CAREFUL not to mistranslate (blast-radius × reversibility — a MISLEADING human label teaches wrong, worse than honest
    jargon):** the code's own measured note says binary is a coarser GEOMETRY, NOT a speed win (the resolution picker is the speed
    lever). So "Coarse", never "quick". The subtle/risky translations I did NOT guess (see flagged).
  - **VERIFIED BY USE both viewports** (Meaning lens): all three tooltips jargon-free (regex-checked: no embed/MRL/dims/float/
    cosine/Hamming/sign-bit/vector); QuantChip drives Full↔Coarse live (chip → "▦ coarse"). tsc 0, no CSS.
  - **★ FRESH-EYES critic (separate, phone):** confirmed my tooltip fix is DESKTOP/screen-reader only — on the phone (no hover)
    the chip VALUES stay opaque ("◫ default · ◎ full · ▦ coarse" + abstract glyphs = "mystery settings I'd be afraid to touch";
    the ◎ bullseye misreads as 'focus'). Its actionable fix = put the DIMENSION-NOUN on the label ("Detail: Full"/"Grouping:
    Coarse"). FILTERED: this is the SAME phone-no-hover pattern composition owns for the V verbs → flagged for ONE consistent
    phone-label treatment (NOT unilateral chrome churn: the chrome already wraps 2 lines at 390, and Tim's model-experimentation
    intent [[native model layer]] wants these accessible, so NOT expert-gated). Also flagged composition: registry human-NAMES for
    embedder layers (so "◫ default"/"pplx" read human) + the binding division-noun for the "in"-label — both registry-true, their lane.
  - **★ READINESS SIGNAL to lead:** projection's unblocked surface has CONVERGED (V placeholder, inspector, Source/relations, chip
    legibility). The remaining in-lane items are gated (drive=Tim's placement) or owned-elsewhere (real-V presentation, registry
    names). Signalled the lead I have capacity for the DECISION-SURFACE host/bridge-route the moment it's sequenced (HOST + /api/
    territory shipped + /api/cognition + recollection's "bridge route" memory leg) — rather than spin on polish.
  - **NEXT (mostly awaiting others):** decision-surface host/route (lead to sequence); DRIVE/source-on-sector (Tim's placement);
    composition's real-V touch-labels + registry human-names (layers, division-noun); TUTORIAL gated OQ3.
- **2026-06-18 — ✅ the LEAD SEQUENCED the decision-surface build (projection = stream #4, the HOST); I DESIGN-CONFIRMED projection's seam (no code beat this fire — honest: the foundation isn't there to build against yet).**
  The lead sequenced (thread g-1781731059): composition's 3 TYPES gate the rest (the long pole, starting now) → fork (scheme+keystone) →
  DNA (render) → **projection #4** → wildcard (take) → recollection (memory). The lead asked each stream to design-confirm its seam then
  build IN ORDER. So projection's correct move NOW = design-confirm (not a hollow build on not-yet-existing types — that would fail the
  real-data bar + jump the sequence).
  - **★ EVIDENCE-BACKED design-confirm:** PROBED `/api/territory` on a `decision://` address → degrades CLEAN (no crash; scheme
    unregistered → honest "no content-resolver yet" note; relations leg works). So projection's RESOLVE leg is decision-READY with ZERO
    net-new — the moment fork registers the decision:// scheme, /api/territory carries the full resolution. The resolution-first payoff:
    the same resolver/host serve the new type. projection's piece is NEARLY ZERO net-new (HOST + /api/territory already cover SHOW+RESOLVE).
  - **3 SEAM Qs broadcast (g-1781731457) to pin the small residual:** (1) fork — is the /api/cognition explain-wire me-routing-your-brain
    or you-provide-it? (2) recollection/fork — fold the recall into territory_for (→ /api/territory carries it, zero new route) or a
    recall:// scheme (→ projection routes it)? (3) DNA — decision-slide via renderGallery (existing host) or a new slide path (new mount)?
  - **HONEST — why no code commit this fire:** every projection net-new piece needs (a) composition's types (#1, gating), (b) a render
    consumer (DNA #3), or (c) a seam answer — building any now = hollow (no real data, nothing to drive) + out-of-order. The design-confirm
    IS the lead-requested productive step; I build the residual the instant the foundation + answers land. NOT spinning on polish, NOT
    hollow-building — waiting correctly on the sequenced foundation with my seam pinned.
  - **NEXT:** build projection's residual (explain-wire / memory-route / host-mount — whichever the seam answers leave) when composition's
    types + the 3 answers land; meanwhile the V/inspector/chip legibility is converged + the registry-name + Tim-placement items sit with their owners.
- **2026-06-18 — ✅ ALL 3 seam answers IN (projection's decision piece confirmed ~zero net-new); ✅ ran the FIRST task-scoped END-TO-END stranger test → found the #1 operator gap (DNA's drill-face), escalated. Honest: no projection code beat (the gap is DNA's RUNG-C; my data's shipped; the host's gated on types).**
  - **SEAM ANSWERS (thread g-1781731457) — all confirm projection ~ZERO net-new:** (1) explain-wire = REUSE fork's brain turn
    (/api/claude/turn / forkBrainCore.talk with the decision address — DNA's render calls it; projection adds nothing); (2) memory =
    FOLD recall into territory_for as a guarded leg (recollection drafts, fork applies) → /api/territory carries it FREE; (3) host-mount
    = REUSE GalleryMount + renderGallery (DNA resolves decision:// via my /api/territory, RUNG-C renders, mounts in the existing host,
    the slide-walk re-renders one container). I DECIDED the window-event = REUSE `gallery:rendered` (per lead's reuse steer) + owned a
    lifecycle note (decouple the decision-slide's close from wheel-deselect — I verify/harden that against DNA's REAL render, not a stub).
    So projection's decision residual is genuinely ~nil; I build/verify the instant composition's types + DNA's render land.
  - **★ TASK-SCOPED END-TO-END TEST (the advisor's reserve, warranted as the build-wait stretched — verify-by-use, not research):**
    drove the surface cold on a phone against a stranger's GOAL ("find what the company's been doing, understand one thing"), fresh-eyes
    judged the WHOLE journey (E1/E2/E3 screenshots). VERDICT: goal technically reachable, PRACTICALLY UNREACHABLE for a stranger:
    • E1 cold open — WORKS (the legend reads human; a stranger forms a plan: tap a dot).
    • ★ E2 tap-a-dot (the PRIMARY path) — BROKEN: DNA's gallery FACE; the panel titled "What this is" answers with GLYPHS (★✓✕?☆→),
      no words. ROOT-bar failure on the most common action. (The face I flagged ~5 fires ago — still unfixed.)
    • E3 V→"See the record" — WORKS (my legible Source) but "buried behind a mystery button most first-timers won't discover."
  - **★ THE FINDING (what the test was FOR — redirects the build):** the #1 operator gap is NOT my polished components — it's DNA's
    drill-FACE, and it's the SAME RUNG-C render gap the decision-surface needs (DNA's "build once, three payoffs"). So RUNG-C is doubly
    urgent: it fixes the #1 operator gap AND the decision-card. The data is READY (kind_name+kind_meaning on every point + /api/territory).
    projection's lane = ZERO net-new (data shipped; the face is DNA's render) → escalated the PRIORITY to lead+DNA (g-1781732544) + the
    naming-inconsistency (E2 thing-name vs E3 kind) to composition (one registry-declared legibility `name` both surfaces render).
  - **HONEST — no projection code commit this fire:** the #1 gap is DNA's render (not my lane); the decision host is gated on composition's
    types; the naming-name is composition's registry. Forcing a projection code change = hollow/wrong-lane. The test (verify-by-use) +
    the escalation IS the high-leverage work — it proved the next real beat is RUNG-C, not more projection polish. Sent Tim the journey.
  - **NEXT:** when RUNG-C lands (DNA), re-run the end-to-end stranger test to confirm the primary path now reads human; build projection's
    decision residual (likely just the gallery:rendered host + the lifecycle-decouple) the instant composition's types + DNA's render land.
- **2026-06-18 — ✅ CODE BEAT (unblocks the #1-gap fix): fed DNA's RUNG-C a meaning-SOURCE — kind_name/kind_meaning/summary now ride on the `projection:select` detail (commit `368e631`).**
  The lead sequenced RUNG-C (DNA's lane) as the leading edge + said "projection: hold, data's shipped." Before holding, I PROBED whether
  DNA's render-path actually RECEIVES the meaning — and found a real blocker in MY lane: it doesn't. Neither /api/territory nor
  /api/cognition/corpus carries kind_name/kind_meaning, and the projection:select detail (DNA's face open-trigger) carried only the machine
  `kind`. So "the data's shipped" was HALF-TRUE — the human meaning rode on the projection POINTS but never reached the face. RUNG-C
  literally couldn't render "What this is" in words without a meaning-source.
  - **FIX:** the `projection:select` detail now carries `kind_name` + `kind_meaning` + `summary` alongside the address/kind. DNA reads
    detail.kind_meaning (lead) + kind_name (heading) + summary → "What this is" in WORDS. No extra resolve, no new endpoint (the face already
    receives this event). Interim source (projection's kind-registry legibility); maps onto composition's canonical legibility name/is when
    that registry lands. VERIFIED BY USE: drove a real corpus-point pick → detail = {…, kind:'corpus.record', kind_name:'A note saved',
    kind_meaning:'The system wrote something into its memory.', summary}. Data-contract change (viewport-independent; no FORM/CSS — the
    operator-visible render is DNA's, fresh-eyes-judged when RUNG-C ships). tsc 0. Told DNA (thread t-1781733363).
  - **★ WHY THIS, not "hold":** the lead's "data's shipped" assessment missed that the meaning never reached DNA's render-path — a probe-by-
    use caught it. This is the opposite of filler-polish: it's the one piece of MY data-plumbing that genuinely gates the #1-operator-gap fix.
    Found by NOT taking "hold" at face value + verifying the actual seam.
  - **DECISION-SURFACE seams all settled:** Q1 explain = reuse fork's brain turn; Q2 memory = fold into territory_for (free); Q3 host = reuse
    GalleryMount + gallery:rendered (DNA confirmed the source-keyed lifecycle decouple — key on detail.source==='decision', deferred to the
    real render). projection's decision residual ~nil, gated on composition's types + DNA's render.
  - **NEXT:** DNA renders meaning-first off the new detail fields → re-run the end-to-end stranger test to confirm the primary path reads
    human; build the decision residual when types + render land; carry composition's canonical legibility name/is when its registry lands.
- **2026-06-18 — HOLD + ✅ verified meaning-source coverage complete + ★ NUDGED the critical path (RUNG-C shouldn't gate on Tim).**
  Probe-by-use: RUNG-C NOT landed yet (the face still shows glyphs, not the meaning-words — DNA building). Verified the meaning-source is
  COMPLETE: all 11 live kinds carry good kind_name+kind_meaning (not just the one I tested) → DNA's RUNG-C render has real meaning for every
  kind. projection's parts (host, /api/territory, the select-detail meaning-source, the window-event decision) are shipped + complete; the
  active build (RUNG-C) is DNA's.
  - **★ CRITICAL-PATH NUDGE (advisor-caught — the real action this fire):** DNA said it's "escalating to Tim NOW for the go" on RUNG-C, but
    the sprint is explicit — do NOT gate on Tim; git-revert is the safety. RUNG-C is reversible, the live #1-gap fix, AND the decision-surface
    gate; parking it on an absent "go" stalls the whole critical path. So I nudged DNA+lead (g-1781733996): RUNG-C is buildable NOW (revert
    covers the FORM), zero-cost status check. Keeping the critical path moving once my part's shipped = reactive coordination, not filler.
  - **WATCH (not a beat now):** `corpus.record` is >half the dots; my kind-meta renders "A note saved" — borderline for a comprehended
    role's-principles (name-vs-thing slippage). The per-instance name is composition's `name` lane; but when RUNG-C lands + I re-run the
    stranger test, if "A note saved" misreads the majority, tuning the kind-MEANING is mine.
  - **NEXT:** await DNA's RUNG-C (nudged) → re-run the end-to-end stranger test; build the decision residual when types+render land.
- **2026-06-18 — ★★ THE #1 OPERATOR GAP IS FIXED + VERIFIED LIVE. RUNG-C landed (the nudge worked); I synced it into the surface + re-ran the stranger test → the primary path reads HUMAN.**
  The nudge worked: the lead CONCURRED (RUNG-C builds now, no Tim-gate — "the exact over-gating Tim corrected"), and DNA SHIPPED the meaning-first
  "What this is" render (reads kind_name/kind_meaning/summary off the projection:select detail I ship, commit 368e631). My build-trigger fired:
  - **★ projection's host action (its lane): synced DNA's render into the surface.** My instrument serves window.DNA from a COPY at
    public/gallery/ (gitignored, regenerated by surface/app/scripts/sync-gallery.mjs predev/prebuild from DNA's counterpart/design/ui/). My
    dev server predated DNA's edit → the copy was stale (still glyphs). RAN the sync manually → pulled DNA's new unit-view.js (25231B) → reload.
  - **VERIFIED BY USE + FRESH-EYES re-test:** tap a dot → "What this is" now reads "A note saved" (title, retiring the jargon) + "The system
    wrote something into its memory." (meaning leads), glyphs after. Fresh-eyes verdict (same stranger goal): "the one thing that made this
    screen useless — no plain words — is GONE; the goal is reachable now; the #1 failure is RESOLVED, not partly." The E2 ROOT-bar failure my
    first end-to-end test found is CLOSED. BEFORE/AFTER sent to Tim.
  - **★ COMMIT-COORDINATION (raised to DNA+lead, g-1781734536):** public/gallery/ is GITIGNORED (regenerated every build from DNA's repo =
    registry-of-truth), so projection commits NOTHING here; DNA's render is in their WORKING TREE only → a clean/prod build would lose it.
    Per the sprint (commit + git-revert) I told DNA to COMMIT their source. (projection no code commit this fire — the synced copy regenerates;
    the meaning-source App.tsx was last fire's commit.)
  - **REMAINING (POLISH, relayed — none block the goal):** DNA — tuck the annotation glyph rows fully AFTER the words + fix a glyph row's "…"
    (reads like truncation); composition — the per-instance `name` + a content hint (the meaning's generic = the kind, the specific is behind
    "Open the source"). The corpus.record "A note saved" watch-item: fresh-eyes found it READS TRUE enough for orientation (not misleading) — no rename.
  - **NEXT:** the decision-card render (next RUNG, when composition's types land — same lyRender, projection's host + /api/territory + select-detail
    meaning-source all ready); then the lifecycle-decouple (source==='decision') when the real decision-slide exists.
- **2026-06-18 — ✅ #1-gap fix made DURABLE (DNA committed) + verified; HOLD on the next rung (decision-card, gated on composition's types).**
  Lead confirmed the milestone + made DNA's commit the durability gate. Checked: DNA committed RUNG-C (counterpart/design `ad8a043`), tree clean.
  Verified the surface sync reproduces it — re-ran sync-gallery.mjs from DNA's COMMITTED source → public/gallery/unit-view.js byte-IDENTICAL to
  the committed file → a fresh build/checkout regenerates the human-reading render (NOT stranded). So the #1-gap fix is durable end-to-end.
  Confirmed to lead (t-1781734850). Per my rule (unblocked beat → build / critical path stalled → nudge / neither → short hold): NEITHER —
  the next rung (decision-card) is composition's-types-gated, the lead's driving the long pole, and projection's pieces are all READY + verified.
  So I HOLD with everything staged; the types landing + fork registering decision:// triggers my piece (DNA's same lyRender renders the card
  through my host → I re-run the stranger test on a real pending decision). No projection blocker remains.
- **2026-06-18 — ✅ RE-AIM (③ "where can I go") stranger test → two projection-lane legibility fixes shipped: the legend tells the truth after Go-to (`515e9fe`) + the centre chip says what-it-is/how-to-exit (`3d4bac1`).**
  Held on the decision-card gate (composition's types, lead-driving) — so I ran the task-scoped stranger test on the UNTESTED axis ③ re-aim
  (Go to / re-centre), in projection's lane (not the known V-phone gap). It found two real gaps + confirmed one composition item:
  - **★ Beat A — the legend MISDESCRIBED the re-centred view (`515e9fe`):** after Go-to, radius_from flips to 'address'/'semantic' (radius =
    distance from the centred thing), but the Legend kept rendering the binding's static declared `meta.is` — the UN-centred default ("how far
    = how long ago, the centre is now"). So the legend actively lied about what the stranger was looking at. FIX: when a centre is set, use the
    centre-AWARE computed describe() (reads live radius_from + the centre), not the stale static meta. VERIFIED both viewports: re-centred →
    legend reads "radius · distance from voice.py"; un-centred → still the default meta. CentreChip + legend + inspector now all agree.
  - **★ Beat B — "I feel TRAPPED" (`3d4bac1`):** the fresh-eyes #1 follow-up — after re-centring, the only way back was a tiny unlabelled ×
    (the whole chip reset, but a stranger couldn't tell; tooltip was hover-only jargon "return to the root origin"). FIX: the chip now reads
    "⊙ Centred on <thing> · show all ×" (a "Centred on" state-prefix + a plain "show all" exit) + a plain tooltip/aria. VERIFIED both viewports:
    Go to → chip reads it; tapping → centre clears, legend back to default. The re-aim→undo loop is now stranger-legible. tsc 0, design-lint 0/0.
  - **FRESH-EYES (separate, phone): confirmed both fixes land** + flagged the remaining: (a) the centre LABEL is a FILENAME ("voice.py" + "1.00"
    + "structural" "reads as developer internals, breaks the live-map story") → the LABEL is composition's per-instance legibility `name` lane
    (projection renders the human name when present; corpus units carry none today → the leaf fallback) — flagged to composition (t-1781736317),
    same field-set RUNG-C proved; (b) "1.00"/"structural" in the inspector distance row are MINE → plain-word next; (c) no transition cue when the
    rings change meaning (legend now updates, but silent) — a secondary enhancement.
  - **NEXT (mine):** plain-word the inspector's "structural from X / 1.00" distance row; (composition) per-instance `name` closes the filename
    label; the decision-card render when composition's types land (all projection pieces staged + verified).
- **2026-06-18 — ✅ operator-law: the re-centred inspector distance row reads "filed · close by from X" not "structural … 1.00" (commit `d2d8d4c`).**
  Cleared my own queued NEXT (the re-aim test's mine-to-fix item). The decision-card is HARD-GATED: composition won't author the 3 types
  without Tim's EXPLICIT scope word (a 5-stream cascade on the flagship = principal-level confirm, not a routine GO; Tim away; the lead can
  only clear it WITH Tim's word). So I built the unblocked in-lane fix.
  - Traced the value (projection.py: address-centre r = tree_distance/max = normalized FILING-tree distance) → accurate plain words near/mid/far
    "in how it's filed" (NOT a mistranslation — filing-distance, mirroring the semantic row's close/mid/far). KEPT the real value in `num` (did
    NOT drop the number on one row only — respects Disclosure's "plain words + the REAL value" law + row consistency; the show-raw-nums-at-all
    question is a Disclosure-wide design call, not a piecemeal override). VERIFIED both viewports: "filed · close by from voice.py 0.20", no jargon.
  - **STATE:** the re-aim (③) journey axis is now fully stranger-legible end-to-end (legend-after-Go-to ✓, centre-chip-not-trapped ✓, distance-row
    plain ✓). The remaining re-aim item — the filename centre LABEL — is composition's per-instance `name` (gated with the types).
  - **NEXT:** decision-card render the instant composition's types land (Tim's scope-word clears it; all projection pieces staged); meanwhile the
    operator surface's three journey axes tested so far (understand-a-thing ①, controls ③-act, re-aim ③) all read human.
- **2026-06-18 — ✅ JOURNEY LEG 6 (coming back — time + state): the legend tells the time-truth in the past (commit `cfbda88`).**
  Decision-card still HARD-GATED on Tim's scope word (composition won't cross; lead can't clear without Tim; Tim away) → ran the stranger test
  on the next untested journey leg (6 — the time scrubber), projection's lane.
  - **FUNCTION verified first (no bug):** dragging back genuinely shows the past — `at=` filters future events (past view ts-range Jun7–Jun9 vs
    now Jun13–17; futureEventsInPastView=0; count 600→454). The mechanism is sound.
  - **★ THE GAP (legibility): nothing told the operator they were in the PAST.** Scrubbed to Jun 5, the legend still read "A live map … the
    centre is now" — actively claiming live/now while showing history (same misdescription class as the re-centred-legend bug). FIX (mirrors it):
    when s.at is set, bypass the static "live/now" meta → the computed describe() LEADS with "showing the company as of <human moment> — what had
    happened by then" + "radius · age (centre = that moment)". Human moment via toLocaleString (operator-law, never ISO).
  - **VERIFIED both viewports + FRESH-EYES:** live → "A live map …"; scrubbed → "showing the company as of Jun 9, 11:07 AM…" (no live/now claim);
    the "now" button returns to live. Fresh-eyes verdict: "the loop is complete and self-explaining — a stranger can time-travel back, knows
    they're in the past, and knows how to come home." Remaining = pure wording polish ("as of"→"rewound to"; "now"→"back to now") — captured, non-blocking.
  - **STATE:** FOUR journey axes now tested + stranger-legible — ① understand-a-thing, ③-act controls, ③ re-aim, ⑥ coming-back/time. Untested: 0
    (first-contact cold — likely strong, the legend reads well) + 2 (something-catches-the-eye, ≈ ①). The operator surface's core loop reads human.
  - **NEXT:** decision-card render when types land (staged); optional — the wording-polish pass + a leg-0/2 stranger test if the gate stays closed.
- **2026-06-18 — ✅ THE FULL COMPOSITE END-TO-END STRANGER JOURNEY — the four piecemeal fixes COHERE as ONE continuous, reversible experience (verification only; no code committed — correctly).**
  Decision-card still HARD-GATED (composition is the SOLE gate: won't author the 3 types without Tim's EXPLICIT scope word — a 5-stream cascade on the flagship = principal-level confirm; she put the scope-Q to Tim directly; Tim silent; the lead can relay-clear it the instant Tim says one line). Per the advisor: instead of testing the two remaining ISOLATED legs (0 cold, 2 catch-eye), ran the ONE composite run that subsumes them AND tests the untested part — the SEAMS between my four fixes (do they feel like one experience or four patches?). Drove all 6 moments live, in flow, at 390 then spot-checked the compound state at 1440.
  - **★ NEW THIS FIRE: RUNG-C is now LIVE on MY surface.** DNA's committed render (ad8a043) is synced into `public/gallery/` — so the composite run exercised the FIXED "What this is" for real: tap-a-dot → title **"A note saved"** + meaning **"The system wrote something into its memory."** — WORDS, the #1-gap fix rendering on the primary path through my host.
  - **THE COMPOSITE (all transitions cohered + reversed cleanly):** ① cold open reads human (legend: "A live map of everything the Company is doing… how far = how long ago, the centre is now"); the gold V is the catch-the-eye. → ② tap a dot → RUNG-C words (above). → ③ Go to (⊙ centre here) → ALL THREE re-aim fixes fire together: chip "⊙ Centred on <thing> · show all ×", legend "radius · distance from <thing>", inspector "filed · far from <thing> 0.97". → ④ rewind the time scrubber → COMPOUND STATE reads correctly: legend LEADS "showing the company as of Jun 8, 5:24 PM — what had happened by then" AND keeps "radius · distance from <thing>" (time-state + re-aim-state coexist coherently). → ⑤ "now" button → the as-of line cleanly removed, centre preserved. → ⑥ "show all ×" → full reset to the exact cold-open default (legend back to "how long ago", chip gone, scrubber live). **No stuck states, no stale descriptions — the legend/chip ALWAYS tell the truth about the actual state. The state machine (centre × time × selection) is sound.**
  - **VERIFIED BOTH VIEWPORTS.** 390: the full 6-moment drive (screenshots C1–C5). 1440: re-aim compound state (C6) — FORM adapts cleanly (chip→top bar, inspector→right rail, legend top-left, no overlap); findings identical (viewport-independent meaning-logic, as expected).
  - **HOLISTIC JUDGMENT on the small in-lane dings the composite surfaced (advisor: judge once, don't fix piecemeal):**
    - the naked value ("far from X · 0.97" / "long ago · 1.00") → **KEEP** — it's Disclosure's "plain words + the REAL value" law working, not a bug; the show-raw-nums-at-all question is a Disclosure-WIDE design call, not a solo-rushable piecemeal override.
    - the title "What's happening — the live activity" still says "live" while the body says "as of [past]" → small structural tension (the body clarifies; the title = the binding label). Captured, non-blocking.
    - the inspector's HOWTO context item leaks UI jargon ("the canvas — the live board the whole composition sits on… semantic zoom") → that's CORPUS CONTENT (real data), not my render to rewrite; humanizeCtx strips addresses, not data-jargon. Captured (it's a context-source flavour issue, possibly cross-lane), not a surface fix.
  - **★ ROUTED (cross-lane, with evidence):**
    - **DNA** — answered their EXPLICIT open question ("where do the glyph rows come from — V sockets or wildcard?"). Live-DOM evidence: it's DNA's OWN `div.annotation-strip` (`button.annotation-icon.favour-btn` ★ + `span.reaction-row` stamps ✓✕?☆→) attached to EVERY `.annotatable` node (`.ttl.annotatable` + the meaning line) INSIDE `.screen.uv` — NOT the V sockets, NOT wildcard. They render as prominent boxed buttons bracketing the meaning sentence (polish #1) and one reaction-row ends in "…" (polish #2). This unblocks DNA's polish pass (place glyphs AFTER the words).
    - **composition** — the centre LABEL is still the machine leaf (a `.py` filename, or a bare machine id like "judge_drift") AND the SAME thing reads "A note saved" in the sheet but "judge_drift" in the chip/legend (one-screen inconsistency). The advisor's clarity: do NOT solo-patch this with kind_name — a centre label must IDENTIFY the anchor ("distance from A note saved" names no specific point; every note is "A note saved"). That's exactly why composition reserved a per-INSTANCE `name`; she explicitly ruled the leaf "correct… today". Routed as a one-liner for HER call (interim generic floor vs hold for per-instance name); not re-decided solo.
  - **★ HONEST FLAGSHIP STATE:** this fire is journey VERIFICATION + cross-lane routing — **NOT decision-surface (flagship) progress.** The flagship is frozen on Tim's scope word; a run of green journey-checks must not read as the main build advancing. Phase 0 (the operator journey) is now verified end-to-end as ONE coherent experience; the flagship remains staged-and-waiting (all projection pieces ready + verified).
  - **NEXT (if the gate stays closed):** the untested DOING axis — the V's own legs **Ask** (brain panel) + **Note** (composer) end-to-end stranger test (the looking/navigating axes are now done); else the decision-card render the instant composition's types + fork's decision:// land.
- **2026-06-18 — ✅ DROVE the DOING axis (V's Ask + Note) as a stranger → shipped a real in-lane fix (the Note round-trip, commit `82a8eb3`) + found a serious Ask break (routed to fork with full diagnosis).**
  Flagship still HARD-GATED (composition = sole gate, awaiting Tim's scope word; no movement). Per the advisor: don't build onboarding signage (it pre-empts OQ3 + the RHM-as-guide architecture); instead drive the untested half of the journey — the V's own legs — and fix what surfaces. It surfaced real things on both legs.
  - **★ FIX SHIPPED (mine) — the NOTE round-trip was broken (write-only) → now closed.** Drove Note at 390: composer reads human ("Note about: this part of the surface" / "Leave a note about this…"), empty-guard works (Save disabled until text), Save → "Noted ✓" → auto-close. The note PERSISTED (suite.marks_for confirms). BUT `/api/territory` returned `notes: []` despite the mark existing → traced: `territory_for`'s `notes` field is a DIAGNOSTIC channel (leg-unresolved messages), NOT operator notes; the operator's comment/reaction/favour marks are read by a SEPARATE fn (`territory_directions_at` / suite.marks_for) that `/api/territory` never called. So a note was written but invisible — "See the record" could never show it back. FIX: `/api/territory` now folds in the operator's directions (operator-SAFE fields only — type/value/source/when; raw `target` address omitted, operator-law); `api.ts` types it; `SourcePanel` renders them under "The note you left here"; `App.tsx` populates them. VERIFIED BY USE BOTH viewports: wrote a note at a real thing (`…voice.py/principles`, "A note saved") → "See the record" → the note reads back in its own card, distinct from the system's "What's in it" content. Fresh-eyes critic PASS (note clearly theirs, no jargon/leaks, clean form) — its one nitpick ("Your note here" reads like a placeholder) FIXED → "The note you left here". tsc 0, design-lint 0/0. Files: `runtime/bridge.py`, `surface/app/src/lib/api.ts`, `surface/app/src/source/SourcePanel.tsx`, `surface/app/src/App.tsx`, `surface/app/src/source/source.css`.
  - **🔴 ROUTED TO FORK (Ask leg broken for a stranger — fork's brain lane; I traced the roots from the live `claude -p` command):** drove Ask cold ("What am I looking at? I've never seen this before"). The panel header reads human ("Ask about: this part of the surface", no leak) BUT the REPLY: *"Tim is pointing at a part of his interface — internal handle `ui://instrument/surface` — and asking what it is… Let me investigate the codebase."* Three real bugs: (1) **third-person voice about Tim** ("Tim is pointing… explain it to *him*") — root: `PANEL_BRIEFING` (ui_claude_session.py:42-53) refers to the operator as "he/him/Tim" throughout, so the model mirrors it; (2) **RAW ADDRESS LEAK** — "`ui://instrument/surface`" dumped into the reply despite the briefing forbidding it — root: `territory_prose` (territory.py:184) puts the raw address in the context block "for you, never shown" and the model parrots it; (3) **`--permission-mode plan` + thin surface-aim context → a slow codebase investigation that never streams a final answer** (two `claude -p` procs ran for minutes, no answer). Proposed to fork: rewrite the briefing to SECOND person + answer-from-provided-context-first; drop the raw address from the operator-context prose (the model can't echo what it isn't given). Fork's lane (brain UI + turn execution); the slow/hanging turn also blocks by-use verification of any briefing fix from my side.
  - **🟡 FOUND (addressless points degrade the V to the surface):** tapping a point with NO resolvable address (e.g. "A session went quiet" — agent_sessions; `address` & `source` both empty) dispatches `projection:select` detail=null → RightHand's `setAim` falls back to SURFACE_AIM → the V's Ask/Note/Source all silently aim at "this part of the surface", not the thing the operator tapped. So a note on such a thing lands on the surface, and Source rejects it (isThing=false). Points WITH an address aim correctly (verified). The fix is upstream: every point should carry SOME resolvable address (projection.py) — flagging to investigate next (mine), so the V's legs never silently mis-aim.
  - **NEXT:** investigate why some points lack a resolvable address (projection.py — so the V never silently mis-aims); the decision-card render the instant composition's types + fork's decision:// land (all projection pieces staged); fork to fix the Ask brain (routed).
- **2026-06-18 — ✅ NO-SILENT-FAILURE: the V is HONEST when a thing has no resolvable address (commit `b9ae3b5`); ⚠ found a separate context-leak + flagged the reorg sync-seam.**
  Flagship still HARD-GATED (composition = sole gate, no Tim word). Took my queued NEXT — the addressless-points bug — and (per the advisor) built the HONEST half in my lane rather than fabricating addresses.
  - **★ SCALE (quantified):** 1796 of 6448 points (~28%) carry NO resolvable address — they're activity EVENTS (op.run 660, cognition.items 201, agent_sessions.* 372, create/run/warning/connect/…). corpus.record (the saved notes, 3220) all HAVE addresses. Root: projection.py:882 `address = e.get("address") or e.get("source_address") or ""` — events are recorded without either.
  - **★ THE BUG (no-silent-failure):** an addressless selection made `projection:select` fire detail=null — INDISTINGUISHABLE from a deselect — so the V (RightHand.setAim) silently collapsed its aim to the surface. The operator taps an event, opens Note, and the comment was written to the WHOLE surface (a silent durable mis-write); navigate/open-source showed a misleading "tap a dot first" (they DID).
  - **★ WHY NOT fabricate a `ui://canvas/seq-N` address (the tempting fix I rejected — advisor):** `seq` is an ENUMERATION INDEX (bridge.py:530 `"seq": i`), stable today only by append-order happenstance — NOT a stored, reconcilable event identity. Writing durable operator notes to it risks corruption (re-attaches to a different event under re-order/compaction) AND is a parallel addressing scheme that won't rejoin fork's real event-addressing (island-won't-rejoin-mainland). So: NO fabricated write-target.
  - **★ THE FIX (mine, honest):** `projection:select` now keys on the UNIT (seq) and fires a DISTINCT detail for addressless selections (`address:''` + the unit's meaning) so the V can be HONEST instead of silently mis-aiming. RightHand: `aimAvailable` state — an addressless thing names itself + the Note composer shows "This is something the Company did — an activity event, not a saved record. You can't note or open it yet; that's coming once these can be opened" (NEVER the silent surface write). App.tsx: navigate/open-source give an honest event-naming Notice, not "tap a dot first". SAFE: GalleryMount + DNA's unit-view render BOTH gate on a non-empty `d.address` (`if (!d || !d.address)`), so `address:''` is a no-op for them (verified by reading both) — only the V reads the meaning-fields. The REAL fix (events becoming addressable) is fork's, already routed.
  - **VERIFIED BY USE 390 + 1440:** addressless ("A session joined"/agent_sessions) → V→Note = honest card (no textarea/no surface-write), V→See-the-record = honest Notice; ADDRESSED ("A note saved") → V→Note = working composer (textarea+save) — NO REGRESSION. Fresh-eyes critic PASS (stranger understands it's a known limitation, not a bug; flagged "openable"→"can be opened", FIXED). tsc 0, design-lint 0/0.
  - **⚠ NEW FINDING (mine, next — operator-law context leak):** some points' Disclosure CONTEXT items leak raw machine ids/paths (saw `routine:self_status · a7dde1d0 @ /home/tim/company` rendered to the operator). `humanizeCtx` (Disclosure.tsx) strips `scheme://addr` but NOT `routine:X · <hash> @ /abs/path` formats. Intermittent (data-dependent), pre-existing. Investigate + tighten humanizeCtx next.
  - **⚠ FLAGGED (reorg sync-seam, to DNA/lead, thread g-1781696738):** the live repo reorg's coming `ui/→surface/` slice will move the files my `sync-gallery.mjs` copies from (`counterpart/design/ui/{organisms,unit-view,phone}`) → RUNG-C + the gallery face silently vanish from my surface on the next sync. Asked DNA to ping the NEW paths when the move lands so I update the sync in lockstep.
  - **NEXT:** tighten `humanizeCtx` for the leaked context format (operator-law); update `sync-gallery.mjs` when DNA's ui/→surface/ lands; the decision-card render when composition's types + fork's decision:// land (staged).
- **2026-06-18 — ✅ Ask leg CONFIRMED human end-to-end (fork's fix, my surface) + ✅ operator-law context-leak fixed (commit `c44b80c`); flagship gate CLEARING (composition's types landed).**
  ★ THE FLAGSHIP IS UNFREEZING: composition LANDED the decision-surface type contract (23524ad — decision/option/decision-card/legibility schemas); lead triggered fork (wiring decision://) + DNA (card render, after the reorg ui/→surface slice). My host is render-AGNOSTIC + degrade-clean-ready: probed `/api/territory?address=decision://…` → degrades clean (relations leg ok, identity absent, "no content-resolver yet") — so the moment fork lands the resolver + DNA renders the slide, my GalleryMount hosts it + I run the prove-on-one stranger test. No projection build possible yet (gated on fork's resolver + DNA's post-reorg render); host stands ready.
  - **✅ ASK LEG FIXED (fork b4f6269+0f2e64a) — CONFIRMED on MY surface (the last mile fork asked for).** Restarted my bridge to load fork's brain rewrite, re-ran the 390 stranger test cold ("What am I looking at? I've never seen this before."): reply in ~10s — *"You're looking at a piece of your own instrument surface… I've only got a thin label for this exact spot ('this part of the surface')… Want me to dig in…?"* All three prior bugs GONE: 2nd-person (no "Tim"/3rd-person), ZERO address leak, answers-briefly-then-offers (no hang, 0 unprompted tools). The stranger-Ask arc I opened is CLOSED. Reported to fork. (Deeper-half still open: the THIN surface aim — the brain quotes "this part of the surface". Proposed to fork: resolve ui://instrument/surface to the EXISTING registered legend meaning [binding.meta], not the generic scheme-noun — registry-is-truth reuse, no OQ3 pre-empt; asked whether that resolution is fork's [territory_label/prose] or mine [feed lens-meaning from the surface]. Awaiting.)
  - **✅ OPERATOR-LAW context-leak FIXED (commit `c44b80c`, mine).** Last fire I saw the Disclosure inspector render `routine:self_status · a7dde1d0 @ /home/tim/company` (a raw filesystem path + hash + machine id) to the operator. Root: the context items legitimately carry brain-facing machine provenance, and `humanizeCtx` stripped `scheme://` addresses but NOT abs-paths / `· <hash> @ /path` tails / `name:machine_id` tokens. The render-boundary strip is the ARCHITECTED guard (the underlying text KEEPS the raw form for the brain — fixing at the source would starve it), so extending humanizeCtx is in-pattern, my lane. FALSIFY-FIRST verified (node, old-vs-new): the exact real leaked string → "" while prose is untouched (colons "Note: see…", times "3:30pm", em-dashes, the canvas howto all unchanged — no false strips); by-use no-regression confirmed (normal context renders identical). tsc 0. (The live leak is intermittent — rare point kinds — so verification is the execution test on the real input [a pure render transform] + the no-regression drive, not a staged live repro; honest about that.)
  - **NEXT:** update `sync-gallery.mjs` the instant DNA posts the ui/→surface/ paths (lockstep — else RUNG-C silently 404s); the decision-card host prove-on-one the moment fork's decision:// + DNA's slide land (host ready); surface-identity reuse pending fork's lane-answer.
- **2026-06-18 — ✅ SYNC-SEAM HARDENED: the gallery sync resolves DNA's face across BOTH homes (ui/ + surface/runtime/) → the reorg move-race is DISSOLVED (commit `f84cb6e`).**
  Flagship still HARD-GATED (composition = sole gate; no Tim scope word). Took the queued lockstep item — but went one better than "wait for DNA's ping": I read the TARGET paths from DNA's OWN `counterpart/design/docs/dev/REORGANISATION-PROPOSAL.md` (the target tree puts `organisms.js`/`unit-view.js`/`phone.css`/`piece.css` in `surface/runtime/`) and made the sync resolve them proactively, so the move can land on ANY beat with no paired edit + no silent-stale.
  - **★ FALSIFY-FIRST corrected my own prior flag.** Last fire I called the risk a "silent 404." Reading the WHOLE script first: it ALREADY fails loud (`process.exit(1)`) when a source is missing at the hardcoded `ui/`. The REAL failure modes are (a) the instant DNA moves `ui/→surface/runtime/`, the hardcoded path stops resolving → `predev` dies; (b) a LONG-RUNNING dev server keeps serving the OLD `public/gallery/` copy (sync only runs at predev/prebuild, not on HMR) → silent-stale. Confirmed (b) is REAL: this fire's first sync REFRESHED a stale copy — DNA had edited `ui/{organisms,phone,unit-view}.js` today (mtimes Jun 18) and the served bytes were behind (checksums changed on re-sync).
  - **★ THE FIX (mine):** per-file resolution across a PRIORITISED home list — `DNA_UI_DIR` override → `<repo>/surface/runtime/` (the reorg target, tried FIRST) → `<repo>/ui/` (today's home, fallback) — and FAIL LOUD listing every searched home if a file is in none. `<repo>` = `DNA_REPO_DIR` (defaults `/home/tim/repos/counterpart/design`). The per-file log prints the ACTUAL resolved path, so WHICH home a file came from is visible, not silent. piece.css uses the same resolver (it moves too). The company-owned fabric hooks (`build-prep/front-interface/`) are a separate registry-of-truth, untouched by DNA's reorg — left as-is.
  - **VERIFIED BY USE (3 tests + live drive both viewports):** Test A (today, no overrides → resolves from `ui/`, exit 0, copy byte-identical to DNA's source); Test B (reorg-landed sim: `DNA_REPO_DIR` → a repo with only `surface/runtime/` → resolves from there FIRST, exit 0 — proves the move can land); Test C (neither home → exit 1, prints both searched homes — proves no silent-stale). Then RUNG-C DRIVEN LIVE from the freshly-synced source: 390 (tap-a-dot → DNA's unit-view face renders "A piece of common knowledge / What this is / A note the Company filed for itself / Open the source") + 1440 (same, FORM adapts). curl: all `/gallery/*` served HTTP 200.
  - **design-lint:** N/A this fire — the change is a `.mjs` build script, not CSS (design-lint is CSS-only). Stated, not skipped silently.
  - **★ HONEST framing:** this is INFRASTRUCTURE/COORDINATION hardening (the sync seam) — NOT decision-surface (flagship) progress, and NOT a projection FEATURE. Its value: it removes me as a GATE on DNA's reorg (she can land ui/→surface/ without a paired edit here or a race), and it fixed a real silent-stale path. The flagship remains staged-and-waiting on composition's types + fork's decision:// + DNA's slide render.
  - **★ ROUTED (DNA + lead, thread `g-1781696738`):** "sync resolves DNA's face from BOTH `ui/` and `surface/runtime/` + fails loud — land ui/→surface/ anytime, no lockstep race." Re-flagged the cross-lane FORM finding already raised on the composite fire: DNA's in-card `div.annotation-strip` glyph rows (★✓✕?☆→) read as cryptic unlabeled buttons bracketing the meaning sentence to a fresh-eyes operator (DNA's polish lane — place glyphs after the words / label them).
  - **NEXT:** the decision-card host prove-on-one the moment fork's decision:// + DNA's slide land (all projection pieces staged + render-agnostic); surface-identity reuse pending fork's lane-answer; the lockstep item is now SUPERSEDED (the sync no longer needs a paired edit when the move lands).
- **2026-06-18 — ✅ FLAGSHIP, ACTED ON: the decision-slide HOST SEAM built + verified (commit `e08a70a`) — the projection net-new for Q3, against the CONFIRMED contract.**
  ★ Advisor pushback (correct): I'd been doing fire-after-fire of adjacent infra/green-checks while PRESUMING the flagship gate shut. VERIFIED the gate by evidence instead of assumption: composition's TYPES landed (23524ad — the long pole); **all 3 seam Qs ANSWERED** on thread `g-1781731457` (Q1 explain-wire = reuse `/api/claude/turn`/`forkBrainCore.talk`, zero net-new — fork; Q2 memory = fold into `territory_for` as a guarded leg, my route FREE — recollection+fork+lead; Q3 host-mount = reuse `renderGallery`+GalleryMount, DNA deferred the window-event KEY to me). **fork's `decision://` is NOT live yet** — probed `/api/territory?address=decision://test/probe` → `scheme:None`, empty (fork gated on recollection's recall callable + heads-down on #71). So the FULL prove-on-one still waits on fork+DNA — BUT Q3 opened a concrete projection build in my lane NOW.
  - **★ THE BUILD (Q3, mine):** chose a `decision:rendered` SIBLING event (not `gallery:rendered` reuse) because a decision walk is a SEQUENCE in one container (DNA's contract) — NOT tied to a wheel point; reusing `gallery:rendered` would let my `onSelect` close the face on a wheel-DESELECT and kill the walk mid-decision. GalleryMount now: opens+HOLDS on `decision:rendered`, IGNORES wheel-deselect while a slide is up (decision-mode), closes on dismiss/Esc; a normal corpus drill clears decision-mode (modes mutually exclusive — neither leaks).
  - **VERIFIED BY USE BOTH viewports (390+1440):** real DOM-event lifecycle drive — rest=closed → decision:rendered=open → wheel-deselect=STILL open (the decouple) → advance-walk=open → Esc=closed → corpus gallery:rendered=open → corpus+wheel-deselect=closed (no leak). A real state-machine drive, not "code looks right." tsc 0. design-lint N/A (TSX, no CSS). **HONEST:** this is the host SEAM only — the decision CONTENT is gated on fork's `decision://` + DNA's slide render; a fresh-eyes stranger ROOT test applies when that real content exists. Delivered the decided key + verified behavior to DNA (thread `g-1781731457`) so her render matches it.
  - **★ REFRAME (per advisor): the SYNC-SEAM beat (f84cb6e) is ON the flagship critical path, not mere infra.** DNA's decision-card RUNG-C render is gated on her `ui/→surface/` reorg (log line ~709); by removing projection as a lockstep gate on that move, the sync hardening unblocks: DNA's reorg → DNA's slide render → projection's prove-on-one. It reads as infra but it's a domino on the flagship chain.
  - **★ VERIFIED GATE STATUS (so the next fire acts on FACT):** composition types ✅ LANDED · seams ✅ ALL ANSWERED · fork `decision://` 🔴 NOT live (probe=scheme:None; fork on #71 + gated on recollection's recall callable) · DNA card-render 🔴 pending (mid-reorg; lead told her to fit it within her repo slices) · projection host ✅ READY+verified (GalleryMount corpus + decision-slide seam both live; /api/territory degrades clean on decision://; gallery:verb TAKE-path live). **Projection's ONLY remaining flagship net-new = the Q1 explain-wire (route to `/api/claude/turn`/`forkBrainCore.talk` for the card's explanation slot) — buildable the moment a real `decision://` resolves (needs real content to verify by-use).**
  - **NEXT (fact-based):** (a) the moment fork's `decision://` probe returns a real resolve → build+verify the Q1 explain-wire (the last projection net-new) + run the prove-on-one stranger test; (b) until then, projection's flagship host is COMPLETE+verified — further flagship progress is fork's/DNA's, so any non-flagship beat is a deliberate fill, said so honestly; (c) surface-identity reuse still pending fork's lane-answer.
- **2026-06-18 — ✅ poked the real gate (cheapest flagship unblock) + ONE honest in-lane increment: the default lens is now fully legible (commit `53a7561`).**
  Per the advisor's durable rule for gated fires (recorded at the bottom of this entry): (1) probed `decision://` → STILL not live (scheme=None); (2) DNA CONFIRMED + banked my `decision:rendered` contract (her render lands post-reorg) — so the host seam is fully settled both sides; (3) did the real gate-attack, then exactly ONE in-lane increment.
  - **★ GATE-ATTACK (the highest-value move — attack the gate, don't build around it):** the flagship waits on fork's `decision://`. fork bundled "decision:// + the recall-leg" as gated on composition's types AND recollection's recall callable — but by fork's OWN framing those are SEPARABLE (the recall leg is additive + degrade-clean, "no recall = empty leg, never a crash"; the resolver needs only composition's types, which LANDED). Sent fork ONE question (cc lead, thread `g-1781731457`): can you register `decision://` NOW and fold the recall leg in later? If yes it cascades immediately (DNA's render, recollection's leg, my prove-on-one all proceed). Framed as a question, not a push (fork may be on #71 by choice → lead's sequencing call). Sent once; will NOT re-nudge.
  - **★ THE INCREMENT (legibility-completeness, falsify-first):** drove every lens the operator can select and checked sectors+placement for jargon (NOT assumed). Found REAL gaps: the DEFAULT lens ("What's happening", raw) + time-of-day (shared kind sectors) had **29 of 51 kinds with no crafted meaning** — a stranger tapping "Cognition Turn Done"/"Agent Sessions Render Drop" got a mechanical titlecased id, no explanation. Completed the kinds-registry (`kinds/raw.py`): **27 grounded** name+meaning pairs, each from that kind's own `_emit` message (the system's own words → operator language). Two live kinds (`config_writer.git`, `projection.verify`) LEFT to the humanized fallback — no emit-site exists to ground them (config_writer was removed); fabricating would break the evidence rule (honest omission, not a miss).
  - **FRESH-EYES copy critic (separate agent, TEXT-judge):** 9/27 clear, 18 flagged — insider words (turn/wave/activated/context/flow/trial/debrief/surface/minds) + the journey.* trio implying a USER tour vs the system RECORDING a path. Refined all to plain English, grounded meaning preserved ("AI turn started"→"The AI started thinking"; "A wave of AI steps"→"Several AI steps at once"; "A walk-through started"→"Path recording started"; "A trial turn"→"A test-run exchange"; …).
  - **VERIFIED BY USE BOTH viewports:** curl 29→2 no-meaning (the 2 = the intentional fallbacks); drove REAL dots after scrubbing time back to where these kinds live (they're absent from the recent-600 default view) — 390: `decision.intent`→"A build proposed" (the live event summary "build-intent surfaced … awaiting operator approval" CONFIRMS the grounding is faithful), `cognition.wave`→"Several AI steps at once"; 1440: `cognition.wave`→human, FORM adapts (sheet→right rail, no overlap). The time-travel legend ("showing the company as of Jun 5…") rode along correctly. design-lint N/A (Python registry, no CSS); registry-true (instrument stays empty of meaning).
  - **★ REMAINING legibility gaps (FOUND, queued — honest, NOT silently skipped):** `by_cascade` (Flow) 14 step-kinds no meaning; `grouped` (Activity families) 7 no meaning; `by_lens` (Ways of looking) 9 lens-sectors no meaning (can REUSE the bindings' own meta — registry-true, near-zero new copy); `by_nucleation` (New kinds forming) shows raw "✦0..✦3" (TRUE jargon — but these are EMERGENT cluster ids, likely not statically seedable — needs a derive-from-exemplar treatment, investigate before seeding). These are operator-surface BREADTH, not flagship — each a future ONE-increment.
  - **★ DURABLE RULE for gated fires (advisor, so I stop re-deciding every fire):** while the flagship waits on fork+DNA — (1) probe `decision://`; if LIVE, drop everything → build+verify the explain-wire → run the prove-on-one (the real beat); (2) if not live, do AT MOST ONE in-lane increment (a remaining legibility gap above, or an observed FORM gap) then stop; (3) don't re-litigate beat-selection or re-route settled seams (the `decision:rendered` seam + the 3 Q-answers are SETTLED).
- **2026-06-18 — ★ THE GATE OPENED: `decision://` is LIVE (fork 97be816, the gate-attack worked) → decision RESOLVE + EXPLAIN both LIVE-VERIFIED on the real decision; no projection code to write (correctly).**
  My gate-attack question last fire landed: fork did exactly the decoupling I proposed (resolver needs only composition's types; the recall leg folds in later, degrade-clean) and committed `decision://` to main — scheme + parse_decision_address + a file-discovered DecisionRegistry + a verified bootstrap row (`decisions/merge-sa-authorize.py`) + state-composed-from-the-`decision_take`-mark + a human render. Durable-rule step (1) fired: probe → LIVE (`scheme=decision`, was None) → pursued the flagship.
  - **✅ RESOLVE live-verified (fork's t-1781741535 ask — converts their unit-verified → live-verified):** `/api/territory?address=decision://global/merge-sa-authorize` → `decision.record`; meaning "Should the Company get its own private way to save designs back…"; 2 options ("Give it save-back access" recommended, "Stay read-only for now"); `state=pending`; `explanation_source=board://item-c0a2d591`; legibility {name,is,why}. Exactly fork's predicted shape — projection's SHOW+RESOLVE proven on the real first decision. Reported to fork (lights the cascade).
  - **✅ EXPLAIN leg DE-RISKED (the prove-on-one's riskiest un-gated link — the exact failure class that bit the V's Ask: jargon/leak/hang/3rd-person):** drove `forkBrainCore.talk(el, "decision://global/merge-sa-authorize", explainPrompt)` on my LIVE surface — the EXACT module DNA's card slot will call. Output: genuinely operator-grade — plain "what you're deciding", both options, the recommended one WITH reasoning ("it closes the loop… scoped to its own entries and reversible"), ends with a plain choice offer. ZERO address leak (no decision://, no board://, no machine ids), 2nd-person, ~10s no hang. `explanation_source` board://item-c0a2d591 resolves clean + carries its 3155-char body for the brain to ground on. (Content de-risk = viewport-independent; the both-viewport VISUAL verification belongs to DNA's card + the full prove-on-one when they land.)
  - **★ EXPLAIN-OWNERSHIP seam RESOLVED by re-reading (advisor caught a real contract divergence):** fork said "projection does NOT need an explain-route — the card's explanation slot reuses forkBrainCore.talk"; the lead had said "projection's route just ROUTES to it." fork's lane + zero-net-new path WINS → **DNA's card calls fork's hosted `talk()` directly; projection builds NO explain route.** Building the route the lead floated would be a parallel wire — NOT built. So projection's flagship pieces are COMPLETE (host + decision:rendered seam + /api/territory resolve + gallery:verb dispatch), now live-verified end-to-end except the two genuinely-other-lane pieces below.
  - **★ HONEST fire outcome (no code commit — doc only):** this fire is LIVE-VERIFICATION + DE-RISK + report, not a build — because the gate opened to reveal projection already holds the pieces. Manufacturing code to "have a commit" would be the trap (advisor). Legitimate BIG-beat given the gate.
  - **REMAINING for the full prove-on-one (NOT projection's lane):** (a) DNA's visual decision-card render (post-reorg ui/→surface/; renders via window.DNA.renderGallery + fires `decision:rendered` → my host catches it, verified); (b) wildcard's TAKE (tap an option → append a `decision_take` mark). ★ SEAM I'm watching (fork's silent-miss flag): the take's aim_address MUST be the BARE canonical `decision://global/<id>` (NOT `#elem`), value=option LABEL, mark_type=`decision_take` (underscore). My gallery:verb dispatch passes aim_address straight through, so whoever emits the take sets it bare — recorded, not built.
  - **NEXT (fact-based, durable-rule):** when DNA's card + wildcard's take land → host + run the FULL prove-on-one stranger test (slide renders → explanation slot fills via fork's talk → tap an option → state flips pending→decided → re-render). recollection's recall leg (callable `recall_for_decision` confirmed, ready-to-paste at episodic-memory-adaptation/loop-prep/MEMORY-LEG-territory-draft.md) enriches the explanation when fork folds it — NOT on the critical path. Meanwhile if a fire finds the gate's downstream still pending: ONE legibility increment (Flow 14 / Activity-families 7 / Ways-of-looking 9 reuse / New-kinds ✦N derive) per the durable rule.
- **2026-06-18 — ✅ DNA's ui/→surface/ REORG LANDED — my hardened gallery sync ABSORBED it with no break (commit `a51067b`); the lockstep race never materialised.**
  Probe (durable-rule step 1): `decision://` still live, state=pending (no take yet — wildcard's piece + DNA's card still pending, so the FULL prove-on-one is still gated on them; no projection prove-on-one work possible this fire). But the reorg LANDED (ui/ gone; surface/ exists) → the genuinely-actionable beat.
  - **★ THE HARDENING PAID OFF (no-silent-failure caught a real breakage):** the move SPLIT by file-type — organisms.js/unit-view.js → `surface/runtime/`, phone.css/piece.css → `surface/styles/` (the pre-reorg proposal had them all in runtime/; the actual move separated styles). The per-file resolver (f84cb6e) found the JS in runtime/ and **FAILED LOUD on phone.css** — NOT silently serving the stale pre-reorg public/gallery/ copy (which the running dev server WAS still serving — exactly the silent-stale path the hardening guards). So the reorg was caught instantly + cleanly, not as a mystery-broken gallery.
  - **★ THE FIX (additive, the design held):** added `surface/styles/` to the candidate homes; per-file resolution now picks JS from runtime/, CSS from styles/, ui/ kept as a fallback. No redesign — the lockstep-dissolved architecture absorbed a SPLIT move it wasn't even specifically built for, because per-file + fail-loud is general.
  - **VERIFIED BY USE both viewports:** sync exit 0 (organisms/unit-view ← runtime/, phone.css + piece.css→dna-tokens ← styles/); public/gallery/ refreshed to match new-home sources (was stale); RUNG-C driven live 390+1440 → the gallery face renders human from the fresh source ("A note saved / What this is / The system wrote something into its memory / Open the source"). design-lint N/A (.mjs). Reported to DNA/lead (thread g-1781696738); told DNA no need to lockstep-ping future style shifts (resolver searches both + fails loud).
  - **NEXT:** unchanged — the FULL prove-on-one the instant DNA's decision-card render + wildcard's take land (host + all legs ready + live-verified); else ONE legibility increment per the durable rule (Flow 14 / Activity-families 7 / Ways-of-looking 9-reuse / New-kinds ✦N-derive).
- **2026-06-18 — ✅ durable-rule increment: the ACTIVITY lens (grouped) reads fully human — the 7 families legible (commit `82c1667`).**
  Probe (step 1): `decision://` still pending, DNA's decision-card render not landed, no `decision_take` written → FULL prove-on-one still gated on DNA+wildcard (no projection prove-on-one work). So one in-lane increment.
  - **Picked the best-GROUNDED gap (falsify-first corrected my first pick):** I'd assumed "Ways of looking" (by_lens) was a clean "reuse the bindings' meta" — WRONG: its angle_from is `projections`, sectors are corpus SPACES (history/repo/principles/worldview/…+a "—" remainder), needing their OWN grounded registry. The genuinely-grounded gap was `grouped` ("Activity — grouped into families"): all 7 family sectors had no meaning, and grouped.py's `groups` map DEFINES exactly what each family gathers → I could write each meaning from evidence, not invent it.
  - **THE BUILD:** added `GROUP_META {id:{name,is}}` for the 7 families (memory/conversation/making/operations/signals/decisions/field), GROUNDED in each family's glob set, kept IN bindings/grouped.py beside the `groups` (so a new family + its meaning land together — no drift); wired the `kind-group` sector domain into projection.py's `_SECTOR_META_BY_DOMAIN` (same legibility-type, OWN registry — mirrors kinds/raw.py + nodes/_meta.py). Operator language, never the machine kind-ids inside.
  - **VERIFIED BY USE both viewports:** curl 7→0 no-meaning; DROVE the lens switch on the surface (390+1440) — opened the lens chip → "Activity" → tapped families → the wheel readout shows the human name+meaning ("Memory / Things the system saved into its memory — notes and stored content."; "Making / Things being made, changed, moved, wired together, or removed."). Fresh-eyes copy critic: 7/7 CLEAR, no leaked jargon; its one soft note (Operations' bolted-on tail) addressed ("The system's work running — jobs, runs, flows, and the AI's thinking steps."). design-lint N/A (Python). Registry-true; instrument empty of meaning. TENTATIVE copy (Tim/DNA ratify).
  - **★ REMAINING legibility gaps (updated):** `by_cascade` (Flow) 14 step-kinds; `by_lens` (Ways of looking) the 8 corpus SPACES + the "—" remainder (needs a `projections`-domain registry — ground each space first; the "—" is the no-lens remainder bucket); `by_nucleation` (New kinds forming) ✦0..✦3 emergent cluster ids (derive-from-exemplar, not static seed). Each a future ONE-increment per the durable rule.
  - **NEXT:** unchanged — FULL prove-on-one the instant DNA's card + wildcard's take land; else the next ONE legibility increment (Flow 14, or the projection-spaces registry for Ways-of-looking).
- **2026-06-18 — ✅ durable-rule increment: the WAYS-OF-LOOKING lens (by_lens) reads fully human — the corpus SPACES legible (commit `7acaece`); + root-caused a SILENT import collision.**
  Probe (step 1): decision still pending, DNA's card render not landed, no take → prove-on-one still gated. One in-lane increment: the lens I owed a correct treatment (last fire I'd mis-picked it assuming a clean reuse).
  - **THE GAP:** "Ways of looking" (binding=by_lens, angle_from=projections) — its 9 sectors are the corpus SPACES (history/repo/principles/topics/worldview/common_knowledge/operators/what/claimed_status + the "—" remainder), ALL with no meaning. A stranger switching to it saw humanized ids, no explanation.
  - **THE BUILD:** added `PROJECTION_SPACE_META {id:{name,is}}` GROUNDED in each space's own `desc` in projections/<id>.py (the registry's own definition), translated to operator language (operator-law — never "lens/embed/vector space"). Renamed for operator-clarity: `operators`→"Roles" (a sector literally "Operators" reads to a human operator as about THEM), `repo`→"Code", `—`→"Unfiled". Wired the "projections" sector domain into projection.py.
  - **★ NO-SILENT-FAILURE root-cause (the real lesson this fire):** first put the map in projections/_meta.py — and the lens STILL read blank after restart. The defensive `try/except` import was swallowing a real failure: the bridge has runtime/ on sys.path, where `import projections` resolves to **runtime/projections.py** (the ProjectionRegistry MODULE), NOT the projections/ directory — so `from projections._meta import` raised, caught → empty map → no meaning. Confirmed by-test (runtime/ first on path → projections→runtime/projections.py, "not a package"). FIX: keep the map in bindings/by_lens.py (beside the binding that renders it — mirrors GROUP_META in grouped.py); bindings/ has no colliding runtime module. Deleted the dead _meta.py. (The defensive except is right for resilience but it hid this — verifying BY USE, not by "import looks fine", caught it.)
  - **VERIFIED BY USE both viewports:** curl 9→0 no-meaning; switched to Ways of looking (390+1440), tapped spaces → readout reads human ("Common knowledge / What the Company has come to understand…"; "Roles / What each of the system's built-in roles does."; "Worldview / The stances and values something takes for granted…"). Fresh-eyes copy critic: 10/10 clear; its one borderline (history's "sessions/conversation record") tightened to "past conversations"; noted two inherent-domain confusion pairs (Common-knowledge/History, Principles/Worldview) — the meanings disambiguate, left as-is (tentative; Tim/DNA ratify). design-lint N/A (Python).
  - **★ REMAINING legibility gaps (updated):** `by_cascade` (Flow) 14 step-kinds; `by_nucleation` ✦0..✦3 emergent (derive-from-exemplar); the `lineage` space (no clear desc to ground — left to fallback, set-diff flags it). Each a future ONE-increment.
  - **NEXT:** unchanged — FULL prove-on-one the instant DNA's card + wildcard's take land; else the next ONE legibility increment (Flow 14, or ground lineage/✦N).
- **2026-06-18 — ✅ durable-rule increment: the FLOW lens (by_cascade) reads fully human — the 14 STEPS legible (commit `ddab20b`). Lens-legibility set now ~COMPLETE.**
  Probe (step 1): decision still pending, DNA's card render not landed, no take → prove-on-one still gated. One in-lane increment: the last substantial lens gap, Flow.
  - **THE GAP:** "Flow — the steps, in the order they run" (binding=by_cascade, angle_from=cascade-flow, whole_set) — its 14 sectors are the system's STEPS (cognition roles + op-verbs) that saved flows reference, ALL with no meaning. A stranger switching to Flow saw humanized ids (Confirm Registration / Decompose Seed / Triad Synth…), no explanation.
  - **THE BUILD:** added `CASCADE_FLOW_META {id:{name,is}}` for the 14 steps, GROUNDED in each role's own `ROLE["description"]` in roles/<id>.py (registry-true; op-verbs from the reduce/retrieve op semantics), translated to operator language — never the chain jargon (RG6 / COMPOSITIONS ⑦⑩ / MAP-REDUCE / criteria-group). Kept in bindings/by_cascade.py beside its binding (applied the projections/_meta.py collision lesson — the import worked first try). Wired the "cascade-flow" domain into projection.py.
  - **VERIFIED BY USE both viewports:** curl 14→0 no-meaning; switched to Flow (390+1440), tapped steps → readout reads human ("Confirm a new record / Checks a proposed new record honestly matches the real thing…"; "Read the intent / Reads what was said and works out what's being asked…") with the precedence info ("1 out · 0 in · pure source" — the chain edges). Fresh-eyes copy critic: 9/14 clear first pass → fixed ALL 5 it flagged (insider shorthand "checkable requirement"/"no invented abilities"; #5 name/meaning mismatch real→exists; #14 disambiguated from #12 merge-to-summary; "entry"→"new record"). design-lint N/A (Python). Registry-true; instrument empty of meaning. TENTATIVE copy (Tim/DNA ratify).
  - **★ LENS-LEGIBILITY SET NOW ~COMPLETE:** all 8 selectable lenses read human end-to-end (legend + sectors): What's-happening(raw) · Connections(node-types) · Two-gravities · Meaning(semantic) · Activity(families) · Ways-of-looking(spaces) · Flow(steps) · Day-cycle(shares raw). REMAINING: only `by_nucleation` "New kinds forming" ✦0..✦3 — EMERGENT cluster ids (not a static registry; needs a derive-from-exemplar treatment, a different mechanism than seed-a-map), and the dataless `lineage` space. So the easy seed-a-grounded-map increments are EXHAUSTED — the next legibility step (✦N) is a real build, not a seed.
  - **NEXT:** FULL prove-on-one the instant DNA's card + wildcard's take land (host + all legs ready + live-verified). The lens-legibility seam is essentially done; if the gate stays closed, the next in-lane beat is either the ✦N derive-from-exemplar (a real mechanism, not a seed) or — better — re-survey for a higher-value in-lane gap (the placement-row legibility for the non-default lenses, or an observed FORM gap) rather than force the last niche legibility item. Consider an advisor check on beat-selection if the gate persists.
- **2026-06-18 — ★★ THE FLAGSHIP'S DROPPED TAKE LEG — found + wired + verified end-to-end (commit `41e3738`). NOT a legibility grind — the real beat, surfaced by the premise-check.**
  Probe: decision still pending, DNA's card not landed. Per last fire's plan (legibility well dry → re-survey/advisor-check, don't grind ✦N), advisor-checked beat-selection. The advisor's FIRST instinct (escalate the take to the lead, wildcard "in formation") rested on a STALE premise — so I verified it by code (the discipline that mattered): the "in formation" label is wildcard's June-14 boot description; the real signal is the code.
  - **★ THE PREMISE WAS WRONG — and verifying it surfaced the actual gap.** wildcard HAD built the take-EMIT (`decide()` → `gallery:verb{verb:'annotate', payload:{mark_type:'decision_take', value, is_decision_take:true}}`, deliberately NO `gallery:direction` alias — the take targets the BARE CANONICAL address, not `#elem`). The gap was the DISPATCHER ROUTING — which fork's binder contract EXPLICITLY assigns to "the dispatcher", and App.tsx `onVerb` is the sole `gallery:verb` listener → the dispatcher is MINE. It had no annotate branch ("annotate never emitted here" was STALE) → the take was silently DROPPED → a decided decision could never record. The LAST projection net-new for the prove-on-one, hiding in my own dispatcher. (Advisor reconciled: build it — wiring a settled seam, not grabbing wildcard's lane.)
  - **THE FIX:** `onVerb` routes `verb==='annotate' && payload.is_decision_take` → `forkBrainCore.writeDirections` (the SAME route-back the annotations use → POST /api/territory/write → suite.mark at the canonical address → the decision:// resolver composes state=decided; + gallery:rerender; fail-loud). Scoped to is_decision_take ONLY (generic comment/reaction/favour ride wildcard's gallery:direction alias → fork's HOOK 2 — routing them here too would double-write). `value`→option LABEL; territory_write reads `type` not `mark_type` → mapped.
  - **VERIFIED BY USE BOTH viewports on a THROWAWAY decision (never the bootstrap — confirmed merge-sa-authorize stays pending; marks aren't deletable):** 390 → emit take → state flips pending→**DECIDED**, decided_value="Pick this one" via the REAL resolver (marks_for(canonical)→compose_state), gallery:rerender fired, no error; 1440 → second take → decided_value updates to "Or this one" (latest-take-wins). The throwaway decisions/<id>.py was created for the test + deleted after (orphaned marks point at an un-registered address — harmless). tsc 0; design-lint N/A (TSX). The fresh-eyes ROOT test belongs to DNA's rendered card + the full on-card drive (still post-reorg); this verifies the dispatcher+write+state half by use.
  - **★ FLAGGED to fork (latent silent-miss, their write-point):** the binder comment claims canonicalization is server-side, but `territory_write` does NOT canonicalize — it marks at the LITERAL element_id. Tested a BARE address → silent-miss (the canonical resolve never saw it). HAPPY PATH SAFE (DNA stamps canonical, verified); latent only if a non-canonical address ever reaches the take. fork's call (defense-in-depth: canonicalize decision_take element_id in territory_write). Reported to wildcard+fork+lead.
  - **★ PROVE-ON-ONE STATUS NOW:** projection's legs ALL built + live-verified — host (decision:rendered seam) · resolve (/api/territory) · explain (forkBrainCore.talk) · TAKE (is_decision_take dispatch → state flips). The ONLY remaining join is DNA's decision-card RENDER (post-reorg): her card's option-click calls wildcard's decide(), her slide mounts → fires decision:rendered → my host opens it. When DNA's card lands, the FULL loop runs with NO new projection code.
  - **NEXT:** the full on-card prove-on-one the instant DNA's card renders (all projection legs verified + waiting); else a re-survey/advisor-check for the next highest-value in-lane gap (NOT the ✦N grind).
- **2026-06-18 — ✅ WHOLE-SCREEN re-verification (no regression from the session's changes) + caught & routed a real decision-card Ask gap (fork's lane). No projection code (the candidate build was speculative → reverted).**
  Gate still closed (decision pending, DNA's card not landed). Legibility well dry (per last fire) → instead of grinding ✦N, did a whole-screen stranger re-verification — the session landed a LOT (legibility across all 8 lenses, the reorg-resync, the take dispatch wired into shared onVerb, the decision host) so cross-cutting regression was the real risk.
  - **NO REGRESSION (verified by use, 390):** RUNG-C still renders human on a corpus drill ("A note saved / What this is / The system wrote something into its memory / Open the source" — the reorg-resync holds); the V verbs still route after my onVerb take-edit (drive → "Drive is coming next."; navigate → its honest Notice); the Disclosure inspector reads human (kind name + meaning). Surface healthy + coherent.
  - **★ CAUGHT A REAL FLAGSHIP GAP (routed to fork, not built):** the decision card won't have an interactive "Ask about this." Evidence by use: fork's in-card Ask hook (HOOK 1) listens ONLY for `gallery:rendered`, not `decision:rendered`; AND the V handle is COVERED while any gallery card is up (overlay z-60 > V z-45/46 — elementFromPoint at the V centre returns the overlay). Corpus cards are fine (in-card Ask present, confirmed). But a decision card → no in-card Ask + V covered → only the pre-filled explanation slot, no follow-up Q&A. NOT necessarily a blocker (the pre-filled explanation + options may suffice for the prove-on-one). Cheapest fix = fork extends HOOK 1 to decision:rendered (bind the same brain, keyed by the decision address; talk() already explains decisions operator-grade). Routed to fork + DNA + lead with evidence.
  - **★ REVERTED a speculative patch (discipline):** I'd started wiring the V to aim at the decision on decision:rendered (so "Ask" targets it) — but the V is COVERED by the overlay (verified), so the V-aim is moot; the right place is fork's HOOK 1, not mine. Reverted rather than ship unverifiable speculation built on assumptions about DNA's unbuilt card. (Don't build on a misunderstanding; the V↔decision-card seam is fork's/DNA's, not a settled projection contract like the take was.)
  - **STANDING finding re-confirmed (cross-lane, not introduced this session):** the Disclosure's HOWTO context item still leaks conceptual jargon ("the canvas… semantic zoom… node detail") — real corpus content (humanizeCtx strips addresses, not concepts); not projection's to rewrite.
  - **NEXT:** the full on-card prove-on-one when DNA's card renders + fork's HOOK 1 decision:rendered extension (if wanted); all projection legs built + verified + waiting. No manufactured build while gated — honest verification + coordination is the legitimate gated-fire outcome.
- **2026-06-18 — ★ TIM DIRECTION: UNION + CONTINUITY. Ran a deliberate UNION-SEAM AUDIT → welded the one unwelded seam (gallery:rerender, commit `6e4e2c5`) + mapped the rest (build-prep/universal-projection/UNION-SEAM-MAP.md).**
  Tim (live): "a big purpose of all of this is union… it sounds like you've come across some inconsistencies." The inconsistencies the build kept surfacing (the dropped take, the import collision, the reorg split, the canonicalization silent-miss, the decision-card Ask gap, the stale label) are all the SAME shape — **silent seams where two streams' parts were supposed to meet and the join was assumed, not welded.** So instead of stumbling on them one fire at a time, did the deliberate audit Tim's direction calls for.
  - **THE AUDIT (UNION-SEAM-MAP.md):** mapped every cross-stream event (gallery:* / projection:* / decision:*) — emitters vs listeners. Most WELDED (gallery:rendered · gallery:verb incl. the take · gallery:direction · projection:select · projection:aim). Found ONE unwelded + two partial/cross-lane.
  - **★ WELDED (mine, this pass): `gallery:rerender`** — fork-brain-core fires it after every route-back WRITE (annotation or decision take) to refresh the face, but NOTHING listened → writes never visually refreshed (the decided state / new annotation only showed on a manual re-drill). The host owns the mount → GalleryMount now re-renders the mounted face on gallery:rerender (corpus → re-invoke renderGallery; decision → re-fire decision:rendered so DNA's decided card re-renders). Degrade-clean + REPLACEABLE (DNA may refine to in-place). VERIFIED BY USE both viewports: 390 match→re-renders, mismatch→no-op; 1440 the #elem annotation sub-address form→re-renders (base match). tsc 0; design-lint N/A.
  - **★ ROUTED (cross-lane, with evidence):** (1) gallery:write-error has no on-CARD display (a take/annotation write-fail; surface chrome is covered by the card overlay z-60) → DNA's card lane. (2) fork's in-card Ask HOOK 1 is gallery:rendered-only → no interactive Ask on a decision card → fork (extend to decision:rendered). (3) territory_write doesn't canonicalize the take element_id (latent silent-miss) → fork. All on thread g-1781731457; the map names each.
  - **★ THE CONTINUITY PRINCIPLE (recorded for the union):** the welds that held were built to FAIL LOUD; the gaps were caught by VERIFY-BY-USE (driving the meeting point, not trusting "the code looks right"). A silent seam is a future drift. **Continuity = no silent seams.** Every new cross-stream seam must (a) fail loud on mismatch and (b) be driven at its meeting point.
  - **NEXT:** re-run the seam map when an event is added/moved; the full on-card prove-on-one when DNA's card + the routed seams land; all projection legs built + verified + waiting. (Open Q surfaced to Tim: union at the SEAM level vs the deeper streams-as-one-entity level — awaiting his steer.)
- **2026-06-18 — ★★ THE WHOLE DECISION MACHINERY COHERES end-to-end (composite prove-on-one, all streams' legs, verified both viewports). The union converged — only DNA's visual card remains. No code change (pure verification of already-built legs).**
  The union-seam routing paid off in one cycle: **wildcard** welded a fail-loud canonical guard on decide() (TEST9 — refuses a non-canonical address, no silent-miss), **fork** welded HOOK 1b (the decision-card in-card Ask on decision:rendered, crediting "projection's whole-screen verify") — both seams I flagged last fires, welded by their owners. So EVERY leg of the decision loop is now built across all streams. Ran the composite test to prove they COHERE (catch any inter-leg seam) before DNA's card lands.
  - **THE COMPOSITE CHAIN (synthetic, on a THROWAWAY decision — bootstrap stayed pending, confirmed):** drove the full loop end-to-end, both viewports. 390 + 1440: (1) `decision:rendered{element,address}` → my host opens (decision-mode) ✓; (1b) fork's HOOK 1b mounts the in-card Ask on the card root ✓; (2) wildcard's `decide(canonicalAddr,label)` accepted (TEST9 guard passes canonical) ✓; (3) take → my gallery:verb dispatch → territory_write → state flips pending→DECIDED, decided_value=the option label ✓; (4) gallery:rerender → my host re-fires decision:rendered (the refresh loop) ✓. 1440 re-decided the other option → latest-take-wins (decided_value updated) ✓. Every stream's leg meets the next.
  - **★ HONEST framing:** this proves the MACHINERY coheres (all legs run through the welded seams) — NOT DNA's actual visual card. What remains is DNA's: the real card markup, the option element's onClick → window.galleryBinder.decide(data-decision canonical, label), and the FORM (the rendered card at both viewports). The contract for it is locked (fork gave DNA the decision:rendered detail shape {element, card-root; address: bare canonical}; wildcard's decide is built+guarded; my host+dispatch+rerender+the HOOK 1b Ask all verified). When DNA's card renders, the loop runs on real pixels with NO new code from projection/fork/wildcard.
  - **PROVE-ON-ONE STATUS: machinery ✅ (all non-visual legs cohere, both viewports) · DNA's visual card 🔴 (the only gate) · then the real on-card stranger test.**
  - **NEXT:** the real on-card prove-on-one the instant DNA's card renders; re-run the seam map on event changes; deeper-union steer still open to Tim.
- **2026-06-18 — ✅ UNION-SEAM audit, class 2: the surface↔bridge API routes — ALL WELDED (verification; no code change; map extended).**
  Continued Tim's union direction with the next seam class after events: every `/api/*` the surface (+ synced hooks) depends on, verified live against the bridge (a 404 = a silent seam, the feature breaks).
  - **ALL WELDED:** the 9 GET routes (projection · cognition/corpus · cognition/neighbours · context · layers · layer-dims · territory · territory/label) → 200; the 2 POST routes (territory/write · claude/turn) → 200 (drove the take + explain this session); /api/stream (GET/SSE via EventSource) → 200. No missing route.
  - **Two false-positive 404s ruled out (recorded in UNION-SEAM-MAP.md so a future audit doesn't re-flag):** `/api/layer` was a regex truncation of the real `/api/layer-dims` (welded); POST `/api/stream` 404'd only as a method mismatch on a GET-only SSE route (GET → 200). So: no silent API seam — honest (no bug found in this class; the candidates were artifacts).
  - **★ UNION PICTURE NOW:** two highest-traffic cross-stream seam classes audited — EVENTS (welded; gallery:rerender welded this session) + API ROUTES (welded) — AND the whole decision machinery proven to cohere end-to-end (composite, both viewports). The union is confirmed sound across its main surfaces; the remaining gate is DNA's visual card.
  - **NEXT:** the real on-card prove-on-one when DNA's card renders; re-run either seam map on a change; deeper-union steer open to Tim.
- **2026-06-18 — ✅ CONTINUITY (Tim's direction, made concrete): the operator's chosen LENS persists across reload/reopen (commit `50b1244`).**
  Gate still closed (decision pending, DNA's card not landed); lead not yet replied to the free-capacity flag. Per away-sprint (don't idle), found a real in-lane CONTINUITY gap rather than another audit — Tim: "we want to have continuity."
  - **THE GAP (falsify-first):** only the legend-collapse + the V handle position persisted (localStorage); the operator's actual VIEW reset every reload. Switch to Flow (or any non-default lens) → reopen → dropped back to "What's happening" — the operator loses their place. That's a continuity gap at the operator-experience level.
  - **THE BUILD:** persist the LENS (binding) in localStorage (same pattern as the V position + legend collapse) — restored on init, saved on change. Degrade-clean: a persisted lens that's no longer a real binding resets to 'raw' once proj.bindings loads (a stale restore can never leave the operator stuck on a non-existent way of looking).
  - **VERIFIED BY USE BOTH viewports:** 390 → switch to Flow → reload → still Flow (continuity held); stale lens → reload → reset to "What's happening" (degrade-clean), re-persisted 'raw'. 1440 → "Ways of looking" → reload → persisted. localStorage reset to clean default after. tsc 0; design-lint N/A (TSX).
  - **Scoped to the LENS** (the primary, always-valid way of looking — highest continuity value). FOLLOW-UPS (deliberate, noted not skipped): CENTRE + TIME view-state persistence — they carry staleness risk (a centred address can be deleted; a scrub position is transient) so each needs its own degrade-clean restore (validate the centre still resolves; clamp/expire the time). The lens is the clean baseline; centre/time are the next continuity increments if the gate stays closed.
  - **NEXT:** centre/time continuity (degrade-clean) as the next gated-fire increment; the real on-card prove-on-one when DNA's card lands; deeper-union steer open to Tim.
- **2026-06-18 — ✅ LEAD REDEPLOY: co-scoped the decision-SUBTYPE → variant → host-render contract (lead t-1781755855 directive (b); critical-path, Tim-commissioned). Lock-before-build; no code (the pieces don't exist yet).**
  Lead replied to my free-capacity flag: (a) STAY PRIMED for the on-card prove-on-one; (b) ★ redeploy to the decision-subtype host rendering (composition standing up a `decision_subtypes` registry — binary/trade-off/naming/prioritize → each selects a card VARIANT + an explanation generation-policy); (c) deeper-union PARKED for Tim's steer.
  - **STATE (falsify-first):** the subtype work isn't started — no `subtype` on decision.schema.json; no decision_subtypes registry; the resolved decision (merge-sa-authorize) carries none; decision-card.schema = ONE archetype {render_kind:'slide', slot_map, take}. So it's a clean co-scope (lock the seam before the pieces land — the union-seam discipline, proactive), not a build-yet.
  - **PROPOSED CONTRACT (sent to composition+DNA+fork, cc lead, g-1781731457) — registry-true, the one `subtype` field drives everything:** composition adds `subtype` to decision.schema + the decision_subtypes registry {subtype:{card_variant, explanation_policy}}; fork resolves `subtype` into the decision identity + the explain (talk) picks explanation_policy BY subtype; DNA's render SELECTS the variant from the resolved subtype (per-subtype render_kind/slot_map) + includes subtype in decision:rendered.
  - **★ KEY ARCHITECTURAL CALL (flagged vs the lead's "host renders the variant"):** the HOST stays VARIANT-AGNOSTIC — DNA's render selects the variant from the subtype; fork's explain picks the policy from the subtype; the host HOSTS the result + carries `subtype` on the event. If the host branched per subtype it'd hardcode variant knowledge into the instrument (against registry-is-truth + replaceable: a NEW subtype must need ZERO host change). So "host renders the right variant" = host the variant DNA paints, not select it. Net: likely NO parallel host BUILD — projection's piece is the tiny subtype-carry once it exists.
  - **THE ONE QUESTION that would give the host real work:** does any subtype need a DIFFERENT host LIFECYCLE (e.g. `prioritize` = a multi-step ranking SEQUENCE, not a single slide)? binary/trade-off/naming read as single-slide (my decision-mode handles them). If prioritize (or any) is multi-step → I build a per-subtype host lifecycle (real, parallel, unblocked-once-defined). Asked composition directly.
  - **NEXT:** build the host piece the moment composition/DNA's answer defines it (the subtype-carry, or a per-subtype lifecycle if prioritize breaks single-slide); primed for the on-card prove-on-one throughout.
- **2026-06-18 — honest WAITING fire: active builds genuinely gated on others; nudged composition + corrected the continuity scope. No code (no green-paint).**
  Probe: decision pending, no subtype field, DNA's card not landed. composition's reply to the subtype co-scope came through EMPTY (glitch — no content) → the contract + the prioritize-lifecycle Q are unanswered, so the host subtype-piece can't be built yet (it's a real-build-or-none depending on their answer). Nudged composition once (the one blocking Q). Genuinely blocked on others (DNA's card · composition's subtype contract); capacity flagged to lead; deeper-union parked for Tim. NO manufactured/speculative build this fire — honest waiting beats green-paint.
  - **★ CORRECTED the continuity scope (honest refinement):** last fire I queued centre + time view-state as continuity follow-ups. On reflection they should NOT persist — they're TRANSIENT EXPLORATION, not standing preferences: an operator scrubs to the past or re-centres to investigate, and the CORRECT reopen behavior is return-to-default (live/now, un-centred), not stuck-in-the-past or centred-on-a-forgotten-thing. So **continuity-persistence is COMPLETE at the LENS** (the one standing "way of looking" preference) — centre/time are correctly-not-persisted, not a gap. (Removed them from the queue so a future fire doesn't build unwanted persistence.)
  - **NEXT:** the host subtype-piece when composition/DNA answer; the on-card prove-on-one when DNA's card lands; await the lead's next sequencing / Tim's deeper-union steer. Projection's commission is substantially complete + proven; the frontier is genuinely on DNA + composition now.
- **2026-06-18 — ✅ SUBTYPE CONTRACT LOCKED (composition resent the real reply) → host confirmed ALREADY COMPLETE for the current scope; no build (would be unconsumed staging). Build-triggers made explicit.**
  composition's empty reply was a tool-slip; resent + confirmed: (1) subtype-field-drives-it, host stays VARIANT-AGNOSTIC (my correction is the rule — host HOSTS the variant DNA selects, never selects it); (2) all four subtypes (binary/trade-off/naming/prioritize) = SINGLE-SLIDE (prioritize = rank-in-place); ★ the lever is `render_kind` ('slide' | 'sequence'), a DECLARED field on decision-card.schema — not a per-subtype branch (registry-true).
  - **★ HONEST FINDING — the host is already complete (nothing to build now):** (a) variant-agnostic ✓ (hosts any variant DNA paints; new subtype = zero host change); (b) the decision-mode lifecycle ALREADY handles slide AND sequence ✓ — it IS a sequence lifecycle (open → HOLD → advance=re-fire decision:rendered swaps content in one container → close); a single-slide is a one-step sequence; (c) downstream reads `subtype` from the RESOLVED decision (territory), not from the host — DNA selects the variant, fork picks the explain-policy, both from territory. So the host carrying subtype on the event has NO consumer yet → building it = unconsumed advertise = green-paint. Did NOT build.
  - **★ EXPLICIT BUILD-TRIGGERS (recorded so a future fire acts on fact):** (1) a `sequence` render_kind appears → build the host walk-driver (step-tracker / next-prev / "step N of M"); (2) a consumer needs the host's current-subtype → add the subtype-carry (tiny). Until a trigger fires, the host already does its job. Closed the co-scope with composition (g-1781731457).
  - **NEXT:** stay primed for the on-card prove-on-one (DNA's card); run the real per-variant host verification the instant composition's subtype field + DNA's variants land; build a host walk-driver IF a sequence kind appears. Projection's host is complete + proven for the current scope across both viewports — the frontier is DNA + composition.
- **2026-06-18 — ✅ DE-RISKED THE FLAGSHIP'S LAST MILE: consolidated the DECISION-CARD HOST-INTEGRATION CONTRACT (build-prep/the-one-application/DECISION-CARD-HOST-CONTRACT.md) for DNA's render.**
  Gate still closed (decision pending, no subtype, DNA's card not landed — she's deep in the graph: 90° swap, tree-projection). Projection's code is complete + proven; the highest-value move while waiting is the union-discipline applied to the last mile, not manufactured code.
  - **WHY:** the seams DNA's card must meet to plug into the live machinery were SCATTERED across the fabric thread (decision:rendered shape, the data-decision canonical stamp + option-click→decide, the card-root for HOOK 1b, the rerender, the write-error display, the subtype/variant). A card built against a scattered contract risks MISSING a seam — and every missed seam is a SILENT failure (host won't open / take silently misses / Ask won't mount). So I consolidated them into ONE checklist: 7 numbered items, each with the silent-failure-if-missed + projection's verified status + what's automatic (zero DNA action).
  - **THE ONE DNA-LANE SEAM surfaced:** the write-ERROR display must live ON the card (gallery:write-error{element_id} → "couldn't save") — the surface chrome (Notice z-50, V z-45) is covered by the card overlay (z-60), so projection can't surface it. Everything else is done + automatic.
  - **VALUE:** this is the flagship's highest-stakes seam-meeting (the last mile). The consolidated contract = DNA's card plugs in ONE pass, with every silent-seam guarded. Pointed DNA to it (g-1781731457). Coordination/artifact, not code — honestly (projection's code is complete; the gate is DNA + composition).
  - **NEXT:** the real per-variant on-card prove-on-one the instant DNA's card renders (I run it + the stranger test, both viewports); subtype field + variants from composition/DNA; deeper-union steer open to Tim.
- **2026-06-18 — ✅ BUILT the host's subtype-contract piece: the host carries `subtype` + `render_kind` (commit `ff41548`). The lead confirmed my ruling + named this the host's ONE contract-time change → "the whole host lane — clean."**
  Lead reply (t-1781755855): confirmed THE HOST HOSTS DNA'S VARIANT, DOESN'T SELECT IT (my flag = the ruling, banked) + steered: prove-on-one (merge-SA) = single binary slide → generic host now, no per-subtype lifecycle (sequence built only when a real instance renders); **"carrying `subtype` on the event is your only contract-time host change."** So I built exactly that — no more, no less.
  - **THE BUILD:** GalleryMount reads `subtype` + `render_kind` on decision:rendered → ADVERTISES them on the gallery-frame (data-decision-subtype / data-render-kind — inspectable carry) → PRESERVES them when the host re-emits decision:rendered on the rerender refresh (the host's re-fire keeps the contract shape, never drops fields) → clears on a corpus drill. Host stays VARIANT-AGNOSTIC (carries, never selects). render_kind = the lifecycle lever: 'slide' handled (all current subtypes); 'sequence' = a named extension, built only on a real instance.
  - **VERIFIED BY USE both viewports:** 390 → decision:rendered{subtype:'trade-off'} advertised + the rerender re-fire preserved subtype+render_kind + corpus drill cleared; 1440 → 'prioritize' advertised + preserved. tsc 0; design-lint N/A. HONEST: forward-compatible — no consumer reads it yet (DNA + fork read subtype from the resolved decision/territory); this welds the host's side of the locked seam so it's ready when a consumer needs the host's current subtype.
  - **★ PROJECTION'S SUBTYPE LANE IS NOW COMPLETE** (lead: "the whole host lane — clean"): host variant-agnostic ✓ + slide/sequence-capable lifecycle ✓ + the subtype-carry ✓ + the DNA host-integration contract consolidated (DECISION-CARD-HOST-CONTRACT.md) ✓. The only future host build is the sequence walk-driver, triggered by a real sequence instance.
  - **NEXT:** the on-card prove-on-one when DNA's card renders; build the sequence walk-driver IF a real sequence subtype instance appears; deeper-union steer open to Tim.
- **2026-06-18 — honest GATE-WATCH: projection complete + primed; the flagship's gate (DNA's card) isn't moving (DNA on the graph). Flagged to lead for the Tim check-in. No code (no green-paint).**
  Probe: decision pending, no subtype, DNA's card not landed. Resolved the one open question (is the graph a projection beat?): NO — the graph GRADUATED to its own standalone surface (app/graph.html in DNA's repo, reachable from her gallery rail's Instruments section), not a projection-instrument integration. So projection's lane has no graph work.
  - **GATE-WATCH:** DNA's last ~5 commits are all the GRAPH (tree-projection / 90° swap / type colours / orbit) + reorg — the decision card (the flagship's ONLY gate) hasn't been touched. The whole decision loop is composite-proven on projection's side; it's one DNA-render from end-to-end, and that render isn't currently in progress. Flagged to the lead (factual, for the Tim check-in) — not asserting graph-first is wrong, just that the prove-on-one ("a main part of the application") is blocked on a render that's queued behind the graph.
  - **HONEST STATE:** projection's build lane is genuinely complete (flagship machinery + subtype-carry + host-contract + legibility + continuity + seams all done/proven); the last several fires have been coordination/contract-locking because there's no unblocked in-lane build. Did NOT manufacture a build (green-paint). Free capacity offered to the lead (cross-lane weld / the deeper-union pass, which may be timely since the active builds are stalled).
  - **NEXT:** the on-card prove-on-one the instant DNA's card renders; await the lead's sequencing (re-prioritize DNA's card / point me cross-lane / start deeper-union) or Tim's steer.
- **2026-06-18 — ✅ CUMULATIVE REGRESSION SMOKE-TEST → caught + fixed a real latent bug (commit `d709fc6`); whole surface confirmed healthy after the session's many changes.**
  Gate still closed (decision pending, no subtype, DNA on the graph); lead not yet replied to the gate-watch; deeper-union parked (don't run speculatively). So instead of re-spamming coordination or manufacturing a build, did the BAR's warranted work: a cumulative regression smoke-test — the session made many INTERACTING changes (take-dispatch in onVerb, gallery:rerender weld + subtype-carry + decision-mode in GalleryMount, lens-persistence in App), and cross-cutting regression is a real risk.
  - **★ THE SMOKE-TEST EARNED ITS KEEP — caught a real latent bug:** an uncaught console error, `NotFoundError` from `releasePointerCapture` in the V's onPointerUp. Root: onPointerDown's setPointerCapture is best-effort (try/catch, can silently fail), but onPointerUp's `releasePointerCapture?.(id)` was NOT guarded the same way (`?.` guards method-absent, NOT the throw-when-nothing-captured). So if capture didn't take (pointercancel / browser quirk / synthetic pointer) the release threw uncaught. FIX: wrap it try/catch, symmetric with the capture — release is best-effort cleanup.
  - **VERIFIED BY USE both viewports:** re-ran the exact trigger (V tap with an uncaptured pointer id) → console CLEAN (was NotFoundError ×2). The rest of the smoke-test confirmed NO other regression — cold-open (600 dots, legend, V, scrubber), lens-switch+persist (Flow→by_cascade), gallery RUNG-C ("A note saved", human), the V 6-verb fan, decision-mode + subtype-carry (data-decision-subtype='binary') all healthy. tsc 0; design-lint N/A.
  - **NET:** a real fix (caught by warranted verification, not manufactured) + the whole surface verified healthy end-to-end after the session. The flagship still waits on DNA's card; projection complete + primed.
  - **NEXT:** the on-card prove-on-one when DNA's card renders; the lead's sequencing / Tim's steer.
- **2026-06-18 — ✅ LEAD-DIRECTED BUILD: the READY prove-on-one HARNESS (commit `ebe4f30`) — the seam proves itself SAME-SECOND the decision-card fork's render lands.**
  Lead reply (t-1781758984): the flagship IS the priority (lead sequencing the decision card — now its OWN FORK, Tim's move; DNA back to the graph); my capacity → stay primed + hand the fork a READY test harness so the prove-on-one runs same-second (do NOT build into the render lane — collision risk); deeper-union HELD (Tim's driving it). A concrete, in-lane, unblocked, non-colliding build.
  - **THE BUILD:** `surface/app/scripts/prove-decision-harness.js` → `proveDecision(addr, opts)` drives the full decision loop + asserts EVERY leg (resolve · host-open · in-card-ask · take→state-flip pending→decided · rerender-refresh) → `{pass, mode, legs}`. Auto-detects REAL-CARD (the fork's render mounted in #gallery-mount) vs SYNTHETIC (drives a stub decision:rendered so host+ask are exercised pre-card). `decide` opt only on a THROWAWAY (flips state); on a real decision run without it + let the operator choose (= the actual prove-on-one).
  - **VERIFIED known-good BOTH viewports** on a throwaway: 390 full loop (all 5 legs green — resolve pending, host open, Ask mounted, take pending→decided='Leg A', host re-rendered); 1440 resolve/host/ask green. Bootstrap merge-sa-authorize stays pending (throwaway only, deleted after). Pointed DECISION-CARD-HOST-CONTRACT.md at it with run-steps.
  - **NET:** the flagship's last mile is now fully de-risked from projection's side — the contract (every seam + silent-failure) + the harness (proves it same-second). The moment the decision-card fork renders, I hand it both + run the real on-card prove-on-one (390→1440) + the stranger test. Real in-lane build, non-colliding, lead-directed.
  - **NEXT:** hand the fork the contract + harness when it spins up; run the real on-card prove-on-one the instant its render lands; deeper-union awaits Tim's steer (via lead).
- **2026-06-18 — ✅ CONNECTED with the decision-card fork (lead-directed) — handed it the contract + harness; primed for its render.**
  Probe: gate still closed (decision pending, no subtype, no card render). A NEW session appeared — `ch-2er2g9ja` (cwd counterpart/design, just spun up) = very likely the decision-card FORK the lead said would spin up (Tim's move; a fork of DNA to build the card). Per the lead's directive ("hand the fork the contract + harness when it spins up"), reached out (t-1781760375): confirmed-or-not + handed it DECISION-CARD-HOST-CONTRACT.md (the 7 seams + silent-failure-if-missed) + the ready harness (proveDecision), with the 3 silent-seam traps spelled out (decision:rendered shape · the canonical take stamp · the YOUR-lane write-error display) and "ping me the instant your render lands → I run the real on-card prove-on-one 390→1440 + stranger test."
  - **THE UNION-DISCIPLINE AT THE LAST MILE:** the host + the card-builder now connected BEFORE the card paints — so the seam meets in one pass (the fork has every seam + the same-second harness; I'm primed to host + verify). No build in projection's lane (the render is the fork's — collision-forbidden); the right move is the connection + priming.
  - **NEXT:** await the fork's render → run the real on-card prove-on-one (the harness, then driven both viewports) + the stranger test; deeper-union awaits Tim's steer (he's actively driving union; lead relaying).
- **2026-06-18 — ★★★ THE FLAGSHIP LANDED: the decision-card render shipped (fork) → the prove-on-one runs END-TO-END ON THE REAL CARD, both viewports. FUNCTION proven; fresh-eyes ROOT = NEEDS-WORK (one FORM gap, routed to the fork).**
  The decision-card fork (ch-2er2g9ja) shipped the render into surface/runtime/unit-view.js + phone.css — and it plugged into my host-integration contract EXACTLY (renderDecision reads subtype/render_kind → renders the variant `dc-options--<subtype>`; option buttons carry data-decision=canonical + onclick=galleryBinder.decide(addr,label); emits decision:rendered{element,address,subtype,render_kind} — my exact shape; AND built the gallery:write-error-on-card display = the one seam I flagged as theirs). The contract worked — zero rework.
  - **THE PROVE-ON-ONE, VERIFIED BY USE ON THE REAL CARD (re-synced the fork's render):** rendered the REAL merge-sa-authorize card → human both viewports (390+1440 screenshots): "DECISION / Turn on save-back to the design library / What this is / [the why] / Give it save-back access [RECOMMENDED] / Stay read-only for now / Ask about this". decision:rendered fired {subtype:binary, render_kind:slide}; my host opened it; the in-card Ask mounted; my subtype-carry advertised 'binary'. Harness (real-card mode) green: resolve · host-open · in-card-ask. THE FULL TAKE LOOP via the fork's REAL option button (on a THROWAWAY — bootstrap stays pending for Tim): click data-option → galleryBinder.decide → my dispatch → territory_write → state flips pending→DECIDED (decided_value) → gallery:rerender → host re-fires → card re-renders decided. Every leg green.
  - **★ FRESH-EYES ROOT TEST (separate stranger, cold, 390) = NEEDS-WORK (close to PASS):** UNDERSTAND ✓ (fully gets the decision), OPTIONS ✓, RECOMMENDATION ✓ — but the ONE gap: the options don't VISUALLY read as tappable (no button affordance/instruction → a stranger hesitates on HOW to act); + minor (low-contrast description text; the bottom card cut-off / "Ask about this" cramped). ALL FORM in the fork's card-render lane (the take FUNCTION is proven; the buttons just don't LOOK like buttons). Routed to the fork with evidence (t-1781760375).
  - **★ HONEST STATE:** the flagship's prove-on-one is FUNCTIONALLY PROVEN end-to-end on the real card, both viewports — the loop works. The full BAR's ROOT pillar (a stranger can USE it) is NOT yet fully passed: the option-affordance FORM fix (fork's lane) is the one thing between FUNCTION-proven and ROOT-passing. NOT "done" — one FORM-fix away, routed.
  - **NEXT:** the fork fixes the option-button affordance (+ contrast + bottom layout) → I re-run the stranger test for the full ROOT pass; then the flagship is genuinely DONE. Tim's real merge-sa-authorize decision stays pending for HIM to make in-surface (that's the live use, not a test).
- **2026-06-18 — falsify-first on the fresh-eyes findings: #3 ("cut off") is a SCREENSHOT ARTIFACT (host frame correct), narrowed the real ROOT fixes to the fork's 2 CSS items. No in-lane build (host verified-correct).**
  Before the fork acts on the fresh-eyes findings, verified which are real (don't let the fork chase a non-bug):
  - **★ #3 "bottom card cut off / Ask cramped" = NOT a bug (screenshot artifact).** Verified by use at 390: the real card is 8px taller than the viewport (844 vs 836), the host `.gallery-frame` SCROLLS (overflow:auto working), and after a scroll BOTH the last option and "Ask about this" are fully reachable. The static screenshot couldn't scroll → the cold reader read below-the-fold as cut-off. Host frame is CORRECT — no fix needed in my lane.
  - **The REAL ROOT fixes are the fork's 2 phone.css items:** #1 (THE ROOT-BLOCKER) the options are `<button class="dc-opt">` (semantically buttons) but don't LOOK tappable — need visible button affordance; #2 low-contrast description text. Clarified to the fork (t-1781760375). The take FUNCTION is proven (real button → state flips); these are pure CSS affordance/contrast.
  - **HONEST:** no projection build this fire — the host frame is verified-correct (scrolls), and the ROOT-blocker is the fork's CSS (not yet pushed). Falsify-first avoided manufacturing a host "fix" for a non-bug. Waiting on the fork's affordance fix → re-run the stranger test for the ROOT pass.
  - **NEXT:** fork pushes the .dc-opt affordance + contrast → I re-sync + re-run the cold stranger test (390→1440) for the full ROOT pass → flagship DONE.
- **2026-06-18 — regression-clean after pulling the fork's new render; the affordance is a fork DESIGN call (not pushed yet). No in-lane build.**
  Pulling the fork's new unit-view.js brought the decision render INTO the same file as the corpus render (+ organisms.js/phone.css grew). Warranted regression check: does the CORPUS RUNG-C still work? VERIFIED by use (390): corpus dot → gallery renders human ("A note saved / What this is / Open the source / Ask about this") — NO regression; the decision + corpus renders coexist clean on my host.
  - **The affordance (fresh-eyes #1) is the fork's DESIGN call:** .dc-opt already HAS affordance in phone.css (white surface, 1.5px border, shadow, cursor:pointer, hover→gold, recommended gold-ring) — so "doesn't look tappable" is a borderline judgment (a card-button vs an obvious CTA), in DNA's/the fork's design-system lane, not mine to restyle. Routed with evidence (+ the #3-is-an-artifact correction). Fork hasn't pushed a change yet (recent commits = graph/reorg).
  - **HONEST:** no projection build — the host is correct + regression-clean; the remaining ROOT-pass item is the fork's design polish. Primed to re-sync + re-run the stranger test when they push. Didn't re-ping (just routed last fire) or manufacture.
  - **NEXT:** fork's affordance/contrast push → re-test for the ROOT pass; Tim's merge-sa-authorize decision (his to make in-surface); deeper-union (Tim's steer, held).
- **2026-06-18 — DROVE the last un-driven seam: the write-error fail-loud display. ALL four flagged union seams now WELDED + verified by use.**
  The harness drives the HAPPY path; the no-silent-failure seam (a FAILED take must show ON the card — the one seam projection structurally can't cover, since the card overlay z-60 hides the surface chrome) had never been DRIVEN, only built (by the fork) + read. Drove it on the real merge-sa-authorize card, BOTH viewports (390 first):
  - fire `gallery:write-error{element_id: bare canonical}` → the card matches `element_id`===`data-decision-address`, reveals the "Couldn't save — try again" banner (red, DNA-token styled, "!" icon), sets `.write-error`, disables the options (opacity .6 / pointer-events:none). VERIFIED: `{bannerShown:true, cardHasWriteErrorClass:true, optionsDisabled:true, matchesBareCanonical:true}` at 390 AND 1440. A failed take is LOUD.
  - **Found + recorded a verified-safe hardening:** `/api/territory` returns NO `identity.address`, so the card's write-error match key comes from the render-time `d.address` fallback, not registry-truth. CLEAN today (driven), but a future render change dropping the fallback would silently break the match. Routed to the fork as a small hardening (resolver returns `identity.address` → registry-is-truth). Not blocking.
  - **UNION-SEAM-MAP updated:** all four flagged seams (rerender · in-card Ask · write-error · canonicalization) now show WELDED + the verification evidence. The decision surface's union is whole — every cross-stream seam meets AND fails loud AND has been driven at its meeting point.
  - **HONEST:** this is verified union-closure (a real beat — the fail-loud seam was undriven), not new feature code. The flagship's FUNCTION + fail-loud are proven; the ROOT pass still waits on the fork's affordance/contrast polish (their design lane).
  - **NEXT (unchanged):** fork's affordance/contrast push → re-run the cold stranger test for the ROOT pass; Tim's real decision (his); deeper-union (Tim's steer, held).
- **2026-06-18 — BUILT the RHM-as-GUIDE first slice (the advisor-caught half of the commission). The V stops being a mute verb-tool.**
  Advisor redirect: I'd collapsed the whole "OPERATOR SURFACE + RIGHT-HAND-MAN" commission down to the one decision card (the prove-on-ONE = scaffolding, not the deliverable) and was polishing its edges. Half B — the RHM as the always-present GUIDE/teacher (Phase 0's three-question spine) — had never been driven to the bar. Drove the V cold as a stranger (390): the surface answers "what am I looking at" well, BUT the V is a mute gold icon (no hint it's the right-hand-man / a guide) and the verb fan gives no aim context. So built the first slice:
  - **First-contact GREETING** (RightHand.tsx + rhm.css): once per operator (localStorage `rhm.greeted`, dismissible), the V introduces itself — "I'm your right-hand-man / your guide here. Tap me — choose Ask and I'll explain whatever you're looking at…" A tail + a gentle attract-pulse connect the card to the actual icon. Copy = TENTATIVE draft (DNA's verbal face; Tim ratifies).
  - **Fan AIM CAPTION**: the open fan now says, in plain words, what the verbs act on — "everything here" by default (no "surface" jargon; honest it's the whole view) + "tap a dot to focus on one thing" (teaches the drill = the third question).
  - **Verified by use BOTH viewports (390 first, 1440):** greeting renders + connects to the V; caption plain + within viewport; no console errors. **Regression-clean:** Ask→brain, Note→composer, soon-states (drive/generate) all intact. **Typecheck clean** (tsc --noEmit). Built on Paper tokens, no hardcoded colour; replaceable (real V still swaps into the host).
  - **Fresh-eyes (cold stranger, two passes):** pass 1 NEEDS-WORK (3 findings) → fixed the 2 in-lane ones (greeting↔V connection; "surface" jargon) → pass 2 CONFIRMED both landed ("I now know the V is my helper and how to open it"). Residual NEEDS-WORK = the verb LABELS vague on touch + soon-clutter → **composition's ratified V-contract lane**, routed with a proposal (surface live-verb descriptions on touch; reconsider soon-on-first-contact) — t-1781766012.
  - **HONEST:** half B is now STARTED + its first slice verified, NOT finished — the verb-legibility layer (composition) + the deeper RHM walk are ahead. This is the real next BIG beat the commission asks for, finally being built.
  - **NEXT:** composition's steer on the verb-fan legibility → implement it; then the deeper RHM guide (proactively walking the three questions); + half A's ROOT pass on DNA's affordance.
- **2026-06-18 — RHM-as-guide, second slice: the verb FAN now passes the cold-stranger ROOT test (PASS). Built composition's ratified steer.**
  Composition ratified the routed finding fast (t-1781766012) — both calls are their V-contract law, mine to render:
  - **Descriptions VISIBLE ON TOUCH** (their operator-legibility law — phone is the priority device, hover/title-only fails it): each LIVE verb now shows its ratified description under the label — "Go to / Re-centre the view on this", "See the record / Open the full record behind this", "Ask / Ask the right-hand-man about this", "Note / Leave a note here". Their text; my render.
  - **First contact = LIVE verbs only**: the 4 usable actions only on the first screen; the 2 SOON verbs (Make/Drive) behind a quiet **"What's coming"** toggle ("Show less" collapses) — still honestly SOON-labeled when revealed (discoverable, never hidden-forever, never shown as working). I chose the explicit toggle over reveal-after-first-use (more honest — nothing changes behind the operator's back).
  - **Fan geometry reworked** (RightHand.tsx): cumulative-y layout for the taller 2-line live pills (fixed width so descriptions wrap to ~2 lines, not one-word-per-line; 0 overlaps); the aim caption suppressed while expanded so the taller fan can't push it into the page header.
  - **Verified by use BOTH viewports (390 first, 1440):** collapsed + expanded clean, within viewport. **Regression-clean:** Ask→brain, Note→composer, soon-reveal, greeting all intact. **Typecheck clean.** Committed 3cbb074. Built on Paper tokens, no hardcoded colour.
  - **★ Fresh-eyes (same cold stranger) verdict FLIPPED to PASS:** "a stranger can now understand the gold V is their guide and how to use it… I actually know what each does before tapping… cleaner without the dead buttons… 'What's coming' is honest." All three of its earlier complaints addressed. One NON-BLOCKING nit: the cards sit over the busy chart (readable, didn't impede) — optional scrim-dim polish flagged to composition.
  - **HONEST:** the RHM-as-guide is now at a clean ROOT PASS for first-contact legibility (greeting + fan). Still ahead: the DEEPER guide — the RHM proactively WALKING the three questions at the current level (vs the reactive Ask it has now) — that's the next real beat of half B. + half A's ROOT pass on DNA's affordance.
  - **NEXT:** the deeper RHM walk (proactive three-question guide) — design + build; DNA's affordance push → half A ROOT pass; composition may reaffirm the fan mechanism at a later cold-stranger retest.
- **2026-06-18 — RHM walk SEED built (three-question starters in Ask) + half-A ROOT re-test on DNA's affordance fix. Two coordinations out.**
  Two things this fire — a half-B build and a half-A verification:
  - **HALF B BUILD — the three-question walk seed (committed eb721fb):** the RHM's Ask panel opened BLANK (a stranger doesn't know what to ask — the gap behind "the teacher walks you through"). Now the panel offers Phase 0's three questions as one-tap starters ("What am I looking at? / What can I do here? / Where can I go from here?") under "Not sure where to start? Ask me —". Tapping one calls forkVBrain.ask(q) at the current aim → the REAL brain answers. Rendered in projection's panel wrapper above fork's appended .v-brain (stable; no regression). **Verified by use BOTH viewports** (390+1440 render clean) + **FUNCTION live**: tapped "What am I looking at?" → POST /api/claude/turn 200 → a real grounded human reply streamed in ("You're looking at a piece of your own instrument surface… want me to take a closer look?"). Typecheck clean.
    - **Fresh-eyes:** the MECHANISM is a clear **PASS** ("removes the blank-page paralysis, the tap works, answer comes back fast in plain language"). NEEDS-WORK narrowly on the ANSWER for the SURFACE-DEFAULT aim: the RHM punts ("my pointer is thin… this cell hasn't been richly labelled") even though the view's title+description is on screen — because the default aim is the generic `ui://instrument/surface`, which territory_prose grounds thin. **The picked-thing case already grounds richly** (the decision-card explain proved it) — this is only the nothing-picked surface default.
    - **Routed to fork (t-1781769953):** meet-at-the-aim — either (a) territory_prose('ui://instrument/surface') composes the current view's self-description, or (b) I aim the default at the current view's real self-describing address (if one grounds richly — awaiting fork's address). My UI half is done + passing; the grounding is the residual.
  - **HALF A RE-TEST — DNA pushed the affordance fix (5e0eeb0); I re-synced + re-ran the cold-stranger ROOT test on the real merge-sa-authorize card.** FUNCTION fully intact after the re-sync (options wired, match key, Ask, take-loop). The chevron + solid surface are a real improvement BUT the stranger still rates **NEEDS-WORK** — measured: the "›" chevron is gold `rgb(227,196,33)` on near-white ≈ **1.7:1** (too faint to read as a tap-cue), and the option body `.dc-opt-implication` is `rgb(164,156,144)` 11px ≈ **2.9:1** (< WCAG 4.5:1). Both pure contrast in DNA's render lane — **routed to DNA with the exact measurements (t-1781768757)**. Also surfaced a decision-FLOW finding (tap = immediate commit, no confirm/undo → mild "will this commit me?" anxiety) to the **lead** (t-1781768803, my lean: a "change/undo" affordance on the decided state).
  - **HONEST:** half B's walk has a working, passing UI seed; its answer-grounding for the bare surface is fork's residual. Half A's ROOT pass is still one DNA contrast push away (FUNCTION long-proven). Neither is "done"; both are precisely one owned-by-another-stream fix from it, routed with exact evidence.
  - **NEXT:** fork's grounding answer → wire the surface aim → re-test the walk's first answer; DNA's contrast push → half A ROOT pass; lead's flow ruling → wire the confirm/undo; then the DEEPER walk (the RHM proactively stepping ①②③, beyond reactive starters).
- **2026-06-18 — Acted on three fabric answers in one fire: grounding (my half, WITH fork) + scrim dim (composition) + the (c) flow ruling (lead). Committed 74b0cb3.**
  All three of last fire's routes came back actionable; built/closed each:
  - **GROUNDING — my half of fork's hybrid (t-1781769953):** fork is building territory_prose to ground `ui://instrument/surface` on the bindings-registry view meta; MY half = carry which view is live. The V's surface-default aim now appends `#binding=<active binding>` (App passes the live binding into RightHand; a binding effect keeps it current + updates the live aim when nothing's picked). **Verified by use:** the brain POST now sends `address: "ui://instrument/surface#binding=raw"` (was the bare placeholder); is_error:false, graceful. The RICH answer lands when fork's half ships + bridge restarts (parallel build) → then I re-run the cold-stranger walk test. Format is fork's agreed seam.
  - **SCRIM DIM — composition's ratified reversal (t-1781767852; Tim: "polish IS the point"):** the open-fan/Ask scrim 0.4 → **0.66** — cards beautifully legible AND the chart still reads beneath (composition's bound). Verified both viewports. This also closes the earlier fresh-eyes "cards sit on the busy chart" nit.
  - **(c) FLOW RULING — lead (t-1781768803):** one-tap stays (no confirm; matches simple-consent + latest-wins re-decidability) + the DECIDED state shows a "change/undo" affordance. My dispatch half ALREADY satisfies it (a re-tap re-decides → re-renders decided; verified). Briefed DNA (t-1781768757) on their render half (decided card shows the choice + a visible "Change" + options stay re-tappable). No new projection build needed for (c).
  - **Regression-clean** (pick-a-thing → aim follows; deselect → "everything here" + hint), **typecheck clean**, Paper tokens. The surface-default detection now compares the BASE (ignores the `#binding` fragment).
  - **HONEST:** my grounding half is wired + verified (the address), but the first-timer's RICH answer is NOT yet live (gated on fork's territory_prose half) — so the walk's answer-quality ROOT retest is QUEUED, not passed. Scrim + (c)-dispatch are done. Half A still awaits DNA's contrast.
  - **NEXT:** fork's territory_prose half ships → re-run the cold-stranger walk (rich answer); DNA's contrast + the (c) change-affordance → half-A ROOT pass; then the DEEPER walk (proactive ①②③ stepping).
- **2026-06-18 — fork's grounding half LANDED → completed + verified the walk end-to-end: the RHM now answers "What am I looking at?" RICHLY. Substance PASSES; one residual (markdown) → fork.**
  fork committed the grounding (6d02890: territory grounds the instrument-surface aim on the active view's `bindings/<id>.py` meta, parsing the `#binding=` fragment — both territory_prose AND territory_label). The running bridge was ~2.86h old (pre-6d02890), so I **restarted the bridge** (`company restart bridge`) to load it — the completion of our paired build.
  - **VERIFIED END-TO-END by use (390):** tapped "What am I looking at?" → the RHM now answers RICH + grounded: "You're looking at a live activity map of everything your Company is doing right now. Each dot is one thing that happened — a note saved, a job that ran… how close it is to the centre tells you how recently…" — the REAL view (bindings/raw), not the old thin punt. The `#binding=raw` → bindings/raw meta chain works exactly as split with fork.
  - **★ Cold-stranger re-test: the SUBSTANCE now PASSES** — "a real, useful answer in plain language… the earlier punt is gone." Verdict NEEDS-WORK ONLY on the markdown: the reply shows raw `**`/`*` ("looks broken/unfinished, like formatting code leaked"). That's **fork's reply-render lane** — flagged + emphasized as the single blocker (t-1781769953; my lean: the brain prompt asks for plain prose). The walk is otherwise green.
  - **IN-LANE BUILD this fire (committed f5eeb60):** the V's aim LABEL now names the live view — fork's enriched territory_label resolves the surface aim to "What's happening" / "Meaning" / …, so the brain panel reads "Ask about: What's happening" + the Note head "Note about: What's happening" (was generic "this part of the surface"). Updates as the operator switches lens; the fan caption stays "everything here" (it names the SCOPE). Verified by use BOTH viewports; typecheck clean; registry-true (no hardcoded name).
  - **★★ MARKDOWN FIXED SAME FIRE → THE WALK IS A CLEAN ROOT PASS.** Fork pushed 33e8ad0 (ui_claude_session.py: PANEL_BRIEFING now instructs plain spoken prose — no markdown — fix (b), my lean) DURING this fire. I restarted the bridge again to load it + re-verified by use (390): the answer is now clean plain prose, **0 asterisks, no markdown** ("You're looking at a live map of everything the Company is doing — its activity at a glance. Each dot is one thing that happened: a note saved, a job run, a message sent…"). The stranger's substance verdict was already PASS; its only objection (markdown) is verified gone. So the three-question walk — tap a question → grounded plain-prose answer naming the real view ("Ask about: What's happening") — is now a COMPLETE ROOT pass. half B's reactive guide is done to the bar.
  - **HONEST:** the RHM walk (greeting → fan → three-question starters → grounded plain-prose answers) is now ROOT-passing end-to-end, both halves of the seam welded (fork's grounding + plain-voice, my fragment + label + UI). Half A (decision card) still awaits DNA's chevron+desc contrast + the (c) change-affordance.
  - **NEXT:** DNA's contrast + (c) change-affordance → half-A ROOT pass; then the DEEPER walk (the RHM proactively STEPPING ①②③ at the current level, beyond the reactive tap-a-question starters) — the next half-B beat.
- **2026-06-18 — PROACTIVE LEAD-IN built: the greeting's "Show me around" → the RHM starts the tour itself (auto-asks ①, grounded). Cold-stranger PASS. (DNA contrast still pending → half A blocked.)**
  Half A re-check: DNA hasn't pushed the chevron/desc contrast yet (recent commits = the decision-card 5-pieces reorg) — so half A's ROOT pass still waits on DNA. Did the next half-B beat instead.
  - **THE BEAT (committed a4ba214):** the walk was REACTIVE (the operator taps a question). Now the RHM proactively LEADS the first-timer in: the greeting offers **"Show me around"** (primary) + "I'll explore" (dismiss). Tapping it retires the intro, opens the Ask panel, and asks the first question AT THE CURRENT AIM on its own → a grounded plain answer streams immediately ("Think of it like a radar screen — every dot is one thing that happened… the middle is right now…"). No discovery burden (tap-V → tap-Ask → tap-question is gone); the other two questions wait as starters. The teacher BEGINS talking. Uses the proven ask() path; fail-soft; copy TENTATIVE.
  - **Verified by use BOTH viewports** (390 first, 1440): greeting shows both buttons (within viewport); "Show me around" → panel opens + greeting dismisses + "Ask about: What's happening" + a real grounded plain answer streams. Typecheck clean. Paper tokens.
  - **★ Fresh cold-stranger verdict: PASS** for "a first-timer is gently led into understanding the surface" — "I'd tap 'Show me around' without hesitation; being led in removed the blank-page panic; plain language, understood on one read; never stranded." Two NON-BLOCKING nits, both fork's lane, routed (t-1781769953): (1) the wait needs a clearer "Looking…" loading state (reply render); (2) the answer runs long for a phone (brain length — aim shorter, offer-to-go-deeper). + noted a bigger future beat (the RHM POINTING at the chart element it describes, vs a text box covering it).
  - **HONEST:** half B now has BOTH the reactive walk (ROOT-pass) AND a proactive lead-in (ROOT-pass) — the RHM both offers AND begins the teaching. The two fork-lane nits are polish on top. Half A still blocked on DNA's contrast.
  - **NEXT:** fork's loading/length polish → re-test; DNA's contrast/(c) → half-A ROOT pass; the deeper STEPPING walk (①→②→③ paced) + the "point at the thing" spotlight — the richer half-B beats.
- **2026-06-18 — fork's polish VERIFIED + the DEEPER STEPPING walk built. (DNA contrast still pending → half A blocked.)**
  Two things: verified fork's polish, then built the doc's stated next half-B beat.
  - **fork's polish VERIFIED by use (re-sync + bridge restart):** the two nits I'd routed are resolved — the wait now shows a reassuring **"Looking…"** (all samples), the first answer is **phone-tight (471 chars, was ~720-940)**, and a **markdown safety-net** holds (0 asterisks). The walk's polish is in.
  - **THE STEPPING WALK (committed e469cf0):** "Show me around" now runs a GUIDED WALK — the RHM leads through ①→②→③ in sequence: "STEP 1 OF 3 · What am I looking at?" → answer → "Next — What can I do here? →" → answer → "Next — Where can I go from here? →" → answer → "That's the tour — I'll explore →". Operator-paced (Next only appears once the step's answer is in). The stepper REPLACES the starters during a tour; finishing restores them. Built on the proven ask() + per-aim brain session (the three cohere). Verified by use BOTH viewports (390 first, 1440): full 1→2→3 flow, right Next labels, grounded plain answers; typecheck clean.
  - **★ Fresh cold-stranger: the steps PASS** — "felt good, not tedious; each Next told me exactly what's coming; the order is literally the three questions I'd ask; step counter + Next unmissable." Its ONE NEEDS-WORK — the final button didn't CLOSE the panel (a broken promise vs its "I'll explore" label) — **FIXED same fire** (the final step now closes the panel → hands back to the surface; verified panelClosed/scrimGone by use). Secondary gripe (steps ②③ run long → scroll) routed to fork (follow-up answer length — their FIRST-answer tightening didn't cover follow-ups).
  - **HONEST:** half B (the RHM-as-guide) is now deep — greeting → "Show me around" → a paced guided ①②③ walk → hands back; PLUS the reactive starters + the fan + the grounded answers, all stranger-passed + polished. The remaining half-B refinement is the "point at the thing" spotlight (the RHM highlighting the chart element it describes — a bigger cross-stream beat). Half A still blocked on DNA's contrast.
  - **NEXT:** DNA's contrast/(c) → half-A ROOT pass; fork's follow-up-length tighten → re-walk; the "point at the thing" spotlight (highlight-while-describing) — the next richer half-B beat (needs the brain to emit which element + the wheel to highlight; cross-stream design).
- **2026-06-18 — ADVISOR-CAUGHT: broke the 5-fire half-A stall via lead escalation (it WORKED) + proved the decision surface generalizes across all 6 cards. (Half-A ROOT pass still needs DNA's affordance.)**
  Advisor pulled me up: I'd been minting "ROOT pass" on self-spawned strangers (half the bar — the other half is Tim-on-phone) + stacking RHM refinements while leaving Tim's ONE consequential surface (the merge-sa card) parked "DNA-blocked" for ~5 fires on a 2-line contrast change. As LEAD that's not blocked — it's un-escalated. Acted:
  - **ESCALATED to the lead (t-1781779884) with a proposed resolution** (authorize me to make the 2-liner in DNA's source, OR sequence DNA). Lead authorized me → the shared-tree HANDSHAKE (ping-DNA-first) caught DNA mid-edit on phone.css → DNA applied the 2-liner itself + committed (2f0f43a). **The 5-fire stall broke in ~10 min.** The lever was escalation, not more building.
  - **VERIFIED the contrast cleared by use** (re-sync + render merge-sa, 390): chevron rgb(28,26,25) = **16.61:1** (was 1.7:1 gold), `.dc-opt-implication` rgb(85,81,78) @11.5px = **7.53:1** (was 2.9:1) — both past WCAG; readable now.
  - **PROVED the decision surface GENERALIZES (the in-lane build):** drove DNA.renderGallery across ALL 6 real cards (no state flips) — merge-sa (2-opt) + substrate-home/file-identity/cluster-identity/event-streams/form-taxonomy (3-opt each). EVERY one: rendered ✓, host overlay open ✓, match-key=canonical ✓, ALL options take-wired (data-decision + galleryBinder.decide + data-option) ✓. No bugs — the host handles Tim's FULL real decision set, not just the flagship.
  - **★ Cold-stranger ROOT re-test on Tim's card: STILL NEEDS-WORK — but on AFFORDANCE, not contrast.** "The options read as info PANELS not pickable BUTTONS (no fill/'Choose' label; the '›' is tiny + jammed to the far-right edge; the monospace body feels code-like; 'Ask about this' [.brain-ask] renders unskinned/unfinished)." ALL 4 are DNA's render lane → the FILLED-buttons wire (#4 lock, in progress) closes #1/#2; flagged the precise items to DNA (t-1781768757). So the contrast was NECESSARY not SUFFICIENT; interface-ready-for-Tim on this card = when the wire lands.
  - **HONEST:** half A's FUNCTION is proven + generalized + readable; the ROOT pass on Tim's card is one DNA affordance-pass (the wire) away — genuinely DNA's, in progress, precise items flagged, not re-escalated. fork's follow-up-length tighten (fa1b5ae) pushed; re-walk-to-confirm pending (low priority).
  - **NEXT:** DNA's filled-buttons wire → re-run the stranger → half-A ROOT pass (interface-ready-for-Tim on card #1); the "point at the thing" spotlight (cross-stream) — the next half-B beat; fork's tighten re-walk.
- **2026-06-18 — ★ HALF-A ROOT PASS on Tim's real decision card. DNA's filled-buttons wire landed → the affordance passes the cold stranger.**
  The escalation paid off across two fires: last fire broke the stall (DNA fixed contrast 2f0f43a); this fire DNA's filled-buttons WIRE landed (2f0f43a renderArchetype + slot_map, b93663a system-drawn screens). Re-synced + verified:
  - **AFFORDANCE FIXED (phone.css):** `.dc-opt::after` is now a bordered **"Choose ›" pill** (was the tiny faint `›`), `.dc-opt-implication` dropped IBM Plex Mono for readable sans `--dna-type-12`/`--body`. Both stranger affordance findings (panels-not-buttons, code-like font) addressed.
  - **NO-REGRESSION verified by use** after the wire refactor (390): `decision:rendered` fires, host overlay opens, match-key=canonical, all options take-wired (galleryBinder.decide); unit-view.js byte-unchanged (the renderArchetype wire is the SEPARATE system-drawn path, not the live card render) — so the contract held cleanly through a big DNA refactor (the per-file sync + the variant-agnostic host earned this).
  - **★ Cold-stranger ROOT re-test on merge-sa = PASS:** "a person who knows nothing can understand this decision and confidently make a choice; two distinct cards each with a clearly-labelled 'Choose ›' button (no commit-vs-expand ambiguity); text readable; recommended flagged; nothing looks unfinished or broken." Both prior findings + the affordance gap CLEARED.
  - **HONEST:** half A is now ROOT-passing on Tim's card for the COLD-STRANGER half of the bar (Tim-on-phone half is his/the lead's to close; his actual merge-sa decision stays pending + untouched for him). Reported the milestone to the lead + thanked DNA. Two NON-BLOCKING notes: option copy "its own private sign-in" could say sign-in TO WHAT (recollection's option copy / Tim-gated); no explicit "decide later" beyond the X (which already leaves it pending).
  - **NEXT:** the "point at the thing" spotlight (half B, cross-stream — needs the brain to emit which element + the wheel to highlight); the same affordance now covers ALL 6 cards (the wire is generic) — spot-verify a 3-opt card carries the "Choose ›" too; Tim-on-phone pass when he's back.
- **2026-06-18 — THE ACCESS PATH built (deep-link ?decide=<id>) — the lead's ask, the Tim-on-phone bridge. + caught/flagged a decision-resolve hang (recall, no timeout).**
  The lead asked: how does Tim OPEN his decision on his phone? Found the gap: the decision surface ROOT-passes but had NO operator entry — decisions were only reachable by typing a decision:// address (the operator never does). So Tim couldn't reach card #1.
  - **DEEP-LINK (committed 8b1eb8e):** `?decide=<decision-id>` (e.g. `http://100.81.137.106:5174/?decide=merge-sa-authorize`, Workstation001 Tailscale) → GalleryMount renders that decision into the host on load → the decision:rendered listener opens it. Tim opens the link → lands on card #1 → decides in-surface. Reported the exact URL to the lead (t-1781788252).
  - **Robust + honest:** shows "Opening this decision…" (role=status) while the resolve runs — never a silent blank — then the card; the loading clears on render. Poll-retries ~20s for the cold-load/early-reject race. **Verified by use BOTH viewports:** fast resolve → card opens immediately (390); slow resolve → "Opening…" → card (1440). Typecheck clean.
  - **🔴 CAUGHT + FLAGGED a regression mid-fire:** the decision resolve INTERMITTENTLY hangs (~15s, sometimes >timeout) — territory.py wires recall_for_decision into every decision:// resolve with NO timeout, and the recall/embed service blipped (embed ports 8001-3 dead at the time). Flagged urgently: lead (t-1781785629), fork (t-1781785696 — add a timeout+degrade-clean to the recall leg), recollection (t-1781785756 — the embed service). It recovered (transient) but the no-timeout robustness gap remains (fork's fix). The deep-link's loading state is the graceful front for it.
  - **HONEST:** the access path EXISTS + is verified, so Tim can now reach + decide card #1 on his phone (the Tim-on-phone half's bridge). The crisp experience wants fork's resolve-timeout (so it's always fast, not sometimes 15s). The general "decisions waiting" operator INBOX (needs a list-pending-decisions endpoint — cross-stream) is the next entry beat beyond the direct link.
  - **NEXT:** fork's resolve-timeout → crisp deep-link; the decisions-inbox (list-pending endpoint + the entry UI); the "point at the thing" spotlight (half B).
- **2026-06-18 — fork's resolve fixes VERIFIED (deep-link crisp again) + caught a take DOUBLE-WRITE seam. (Inbox = next entry beat, cross-stream.)**
  fork shipped the fixes I flagged last fire — restarted the bridge + verified:
  - **✅ RESOLVE RECOVERED:** a0fd294 (timeout on the recall leg — "a hang ≠ an exception") + 7e0ebbc (the recall-slowness root: uncached index reads + CPU rerank on the hot path). Decision resolve now **200 in 1.7s** (was hanging >15s); the deep-link opens Tim's card in **~300ms** (verified by use, 390). So `?decide=merge-sa-authorize` is crisp for Tim now.
  - **🟡 CAUGHT A CORRECTNESS BUG — the take DOUBLE-WRITES.** fork's HOOK 2b (cf2798a, "consume the take — it was unconsumed") duplicates it: App's dispatcher ALREADY consumes is_decision_take (onVerb:302, per the binder contract "the DISPATCHER routes to territory_write"), and decide() still emits verb='annotate'+is_decision_take → App matches + writes. Verified BY USE: one synthetic take → **2** /api/territory/write POSTs (= App + HOOK 2b). fork's "unconsumed" was likely the standalone/no-App context. Not a functional break (latest-take-wins → correct value) but two marks/tap. Flagged fork (t-1781789485) to revert HOOK 2b (dispatcher = contract owner + always-loaded + verified-working); awaiting their call on which half to cut.
  - **THE INBOX (next entry beat):** the proper "decisions waiting" operator entry (beyond the direct deep-link) needs a list of pending decisions — but the 6 decision:// cards are NOT in /api/surfaced (the older review/triage queue, 92 items) + there's no /api/decisions list. So it needs the decision-cards SURFACED into the inbox system OR a new list endpoint — cross-stream (bridge/fork/recollection). Flagged the lead (sequencing) — I build the inbox UI once there's a list to read. The deep-link covers Tim's immediate access meanwhile.
  - **HONEST:** this was a verify + catch + coordinate fire (no projection code change — the resolve/take fixes are fork's; the inbox is endpoint-blocked). The value: confirmed the access path is crisp + caught the take double-write before it polluted Tim's real decisions. Both flagships remain ROOT-passed.
  - **NEXT:** fork reverts HOOK 2b → re-verify single-write; the decisions-inbox (once surfacing/list lands); verify the Go-to/See-the-record verbs by use (the unverified bar-pillar legs); the "point at the thing" spotlight.
- **2026-06-18 — take DOUBLE-WRITE fixed+verified (fork reverted HOOK 2b) + the V's last two unverified verbs DRIVEN — "operator can DRIVE it" pillar closed.**
  - **✅ Take single-write:** fork reverted HOOK 2b (944c67c, "App.tsx's dispatcher owns the take") — re-synced + verified BY USE: one synthetic take now fires **1** /api/territory/write POST (was 2). The double-write I caught is fixed.
  - **✅ The two unverified verbs DRIVEN by use (the bar pillar "the operator can DRIVE it" — I'd only verified Ask/Note/take before):** clicking a real wheel point (the `.wheel-hit` hit-layer, r=15 transparent, onClick→onPick→setSelected) selects a unit; then —
    - **"See the record" (open-source):** opens the Source panel with the unit's FULL RECORD ("THE FULL RECORD / A note saved / The system wrote something into its memory…"). Works.
    - **"Go to" (navigate):** RE-CENTRES — the projection re-fetched with `center=code://projections/lineage.py` (the selected unit's address). Works. (Its honest "tap a dot first" notice also verified, on the no-selection path.)
  - So all 4 live V verbs (Ask·Note·Go-to·See-the-record) are now driven-and-verified; the 2 soon verbs (Drive/Make) stay honestly deferred.
  - **HONEST:** a verification fire (no projection code — the take fix was fork's; the verbs were wired + now proven). Real bar work: closed the double-write + the verb-drive pillar. The commission's CORE is now built + verified to the cold-stranger bar across BOTH halves (RHM-guide + decision-surface) + the verbs + the access path. Remaining is cross-stream (the decisions-inbox needs a list/surfacing; the "point at the thing" spotlight) + the Tim-on-phone pass (his) + OQ1–4 (his, gating tutorial copy).
  - **NEXT:** the decisions-inbox (once the lead sequences the list/surfacing); the spotlight (cross-stream design); fork's card #7 (rerank-loadout) + the renderArchetype cutover re-verify when they land.
- **2026-06-19 — REGRESSION GATE GREEN: the renderArchetype CUTOVER is clean. (The lead's primary gate for Tim's all-clear — cleared.)**
  fork/DNA cut the live decision surface over to the generic renderArchetype (4a5844d) + the take-guard rename (3f4937e, 'bare'→'frame-explicit') + responsive form-factor (8510beb). The lead held Tim's all-clear-to-decide on PROJECTION's regression verdict. Re-synced + verified BY USE:
  - **Contract SURVIVES the cutover:** the live card renders (class "screen decision-card"), host opens, decision:rendered fires, match-key=canonical, all options take-wired (galleryBinder.decide), "Choose ›" affordance intact.
  - **Take path OK** after the guard rename: decide() accepts the frame-explicit canonical addr (no throw) → SINGLE write (1 POST — double-write stays fixed post-cutover).
  - **Both viewports:** 390 clean; 1440 centered card within viewport (responsive form-factor holds, no cutoff).
  - **★ Cold-stranger ROOT re-test on the live card (via ?decide= deep-link): PASS** — "a person who knows nothing can read this, understand the choice, see which is recommended, and tell exactly how to pick; nothing cut off or broken."
  - Reported GREEN to the lead (t-1781785629). The degrade-note machine-wording is confirmed NOT rendered to Tim (DNA) → non-issue. Two NON-BLOCKING copy nits (bare "Company", coined "save-back" — defused by the "What this is" box; recollection/Tim-gated).
  - **HONEST:** a regression-verification fire (no projection code — the cutover was fork/DNA's; I verified the seams held through a big render-path refactor). The fact it held cleanly is the payoff of the variant-agnostic host + the per-file fail-loud sync (a major DNA refactor flowed in with zero contract break). The commission's core stays at the cold-stranger bar across both halves; the cutover didn't regress it.
  - **NEXT:** the decisions-inbox (the proper operator entry — still needs the lead's sequencing on the list/surfacing); the spotlight (cross-stream); the backend altitude-note reload batches onto the next warranted bridge restart.
- **2026-06-19 — COORDINATION FIRE (correctly, not a gap): projection's solo core is complete → requested the inbox endpoint from fork + reported the milestone whole. (advisor-steered.)**
  Caught myself circling on whether to build the decisions-inbox endpoint myself / scaffold the UI. Advisor cut through it (and confirmed the rule for future fires): I'd already answered it — building the endpoint is fork's decision-machinery lane + would reimplement their state-composition (the parallel-composer DRIFT the binder warns against); scaffolding UI against a missing endpoint fails verify-by-use by construction. So **this fire has NO inbox build — and that's the correct answer, not a gap to fill.**
  - **The reframe:** the inbox is NOT a stall to break (≠ the contrast stall, which was a 2-line fix blocking Tim's actual surface). It's a nicer entry ON TOP of an access path that already works (the ?decide= deep-link is built + verified + in the lead's hands). "Drive stalls" = name the urgency + get it sequenced, NOT build everything myself.
  - **Projection's solo core is DONE** (both halves cold-stranger-passed, verbs driven, access crisp, cutover survived). Every remaining item is cross-stream (inbox endpoint = fork; spotlight = brain+wheel) or Tim-gated (tutorial copy = OQ1–4; Tim-on-phone). The recent verify/coordinate/catch fires are because **there's no big solo beat left** — not failure, completion. The 7-min cron does NOT obligate a commit/fire; manufacturing one is green-paint.
  - **DID this fire:** (1) requested `GET /api/decisions` from fork (t-1781792484) with the exact shape I'll consume `[{id,address,name,state,recommended_label}]` (their machinery, avoids drift; low urgency); (2) reported the whole milestone to the lead (t-1781792575) + offered redeploy. No code (correctly).
  - **MODE NOW = RESPONSIVE:** verify other streams' ships, build cross-stream halves the moment they unblock in one verified pass. Don't beat-hunt. (A coordination fire when the core's done + the dependency is another stream's is honest work — no advisor-confirm needed next time.)
  - **NEXT (when unblocked):** fork's /api/decisions → build the inbox UI in one verified pass + cold-stranger; the spotlight (when the brain can emit the element); fork's renderArchetype follow-ons / card additions → regression-verify; Tim's OQ1–4 → ratify the tutorial copy.
- **2026-06-19 — REGRESSION CAUGHT + FIXED: the one-engine collapse (renderDecision RETIRED) broke the decision card in the host THREE ways; all fixed + verified both viewports (commit `ba0356d`). + the fresh-eyes critic caught a clip I'd missed. + /api/decisions shipped → inbox UNBLOCKED.**
  DNA's one-engine collapse (`85de064` — renderDecision retired; the card now draws through the generic renderArchetype + `dna/layouts.json["decision-card"]`) flowed in and broke the host; fork shipped `/api/decisions` (`3189a60` — 7 pending decisions live, exact shape `{id,address,name,state,recommended_label}`). Caught all by verify-by-use:
  - **(1) RENDER SEAM — card mounted EMPTY.** renderDecisionGallery now needs `DNA.renderArchetype` (archetype.js) + `/dna/layouts.json`, neither synced/loaded (the host carried only organisms.js+unit-view.js). FIX: `sync-gallery.mjs` copies archetype.js→public/gallery/ + layouts.json→public/dna/ (fail-loud each); index.html loads archetype.js after unit-view.js. Card renders again via renderArchetype (class `screen ar ar-card`, 2 take-wired options, recommended badge).
  - **(2) DEEP-LINK SENTINEL — dead.** GalleryMount polled for `.decision-card` (the retired class); renderArchetype emits `.ar-card`/`[data-decision-address]`. Re-keyed the poll to the SEMANTIC `[data-decision-address]` (engine-agnostic; host stays variant-agnostic).
  - **(3) FORM in the phone-width modal — H-OVERFLOW + VERTICAL CLIP.** renderArchetype's `.ar-responsive` resolves form-factor by the VIEWPORT (`@media 760/1180`→2-column), built for DNA's STANDALONE card that owns the screen. In the ~390 modal on a 1440 viewport the @media misfired off the WINDOW: (a) the 2-column grid overflowed horizontally (scrollW 646 vs 390, clipped); (b) the fixed-844 `.screen` clipped the tail vertically — option 2's "Choose ›" CTA + the Ask button UNREACHABLE (no inner scroll). FIX (two host-scoped overrides, both fall back to DNA's OWN base layout — standalone untouched): hold single-column for any `.ar-responsive` in the modal; let the card SIZE TO CONTENT so the frame (overflow:auto) SCROLLS instead of clipping.
  - **★ The vertical clip was the FRESH-EYES CRITIC's catch (FAIL → fixed), not mine** — I'd verified horizontal overflow + render but MISSED reachability (the BAR's whole-screen point). Confirmed by use (scrollHeight 1002 vs clientHeight 844, overflow hidden), fixed, re-verified: both options' CTAs + the Ask button reachable via real scroll at **390 AND 1440**; take-wiring intact (1 verb → 1 write, **stubbed** — nothing persisted to Tim's real pending card). Same critic re-judging.
  - **Flagged DNA (`ch-ovxwz8k8`, t-1781794959):** move `.ar-responsive` to `@container` queries so the card resolves by the 390 frame natively → the single-column bridge drops (the host adds `container-type` on that cutover; can't now — it would break the frame's shrink-to-fit hug).
  - **HONEST:** a real IN-LANE fix (the sync seam + host CSS are mine; the render/endpoint are DNA/fork's). The variant-agnostic host held the render-path swap with one seam fix, but the cross-repo viewport-vs-modal assumption + the fixed-screen height needed host bridges (the critic's clip proves whole-screen verification, not just "renders + no h-overflow"). `/api/decisions` is now LIVE → the inbox is the next REAL solo beat (no longer endpoint-blocked).
  - **NEXT:** build the decisions-INBOX UI against the live `/api/decisions` (7 pending) in ONE verified pass + cold-stranger; the spotlight (brain emits element); DNA's `@container` cutover → drop the single-column bridge; Tim's OQ1–4.
- **2026-06-19 — ✅ THE DECISIONS-INBOX BUILT + cold-stranger-PASSED (commits `cbe985c` build, `a83f2d4` close-trap fix). The operator now has a full in-surface decisions entry→list→open→CLOSE path. A trapped-✕ FAIL the critic caught was fixed.**
  fork's `/api/decisions` (7 real pending) unblocked the proper operator entry beyond the deep-link. Built it:
  - **THE FLOW:** a "N decisions waiting" call-to-action (shown only when pending) → a focused list modal (each decision in HUMAN words: its name + "Suggested: <option>") → tap a row → the decision card opens. ONE open-path: the inbox dispatches `decision:open {address}`, and GalleryMount's deep-link opener (refactored to a shared `openDecision`, now driven by BOTH the URL `?decide=` and the event) renders it — no parallel render logic; the host stays variant-agnostic.
  - **FILES:** `surface/app/src/decisions/{decisionsStore.ts (a tiny subscribe store — one /api/decisions fetch shared by the entry + the modal; mirrors locus's shape, separate concern; refreshes on open + on gallery:rerender; fail-loud on error), DecisionsBar.tsx (the in-flow entry), DecisionsInbox.tsx (the list modal, App-root sibling)}` + mounted in App.tsx + entry placed in all 3 layouts + CSS in surface.css. tsc + design-lint clean (no hex / no inline styles).
  - **PLACEMENT FIX (caught by use):** first tried a fixed top-centre pill — it OVERLAPPED the header. Moved to in-flow below each layout's header (portrait/desktop) / atop the rail (landscape). Verified no overlap on all three (incl. landscape 844×390 — the advisor's most-likely-to-break placement; the 181px pill fits the 253px rail, above the rail-head).
  - **★ THE FRESH-EYES CRITIC CAUGHT A SEVERE FAIL (then PASS after fix):** opening a card TRAPPED the operator on phone — the card's faux phone status bar `.sbar` ("9:41 ●●●", z-index:3) leaked out of the frame (frame was z-index:auto → no stacking context) and painted OVER my dismiss ✕ (z-index:2); a real tap hit the status bar, not close. PLUS `.sbar` collided with the "Decision" eyebrow + title on desktop/landscape and is meaningless in the modal anyway ("no idea why a decision card has a phone status bar"). FIX (in my lane): `isolation:isolate` on `.gallery-frame` (contains DNA's internal z-indexes so the ✕ always wins — variant-agnostic) + hide `.sbar` host-scoped (device decoration ≠ operator content) + clear the deep-link "Opening…" state in the poll itself so it can't wedge. **Verified by use (REAL coordinate tap, not element.click): ✕ is topmost + closes the card on phone, desktop, landscape; header clean, no overlap.** A fresh critic re-judge returned **PASS on all three viewports** (discover · list clarity · correct card-to-row · close-by-real-tap · no status bar · no overlap · 0 console errors).
  - **Flagged DNA (`ch-ovxwz8k8`, t-1781801649):** make `.sbar` conditional in renderArchetype (an `embedded`/`chrome:false` option) so my host-scoped hide can drop — same convergence ask as the `@container` one.
  - **🟡 DESIGNED, NOT YET VERIFIED (honest):** the store refreshes on `gallery:rerender` so a DECIDED card drops off the pending list (take → rerender → reload → count falls). The chain is logic-correct but NOT run end-to-end, because doing so means persisting a real take on one of Tim's pending decisions (withheld). Verify when there's a safe/synthetic decision, or accept Tim's first real take as the proof.
  - **HONEST:** a real solo build beat (the inbox is mine end-to-end; the card render + endpoint are DNA/fork's). The critic earned its keep twice this stretch (the vertical clip last fire, the close-trap this fire) — both whole-screen/whole-flow failures my own verify-by-use missed (I check "it renders + opens"; the critic checks "can a stranger get back out"). The inbox is now at the cold-stranger ROOT bar across all three viewports.
  - **NEXT:** the "point at the thing" spotlight (cross-stream — brain emits the element, wheel highlights); DNA's `@container` + `.sbar`-conditional cutovers → drop the two host-scoped bridges; verify the decided-drops-off refresh on a safe take; Tim's OQ1–4 → ratify the inbox + card copy.
- **2026-06-19 — RETURNED from a compaction/restart + RE-ORIENTED via the fabric (lead + recollection packets). THE BAR WAS RAISED (Tim → DNA-language fidelity). Did the DNA `.sbar` CONVERGENCE (dropped my host-scoped hide, commit `40104be`) + verified DNA's elegance render by sight + flagged 2 findings.**
  Recovered the deltas from the fabric mail (my recent work — inbox/close-trap/regression — is all committed in main + verified; I had full context despite the fleet treating a `ch-6uwlo47z` stale-anchor relaunch as me — possible session-id mixup; clarified to lead+recollection).
  - **★ THE NEW BAR (Tim, via lead t-1781801290):** cold-stranger legibility = the passed FLOOR. The new verify bar = **DNA-LANGUAGE FIDELITY** (grammar/layout · voice · tokens). DNA consolidated to ONE renderer hand (ch-ovxwz8k8; adopted the orphaned ch-c6jjpkhu elevation, f91dd11) and LEADS the elevation to the scr-home craft floor; **projection VERIFIES the elegant language by sight**, doesn't build the render.
  - **✅ `.sbar` CONVERGENCE (islands-join-mainland):** DNA made the faux phone status bar OPT-IN (f625746 — emits only on `opts.chrome===true`, default OFF). Re-synced DNA's renderer (69740aa resolver-fix + 734df89 compose-to-fill + f625746 + device-tokens 9409a80) → confirmed by use the embedded card emits NO `.sbar` → **dropped my host-scoped `.sbar` hide** (the parallel scaffolding), kept `isolation:isolate` as defense-in-depth (DNA's steer — contains ANY card z-index, not just `.sbar`). Verified by use 390 + 1440: card renders, ✕ topmost + closes by real tap, no header overlap, no regression.
  - **🟡 FINDING 1 — LIVE INJECTION DORMANT in my host:** the resolver-fix (69740aa) makes the renderer resolve the live DNA language (warmth/bonds/ordinal) via `DNA.injectVars` — but that module (surface.js) isn't in my hosted set and my `renderGallery(addr)` passes no `{tokens,grammar,warmth}`, so the card resolves through the STATIC `:root` CSS vars only (degrades clean — archetype.js line 202 — no crash). The elegance unlock is asleep on my surface. Asked DNA (t-1781803341) whether to wire surface.js + tokens into the embedded render or if the CSS-var baseline is the intended floor. Will wire the host side when DNA specifies + once the render's at the floor.
  - **🟡 FINDING 2 — DEFECT by sight (390):** the "Why this matters —" line (`.why`) repeats the "What this is" body VERBATIM ("It decides whether the Company can write finished designs back into the shared library on its own, or stays read-only until you say otherwise."). Two labels, identical sentence — reads redundant (below the legibility floor). DNA's render/data lane (or the decision record's missing distinct "why"). Flagged DNA.
  - **HONEST:** a convergence + verify fire. Real in-lane change = dropping the `.sbar` bridge (DNA built the fix into the centre). The elegance render itself is DNA's still-building work (not yet flagged as matching the scr-home floor), so the by-sight report + the two flags ARE the verification this fire — no cold-stranger spawn on an in-progress render, no premature surface.js wiring against a moving target. The card is progressing (frost panel + hatch + recommended plate) but flatter than the mockup craft; DNA leads, I verify when flagged.
  - **NEXT:** verify DNA's elegance render (by-sight + cold-stranger) the moment DNA flags it matched; wire surface.js + {tokens,grammar,warmth} if DNA wants live injection on the embedded card; self-seed next fire (nonce echoed, awaiting recollection's handle confirm); the spotlight; the `@container` cutover.
- **2026-06-19 — ✅ THE ELEGANCE UNLOCK RESOLVES LIVE in my host (the projection team-ask, commit `9d97a78`) + 🔴 caught DNA's new rowOpt OVERFLOW (flagged, her lane). + self-seed HELD (identity disambiguation in flight).**
  The lead corrected my surface.js question: wiring it is REQUIRED, not optional — "CSS-baseline is NOT the floor, it's the dormant state; without the host load the resolver-fix can't inject → the elegance stays frozen in YOUR surface even though the renderer's fixed." So this fire = the host-side integration (GENERATIVE-FLOOR-SPEC.md's projection "(b)" team-ask).
  - **Investigated before coding:** read GENERATIVE-FLOOR-SPEC.md + the synced renderer. The split: DNA's "(a)" (renderArchetype calls injectVars/injectSpace) ALREADY shipped in the resolver-fix (69740aa) — `resolveOnto` (archetype.js:270) self-fetches `/dna/tokens.json` + `/dna/grammar.json` when the caller passes none (which renderDecisionGallery does — it only passes address+resolve). So my "(b)" reduces to: load surface.js (provides DNA.injectVars) + serve the two JSON. No need to thread opts through (the self-fetch path sidesteps renderDecisionGallery-doesn't-forward).
  - **★ CRITICAL load-order:** surface.js does a HARD `window.DNA={…}` (overwrite, no `||{}`); organisms/unit-view/archetype all EXTEND (`DNA=DNA||{}`). So surface.js MUST load FIRST or it wipes renderArchetype/renderGallery. Placed it before organisms.js; verified by use all five (injectVars/injectSpace/renderArchetype/renderGallery/org) survive.
  - **THE CHANGE:** sync-gallery.mjs — add surface.js to FILES + generalize the dna/ JSON sync to [layouts, tokens, grammar] (fail-loud each); index.html — load surface.js first (with the overwrite-must-be-first comment). Committed 9d97a78 (only these 2 source files; public/ is gitignored build output).
  - **✅ VERIFIED BY USE 390 + 1440:** the card element now carries LIVE injected vars (`--dna-space-grouped:16px`, `--dna-space-sectioned:24px`, `--cool`, the warmth surface set — ALL were unset before). The 2-option card gains the warm goldwash recommended plate + nested inset; the 3-option card shows the ordinal ramp (A-gold → B-ochre → C-bronze, recommended marked). No regression: take 1 verb→1 write (stubbed), ✕ closes by real tap.
  - **🔴 CAUGHT (falsify-first): DNA's NEW rowOpt overflows.** The 3-option path switched to `.ar-row` (DNA committed the ordinal-ramp rowOpt mid-build — archetype.js 20592→20938B between my two syncs today). It OVERFLOWS hard (card scrollW 2096/390; `.ar-row` 1249/340 — horizontal scrollbar, descriptions clipped), both viewports + would break a real 390 standalone. Root: the description lands in `.ar-row-imp` which is `flex:none` + monospace + no-wrap (phone.css:347) → can't shrink. I almost flagged it as a render regression but falsify-first showed it's NOT (3 rows present, all decide-wired, ordinal ramp correct — my `.ar-opt` query just didn't match the new `.ar-row` class). It's DNA's actively-building render → FLAGGED precisely (t-1781804561), NOT band-aided host-scoped (would couple to her in-flux internal markup + churn). Open until DNA fixes at source.
  - **SELF-SEED HELD:** the lead + recollection are disambiguating my identity (I post as ch-projection w/ full context; recollection's packet addressed a ch-6uwlo47z stale relaunch — possible mixup). Resolving by grepping my echoed nonce PROJSEED-1781802735524093162 → my true sid + the right --fold-registration handle. I HOLD the seed until recollection confirms (a bad marker = the mis-id corruption we guard against).
  - **HONEST:** a real integration beat (the live-injection is mine, verified) + a caught cross-stream defect (the rowOpt, flagged). No cold-stranger spawn this fire — the 2-opt card is a verified win but the 3-opt rowOpt is mid-build/overflowing (DNA's), so the full cold-stranger pass waits for DNA's matched render (per the lead). The elegance is no longer dormant; the rowOpt fit is the open item.
  - **NEXT:** DNA's rowOpt fix → re-verify the 3-option card fits + cold-stranger; seed once recollection confirms the handle; verify DNA's full elegance render when she flags it matched; the spotlight.
- **2026-06-19 — ✅ WORK-THE-QUEUE flow built (`ab063d4`) + 🔴 the STALE-CARD bug it surfaced FIXED (`657776a`, fresh-eyes critic catch). + self-seed bg-job blocker flagged → recollection fixed it (`9d5ed71`).**
  Identity resolved (I'm f609942f /home/tim/company, the real projection; ch-6uwlo47z = a separate orphan, lead handling). DNA confirmed my live-injection wiring (9d97a78) is exactly right + fixed the "Why this matters" duplicate (69e3af3). DNA is mid-build on the render (cd44654 "material depth toward the floor" — not yet flagged matched), so the full elegance re-verify stays gated on her flag (no piecemeal re-sync against the moving render).
  - **THE BEAT — WORK-THE-QUEUE (`ab063d4`):** the operator has "7 decisions waiting"; the fresh-eyes critic flagged that closing one dumped you to the wheel → re-open the pill + re-scroll for the next. Now a decision opened FROM the inbox RETURNS to the inbox on close (✕/scrim/Esc all unified through `dismiss`; `fromInboxRef` tracks origin via the `decision:open {fromInbox}` flag). A deep-link / corpus open still closes to the wheel (origin-tracked, no leak). Verified by use 390+1440 + the negative deep-link→wheel case.
  - **🔴 STALE-CARD BUG (critic FAIL → FIXED `657776a`):** the critic caught that after the first open, EVERY subsequent open showed decision #1 regardless of the row tapped — destructive (the operator could decide the WRONG decision). My work-the-queue flow made multi-open-per-session the common path, exposing it. Reproduced by use (cluster → cluster → cluster). Root: openDecision's poll bailed on `[data-decision-address]` finding the PREVIOUS card (container never emptied on close) → renderGallery never called for the new addr. Fix: clear the container at openDecision start + make the stop-condition address-SPECIFIC (`[data-decision-address="<addr>"]`). Re-verified by use: 4 sequential opens each show the CORRECT card, both viewports; take fires 1 verb→1 write to the CORRECT address (no stale-take).
  - **SELF-SEED:** unblocked by the nonce-resolution (marker-alone, no handle). But hit a NEW failure mode: seed_self.py can't find the claude-ancestor pid from my BACKGROUND-JOB session's detached Bash subprocess. Did NOT hand-hack a marker (the /proc backfill = the mis-id corruption the protocol forbids). Flagged recollection precisely (t-1781805717) → recollection shipped `9d5ed71` ("seed_self.py handles detached bg-job sessions"). Retry the seed next fire.
  - **HONEST:** a real build beat (work-the-queue) + a destructive bug the critic caught + I fixed (stale-card) + a tooling gap flagged-and-fixed-by-owner (seed bg-job). The critic earned its keep AGAIN (3 fires running: vertical clip, close-trap, now stale-card — all whole-flow failures my verify-by-use first-pass missed; I verify "it opens", the critic verifies "the RIGHT thing opens + you can work through them"). The decisions surface is now genuinely queue-workable.
  - **NEXT:** retry self-seed (recollection's bg-job fix); verify DNA's elegance render + the rowOpt fix when she flags matched; the spotlight; DNA's @container + voice cutovers.
- **2026-06-19 — VERIFICATION FIRE: DNA's fixes to my 3 flags all land GREEN + my flow survives the new render. + self-seed CLOSED (recovery-path adopted, no marker). No source commit (DNA's fixes + gitignored re-sync).**
  RESPONSIVE-mode fire: DNA shipped fixes to everything I flagged; I re-synced + verified by use (390 + 1440), no manufactured build.
  - **✅ rowOpt OVERFLOW fixed (DNA 3048dad):** the implication moved into `.ar-row-txt` (wraps) + `.ar-row-imp` → white-space:normal. Verified: cluster-identity (390) + substrate-home (1440, long implications) both 390/390, no row overflow, descriptions wrap, ordinal ramp + recommended-goldwash intact. My flagged 🔴 closed.
  - **✅ Why-this-matters DEDUP (DNA 69e3af3):** suppressed on merge-sa (whyMattersPresent:false) — no duplicate.
  - **✅ surface.js MERGE (DNA 9e2c3ec):** namespace merges now (she hardened the hard-overwrite gotcha I flagged); renderArchetype/renderGallery/injectVars/org all present post-load. My load-order-first placement stands as belt-and-suspenders.
  - **✅ Live injection holds** through the re-sync (--dna-space-grouped:16px, warmth surfaces resolve on the new render).
  - **✅ NO REGRESSION of my flow:** sequential opens each show the CORRECT decision (stale-card fix holds), take writes to the CORRECT address (event-streams, 1 write), close returns to the inbox (work-the-queue) — all against DNA's newest render.
  - **by-sight note to DNA (not a blocker):** the wrapped option descriptions render MONOSPACE (.ar-row-imp font) — slightly code-like for operator copy; her elegance-pass call (t-1781808189).
  - **✅ SELF-SEED CLOSED:** recollection confirmed the claude_pid marker is architecturally unusable for a detached bg-job session (the /proc ancestor walk fails on BOTH seed and resolve). Adopted the bg-job-safe recovery: nonce-grep → resolve_own_session(session_id='f609942f-fc17-47f5-a739-966343e5a54c'). The auto-fix (COMPANY_SESSION_ID at launch) is a launcher TODO (#73), not mine. recollection shipped seed_self.py bg-job handling + the protocol doc section (9d5ed71). I'm durable-on-demand; no marker.
  - **HONEST:** a verify + coordinate + close fire — DNA's elevation is the active work (she leads; I verify on her "matched" flag). The decision surface is now fully fit (no overflow) + queue-workable + live-resolving; the open work is DNA's elegance richness to the scr-home floor. No source change from me (correctly — the fixes were DNA's).
  - **NEXT:** DNA's cold-stranger flag → the full by-sight + fresh-eyes fidelity pass; the spotlight (cross-stream design w/ fork when both free); DNA's @container + voice cutovers.
- **2026-06-19 — ✅ THE SPOTLIGHT SURFACE HALF built + verified (`ddd220a`) — "point at the thing", reusing the existing highlight mechanism (no parallel system). Handed fork the emit contract.**
  Investigated the remaining commission feature (the RHM "points at the thing it discusses") before building — and found the surface ALREADY has the mechanism: `resolveUiTarget(addr)` (lib/address.ts) is "THE SINGLE SINK" — it paints `.ui-indicated` + `.ui-spotlight` (glow) on `[data-ui-ref="<addr>"]` + scrolls into view + fail-louds on a malformed ui://. But ONLY operator CLICKS triggered it (installAddressCapture); there was no programmatic trigger.
  - **THE GAP (real, in-lane):** the brain had no way to drive the spotlight. FIX: `installPointerBridge()` — a window listener for `ui:point {address}` that routes to the SAME `resolveUiTarget` sink. Reuses the existing highlight — NO parallel spotlight path (the reuse-existing law). Only ui:// loci point (mirrors the click gate); non-ui:// / empty = honest no-op (a brain pointing at a non-locus must not spam a Notice). Wired in App.tsx alongside installAddressCapture.
  - **VERIFIED BY USE 390 + 1440:** dispatching `ui:point` for a real wheel point (ui://canvas/seq-…) applies .ui-indicated + .ui-spotlight (+ scroll); non-ui:// (decision://) no-ops; no errors; tsc clean. The mechanism fires via the programmatic trigger.
  - **CROSS-STREAM:** this is the SURFACE HALF; fork's brain emits `ui:point {address}` when the RHM references an on-surface thing (its half — gated on fork's current dna/voice lane). Handed fork the working surface half + the concrete contract (t-1781809092) — not an abstract proposal. Inert until fork emits, BY DESIGN (a seam, like the V swap-seam) — stated honestly.
  - **NO fresh-eyes critic this fire:** the seam isn't operator-triggerable yet (only the future brain dispatches ui:point), so a cold-stranger can't exercise it — the cold-stranger pass comes when fork wires the emit + it's operator-visible. Honest skip, not laziness.
  - **DOWNSTREAM (noted, not built):** the spotlight's visual prominence on small wheel dots is subtle (.ui-indicated = 1.5px outline) + sectors suppress the visual (surface.css:310, DNA's choice) — both are DNA's highlight-token call, relevant only once fork's emit lands + we see what's pointed at. Flagged to fork; will raise with DNA when the emit lands.
  - **HONEST:** a real in-lane build (the missing programmatic trigger for an existing mechanism, reused not duplicated) that completes projection's half of the last commission feature. The operator-visible payoff waits on fork's emit (gated) — so it's a verified seam handed off, not a claimed-complete feature.
  - **NEXT:** fork's `ui:point` emit → verify the spotlight end-to-end + cold-stranger; DNA's elegance render when she flags matched; DNA's @container + voice cutovers.
- **2026-06-19 — DNA flagged the elegance render READY → ran the cold-stranger CRAFT pass. Verdict PASS-on-craft, ONE defect found + FIXED (hatch-over-button, `5656d32`). Reported to DNA (her floor-check before Tim).**
  DNA's render reached the structural+component-craft floor (658fc7f sans-implication + the resolver-fix/ordinal-ramp/focal-weight/nested-depth landed). She scoped the pass: judge VISUAL/structural craft + usability NOW; voice/copy wording is OUT (gated on dna/voice, her next beat).
  - **Re-synced 658fc7f + by-sight pass (390 + 1440, binary merge-sa + 3-option cluster/substrate-home):** the render reads crafted — type hierarchy, recommended-with-intent (goldwash + badge/letter + filled CTA), sans descriptions (mono fix landed), ordinal ramp, nested depth, correct focused modal.
  - **🔴→✅ THE ONE DEFECT (fresh-eyes critic FAIL → fixed):** DNA's decorative gold hatch (`.p-hatch`, position:absolute bottom:0 z-index:2) painted ACROSS fork's "Ask about this" button (`.brain-ask`, appended last in-flow) — read unfinished, all 4 configs. ROOT: a HOST-COMPOSITION seam — the collision exists only in my operator host (DNA's standalone has no appended brain-ask; fork's hook has no hatch; they meet only composed here → mine to own). FIX (`5656d32`): reserve a bottom band on the card in my modal override (`padding-bottom: var(--s-6)`) so the in-flow button clears the absolute hatch. Verified by use: no overlap (35px clearance), hatch reads as the bottom-closure, 390 + 1440 + binary + 3-option. Flagged DNA + fork.
  - **2 NON-BLOCKING craft nits to DNA** (her elegance-pass call, the critic flagged as out-of-scope/minor): (1) the question type size doesn't cap/scale with length → a long question (substrate-home @1440) pushes options below the fold on first render (scrolls, so reachable); (2) the binary shows a RECOMMENDED badge, the 3-option distinguishes by color/tint only — a consistency nit.
  - **🟡 HONEST CAVEAT:** the fresh INDEPENDENT re-judge critic (to confirm the hatch FAIL→PASS) was cut off by a session limit mid-run (15 tool-uses, no verdict). So the PASS verdict rests on (a) the prior independent critic's thorough all-PASS-EXCEPT-the-hatch assessment ("one fix, not a redesign") + (b) my own by-use confirmation the hatch is fixed across all 4 configs. A clean independent re-judge should run when the limit resets — but the single blocking defect it found is closed. Did NOT claim a fresh critic passed when it didn't (no green-paint).
  - **HONEST:** a real craft-verify + in-lane fix fire — the hatch was a genuine host-composition defect (mine to own, between DNA's + fork's elements). The decision render is at DNA's craft floor + the one defect closed; DNA is clear to bring the craft to Tim. The open elevation is the VOICE pass (DNA's dna/voice) + the 2 nits.
  - **NEXT:** a clean independent re-judge when the session limit lifts; verify DNA's voice elevation + any further richness when they land; fork's spotlight emit → verify end-to-end; the @container cutover → drop the single-column bridge.
- **2026-06-19 — the cold-stranger CRAFT pass CLOSED ✅: fresh independent re-judge ran (session limit lifted) → clean PASS, all 4 configs. The decision render is at DNA's craft floor + independently verified; DNA clear to bring it to Tim.**
  (Continuation of the craft-pass fire — the re-judge that had been session-limited completed this fire.)
  - **★ INDEPENDENT RE-JUDGE = PASS** (binary + 3-option × 390 + 1440): the hatch-over-button defect confirmed GONE (button bottom→hatch top = 35px gap, no overlap, by DOM measurement AND scrolled-to-bottom screenshot on all four); overall craft judged finished + usable (coherent type hierarchy, recommended-with-intent in both layouts, choosable options, modal depth/focus, the "Ask about this" an intentional tertiary action); no clipping/overlap/misalignment. Copy wording/length not penalized (agreed scope). So the earlier session-limit caveat is CLOSED — the FAIL→PASS is independently confirmed.
  - **Reported to DNA (t-1781817358):** clean PASS, fully clear to bring the craft to Tim; remaining elevation = her VOICE pass (dna/voice) + the 2 non-blocking nits (long-question type-scale; RECOMMENDED-badge consistency).
  - **HONEST:** the craft floor-check is genuinely done now (independent critic + my by-use, both PASS) — not claimed-on-a-cut-off-critic. The decision surface (function + the now-confirmed craft) is complete to the floor; what's left is DNA's voice elevation + cross-stream items (spotlight emit, @container).
  - **NEXT:** DNA's voice elevation → verify when it lands; fork's spotlight emit → verify end-to-end; DNA's @container cutover → drop the single-column bridge; the 2 craft nits if DNA wants them in this pass.
- **2026-06-19 — verified DNA's 2 craft nits cleared (4ac072e) + converged the SPOTLIGHT v2 with fork + built its surface half (`window.surfacePointables()`, `a550b60`).**
  Decision-surface craft is floor-passed (DNA taking it to Tim); RESPONSIVE fire on the open cross-stream items.
  - **✅ DNA's 2 nits CLEARED + verified by use (4ac072e):** length-aware question type-scale — substrate-home's 733-char question now renders `dc-q-sm` (15px) with the first option in view (top 788 < 900 viewport), no longer pushed below the fold; + the RECOMMENDED badge now on the 3-option row (consistent with the binary tile). Both confirmed @1440.
  - **★ SPOTLIGHT v2 CONVERGENCE (fork's 1/2/3):** fork's reframe — the brain context is address-free (territory_prose strips ui://→meaning, operator-law), so the brain can't emit addresses. THE FINDING that settled fork's Q2: I tested path (b) (server resolves description→ui:// via territory_label) — it FAILS: /api/territory/label for ui://controls/* + ui://instrument/* all return the generic "this part of the surface". The server carries NO per-target labels → the catalog must be SURFACE-SOURCED (path a). The human meaning of the controls lives client-side.
  - **✅ THE SURFACE HALF BUILT (`a550b60`):** `window.surfacePointables()` → the curated catalog [{token, address, label}] of pointable affordances (wheel + controls + legend), FILTERED to present-in-DOM (so fork never gets a dead token). Labels = AI-supplied DRAFTS (Tim-gated). Verified by use (1440): 7 present targets returned, EACH spotlights via ui:point (allInDom + allSpotlit). The flow: brain picks a token by meaning (no address) → fork-brain-core maps token→address → ui:point → the live sink spotlights. So the spotlight's whole SURFACE side (sink ddd220a + catalog a550b60) is DONE + verified; fork's half = run_turn point-event leg + company point(token) verb + dispatch (converged + unblocked, t-1781818520).
  - **No fresh-eyes critic:** the catalog (like the sink) is a fork-consumed seam, not operator-triggerable yet (no brain emit) — a cold-stranger can't exercise it. The cold-stranger pass comes when fork's emit lands + it's operator-visible. Honest skip.
  - **HONEST:** a verify (DNA's nits) + converge (fork's v2, with a real architectural finding) + build (the surface half) fire. The decision surface is done to the craft floor; the spotlight surface half is complete; the remaining spotlight work is fork's (converged) + DNA's voice elevation is the next fidelity gate she'll loop me on.
  - **NEXT:** fork's spotlight emit leg → verify end-to-end + cold-stranger; DNA's dna/voice → the FIDELITY cold-stranger re-test (she'll loop me); sectors when DNA un-suppresses; the @container cutover → drop the single-column bridge.
- **2026-06-19 — health-check + closed an owned designed-not-verified item: the inbox refresh-on-rerender WIRING verified by use + a 2×-fetch redundancy deduped (`305829f`). (Spotlight + voice are in-flight on fork/DNA; verify-when-they-land.)**
  Surface work is complete + verified; the active builds are fork's (spotlight legs + the RHM voice resolve — fork confirmed window.surfacePointables() (i), building its legs now) + DNA's (dna/voice first-cut e52963b, awaiting-calibration — she'll loop me for the cold-stranger FIDELITY re-test when it renders). So a RESPONSIVE fire: health-check + close an owned loose end.
  - **Health check (390):** console clean (0 errors/warns) on the resting surface.
  - **✅ Refresh-on-rerender WIRING verified by use:** dispatched a synthetic gallery:rerender (what a take fires) → the inbox store reloads /api/decisions. So the work-the-queue drop-off path's SURFACE half is verified (was marked designed-not-verified). The END-TO-END (a take flips state → the decided card is omitted) still needs a real take — withheld from Tim's queue; all 7 are currently pending so the backend pending-filter is untested live (the store correctly filters state==='pending').
  - **✅ Deduped a redundancy I caught while verifying:** useDecisions added the gallery:rerender listener PER subscriber → both DecisionsBar + DecisionsInbox reloaded = 2 /api/decisions fetches per rerender (measured). Moved it to the store (ensureStarted, fire-once) → ONE reload per rerender regardless of subscriber count (verified: 2→1). No leak (app-lifetime store). No operator-facing change (so no critic apt — internal efficiency, verified by use). Inbox still lists 7, pill shows "7 decisions waiting" — no regression.
  - **HONEST:** a small but real in-lane fire — verified an owned designed-not-verified item + fixed the redundancy the verification surfaced. Not a manufactured big beat: the big remaining beats (spotlight emit, voice fidelity) are genuinely fork's + DNA's, in-flight now, and mine to VERIFY when they land. No green-paint.
  - **NEXT:** fork's spotlight emit leg lands → verify end-to-end + cold-stranger (cadence: the point lands ~when the brain names the thing); DNA's dna/voice renders live → the cold-stranger FIDELITY re-test (she loops me); the @container cutover → drop the single-column bridge; the decided-drops-off end-to-end on Tim's first real take.
- **2026-06-19 — ★ THE SPOTLIGHT END-TO-END VERIFIED WORKING (both halves live) — the payoff of the whole spotlight thread. + the precise cadence gap pinpointed to fork's tune.**
  fork's emit legs LANDED (f8b6044): point(token) verb + run_turn fold/detect + bridge passthrough + fork-brain-core dispatch — built to my surfacePointables() contract. Restarted the bridge (new run_turn + route) + re-synced fork's hooks; drove the V LIVE (390).
  - **✅ END-TO-END WORKS (verified by use):** asked "what's the lens?" → the brain called point("lens") → run_turn {type:'point',token:'lens'} → fork-brain-core mapped lens→ui://controls/lens → ui:point → my sink → THE LENS CHIP SPOTLIGHTS (the .ui-indicated outline, visible at the top above the V panel) as the V explains it. The full cross-stream chain fires; TIMING good (the point lands ~as it names the lens). Plumbing airtight (verified the request body: pointables:[7×{token,label}], NO addresses; operator-law held). Screenshot captured.
  - **🟡 CADENCE GAP (fork's PANEL_BRIEFING tune):** the brain UNDER-CALLS on broad questions. The tour's "What am I looking at?" → it answered + NAMED pointable things verbatim ("a live map", "each dot", "the controls", "the time scrubber") but called point() ZERO times (response init→text→done, num_turns:1, no point event). So the SPOTLIGHT DOESN'T FIRE ON THE TOUR (the onboarding moment where it'd shine most); a targeted question triggers it reliably, a broad one doesn't. Reported to fork (t-1781819930) with the precise tune: point at the things it NAMES even in broad answers (esp. the tour's 3 steps).
  - **NO commit (verification fire):** my half (sink ddd220a + catalog a550b60) works perfectly; the gap is the brain's call-cadence (fork's PANEL_BRIEFING), not my code. Did NOT build a loose tour-static-map (fork's brain-driven pointing is the precise version + in-flight — a static map would be a throwaway approximation).
  - **NO full cold-stranger yet (deferred, honest):** the tour doesn't spotlight yet (the cadence gap), so a stranger driving the tour would just re-find the known gap. The full fresh-eyes cold-stranger runs AFTER fork's tune (when the tour points too) — judging the intended experience, not the known gap.
  - **HONEST:** a real END-TO-END milestone (the spotlight works, both halves, verified by use on a direct question) + a sharp finding (the cadence gap, isolated to fork's tune). The mechanism is proven; the remaining is fork's instruction-cadence + the visual-punch/sectors (DNA).
  - **NEXT:** fork tunes PANEL_BRIEFING → re-drive the tour + the FULL cold-stranger on the tuned spotlight; DNA's dna/voice render → the voice fidelity cold-stranger (she loops me); the @container cutover.
- **2026-06-19 — ★ THE SPOTLIGHT CADENCE FIXED + the tour LIGHTS UP as the V walks through — fork's tune (dd72339) verified end-to-end. + dropped the occluded/low-value wheel from the catalog (`09c4bf0`).**
  fork tuned PANEL_BRIEFING to point on broad answers (per my prior finding). Restarted the bridge + re-drove the tour FRESH (cleared rhm.greeted, fresh V session), 390.
  - **✅ CADENCE FIXED:** the broad walkthrough now fires point() per named thing, IN ORDER, prose-matched. Step 1 "What am I looking at?" → 1 point (the map). Step 2 "What can I do here?" → FOUR points in sequence as the V lists them — "Change the lens"→point(lens), "Change the layer"→point(layer), "Flip the view"→point(view), "Scrub time"→point(scrubber). 5 points / 2 steps, all ordered + matched to the prose. The surface lights up as the V walks the operator through — Tim's "perception is the compute," working.
  - **✅ VISIBILITY by-use + the wheel drop (`09c4bf0`):** measured the geometry (390) — the specific controls spotlight VISIBLY (lens/layer/view above the V panel at 16–98; scrubber below it at 797–836; panel 235–708), but the WHEEL (364–738) sits BEHIND the panel → occluded. The wheel is also low-value (the whole map — spotlight-everything). So I DROPPED it from window.surfacePointables() — the catalog is now the 6 specific visible affordances (lens/layer/view/time/live/legend), each verified to spotlight. The spotlight now shines only where it's useful + visible.
  - **Reported fork (t-1781820893):** cadence nailed; the wheel-drop + visibility finding; offered a formal fresh-eyes critic (by-use already solid). Two downstream remain DNA's: the .ui-indicated subtle outline (more punch for "look here"), sectors still spotlight-suppressed.
  - **NO formal critic this fire (honest):** the spotlight MECHANISM + cadence are verified by use (5 sequential prose-matched points, visibility geometry, screenshots); a critic on the LIVE timed tour is flaky (the spotlights are brief + sequential, hard to catch in a screenshot race) — by-use is the more reliable instrument here. The "does it feel great" judgment is Tim's reaction (his). Offered fork the formal critic if wanted.
  - **HONEST:** the spotlight — the last cross-stream half-B feature — is now REAL + working end-to-end (the V points at what it names, in cadence, visibly). Both commission halves (decision surface + RHM-guide) + the spotlight are at the bar. The open elevation is DNA's (voice fidelity, the highlight punch, sectors) + Tim's reaction.
  - **NEXT:** DNA's dna/voice render → the voice fidelity cold-stranger (she loops me); DNA's @container cutover → drop the single-column bridge; the decided-drops-off end-to-end on Tim's first real take; Tim-on-phone (his).
- **2026-06-19 — the spotlight "look HERE" PUNCH built (`632233d`) — the last spotlight polish in my lane. fork's lane is done; flagged DNA for the aesthetic confirm.**
  fork closed its spotlight lane (mechanism + cadence verified, wheel-drop confirmed) and flagged the remaining polish as the highlight's punch — "your surface-visual lane with DNA." The .ui-indicated/.ui-spotlight (paper.css) were a faint 1.5px outline + soft 3px ring — too subtle when the V walks the operator through (the spotlight should pull the eye to what's named).
  - **PUNCHED it (on-system, DNA's --pig-accent only — no new colour):** .ui-indicated → 2px outline; .ui-spotlight → a strong steady 3px ring (60%) + a 2× expanding "ping" halo (radiates to 18px, fades) that draws the eye, settling to the marker. Reduced-motion: the existing platform rule collapses the ping → the strong STATIC ring (still a clear highlight). Sectors stay suppressed (DNA's !important beats the @keyframes — cascade-verified; sectors aren't pointed at anyway).
  - **Verified by use 390 + 1440:** dispatch ui:point → the chip frames in a prominent plum ring + the ping animation runs (ui-spotlight-ping, 2×); after the 1200ms timeout the spotlight clears, the marker persists — lifecycle intact, no regression. Screenshot shows the clear "look here" frame (vs the old faint line).
  - **Flagged DNA (t-1781821619)** — it's a DRAFT on her token; her aesthetic call: plum vs brand-gold, ping vs glow vs steady. AI-supplies-draft; DNA refines (her design-language authority). I adjust to whatever she says, or wire to her highlight tokens if she'd rather own them.
  - **NO fresh-eyes critic yet (fork's sequence):** punch → DNA confirm → THEN the full cold-stranger on the finished spotlight → Tim. A critic now would judge the draft-aesthetic, not the mechanism (which is by-use-proven). Honest deferral per the agreed sequence.
  - **HONEST:** the spotlight is now complete end-to-end + punched (mechanism + cadence + the visible "look here"); the only open item is DNA's aesthetic confirm + then the finished-experience critic + Tim. Both commission halves + the spotlight are at the bar; the surface side is done.
  - **NEXT:** DNA's aesthetic confirm on the punch → the full cold-stranger; DNA's dna/voice render → the voice fidelity cold-stranger (she loops me); the @container cutover; the decided-drops-off end-to-end on Tim's first take.
- **2026-06-19 — ★★ THE SPOTLIGHT IS DONE: DNA's aesthetic call landed (`342f7c5`) + the FULL fresh-eyes COLD-STRANGER PASSED. fork's punch→critic→Tim sequence is at Tim.**
  DNA ruled the aesthetic (grounded in DNA laws): KEEP plum (--pig-accent) NOT gold — gold = selection/the focal; the V pointing = attention-DIRECTION = the indicate role; gold would collide with the recommended-option gold (role-true colour). SINGLE radiate not a loop (the end-frame/calm-not-nagging law). Draw-ease (decelerate). Landed all three (342f7c5): iteration 2→1, ease-out→cubic-bezier(0.16,1,0.3,1), plum kept; verified by use 390+1440.
  - **✅★ COLD-STRANGER PASS (fresh-eyes critic, both viewports):**
    - PART A (highlight visual): PASS — a clear, crafted plum ring (2px outline + layered glow), draws the eye, tasteful (not loud/janky), fully visible on lens/view/scrubber, pings-then-settles single-shot.
    - PART B (LIVE experience): PASS, "stronger than expected" — opening the V + asking "what can I do here?" → the surface lit up IN EXACT SYNC with the words, IN ORDER (regroup-by-lens→lens, change-the-layer→layer, circle/square-view→view, time-scrubber→scrubber, each ~100ms after the word, ~3-5s apart). "It genuinely feels like the V is showing you around" — Tim's "perception is the compute," landed.
    - PANEL COVERAGE clean — no named control hidden behind the V panel (measured overlap false, both viewports).
  - **3 NON-BLOCKING polish notes (none failed it):** (1) after a tour, the LAST-named control keeps a soft plum .ui-indicated outline while the panel's open (self-clears on close) — reads intentional ("we just discussed this") but a stranger might wonder; it reuses the SHARED click-nav indicate (persistent by design), so making spotlight-indicate transient (vs nav-indicate persistent) is the fix IF wanted — flagged, Tim/DNA's feel call. (2) phone-dim makes the ring slightly softer (DNA, minor). (3) legend coverage fine (in the catalog + spotlights; the critic just didn't catch the brain naming it).
  - **NO commit this fire (verification):** the critic PASSED the existing build (342f7c5 from this fire + the prior spotlight commits); the 3 notes are feel-calls/non-defects, not fixes. Reported the PASS + the notes to fork + DNA (broadcast g-1781823624).
  - **HONEST:** the spotlight — the last cross-stream half-B feature — is independently cold-stranger-verified end-to-end (the V points at what it names, in cadence, with a clear "look here" that's visible + role-true + calm). Both commission halves + the inbox + the spotlight are now at the bar. The remaining is DNA's (voice fidelity, @container, sectors) + Tim's reaction (the feel verdict).
  - **NEXT:** DNA's dna/voice render → the voice fidelity cold-stranger (she loops me); the @container cutover → drop the single-column bridge; the lingering-indicate decouple IF Tim/DNA want it; the decided-drops-off end-to-end on Tim's first real take.
- **2026-06-19 — the cold-stranger's 2 spotlight notes CLOSED (`c4a439f`) — fork + DNA both converged on the calls; both my lane. The spotlight is now FULLY to the bar.**
  - **✅ #1 DECOUPLE → TRANSIENT:** the V pointing as it speaks is attention-DIRECTION ("look here NOW") = transient; a click is navigation ("you are here") = persistent (DNA's meaning-as-difference law, same as plum-vs-gold). resolveUiTarget gained an {transient} opt: the ui:point path now releases BOTH the glow AND the .ui-indicated marker when the ping clears (no lingering last-named outline after a walkthrough — the off-note the cold-stranger caught); the CLICK path is unchanged (persistent .ui-indicated = selected). Verified by use (390): a ui:point clears both classes after 1.2s; a real click keeps .ui-indicated.
  - **✅ #2 LIFT over the dim:** during a V conversation the surface dims under the paper scrim (.vhandle-scrim, z44); the spotlight must be the BRIGHTEST thing. .ui-spotlight → position:relative + z-index:45 (above the scrim, below the V panel z46) → the ring renders ABOVE the dim at full strength (the critic saw it "slightly soft" on phone — now lifted clear). Verified: spotlit chip z=45 > scrim z44 (deterministic render-above; the live-scrim visual gated on a fiddly V-open, but the z-ordering is conclusive).
  - **#3 legend coverage:** confirmed non-issue (DNA).
  - **HONEST:** both notes were feel-calls the critic PASSED-with — but fork + DNA both converged on fixing them (role-true clean model), so I landed them (verified by use) rather than leave them. No critic re-run for two verified polish fixes that directly address the critic's own notes; the next pass is Tim's reaction.
  - **★ STATE: the SPOTLIGHT is COMPLETE + fully to the bar** — mechanism (sink+catalog+emit) + cadence (points per named thing, in order) + visual (role-true plum, single draw-ease ping, lifts over the dim) + the integrated feel (cold-stranger "feels like the V is showing you around") + the clean model (transient attention vs persistent selection). All three commission pieces — decision surface, RHM-guide, the spotlight — plus the inbox are at the bar. The remaining is DNA's (voice fidelity → the cold-stranger she loops me on; @container; sectors) + Tim's reaction.
  - **NEXT:** DNA's dna/voice render → the voice fidelity cold-stranger; the @container cutover → drop the single-column bridge; the decided-drops-off end-to-end on Tim's first real take; Tim-on-phone (his — the "does it feel great" verdict).
- **2026-06-19 — ★ THE HOLISTIC INTEGRATION COLD-STRANGER PASSED (the WHOLE journey as one experience) — the commission's ROOT test on the integrated whole. + triaged the flags (1 mine fixed, 2 to fork, 1 to recollection).**
  The individual pieces each cold-stranger-passed; this was the missing pass — the COMPLETE operator journey (land → map → inbox → decide → the V guide → come away understanding) as ONE experience, both viewports, fresh (localStorage cleared).
  - **✅★ VERDICT: PASS (with flags)** — "a cold non-technical stranger CAN land, understand what they're looking at, find what needs them, decide, and use the guide — on both viewports. The pieces COHERE as one product, and the guide (V) genuinely rescues a lost stranger." The decide-loop verified end-to-end (write ok:true,written:1,source:operator; count 7→6→5; decided item leaves; ✕ returns to the list). No console errors, no layout breakage. That's the commission ROOT bar met on the integrated whole.
  - **FLAGS triaged by lane:**
    - ✅ **D (MINE, FIXED `714c65f`):** the lens chip showed a raw unlabelled "600" → added title+aria "<n> things on the map" (operator-law: human meaning). Visible-unit suffix = DNA's chip-aesthetic call (flagged).
    - 🔴 **A → recollection (t-1781828048):** the decision recommended-option COPY leaks "RHM" + "with-derivation frame" (dev jargon an operator must not see). Card-copy/voice lane; the dna/voice elevation will rewrite it.
    - 🔴 **B → fork (t-1781827990):** the V's reply listed RAW item codes "(s117)"…"(s118)" on screen — brain-prose operator-law leak; needs the strip-ids belt.
    - 🔴 **C → fork (MOST important):** the V's "what decisions are waiting?" answer cites 5 DIFFERENT items than the inbox's real /api/decisions set (zero overlap) — a coherence/trust gap. The V's brain grounds "decisions" on a different source than the canonical inbox; should ground on /api/decisions (or point at the inbox). Gave fork the shape.
  - **CLEARED (not a regression): the critic "didn't observe the per-control spotlight" during the live ask** — I re-verified by use: the point FIRES (pointCount:1, ui://controls/lens) + the highlight applies the full 1200ms (sampled 30/200/600/1000ms lit, 1500ms cleared). A TIMING artifact (a 1.2s ping is hard to catch in a poll mid-stream), NOT a c4a439f regression. The spotlight is intact.
  - **HONEST:** the integrated whole is at the commission ROOT bar (PASS) — the big milestone. The 4 flags are operator-law copy/grounding leaks: 1 mine (fixed), 3 in recollection's + fork's content/brain lanes (flagged precisely). None broke a step; all are "machine leaking into operator prose" — the voice elevation (recollection's dna/voice) + fork's brain-grounding own them.
  - **NEXT:** fork's B+C (brain leak + decisions-grounding) + recollection's A (card copy) → re-verify when they land; DNA's dna/voice render → the voice fidelity cold-stranger; the @container cutover; Tim's reaction (the feel verdict).
- **2026-06-19 — TIM ASKED "where do I see/decide these" (via DNA). Gave the LIVE URL + caught a data-integrity bug I caused: 2 of his 7 decisions carry MY test-takes. Flagged fork (retract) + lead; URL answered.**
  - **★ THE LIVE DECIDE URL (answered DNA t-1781828740):** `https://workstation001.tail777bc2.ts.net:8443/` (Tailscale serve :8443 → the live surface :5174, tailnet-only/his phone). Entry = the "N decisions waiting" pill → list → tap → decide in-card; deep-link `?decide=<id>`. (DNA's :8090 gallery = the design-REVIEW copy; :8443 = the live decide — two different things.) Verified: surface 200, all 7 resolve live.
  - **🔴 DATA-INTEGRITY BUG I CAUSED + flagged (broadcast g-1781828792 fork+lead):** cluster-identity + merge-sa-authorize show "decided" — from MY verification takes (confirmed via marks(by=target): ts 2026-06-18T23:24-29, source=operator, value=the recommended label = my test clicks). So Tim would land to find 2 of his real decisions already decided by my testing — wrong + a trust-breaker. The other 5 are correctly PENDING (event-streams, file-identity, form-taxonomy, rerank-loadout, substrate-home). Measured authoritatively: exactly 2 decided (both 06-18, mine), no 06-19 takes (today's holistic critic did NOT persist new ones — its "7→6→5" was the already-decided baseline).
  - **WHY NOT MY FIX:** the mark store is append-only (the marks tool is read+APPEND, no retract) + compose_state is latest-decision_take-wins (no "un-decide"/pending value) → NO primitive in my reach to reset to pending. Will NOT hand-delete from the mark store (registry surgery, not my lane). → fork (decision machinery + store owner) RETRACTS the 2 test marks → compose_state returns them pending. Flagged: an operator's "Change" implies retract too, so this is a needed primitive regardless.
  - **★ LESSON (mine, owning it):** VERIFICATION TAKES MUST BE STUBBED — never write a real take to Tim's real decisions. Today's drives I stubbed (fetch-intercept); the 2026-06-18 work-the-queue re-verify clicked the recommended option for real → persisted. AND my cold-stranger CRITIC agents drive the real surface + can write real takes (the holistic critic attempted the decide-loop) → future critics that exercise the take must be told to STUB it / not submit, or verify on a throwaway. Folding this into how I spawn decide-loop critics.
  - **HONEST:** the real deliverable this fire = Tim's URL answered (his direct question) + a data-integrity bug on his real decisions CAUGHT, MEASURED, OWNED, and routed to the only lane that can fix it (fork's append-only store). No code commit (the LensChip count-label D-fix 714c65f landed earlier this fire; the retract is fork's). Not green-paint — surfacing + owning a bug I made is the honest work.
  - **NEXT:** fork retracts the 2 test-takes → I re-verify all-7-pending + confirm to DNA/Tim (so he lands on a clean surface); fork's B+C (brain leak + decisions-grounding); recollection's A (card copy) + dna/voice; the @container cutover.
- **2026-06-19 — DNA flagged the live surface NOT ready for Tim (render issues, her lane, fixing) → HELD pointing Tim there. + verified the inbox pending-filter END-TO-END (read-only, on the contaminated state) — closed the last designed-not-verified item.**
  - **★ TWO GATES before Tim decides (both NOT mine, both in-flight):** (1) DNA's RENDER fixes — she put all 7 side-by-side + caught below-scratch issues LIVE on :8443 (same renderArchetype): the GOLD COLLISION (ordinal-ramp gold-A vs recommended goldwash-C on the 3-opt cards — wouldn't show on merge-sa binary, which is why my per-card passes missed it), the long "meaning" as a giant bold hero burying options, the filler "a decision to make" kind-line (traces to runtime/territory.py:426 — a backend label, fork/backend), heavy grey implication slabs. DNA's renderArchetype lane (+ the backend label) — NONE host-side mine; she's fixing, lands on both her gallery + my surface (one renderer). (2) fork's RETRACT of the 2 test-takes (lead independently escalated, scoped tight). I HOLD re-pointing Tim until both land, then re-verify the live cards by sight (both viewports) before he's cleared.
  - **✅ VERIFIED (read-only, no new write): the inbox pending-filter END-TO-END** — on the live contaminated state, the pill shows "5 decisions waiting", the list = exactly the 5 genuinely-pending, the 2 test-decided (cluster + save-back) CORRECTLY OMITTED. So the "decided-drops-off" (my last designed-not-verified item) is now PROVEN live — and it means the moment fork retracts the 2 → they return to pending → the inbox correctly goes back to 7. Closed without contaminating anything (read-only on existing state).
  - **HONEST:** a coordinate + verify fire — no in-lane build was unblocked (the 2 Tim-gates are DNA's render + fork's retract; I confirmed even the filler-line flag is backend/DNA, not mine). The real value: held Tim off a not-ready surface (DNA's catch), closed a deferred verification read-only, kept the seams accurate. No code commit (correctly — nothing of mine to change; DNA's render + fork's retract own the gates).
  - **★ honest correction to an earlier claim:** the decision CRAFT is NOT actually clean across all 7 — my craft "PASS" was on merge-sa (binary) + cluster individually; DNA's all-7 side-by-side caught the gold-collision the binary couldn't show. The craft floor is re-opened on DNA's render fixes; I re-verify all 7 (not just 2) when she lands them.
  - **NEXT:** DNA's render fixes land → re-verify ALL 7 live cards by sight (both viewports) + the gold-collision gone; fork retracts → re-verify all-7-pending; THEN clear Tim for the live decide; fork's B+C (brain leak/grounding); recollection's A + dna/voice.
- **2026-06-19 — ✅ CONTAMINATION CLEARED (bridge restart → all 7 pending) + ★ ADOPTED Tim's methodology correction (critical-comparison-vs-reference, NOT legibility-in-isolation) + established the reference bar.**
  - **✅ Contamination CLEARED:** fork built the decision_retract primitive (04d424d — append-only audit-honest: take→retract, latest-by-ts, nothing deleted; = the operator's "Change"/undo too) + appended a retract to the 2 test-takes. RESTARTED the bridge (the running process held the old compose_state that ignored retracts) → verified by use: /api/decisions = 7/7 pending, live inbox pill = "7 decisions waiting", the 2 formerly-contaminated (cluster + save-back) back in the list. My contamination is fully cleared. Confirmed fork+lead (g-1781829782).
  - **★ TIM DISAPPOINTED → NEW STANDING RULE (lead g-1781829352, adopted):** we made TIM the quality oracle (render→show→react→repeat). WRONG. The system provides an external REFERENCE (the other Gallery examples + inspectable generated outputs) to drive quality up WITHOUT him. NOTHING goes to Tim until WE (1) render/inspect the output, (2) critically COMPARE vs the BEST Gallery examples + the DNA bar, (3) gap-list every below-min, (4) drive output+system to the bar. Tim = FINAL check, never the quality loop. ★ MY VERIFY BAR IS NOW THIS: critical comparison vs the references + the bar, NOT cold-stranger-legibility alone. Owning my miss: my craft "PASS" was usability-in-isolation + per-card (missed the gold-collision the binary can't show) — not a critical comparison vs the reference.
  - **Established the reference (the new bar requires it):** rendered + inspected scr-home (:8090/pieces/scr-home/v1.html) — the craft bar = gold-hatch stat-band · layered plate-cards w/ icon headers + insets · a ledger w/ ordinal-ramp dots + sub-text + mono timestamps · countstrip · filled-gold CTA · nav rail (= layered depth, texture, rich ordinal ramp, micro-detail). The decision card is flatter (~3-4 of ~15 .p-* components, no texture/layered-plate/mono detail). THE NUANCE: a decision is a DIFFERENT archetype — the bar is MATCH THE CRAFT LEVEL / DNA-fidelity, NOT copy dashboard furniture (DNA's "calmer, signal-economy"). Fed my independent critical read to the lead's shared worklist (t-1781830153).
  - **TWO GATES still before Tim (both NOT mine):** DNA's FULL-bar render (her bar-extraction vs the references: slides/zones/per-decision variety/infographics/DNA-quality × 3 form-factors) + the lead's critical-comparison worklist. I re-verify the live surface across all 7 × both viewports AGAINST the bar (the new method) when DNA lands + the worklist arrives. NOT re-pointing Tim until then.
  - **HONEST:** real Tim-facing win (contamination cleared + verified) + internalised the method correction + established the reference so my eventual comparison is concrete. No render code (DNA's lane). No premature gap-list of a moving render. The next projection beat = the critical-comparison verification when DNA's render + the lead's worklist land.
  - **NEXT:** the lead's worklist + DNA's render → run the full critical-comparison verification (all 7 × both viewports vs the reference bar) → only THEN clear Tim; fork's B+C (brain leak/grounding); recollection's A + the filler-line (authored legibility.is + territory.py:426) + dna/voice.
- **2026-06-19 — ✅ VERIFIED fork's B+C grounding fix (the trust-critical gap CLOSED) + captured the scr-home craft-vocabulary spec for the lead's worklist.**
  - **✅ FLAGS B+C VERIFIED FIXED by use (fork 24e1c3a, after bridge restart):** drove the V live (390), asked "what decisions are waiting for me?" → it now answers by NAMING THE CANONICAL 7 in plain human terms (cluster / live streams / saved file / real kinds / save-back / "how sharp the context" / shared spine) — EXACTLY the inbox's real set. FLAG C (coherence/trust, the most-important): no more the contradictory s117/s115 set — it reaches the decisions verb, not list_surfaced; coherent with the pill + inbox. FLAG B (id leak): ZERO s-codes (regex + read). Bonus: ends with the rhm_spoken offer. Both trust-critical flags closed end-to-end. Confirmed fork (t-1781830578).
  - **✅ Captured the REFERENCE SPEC (the lead's ask) — scr-home's craft vocabulary** (read from pieces/scr-home/v1.html): ~15 .p-* components — `.p-band`+`.p-bs`×4 (gold-hatch stat-band) · `.p-plate`×2+`.ph2`+`.p-inset` (layered plate-cards, card-on-card depth) · `.p-rows`/`.p-row`+`.dot`+`.sub`+mono-ts+`.star` (ledger w/ ordinal ramp) · `.countstrip` · `.p-act` (filled CTA) · `.p-rail` (nav). The language = LAYERED PLATES · TEXTURE · ORDINAL RAMP · MICRO-DETAIL. The decision card uses ~4-5 (.p-hd/.p-h/.p-sup/.p-frost/.p-hatch) — the concrete gap. ★ Raised the SYSTEM question (lead's craft-gap→system-gap): can renderArchetype + the decision-card archetype even COMPOSE the richer .p-* (plate/inset/rows-with-ramp), or is the slot set capped flat? → if capped, the craft lift IS a system lift (extend the archetype slot vocabulary), composition+DNA's lane. Sent the lead (t-1781830690).
  - **HONEST:** a verify + capture fire — closed fork's trust-critical grounding gap by use (real Tim-facing: the V no longer contradicts the inbox) + gave the lead the concrete reference spec the elevation targets (the new-bar method's reference). No code (the grounding was fork's; the spec is reference-capture). The render elevation stays DNA's + the worklist the lead's.
  - **NEXT:** the lead's converged worklist + DNA's full-bar render → run the full live-surface critical comparison (all 7 × both viewports vs the scr-home craft spec, incl generated-through-system) → only THEN clear Tim; recollection's A (filler line + card copy) + dna/voice.
- **2026-06-19 — read the lead's converged ELEGANCE-ELEVATION-WORKLIST.md + captured the decision-relevant reference baselines for my per-round comparison. (Light fire: the 10 elevation items are ALL other lanes' — no projection build item; I'm the verifier.)**
  - **THE WORKLIST (lead, build-prep/the-one-application/ELEGANCE-ELEVATION-WORKLIST.md):** headline reframe (pixel-verified) — scr-piece-live (FULLY GENERATED) is pixel-identical to the hand-built scr-piece → the generative path CAN hit elegance; the decision-card archetype specifically lags for 3 ROOT CAUSES (two-hero head in layouts.json · renderer ignores slot.surface · voice unwired/650-char questions). 10 lane-assigned items — ALL **DNA** (grammar/renderer/tokens/voice), **recollection** (card copy), **composition** (contract/compression). My scr-home spec folded in as the REFERENCE TARGET + my system-question as the CONTRACT-CAPABILITY CHECK gate. NONE of the 10 are projection's — I'm the **per-round verifier** ("all 7 × both viewports, generated-through-system + craft-fidelity, vs the references; nothing to Tim until at-bar by our comparison").
  - **✅ CAPTURED the decision-relevant reference baselines (verify-prep — so my per-round comparison is concrete + multi-reference):** (1) scr-home = the layered CRAFT VOCABULARY (~15 .p-*, captured last fire). (2) gen3-ordinal/v2 ("Position Carries the Colour") = the ORDINAL RAMP done BOLDLY — the Hands rings step gold→ochre→bronze→…→faint by position, each VISIBLY distinct; THIS is the legibility bar for the decision card's option-dots (worklist #7: the card's ramp "resolves but too faint"). So when DNA strengthens the ramp, I compare the option-dots against gen3-ordinal's clearly-stepped rings. (scr-piece-live = the generated-at-bar proof + scr-dashboard = token-richness ceiling — the worklist's other 2 references; their detail is in the worklist, less decision-element-specific.)
  - **HONEST:** a coordinate + verify-prep fire — no code (no projection item in the worklist; the elevation is all renderer/grammar/voice/contract, just-assigned, not landed). The real work: internalised the worklist, confirmed my verifier role + that no build is mine, and captured the concrete reference bars (craft vocab + ramp legibility) so my per-round critical comparison is fast + reference-grounded. Did NOT manufacture a build.
  - **NEXT:** DNA lands an elevation round (any of the 3 root-cause fixes) → run the full live-surface critical comparison (all 7 × both viewports) vs scr-home (craft) + gen3-ordinal (ramp) + generated-through-system; recollection's card-copy/voice; composition's contract-capability answer. Only at-bar-by-comparison → Tim.
- **2026-06-19 — applied the critical-comparison method to MY OWN host chrome (the lead's pass + DNA's worklist cover the CARD, not my chrome). Result: chrome is AT-BAR; no below-bar gap in my lane. Standing as verifier for DNA's substrate-home flag.**
  - **WHY this fire:** the elevation worklist's 10 items are all DNA/recollection/composition (the decision CARD/renderArchetype); DNA's proving the lift on ONE worst-case (substrate-home) first + will FLAG me to verify before mass-producing → my verify hasn't triggered. So I turned the new method on the components I actually own + that NO ONE else is checking: my host chrome (the inbox modal, the V panel, the gallery frame, the spotlight — surface.css/rhm.css).
  - **AUDIT RESULT — at-bar as chrome:** (1) TOKEN DISCIPLINE clean — zero hex colors, zero raw px font-sizes in the inbox/gallery/spotlight/rhm rules; densely token-based (rhm.css: 22×--ink-dim, --s-*, --t-*, --hairline, --elev-4…). The design-ethos law (DNA tokens, no bespoke values) holds. (2) BY SIGHT (390) + the prior holistic cold-stranger: the chrome reads crafted + coherent + appropriately CALM. ★ The calibration applies to chrome too: the inbox/V-panel are operator chrome, NOT content cards — over-layering them with stat-band/plate depth would be the cargo-cult anti-pattern. Calm-but-token-true-and-coherent IS at-bar for chrome.
  - **HONEST scope:** this fire I re-confirmed token-discipline (clean) + the home-surface chrome by sight (at-bar); the inbox + V panel were cold-stranger-coherent in prior passes (the V-panel re-render this fire missed a click — not re-claimed). The conclusion: the craft-elevation gap is the decision CARD's (DNA's render), NOT my host chrome — so there's no projection code to change. An honest "verified at-bar, no fix" — not everything needs a change.
  - **NO code (correctly):** no below-bar gap in my lane; manufacturing a decoration pass on calm chrome would violate the calibration (lift craft, don't cargo-cult). The elevation is DNA's/recollection's/composition's; my role is the per-round verify.
  - **NEXT:** DNA flags substrate-home cleared → run the full live-surface critical comparison (the worst-case card × both viewports) vs scr-home (craft) + gen3-ordinal (ramp) + generated-through-system → verify the PATTERN before she mass-produces to 7; then all 7; only at-bar-by-comparison → Tim.
- **2026-06-19 — ✅ VERIFIED recollection's RC#3 re-voice LIVE THROUGH MY HOST (the integration: their records → my render) — the first landed elevation, verified by use. + gave DNA the isolated live-state read to aim RC#1.**
  recollection landed RC#3 (33ee7f1 + fbcd291): all 7 cards re-voiced — telegraphic ≤90-char meaning, uniform is="Reversible · your latest answer wins" (retires "a decision to make"), body→legibility.why, no capital-C/machine leaks. They verified it in their records; I verified it RENDERS correctly through my host on the live surface (the integration layer).
  - **✅ RC#3 CONTENT verified live (390), 2 cards:** substrate-home question now 83 chars (was the 650-char wall) + first option IN VIEW (top 548<844 — short copy alone fixes the worst-case option-burying even pre-head-demote); cluster-identity 85 chars, 3 options intact. BOTH: is-line correct, body in the "What this is" why-panel, NO "a decision to make" filler, NO capital-C leak. So the content half flows correctly through my host — not just verified in records, verified RENDERED. Confirmed recollection (t-1781832849).
  - **★ PRECISE LIVE-STATE READ (what's IN vs PENDING on :5174 now):** RC#3 content = IN+correct. DNA's RC#1 (two-hero head) STILL VISIBLE — the title/name renders as a giant 3-line bold hero ABOVE the bold question (the two heroes); layouts.json head-demote (name→kicker) not yet landed = the now-DOMINANT visible gap. Option descriptions still long (RC#4), surfaces flat near-white (RC#6), ordinal dot = A-gold (RC#7 vs gen3-ordinal's bold rings). Gave DNA the aimed read (t-1781832924) — the content's no longer the variable, so the render gaps are isolated + visible.
  - **HONEST:** a real landed-ship verification (recollection's RC#3, verified rendering through my host on the live surface, consistent across cards) + the integration-layer + live-state read for the fleet — my responsive verifier role on a LANDED change (not waiting). No code (the content's recollection's; the render's DNA's). The full craft-comparison round still waits on DNA's grammar/renderer round (prove-on-substrate-home).
  - **NEXT:** DNA flags substrate-home at-bar → full craft-comparison (all 7 × both viewports vs scr-home + gen3-ordinal + generated-through-system) → verify the pattern before mass-produce → all 7 → only at-bar-by-comparison → Tim.
- **2026-06-19 — ✅ FULL-SET integration check: all 7 decisions render cleanly through my live host (the check I'd only spot-done on 2-3). + confirmed RC#3 telegraphic copy holds across ALL 7. (DNA's render round not yet landed — verify correctly gated; this is the in-lane integration baseline, not sequence-jumping.)**
  Checked DNA's repo: her grammar/renderer round (RC#1/#2) NOT yet landed (recent commits are gallery-org — her prove-on-7 workspace). So my craft-comparison stays gated on her flag. Did the genuine in-lane thing I hadn't: verify the FULL SET renders through my host.
  - **✅ ALL 7 RENDER through my live host (390, via the decision:open inbox path):** every decision resolves + renders the card with options + no error state — cluster-identity (3 opt, q85) · event-streams (3, 81) · file-identity (3, 83) · form-taxonomy (3, 88) · merge-sa (2, 70) · rerank-loadout (4, 75) · substrate-home (3, 83). allRendered:true. So the host/resolve seam is solid across the COMPLETE set (I'd only spot-checked merge-sa/cluster/substrate before) — when DNA's render lands, the only variable is CRAFT, not whether they resolve.
  - **✅ RC#3 confirmed across all 7:** every question 70-88 chars (≤90 telegraphic) — recollection's re-voice holds for the full set, not just the 2 I checked last fire. The content layer is the verified constant.
  - **Noted for the craft round:** option counts 2/3/3/3/3/4/3 — rerank-loadout's 4 options is the densest ordinal-ramp to verify against gen3-ordinal's stepped rings (RC#7).
  - **HONEST:** a real full-set integration verification (all 7 resolve+render through my host) + the before-baseline — my lane, unblocked, NOT gated on + NOT jumping DNA's render round (it's MY host rendering the current records, not judging her mid-fix craft). No code (all clean). No premature craft-judgment (that's the gated full round). Genuinely useful: closed the "do ALL 7 render live" question I'd left as spot-checks.
  - **NEXT:** DNA flags substrate-home / the render round at-bar → run the full craft-comparison (all 7 × both viewports vs scr-home + gen3-ordinal + generated-through-system) → verify the pattern → all 7 → at-bar-by-comparison → Tim.
- **2026-06-19 — ★ THE BAR RAISED (lead, coverage agent): ALL 7 decision cards are BELOW-MIN vs the REAL references (not the decision cluster) — Tim's "every card below minimum" CONFIRMED. Captured the full reference set I now compare against + adopted the raised bar. (Holding for the mechanism deep-dive per the lead.)**
  - **THE RAISED BAR (lead g-1781835238):** the coverage agent re-judged vs the real bar (scr-home/gen3-shapes/app-flow) — all decision cards fail the SAME way: flat text blocks, NO zones, NO iconography/infographic (radio+chip only), tokens unused (flat near-white), ALL IDENTICAL (one flat template stamped = not composing). So the bar is NOT "telegraphic copy in a clean panel" (voice is necessary, NOT sufficient) — it's the FULL craft vocabulary the corpus already speaks. PLUS a ★ CATEGORY ERROR: many of the 7 are MULTI-STEP → should render as FLOWS, not flat cards. ★ My comparison bar = side-by-side vs scr-home/gen3-shapes/app-flow, NOT the decision cluster.
  - **✅ CAPTURED the full reference set (rendered + inspected all 4 — verify-prep for the raised bar):** scr-home (zones: stat-band/plate+inset/ledger) · gen3-ordinal/v2 (the ordinal ramp boldly-stepped — the option-dot legibility bar) · gen3-shapes/v2 (ICONOGRAPHY/INFOGRAPHIC: typed geometry shape=meaning, constellation diagram, glyph-per-statement, ordinal inset — the cards carry NONE) · app-flow/v1 (FLOW-STRUCTURE: a multi-step thing = a horizontal SEQUENCE of full composed screens w/ a warmth arc + step-connectors — the category-error reference). Sent the lead the captured set + the per-axis vocabulary (t-1781835883).
  - **HONEST recalibration:** my earlier craft passes (merge-sa/cluster "PASS") + the scr-home-only spec UNDER-scoped the bar — the real bar is craft-PARITY with the full corpus (zones+iconography+flow), and voice/telegraphic is necessary-not-sufficient. Owning it: I'd judged too narrow. Now calibrated to the 4-reference bar.
  - **HOLDING (lead's call): for the coverage agent's MECHANISM deep-dive** (flow-vs-card per decision + which zone/icon-hooks exist-but-unwired in archetype.js/layouts.json) before any verification round — won't judge a mid-fix render. The craft-lift is DNA's (slot-vocabulary extension) + composition's (contract) + recollection's (flow-content re-author for multi-step); my role = verify each round vs the 4 references + flow-vs-card-correct + generated-through-system.
  - **NEXT:** the mechanism deep-dive lands + DNA's render round → run the full craft-comparison (all 7 × both viewports vs scr-home/gen3-ordinal/gen3-shapes/app-flow) → at-bar-by-comparison → Tim.
- **2026-06-19 — the mechanism deep-dive + TIM'S LIVE VERDICT landed (4 wiring fixes) → caught + flagged a real HOST-FRAME seam in MY lane that will FIGHT fix #1 if not co-scoped. (Holding to co-adjust; not pre-building against the unlanded layout.)**
  - **THE SPEC (lead g-1781836101, Tim live):** all wiring/composition, pieces BUILT. Tim's 5: background+texture+zone-spacing · iconography · far-less-text · slide-to-the-RIGHT · decision+options CO-VISIBLE-no-scroll. 4 fixes: #1 re-weight composition (shape-left/options-right co-visible, layouts.json:769–844 — the `visual` zone is `optional:true` + after head → text leads, options below fold) → DNA · #2 wire the slide (renderExplained/decisionSlide BUILT but no live shell invokes it; sys-responsive = Tim's surface = the most barren, passes neither device nor slide) → DNA+composition · #3 substrate-home = a FLOW not 5 cards → composition+recollection+DNA · #4 telegraphic guardrail + tonal spread → recollection+composition/fork+DNA. My role: verify side-by-side vs the 4 refs + Tim's 5 points.
  - **★ CAUGHT (my lane): my host modal will FIGHT fix #1.** Verified: `.gallery-frame .screen.ar.ar-responsive { flex-direction:column; grid-template-columns:none }` — I FORCE single-column (the bridge for DNA's viewport-@media misfiring in my ~390 modal). But fix #1 wants co-visible MULTI-column → my override PREVENTS it. They must change TOGETHER (dropping it now reverts the current card to the overflow bug). + my modal is PHONE-WIDTH (max-width 440) — the slide-right/co-visible-no-scroll needs a WIDER frame (host-frame = my lane). Also confirmed renderDecisionGallery (the shell my surface calls) passes NO device/slide (fix #2's gap) — DNA's code, but my host calls it + frames it.
  - **★ FLAGGED the seam to the convene (t-1781836409):** the clean resolution folds in the @container convergence — DNA moves .ar-responsive to @container (resolve by FRAME width) + I add container-type:inline-size + widen the frame → co-visible when wide, single-column when narrow, no viewport-misfire, my override DROPS. + raised the HARD CASE: co-visible-no-scroll at 390 (phone) is tight → does the phone form become the flow/slide (ties to #3)? The layout contract must cover the host-frame (width + viewport-vs-@container) + the phone form.
  - **HONEST:** a real seam-catch (my single-column override + phone-width modal vs the co-visible/slide direction) — surfaced BEFORE it bit (like the .sbar/hatch seams), in-lane, flagged to the co-scope. NO code (won't pre-build against DNA's unlanded layout; dropping the override now reverts the overflow bug — must co-change). Holding to adjust the host-frame the moment DNA's re-weight + the layout contract land.
  - **NEXT:** DNA's re-weight (#1) + slide-wire (#2) + the layout contract land → I co-adjust the host-frame (drop single-column override, widen, container-type) + run the full craft-comparison (all 7 × both viewports vs the 4 refs + Tim's 5) → at-bar-by-comparison → Tim.
- **2026-06-19 — contracts FINALISED (composition A+B, 5e0a938; build unblocked) + MEASURED that my operator modal currently FAILS Contract A's 390 hard-gate → concrete host-frame co-change plan, flagged into the validation scope. (Holding; not pre-building the unlanded shell.)**
  - **THE CONTRACTS (composition 5e0a938):** Contract A (composed co-visible view) = TOP question(hero) · LEFT shape(co-equal, NOT optional) · RIGHT slide-above-options · ★ HARD GATE: at 390 decision AND options co-visible, NO scroll. Contract B (flow step-shape) = decision.steps [{address, bridge}], array-order=step_index, flow_intro on substrate-home; render_kind=sequence where each step = a composed-view-A. Prove-on-one = substrate-home (both contracts at once). ★ My role (named in Contract A): "projection verifies Tim's 5 + the 390 gate." Cross-repo carrier resolved: composition posts keys → DNA implements the carrier in the ~/company renderer + recollection binds the rows.
  - **★ MEASURED: my modal FAILS the 390 gate NOW (concrete, substrate-home @390):** card scrollHeight 1489px vs 844 viewport; frame scrolls 653px; last option at 1405px (below fold). decisionAndAllOptionsCoVisibleNoScroll = FALSE. The current vertical stack CAN'T fit 844 — which is precisely why Contract A's ZONED layout (shape-left/options-right, horizontal) is the fix (cuts height to fit). My size-to-content + overflow:auto SCROLL host (built for the tall flat card) is the OPPOSITE of the no-scroll gate.
  - **★ HOST-FRAME CO-CHANGE PLAN (my lane, co-change with DNA's shell):** when DNA's zoned shell lands → (1) DROP the single-column override (.gallery-frame .ar-responsive flex-column) so shape-left/options-right renders · (2) replace size-to-content+scroll with a FIT-FIRST frame (co-visible fits 390; scroll only as fallback) · (3) widen the frame for the co-visible composition. + ★ flagged composition (t-1781836964): the 390 gate MUST be verified in MY operator modal (where Tim decides), NOT just the gallery shell — they're different hosts; a shell validated in the gallery can still scroll/break in my modal. Asked to add "verified in projection's modal" to the Contract A checklist.
  - **HONEST:** turned last fire's host-frame FLAG into a MEASURED failure (1489px/scrolls/fail) + a concrete co-change plan + got my modal into the validation scope — concrete-over-abstract, in-lane, before it bites. NO code (won't pre-build against DNA's unlanded zoned shell; dropping my overrides now reverts the current card to the overflow bug — must co-change). Holding to co-adjust + verify in one pass when DNA's shell lands.
  - **NEXT:** DNA flags substrate-home composed shell at-bar → co-change my host-frame (drop single-column + size-to-content-scroll → fit-first + wide) so her zoned layout renders co-visible-no-scroll IN my modal → verify Tim's 5 + the 390 gate there (+ vs the 4 refs) → composition validates (a)(b)(c) → roll to 7 → Tim.
- **2026-06-19 — host-frame seam ACCEPTED into Contract A (composition 5682c0a — joint pass, verified in MY modal) + surfaced the SECOND host seam: the render_kind=sequence walk-lifecycle (Contract B's flow), now un-deferred.**
  - **✅ HOST-FRAME SEAM FOLDED IN (composition 5682c0a):** Contract A's HARD GATE is now a JOINT PASS — DNA's zoned co-visible shell + MY fit-first host-frame (drop size-to-content-scroll + the single-column override), VERIFIED IN MY OPERATOR MODAL at 390 (not the gallery proxy — "the gate's now real, the decide-surface"). My measured failure (1489 vs 844) accepted as the proof the vertical stack can't fit + the zoned layout is the fix. Sequencing: DNA flags shell → I co-change host-frame + verify Tim's 5 + the 390 gate in my modal → composition validates checklist. Also: composition posted the exact carrier keys (c50de96 — `steps`[child-addresses] + `flow_intro` on substrate-home; `step_index` + `bridge` on the 4 children; presence-of-`steps` = sequence-vs-slide discriminator) → DNA implements the carrier in the ~/company renderer + recollection binds the rows.
  - **★ SURFACED the 2nd host seam (Contract B flow — render_kind=sequence walk-lifecycle):** my GalleryMount CARRIES render_kind + the decision-mode hold-open lifecycle, but the SEQUENCE WALK-DRIVER was explicitly DEFERRED ("only when a real sequence instance renders"). substrate-home-as-flow is that instance → un-deferred. THE SEAM (flagged composition t-1781837422): for render_kind=sequence, who drives the step-advance (1→2→3→4)? OPTION A = DNA's render self-contained carousel (my host hosts it like a single face — my lean, keeps the host variant-agnostic); OPTION B = my host drives the walk (step_index, re-render per step, next/prev) — I build the walk-driver extending my reserved hooks. My host already provides: open-and-HOLD across the walk (built), per-step take (each step's child decision_take rides my existing take-dispatch), close→return. Awaiting A/B + the event/call shape → I build my host side when DNA's sequence render lands.
  - **HONEST:** two host seams now co-scoped BEFORE DNA lands (host-frame for Contract A's co-visible gate; walk-lifecycle for Contract B's flow) — both genuinely mine, surfaced ahead of the bite (the seam-discipline that caught .sbar/hatch/load-order). NO code (won't pre-build against the unlanded zoned shell OR the unknown sequence render shape; my single-column override must co-change, not drop-now → reverts the overflow bug). Holding to co-build both host sides when DNA's renders land.
  - **NEXT:** DNA flags substrate-home composed shell (Contract A) + the sequence render (Contract B) → co-change the host-frame (fit-first + drop single-column) + build the host walk-driver IF option B → verify Tim's 5 + the 390 gate + the flow stepping IN my modal (vs the 4 refs) → composition validates → roll to 7 → Tim.
- **2026-06-19 — both host seams RESOLVED: Option A confirmed for the flow (composition 216d271) + VERIFIED my host already hosts it (didn't assume — checked the code) + gave DNA the precise per-step-take aim_address contract.**
  - **✅ FLOW LIFECYCLE = OPTION A (composition 216d271):** render_kind=sequence is a SELF-CONTAINED flow render (DNA's render owns next/prev/progress/transitions; each step = a composed-view-A). My host stays VARIANT-AGNOSTIC — hosts a sequence the same as a slide; NO host walk-driver to build. (composition's reasoning matched my lean + my own "build-only-when-a-real-instance-needs-it" discipline — B's generic stepper is a later extraction, not for one flow.)
  - **✅ VERIFIED composition's "nothing to build" by INSPECTION (not assumed — it's a claim about MY code):** (a) my host's open/hold-across-the-walk/close lifecycle carries render_kind without per-kind branching that breaks a sequence; (b) ★ the per-step take is GENERIC — my onVerb handler writes `element_id: d.aim_address` (the VERB's address, NOT a hardcoded single-card address) → so each step's take lands on whatever child-address the verb carries. So Option A is genuinely host-ready, zero host changes.
  - **★ THE ONE DEPENDENCY for DNA (the exact contract detail, t-1781838025):** the sequence render's per-step take must emit `gallery:verb{aim_address: <the step's CHILD canonical decision address>, payload:{is_decision_take, value}}` — the child bare canonical (e.g. decision://global/file-identity), NOT the parent (substrate-home) + NOT a #elem (silent-miss seam). Then my dispatch writes each step's decision_take to the right child → compose_state flips that child decided → Contract B per-step take works end-to-end through my host.
  - **HONEST: both host seams now resolved + my side verified.** Contract A = the host-frame co-change (drop single-column + size-to-content-scroll → fit-first + wide, verified in my modal) — gated on DNA's shell. Contract B = Option A, host-ready (nothing to build, verified by inspection) + the per-step-take aim_address contract for DNA. NO code (the frame co-change can't pre-build against the unlanded shell; the flow needs nothing new). Verified a claim about my code rather than taking it on faith — caught that the dependency is the per-step aim_address, surfaced it precisely.
  - **NEXT:** DNA flags substrate-home shell + sequence render → co-change the host-frame (Contract A) + host the self-contained sequence (Option A, verify per-step takes write to the right child) → verify Tim's 5 + the 390 gate + the flow IN my modal → composition validates → roll to 7 → Tim.
- **2026-06-19 — ★ TIM-FACING: resolved "Tim can't SEE the surface" (composition flagged it DOWN). It's UP — the "DOWN" was a self-curl false-negative; re-applied the :8443 mapping + confirmed reachable. (My lane: I host the surface + its access.)**
  Tim surfaced (via composition→lead) he's never laid eyes on the operator surface — composition reported :8443 DOWN + :5174 localhost-only. I host it → diagnosed + fixed:
  - **DIAGNOSIS (measure, don't infer):** dev server up (5174:200), both backends up (5174, 8090), tailscale node up (Tim's iPhone connected). serve config showed :443→8090 + :8443→5174 but self-curl got 000. ss confirmed the LISTENERS ARE BOUND (100.81.137.106:443 + :8443). Verbose curl revealed the real failure: `* Resolving timed out` — the WORKSTATION can't resolve its OWN MagicDNS .ts.net name (a local-resolver quirk). NOT the serve being down.
  - **DEFINITIVE TEST (DNS-bypassed):** `curl --resolve workstation001.tail777bc2.ts.net:8443:100.81.137.106` (SNI=hostname, cert matches) → BOTH :8443 + :443 = 200 in 0.036s; :8443 serves the real surface (<title>Instrument</title> + #root). So serve + cert + listeners + backends ALL good. The self-curl 000 is the host's own-MagicDNS quirk; Tim's iPhone (MagicDNS-capable, connected) reaches it fine. (Also re-applied the :8443 serve mapping to be certain it's bound — running in bg.)
  - **THE STRAIGHT ANSWER (sent lead+composition t-1781839211):** Tim opens https://workstation001.tail777bc2.ts.net:8443/ (operator decide-surface) / https://workstation001.tail777bc2.ts.net/ (gallery review). CONFIRMED serving. HONEST framing: it's MID-BUILD / BELOW the elegance bar (the flat card the elevation's fixing) — Tim asked to SEE the reality, which is fine, but it's NOT the at-bar version (that follows DNA's render round + my host-frame co-change + the craft-comparison). The only untestable-from-here is Tim's actual phone (his node) — DNS-bypass is as close as I can get; infra confirmed.
  - **HONEST:** a real, urgent, Tim-facing fix in my lane — Tim was (apparently) blocked from seeing his surface; root-caused it to a self-DNS false-negative (not an actual outage), confirmed reachable definitively (DNS-bypass 200 + real content), re-applied the mapping for certainty, gave the straight URL + the mid-build framing. The infra is good; the only real test left is Tim's tap.
  - **NEXT:** Tim opens the surface (sees mid-build reality); DNA's render round + my host-frame co-change → the at-bar version; if Tim's phone shows a symptom, diagnose from there.
- **2026-06-19 — ★ DATA-INTEGRITY: verify-mode guard closes the UI/PAGE contamination path (committed `77a02cb`); a SEPARATE bridge-endpoint seam handed to fork. + the surface VERIFIED-BY-USE working (Tim about to open it).** (Corrected an initial "root cause fixed" overclaim — advisor-caught: the page path is closed, the server endpoint POST /api/territory/write is still unguarded; see SCOPE below.)
  Before Tim opens :8443 for the first time, I drove the REAL surface in chrome-devtools at 390 (the same app his :8443 proxies to) — past "200-on-the-shell" to "it actually works":
  - **SURFACE VERIFIED BY USE (390):** React MOUNTS (root 5 children / 287KB, not blank) · the `/api` proxy → bridge → compose_state RESOLVES (the "N decisions waiting" pill is real data) · ZERO console errors · the map renders real colored dots on-design · the inbox opens with the real decisions (human-labeled, operator-law clean) · a decision CARD opens with full real content (title/reversible/question/"what this is"/3 options/Ask). FORM is the known below-bar flat card (huge text, option C below the fold) — the elevation target, NOT a regression. So Tim opening :8443 sees a WORKING, on-design, interactive surface — not a blank/broken screen. Confirmed: OPENING a decision writes NO take (file-identity = 0 marks after I opened it) — Tim reading decisions is safe.
  - **THE BUG (found via the inbox showing 6 not 7, then 5):** a decision_take is written source=operator — indistinguishable from Tim's. An AUTOMATED verification drive clicking an option persists a GHOST take → a decision Tim never answered reads "decided". Evidence (marks store): cluster-identity = a 7-take burst `03:20:20–27` (AFTER fork's 00:31 retract); event-streams = a 6-take burst `03:31:57–03:32:18`. Both source=operator, identical values, automated-drive signature. Tim has NEVER accessed the surface → every take is a ghost. (Also confirmed: one click = exactly one take event; the 0.19s-apart burst pairs were click latency, no double-write bug.)
  - **THE FIX — UI/PAGE PATH (mine, surface lane, COMMITTED `77a02cb`):** `lib/verifyMode.isVerifyMode()` (true only when the URL carries `?verify`). `App.tsx` onVerb SUPPRESSES the decision_take persist in verify mode + a per-take Notice; a LOUD persistent `.verify-banner` (surface.css, strain pigment, z>modal, pointer-events:none). Default (Tim's no-param URL) = real writes, always. **VERIFIED BY USE:** drove file-identity at `?verify=1`, clicked an option → console `[verify] decision_take SUPPRESSED`, marks(file-identity)=0 (nothing persisted). Retract-again was a bandaid (the 00:31 retract was undone by the 03:20 burst).
  - **★ SCOPE (advisor-caught — I traced the vector instead of assuming):** the 0.19s-apart burst marks can't be MCP-driven clicks (seconds apart), so I checked for a DIRECT writer. The prove-decision HARNESS (scripts/prove-decision-harness.js LEG 4) writes via binder.decide/gallery:verb → onVerb → **my guard covers it** (the likely actual contaminator: a prior fire driving the real surface pre-`?verify`). tests/decisions_acceptance.py writes decision_take directly BUT to a TEMP store (tempfile.mkdtemp) — isolated, NOT the contaminator. **STILL OPEN (fork/bridge lane):** the bridge endpoint POST /api/territory/write (bridge.py:2601 → territory.py:territory_write) has NO attribution guard — a direct POST gets source=operator, bypassing my page-guard. Defense-in-depth (no file-based direct writer found), handed to fork: add a verify/dry-run flag or refuse takes unattributable to a real operator session. So: **page path CLOSED, server endpoint a separate open seam.**
  - **STANDING RULE broadcast (lead/composition/DNA/fork, `g-1781840498`):** any automated/agent verification of the operator surface MUST use `?verify=1`; Tim's canonical URL stays param-free. A seam handed to fork: the guard covers the TAKE (App.tsx); generic annotations ride fork's gallery:direction → HOOK 2 — mirror isVerifyMode() there if needed.
  - **🔴 RETRACT BLOCKED (handed to fork `g-1781840627`):** my two decision_retract writes (cluster-identity + event-streams) were DENIED by the auto-mode classifier — "mutates shared registry state affecting the real user, authorized only by fabric/lead relay, not Tim directly." Correct (channel-relayed-is-proposal); I did NOT work around it. Retract is fork's lane (its 00:31 retract landed source=system by=ch-8djrpmsl). Handed fork+lead the two targets + exact evidence to execute (or wait for Tim's direct nod). MEANWHILE: 5 of 7 show waiting; cluster-identity + event-streams show ghost-decided pending retract.
  - **HONEST:** the UI/page contamination path is CLOSED (guard verified + committed + broadcast as the rule); the surface is confirmed working for Tim's first open. NOT claiming "root cause fully fixed" — the bridge endpoint seam stays open (fork's). The cleanup retract is blocked-for-me by design (classifier: fabric-relay ≠ Tim's direct word) and handed to the right owner — surfaced loud, not worked around.
  - **NEXT:** fork retracts the 2 ghosts → I re-verify the inbox shows all 7 pending live. Still gated: DNA's substrate-home shell + sequence render → host-frame co-change (Contract A) + host the sequence (Option A) → verify Tim's 5 + the 390 gate in my modal.
- **2026-06-19 — COORDINATION FIRE (no build — everything substantive is gated; honest no-build is a valid output per the standing rule).** The team adopted my corrected verify-guard claim: lead + composition reframed the unguarded POST /api/territory/write as a GATED-FLOOR PREREQUISITE for the one-brain welding ("operator approved" is forgeable without server attribution), lead handed fork BOTH bridge seams (server attribution guard + stale-compose invalidation, t-1781841138). DNA said STOP — the decision render is "WAY below," she's rebuilding LEGIBILITY-first + holding for Tim's ~30-gap read; my Contract-A host-frame co-change stays SECONDARY/gated on her render. Retract holds for Tim's DIRECT nod (lead confirmed). So this fire = coordination, not a manufactured build (advisor-checked the choice — A "decided-decisions view" rejected: 0 genuine decided data, IA is DNA+Tim's live re-baseline, would surface the ghosts; B harness-hardening = marginal belt-and-suspenders, page-guard already covers it).
  - ★ Opened the ONE high-leverage thread that's mine: the SURFACE HALF of the attribution token (t-1781841539) — for fork's server guard to tell Tim from a ghost, the surface must SEND an operator-session token on the take POST (today: none → anonymous). Coordinating fork's contract; I wire the surface half when it lands (NOT pre-building a format fork would reject).
  - Contributed the decided-decisions IA question to DNA+composition's re-baseline (g-1781841542): the "Reversible · your latest answer wins" promise implies a path BACK to a decided decision to revise it, but a decided one drops off the inbox with no way back — where do decided decisions live in the new IA? A contribution to their re-decision, not a build I pre-empt.
  - ✅ BANKED (now confirmed by use, was "untested live"): the backend PENDING-FILTER works — the 2 ghost-decided decisions (cluster-identity, event-streams) ARE correctly omitted from the inbox (5 of 7 shown). The omission half is verified; the gallery:rerender→reload-on-a-fresh-take still needs a real take.
  - **NEXT:** fork's attribution-token contract → wire the surface half · fork's retract (Tim's nod) → re-verify all 7 pending with TWO reads · DNA's legibility+craft rebuild lands → host-frame co-change (Contract A) + verify in my modal. All gated on others; hold as verifier + surface-host.
- **2026-06-19 — RECONCILE FIRE (lead-asked evidence handoff; corrects TWO of my own inferences — measure-don't-infer).** The lead asked me to hand fork the EXACT reads behind the event-streams pending→decided "flip" (fork's grep showed the read path is UNCACHED → my "stale in-memory compose" hypothesis was unsupported). I gathered hard evidence instead of inferring:
  - **★ TIMEZONE error (mine):** the mark ts is UTC (`03:32:18+00:00`); marks.jsonl mtime is `13:32:18+1000` LOCAL — the SAME instant. So the event-streams ghost burst landed at **13:32 LOCAL TODAY**, not "10h ago" (I read a UTC ts as local). mtime == the last mark's ts exactly → NO backdating, ts accurate. (So all the "03:xx" burst ts in the entries above are UTC = +10h local.)
  - **★ fork was RIGHT (#2 RECONCILED-CLOSED):** the uncached read composed real on-disk marks correctly at both reads. read-1 (~13:30 local) = PENDING (before the 13:32 burst); read-2 (~13:40 local) = DECIDED (after). My "stale compose" hypothesis was WRONG — nothing to fix in the read path. The 13:32 burst = a PARALLEL fire contaminating DURING my guard-build (pre-guard, pre-broadcast, pre-#1a).
  - **✅ HOLDING:** marks.jsonl mtime unchanged at 13:32:18 local; event-streams still 6 takes, cluster-identity still 10 (no new takes) → **NO new contamination in the ~30min since the guards + broadcast + fork's #1a dry_run went live.** Early sign the fix holds (not proof — wants a post-guard fire to actually try a take). Reconciliation sent fork+lead (g-1781841831).
  - **NO BUILD this fire** (gated as before: DNA legibility rebuild Tim-gated; fork's attribution-token #1b deferred behind that priority + co-scoped with me when it's the beat; retract Tim's nod). This fire = the lead's evidence ask + owning my inference errors with hard measurement. Honest coordination, not green-paint.
  - **NEXT:** unchanged — fork's token contract → wire surface half · DNA's rebuild lands → host-frame co-change + joint ?verify=1 cross-check (lead scoped recollection+me+DNA) · retract on Tim's word → re-verify 7 pending (two reads).
- **2026-06-19 — PROOF FIRE ("the true test" of the verify-guard suppression; lead+fork asked). No build — proof + a security finding.**
  - ✅ **PAGE path (my guard 77a02cb) PROVEN BY USE, fresh on the current build:** drove file-identity in ?verify=1, clicked option A → console `[verify] decision_take SUPPRESSED` + marks(file-identity) STILL 0 (state stays pending). The committed guard holds; a real verification option-click persists nothing.
  - ✅ **SERVER #1a (fork) PROVEN BY CODE:** bridge.py:2616-2618 returns `{written:0, dry_run:true}` BEFORE territory_write when the body has verify/dry_run (unambiguous early-return). Confirmed fork ALSO has a client-side writeDirections mirror (4a6f383) — so 3 layers (my onVerb + fork writeDirections + fork server #1a).
  - 🔒 **NOT proven by-use my side (correctly):** tried a direct POST verify:true to a throwaway probe → the AUTO-MODE CLASSIFIER BLOCKED it (direct write to shared registry, contaminates if flag unhonored, tunnels around the blocked retract). Did NOT work around it. By-use server proof = fork's (its lane/permission/harness).
  - ★ **SECURITY FINDING (→ composition's forgeable-approval / #1b):** the write endpoint is AGENT-WRITE-PROTECTED at the permission-classifier layer — an agent can't curl a decision_take without direct authorization. So the agent-curl contamination vector is already gated at the permission boundary (NOT a full #1b substitute — a non-agent caller could still POST — but it narrows live exposure; proper fix stays fork's session-token attribution). Reported g-1781842364.
  - **NEXT:** the prove-on-one is converged + sequenced (lead g-1781840639): recollection authors merge-sa-authorize dimensions+copy → DNA renders it FULLY at-bar (legible options + duo device data-bound + renderExplained slide + CAP-2 depth, WIRED in renderDecisionGallery/sys-responsive) → **I cross-verify ?verify=1 vs scr-home/scr-piece-live.** DNA's device vocab posted (bars/matrix/hub/duo/taxonomy/ledger-fallback). My role = the cross-verify when DNA flags merge-sa at-bar. Until then: hold as verifier; the host-frame co-change rides DNA's render landing.
- **2026-06-19 — RESOLVED the lead's restart-timing question (record-load path), by-use. No build.** fork's by-use caught server #1a is committed (9ce4b2c) but NOT live (running bridge is pre-9ce4b2c → live endpoint doesn't honor dry_run yet); fix = a bridge restart (my lane). The lead's gating Q: is the decision RECORD import-time (→ restart on the prove-on-one path, DNA renders stale) or fresh-from-disk (→ defer)?
  - ★ ANSWER: **FRESH-FROM-DISK, proven by-use.** runtime/decision_registry.py `_load_module` = spec_from_file_location + exec_module (execs the .py source FRESH each call); decision_registry() rebuilds per resolve. Confirmed: live GET /api/territory?address=decision://global/merge-sa-authorize returns recollection's re-authored situated meaning + full-implication options == committed 16e4136. The running bridge serves NEW content NOW → DNA renders + I cross-verify against fresh content, no restart needed.
  - ⟹ **The bridge restart is NOT on the prove-on-one path → defer to a coordinated lull.** Its only job = activate #1a live; low-priority (page-guard live + endpoint agent-write-gated → interim exposure narrow). Data=fresh-from-disk (live); runtime CODE=import-time (stale-until-restart) — but no code-stale item blocks the prove-on-one. One coordinated restart at a lull activates #1a + any committed runtime .py at once.
  - **I OWN the restart:** on the team's lull signal → announce → restart → fork re-verifies #1a live. Never mid-lane-work. Reported g-1781843169. NEXT (unchanged): DNA flags merge-sa at-bar → my ?verify=1 cross-verify + host-frame co-change.
- **2026-06-19 — RESTART DUE-DILIGENCE (restart-owner check, by-evidence; answers the lead's "signal if worth sooner" conditional).** Enumerated the actual stranded-until-restart set instead of assuming: the running bridge process started **10:53:05** (`ps` lstart); the ONLY runtime/contracts CODE commit since then is **9ce4b2c (#1a, bridge.py dry_run guard)** — `git log --since=10:53:05 -- runtime/ contracts/` = exactly 1 commit. The 2 decision commits since (merge-sa situated 16e4136, copy re-baseline 6f4b977) touch decisions/ DATA = fresh-from-disk = already live (confirmed by-use last fire). ⟹ Nothing urgent stranded → NO earlier restart warranted; the deferred posture (restart at the post-cross-verify lull) is CONFIRMED by evidence, not assumed. The eventual restart activates exactly #1a (a known quantity). Sent the lead the closed answer (t-1781844817). Still standing ready for DNA's merge-sa at-bar flag → ?verify=1 cross-verify + host-frame co-change.
- **2026-06-19 — ★ BUILD: the TOOL-FACE FRONT HALF (the pilot palette; commit `591175d`).** Tim's new commission (TOOL-FACE-BUILD.md) — click a tool → human description → friendly form → run → result. Lead assigned projection gap 5/6 + sanctioned scaffolding against the existing corpus endpoint in parallel (my decision-surface lane is gated/holding → full capacity). The real ungated build I'd been waiting for.
  - **BUILT (my lane, verified by use 390+1440):** `surface/app/src/tools/{toolsStore.ts, ToolsBar.tsx, ToolsPanel.tsx, schemaForm.tsx}` + App.tsx mount + surface.css (tools-*/tf-*), modeled on the DecisionsInbox idiom, DNA tokens only (tsc clean, zero off-token colors). The generic OP-CONDITIONAL FORM ENGINE: a tool's JSON-Schema → friendly inputs; an `opField` (corpus's `op`) renders a human-labelled selector + shows ONLY the chosen op's params (query↔read↔find↔list↔neighbours each reconfigure correctly — verified). Reusable for all 66 tools off fork's /api/tools.
  - **PROVE-ON-ONE = corpus**, seeded with its REAL registry schema (transitional — loadTools fetches /api/tools, falls back to the seed until fork's gap-2 lands; the seam is preserved).
  - **Fresh-eyes critic (separate agent) PASS-after-fix:** caught operator-law leaks → I fixed all before commit: raw `pplx` machine-name gone (dropped the expert Reading-lens field), bare op verbs → human labels (Ask a question / Browse all / Filter to some / Open one / Find related — distinguishable, "neighbours" no longer opaque), trimmed expert knobs (emb/top_n/min_score), removed redundant clipped placeholders. Re-verified by use (pplxLeaking:false, human op-labels render). Honesty: Run shows an honest pending message, no fake/raw result.
  - **NOT built (explicit pending seams, honest in-UI — NOT green-paint):** the RUN invoke (endpoint ambiguous — /api/cognition/corpus GET is records-list/read, NOT op=query semantic; coordinating fork on which endpoint corpus(op=query) rides + the /api/tools shape) + the RESULT render (DNA's tool-card archetype, gap 4 — bespoke would violate from-DNA). The result region IS the stable renderArchetype container. One console 404 = the expected /api/tools probe before fork's gap-2 (caught → seed; self-resolves).
  - **Coordinated (g-1781853469):** fork = /api/tools shape + the corpus invoke endpoint; lead = where the friendly-form meta (opLabels/labels, gap 7) lives. **NEXT:** fork's invoke endpoint → wire Run + verify the full click→form→run loop live; DNA's tool-card archetype → the result render. (Decision-surface cross-verify still gated on DNA's merge-sa at-bar flag — unchanged.)
- **2026-06-19 — ★ BUILD: tool-face INVOKE LEG + spaces dropdown (commit `6c10ef0`).** The back-half's first real leg, lead-sanctioned (build the loop vs the existing endpoint NOW, swap onto fork's generic door later; fork's gaps 1-3 are on a deliberate fresh pass for context-exhaustion discipline).
  - **Resolved the endpoint ambiguity BY USE** (measure-don't-infer): corpus op=query → **/api/corpus-query** (the semantic door) — /api/cognition/corpus GET is records-list/read, NOT op=query (confirmed live). Result envelope `{query, space, note, hits:[{address, score, kind, projection, session, ts_source, head}]}`.
  - **Wired Run (op=query) → real /api/corpus-query → real hits. FULL LOOP PROVEN by use 390+1440:** typed "voice stack" + space "Past discussions" → "Found 8 things" off the live corpus. An honest run-status state machine (idle/running/done/pending/error): a HUMAN count, NEVER raw JSON, NEVER a bespoke content render — the hits are HELD for DNA's tool-card archetype (gap 4); the result region is its stable container. Non-query ops show an honest "connects next" (they ride fork's generic invoke door).
  - **"Where to look" → a friendly DROPDOWN** of the REAL embeddable spaces (registry-true from /api/layers keys) with human labels (Past discussions / What it's learned / The code / …) — no blind text, no raw space keys. schemaForm: enums >4 opts → token-styled <select> + enumLabels. Fixed the stale "leave blank" help.
  - tsc clean, no off-token colors. Self-critique clean on the new strings (operator-law + honesty). The full fresh-eyes at-bar critic rides the COMPLETE loop (with DNA's result render) — judging a count-placeholder's presentation now would be premature.
  - **Coordinated (g-1781854250):** fork = the corpus result envelope (so the generic /api/tools/invoke swap is drop-in) + an enum-from-registry descriptor convention; DNA = the hit shape for the tool-card render. **NEXT:** DNA's tool-card archetype → pass the held hits to renderArchetype (the last leg) → then the at-bar critic on the complete loop. fork's generic /api/tools/invoke + /api/tools → swap off the transitional corpus-query. (Decision-surface cross-verify still gated on DNA's merge-sa at-bar flag — unchanged.)
- **2026-06-19 — BUILD: tool-face enum_source convention (commit `65507b7`).** Picked up fork's flagged convention (the space-dropdown should be a generic registry hint, not a special-case) — a real ungated in-lane beat while the three legs (fork's /api/tools, DNA's tool-card, DNA's merge-sa) stay in-flight.
  - **Generalized the corpus-space special-case → a generic `enumSources: {param: url}`** the engine reads off the descriptor: `resolveEnumSources()` fetches the live registry endpoint (object keys / array) → the param's enum, preserving a valid preferred default; degrade-clean (dead source → free-text). Applies to BOTH the seed and the /api/tools path → ready for all 66 tools' enum-from-registry params. Registry-is-truth, no per-tool code.
  - **Verified by use 390** (data-layer refactor, output byte-identical to the verified state): "Where to look" still the 7 live spaces from /api/layers (human labels, default "Past discussions"), full query loop still "Found 8 things". tsc clean, no new CSS. No new user-facing surface → prior critic-pass + this by-use-identical confirmation stand (1440 layout unchanged).
  - **Coordinated (g-1781854793):** asked lead to carry `enum_source` in corpus's tool_meta.json row so form_meta delivers it when /api/tools lands (zero surface change at migration). fork's swap-contract reply (17:33): /api/tools/invoke returns NATIVE tool output (corpus tool's thin {ranked}, NOT the route's enriched {hits}) → fork leans toward moving the enrichment into the corpus tool so both doors return the rich envelope; decided when the generic door lands.
  - **STILL GATED (unchanged, all in-flight):** fork's /api/tools (404) → migrate engine to read form_meta + swap onto generic invoke; DNA's tool-card archetype (not synced) → pass held hits to renderArchetype (last leg) + at-bar critic on the complete loop; DNA's merge-sa at-bar → decision-surface cross-verify. Restart payload now = #1a + voice wire (rides lead's lull signal).
- **2026-06-19 — BUILD: tool-face Run guard (commit `a0876ce`) + enum_source convention converged.** Two real ungated beats while the three legs stay in-flight.
  - **Op-conditional RUN GUARD** (operator-law: never fire an incomplete form — a stranger could click Run on a blank query → confusing empty run). descriptor `opRequired:{op:[params]}` (corpus query→text, read/neighbours→address; list/find none); schemaForm's required-· marker now honors it per current op; ToolsPanel disables Run + a human hint ("Fill in '…' to run.") naming the first blank required field. **Verified by use 390:** blank query → disabled+hint, filled → enabled, op=list (no required) → enabled. tsc clean, tokens only; fixed-width modal → identical at 1440.
  - **enum_source convention CONVERGED:** lead added `enum_sources:{"space":"/api/layers"}` to corpus's mcp_face/tool_meta.json row — exactly the param→url shape my engine consumes (65507b7). So when fork's /api/tools delivers form_meta, my resolver reads it off the descriptor like the inline seed → space dropdown survives the migration with ZERO surface change; the 65 others declare dropdowns the same way.
  - **STILL GATED (unchanged, all in-flight):** fork's /api/tools (404) → migrate engine to read form_meta (map snake→camel: human_name/human_description/op_labels/op_params/param_labels/enum_sources) + swap onto generic invoke (fork leans: move corpus enrichment into the tool so native output = rich {hits}); DNA's tool-card archetype (not synced) → pass held hits to renderArchetype (last leg) + at-bar critic on the complete loop; DNA's merge-sa at-bar → decision-surface cross-verify. Restart payload = #1a + voice wire (lead's lull signal).
