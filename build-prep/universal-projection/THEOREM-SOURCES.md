# Where Tim's theorem lives — the vault landscape (firsthand, 2026-06-20)

*The answer to "find things I've said before": Tim's theorem is spread across ~5 of his 10 indexed vaults
(8,629 files / 116,712 chunks total). The earlier 4-lens sweep mined ONLY `relative-difference`. The other
four theorem-bearing vaults are UNMINED. Discovered firsthand via substrate `list_vaults` + `get_vault_landscape`
(semantic search :8007 is down → structural-only). Read-only.*

## THE THEOREM-BEARING VAULTS (priority order for discovery)

1. **`relative-difference`** — THE formal theorem (universal-mechanics). 1,844 files / 31,363 chunks. ACTIVE.
   Path: `/mnt/c/Users/Workstation001/Documents/Claude/Projects/Relative difference/universal-mechanics`.
   ★ **It explicitly catalogs Tim's own words**: relation-kinds include `tims_verbatim`, `tims_direction`,
   `tim_done_bar_recorded`; reference-fields `tims-verbatim-path`, `tims-direction-path`. Tag-axes include
   **`axis`, `face`, `body`, `layer`, `articulation`, `source`, `round`** — his theorem is tagged by AXIS and
   FACE. So "find what Tim said about the axes" has a direct structural handle. (Sweep read this structurally;
   the 189-chunk formal package + the `tims_verbatim` catalog are still under-mined.) 39 open seams.

2. **`visual-dna`** — the VISUAL / GEOMETRIC theory (the square+circle/DNA roots). 727 files / 11,013 chunks.
   Path: `/mnt/c/.../Visual Designer/Visual-DNA-Vault`. Folders: `notes` 330, `_system` 142, `Build` 74,
   **`Theory` 25**. Tag-axes: `domain, layer, lens, mechanism, project`. UNMINED. Highest-value for the
   geometry/instrument side (the cube, the square+circle, the DNA grammar's roots in his own words).

3. **`unification-vault`** — the PRINCIPLES + axis→schema projections. 2,803 files / 25,088 chunks.
   Path: `/mnt/c/.../Documents/Vaults/unification-vault`. Folders: `11_EMERGENT` 1,266, `01_INVENTORY` 491,
   **`02_PRINCIPLES` 143**, `Coordination` 111. (Sweep flagged it "downstream build-system" but it carries the
   operationalized axis-theory — EM-865 axis-to-schema, the 20-axis DB projection.) 30 open seams. UNMINED.

4. **`ulm-inventory`** — the REGISTRY / SCHEMA / ALLOCATION system (the resolver's ANCESTOR). 215 files.
   Path: `/home/tim/repos/graph-editor-ulm/ulm/inventory-vault`. Folders: `Registries` 69, `Schemas` 41,
   `Patterns` 41, **`ALLOCATIONS` 24**, `Questions` 16. Relation-kinds: extends/instances/patterns/validates.
   UNMINED. Directly relevant to "everything is a registry / resolve(invariant, coordinate)".

5. **`vi-context-design`** — has folder **`07 — Unarticulated` (21 files)** + `08 — Edge Cases` (11). 140 files.
   Path: `/mnt/c/.../Claude/Projects/vi-context-design`. The "unarticulated" folder = his own open-territory /
   not-yet-said. 37 open seams. UNMINED.

(Not theorem: `visual-design-corpus` = DB-validation; `claude-code-atlas`/`claude-platform-docs` = docs mirrors;
`elevenlabs-mcp` = wizard; `_issues` = issue tracker.)

## CONSEQUENCE
- "Find things I've said before" = mine these 4 unmined vaults (esp. visual-dna `Theory`, unification `02_PRINCIPLES`,
  ulm-inventory `ALLOCATIONS/Schemas`, vi-context `07-Unarticulated`) + the `tims_verbatim` catalog in
  relative-difference. The theorem-as-base must rest on ALL of these, not one.
- The recollection-population gap stands: this is his VAULT work (substrate-indexed), NOT embedded in the company
  corpus (code-only). To make the theorem the base, it must also be fed INTO the company's own recall.
- Infra: substrate semantic (:8007) down → structural-only; company session-index corrupted. Both flagged to lead.

## FIRSTHAND FINDS — `relative-difference` structural probes (2026-06-20)

**Deep formalizations cataloged as `articulation`-types, NOT yet in the map** (each one `get_by_address` away —
the formal spine the map lacks): **monodromy-structure** (= the winding / 4π double-cover, formalized),
**coupled-coalgebra-runtime** (= the recursive resolver / "resolve(invariant,coordinate)" structure, formalized),
**eigenpoint-lattice** (+ `eigenpoint-lattice-runtime`), **universal-type-signature**, **universal-evaluator**,
**parametric-substrate-operation** (+ `parametric-operation-as-runtime`), **canonical-theorem**,
**parent-theory-decomposition**, **typed-coordinate-input**, **projection-as-consequence**,
**universal-substrate-coordinate-space**, **coupled-coalgebra-runtime**, **repo-as-bounded-unit**,
**ai-as-units**, **platform-and-location-coupling**, **cognitive-pattern-recognition-fibre**. These read as the
RUNTIME/EVALUATOR formalization of the theorem — i.e. the theorem already written AS an operational engine
(evaluator/coalgebra/runtime), which is exactly the resolver. High priority to read.

**Tim's tagged verbatim** (`articulation: "tim geldard"`, 15 units, all `notes/substrate-test/{Term,Design}/`):
a coherent set on SEQUENCE / INCOMPLETION / SELF-APPLICATION — Sequenced State Progression · +1 mechanic ·
Addressable incompletion · Queryable incompletion · Anti-summary rule · Confidence-is-not-processing-authority ·
Order-mechanic · Orphan relation · Question-led progression · Self-application (sequence processing) · Sequence ·
Start boundary · Store before next · Layered extraction of long recordings · Mapping-design-implementation
separation. ★ These ARE the invariant-law lens, in Tim's own words: **Orphan relation** = S-absence1 inverse-proto
(content with no ratified address = circle-without-square); **Addressable/Queryable incompletion** = distinct-
absence made operational; **Self-application** = the fractal recursion; **Anti-summary** = record-expansively;
**Confidence-is-not-processing-authority** = confident-not-correct. → his WORKING RULES are the theorem applied to
itself. (`source: "tim-direct"` = 5 more direct units.)

**His tagged vocabularies** (facets): AXIS = {scale, multi-axis, state, dynamic, orientation, division, winding,
code-fibre, self-application}; FACE = {operational, structural, navigational, semantic, mathematical, composed,
multi-orientation}; BODY = {universal-mechanics, substrate-operational-mechanics, bounded-unit-mechanics}.

**NEXT firsthand reads (highest-signal, as quota allows):** the runtime/evaluator formalizations (monodromy,
coupled-coalgebra-runtime, universal-evaluator, eigenpoint-lattice) via get_by_address — the theorem-as-engine =
the resolver's formal ancestor; then visual-dna `Theory`, unification `02_PRINCIPLES`, ulm `ALLOCATIONS`.
