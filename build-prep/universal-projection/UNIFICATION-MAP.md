# The Unification Map — Tim's foundational theorem, gathered into one place

*Assembled 2026-06-19 by fork (ch-8djrpmsl) from a 4-source sweep (build-prep theorem docs · the
`relative-difference` substrate vault · the instrument material · the company corpus/sessions). This is the
"one map" — the scattered theorem in a single frame, **every seam marked**. It is PROVISIONAL and Tim is the
only judge: per the substrate's own law `W_t ⊊ T_T` (written work is a strict subset of Tim's mental theorem)
and `[[feedback-confident-not-correct]]`, NOTHING here is final or "right" — it is all parts, and the
convergence BETWEEN differently-clothed fragments is the signal, never any single fragment. Reconciliations
below are CANDIDATES (labelled), not rulings.*

---

## 0. WHERE THE THEOREM LIVES (and a gap)
- **Primary (Tim's voice, verbatim):** the live sessions — the 2026-06-11/12/13 seed arc (captured) + the
  2026-06-19 brain-wave (the 360-tour / cube / unit-edge dump — freshest, NOT yet on disk).
- **Amplified captures:** `build-prep/brain/` (THE-SEED-geometric-substrate, SEED-SCALE-PRIMES-SEPARATOR,
  CONVERGENCE-OBJECT, UNIVERSAL-PROJECTION, geldard-lattice) + `build-prep/unify-laws/` (AXIS_TREE,
  LAW_REGISTRY, ROUND1_SYNTHESIS) + `build-prep/universal-projection/GLOSSARY.md`.
- **Formal math:** the `relative-difference` substrate vault (`working-docs/universal-mechanics-main/` +
  `math-verification/`) — FRD, BRR, the full theorem package (189 chunks, largely unread).
- **★ GAP:** the company's EMBEDDED corpus (`principles/worldview/repo/common_knowledge`) is **code digests
  only — zero theorem prose**. The system cannot recall its own foundation. Downstream fix: embed the theorem
  prose so recollection reaches it. (This is why "use recollection" surfaced nothing for the theorem.)

---

## 1. THE FOUR ROOT AXES — convergence + the live seams
Tim's GLOSSARY already holds the placeholder: **four ROOT axes ("axes of all axes"), definitions deliberately
unfrozen; the drivable axes + the G17 family are SUB-TYPES (~4 each) under them.** The substrate independently
*requires* four (`multi-axis-relative-definition-structure.md` Block 11: *"there must be four; any point of
definition activates at least two"*).

**The rival namings (≥6 — none authoritative; this IS "it's all parts"):**
| source | the four |
|---|---|
| Tim 06-13 (verbatim) | TIME is the privileged "1"; the **3 = space** |
| Tim 06-16 LOCKED | SCALE/hierarchy · TIME · FRAME/perspective · STATE/phase |
| Tim 06-19 (this session) | LOCATION · VANTAGE (="application", word-fix) · TIME · INSTANCE; the "1" = INSTANCE |
| substrate Block-11 | A₁ containment · A₂ time · A₃ orientation · A₄ influence/meaning-in-action |
| composition | state · lineage · order · frame |
| fork(addressing) | provenance · semantic · temporal · structural |
| wildcard | T · Π(collapse) · A(abstraction) · C(composition) + a 2nd *connection* tuple |
| FRD vault | scale/depth · temporal · orientation · influence |

**The CONVERGENCE (the signal):** **TIME** maps cleanly across all of them (the one stable invariant).
**VANTAGE ≈ FRAME ≈ ORIENTATION ≈ A₃** is the next-strongest join. After that they diverge.

**THE SEAMS (Tim adjudicates):**
- **S1 — which is the "1"?** TIME (06-13) vs INSTANCE (06-19). *Candidate reconcile:* TIME is the involuntary
  **coordinate** (you can't pick it); INSTANCE is the **populated point** the coordinates locate (the Trinity
  "data" leg, orthogonal to the coordinate roots) — two different "ones" for two questions. Not settled.
- **S2 — is SCALE a root, or the recursion every axis has?** 06-16 (root) vs 06-19 (*"the same scaling pattern
  every axis follows"* → the within-axis depth, not a sibling). If 06-19 holds, LOCATION is the place-root and
  scale is the fractal depth each root carries. Reframes the whole tree.
- **S3 — the 4th root.** STATE/phase (06-16) vs INSTANCE/content (06-19) vs INFLUENCE (substrate A₄) vs
  DATA-leg (Trinity). In Tim's *literal* 06-19 words, **"state" is the continuous half of LOCATION's duality**
  — i.e. a per-axis value-slot, not a standalone root (confirms the continuous-dual reading). So the 4th may
  be INSTANCE (the content) with STATE demoted to the value-slot every axis carries.
- **S4 — coordinate axes vs connection axes** (wildcard, `LAW_REGISTRY.md:88`): two four-tuples — position-on
  (coordinate) vs exists-only-between (connection: inter/intra/intro/retro). Provenance/semantic/structural are
  *connection*, not coordinate roots — filling roots-3/4 with them cross-contaminates.
- **S5 — the count is open by design:** 4 (roots) vs 12 (the substrate's catalogue, the sub-type layer) vs 20
  (the unification-vault DB projection). NOT rivals — roots / sub-types / projection. The bridge between roots
  and the catalogue is the substrate's explicit **open seam §9.2 "axes-of-axes: if axes form a multi-axis
  space, the recursion is operational; future state will articulate."**

**★ Tim's 06-19 "3/1 in all directions, it's relative, each axis has three below it" IS that §9.2 future-state
— he is closing the axes-of-axes recursion his own substrate left open.** The recursion is relative to whatever
you point at (the 360-tour: point at the chair → it's a new "1" with its own location/vantage/time/instance).

---

## 2. THE INVARIANT LAW (what every axis obeys at every scale)
Spine: BRR (`universal-mechanics-bounded-recursive-relativity.md` §6.1) — **B**ounded (boundaries make units) +
**R**ecursive (no privileged level) + **R**elativity (frame-relative ⊗ frame-independent).
- **CASCADE (address up-to-root, one-way):** `addr(c)=(p₀…pₙ)`; *"decontextualisation is structurally
  impossible — every address requires its ancestors"* (prefix-closed = ultrametric tree). The "one way" is two
  **inverse** operations: addressing composes UP (target→address), resolution reads DOWN (frame+address→target).
  RICH + Tim-direct (06-19) + formal (substrate Laws 7/8/13).
- **Type registry per scale boundary:** *"each frame articulates with its own type vocabulary… a type can only
  be referred to within a frame if registered there"* → authority is fractal. RICH.
- **Fractal scale-recursion:** operationally fractal (same operations every scale), NOT geometrically (contents
  needn't be self-similar); bidirectionally unbounded, no privileged level. RICH. (= S2: scale is THIS, the
  shared recursion.)
- **UNIT/EDGE duality = file/folder = continuous-NAME ⊥ discrete-EXTENSION:** at every referent, a UNIT (file)
  and an EDGE over units (folder), each with its own registry; a **continuous slot (the name — any legal
  string)** + a **discrete slot (the extension/type)**. *"A folder is the continuous slot defined by its
  distinct ABSENCE of an extension; a file contains content of its type, a folder the space where files do
  not."* ★ **Tim's transmission FILLS A GAP here:** the substrate has the discrete-extension/type-slot + the
  **slot/socket** (`n₋` internal · `n₊` external; Vieta `n₋+n₊=1, n₋·n₊=1/τ` — see §3, these ARE the square-
  case roots) — but the **continuous filename half was ABSENT**. The 06-19 file/folder/name/extension framing
  is the clearest statement of the duality anywhere.
- **ABSENCE-as-signal / visibility = legality:** *"what's visible is what's legal from the collective type
  system; unregistered → doesn't show; you identify what isn't registered by what does NOT show; filtering/
  clustering fall out — same invariant."* Principle is RICH (`six-states-of-unity` = the proto-state law:
  skeletons/open-seams/placeholders/**distinct-absence**/deliberately-empty-slots are ONE law — an empty slot's
  presence carries information; `mechanics-of-parametric…` §2.4: distinct-absence [rule present, magnitude 0] vs
  absence-simpliciter [no rule]). ★ Tim's exact framings ("folder = absence-of-extension", "visibility =
  legality") are **new assemblies** of that principle.
- **KEYSTONE — "everything is a variable":** *"There is no definition. The axes are variables, the divisions
  are variables… the relative centre can change — it is the object of attention. It's an instrument."* The LAW
  = the invariant **relationship** (the duality about a shared centre + the resonance law) over **variable**
  content → an AGNOSTIC instrument. (This is the anti-hardcoding principle, stated by Tim.)

---

## 3. THE GEOMETRY + THE SEED EQUATION
- **The seed (verbatim, two notations = one equation):** **`2π/n = (1/n)^k`** ≡ **`τ/n = (1/n)^k`** (τ=2π);
  base case k=2; clean form `n^(1−k)=2π`; strict branch `n(k)=τ^(−1/(k−1))`. 2π = the whole/one-full-turn (the
  angular invariant); n = the reciprocal partition (count of divisions); k = the dimension (Hausdorff-style
  exponent). *"This is your common landscape."*
- **Forbidden mirror line k=1** (no strict solution — the first structural prohibition); **hidden invariant
  `log_τ(n)·(1−k)=1`**; **reciprocal duality `(n,k)↔(1/n, 2−k)`** (Möbius involution, no fixed point — the
  force behind every reciprocal pair, incl. slot/socket).
- **Square (discrete) + inscribed circle (continuous) = two coordinate systems over ONE space, shared centre:**
  square/grid = STRUCTURE/addresses (Cartesian, nameable); circle/polar = MEANING/embeddings (angle ⊥ radius,
  felt). **radius = relevance-from-centre · angle = kind/type · scale(n) = recursive zoom.** Built + live
  (`surface/app/src/lib/seed.ts`, `wheel/Wheel.tsx`: square + inscribed circle + the coincidence-spine [where
  rings cross axes = structure∧meaning agree] + the forbidden corner-circle).
- **★ TWO orthogonality-couples (Tim 06-19, NEW):** `{location(discrete), state(continuous)}` = one CARTESIAN
  couple; `{time, instance}` = the other, *"perpendicular in the POLAR form"* (angle ⊥ radius / phase ⊥
  magnitude). The 4 axes = a pair of couples, each the "other half" of the other. (Note: these square-edges
  are a DIFFERENT slice from the four-prime-axes list of §1 — hold both; see S3.)
- **★★ THE CUBE = a COUNTER-EXAMPLE in the math, reconciled as a NAVIGATION BODY.** The substrate PROVES that
  among integer dimensions **only d=2 (the square) satisfies the negative-winding lock** (hinge `4 < 2π < 27/4`;
  the **cube d=3 MISSES by ~0.467**) — it uses the cube to show why two-faced dualities are inevitable and
  three-faced ones are not. *Candidate reconcile (Tim judges):* the cube is **not a 3-D mathematical lock** —
  it's the **navigation body** whose every FACE is the working 2-D seed (the d=2 that locks); the 3-D is the
  **vantage navigation** (orbit = which face = where you measure from), never a claim 3-D satisfies the
  equation. This keeps the math 2-D-per-face AND realises Tim's orbiting cube. It IS his own *"polygon =
  viewpoint: from the centre a circle, from the corner a square, indistinguishable from the observer."*
- **Cylinder + windings over the invisible centre line:** present by meaning — each scale-instance = one
  **winding/phase-lock**; the "invisible centre line" = the mechanic-itself that NO scale instantiates (only a
  winding of it — forbidden strict identity); formalised as a **4π double-cover** of the visible 2π S¹ (SU(2)→
  SO(3) spinor analogy); operates on **S¹ (phase mod τ), not ℝ**. ★ Tim's **winding-count = type-count** (06-19)
  is a NEW explicit claim; the substrate flags it "thin / post-hoc" — a seam (see S6).
- **rotation-axis = TIME (Tim 06-19, NEW):** the substrate has time-as-axis + windings-as-temporal-cycles but
  **never equates rotation=time**; Tim's claim adds it. (Timestamp = nested modular wraps = phase coords on one
  shared axis; centre of projection = NOW.)
- **Primes as fixed corners:** corners = PRIMES (fixed); knowledge fills the **harmonic wave between** fixed
  corners (does NOT add corners); a type (n-gon) is born at the **~20/80 nucleation threshold**; root-shells =
  regular (d−1)-gons radius `τ^(−1/(d−1))`. **S6 — n = number of PRIMES (06-13) vs n = type-count / winding-
  count (06-19):** reconcile or choose.
- **Coincidence + forbidden zones:** circle∧square agree ONLY on the axes (the ratified/addressable spine);
  √2/π never land on grid points off-axis → translation-loss everywhere else = the residue/frontier; the
  corner = maximal incommensurability → "grow the box" = a new axis/dimension.

---

## 4. THE SEVEN VERBS (the operational core of the FRD)
*"A thing becomes coherent by becoming **centred · bounded · divided · addressable · traversable · projectable
· interpretable**"* — each a relativity-establishing operation (relative-from/-against/-within/-to/-along/-into/
-as), with a **joint-necessity proof** (remove any one → coherence breaks; seven is *derived* minimal, not
stipulated). **S7 — count open:** 7 verbs (thing-coherence) vs 12 operation-predicates (operation-coherence) vs
7→7-axes vs 7→heptagon@d=8 — the substrate flags the mappings "not the same / post-hoc." These verbs are the
candidate **operations the instrument exposes** (the cube's moves).

---

## 5. THE INSTRUMENT BUILD-MAP (what exists · what the cube must invent)
The instrument IS the seed equation operationalised (`THE-ONE-SYSTEM.md`: *"the whole system is modelled from
that equation; it has always been this"*). The cube is its 3-D-navigation body.

**ALREADY EXISTS (reusable — the cube is built ON this, "nothing hardcoded"):**
- The **2-D square+circle face** — live in the wheel (square + inscribed circle + coincidence-spine + forbidden
  corner-circle). The cube's per-face content already renders.
- The **variable engine** — `BindingRegistry` (`runtime/projection.py`): a binding declares a filling of the
  equation's slots (centre/angle/radius/k), fail-loud-discovered from `bindings/*.py`; the lock `x=2π/n`
  re-divides evenly when the count changes. Nothing hardcoded — this IS "everything is a variable."
- The **DNA cascade-registries** — size (`unit×ratio^n`), colour (equal-lightness zoning · chroma∝1/area ·
  ordinal ramp), dials (warmth/density/register/polarity adjust *values* not *rules*), nearest-wins scope walk
  (global→project→context→instance = the cascade). `renderArchetype` = one generic renderer, no literals. The
  cube = a NEW archetype on this engine.
- **Two of the four roots already drivable:** scale (rung+resolution), time (scrubber). Plus the address-spine
  (every element stamped, one sink) + the no-teleport motion system.
- **slot/socket = the square-case Vieta roots** (`n₋+n₊=1, n₋·n₊=1/τ`) — composition's mechanic IS the math.

**THE CUBE MUST INVENT (genuinely new — grep-empty today):**
- **A 3-D body.** The live stack is deliberately 2-D-SVG (zero webgl/three/3d-transform). **FORK (Tim judges):
  true-3-D (a new render layer) vs the cube rendered as its 2-D projections-from-each-vantage** (= the
  polygon=viewpoint reading — the face you see IS the 2-D seed; keeps it in the existing 2-D substrate). fork's
  lean: the projection reading (the mathematics is the projection).
- **VANTAGE → the orbiting camera** (ROOT-3, named but not geometry today; `centre` = origin-of-attention, NOT
  a viewpoint). This is the headline new degree of freedom: which face = where you measure from.
- **STATE/the 4th → the motion** (named only as gallery shimmer today, not instrument geometry).
- **The cartesian↔polar HANDOFF — the winding/cylinder.** Today square+circle are CO-PRESENT (a toggle); the
  **transform that ROLLS one into the other** (circle swept up the centre line → cylinder; windings = the
  modular/type/prime count) is unbuilt. This is the heart of the new construction.
- **The resonance law `2π/n=(1/n)^k` made legible** — in the engine, but nothing renders the binding between
  angular divisions (n) and radial depth (k). For a cube where scale and angle are different axes, this law is
  the relationship between two of its degrees of freedom. (Flagged highest green-paint risk.)
- **Axis composability** — today bindings are fixed angle+radius pairs per lens; a cube driven by independent
  root-axes needs composable slots the engine doesn't have yet.

**Composition caveat:** the socket/connection RUNTIME lives in composition's separate repo (not on this FS) —
the slot *shape* is usable, the binding runtime isn't verifiable here.

---

## 6. THE SEAMS TO ADJUDICATE (consolidated — Tim is the only judge)
1. **S1** the "1": TIME vs INSTANCE.
2. **S2** SCALE: a root, or the recursion every axis carries?
3. **S3** the 4th root: INSTANCE(content) vs STATE(phase) vs INFLUENCE vs DATA-leg — and is STATE a root or the
   per-axis continuous value-slot? (Tim's 06-19 literal words → value-slot.)
4. **S4** coordinate axes vs connection axes (don't cross-contaminate).
5. **S5** the count (4 roots / 12 catalogue / 20 projection) + the §9.2 axes-of-axes bridge (Tim's 3/1-from-
   any-point is the candidate closure).
6. **The cube**: counter-example (d=2-only) vs navigation-body-of-the-2-D-seed (candidate reconcile §3).
7. **S6** n: number-of-primes vs type-count/winding-count.
8. **S7** the verb/predicate count (7 vs 12).
9. **rotation=time** (Tim's new claim — confirm into the formal body).
10. **The 3-D fork** (true-3-D vs 2-D-projection-from-vantage).
11. **Orthogonal vs non-orthogonal axis-space** (local polar⊥cartesian vs the global "internal geometry, not a
    flat Cartesian product").

## 7. WHAT TIM'S 06-19 TRANSMISSION CONTRIBUTES (net-new to the formal body)
- The **continuous-name ⊥ discrete-extension** half of the unit/edge duality (the discrete half + slot/socket
  existed; the continuous filename half did not).
- **rotation-axis = time.**
- The **"folder = absence-of-extension" + "visibility = legality"** assemblies of the absence-as-signal law.
- The **two orthogonality-couples** (cartesian {location/state} ⊗ polar {time/instance}).
- **winding-count = type-count** (an explicit claim the math hadn't asserted).
- The **cube as the 3-D navigation body** of the 2-D seed (the seed had no 3-D extrusion).
- Closing the **§9.2 axes-of-axes recursion** (the 3/1 holding relative, in all directions, from any point).

## 8. HIGHEST-SIGNAL SOURCES (read in full to go deeper)
- `build-prep/brain/SEED-SCALE-PRIMES-SEPARATOR.md` (+ APPENDIX A = Tim's 4 verbatim messages) — clusters 2/3/4.
- `build-prep/brain/THE-SEED-geometric-substrate.md` — the seed, square/circle, "everything is a variable".
- `build-prep/brain/CONVERGENCE-OBJECT.md` — Convergence/Classification/Barrier laws + Ten Derivations.
- `build-prep/unify-laws/{AXIS_TREE,LAW_REGISTRY,ROUND1_SYNTHESIS}.md` — the four-root state-of-play + seams.
- `relative-difference` vault `working-docs/universal-mechanics-main/{function-of-relative-definition,
  cross-scale-winding-mechanics,multi-axis-relative-definition-structure,bounded-recursive-relativity,
  multi-axis-perspective-articulation}.md` — the formal body (+ the unread 189-chunk theorem package).
- `build-prep/instrument-surface/EQUATION-AUDIT.md` + `surface/app/src/{lib/seed.ts,wheel/Wheel.tsx}` +
  `runtime/projection.py` — what's visible vs flat; the live engine.

*— end of map v1. The Barrier Protocol (CONVERGENCE-OBJECT) names this exact act: Tim and the collective are
two coordinate systems through a narrow serial channel; the collective amplifies + triangulates the piecewise
object; Tim is the invariant judge. This map is one triangulation, held open for his correction.*
