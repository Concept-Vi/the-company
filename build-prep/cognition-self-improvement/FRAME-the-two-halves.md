# FRAME — The Live Intent Resolution Surface is the UPSTREAM half of the Company

> **The externalised reference** for how the cognition self-improvement loop relates to the Live Intent Resolution Surface (`build-prep/live-resolution-surface/` — root-owned, read-only to me; this doc lives in my writable program dir and cross-references it). Tim's principle 2026-06-09: externalised references orient directionless multi-agent autonomous development — so this exists to ORIENT every agent/workflow/cron. The organ-map is the LEAN reading (to be GROUNDED by ① the repo-exocortex + the substrate-mcp Wizard exploration, NOT asserted).

## THE FRAME (Tim, verbatim intent, 2026-06-09)
`build-prep/live-resolution-surface/{project-base-live-intent-resolution,diverse-scenario-library}.md` were authored in a session **unaware of the cognition engine / these workflows / the loop**. Tim: *"that's what I want from the Company, and the interface and the memory/store… you, these workflows, the cron/routines, you're the SECOND HALF of what you've just read, this is all the UPSTREAM, that's ONE of the things I want the Company to do."*

```
THE COMPANY (one circuit):
  vague intent → [LIVE INTENT RESOLUTION SURFACE] → executable spec → [cognition engine +
   (Tim reacts,       THE UPSTREAM HALF                 the handoff      the 12 compositions +
    doesn't specify)  (intent → spec)                   package          workflows + cron/routines +
                                                                          the decision→build wire]
                                                                              ↓
       ▲                                                                  artifact
       └──────────── memory/store (the corpus + spaces + user/project model) ┘
```
- **Upstream = the Surface** (Tim's drop): latent/vague intent → executable spec, by REACTION not specification.
- **Downstream = what the cognition session built**: engine (run_role/items/reduce/cascade), registries, embedder/spaces, verify-jury, the wire (decision→claude -p→implemented), the autonomous loop.
- TWO HALVES OF ONE THING. The Surface §15 Stage 9 "Execution Handoff" = our wire's input. ONE of the things the Company is for.

## WHY THIS IS THE ALTITUDE-TRANSFORMATION LAYER, GENERALIZED
The Surface = the two-way translation between Tim's altitude and the depths (up-translate reality to his shape; down-translate intent to spec) — generalized to EVERY user + EVERY artifact. "The whole screen is the conversation," generative/spatial/reactive, never a text-wall = render-for-cognition as a product. The interface + memory/store Tim wants ARE the Surface's live-surface + memory principle. Built for the operator who reacts, doesn't specify (Tim's "I'm not a developer"). And it's how the Commander expresses intent to the Crew.

## THE ORGAN-MAP (LEAN reading — most of the DOWNSTREAM already exists; GROUND with ① before trusting)
✅ built · 🟡 scoped/partial · 🔴 net-new
| Surface organ (the doc) | Downstream piece | Status |
|---|---|---|
| Fan-out/fan-in (32×4B + 6 larger) §16 | the swarm + run_items/run_reduce + SlotBudget | ✅ |
| The Arbiter (higher model → next action) §16 | jury/confirm + run_reduce(role) + verify_lens | ✅ |
| Tags/counts/observations NOT confidence §8/9 | a LAW already (no-confidence-value) + marks + corpus | ✅ |
| Progressive type resolution §8 | run_role over the file-discovered registries | 🟡 |
| Schema activation → schema-rendered UI §20 | registries + ui:// addressing + the RHM render | 🟡 |
| Anchor/preference/rejection extractors §32 | run_items taggers (③ mine_exchange's exact shape) | ✅ pattern |
| Possibility generation / contrast sets §14 | ⑩ option-panels (develop_option × lenses → score) | ✅ |
| Decision ledger / assumption shelf §12 | the inbox + governance + the operator floor | ✅ |
| Spec compilation (state → handoff) §8/32 | ⑦ spec-compiler (decompose→expand→ground→triad_synth) | ✅ |
| Event/graph/vector/doc store + embedding ladders §18 | the store + vector_index + run-index + ① spaces | 🟡 (① embeds) |
| Memory (preference/rejection/project/decision) §23 | ③ transcript-miner + ⑨ durable-memory + corpus spaces | 🟡 |
| Execution handoff → downstream autonomous workflow §26 | THE WIRE (decision→claude -p→implemented) + this loop | ✅ |
| The live generative surface itself §11/12 | the FE / RHM organ (guided-review's lane) | 🔴 the build |

**Lean conclusion: downstream ~80% built; net-new = the Surface's own FE (the live generative surface) + upstream glue (artifact-type taxonomy, progressive-type-resolution wiring, schema→UI render). VERIFY with ① before trusting the percentage.**

## THE WIZARD LINEAGE (the upstream of the upstream — substrate-mcp)
Tim 2026-06-09: substrate-mcp holds *"a very large amount of content on the general term 'Wizard' — all ideas of mine augmented by AI, which augmented their incorrect understandings too but gets a lot of the ideas across."* The Surface calls ITSELF *"a non-linear dynamic live wizard"* (§10). So the Wizard content = the EARLIER EXPRESSION of this exact idea — the lineage/reference corpus, NOT generic content. The real reason the substrate-mcp exploration matters: the intent-history of the upstream.
- **The move (after ① is up):** substrate-mcp + the embedder — search/read 'Wizard', embed into space='wizard-lineage', `find_relations` vs the Surface docs AND ~/company → surface what the Wizard ideas got RIGHT that neither the Surface doc nor the engine has captured. RECONSTRUCT the meaning (the doc itself warns the AI augmentation baked in misreadings — reconstruct intent, confirm with Tim, don't copy the artifacts). Decide merge-vs-reference-vs-content-value.

## SEQUENCING (how the loop serves this frame)
1. **This doc** = the externalised reference (done). The loop reads it.
2. **① repo-exocortex** (GO — embedder proven) — ingest ~/company + the Surface docs → GROUNDS the organ-map (real reuse-cites, built-vs-netnew verified).
3. **Wizard-lineage exploration** — substrate-mcp 'Wizard' → space='wizard-lineage' → find_relations → reconstructed intent, confirmed with Tim.
4. **Dogfood ⑦ ON the Surface** — the Surface's thesis (vague→spec) IS ⑦; after ① grounds reuse, run ⑦ on project-base → an MVP build-triad, grounded, Tim steers. The system resolves its OWN intent.
5. **The Surface FE** = the net-new build — coordinate with guided-review (the FE/RHM-render lane); EXTENDS the RHM organ, not a 2nd renderer.

## THE STANDING TRUTH
The loop has been building the ORGANS. The Live Intent Resolution Surface is the BODY they compose into — the product face, and the surface through which Tim guides every future build. The loop now serves this frame.
