{
  "lane": "verify_lens",
  "files_written": [
    "roles/verify_lens.py"
  ],
  "new_role_ids": [
    "verify_lens"
  ],
  "how_verified": "RoleRegistry().discover(['roles']) shows verify_lens live (can_fire=True, is_jury=False, op=generate, in the 'verification' cast only, listening cast untouched: focus/recall/ground/connect/check/voice). Fired BY USE via run_items over 2 lens-units (correctness + adversarial-disprove) against a mock change (def add returns a-b but claims sum) on the resident chat-4b (company gpu: chat-4b ~8GB resident) -> both lenses returned a well-formed {lens,verdict,evidence,breaking_case}: correctness verdict=fail ('returns a-b not the sum'), adversarial-disprove verdict=fail with breaking_case='add(5,3) returns 2, not 8'. The lens rode in each unit (ctx['utterance']) and each draw judged through ITS lens only. tests/cognition_governance_acceptance.py = ALL 42 checks PASS (the floor: roles/* is in COG_SOURCES; verify_lens emits no resolve/approve/dispatch, launches no claude -p; the role-spec-no-forbidden-verb check passes). tests/roles_acceptance.py passes every structural check then fails at EXACTLY line 174 'drift: [verify_lens]' (the un-reflected-role drift-home assertion) and nothing earlier -> EXPECTED (I cannot edit roles/AGENTS.md this beat; the next beat's commit reflects it). The exact missing list is ['verify_lens'], confirming no structural defect.",
  "status": "built-verified",
  "needs_tim": "",
  "notes": "THE TALLY is the next-beat follow-on, NOT built here (flagged per brief): the deterministic verdict-tally over the cross-lens verdicts (green iff all pass / any fail->fail / any uncertain->FLAG) is an additive reduce-rule entry in mcp_face's _REDUCE_RULES (the OTHER lane this beat), run via run_reduce(mode='rule'). I did NOT add it here (it would collide). verify_lens is usable NOW via run_items + an existing reduce or a manual tally over the per-lens verdicts. DESIGN: verify_lens is deliberately NOT a draws-jury (no draws/verdict_rule) -- its 'jury' is the cross-LENS fan (N distinct lenses, one draw each via run_items), per COMPOSITIONS section 6; verify_jury.py/confirm_registration.py were mirrored only for the clean-output + self-registering ROLE-dict shape, not the draws mechanism. input_addresses stays the DEFAULT ('utterance',) -- lens/change/bar are KEYS inside the unit dict run_items places at ctx['utterance'] (byte-identical default run_role path), NOT separate declared inputs. verdict is Literal['pass','fail','uncertain'] (closed vocabulary -> the downstream tally is an exact-token reduce); breaking_case defaults to '' so non-adversarial lenses validate. mode_scope={'verification'} is a new cast-beyond-listening context (mirrors register_element's {'registration'}); an unknown mode yields an empty cast so the listening cast is untouched. NEXT BEAT must reflect verify_lens in roles/AGENTS.md (turns the one expected roles_acceptance red green) and commit. Did NOT commit (per brief). by-use harness at /tmp/verify_lens_byuse.py. UNIT CONTRACT for the next-beat cascade builder: each unit is {lens, change, bar} -- the key is 'change' DELIBERATELY (the brief's input is 'a diff/criterion/claim'; 'change' is the truer generalization), NOT the COMPOSITIONS section-6 example's 'diff'. The prompt reads 'change'; the verify-jury save_cascade must pass 'change' (do not copy the doc's 'diff' key verbatim or it mismatches the prompt)."
}