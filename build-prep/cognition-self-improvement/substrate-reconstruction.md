# Substrate-MCP Reconstruction — the AXES model + the Wizard/Virtual-Intelligence lineage

> **RECONSTRUCTED INTENT, NOT GROUND TRUTH.** Per Tim's caveat (2026-06-09): the substrate-mcp is the OLDEST work, NEVER reviewed by Tim — the AI-augmentation presents it as certain/complete when it ISN'T. So this doc CITES what the substrate SAYS (real chunks/addresses I retrieved) and reconstructs the underlying INTENT — it is evidence to CONFIRM WITH TIM + design fresh, never carried-forward assertion. Recency supersedes: the 3 Surface files (live-resolution-surface/, most recent) outrank the substrate framing on any conflict. Bounded first read (substrate-mcp, self-indexed, own ollama embedder); deeper passes can follow.

## A · THE AXES MODEL (G17 — the data/address/memory/state axes the Company describes thinly)
The substrate's SCHEMA (relation-kinds + axes + temporal_fields — structural metadata, more trustworthy than the unreviewed prose) ALREADY NAMES the axes Tim flagged. Source: `get_vault_landscape(['unification-vault','relative-difference'])`.

| Axis (Tim's term) | How the substrate modelled it (cited) |
|---|---|
| **HIERARCHY** | relation-kinds `parent` · `siblings` · `layer` · `parent_work`; folder hierarchy (11_EMERGENT/01_INVENTORY/02_PRINCIPLES) |
| **SEQUENCE** (logical order ≠ clock) | `follows` · `precedes` · `next` · `prev` · `stages_traversed` · `preceded_by` · `follows_protocol` |
| **SCALE / altitude** | the `layer` axis + `aspirational`/`implemented` (a maturity scale) — the altitude axis Tim names |
| **TEMPORAL** (G16, already first-class there) | `temporal_fields`: created · updated · attack_verified · discovered_date · date-of-this-articulation · generated · timestamp — a RICH temporal model, not a bolt-on |
| **TYPE-GRAPH** | the autopoietic type registry — 1,413 types ON AXES (area/domain/type/status); types have axis-placement |
| **STATE** | `state_axis = status` — state is a first-class declared axis |
| **STRUCTURAL** | `flows` · `gates` · `components` · `emitted_by_components` · `read_by` · `written_by` · `depends-on` · `derives-from` |
| **SEMANTIC** | the embedded chunks (own nomic-embed index) — the substrate's vector face |
| **ADDRESS / reference** | `reference_fields`: link · schema_ref · invocation · external_source_path · source_url · worktree |
| **ITERATION** (new — worth noting) | `round` axis (relative-difference) — iteration-as-an-axis (a sequence/temporal hybrid) |

**RECONSTRUCTED INTENT (confirm with Tim):** the substrate treated a unit's PLACE as a multi-axis coordinate — hierarchy + sequence + scale + temporal + type + state + structural + semantic — exactly the "data/address/memory/state" model the Company under-describes today. The Company has IDENTITY (the address scheme) + SEMANTIC (embedding spaces) richly; this is the evidence for making hierarchy/sequence/scale/state first-class **alongside temporal (G16)** — the relation-kinds above are the candidate vocabulary. **Design fresh** against the Company's registries (don't import the substrate's schema); the VALUE is the axis-vocabulary + that it's a coordinate-space, confirmed.
- ⚠ Note: a `certainty` axis exists in relative-difference — surfaced for honesty, but it's an axis-VALUE (enum/tag), not a confidence FLOAT, so it's consistent with the no-confidence law (tags/counts, not scores). Confirm Tim's read.

## B · THE WIZARD / VIRTUAL-INTELLIGENCE LINEAGE (the upstream-of-the-upstream of the Surface)
The Live Intent Resolution Surface has a NAMED ANCESTOR in the substrate. Cited chunks (unification-vault, dup in relative-difference):
- **"Virtual Intelligence"** (`inbox/01_glossary.md`, `inbox/00_project_brief.md`): *"the collective name for the adaptive wizard architecture the human has been building. An instance of virtual intelligence takes a user with a vague goal, maps that goal against a specific domain or platform... reaches an outcome without needing pre-knowledge, instructions, or specific intent."* → **This IS the Surface's core thesis** (vague intent → spec, by reaction not specification), under the earlier name "Virtual Intelligence / adaptive wizard."
- **"wizard"** (glossary, vi-sense): *"a guided flow for users who lack pre-knowledge, instructions, or specific intent. The wizard shape is not fixed; it adapts to the user's goal and the domain."* → the Surface's "non-linear dynamic live wizard," schema-rendered/adaptive.
- **EM-064 "Schema Cascade"** (`11_EMERGENT/EM-064 Schema Cascade.md`): *"one `engine_schemas` row produces: StructuredOutput fields (AI processing), MCP tool definitions (agent interface), graph edge rules (relationship types with inverses), trigger reactions."* → **prototypes BOTH the Surface's schema-rendered UI (§20) AND the Company's own registry-is-truth/projection principle** (one source → all consumers). Convergent: the substrate reached for the same projection law the Company now lives by.
- **"Structured-output configuration through conversation + live preview"** (relative-difference) + **schema-binding as a first-class entity with its own lifecycle** → the Surface's live spec-compilation + the configure-by-reacting model.
- **EM-867 "Wizard-Engine Crossover as Architecture Validation"** → the wizard-engine was used to PRESSURE-TEST the vault architecture (the substrate's own self-validation move — the same dogfooding pattern).

**RECONSTRUCTED INTENT (confirm with Tim):** the Surface (most recent) = the matured crystallization of "Virtual Intelligence / the adaptive wizard" (oldest). The lineage carries: the vague-goal→domain-mapping→outcome thesis; the schema-cascade (one schema → many projected consumers); configure-through-conversation + live-preview; schema-binding-as-first-class. These are INTENT to design fresh into the Company's Surface build — NOT artifacts to import.

## C · MERGE vs REFERENCE vs CONTENT (the decision — leans REFERENCE, per the caveat)
- **NOT content-merge.** The substrate is oldest + unreviewed + AI-falsely-complete; importing its content into ~/company would violate the no-fiction floor. (And it's a separate embedder — nomic vs bge-m3 — so the vectors don't transfer anyway.)
- **REFERENCE / intent-archaeology = the value.** Two concrete recoveries worth designing-fresh into the Company: (1) the **AXES coordinate-model** (§A) → informs G17 + the temporal-axis family; (2) the **Schema-Cascade + Virtual-Intelligence lineage** (§B) → informs the Surface build (it validates the Surface's thesis has deep roots + that the Company's projection law was already Tim's instinct).
- **OPEN for Tim:** is there specific CONTENT (not framing) worth pulling — e.g. the relative-difference math-verification body (356 files), or the visual-DNA/visual-design corpora — as reference material for specific future artifacts? That's a per-need call, not a wholesale merge. (Part 2 — find_relations substrate-vs-~/company — needs ① online to compare; deferred to after G18.)

## STATUS
Bounded first reconstruction (2 of 8 vaults' schema + one Wizard search). Reconstructed-intent, caveat-bound, for Tim to confirm. Next (deeper): the full type-graph axis-values, the other vaults, and (after ① online) the find_relations substrate↔~/company comparison for the merge-per-need call. Feeds G17 (the AXES principle doc) + the Surface FRAME.
