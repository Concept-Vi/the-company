# MCP / System Gaps — the running ⑫ backlog (for next workflows)

> Things the engine/MCP needs that surfaced BY USE. Each is a buildable seam for a future workflow (the engine-improves-engine front-end reads this). Status: 🔴 open · 🟡 partial · ✅ closed. Tim's standing law: first-use friction is the input, not a failure — note it here as found.

## Surfaced 2026-06-09 (the registry-coverage audit test — "use the models")
- ✅ **G1 · Ephemeral/draft-role run path on the MCP face.** CLOSED (15ebc90 — run_draft + run_draft_items). Original: `run_role`/`run_items` take a registered role id (string). A one-off bounded classification (e.g. "classify these 39 candidates once") forces either `create_role` (commits a throwaway role → registry pollution + a `.git/index.lock` race with any concurrent commit) or dropping below the MCP. NEED: `run_role(draft_spec={...})` / `run_items(draft_role={...})` — fire an inline role spec, validated + run, NEVER written/committed. (The internal `_resolve_role` reportedly accepts a dict draft; the MCP wrapper types `role: str` and doesn't expose it.) This is the grunt-offload reflex's (⑪) missing primitive. [testing the draft path now]
- 🔴 **G2 · New role not live in the running MCP server until rediscover.** A role file written to `roles/` is only seen by the running bridge/MCP after `create_role`'s in-process `_rediscover_roles` (or a server restart). So you can't "write a role file + immediately run_items it" through the live server without committing via create_role. Relates to G1 (the draft path sidesteps this entirely).

## Surfaced earlier (the compositions grounding — COMPOSITIONS.md / WORKFLOW-ARCHITECTURE.md seam backlog)
- ✅ (closed: the G3-CLOSED section — S1/S2 landed; the proof ran) **G3 · Cascade multi-variable prompt substitution.** A saved cascade decl threads ONE `inputs` value + fixed per-step roles; richer per-step `${var}` interpolation into prompt_templates is unbuilt.
- ✅ **G4 · The `retrieve`/`similarity` cascade ops.** No engine primitive — retrieve runs inline + feeds `inputs`. Blocks the corpus-RAG cascades (①/④/⑨) from being fully save_cascade-able.
- ✅ **G5 · Role-scoped capability gating.** B5 projects op/thinking/tools against the BRAIN, not a role's bound model. A role bound to a non-brain model gets the wrong capability set.
- ✅ **G6 · `suite.py:capability_providers()` live-bind resident-only.** CATALOG (C2.5) widened the model DATA; the live-provider set the swarm binds against is still resident-4b-only. The other half of the catalog widening.
- 🟡 **G7 · Embedder-resident handling under contention.** `ensure_resident(op=embed)` proven on a stub; unproven BY USE under real swap on the contended card (the V-A RG10 cluster-reduce is the first real test). No steady state yet → the swap path matters.

## How a future workflow consumes this
⑫ (engine-improves-engine) reads this file → scouts each gap against the code → drafts a buildable lane-cut → I review + dispatch. Closed gaps move to ✅ with the commit sha.

## Surfaced 2026-06-09 (running the unified V-A workflow)
- 🔴 **G8 · The workflow Commit-phase mega-agent times out (API stream-idle).** One agent doing gate + 6 commits + AGENTS.md + records is too big — it died on the stream-idle timeout TWICE (the build pieces stranded, adopted manually). FIX: split the Commit phase (one agent per piece, or a pipeline), OR make the runner (me) commit directly rather than via a sub-agent. Workflow-pattern lesson: keep agent turns bounded; a long serial commit agent is fragile. The build-parallel/commit-serial split should be commit-per-piece, not commit-all-in-one.

## Surfaced 2026-06-09 (the seam-build beat — verify-jury)
- ✅ **G9 · The verify-jury verdict-tally reduce-rule.** BUILT (d2a88fd — verdict-tally in _REDUCE_RULES; verify_lens+tally chain works via MCP run_reduce). Original: verify_lens (⑥) is built+live; the deterministic green-iff-all-pass / any-fail→fail / any-uncertain→FLAG tally over the cross-lens verdicts needs an additive entry in mcp_face's `_REDUCE_RULES` (run via `run_reduce(mode='rule')`) + a saved verify-jury cascade (unit contract `{lens, change, bar}`). Next-beat follow-on (deferred from the seam beat to keep it file-disjoint from G1's mcp_face lane).

## Surfaced 2026-06-09 (the G9 verify-jury beat) — a LAW-0 unification gap
- ✅ **G11 · Parallel reduce-rule literals (built-twice) block run_cascade.** CLOSED (07e36a7 — single-sourced to cognition.REDUCE_RULES; run_cascade(verify-jury) runs end-to-end). Original: The named reduce-rules live in TWO places: `mcp_face/server.py:_REDUCE_RULES` (the MCP `run_reduce` path) AND `runtime/suite.py:842 _CASCADE_REDUCE_RULES` (the `cognition.run_cascade` path) — the suite comment even says "mirrors the MCP run_reduce _REDUCE_RULES seam." So `verdict-tally` (added to _REDUCE_RULES) is invisible to `run_cascade('verify-jury')` → it fails loud ("not a known named rule [concat,count,first]"). PROVEN (G9 lane): with the rule injected in-memory, run_cascade ran the verify-jury end-to-end — the parallel dict is the ONLY blocker (NOT G3). FIX (reuse-don't-parallel): single-source the two dicts (the cascade runner reads the one _REDUCE_RULES) so a rule can't be added to one and not the other. Unblocks run_cascade('verify-jury') = ⑥ as a one-call saved cascade. suite.py is clean → a safe targeted next beat. The exemplar built-twice for the whole registry-is-truth law.
- 🟡 **G12 · option-panel save_cascade.** ⑩ works now via run_items+run_reduce; a one-call save_cascade('option-panel') wants the per-lens {lens,question,constraints} units templated → G3 (cascade multi-var). Deferred to G3.

## Surfaced 2026-06-09 (the ③ transcript-miner beat)
- 🟡 **G13 · The ③ embed-cluster (cross-transcript failure-pattern discovery).** mine_exchange + judge_mining are BUILT+live (cf0c225); a bounded 13-exchange run proved the organ works (surfaced the verification-discipline cluster + my avoidance-corrections, mapping onto standing feedback-*.md). The REDUCE half — run_reduce(mode='cluster') over space='history' across ALL 462 transcripts → NAMED standing failure-patterns → draft new feedback-*.md (for my review) — needs the bge-m3 embedder window (capture()/embed-on-write into space='history'). Embedder-gated (with ①/②/RG10). The mining MAP works on the 4B now; the discovery is the cluster.

## Surfaced 2026-06-09 (Tim's directives — embedder GO + the swap-approval design)
- ✅ **LIVE-EMBED CLOSED (the standing needs-tim).** Tim greenlit autonomous embedder use ("the tools should handle it; agents should perform the actions they need"). Proven by use: `run_role('embed', op='embed', ensure=True)` → real 1024-dim BAAI/bge-m3 vector, the embedder loaded into free VRAM (no eviction, no stomp). The embedder-gated backlog is now **GO autonomously**: ① repo-exocortex · ② drift-radar · ③ failure-pattern cluster (G13) · RG10 convergence · G7. (No "surface to Tim" for the embedder anymore — only RG10's operator-APPROVE moment stays Tim's, the floor.)
- ✅ **G14 · The swap-approval response (Tim's design).** ensure_resident today: fits→loads; needs-eviction→fails-loud OR `evict=True` auto-evicts. Tim's spec: when a load needs a SWAP (eviction), the actuator should RETURN "swap needed — approve?" to the agent (an interactive approve-at-call-time), not hard-block and not silently auto-evict. The embedder FIT (no swap) so this wasn't triggered now, but the design is owed for when a future load needs the card cleared. (Build into the #50 actuator + surface through run_role(ensure=) / the MCP.)
- **JUDGEMENT CALLS = mine** (Tim: "I decide, surface outcomes — so long as it's easy to find and update them"). The registry-candidacy (6 vocabularies) + the built-twice one-sourcing are MY calls now; the binding constraint = whatever becomes a registry / one-source must stay EASY TO FIND + UPDATE (registry-is-truth, discoverable, never buried).
- ✅ **SUBSTRATE-MCP exploration (DONE 2026-06-10 — SUBSTRATE-EXPLORATION.md) (Tim, "maybe after").** The `substrate-mcp` is an EARLY simplified prototype of one part of the substrate — separate from ~/company — holding ~6 Obsidian vaults of Tim's AI-augmented bodies-of-work (the hallmark output of this whole system; his work, augmented). COULD be merged (feasible via a composition of these workflows + the loop + embedding/swarm/company tools), OR reference-only, OR its CONTENT is the value — TBD. Interact with it somehow + work out the value. AFTER the embedder spine (①/②/③-cluster/RG10). A real exploration beat (read it, embed its vaults into a space, find_relations against ~/company, decide merge-vs-reference).

## ★ THE FRAME (2026-06-09) — the loop now serves the Live Intent Resolution Surface
Tim confirmed: `build-prep/live-resolution-surface/` (the Surface) is the UPSTREAM half of the Company (intent→spec); the cognition engine + these compositions + workflows + cron + the wire are the DOWNSTREAM half (spec→artifact). TWO HALVES OF ONE CIRCUIT — the product the loop now serves. Full externalised reference: build-prep/cognition-self-improvement/FRAME-the-two-halves.md. Lean reading: downstream ~80% built (verify with ①); net-new = the Surface FE + upstream glue. NEW priority order for the loop: ① repo-exocortex (grounds the organ-map) → Wizard-lineage exploration (substrate-mcp 'Wizard' = the upstream-of-the-upstream, Tim's AI-augmented idea history) → dogfood ⑦ ON the Surface (vague→spec IS ⑦) → the Surface FE (coordinate guided-review). The substrate-mcp exploration is now FRAMED: search 'Wizard', it's the lineage, reconstruct-intent not copy.

## Surfaced 2026-06-09 (① repo-exocortex — the real blocker, verified)
- ✅ (closed: ① COMPLETE — projections/repo.py live, 584 files embedded) **G15 · No 'repo' embeddable space.** ① assumed `capture(...projection='repo')` but the live embeddable spaces are only `principles/topics/worldview` (the coherence/discovery lenses) — a record naming `projection='repo'` FAILS LOUD ("not an embeddable space"). So ① could never have populated regardless of the ingest lane's stream-timeout. UNBLOCK (small, mine, declarative-direct): declare `projections/repo.py` — a file-discovered EMBEDDABLE projection mirroring `projections/topics.py` (the repo-digest lens: a file→its summary/concepts, embedded into space='repo'). THEN the deterministic walk + `capture(role='repo_digest', projection='repo', ...)` populates it. This is the ① prerequisite; do it next beat, then run the bounded ingest by use (embed-bge fits co-resident, no swap). Grounded organ-map (bbf86e2) confirms: corpus EMPTY, ① code-exists-but-offline. NOTE: the GROUND lane's organ-map = real, committed; the INGEST lane failed (G8 fat-agent stream-timeout again → do the ingest DIRECTLY, not via a timeout-prone sub-agent).
- **Two reuse-decisions surfaced by ① GROUND (Tim's call, non-blocking):** (a) RESOLVED (Tim 2026-06-09: "don't use episodic memory — it sucks"): ⑨ durable-memory builds on the CORPUS+SPACES (+ the file-memory ~/.claude/.../memory/), NOT episodic-memory. No extend/complement of episodic-memory. (b) RESOLVED (Tim 2026-06-09 "no confidence, tags and counts"): ENFORCE no-confidence as a hard invariant — tags+counts are the evidence, never a confidence float. + NEW LAW: the TEMPORAL AXIS is first-class (a count is a trajectory; recency-weighting; decay; replay). See PRINCIPLE-tags-counts-temporal.md.

## Surfaced 2026-06-09 (Tim's tags+counts+temporal law)
- ✅ (closed: no_confidence_acceptance standing; tags landed; temporal axis folded into GC14/Track-1) **G16 · No-confidence ENFORCED + counts-as-trajectories + temporal-axis-first-class.** Tim: "no confidence, tags and counts; temporal axis plays a real role." (1) Enforce no-confidence (a floor/schema invariant; migrate the existing confidence:float fields on register_element/judge_mining/etc. to count+trajectory). (2) A count carries its temporal shape {count, first_seen, last_seen, slope} not a scalar (rising/fading/steady/bursty). (3) Recency/decay-weighted retrieval over the corpus+spaces; the user/project model is a time-series. (4) ②drift + ③failure-patterns gain the temporal dimension (drift = diverged-over-time; failure = rising-or-fixed). Full principle: PRINCIPLE-tags-counts-temporal.md. Design choices surfaced for Tim (decay function · trajectory granularity · temporal-space-vs-weighting). The trajectory-count + the no-confidence invariant are the first concrete builds.

## Surfaced 2026-06-09 (Tim — the AXES family, beyond temporal)
- 🔵 **G17 · The AXES of the data/address/memory/state space are under-described.** Tim: temporal (G16) is ONE of a family — also HIERARCHY · TYPE-GRAPH · SCALE · SEQUENCE (near each other + near temporal). The Company has rich IDENTITY (the address system) + SEMANTIC placement (embedding spaces) but THIN description of hierarchy/type-graph/scale/sequence — and "the data space, address space, memory, state" aren't well-described overall. These axes are exactly what the Surface's UPSTREAM needs: progressive type-resolution (Thing→Artifact→Web-app→portal) = HIERARCHY+SCALE; the workflow-lifecycle = SEQUENCE (logical order ≠ temporal clock); counts-over-time = TEMPORAL. THE MOVE (after ① is online so it's GROUNDED): a RESEARCH WORKFLOW — agents map, from the SH self-hosting state + the code + the Surface docs, how each axis (semantic·temporal·structural·hierarchy·type-graph·scale·sequence) IS or ISN'T described across data-space/address-space/memory/state → propose how each becomes first-class (like temporal did) → write an AXES principle doc + the gaps. Unbiased fan-out (research-wave style), grounded in the now-online corpus. Tim: "those are the right terms and near each other... might help, maybe not" — exploratory, real, his framework (relational systems; the altitude layer IS the scale axis).

## Surfaced 2026-06-09 (① ingest failed by use — real bug + Tim's substrate-mcp redirect)
- ✅ (closed: the G18-FIXED section — source_address guard + code:// keys) **G18 · capture() uses the UNIT verbatim as source_address → long-content units overflow the filename.** The ① ingest fired capture(role=repo_digest, units=[file-CONTENT,...]) → OSError "File name too long" (the unit text became the .data/store/refs/run__corpus...<file-content>.tmp filename). ROOT: capture treats each unit AS the source_address (the retrieval key) — units are meant to be ADDRESSES (short keys the role resolves to content), NOT content blobs. FIX (the clean one, ties to G17/Tim's "address space under-described"): repo-ingest units = file ADDRESSES (code://<path> — code:// IS a registered scheme) that repo_digest resolves to content via resolve_address; OR capture gains a source_address-distinct-from-content param. ① STILL NOT ONLINE (corpus empty) — this is the real blocker now, past G15. Caught BY USE (verify-by-use working). embed-bge IS resident (warmed this beat, real bge-m3 vector).
- ★ **G17 REDIRECT (Tim 2026-06-09 "I meant in the substrate mcp"):** the AXES research (hierarchy/type-graph/scale/sequence/temporal) centers on the **substrate-mcp**, NOT (just) the SH state — substrate-mcp is the EARLIER PROTOTYPE of "one part of the substrate" where Tim grappled with exactly type-graphs/hierarchies/addresses/scale/state. So G17 (axes) + the Wizard lineage + the substrate-mcp exploration (h) are now ONE beat: explore substrate-mcp for (a) how it described the AXES (the data/address/memory/state model the Company describes thinly), (b) the 'Wizard' content (the upstream-of-the-upstream of the Live Intent Resolution Surface). The SH-state/codebase "would help too" but substrate-mcp is the target.

## Surfaced 2026-06-09 (substrate-mcp orientation — it's rich + SELF-INDEXED)
- ★ **substrate-mcp is a SERIOUS grounded corpus, NOT a throwaway prototype, and it's SELF-INDEXED → the axes/Wizard exploration does NOT need ① online.** get_status: 8 vaults · ~5,486 files · ~65,000 EMBEDDED chunks · 1,413 types (the autopoietic type registry). Biggest: unification-vault (2803f/25k chunks — Tim's core "unification"), relative-difference (639f/19k — the field-of-science body), visual-design-corpus (1243f), visual-dna, vi-context-design, elevenlabs-mcp, ulm-inventory. Own embedder (ollama nomic-embed-text, healthy — DIFFERENT from the Company's bge-m3, a merge-detail). It ALREADY MODELS THE AXES Tim flagged: a type-graph ON AXES (status/domain/…), state-axis + tag-axes, relation-kinds (structural), schema-supersession history (vocabulary drift OVER TIME = temporal). KEY UNBLOCK: because it has its own live vector index, the G17 axes-research + the Wizard-lineage exploration can run NOW via search_semantic/get_type_graph/get_vault_landscape — independent of the G18 ① ingest blocker. So the substrate exploration moves UP (no longer "after the spine"). NEXT BEAT: the substrate-exploration workflow — (a) get_type_graph + get_vault_landscape('*') → how it models the AXES (the data/address/memory/state principles the Company describes thinly); (b) search_semantic 'Wizard' across the vaults → the Live-Intent-Resolution-Surface lineage (reconstruct intent, the AI-augmentation baked in misreadings); → an AXES principle doc + a Wizard-lineage doc + merge-vs-reference-vs-content decision. Embedder co-resident note: substrate uses ollama nomic (:11434), Company uses bge-m3 (:8001) — both can be up; no contention with chat-4b for the substrate reads (they're ollama-served).

## ★ CAVEAT (Tim 2026-06-09) — the substrate content is OLDEST + UNREVIEWED → false certainty
Tim: the substrate-mcp is the OLDEST work and was NEVER reviewed by him — so the AI-augmentation presents a lot of it as certain/complete/authoritative WHEN IT ISN'T. The three Surface files (live-resolution-surface/) are the MOST RECENT crystallization of the same lineage. Temporal ordering of Tim's own corpus = the signal: substrate(oldest,unreviewed,falsely-complete) → the Company → the 3 Surface files(most recent). This is G16 (the temporal axis) applied to HIS work: RECENCY SUPERSEDES — where old substrate and the recent Surface diverge, the recent wins; old content is lossy unverified evidence, never authority (the patterned-visibility / reconstruct-intent principle).
**EXPLORATION DISCIPLINE (binds every substrate-exploration agent — no-fiction floor):**
1. Treat EVERY substrate claim as UNVERIFIED EVIDENCE — never ground truth, never "complete because it presents so." Strip the AI-inflated certainty.
2. Reconstruct the underlying INTENT (the MEANING), not the surface artifacts.
3. Recency supersedes: the 3 Surface files outrank old substrate framing on any conflict.
4. Output = reconstructed-intent + a confidence/recency mark + "confirm with Tim" — NOT carried-forward substrate assertions.
5. → merge-vs-reference leans HARD to REFERENCE / intent-archaeology, NOT content-merge: importing unreviewed false-complete content into ~/company would violate the no-fiction floor. The VALUE is the recovered intent (esp. the axes model + the Wizard lineage), confirmed with Tim, then designed fresh — not the artifacts.

## Surfaced 2026-06-09 (① ingest ONLINE + the query-path dim note)
- ✅ **G18 FIXED + ① STORAGE ONLINE.** runtime/corpus.py write_record now guards source_address (len>200 or newline → CorpusError with guidance: it's a retrieval KEY/address, content goes in `output`) — fail-loud at the boundary, not a raw OSError. By use: content-blob→CorpusError, address→works; corpus 23/floor 42/drift 5. THEN ran the ① ingest the RIGHT way (source_address=`code://<path>`, repo_digest digests the content, projection='repo') → **list_corpus(company) = 8 records, embedded at capture into space='repo' — ① STORAGE IS ONLINE** (the corpus was empty for ~6 beats; now populated). Learnings: capture_corpus's return-shape keys read None but list_corpus is the truth (8); run_role returns the validated schema dict DIRECTLY (not wrapped in `output`).
- ✅ **G19 · ① QUERYABLE — verified by use.** CLOSED. The 33-dim was MY HARNESS (passed text where query_index expects a query_VECTOR); through the real embed path dims match (1024). PROVEN: A raw `query_index(space='repo')` from my harness raised `vector dim mismatch: 33 vs 1024` — the QUERY embed produced 33-dim while the stored vectors are bge-m3 1024. Likely my-harness (calling query_index without the resident embed config) NOT an engine bug — the real query entry is `find_relations` (wires the embedder + needs near+far spaces). NEXT: verify ① is QUERYABLE by use through find_relations (or the MCP query path) with the embedder resident — that's the true "① online" gate (storage ✅, query 🟡-unverified). If find_relations also mismatches, it IS a real dim-consistency bug (the GROUND lane's flagged "one silent-correctness hazard") → fix the query embed to use the same model/dim as capture. Then: full repo ingest (not just 8) + ② drift + the substrate↔~/company find_relations comparison are all unblocked.

## ✅ 2026-06-09 — ① REPO-EXOCORTEX ONLINE (storage + query both proven by use)
The keystone is LIVE. query_index over space='repo' (query embedded via the real bge-m3 path, 1024-dim) → "the embedding model and vector configuration" returned code://fabric/config.py (0.485, the exact file defining the embed model) · contracts/address.py · fabric/vram.py — REAL semantic retrieval over the repo corpus. ① = ask-the-codebase, working. The 8-file bounded ingest proves the pipeline end-to-end (extract→digest→capture→embed→query); the FULL repo ingest is now a scale-run, not a question. UNBLOCKED: ② drift-radar (cluster over space='repo'), the substrate↔~/company find_relations comparison (the merge-per-need call), ④ retrieve-as-input, ⑨ durable-memory, ⑫ scout-over-corpus. NOTE for the query API: query_index takes a query_VECTOR not text — a `query(text, space)` convenience that embeds-then-queries would be the clean agent-facing wrapper (small G — the agent shouldn't have to embed first); find_relations does this internally but needs near+far. Flag: a text→query convenience (G20, minor).

## 2026-06-09 — ① scaled to 84 records (the core); ingest is a reusable module
- ✅ ① at REAL scale: exocortex_ingest.py (reusable) ran over the 78 core .py → corpus 8→84, embedded in space='repo', queries discriminate (GPU q → ops/cli/gpu.py @0.586). UNBLOCKS ② drift / substrate-compare with real material. Re-run on more dirs (build-prep, canvas, tests, design) to grow toward the WHOLE repo — active, not deferred (next ingest passes).
- 🟡 **G21 (active) · capture() MCP tool can't separate source_address from input.** The capture tool fans role over units where unit = BOTH source_address AND the role's input — so it can't do "path-as-key, content-as-input" (the repo-ingest shape); exocortex_ingest works around it (run_items + capture_corpus directly). FIX (active backlog): a `source_addresses=[...]` param on capture (parallel to units), OR promote exocortex_ingest's pattern to a Suite.ingest_paths method + MCP tool (the clean first-class home). Not deferred — sequenced after ② is moving.
- note: query_corpus returns query_index's rows verbatim ({id, score}) — honest reuse; an agent reads `id` (the code:// address). Fine as-is.

## 2026-06-09 — ① grown to 130 records (engine + node-types + Surface docs + design-system)
Ingested nodes/ + build-prep/cognition-self-improvement/ + build-prep/live-resolution-surface/ + design/_system/ via the reusable module → corpus 84→130. Now spans CODE + the Surface DOCS (so the substrate↔company comparison has the company side) + the design system. Cross-domain query discriminates (intent→spec query → Surface docs; embed-node query → nodes/embed). ② drift-radar can now run HONESTLY over a broad corpus (not "clean because we only looked at 84"). Still growable (tests/, canvas/, the rest of build-prep) — active, sequenced, ②-is-honest-enough-now.

## 2026-06-09 — ② DRIFT-RADAR works (file-level); a symbol-grain pass is the deeper ②
- ✅ ② built+proven: cluster(space=repo, 130 recs) → judge_drift (built-twice|overlap|distinct) → mark. By use: filtered 4 false-positive near-pairs to 'distinct', surfaced 1 genuine OVERLAP (nodes/retrieve.py ↔ nodes/similarity.py — shared cosine core) + marked it. judge_drift = the FP guard (was the whole point). The loose 0.82 cluster over-groups (mega-clusters of 'all docs') — judge_drift is what makes it useful.
- 🟡 **G22 (active) · ② symbol-grain drift.** File-DIGEST clustering finds FILE-level overlap; the built-twice I know (sampling-keys ×3 [now 1 via G11], plan/apply ×2, propose/panel/extend ×2) are INTRA-file symbol/vocabulary duplications → invisible to file-clustering. A deeper ② pass clusters at SYMBOL/vocabulary grain (e.g. ingest the literal-vocabularies from the registry-coverage audit as units, cluster those) — active, sequenced. Also: a tighter cluster_threshold + dropping the mega-clusters pre-judge would sharpen the file-level pass.

## 2026-06-09 — ③ FAILURE-PATTERN CLUSTER works (G13 closed); patterns map to existing memories
- ✅ ③ ran end-to-end (the embedder-gated half, now unblocked): extract exchanges (3 substantive transcripts) → run_items(mine_exchange, 39) → run_reduce(cluster) → 28 clusters / 7 recurring. The DOMINANT recurring pattern = verify-by-use (9×) — stable across sessions (matched the earlier 13-run). The 5 exchanges carrying a CORRECTION/error (the failure signal): verify-by-use · reuse-dont-parallel · bias-from-personas · no-file-no-echo · focus-concurrency-not-replacement.
- HONEST FINDING (no green-paint): EVERY recurring failure-pattern maps to an EXISTING feedback memory (verify-before-claiming · not-a-replacement/make-each-thing-work · unbiased-fan-out · expand-dont-echo) → NO new memory drafted this run; the data VALIDATES the memories are on-target + recent discipline held (verify-by-use dominates = the one most internalized). ③ is the mirror: it confirms the standing memories empirically rather than inventing.
- ◑ **G23 (batches 1-2 done; space live; batches continue) (active) · ③ at scale + persisted.** This was 39 exchanges / 3 transcripts; the full 462-transcript mining + persisting extracts to space='history' (the ⑨ durable-memory overlap, corpus+spaces NOT episodic-memory) is the scale run — would surface tail-patterns the bounded run misses + a queryable history space. Active, sequenced (GPU-heavy — a bounded batch per beat toward all 462). The named-cluster synthesis (a cluster→name reduce-role) would auto-name the groups vs my manual read — a small ③ enhancement.

## ✅ 2026-06-09 — RG10 CONVERGENCE PROVEN by use + G16 no-confidence ENFORCED on the registration chain
The V-A headline (a once-mute mockup element → a grounded proposed dossier → surfaced for Tim's approve) is PROVEN by use on the C1-inbox mockup, the **manual cascade form** (build-prep/cognition-self-improvement/registry_generation_run.py — the reusable module, parallels exocortex_ingest.py). GROUND(screen_reader) → MAP(run_items register_element ×43, grounding SUPPLIED) → REDUCE(cluster 43→28) → CONFIRM(run_jury + refcheck floor): **43 → 28 clusters → 14 confirmed / 14 flagged**. Surfaced to Tim via surface_review (**s94-review**, unresolved → his gate; the floor held). Batch artifact: rg10-batch-C1-inbox-desktop.json. [M]-handoff posted to MESSAGES.md.
- ★ **THE FINDING (two-layer gate vindicated by use):** the deterministic refcheck floor caught **3 INVENTED feature ids** (INB-focus/INB-excluded/INB-propose) the 4B coins by analogy to the real INB-* family — **DESPITE** a verbatim-copy prompt instruction I added. Prompt-engineering alone did NOT stop the fabrication; the floor did. This is the strongest by-use evidence yet that no-fiction must NOT rest on the model (the confirm_registration two-layer design is right). Layer 2 (the jury) FLAGGED 11 — a MIX of real mis-grounding (e.g. process←INB-lanes, 0/3) AND correlated-sample variance (the E4 caveat the role's own docstring warns of: N draws on one 4B measure variance, not independent error). Layer 1 is the GUARANTEE; Layer 2 raises-and-flags, never the floor — don't over-credit the soft layer.
- ✅ **G16 — no-confidence ENFORCED as a STANDING INVARIANT (all 3 roles + a gate, not a per-role cleanup).** `register_element` confidence:float → `grounding` tag (built|proposed|uncertain|defer); `judge_mining` confidence:float DROPPED (the `grounded` bool + the named `unsupported` field ARE the evidence — the float was redundant); `score_options` ScoredOption.score:float → `rank:int` ordinal (the fake-precision clone — its own docstring admitted it "mirrors confidence:float"). ★ THE KEYSTONE (Tim's "enforce, don't convention"): **tests/no_confidence_acceptance.py** — a registry-walk over EVERY discovered role's output_schema (+ nested sub-models) that FAILS LOUD on any field named `confidence` (22 schemas clean; a new confidence-field role can no longer pass the gate). Empirical justification: the floats were FLAT NOISE (register_element all 0.85). **VERIFIED BY USE (not just discovery — each role FIRED post-migration):** register_element via the RG10 run (real built/proposed tags on every dossier); judge_mining fired → `{grounded:False, unsupported:'...'}` (NO confidence, grounded a real bool — it even caught a subtle paraphrase mis-grounding); score_options fired → `scored:[{lens, rank:1/2/3, why}]` (int ordinals, no float). roles_acceptance 30/30, drift 5/5, no_confidence 2/2; the new suite registered in STATE (refresh_self_description). RG3 dossier-shape change announced to [M] (ConfidenceTag→GroundingTag; registry_writeback provenance→grounding).
  - ⚑ **FOR TIM (a design consequence of my score→rank judgment call — surfaced, not buried):** an ordinal `rank` drops MAGNITUDE — a 0..1 score showed whether the runner-up was CLOSE (0.82 vs 0.78) or FAR (0.82 vs 0.20); a pure rank can't. ⑩'s job is grafting runner-up strengths onto the lead, where that distance sometimes matters (the `why` field carries the qualitative reasoning, but not the gap). The migration is defensible under your no-confidence law + within my authority — flagging it so you can say if you'd want the magnitude kept as a TIER TAG (e.g. lead/close/far) rather than a float. Not reversing it on my own.
- 🟡 **G16 (remaining) — the TEMPORAL half only:** the no-confidence/tags part is DONE + enforced (above). What's LEFT is net-new — count-as-trajectory ({count, first_seen, last_seen, slope}), recency/decay-weighted retrieval over corpus+spaces, the temporal drift/failure queries (②/③ gain "diverged-over-time" / "rising-or-fixed"), the time-series user/project model. Tim's 3 design-choices stay open (decay fn · trajectory granularity · temporal-space-vs-weighting) — his axis to shape.
- 🟡 **RG10 engine seams (SEQUENCED — [C]'s next build) — make run_cascade('registry-generation') run end-to-end:** the 3 seams threaded MANUALLY in the proof (the manual run IS the spec): (a) {mockup}-keyed screen_reader ground per element; (b) resolve-once shared exemplars/inventory ctx into run_items/run_cascade; (c) confirm step firing run_jury+confirm_status (not a single run_role). Plus save_cascade('registry-generation') once the seams land. Then the **full-940 scale run** (23 mockups) — a scale-run, not a question.
- 🔵 **NOW-UNBLOCKED:** the substrate↔~/company find_relations comparison (both corpora exist — note the cross-embedder caveat: bge-m3 vs nomic) + ② symbol-grain (G22) can ingest the registry-coverage vocabularies.

## ★ 2026-06-09 — THE MCP-DESIGN PROGRAM (Tim's #1 priority: full coverage through the MCP + agent-intuitive)
Standing law: build-prep/cognition-self-improvement/MCP-DESIGN-PRINCIPLE.md (A, 9d23dfb). Plan C: principle → corpus consolidation → sweep.
- ✅ **G20 · ask-the-codebase via the MCP — CLOSED** by corpus(op=query) (the modular consolidation, not a flat tool). Suite.query_corpus existed; the gap was the face.
- ✅ **MCP modularity** — tools now file-discovered (mcp_face/tools/<resource>.py, register(mcp,suite)) — mirrors the registries; each cluster consolidates in its own file → parallel-safe (no longer the one-hot-file serial bottleneck).
- 🟡 **MCP-SWEEP (active, next):** create_* 8→1 `create(kind=,spec=)` (biggest win, registry-is-truth) · marks (mark + marks(by=)) · runs/state · node verbs · rule(op=) · the discovery cluster. Each → its own mcp_face/tools/ module. The audit + plan table: MCP-DESIGN-PRINCIPLE.md.
- 🟡 **MCP coverage pass:** expose still-direct-only capabilities (compositions ②/③/⑥/⑦/⑩ runnable via MCP; ingest_paths G21) until "direct" is unnecessary.
- 🟡 **Intuitiveness eval:** fresh-agent drives the core workflows through the MCP unaided (the Anthropic eval-driven bar) → refactor → re-eval.
- NOTE: a consolidated/new MCP tool is LIVE only after a /mcp reconnect (stdio server). corpus(op=) lands on Tim's next reconnect; THEN MCP-test + eval.

## 2026-06-09 — MCP-SWEEP next: create_* 8→1 (the plan + the wrinkle, for a clean beat)
B (corpus) PROVEN by use via the live MCP: corpus(op=query, space=repo) → store/vector_index.py(0.61)/nodes/embed.py(0.56) (real ask-the-codebase, core files present); corpus(op=list, detail=concise) → light 4-field records, capped (the 65k fixed). G20 closed end-to-end.
NEXT = create_* 8→1 → `create(kind, spec)` in mcp_face/tools/create.py. THE WRINKLE (found by use): the 8 declarative kinds = role·skill·context·projection·mark_type·generation_policy·relation_type·ai_tic. 7 are uniform Suite.create_<kind>(spec); **create_projection is INLINE in the server tool (server.py:874+), NOT a Suite method** — the tool's own docstring flags the SEAM (BAR2: "long-term home is Suite.create_projection mirroring create_skill/create_context"). create_node is on Suite but is GRAPH (not a declarative registry) → it belongs to the node(op=) cluster, NOT create.
THE CLEAN BEAT (sequenced): (1) LIFT create_projection's inline logic into Suite.create_projection (close the flagged seam, LAW-0 unify — mirror create_skill: render→registry gate→write→_commit_or_rollback→rediscover). (2) Build create(kind, spec, model="") deriving kinds from Suite.create_<kind> MINUS 'node' (registry-is-truth; a new create_<x> = a new kind), model passed only to kinds whose signature takes it (role). (3) Remove the 8 flat create_* tools from server.py (scattered: 614/632/641/874/976/987/999/1009). VERIFY: import + registry (create present, 8 gone, 59→52), floor/drift green. NOTE: create is declarative-DIRECT (commits) → MCP-test by creating a throwaway kind (e.g. a test context) then git-revert, or the fresh-agent eval (live on next /mcp reconnect).

## ★ 2026-06-10 — MCP-COMPOSE-EVAL findings (fresh-agent friction = the prioritised backlog; eval-driven, the FLOOR)
The full make→check→use→run→save loop run MCP-only by a cold agent. SMOOTH (preserve): create_role + enum field (first try) · run_items chaining · run_cascade output→input threading. FRICTION → backlog (gap = fix; ★ = OPPORTUNITY/addition, Tim's "not just gaps"):
- ✅ **M1 (DONE) · REDUCE/JOIN is half-stocked — no generic group-by/histogram.** MAP is fully generic (run_items any role); JOIN has only concat/count/first/verdict-tally (count=total-N, verdict-tally=jury-vote on a hardcoded `verdict` field). "count per label" (a dead-common op) has NO deterministic primitive → forced authoring a bespoke reduce-role. ADD a deterministic `tally-by:<field>`/`group-count` to cognition.REDUCE_RULES (the single-source, G11). The map→reduce story half-stocked is the single biggest composability gap.
- **M2 · No per-role inspector.** object_info is the node-type LIBRARY (mis-picked); checking one role = the 54KB cognition_info firehose or scanning cognition_inputs.roles. FIX: a scoped cognition_info(role=)/section= OR rename object_info→node_type_library. (discovery)
- **M3 · cognition_info dumps 54.7KB** (had to spill to file) + capabilities re-embeds the same roles block (built-twice). FIX: scoped/filtered cognition_info(section=|role=); dedup the roles block. (response-ergonomics + the detail pattern + unification)
- **M4 · create_role doesn't auto-reflect the drift-home** (roles/AGENTS.md) → roles_acceptance reds until manual. FIX: the consolidated create(kind,spec) auto-reflects the drift-home (registry-is-truth: the reflection derives). (coverage/correctness)
- **M5 · run_cascade `inputs` typed STRING** but a MAP-headed cascade wants a JSON array — inferred (worked). FIX: union type or say so in the description. (composability clarity)
- **M6 · run_reduce echoes each input address TWICE** [['a','a'],...] — noise. FIX: flat list or label the pair. (response-ergonomics)
- **M7 · verdict-tally silent null** on a field-absent input → verdicts:[null] tally:'flag' (violates no-silent-failure). FIX: fail loud naming the missing field. (the floor / fail-loud law)
- **M8 · save_cascade description** should state "authoring is a precondition, not a step (ops are generate|embed|reduce)"; **field_types** should show an example field shape (values as sibling key). (self-teaching descriptions)
These map to the upgrade dimensions: M1=composability/coverage(new primitive) · M2/M3=discovery+ergonomics · M4=correctness · M5/M8=self-teaching · M6=ergonomics · M7=fail-loud. NOTE: B/C/D eval tasks' detail to be re-extracted (the notification truncated them). NEXT BEATS: drive these + the consolidation sweep (create_* 8→1 with the projection-lift + M4 auto-reflect) together — each via mcp_face/tools/, registry-is-truth, floor-as-boundary, all three consumers.

## 2026-06-10 — MCP-SWEEP progress (4/5 consolidated; create next)
DONE: M1 (tally-by group-by, 9efeb8d) · corpus (op=list|find|read|query) · marks · node · rule · runs → 5 file-discovered mcp_face/tools/ modules; 61→51 tools, all flats removed, floor/mcp_engine/drift green. The parallel sweep workflow (wb099grxn) died on the quota pause after 4 lanes; adopted them (incomplete-work-in-scope, all built-verified-in-isolation, right calls: CQRS on marks, floor-preserved on node, no-god-tool on runs).
DONE: the create lane (8→1) ✅ + projection-lift — never ran (quota). It's the projection-LIFT (Suite.create_projection via _write_registry_file + a _CORPUS_REGISTRIES 'projection' row — close the inline-in-server seam) THEN create(kind, spec, model="") deriving kinds registry-is-truth (minus node) + M4 (response teaches the drift-home). Only-lane that touches suite.py → do it directly/own beat (not a fat agent — keep bounded). Removes the 8 create_* flats → 51→44.
REMAINING eval backlog: M2 (per-role inspector / scoped cognition_info) · M3 (cognition_info 54KB → section= + dedup the capabilities roles-block) · M5 (run_cascade inputs union/doc) · M6 (run_reduce double-echo) · M7 (verdict-tally silent-null → fail loud) · M8 (self-teaching descriptions). Then coverage (compositions ②/③/⑥/⑦/⑩ + ingest_paths via MCP) + the fresh-agent re-eval.

## 2026-06-10 — MCP-SWEEP create lane DONE (8→1 + projection-lift) — 44 tools
create(kind,spec,model) consolidates the 8 flat create_* (committed). Suite.create_projection lifted (BAR2 seam closed). Tally so far: 61→44 (corpus 4→1, marks 3→1, node 4→1, runs 2→1, rule 3→1, create 8→1) + M1 (tally-by). FLOOR preserved everywhere; registry-is-truth (kinds/enums derive). mcp_engine 35/floor 42/drift 5/reduce 28. Lands on /mcp reconnect → then fresh-agent re-eval.
REMAINING: M2 (per-role inspector / scoped cognition_info) · M3 (cognition_info 54KB → section= + dedup capabilities roles-block) · M5 (run_cascade inputs union/doc) · M6 (run_reduce double-echo) · M7 (verdict-tally silent-null → fail loud) · M8 (self-teaching descriptions / field_types example). Then COVERAGE (compositions ②③⑥⑦⑩ + ingest_paths via MCP) + the fresh-agent re-eval (the intuitiveness bar).

## 2026-06-10 — M2+M3 DONE (the discovery face, post-restart first beat on Fable 5)
cognition_info(section=,role=,detail=) — no-args = a 2KB concise overview (was 41.7KB, 20×); per-role inspector; teaching errors; detail='detailed' back-compat. capabilities.cognition.roles → {ids, full_specs pointer} (the dup gone, 17.9K→14K). M-series now: M1✅ M2✅ M3✅ M4✅(reflect_in) M6✅ M7✅ · M5/M8 deferred (cosmetic). NEXT: COVERAGE (⑦/⑩ as saved cascades · ②/③ runnable flows · ingest_paths G21) → the fresh-agent RE-EVAL (measure the gain vs eval #1).

## 2026-06-10 — COVERAGE beat: ⑩ option-panel SAVED + RUN via the live MCP; G3 now HAS a design (from the engine itself)
- ✅ Reconnect verified by use: cognition_info() → the 2KB concise overview (live, with the teaching _hint); create(kind=) live, 8 flats gone.
- ✅ ⑩ option-panel = a saved cascade (save_cascade ok; steps: items(develop_option) → reduce(role=score_options)). RUN end-to-end via run_cascade on a REAL fork (the G3 mid-chain fan-out question, 5 lenses, JSON-array inputs) → a ranked option-space + recommendation + graft. Saved cascades now: verify-jury · eval-classify-flow · option-panel.
- ★ G3 DESIGN (the engine's own recommendation, rank-1 reuse-first + a framework-first graft): mid-chain fan-out = a small ADDITIVE hook on an items step in the EXISTING runner — the step reads the prev role-step's output, explodes a named list field (e.g. fan_field='groups') into N units, fans. No new decomposer, no 2nd engine. This unblocks ⑦ spec-compiler as a saved cascade (decompose → expand needs exactly this) + G12. Build it as a bounded engine beat (cognition.run_cascade + build_action validation of the fan_field key).
- REMAINING coverage: ⑦ as a cascade (after the fan_field hook) · ②/③ as documented runnable flows · ingest_paths (G21) as a first-class mcp_face/tools/ module · then the FRESH-AGENT RE-EVAL.

## 2026-06-10 — G3 ✅ BUILT (the panel's design) + ⑦ spec-compiler = a saved one-call cascade
fan_field mid-chain fan-out landed in the existing runner (additive, fail-loud+teaching edges, adversarially verified). save_cascade('spec-compiler') PROVEN by use end-to-end (seed → 4 groups → 4 two-faced criteria → grounded → the triad docs). Saved cascades now: verify-jury · option-panel · spec-compiler · eval-classify-flow. G12 unblocked by the same hook (option-panel could fan_field too if ever role-headed). LEARNINGS: (a) the synth step wants ~5000 max_tokens — run-level budget today; a per-step max_tokens on the step decl = a small additive upgrade (noted, not built); (b) the panel→build circuit WORKED (deliberate → design → build → verify, all in 2 beats).
REMAINING: ingest_paths (G21) as mcp_face/tools/ module · ②/③ as documented flows · the FRESH-AGENT RE-EVAL · M5/M8 · G4/G14/G5/G6 · substrate exploration.

## 2026-06-10 — G21 ✅ ingest first-class (① complete through the MCP) + a new finding
ingest(roots/paths, space, max_files, force) live as mcp_face/tools/ingest.py → Suite.ingest_paths (walk_files promoted to runtime/corpus.py). Incremental proven by use (28/29 skipped on re-run); failures NAMED in the response. 45 tools (the +1 is a NEW capability).
- ✅ **G24 · the 4B digest emits malformed JSON on some content (runtime/bridge.py, deterministic at temp 0).** The CLASS-fix: schema-GUIDED decoding at the transport (fabric passes the role's output_schema as a vLLM guided-json constraint → unparseable JSON becomes impossible for every role, not just digests). Check what fabric/transport.py json_schema support already does (L-transport built something) — wire or finish it. Until then: bridge.py is un-ingested (named loudly each run, never silent).
- ✅ **G25 · sha-refresh ingest** — skip-if-present is path-keyed; a CHANGED file needs force=True. The staleness pass (index_staleness is the read seam) = the follow-on for a self-refreshing corpus.
REMAINING: full-repo ingest via the new tool (a few bounded calls) · ②/③ flows · the FRESH-AGENT RE-EVAL · M5/M8 · G4/G14/G5/G6/G24/G25 · substrate exploration.

## 2026-06-10 — ① AT FULL SCALE (the full-repo ingest, via the G21 pipeline)
192 corpus records (was 130; +62 in 2 bounded batches, remaining=0): all engine/registry/tools code + build-prep/cognition-self-improvement + the Surface docs. Probes: mark-types Q → runtime/mark_types.py (0.56) ✓ · the Surface Q → the live-resolution-surface docs + FRAME (0.67) ✓ — code AND vision queryable in one space. HONEST: (a) the decision-wire Q ranked mid (0.53, wrong files) — digests cover only the file HEAD (6000 chars), so deep symbols (implement.py's dispatch) are under-represented → the symbol/chunk-grain ingest (G22-adjacent) is the retrieval-quality upgrade; (b) G24 STRENGTHENED: now 3 files deterministically fail the 4B digest with malformed JSON (runtime/bridge.py · roles/check.py · nodes/portal.py) — the guided-decoding transport fix would eliminate the class; all 3 named loudly every run.
NEXT: ② drift-radar over the FULL corpus (cluster 192 → judge_drift → marks) · ③ flow · the FRESH-AGENT RE-EVAL · M5/M8 · G24/G25/G4/G14/G5/G6 · substrate exploration.

## 2026-06-10 — ② DRIFT-RADAR over the FULL corpus (192 records) — run complete
15 candidate clusters (thr=0.90, mega-dropped) → judge_drift over all 15 → **1 confirmed built-twice, 14 correctly distinct** (same-registry siblings rejected with sane notes — the FP-guard works at scale).
- ★ THE FINDING (marked built_twice, surfaced for Tim — his content, operator decides): `build-prep/live-resolution-surface/live-intent-resolution-surface.md` ↔ `project-base-live-intent-resolution.md` substantially share the same definition/content (consistent with the deleted combined.md being the same concatenation — live-intent-resolution-surface.md likely CONTAINS project-base). Render-not-judge: no auto-fix; Tim picks the canonical.
- ⚠ HARNESS LESSON (bit me twice now — G18 noted it once): in-engine `C.run_role(...)` returns the VALIDATED DICT DIRECTLY; only the MCP wrapper nests it under 'output'. The first drift pass read `.get('output')` → None → a false "0 findings". Caught by look-at-output. Consider a one-line note in run_role's docstring (tiny, next touch).
② is now PROVEN at full scale end-to-end: cluster → judge → mark → marks(by='type') readable. NEXT: ③ flow · the FRESH-AGENT RE-EVAL · M5/M8 · G24/G25 · substrate exploration.

## ★ 2026-06-10 — THE RE-EVAL (5 cold agents, same tasks as eval #1 + the knowledge-loop) — the scorecard
**5/5 completed. THE M-FIXES ALL VERIFIED COLD:** tally-by "discoverable upfront, genuinely good" (M1 — eval#1's top gap, now a non-event: the per-label tally took ONE named rule vs building a custom role) · the 2KB overview was every agent's entry ("the structure is genuinely one-place", M3) · per-role inspector used naturally (M2) · propose-vs-create chosen correctly first try (M8) · no M6/M7 complaints · the run layer "chains beautifully, zero guessing" · the run://corpus address template praised as "regular, predictable".
**THE FRICTION CLASS SHIFTED — from missing-capabilities (eval#1) to seams-between-tools (now). The N-series backlog:**
- ✅ **N1 (CLOSED — marker was stale; verified in code 2026-06-10: corpus.py op='read' resolves a source_address to its records newest-first instead of the silent {record:null}) · the retrieve→read seam:** corpus(op='query') returns code:// source ids that corpus(op='read') can't dereference — and it returns {record:null} SILENTLY. inspect_address rejects code:// too. FIX: op='read' accepts a source_address (derive the record address via the regular run://corpus/<project>/<src>/<projection> pattern) + the null becomes a TEACHING error. One id space that round-trips = the composition currency.
- ✅ **N2 · authoring-shape discoverability:** cognition_info(role=) omits exactly the authorable fields (output_schema/prompt_template); create's gate errors teach ONE constraint per failure with no example shape; the {utterance} template vocabulary is undocumented. FIX: role-inspector includes the full spec; errors carry a valid-shape example; template vars documented in create's description.
- ✅ **N3 · save/run validation parity:** save_cascade ok:true on a decl run_cascade rejects (role missing on a rule-reduce step) — the "one door" isn't. FIX: save-time validation == run-time requirements (+ the role-on-rule-step is only an address-naming token — say so or make it optional).
- ✅ **N4 · the cascade INPUT contract (M5 properly, hit by A+D):** list_cascades shows what a cascade PRODUCES, never what it EATS; run_cascade inputs is string-typed (a MAP-first cascade needs a list — one agent baked the data into the decl, freezing it). FIX: input hint per cascade (first-step kind + expected shape) + inputs typed/documented array|string|address.
- ✅ **N5 (CLOSED — marker was stale; verified in code 2026-06-10: ingest_paths returns `addresses`, normalizes abs→relative under the repo so one file = one code:// id, and the explicit-path read error teaches the cwd convention) · ingest chainability:** returns counts but NO record addresses (nothing to chain); absolute vs relative paths mint TWO ids for one file (normalize); FileNotFound on a relative path should hint the cwd convention.
- ✅ **N6 · auto-reflect the drift-home (M4's full fix):** reflect_in is a dangling instruction for an MCP-only agent (can't edit files through this surface) — create should auto-append the drift-home entry (or expose the op).
- ✅ **N7 · registry-view consistency:** capabilities/overview said 24 roles while section='roles' said 25 (a stale-view split — silent); models_for_role's param is `requires` not a role id (rename or alias); 'mode' is polysemous ×3; list_types duplicates capabilities.node_types (fold-in candidate); detail='detailed'+role= is a silent no-op combo; spaces-vs-projections relationship unexplained.
NEXT: N1 (the silent-null round-trip — the fail-loud violation) → N3+N4 (the cascade contract) → N2 → N5/N6/N7 sweep → then M5/M8 die with N4. The substrate exploration + ③ flow remain queued.

## 2026-06-10 — per-step max_tokens + the run_role return-shape note (two carried items CLOSED) + RG10 full-scale rolling
- ✅ **Per-step `max_tokens` on cascade decls** (the spec-compiler truncation lesson, carried from the fan_field beat). Additive, both halves: build_action validates the optional field (positive int, teaching error — bool/str/negative refuse); run_cascade honours `step.max_tokens` on EVERY step kind (kw_common for role/items + the reduce rkw), absent → runner default. Mirrors the per-step `model` override exactly. save_cascade's face docstring documents it (MCP-first: discoverable where authors author). VERIFIED BY EXECUTION: spy on run_role saw step0→999 / step1→256 through the real runner; validation door 5/5; cascade_acceptance 26/26 live.
- ✅ **run_role RETURN-SHAPE docstring** (the bit-twice harness lesson): the engine returns the validated dict DIRECTLY ({"what_this_is":…}), the {"output":…} nesting is MCP-face-only — now stated at the TOP of run_role's docstring.
- ◑ **RG10 AT FULL SCALE — rolling.** rg10_run.py REWRITTEN as a thin incremental wrapper around the PROVEN registry_generation_run.run_registry_generation (reuse-don't-parallel — first draft re-implemented the chain worse: silently swallowed res.failed (940 units!), skipped REDUCE, juried blind without the element input). ROOT CAUSE of the all-fail first batch: register_element declares the templated `run://<turn>/screen_reader/{mockup}` input whose {mockup} chainer is UNBUILT — the proven module threads ground as a BARE-NAME shared-ctx input (an in-memory role variant). Fail-loud state now: a mockup with a raise OR 0-dossiers-from->0-candidates goes to state.failed (retried next batch, MAX 2 attempts, then left loud). Smoke A1: 28/28 dossiers · 21 clusters · 13 OK / 8 FLAG. C1 artifact ADOPTED (43→28→14/14). Full run over the remaining 21 mockups (~870 candidates) is EXECUTING — per-mockup artifacts rg10-batch-<stem>.json; ends at Tim's APPROVE gate (RG9 write-back only after).

## 2026-06-10 — TIM'S FEEDBACK on the RG10 run (the design law + a new design seed) — new backlog items
Tim reviewed the run (didn't yet approve s96): his instinct was "no context → fiction, a design failure not a model failure" — the chain WAS grounded (exemplars+inventory+ground+parent), which is why the sample quality landed; but his prediction held twice anyway: (1) the 4B invented feature-ids DESPITE the context (the floor caught them — floor-over-prompt vindicated again), (2) I MYSELF rebuilt the runner ungrounded at scale (the first-draft failure). His multi-part conclusion is now a STANDING LAW (memory: grounded-chain-law):
- ✅ (closed: the flows registry + the registry-generation saved cascade) **GC1 · the grounded chain must be the MCP-easiest path:** RG10's chain (and the pattern generally) reachable through the company MCP as ONE saved/runnable thing — a cold agent invokes the production line, cannot rebuild it ungrounded. Today registry_generation_run.py is a build-prep script (invisible to the MCP face). Shape TBD: a saved cascade once the engine seams close ({mockup} chaining, shared-ctx resolve-once, jury+refcheck step) OR a first-class flow tool. This CONVERGES with the open [C] engine seams — the manual module is the spec.
- ✅ (closed: REGISTRY-FILLING-PATTERN.md + the saved cascade; 2nd instance ran — the gap intake) **GC2 · the registry-filling pattern, reusable:** generate → ground → dedupe → deterministic-floor → propose → operator-gate as a NAMED reusable pattern (Tim: "a lot of registries that don't have all the data entries... very high value, immediately useful"). Document + make instantiable (the chain parameterised over: the unit source, the grounding package, the closed vocabularies, the floor checks, the surface card).
- 🌱 **GC3 · address accumulation (design seed, Tim's idea — needs the design conversation):** addresses as accumulation points — RHM conversations write NEW FIELDS onto the SAME address through the SAME propose→approve gate; consumers see per-need field projections. The registry becomes the junction of Tim's intent + system self-knowledge at fixed coordinates. (Memory: address-accumulation-idea.)
- s96-review stays at Tim's gate (he's calibrating quality first — offered a sample-judging walkthrough).

## 2026-06-10 (later) — G23 DEEPENING + the full-population accounting + a G24 edge
- ✅ **G23 driver reworked to per-EXCHANGE idempotency** (the corpus's own exchange://<sid>/<i> keys are the skip-list — substrate-is-truth at finer grain). Capped sessions now DEEPEN across batches; band parameterised (the 6 >20MB foundational sessions mined — the richest tranche). Space='history': 173 → **1102 extracts**. Full population accounted: 465 transcripts = 19 dialogue sessions (mined/mining) + 64 harness transcripts (thin, recorded) + 379 tiny verified-zero-dialogue + the live ones ripening. Foundational-tranche quality spot-checked (real corrections: unify-dual-registries, no-files-just-talk).
- 🔴 **G24-EDGE (one unit, deterministic):** ONE mine_exchange unit (exchange 68e14d07/…) emits a trailing-comma JSON IDENTICALLY on retry (same char), through schema-guided decoding + 4 client retries. Guided decoding should make this grammatically impossible → either the json_schema opt isn't reaching vLLM on some run_items path, or a vLLM structured-output edge on this content. ONE exchange affected of ~1100 (left loud in .build/g23/state.json, attempts=2). Bounded investigation when convenient: fire that exact unit once with transport logging on.
- G13 (the failure-pattern cluster over the FULL mined set) is now RIPE once the last ~5 sessions converge — the payoff report Tim asked to hear about.

## 2026-06-10 (Tim's step-4/5 questions) — GC5 BUILT (cross-mockup reconcile) + GC4 registered (floor re-ask) + the review-walk design need
Tim's "does the number look right?" CAUGHT A REAL HOLE: dedup ran only WITHIN each mockup — 551 "unique" proposals were 463 distinct addresses (57 proposed by up to 6 mockups; ui://toolbar/wire ×6). His structured-output suggestion = already the built shape (guided decoding + field-wise floor) — confirmed to him honestly.
- ✅ **GC5 · cross-mockup reconcile (rg10_reconcile.py, deterministic, no model):** group-by-address over the 23 artifacts → agreeing duplicates MERGE (confirmed-preferred representative, sources recorded); maps_to_feature/grounding DISAGREEMENT → class 'conflict' (all variants kept — the disagreement IS the operator signal). TRUE COUNTS: **189 confirmed · 216 jury · 33 conflict · 25 floor = 463.** Re-surfaced as **s97-review** (supersedes s94/s96 — retired via the STATUS lane, resolved untouched). Dup-guard adversarially verified. Artifact: .build/rg10/reconciled.json.
- ✅ (closed: rg10_reask.py ran — 7 fixed, 18 = inventory gaps) **GC4 · the floor-triggered RE-ASK (registered, next bounded build):** a floor-fail's reason is perfect feedback — ONE bounded re-fire of register_element with the reason + the closed lists, then re-floor. Input = the 25 class='floor' entries (unit text reconstructable: artifact cluster_members → candidates.json order per mockup). Converts fixable fabrications; stubborn ones stay flagged. Update reconciled.json IN PLACE (no-versioning); reconcile the s97 payload-counts staleness then.
- ✅ (closed: the walk was composed s107–s110, then executed CONVERSATIONALLY with Tim — all 5 decisions done) **GC6 · the CLASS-GROUPED REVIEW WALK (the layer between the batch and Tim):** the walkthrough organ + RegistryProposals.tsx exist; the missing wiring = s97 → a walk session grouped BY CONDITION (confirmed→one block-approve · conflict→per-item decision with variants side-by-side · jury→paged eyes-pass). Reusable for EVERY future approval batch (the GC2 pattern's SURFACE stage made real). Needs the [M]-lane coordination (canvas surface is theirs; the walk-session backend verbs exist).

## 2026-06-10 (Tim's five-point reply) — GC4 BUILT+RUN · the walk/altitude investigation · new logged-unbuilt items
Tim's directives encoded: (2) re-ask greenlit→BUILT; (3) cross-mockup reconcile → deepen into HOW reusable-chain SETUP works through the MCP (folds into GC1 — the design task is now "chain setup as an MCP-intuitive capability, this chain the first instance"); (4) jury = a PANEL (his word; "paddle" = transcription) — extensibility confirmed; (5) the 82 entries were AI-WRITTEN under his direction (provenance corrected in the pattern doc); LOG ALL recognized-unbuilt (this section's standing practice, now an explicit directive).
- ✅ **GC4 · floor re-ask BUILT + RUN (rg10_reask.py):** 25 re-fired with the floor's reasons as correction + same gates (floor+jury, no shortcut) → 1 confirmed · 6 floor-clean-but-jury · **18 STUBBORN — the model repeats the SAME invented id after correction (CAN-brand/NODE-model/RHM-identity) ⇒ the element represents a real concept the FEATURE INVENTORY doesn't name. The stubborn-floor class is an INVENTORY-GAP DETECTOR** (reviewing them may grow register.json features, not just addresses). reconciled.json updated IN PLACE (prior_dossier kept). Final card **s98-review** (190 confirmed · 222 jury · 33 conflict · 18 inventory-gap); s97 retired via status lane.
- ✅ (closed: verdict_panels/ registry + run_panel + the panel tool) **GC7 · jury-as-PANEL extensibility:** today = 3 same-role draws (self-consistency). Tim expects add-a-juror ease: distinct LENS roles (grounding/voice/altitude-fit) as panel seats — run_jury already takes the role; the seat-list should be a declared registry row, not code. Log: design when the panel next matters.
- 🔴 **GC8 · dynamic walk re-composition:** sessions are graphs with operator-paced go-gates (built, good); but a session's ITEMS are fixed at start_session — no mid-walk insertion/branch-on-response (e.g. "show me the other variants of this one" spawning steps). The B5 gate/branch machinery exists on the graph side; the walk-side composition verb is unbuilt.
- ✅ (closed: gc9_gap_intake.py — 17 features proposed, D3 executed) **GC9 · inventory-gap intake:** the 18 stubborn entries imply missing register.json features — there is no flow that turns a stubborn-floor entry into a PROPOSED FEATURE for Tim's approve (a second registry-filling instance: the feature inventory itself). Natural GC2-pattern reuse.

## 2026-06-10 (GC6 backend) — the CLASS-GROUPED TRIAGE WALK is COMPOSED + LIVE
- ◑ **GC6 backend BUILT + verified by use (rg10_walk.py):** the s98 batch → ONE walk session (**1781052408-4**, 4 stops, cursor at stop 1, gates closed — Tim's pacing is structural) on the EXISTING organ (surface_review + start_session + present_current/coa; zero new machinery). Stops = decision-kinds: ① approve-the-190-block (his approve verdict here is what RG9 binds to) ② the 33 conflicts (all variants in-payload; compose_conflict_walk() built-not-fired — fires on his ask, one decision per conflict) ③ the 18 inventory-gaps (each shows the feature name the model insists on) ④ the 222 jury pile (the block PROPOSES the GC7 diverse-panel re-jury before any one-by-one effort). Stop-1 presentation verified live: coa framed it at altitude with options + recommendation (real 4B output, decent quality). LESSON: start_session RETURNS present_current's dict (key 'session', not 'id') — the first run misread it; the partial state recovered cleanly (blocks s99–s102 + the session all landed).
- needs-tim: WALK IT (FE/voice drive — the canvas walkthrough surface or the phone PWA); his verdicts at the stops are the gate. The write-back (RG9) runs only bound to his approve seq on s99.

## 2026-06-10 (G13 COMPLETE) — the failure-pattern cluster over the FULL mined history
- ✅ **G13 RUN (g13_cluster.py + G13-PATTERN-REPORT.md):** 1267 extracts (100% tagged) · 884 distinct tags → clustered @0.78 (conservative) · signal density 189 corrections/30 frustrations/26 needs-tim. **HEADLINE (4th independent confirmation): verify-by-use (143) + verify-before-claiming (50) dominate — the standing memories are VALIDATED, no new failure class.** Two BORDERLINE-NEW drafted in the report for Tim's review (NOT minted): describe-vs-request discrimination (weight 13) · build-and-show-don't-spec (recurring correction). The history space is the queryable substrate; the report updates in place on future minings.
- The G23 mining lane is now CONVERGED for settled sessions (live sessions keep ripening — future batches cheap+incremental). ③ is structurally COMPLETE: extract→mine→embed→cluster→report→propose-rules circuit closed.

## 2026-06-10 (GC1 BUILT) — the FLOW registry: grounded chains are now ONE MCP call
- ✅ **GC1 · flows registry + MCP tool (Tim's directive "set that up, viable+intuitive through the MCP"):** runtime/flows.py (file-discovered, id==stem, proposes_only:True REQUIRED at discovery, teaching errors on unknown flow/param) + flows/{registry_generation,transcript_mine,pattern_cluster}.py (the proven chains as rows; thin wrappers over the canonical build-prep scripts) + mcp_face/tools/flows.py (ONE parameterised tool, op=list|describe|run — auto-discovered by the face) + flows/AGENTS.md (drift home) + floor source-invariant enrollment (loader AND every row scanned; 43 governance checks green). VERIFIED BY USE: discovery 3/3 · both teaching errors fire · list/describe/run through the actual tool callable · pattern_cluster ran LIVE through it (1267/884/717 — identical to the direct run). The one-door split documented: DATA chains = save_cascade · CODE chains = flows (repo-authored, MCP-invoked).
- ✅ (closed: B2 — the gated propose_flow circuit, full adversarial verify) **GC10 · propose_flow (logged, open):** the gated authoring path for code-chains through the face (mirrors propose_role) — an agent PROPOSES a flows/<id>.py for review; lands only via the operator/commit gate. Until then: new flows are repo commits.
- needs-tim: **/mcp reconnect** to land the `flows` tool live in this session's face (the suite-side callable is verified; the face registers tool modules at connect).

## 2026-06-10 (① COMPLETE) — the repo-exocortex covers the WHOLE repo
- ✅ **① DONE (was silently stalled at 195/583):** root cause = walk_files counted `.voice-venv`/`.litellm-venv` (13k phantom files; only `.venv` was name-skipped) + `./`-prefix path duality. Hardened (hidden dirs skip as a CLASS + normpath one-file-one-id), then 8 ingest batches → **all 584 repo files digested+embedded; corpus 1854 records; convergence proven by the final no-op batch (0 digested, 0 remaining, 584 skipped-existing)**. VERIFIED BY USE: query_corpus("where is the deterministic no-fiction floor checked?") → code://roles/confirm_registration.py @0.65 over 587 indexed — the codebase is ASKABLE. repo_ingest = the 4th flow row (hash-aware; re-runs keep it current).
- ② DRIFT-RADAR at full scale is now UNBLOCKED (the complete repo space is its floor) — next lane: find_relations high-similarity cross-file pairs → judge_drift → built_twice/overlap marks → report.

## 2026-06-10 (② COMPLETE) — the drift-radar's first full-repo sweep
- ✅ **② RUN at full scale (drift_radar.py + DRIFT-RADAR-REPORT.md + the 5th flow row):** 586 files → 1626 near-pairs ≥0.82 → 21 clusters judged → **8 confirmed marks (surface-direction) · 13 distinct (the FP-guard rejecting near-but-legit siblings)** · 1 doc-drift candidate (ai_tics/AGENTS.md @0.436 vs its module — the stalest constitution). Real targets: the 3 build-methodology docs (one protocol ×3) · MERGE-COORDINATION vs WORK-SPLIT · the live-resolution-surface draft-pair (cos .94, Tim's renamed folder) · 2 consent-mechanism test suites. Lead-flagged borderline-FP: tests/speakable↔voice/speakable (a test mirroring its module is the test working).
- ⚠ **THE FIRST SWEEP REPORTED CLEAN ZEROS over an EMPTY set** (the repo-space index keys are vec://-wrapped; the bare-code:// filter matched none). Caught by zero-is-suspicious; the flow row now FAILS LOUD on zero-everything (a silent 'all clean' is not a believable radar).
- ✅ (closed: deterministic intent-relations filtered before the judge) **GC11 · radar intent-context (logged):** feed judge_drift the pair's RELATION (test-of? anchor-of? same-dir?) as declared context — ANCHOR docs + test↔module mirrors currently read as overlap (the GC2 context-package law applied to the radar itself).

## 2026-06-10 — RE-EVAL #3 (6 cold agents incl. the flows test) — the P-series backlog
**6/6 completed (no blockers). The friction class shifted AGAIN: missing-capabilities (eval#1) → tool-seams (eval#2) → now DESCRIPTION-DRIFT + one real REGRESSION.** The run:// composition spine got explicit praise across tasks ("map→reduce chained first try, zero address guessing").
- ✅ **P1 (CRITICAL, FIXED THIS BEAT) · create(kind='role') NameError regression:** my N6 auto-reflect edit read the CALLER'S `spec` local inside _write_role_file → EVERY role-create through the face died on a leaked NameError + STRANDED the written file (3 litter roles). FIX: desc= threaded through both callers; the reflect+commit wrapped (a failure now removes the written file — fail loud + clean, never half-created). VERIFIED BY USE: create→live→fired ({'label':'question'})→AGENTS reflected→cleaned. **THE LESSON (mine): I spot-fired targeted suites after the N6 edit instead of the FULL `company suites` gate — direct_create_acceptance existed and would have caught it. The gate, not the subset.**
- ✅ (closed: P-sweep 3 — the one-place tool error guard) **P2 · error hygiene:** tool handlers can leak raw internal exceptions (the NameError surfaced verbatim); wrap as teaching errors naming the stage + field.
- ✅ (closed: P-sweep 1 — all 8 refs normalized) **P3 · `create_role` naming drift:** descriptions across the server reference a tool name that doesn't exist on the face (create_role vs create(kind='role')) — normalize every cross-reference.
- ✅ (closed: P-sweep 1 — section= + the chains section) **P4 · capabilities scoping + flows visibility (Task F's top gap):** capabilities is a ~15KB unscoped firehose AND doesn't advertise cascades/flows at all — the orientation face misses the production lines. ADD section= (mirroring cognition_info) + a chains/flows section.
- ✅ (closed: P-sweep 2 — detail='detailed' inlines content) **P5 · corpus query → answers not pointers:** op='query' should carry per-hit digests inline (or honor detail=) + state that hit ids are op='read'-able (1 call, not 1+k).
- ✅ (closed: P-sweep 2 — regenerated to the live face) **P6 · skill://corpus_pipeline names 5 dead tools** — the canonical recipe drifted from the live face; regenerate from the registry (descriptions are registry-is-truth too).
- ✅ (closed: P-sweep 3) **P7 · cascade input_schema:** machine-readable step-0 input contract (symmetric with output_schema), not just the prose inputs_hint.
- ✅ (closed: P-sweep 3 + Q11) **P8 · address-identity coherence at the ingest/corpus boundary:** one file = one canonical id regardless of path spelling; query returns the exact address read accepts (the radar hit the same class: vec:// wraps + one absolute-path stray).
- ✅ (closed: P-sweep 1) **P9 · type-name round-trips:** field_types description stale vs live registry; cognition_info(role=) emits Python reprs not registry type names (inspect→copy→create must round-trip).

## 2026-06-10 (P-sweep 1) — P3+P4+P9 CLOSED on the face
- ✅ **P4:** capabilities(section=) — no-args = a CONCISE 14-section map (size hints); section='chains' ADVERTISES the production lines (5 flows + 6 cascades + the run-via pointers; Task F's "the orientation face doesn't advertise the chains" closed); teaching error lists live sections. ✅ **P3:** all 8 `create_role` dead-name cross-refs → create(kind='role'). ✅ **P9:** cognition_info(role=) output_fields now emit REGISTRY type names (enum+values / str / list[...] / nested-model name) — inspect→copy→create round-trips. All verified by direct callable use; mcp_engine 35/35, floor 43/43. needs-tim: /mcp reconnect (lands flows + the scoped capabilities on the live session face).
- Remaining P-series: P2 (handler error-wrap) · P5 (corpus query inline digests) · P6 (skill://corpus_pipeline regeneration) · P7 (cascade input_schema) · P8 (address-identity coherence).

## 2026-06-10 (P-sweep 2) — P5+P6 CLOSED
- ✅ **P5:** corpus query detail='detailed' → each hit carries its record content INLINE (verified: the no-fiction-floor question answered in ONE call) + the note teaches the read round-trip. ✅ **P6:** skill://corpus_pipeline regenerated to the live face — all 5 dead names (findings_for/read_corpus_record/list_corpus/find_corpus/create_projection → marks(by=)/corpus(op=)/create(kind=)) + the inverted-stale "mark is NOT a tool" line (it is). Remaining: P2 (handler error-wrap) · P7 (cascade input_schema) · P8 (address-identity coherence).

## 2026-06-10 (P-sweep 3) — P2+P7+P8 CLOSED ⇒ THE P-SERIES IS COMPLETE (all 9, same day as the eval)
- ✅ **P2:** one-place error guard wrapping EVERY registered tool (registry-derived, post-registration): ValueError/KeyError teaching errors pass UNTOUCHED; any other exception re-raises labelled "INTERNAL ERROR in tool X … a BUG in the tool, not your call" (the P1 NameError class can never again pose as a contract error). ✅ **P7:** the cascade input contract is now machine-readable AND save-time-visible — _cascade_input_contract (one source) feeds both save_cascade's return and list_cascades rows (input_schema beside output_schema). ✅ **P8:** corpus(op='read') normalizes code:// path spellings (absolute-under-repo + ./) to the canonical id — verified both round-trip to the same record. Probe cascade removed; cascade 26/26 · mcp_engine 35/35 · floor 43/43.
- **The eval#3 backlog closed in 3 beats.** The face now: scoped orientation advertising the chains · live names everywhere · round-trippable types · one-call answers · honest internal errors · symmetric input/output contracts · spelling-proof ids. NEXT measurement: a 4th cold eval when the next capability lands (don't eval for eval's sake).

## 2026-06-10 (GC9 BUILT) — the gap intake: the pattern's SECOND instance, fully deterministic
- ✅ **GC9:** gc9_gap_intake.py — 18 stubborn-floor entries → **17 proposed feature-inventory rows (0 flagged)**, written onto reconciled.json's gap entries (`proposed_feature`) so Tim's stop-3 decision is concrete ("approve which features?" not "review these failures"). FULLY DETERMINISTIC — the strongest no-fiction form: id = the model's stable insisted name · area = derived prefix→area from the REAL inventory (ambiguity flags, never guesses) · label = the dossier's at-altitude prose (the id-echo in `represents` caught by look-at-output — howto.what is the prose) · same-id proposals MERGE (one feature, many elements). THE FINDING: **8/17 are RHM features** (presence orb · listening · persona · thinking · status · identity · process · voice) — the mockups envision a far richer RHM than register.json describes; approving stop 3 substantially completes the RHM area's self-description.
- [M] note: reconciled.json gap entries now carry proposed_feature rows — the stop-3 surface should render them as the decision objects.

## 2026-06-10 (GC7 BUILT) — the verdict-panel: diverse lenses, ready for Tim's stop-4 yes
- ✅ **GC7:** verdict_panels/ registry (runtime/verdict_panels.py loader; seats=lens roles + int quorum; floor-scanned) + cognition.run_panel (1 deliberate fire × N DIFFERENT roles — the axes-swap of run_jury; failed seat = recorded dissent, never a silent pass) + the founding row registration_confirm (confirm_registration · voice_lens · element_fit_lens, 2-of-3) + the two lens roles authored through the FIXED create door (P1 dogfooded — worked first try, auto-reflected). **VERIFY-BY-USE CAUGHT TWO SEAT-QUALITY ISSUES before the 222 re-jury could run on them:** (a) voice_lens judged by-design-technical fields (maps_to_feature/how_to_change) — scoped to howto.what/what_you_can_do; (b) my own test fed a PLACEHOLDER element — the lens rightly rejected it (the context law biting my verification). Post-fix on REAL elements: confirmed-control 2/3 PASS (element-fit's conservative dissent absorbed) · jury-flagged 1/3 FAIL with two named real defects. **The s102 re-jury is now ONE call away on Tim's yes.**
- ⚠ **LAW-0 LESSON (mine):** first draft landed in panels/ — the EXISTING UI-panels module (JSON field-defs). Caught at the AGENTS.md write; relocated to verdict_panels/ with the naming note in both constitutions. The drift_acceptance 'panels' block was a visible clue I'd read this session and didn't connect. Full-picture-first applies to NAMING too.
- ⚠ **edit_role footgun (logged):** edit_role(spec with only prompt_template) renders a REPLACEMENT that drops undeclared fields (the s105 proposal would have wiped voice_lens's output schema) — gated so no damage, but the edit-render should MERGE with the live spec, not replace. Filed as GC12.

## 2026-06-10 (GC12 + the panel face) — CLOSED
- ✅ **GC12:** edit_role merges-not-replaces (authoring.output_fields_rows: live schema class → authorable rows, nested objects + Literal enums included, FAIL-LOUD on unrepresentable). Verified: a description-only edit keeps the full schema+prompt; the destructive s105-class proposal is impossible now. (server.py's P9 inline projection is a near-twin — fold onto output_fields_rows next touch, noted.)
- ✅ **GC7 on the FACE:** mcp_face/tools/verdict_panels.py — panel(op=list|describe|run); run teaches the element-context requirement in its description (the placeholder-element lesson encoded as guidance). Verified end-to-end through the face callable: a real dossier judged 2/3 with its real element. needs-tim: /mcp reconnect (panel + flows + scoped capabilities all land together).

## 2026-06-10 — ★ TIM'S STANDING RULE: REFINE BEFORE GATING (+ the re-jury he approved is RUNNING)
Tim approved the s102 re-jury AND made it standing: **"that should be a standing approval — I was not happy with being given 222 things that I had to approve at this stage."** Encoded as memory feedback-refine-before-gating: RE-ANALYSIS/refinement passes (re-jury, re-ask, reconcile, dedupe, classify) carry STANDING APPROVAL — run autonomously BEFORE surfacing; his gate receives the MINIMUM decision set (block-decisions + named dissents + variant-conflicts), never a triage pile; if a refinement is buildable but unbuilt, BUILD IT FIRST. The 222 pile was a design failure of mine — the panel existed as a designed next step and the walk ASKED instead of RUNNING.
- rg10_rejury.py RUNNING over the 222 (real elements, registration_confirm 2/3). After convergence: recompose the triage walk with the refined counts (retire s98–s102 via the status lane; fresh minimal walk).
- The /mcp reconnect LANDED — flows + panel + scoped capabilities are live on this session's face.

## 2026-06-10 — ★ THE REFINEMENT CASCADE CONVERGED: 222 → 27 (the standing rule's first full exercise)
The pile Tim refused to triage went through FOUR autonomous refinement stages: diverse panel (202, named dissents) → calibrated panel (177; mockup-epistemics + domain-vocabulary lessons) → **DETERMINISTIC PROSE FLOOR** (77; prose_check.py — voice judgment moved OFF the model after 2 failed prompt calibrations; 100 false flags cleared in one data-recombination pass, no re-firing) → **RE-DESCRIBE** (27; the named dissents fed back, 50/77 corrected dossiers passed floor+prose+panel; 1 element-unrebuildable recorded). **FINAL GATE (walk s107–s110, stop-1 verified live): 385 block-approve · 33 conflicts · 17 proposed features · 27 true residue with named reasons.** Confirmed pile 190→385 (+103%) with EVERY promotion gated by floor+prose+panel — refinement, never relaxation.
- THE ARC LESSON (the third instance: refcheck → prose_check → ?): a 4B judgment that keeps misfiring on an INSTRUCTION-FOLLOWING constraint should become a deterministic floor check. The model raises; the floor decides.
- dup-guard lesson: retired-via-status cards still read resolved=None — guards must be status-aware.

## 2026-06-10 — Decisions 1+2 EXECUTED through the conversational walk (Tim live)
- ✅ **D1 (385 block):** Tim approved verbatim-recorded on s107 → registry 82→454 (13 curated-collisions held; stamp conflicts recorded, selector-hardening named). ✅ **D2 (33 conflicts):** Tim's MULTI-JOB insight ("elements that do multiple jobs depending on what else is happening") + delegation ("you will be a better judge"). Investigated (corpus + feature-reality + dossier) → 29 resolved+written (registry→483) · **2 MULTI-ROLE entries with the additive `roles` field** (ui://chat/walk = WALK-present+WALK-session; ui://toolbar/layers = CAN-layers+NODE-layer+SM-revert — the poster child) · 4 curated-overlaps joined the held pile (now 17) · RHM-utterance identified as ANOTHER inventory gap · 2 foreign elements mis-collapsed into chat/utterance (a reconcile-grain note).
- 🌱 **MULTI-ROLE = the GC3 seed made registry-real:** the `roles` field is the first contextual-faces data on an address — the accumulation idea's schema direction (one address, several true meanings by context). Design conversation still pending.
- Remaining decisions: D2.5 (the 17 curated-overlap proposals — keep curated vs adopt richer) · D3 (17+1 proposed features) · D4 (27 residue) · D5 (the two drafted rules).

## 2026-06-10 — D3 EXECUTED (Tim's split verdict) + the PROPOSAL LIFECYCLE articulated
- ✅ **D3:** feature inventory 58→76. 10 CONNECTED (real seams named, no fabricated refs) · 8 PROPOSED with status='proposed' + `proposed_at` ui-addresses (additive fields). Verbatim verdict on s109.
- 🌱 **GC13 · the ACCEPTANCE→BUILD trigger (Tim's articulated lifecycle):** "once they get accepted or submitted by me, that then runs the process of the backend build and the registries end making them Real." The Decision→Implementation Wire (branch decision-wire-and-surface, unmerged) IS this circuit's engine; the open seam = an accepted proposed-feature flowing into wire-dispatch → built → status flips. Design+integration item — connects GC3 (fields), the proposal rows (features), and the wire (execution).

## 2026-06-10 — GC11 BUILT (the radar's intent-relations) + a real catch
- ✅ **GC11:** known-intentional pair relations (test-of · summary-of · series-sibling) classified DETERMINISTICALLY and filtered before judge_drift (the refcheck/prose_check arc, third instance) — 5 filtered, the speakable test↔module FP gone from confirmed. **The upgraded radar's first real catch: a phantom `code://company/...` corpus record** (the eval Task-E agent ingested from a different cwd → a second id for one file — the P8 class at the INGEST entry). Radar now skips not-on-disk records; 🔴 **P8b (logged):** ingest_paths should normalize a leading '<repo-basename>/' spelling (and generally resolve relative paths against the repo root, refusing ambiguity loud).

## 2026-06-10 — D4 EXECUTED as the first CONDITIONAL MEMORY + ★ TIM'S SCOPE EXPANSION (common memory + time-resolution)
- ✅ **D4 (defer-with-condition, verbatim on s110):** the 27 static-unverifiable entries → class 'deferred' + a CONDITION-ADDRESSED memory in the corpus (deferred://rg10/static-unverifiable-27, return_when=FE-work-is-now). Tim: "normally I don't accept deferrals" — the condition is what legitimizes this one.
- ★ **TIM'S SCOPE (added to the program):** the Company = COMMON MEMORY for ALL his projects/agents ("all of my agents will connect and access it"). And the architecture move: context resolution extends from SPACE (addresses → context into the RHM at a position) to **TIME** ("time is just the reference for sequence") — memories addressed to a WHEN, INJECTING themselves into responses when the condition matches now ("like that teaching stuff") because no human ever re-reads.
- 🔴 **GC14 · CONDITIONAL-MEMORY INJECTION (the required mechanism, evidence attached):** the passive-recall check FAILED (the deferred record ranked below exchange extracts on an FE-work query) ⇒ injection is necessary, not nice-to-have. Shape to design: a condition registry (when-clauses as data) · a "what is now" signal (the present activity/surface) · the injection seam (teaching-note-style lines in tool responses/RHM context). Converges with GC3 (space-faces) — one resolution primitive, two axes. Design WITH Tim when FE work approaches; the registry/floor laws apply unchanged.

## 2026-06-10 — G3 DESIGN written (the last open G-series seam, design-first)
- ✅ (closed: the proof ran — 'G3 CLOSED' section) **G3:** build-prep/cognition-self-improvement/G3-CASCADE-MULTIVAR-DESIGN.md — the registry chain's manual seams as the spec: S1 keyed per-unit ctx ({field} templates over unit dicts) · S2 resolve-once shared block (resolver vocabulary = the open decision) · S3 jury/panel/check step kinds — **with a checks/ registry (the refcheck/prose_check arc made declarable; the floor as data)** · S4 the flows/cascades split SURVIVES (partition/resume stays code-side). Build order: checks-registry first. Awaits the build greenlight; the design is the deliverable this pass.

## 2026-06-10 — the (e) judgement-calls line CLOSED by archaeology (the last unchecked backlog line)
- ✅ **built-twice one-sourcing trio verified RESOLVED:** sampling-keys = `fabric/transport._SAMPLING_KEYS` + `_apply_sampling` ("the ONE sampling passthrough shared by both transports — agree by construction") · plan-apply = no duplicate in runtime (the propose→apply paths share the write halves, e.g. _write_role_file) · propose-panel = one definition, one caller. Nothing contended remained to skip.
- ★ **BOARD STATE: every line of the loop backlog now has a verified disposition** — done · designed-awaiting-greenlight (G3 data-chains · GC14 conditional-memory injection · GC3/GC13 lifecycle conversations · the consolidation marks) · or at Tim's gate (D5 only). The registry program that anchored this loop is COMPLETE: 82→483 addresses · 58→76 features · 27 conditionally deferred · the walk executed conversationally end-to-end.

## 2026-06-10 — ★ GC15 · THE OPERATOR-MEMORY LAYER (Tim's feature call: "growing memory of me IN the system — currently no real definition")
Tim, closing the walk: my harness memory of him is useful but trapped — only Claude-Code-me reads it. The SYSTEM must hold the growing memory of Tim as a DEFINED first-class feature all agents get. The design shape (tentative, for the conversation):
- **A declared preference/rule registry** (operator rows, file-discovered like everything): each row {rule · why · EVIDENCE links to the mined exchanges that taught it · scope/when (the GC14 condition shape) · status proposed|confirmed}. The mining PROPOSES rows from corrections; Tim's confirm makes them standing (the same propose→approve circuit as everything else). My harness memory files = the seed corpus to migrate.
- **Delivery on all three faces:** the MCP (a scoped operator/preferences read any cold agent hits before working with him) · RHM context resolution (space-scoped: the prefs relevant at this address/surface) · GC14 injection (time-scoped: "presenting to Tim is happening NOW → these rules ride in") — the presentation-preference per-address loop (set_presentation_pref) is the EXISTING seam of the same family; nodes/model_of_tim is the twin lineage hook.
- **What it unifies:** common-memory scope + GC14 conditions + the altitude-transformation layer + the twin. D5's refined rules are the first two confirmed rows.
- HIS parts to shape: what it feels like from his side (does he see/curate it?) · what's in-scope (working rules vs taste vs personal context) · per-agent visibility (does an external agent get the same Tim-model as the RHM?).

## 2026-06-10 — GC15's STRUCTURE IS LIVE (the operator-memory registry)
- ✅ **GC15 structural half:** runtime/operator_memory.py (file-discovered; EVIDENCE REQUIRED — a row without Tim's verbatim words refuses at discovery, verified) + operator_memory/ with **7 confirmed seed rows from today's record** (refine_before_gating · think_back_with_me · easy_decision_surface · no_unconditional_deferrals · multi_job_elements · proposal_lifecycle · never_reads_the_files — each carrying his exact words) + mcp_face/tools/operator.py (op=rules|describe|proposed; the description tells every cold agent: read this FIRST before anything Tim-facing). Floor enrolled; 43/43 + drift 5/5.
- OPEN (the design conversation, Tim's parts): what it feels like from his side · scope of content (rules vs taste vs personal) · per-agent visibility projections · the mining→proposed-row automation (③'s next consumer) · GC14 self-injection delivery. needs-tim: /mcp reconnect lands the operator tool.

## 2026-06-10 — GC15's GROW-CIRCUIT live; its first run found the harness→system gap
- ✅ **memory_proposals.py (the ③→GC15 consumer):** g13 clusters → embed-compare vs existing rows → UNCOVERED strong patterns become status='proposed' rows (conservative: weight≥8 · evidence-required · ≤3/run · covered-validation reported). FIRST RUN'S FINDING: the system registry lacked the THREE most-taught rules in the whole record (verify_by_use ×143 · verify_before_claiming ×50 · expand_dont_echo ×26) — they existed only in MY harness notes. Proposed with real historical evidence; **Tim's one-word confirms pending** (op='proposed' shows them on the face). The remaining harness-notes migration = the same circuit run against my memory files (manual seed pass, next).

## 2026-06-10 — B1a DONE (the checks registry + check step kind)
- ✅ **G3·S3a:** runtime/checks.py (file-discovered; CHECK dict + check() callable, fail-loud) + checks/{dossier_refcheck,prose_clean}.py (wrapping the PROVEN floors — reuse-don't-parallel) + run_cascade kind='check' (role-less · on_fail flag|drop · drops recorded · drop-all starves loud) + build_action parity (unknown check/bad on_fail refuse at save) + both Suite injections + floor enrollment. BY USE: a fabricated dossier caught by name through a saved cascade; suites green. **Declared chains now carry their own floors.** Next on B1: S3b (panel/jury step kinds — executors exist) → S1 per-unit ctx → S2 (after the resolver-vocabulary decision).

## 2026-06-10 — B1b DONE (jury/panel step kinds) — THE DECLARED GAUNTLET IS REAL
- ✅ **G3·S3b:** kind='jury' (role must BE a jury — refused at save otherwise) + kind='panel' (role-less, panel= names the verdict_panels row) in run_cascade; both 1→1 on the thread, verdicts persisted+addressed. FIX en route: step-0 literal units now get MINTED addresses (the empty-thread guard had fired on PASSING units with a misleading 'dropped' message). **BY USE: `registration-gauntlet` (check:refcheck:drop → check:prose:flag → panel:registration_confirm) — a clean dossier passes 2/3; a fabricated one DIES LOUDLY at gate 1. Promoted to the cookbook (7 saved cascades).** The whole confirm machinery is now DECLARED DATA — B1 remaining: S1 per-unit ctx · S2 shared block (resolver-vocabulary decision) · S4 the registry chain as a cascade.

## 2026-06-10 — B1c DONE (S1 per-unit ctx)
- ✅ **G3·S1:** run_items(unit_ctx={name: "run://<turn>/x/{field}"}) — per-unit template substitution + canonical resolution; the registry chain's {mockup}-keyed ground is now ENGINE-NATIVE (was the manual module's seam 2). Cascade items-steps declare it (`unit_ctx` validated at save). Deterministic verify: per-group grounds routed correctly; fail-loud on missing field / non-dict unit. **B1 remaining: S2 shared resolve-once block (the resolver-vocabulary decision) → S4 the registry chain as a saved cascade (the G3 proof).**

## 2026-06-10 — B1d DONE (S2 shared block) — G3's engine seams are ALL CLOSED
- ✅ **G3·S2:** cascade-level `shared:` block (resolve-once → every role/items fire's ctx). Closed entry vocabulary {address|text|corpus_query} — the resolver-vocabulary DECISION made (mine, technical): reuse-only, no new scheme; file-projections etc. are a FLOW's staging job (prepare run:// addresses, then declare). Verified deterministically (3 fires, 2 cascades, all saw the block; bad shapes refuse at save AND run).
- **G3 ENGINE LEDGER: S1 ✓ S2 ✓ S3a ✓ S3b ✓ S4(split) ✓ — remaining: the PROOF (registry_generation re-expressed as a saved cascade + thin flow wrapper).**

## 2026-06-10 — ★ G3 CLOSED: the chain that needed code is now a REGISTRY ROW
- ✅ **The proof ran live:** saved cascade `registry-generation` (shared exemplars/inventory · items register_element with unit_ctx={"ground": "run://rg-ground/{mockup}"} · check dossier_refcheck · check prose_clean · cluster reduce) over 6 REAL candidates — gate 1 caught a fabrication THROUGH THE DECLARED CHECK (5/6), gate 2 clean, 6→4 clusters. register_element's templated forward-contract input UNIFIED into the real bare-name mechanism (its docstring's own promise). Cluster-reduce role-optional at both doors (the first fix conflated role-optional with rule-validation — separated). The spec file's "not end-to-end runnable" caveat replaced with the proof record.
- Remaining flow-side (noted, additive): the per-cluster panel CONFIRM loop (a future 'panel-fan' step kind); the flow stays the batching/staging wrapper (the S4 split, by design).
- **B1 IS DONE. Board: B2 (propose_flow) · B3 rolling · B4 consolidation drafts · B5 memory migration · B6 smalls · B7 eval#4.**

## 2026-06-10 — B2/GC10 DONE: agents can PROPOSE production lines; only Tim lands them
- ✅ **propose_flow:** the gated code-chain circuit — authoring.render_flow_source (proposes_only forced True) + gate_flow_source (forbidden-verb scan: resolve/approve/dispatch/claude -p/subprocess/os.system + tempdir-import through the REAL loader) at PROPOSE and again at APPLY · flow_build=CONFIRM (POLICY) · apply_flow runs only on the substrate-read approve (no caller flag) · N6 auto-reflect on landing. The face: flows(op='propose', spec={id,label,description,params,body}). FULL CIRCUIT VERIFIED: evil-body gate refusal · surface-not-land · GovernanceError sans approve · approved→landed→discovered→ran. **Tim's propose→accept→build→Real lifecycle now covers the system's own toolset.**
- Board: B1 ✓ B2 ✓ — remaining: B3 rolling · B4 consolidation drafts · B5 memory migration · B6 smalls · B7 eval#4 (now warranted: flows+propose, panels, checks, operator memory, declared chains all landed since eval#3).

## 2026-06-10 — EVAL #4 (7 cold agents incl. Task G governed-growth) — the Q-series backlog
**7/7 completed; TWO outcomes 'smooth' (A: the full compose loop · D: run-existing) — the FIRST smooth verdicts in four rounds.** Trajectory: missing-capabilities (M) → tool-seams (N) → description-drift + 1 regression (P) → now SCHEMA-LAYER polish + SERVER-STALENESS. The compose spine drew cold praise again ("addresses out of one step are literally the input type of the next"; "save_cascade answering the next call's open question is the single best affordance in the server — worth generalizing").
**★ STALENESS CLUSTER (explains 3 findings — NOT face design):** the live MCP server predates GC15/B2/the gauntlet — Task G couldn't find the operator tool (found the memory via space='history' and STILL extracted the right two rules: verify-by-use + concrete-over-abstract — the content works even when the door is hidden); flows(op='propose') absent; run_cascade('registration-gauntlet') unknown + capabilities-chains vs list_cascades disagreed (the ActionRegistry loads cascades.json at INIT and never re-reads — out-of-process saves invisible). → **Q3 (real bug): cascade_registry re-reads on file mtime change (match FlowRegistry's rediscover-per-call).** needs-tim: /mcp reconnect (operator · panel · flows-propose · fresh cascades all land).
- ✅ (closed same-day) **Q1 (A's top gap):** legal values into JSON-SCHEMA ENUMS (create.kind · run_role.op · run_reduce.mode · runs.* · corpus.op · flows.op · panel.op · marks.by) — the schema layer is where a cold agent looks first.
- ✅ (closed: Q-sweep 1) **Q2:** list_surfaced = a 278KB no-param firehose — apply the P4 scoping (concise rows default + id/status/limit params).
- ✅ (closed: Q-sweep 1) **Q3:** the cascade-registry staleness (above).
- ✅ (closed: Q-sweep 1) **Q4:** run_cascade.inputs param description carries the JSON-array convention + input_schema in EVERY list_cascades row (half done).
- ✅ (closed: Q-sweep 2) **Q5:** corpus op='read' returns a capture DIGEST framed as 'full content' — say what it is + how to reach the source.
- ✅ (closed: Q-sweep 1) **Q6:** empty-registry responses cross-point (empty flows list ↔ cascades; empty corpus space → ingest hint).
- ✅ (closed: Q-sweep 2) **Q7:** the FEATURE INVENTORY has no MCP surface (register_element's no-fiction contract points at it!) — capabilities(section='features').
- ✅ (closed: Q-sweep 2) **Q8:** a jury role declared kind='items' silently runs single-draw — warn at save (jury semantics dropped).
- ✅ (closed: Q-sweep 1) **Q9:** operator-memory discoverability — a pointer line in capabilities/cognition_info overviews.
- ✅ (closed with Q3) **Q10:** chains-section vs list_cascades one-source (likely collapses with Q3).
- ✅ (closed: Q-sweep 2) **Q11:** ingest path-identity AGAIN (3 duplicate records, literal-path keying) — P8b's ingest-side normalization is now eval-evidenced twice.
- 🟡 **Q12:** strip build-lineage jargon (GROUP N/AK4/#58…) from agent-facing descriptions — repeatedly named noise, never blocking.

## 2026-06-10 — Q-sweep 1: Q2+Q3+Q4+Q6+Q9 CLOSED
- ✅ **Q3 (the real bug):** ActionRegistry._reload_if_stale — every read stats cascades.json, re-parses on mtime change (verified: an out-of-process write became visible without restart). Q10 collapses with it (one source, now fresh). ✅ **Q2:** list_surfaced scoped — concise rows default (278KB→4.5KB), sid= drill-down, status=/unresolved_only= slices, teaching error. ✅ **Q4:** the inputs-shape rule ON run_cascade's param doc. ✅ **Q6:** an empty flows list points at the cascades registry (populated path unchanged). ✅ **Q9:** the capabilities overview carries the operator-memory pointer ("READ IT before preparing anything he will see").
- Tim's /mcp reconnect LANDED mid-beat (the operator tool live on this session's face). Remaining Q: Q1 (schema enums — the big one) · Q5 (digest honesty) · Q7 (features section) · Q8 (jury-as-items warn) · Q11 (ingest path identity) · Q12 (jargon strip).

## 2026-06-10 — Q1 CLOSED (the eval's top gap): the schema layer is self-describing
- ✅ **Q1:** all 11 discriminator params now emit JSON-schema ENUMS (Literal annotations through FastMCP) — corpus/flows/marks/panel/operator/node/rule/runs ops · run_reduce.mode · list_surfaced.status/detail · **create.kind DERIVED from the live Suite's create_* methods at registration** (8 kinds; registry-is-truth — a new declarative kind grows the enum on server start, never a hand-list). A cold agent trusting the machine-readable schema now sees the legal sets where it looks first. Verified: every schema inspected, enum present; 3 suites green.
- Remaining Q: Q5 (digest honesty) · Q7 (features section) · Q8 (jury-as-items warn) · Q11 (ingest path identity) · Q12 (jargon strip). needs-tim: the NEXT /mcp reconnect picks up the enums + the scoped list_surfaced.

## 2026-06-10 — Q-sweep 2: Q5+Q7+Q8+Q11 CLOSED — the Q-series is down to Q12 (jargon strip)
- ✅ **Q5:** corpus op='read' states the digest-vs-source truth. ✅ **Q7:** capabilities(section='features') — the 76-id closed maps_to_feature vocabulary reachable cold (+ overview line); the no-fiction contract's pointer now lands somewhere. ✅ **Q8:** a jury role on a map/role step REFUSES at save (its quorum semantics can never silently drop to single draws); kind='jury' saves clean. ✅ **Q11:** ingest normalizes the path BEFORE the read — verified with FOUR spellings of one file → one id, zero duplicates (the first wiring normalized after the open and died on the prefix spelling; caught by the verify).
- **Q-series: 11/12 closed same-day.** Q12 (strip build-lineage jargon from agent-facing descriptions) = a careful editorial pass across every tool docstring — its own beat. needs-tim: next /mcp reconnect lands sweeps 1+2 + the enums.

## 2026-06-10 — Q12 DONE ⇒ ★ THE Q-SERIES IS 12/12 (eval#4's whole backlog closed same-day, like P's)
- ✅ **Q12:** 10 editorial workflow lanes (tools/*, docstring-only, self-verifying) + the lead's serial server.py pass (14 strips) + 2 stragglers — **verified by regex over all 47 LIVE tool descriptions: zero lineage tokens remain.** Finding: the tools/* modules were mostly already clean — the jargon concentration was server.py's flat tools. BONUS: a lane FLAGGED a real docstring-vs-code drift in create (manual reflect duty vs the automatic reflection) — fixed (reflected/reflected_in, matching code).
- **BOARD: the eval#4 cycle is fully closed. Remaining lanes: B3 rolling · B4 consolidation drafts · B5 memory migration · the Track-1 brain conversation (Tim's).**

## 2026-06-10 — B5 batch 1 (the harness→system migration begins)
- ✅ (B5 complete — 3 batches) the 5 most absolute harness rules → status='proposed' operator-memory rows with his verbatim words (no_versioning · no_silent_failures · make_each_thing_work · no_time_estimates · flag_hardcoding). Registry: 7 confirmed + 8 proposed. **Tim's gate: operator(op='proposed') lists them; his one-word confirms flip each standing.** ~10 more migratable Tim-rules remain in the harness notes (batches of ≤5; project-state notes stay harness-side by design).

## 2026-06-10 — B5 batch 2 (the migration is ~2/3 done)
- ✅ (B5 complete) five more rules migrated (investigate_before_coding · use_existing_resources · record_expansively · render_for_cognition · no_deferral — the last with the standing-rule lineage note pointing at the confirmed conditional-memory refinement). **Registry: 7 confirmed + 13 proposed.** One more batch (~5: expand-dont-echo variants already mined; remaining candidates: confirm-before-writing · look-at-output · not-a-developer/AI-supplies-domain-knowledge · maximal-capture · incomplete-work-in-scope) then the migratable set is exhausted.
- DESIGN NOTE for Tim's gate: 13 one-word confirms is approaching a refine-before-gating violation — the BATCH-CONFIRM form ("confirm all 13" / per-row veto) should be offered, mirroring the registry walk's block-approve.

## 2026-06-10 — B5 COMPLETE: the harness→system memory migration is done
- ✅ **B5 (3 batches):** the migratable working-with-Tim knowledge is FULLY in the system registry — **25 rows: 7 confirmed + 18 proposed**, every one with his verbatim evidence. What stayed harness-side BY DESIGN: project-state notes (session context, not knowledge-of-Tim). The remaining gate: Tim's BLOCK-CONFIRM of the 18 (offered as one decision with per-row veto — the refine rule applied to his own memory).
- **THE BOARD'S LAST LANES: B3 rolling (fire on staleness) · B4 consolidation drafts (other-lane docs, propose-not-apply) · Track 1 (the brain conversation, Tim's).**

## 2026-06-10 — B4 COMPLETE (the consolidation drafts) + two stranded records adopted
- ✅ **B4 (4 lanes, propose-not-apply — sources verified untouched):** the honest-judgment rule produced the RIGHT mix — 2 'consolidate-the-core' (methodology docs: extract the shared protocol, keep the skill-mirrors, real inter-copy drift catalogued; coordination protocols: ONE 14-item PROTOCOL + an archive banner, the relay record preserved as evidence) and 2 'keep-separate-by-design' (the briefs are signed correspondence — merging would orphan two sessions' voices; the consent tests are criteria-traceable with load-bearing repetition). Bonus findings for owners: stale branch-rules in all 3 methodology docs (superseded by no-branches law) · a superseded identity-section in COGNITION-BRIEF · 3 coverage gaps neither consent suite tests · a tests/_consent_harness.py extraction candidate. Drafts: build-prep/cognition-self-improvement/consolidation-drafts/.
- Adopted 2 stranded coordination records (the #55 human-face seam message · the ui_registry orphan OPEN-RED diagnosis).
- **THE BUILD BOARD IS DONE.** Every lane: B1 ✓ B2 ✓ B3 rolling ✓ B4 ✓ B5 ✓ B6 (P8b folded into Q11 ✓ · G24-edge loud-capped · inbox litter flagged · Tim-reminders standing) · B7 ✓ (eval#4 + Q-series 12/12). Remaining = Tim's: the 18-row block-confirm · the brain conversation (Track 1) · the walk-surface FE ([M]'s lane) · chown/keys/vhdx reminders.

## 2026-06-10 — the FLOOR WALK is a standing organ (Tim's wide-pass, generalized)
- ✅ **flows/floor_walk** (the 6th flow): 4 deterministic detectors for the wide-pass's lived death-modes — stranded SETTLED files (mtime-aged, never grabs in-flight edits) · built-never-imported components · stale unresolved decisions · phantom corpus sources. Report + ONE dup-guarded review card; proposes-only (adoption stays judgment). **First run found real work: BlastRadiusReach.tsx + ContextBundle.tsx are ALSO built-but-dead (the RegistryProposals death-mode has siblings) · 40 stale decisions · 2 phantom sources (incl. a g25-probe leftover).** The walking-the-floor function a 100%-AI company otherwise lacks is now a rolling program.
- NEXT (follow-up judgment work from the report): investigate the 2 components (mount-worthy vs superseded?) · the 40-item litter sweep (owners flagged once already — propose a batch-retire to Tim?) · delete the 2 phantom records.

## 2026-06-10 — the floor walk HARDENED by its own first runs (the new-organ shakedown)
- ✅ Two real defects found by verifying the walker itself: the unmounted-detector FP (sibling-relative `./<stem>` imports unmatched — BlastRadiusReach + ContextBundle were ALIVE inside BuildIntentCard; stale card retired, detector widened) · the phantom cleanup done the append-only way (kind='retired' TOMBSTONES via the existing capture — the corpus supersedes, never deletes) + the latest-record read made EXPLICIT max-seq (the naive loop trusted iteration order and kept the OLDEST). **Honest steady-state: 0 stranded · 0 unmounted · 0 phantoms · 41 stale decisions — the inbox litter is now the ONLY finding, and it needs a batch-retire proposal to owners/Tim.**

## 2026-06-10 — the LITTER SWEEP: Tim's inbox is real again
- ✅ 37/41 stale decisions batch-retired by deterministic class (test-probes · empty-title test reviews · Viv trial-debriefs · probe/eval leftovers) — status lane only, resolved untouched. **KEPT (real): s60 (a build review, wire lane's) · s78/s79 (B3 parked offers — live consent conversations) + the fresh walk card.** The inbox went 41-stale → 3 real items. Floor-walk refinement: its own live card self-excludes from the stale detector.

## 2026-06-10 — ⑫ MARKER RECONCILIATION: the journal's stale-open lines flipped in place (34 total)
The doc had grown journal-style — closures recorded in later sections while the original 🔴 lines stayed open (the same staleness class the radar's doc-channel exists for, in its own backlog). All flipped in place with closure pointers. **THE TRUE OPEN SET (5):** G2 (role-rediscover vs the long-lived server — partially mooted by Q3's mtime pattern; the role registry could adopt the same) · G8 (a workflow-pattern lesson, standing guidance not a build) · G24-EDGE (one deterministic unit, bounded investigation queued) · GC8 (dynamic mid-walk re-composition — Track-1-adjacent) · GC14 (conditional-memory injection — THE Track-1 design centerpiece). Everything else: done, designed-awaiting-Track-1, or Tim's gates.

## 2026-06-10 — G2 CLOSED: the last small build on the board
- ✅ **G2:** RoleRegistry reads refresh-if-stale via a dir SIGNATURE (mtime + name-set; pure mtime was FLAKY — this fs quantizes to ~10ms ticks, measured with evidence; same-tick replace+remove were invisible). Out-of-process role files now land/leave a long-lived server without restart — the cascade registry's Q3 pattern, hardened. **THE TRUE OPEN SET IS NOW 4:** G8 (a lesson, not a build) · the G24-edge (bounded curiosity) · GC8 + GC14 (Track-1 design pieces, Tim's conversation). Nothing buildable remains.

## 2026-06-10 — TRACK-1 DIALS BUILT (character traits as adjustable values)
- ✅ **dials/ registry + Suite.set_dial/dial_state + `dials` MCP tool:** anticipation (reactive/warm/hot, def warm) · stability (museum/workshop/stage, def workshop). Values on the system graph (presence-mode seam); condition-scoped overrides STORED+validated in the rules-engine shape (conditions combine — Tim), `overrides_evaluated:False` honest until the now-organ/rules wiring. Verified by use (fresh-suite persistence, non-wiping merge, 3 fail-loud probes, discovery gate); floor enrolled (43/43 + drift 5/5).
- 🔌 **CONSUMER SEAMS (named, unbuilt):** the resolver/now-organ reads `anticipation`; the RHM surface-composer reads `stability`. Their builds MUST read the dial (the dials/AGENTS.md "never" list forbids ignoring it). needs-tim: /mcp reconnect lands the `dials` tool.
- 🧭 **The standing shape:** every future character/behavior question defaults to "declare a dial," not "make a decision."
