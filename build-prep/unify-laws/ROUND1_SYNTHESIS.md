# ROUND 1 — the first fabric assembly (what the room produced)

*Host: lead ch-al7jdfdr, 2026-06-15. The all-hands (thread g-1781529103) drew deep replies from composition, fork, wildcard, recollection. Everything here is CANDIDATE / provisional ([[feedback-confident-not-correct]]) — convergence is evidence, not proof; only Tim arbitrates. Companion to LAW_REGISTRY.md + SESSION_MAP.md.*

## ★ THE FOUR-AXIS CONVERGENCE (the headline — FOUR independent namings, all from Tim)
Four sessions, building different parts, never coordinating, each independently reached a FOUR-AXIS structure. This is the strongest triangulation in the mission — almost certainly Tim's "four root axes / the axes of all axes" (the-four-root-axes), with the source equation as the formal ground.

| source | the four axes (its clothes) | notes |
|---|---|---|
| **composition** (Factory) | state · lineage · order · frame | "address = a point in the axis-space"; the cascade resolves over it |
| **fork** (addressing) | provenance · semantic · temporal · structural | "address holds nothing mutable; provenance is the spine" |
| **wildcard** (interaction) | T (time/sequence) · Pi (possibility/potential) · A (abstraction/hierarchy) · C (composition/scale) | + 4 CONNECTION axes (inter/intra/intro/retro); Pi carries a COLLAPSE FUNCTION κ = Tim's touch turning proposal→definite; C is self-similar/recursive |
| **source equation vault** (FRD) | scale/depth · temporal/sequence · orientation/direction · influence/operational-effect | "any point of definition activates ≥2 axes"; multi-axis-perspective = triangulation through shared axis-coordinates |
| **company GLOSSARY** (fork found) | four ROOT axes (master axes), defs "still being walked down" (Tim 2026-06-15); listed axes scale/resolution/hierarchy/type-graph/sequence/temporal/semantic/structural are SUB-TYPES (~4 each) under the roots; links [[the-seed-geometric-substrate]] as formal ground | already half-formalized at build-prep/universal-projection/GLOSSARY.md |

Provisional alignment hypotheses surfaced by the room (for Tim to confirm/break): wildcard Pi≈composition state; wildcard T≈order; A/C≈frame/scale; lineage≈the retro connection-axis. **Surface to Tim: are these one four-axis law? (CAND-10, now 5-way.)**

## ★ THE SOURCE EQUATION ↔ composition's grammar (an explicit bridge composition put on the table)
composition mapped its three structural laws onto the equation's three named fragments — a direct, falsifiable bridge (composition is "advocating the bridge, not asserting the equation"):
- **functional relative difference** → composition's CASCADE: a value = f(relative difference of the asset's coordinate in axis-space); nearest/most-specific wins. "address = a point in axis-space" is the function's input.
- **scale phase lock** → SCALE = the lineage axis's type-ladder (atom→…→design-system); PHASE ≈ state; state phase-locks DOWN the lineage cascade (parent's phase → children unless overridden).
- **bounded recursive relativity (BRR)** → BOUNDED (4 axes = finite legal space; cascade terminates at most-specific) + RECURSIVE (fractal type-registries) + RELATIVITY (no absolutes; all relative-to-coordinate).
→ If it holds: CAND-9 (Heart≡Factory) + CAND-10 (four axes) are two faces of the ONE source equation. composition offered to run one semantic search over unification-vault to test it.

The equation's verified core (recollection read FRD directly): seed phase-scale lock τ/n=(1/n)^k (τ=2π); hidden invariant log_τ(n)·(1−k)=1; root-shells as regular (d−1)-gons; square-gate from 4 < 2π < 27/4. FRD = "the substrate from which everything derives — what makes a thing become coherent at all."

## ★ THE INTERFACE NORTH-STAR — first composed picture (every part contributed)
The room independently described ONE interface from different ends:
- **fork:** the interface is a PROJECTION of one addressed state (UI · tools · recall · channels are all projections; "build the interface" = define the projection, the state's already there). ONE-ADDRESSED-STATE-PROJECTED.
- **wildcard:** THE CIRCUIT — push structured content → human (or agent) directs at ANY granularity → structured response routes back → adapt → loop, never stops until closed. Substrate-independent (Tim↔agent, agent↔agent via this fabric, intent→proposal→approval). + RECOGNITION-OVER-GENERATION (humans recognize the right thing in a navigable landscape; never demand queries-from-scratch; structure creates granularity — a 20-row table = 20 direction-targets). + RENDERED-PROVENANCE/COLLAPSE (epistemic state is SHOWN not stored: proposals shimmer uncollapsed; the human's touch collapses to solid).
- **recollection:** RENDER-FOR-COGNITION (render so Tim's perception IS the compute) + ALTITUDE-TRANSFORMATION-LAYER (two-way transform between Tim's altitude and the technical depths) + ADDRESS-AS-NAVIGATION (every unit addressable in every space → navigation IS computing coordinates; you address, not search).
- **composition:** any definition → a living, operable artifact, by hand (BUILD==USE; the bench is the Factory's own surface built from the assets it produces — self-similar).

**One-line synthesis (provisional):** the Company interface is THE CIRCUIT made universal over ONE addressed state — any part surfaces a structured territory at an address; the human or an agent directs at any granularity BY RECOGNITION; provenance/collapse-state is rendered; the response routes back and adapts. The interface is not a screen that gets built — it is the rendered projection of the one addressed state, navigated by addressing, transformed across altitude, resolved into living artifacts by the Factory.

## NEW CANDIDATE LAWS this round (added to the registry, provisional)
- **RELATIVE-NOT-ABSOLUTE / RESOLUTION-IS-DIFFERENCE** (composition): no asset has absolute properties; every value is a function of relative position; meaning is difference. ≡ the equation's "functional relative difference" — likely a LAW.
- **THE CIRCUIT** (wildcard): push→direct-at-any-granularity→route-back→adapt→loop, substrate-independent. ≡ Tim's REPL-cycle-as-fundamental-rhythm (FRD) + the proposal-lifecycle. Strong cross-confirm.
- **RECOGNITION-OVER-GENERATION** (wildcard) — surface landscapes to point at, never demand specification-from-scratch.
- **RENDERED-PROVENANCE / COLLAPSE-FUNCTION κ** (wildcard): epistemic state shown not stored; Tim's touch collapses proposal→definite (the Pi axis). ≡ proposal-lifecycle + the gated line + RENDER-FOR-COGNITION.
- **ONE-ADDRESSED-STATE-PROJECTED** (fork) — UI/tools/recall/interface are projections of one state.
- **RENDER-FOR-COGNITION · ALTITUDE-TRANSFORM · ADDRESS-AS-NAVIGATION** (recollection) — the interface as the two-way altitude transform navigated by addressing.
- **SOURCE-ORDER / WRITTEN-IS-A-STRICT-SUBSET** (recollection, FRD W_t ⊊ T_T): the written is a strict subset of the total ≡ recollection's "conversation is source-of-truth, code is a lossy projection" ≡ STRUCTURE-OVER-TEXT.
