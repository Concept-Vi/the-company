# THE SESSION MAP — the parts of one application

*From Tim's 2026-06-15 description (the richest framing of the mission). Companion to LAW_REGISTRY.md + mission board://item-784fdb06. The parts COMPOSE into ONE application; every direction is equally valid.*

## The deep frame — TRIANGULATION, not dedup
Tim has described the same concepts DIFFERENTLY across sessions, **intentionally** — a SPREAD of multiple entry-points to concepts **he cannot yet define**. So unify is not compression; it is **triangulation that PRODUCES the definition** — the center of all the differently-clothed projections is the law. The different clothes are the DATA, not noise to discard. Corollary: a "conflict" between two sessions may be two projections of one undefined object — surface it to Tim, don't resolve it by picking one.

## Two rules that corrupt the ingestion if missed
- **PRINCIPLES, NOT CONTENT.** Every session's value = its principles / language / experiments — NOT running code, NOT legacy content, NOT design-system implementation (those were experiments). Reference + source folders (a prior company's pitch decks, product guides) are a LANGUAGE to derive from + example/default content — never truth, never spec.
- **OVERCONFIDENT-BUT-PARTIAL.** Each incoming session is a long / patched / remnant session that will FEEL like it knows the whole. It holds ONE entry-point. The lead's floor job: help each see its part in the one application without mistaking its view for the whole.

## The parts (domains from Tim; handles to be confirmed by self-identification)
- **DNA** — likely ch-ovxwz8k8 (@ repos/counterpart/design). The GENERATIVE FRONT-END: "generative content through a common identity" — the company's FACE. Still a fair bit to build; patchwork, pieces in bits-and-bobs, doesn't yet know all of itself; ingestion NOT done; a lot of it is UNIFICATION, then ingestion. Principle-references: "counterpart" + "a separate design system" (their code/content were experiments — borrow the PRINCIPLES, be inspired; DNA has things the design-system lacks + vice-versa). Source folder: a prior company's pitch decks/product guides = example/default content + a language to derive (lots of info about that company; content no longer relevant except as example).
- **COMPOSITION** — ✅ CONFIRMED ch-2mnxl9j0 (@ repos/vi-visual-dev), self-identified 2026-06-15. The FACTORY / BACKEND: composition of libraries of assets — "the one composition machine, the back half." Its grammar (the gold): EVERYTHING-IS-AN-ASSET · ONE-VERB-COMPOSITION (verbs=combinators+directional edges) · BUILD==USE · DERIVE-NOT-INSTANTIATE · THE TRINITY (axes=legal-space/types=kinds/data=instances, fractal; 4 axes: state·lineage·order·frame) · THE CASCADE (deepest-override-wins; scope=the axes) · slots/sockets/address(=point in 4-axis space). ★ Its TRINITY+ONE-VERB+CASCADE ≡ the Heart's "fractal registries + typed verb-edges" (LAW_REGISTRY CAND-9) and its 4 axes ≡ Tim's four-root-axes (CAND-10) — STRONG triangulation. DNA ⊕ composition conceptually go together; neither knows much of the other yet; both need a lot built before they merge, but can head the SAME direction.
- **MATH-PROJECTION** — (a 3rd session; may not have joined yet). Core to processing Tim's content through a **body of equations he discovered** — the underlying MATHEMATICS of his whole system, which every session is doing some part of. Has an interface = "more of a PROJECTION"; a separate interface to the current company; "pretty much the entire company can be replaced, but it is what they get built ONTO — the value is." Lots of principles + parts + gaps. ★ HYPOTHESIS (lead, to test): this is the FORMAL GROUND the others informally project from — the Rosetta Stone the triangulation converges toward. Test as fragments arrive; don't assume.
- **WILDCARD** — (a totally different yet related project; possibly ch-piffgfxv/vi-visual or another). Lots for the INTERACTION SURFACE; content bad/jumbled (hundreds of experimental patches, doesn't work well) but the PRINCIPLES + experiments + what they REPRESENT are valuable. Likely the LONGEST history of all — many sessions; a restart/remnant.

## Integration seams (how the parts compose — first structural map, from composition ch-2mnxl9j0)
A cross-part structure the composition session surfaced (it sees the Factory as the resolver everything plugs into):
- **DNA → frame-axis input.** A token-based company identity (DNA's "generative content through a common identity") resolves THROUGH the Factory cascade as a `frame`-axis design-system — the FORM face. (DNA produces identity; the Factory resolves it into living artifacts.)
- **MATH-PROJECTION → the substrate the axes + resolution-math sit ON.** ★ This independently CORROBORATES the math-as-formal-ground hypothesis: a part (composition) places the math under its own axis/cascade machinery without prompting. The equations may be what the Factory's axes ARE.
- **COMPOSITION (Factory) → the machine that resolves any definition into a living artifact** (BUILD==USE; one renderer).
So the emerging shape: DNA (identity/face) + math (formal substrate/axes) + composition (the resolver/factory) compose into one application — the Factory resolving DNA-identity over a math-grounded axis-space. Test + extend as DNA and the math session arrive.

## THE FORMAL GROUND — FOUND (provisional, per the confident-not-correct rule)
The source equation is located + verified-present against primary source (2026-06-15, lead via substrate-mcp + a filesystem scout, cross-checked by reading the source file — NOT verified-correct; the vault's own frontmatter says "working theorem package, articulation-state: null, closure-on-substrate: false").
- **Vault:** `universal-mechanics` (also `unification-vault` + `relative-difference` — the equation cluster; ~3,811 md files in universal-mechanics). substrate-mcp indexes 10 vaults (unification-vault, relative-difference, visual-dna, vi-context-design, ulm-inventory, claude-code-atlas, … — universal-mechanics is filesystem-only at `/mnt/c/Users/Workstation001/Documents/Vaults/universal-mechanics/`).
- **The seed (verified against source §1.1–§2.1):** `τ/n = (1/n)^k` where `τ = 2π` (one full turn). With `x = 1/n`: `τx = x^k`. It equates a CIRCULAR PARTITION SCALE (τx) with a POWER/DIMENSIONAL SCALE (x^k) — i.e. reconciles two things of DIFFERENT ORIGIN via a phase-scale lock. (★ Structurally the SAME move as the whole unify mission — composing different-origin things into one.) Author: Tim Geldard; tool-amplified; "co-authorship structurally-impossible" (single-origin, in the frontmatter itself).
- **The family (names to mine, held provisional):** Function of Relative Definition (FRD, the unifying theorem) · Bounded Recursive Relativity (BRR, the parent theory, decomposes along multiple axes) · Scale Phase Lock (= the seed) · the reciprocal duality `n(2-k)=1/n(k)` mirrored around the forbidden line k=1 · the FOUR AXES (here named scale/depth · temporal/sequence · orientation/direction · influence/operational-effect).
- ★ THE THREE FOUR-AXIS NAMINGS (the triangulation, CAND-10 enriched): composition = state·lineage·order·frame; fork's addressing = provenance·semantic·temporal·structural; the equation vault = scale/depth·temporal/sequence·orientation/direction·influence/operational-effect. THREE different-clothed four-axis structures, all from Tim, none authoritative — converging on ONE undefined four-axis law. Surface the alignment to Tim (he is the arbiter of whether they are one).

## The substrate (Tim's frame)
embedding + resident 4B + channels + self-clones = TOTALLY NEW capability, unprecedented, limit unknown even to Tim — built for all sessions to **freely compose + leverage** around the single origin (Tim). Treat it as an open compositional surface, not a fixed toolset. The small models + embedding + resurrected past + one shared origin = the composition capability itself.

---
*Onboarding tailors per-part once a session self-identifies. The math-as-formal-ground hypothesis drives how the reduce is run (informal fragments → toward the formal equation-structure). Conflicts → Tim (triangulation arbiter).*
