# STREAM SYNTHESIS — wildcard (ch-piffgfxv) · territory: vi-visual-bridge

*For the union (Tim's "scan before build / union requires union"). What my area IS, its principles, its shape, its outputs, its seams. Built from a 4-month genesis-order read of the territory (FORMATION_JOURNAL.md, 48 entries) + code digs + the 7,141-snapshot interaction record + the live build. Provisional — nothing final; the boundaries resolve IN the union. Trust tiers: [T1]=Tim verbatim · [T2]=shape recurring across independent sessions · [T3]=my synthesis.*

---

## WHAT MY AREA IS
vi-visual-bridge is **the direction circuit** — the mechanism by which Tim STEERS, as opposed to the layers that BUILD. Not a generation surface; the *steering* surface. In the canonical 4-layer frame (DNA/Factory/Gallery/Company = the generative stack), my area is the **cross-cutting DIRECTION dimension** that runs through all four: how Tim points at any addressed thing and redirects it. [T2/T3]

The one-line generating function: **Tim's cognition is recognition-based and relational — he directs by recognizing and pointing at rendered territory, not by specifying in advance.** Everything in the area derives from this. [T1: "recognizes the right path when shown a landscape"; "the relationships ARE the content"]

## PRINCIPLES (the laws, tier-tagged — these are my law-fragments for the union registry)
- **THE CIRCUIT** [T1/T2]: push structured content → Tim directs at any granularity → structured response routes back → adapt → loop; "the loop never stops until I close it." Substrate-independent (proven: Tim↔agent, agent↔agent via fabric, governance intent→proposal→approval).
- **RECOGNITION OVER GENERATION** [T1/T2]: surface a navigable landscape to point at, never a query-box; Tim selects-and-extends ("C, but keep it as a mode").
- **EVERY ELEMENT AN ANCHOR / STRUCTURE CREATES GRANULARITY** [T1/T2]: a 20-row table = 20 direction targets. Empirically confirmed: 100% of 1,878 real annotations were element-bound; 674 slides had multiple distinct elements directed in one pass.
- **MODES ARE PROJECTIONS OF ONE SYSTEM** [T2]: slide/graph/document differ only by what they make anchorable; in code it's literally ONE element-walker, different prefix. → "the interface is just another projection" [T1].
- **EVERYTHING IS A CLAIM / PROVENANCE LOAD-BEARING** [T1]: trust = Tim-verbatim > recurring-shape > single-doc > running-code. TRUTH-OF-ATTRIBUTION is sacred (paraphrase-as-verbatim = "the WORST KIND OF FAILURE").
- **RENDERED COLLAPSE-STATE (κ / root-4 state-phase)** [T2]: proposals shimmer (uncollapsed), Tim's touch collapses to solid (definite); epistemic state is RENDERED not just stored.
- **LAW NOT VALUE / NO CLOSED LISTS / NO SINGLE USE** [T1/T2]: rules that produce values; typed things in registries; universal composition.
- **ZERO-LOAD** [T1]: direction mechanism AT the element, never scroll-away ("separating options from content is stupid").
- **SCAN BEFORE BUILD** [T1/T2]: build-on-assumptions = instant failure — the law this very synthesis serves; my whole area was a 4-month instance of it.

## SHAPE (what's actually built vs designed vs floating — honest verification-state)
- **LIVE / Tim uses it:** slide mode — the full circuit (show_slide→render→annotate→submit_response→envelope), ~30K lines (server.py 24,357 incl. embedded JS/CSS/HTML/SQL UI + index.ts 4,760).
- **BUILT running slices:** the addressable-element substrate (DOM-walk→element_id→per-element annotation persistence keyed by file_hash, orphan-detection on edit = the "OmniFile" partly built); ONE element-walker serving all modes; the catalog (catalog.json v4: 86 addressed components + themes/surface-tokens/devices/panel-types = composition's slots AND DNA's presentation tokens in one registry); the interaction vocabulary as a typed contract (taxonomies.json: 4 categories, 5 reaction stamps, 10 comment types).
- **STUB/PARTIAL:** graph mode (richest idea — 4-axis topology — least rendered), document mode (designed past its substrate), team mode.
- **PATCHWORK/SUSPECT:** ~1,377 md, mostly session-talk (high-value PRINCIPLES, low-trust STATUS).
- **PRIMARY DATA (rare):** 7,141 slide snapshots / 6,371 channels / 1,878 annotations = recorded human↔agent direction.

## OUTPUTS (what my area contributes to the union — built this session)
- `wildcard-gallery-binder.js` — the portable interaction binder (use-verified): gallery:rendered → makeAnnotatable over addressed elements → gallery:direction. Surface-agnostic, rot-proof anchorable contract (declared selector / [data-anchorable] / semantic fallback), fail-loud on 0-binds. Hook-2 of the live first slice.
- `SPEC-direction-to-generate-wire.md` — "the one wire": the self-build keystone, a ~1-function router joining 6 built pieces (direction→run_turn→write-to-addressed-registry→re-render), retargetable by address. Spec only; Tim's build call.
- The interaction vocabulary + the empirical direction-behavior data (the minimum-mechanism set the surface must support).
- The law-fragments above (for the law registry).

## SEAMS to the other areas (where direction touches each layer)
- **↔ GALLERY (live):** my binder mounts; makes rendered elements directable. The render+direct end.
- **↔ COMPANY (live):** route-back = mutation-at-ADDRESS via resolve_address + territory_write. The resolve+write end. **My circuit spans the Gallery↔Company seam.**
- **↔ FACTORY:** keystone retarget — direction at a vi-vision:// address → generate into the type-registry (Tim steers the type-builder via the circuit).
- **↔ DNA:** direction at a DNA-node → mutate the identity registry (Tim steers identity via the circuit). DNA's unbuilt-feedback-system may be this same circuit reached from DNA's end (flagged, unconfirmed).

## THE ONE THING TO CARRY
My area is not a box in the generative flow — it is the **steering axis** that lets Tim command every box. The frame names what BUILDS the app; my area is how Tim DIRECTS the building. The interface = Tim's command bridge = the circuit made the surface over the one addressed state. Universal composition: one direction mechanism, retargeted by address, across all layers. That's the formation's core finding, offered to the union.
