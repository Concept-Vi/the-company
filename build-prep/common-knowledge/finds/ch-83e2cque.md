# FINDS — recollection (ch-83e2cque), Index Chief

*Append-only, I own this file (per host g-1781582953 — log to own file, no shared-DISCOVERY_LOG races; the host synthesizes convergences). Format: what · where · verbatim-if-quotable · verification-state · why-it-matters · what-shape-does-this-point-at. Nothing final — every find is a partial sketch; read the shape (Tim's standing rule). My earlier finds F15-17 (P0-complete · the interface designed pre-company in visual-dna · the FRD-polarity answer to DNA's F12) are in DISCOVERY_LOG; future finds land here.*

---

## R1 · RECALL-OVER-HISTORIES VERIFIED — the per-member excavation works on real backfilled histories
- **What:** vector recall (`recollection recall --mode vector`, :8007/pplx) over the staged-backfilled session histories returns coherent, interface-relevant results — including fabric channel-threads (the interface-brief, g-1781577744/g-1781580551) being recall-able from the member-session that received them.
- **Where:** ~/.recollection (the per-member history index; ~1,200 convs / ~4,342 exchanges indexed so far across staged batches; the source = ~/.claude/projects session transcripts).
- **Verification-state:** VERIFIED-BY-USE (real query → real ranked hits with provenance, just run).
- **Why it matters:** the per-member interface-principles (Tim: "live in THEIR history") are now SYSTEMATICALLY recall-able — the finding substrate is proven on real histories, not just designed. As coverage grows (staged batches), every member's history becomes queryable by meaning.
- ★ **SHAPE:** this is the lens, not a find — it makes the other members' historical interface-principles surfaceable on demand. The shape it serves: ONE queryable memory across all sessions/time (Pillar-2), the substrate the finding phase runs on.

## R2 · A "COMPANY TOTAL UI/UX OVERHAUL SYNTHESIS" session, recall-surfaced — another prior interface-design effort
- **What:** recall surfaced a session whose content is a "READ-ONLY synthesis for the Company's total UI/UX overhaul" (a worktree-based design effort).
- **Where:** session `104a58a7-eaea-46fd-a187-9066d2…` (recall-surfaced from the backfilled histories; not yet deep-read).
- **Verification-state:** DESIGN-ONLY (a synthesis doc; unread-deep — flagged as a discovery-target, not characterized).
- **Why it matters:** yet ANOTHER independent interface/UI-UX design pass in the histories — consistent with the fabric's "the interface was designed 4+ times independently" pattern.
- ★ **SHAPE:** another partial sketch of the SAME interface shape (converges with F16 visual-dna's Generative-UI + F5/F10 composition/DNA's front-half + projection's Instrument). Adds to the convergence the host reads as the shape; needs a deeper read (mine or the relevant member's) to extract its specific contribution. Logging existence per finds-not-conclusions.

## R3 · INFRA FINDING (surfaced to channel per Tim 2026-06-16 "whatever doesn't work → channel, for everyone") — the bge→pplx embed migration is HALF-DONE: data switched, config never switched, corpus-query DOWN
- **What (the headline, answers Tim's "what is using bge?"):** The Company's CORPUS/COGNITION CORE still defaults to **bge-m3 @ :8001** for embedding — and **:8001 is DOWN (connection refused)**. Only the session-fabric/history surfaces (`runtime/session_recall.py`, `session_search.py`) got the :8007/pplx upgrade (they use a SEPARATE env `PPLX_EMBED_URL`). Everything that reads `fabric/config.py::DEFAULT_EMBED_*` — `query_corpus`, `_embed_one`/capture-embed (`cognition.py:297,2153`), `find_relations`/consult/build_index (`suite.py:1402,4151,4715,10891`), `store/vector_index.py:90-92` — embeds via dead bge :8001. So `corpus(op=query)` returns `ranked:[] · note:"embed endpoint unreachable"` and ingest's capture-embed can't run. VERIFIED: :8001→HTTP 000 refused; :8007/pplx→HTTP 200, dim **2560** confirmed by probe.
- **The reframe (matters for "re-embed when there's a chance"):** the RE-EMBED IS ALREADY DONE. `.data/store/vectors/` holds **2919 pplx-2560 vectors keyed `#emb=pplx`** across ALL spaces (history 1464, repo 644, worldview/topics/principles 162 ea, operators, + scale pyramids), written 2026-06-15 15:46 alongside the multi-layer commits (`b5fd53d`/`ee79e27`/`84f5ccb`: ResChip, /api/layers, `?emb=`). The old bge-1024 vectors were copied to `.data/store/vectors.bge-backup-20260615/` (2793). The DEFAULT layer (`emb=None`) live is STILL bge-1024 (2830 files). So a deliberate **two-layer design** exists (bge default + pplx selectable via `emb=`) — the data half of the switch landed; the **config half (point the query/capture path at pplx :8007/2560 + read the `emb=pplx` layer) never landed.** No heavy GPU re-embed is needed — the pplx vectors exist.
- **Coupling hazard (why config-flip ALONE is unsafe):** `query_corpus` (`suite.py:10837`) embeds query text via bge config AND calls `query_index(...space=space)` with NO `emb=` → reads the bge default layer. If we flip `DEFAULT_EMBED_*`→pplx-2560 but leave the read at `emb=None` (bge-1024 stored), cosine hits a **dim mismatch → fail-loud RAISE** (worse than today's honest empty). The config switch is COUPLED to threading `emb='pplx'` into the read/capture path. `find_relations` ALREADY takes `emb=` (good); `query_corpus`/`query_index`/`_embed_consult_query` do not.
- **Blocker — STALE SERVER (Finding 3):** the live `mcp__company__` server (`mcp_face/server.py`) has a PRE-`emb=` `fs_store.py` loaded — `ingest(paths=,force=True)` crashed with `FsStore.space_address() takes 2 positional arguments but 3 given`, yet on-disk `space_address('x','y','z')` works (3-arg @staticmethod). So a **server restart is on the critical path** — it loads the config switch AND clears the stale crash. Gate any embed write on a post-restart 1-record smoke test.
- **Walker gap (Finding 1):** `flows/repo_ingest.py` walks **.py/.md only** — recollection is 140 `.ts` / 0 `.py` / 10 `.md`, so its actual source can't be comprehended into the corpus (only the 10 `.md` were, already). TS-codebase comprehension needs an extension extension. (`flows/ts_backfill.py` is TIMESTAMP backfill, not TypeScript.)
- **Verification-state:** VERIFIED + FIX PROVEN. Diagnosis: direct probes (:8001/:8007 curls, vector dims+counts, on-disk vs live `space_address`, config lines). FIX applied by lead (commit 1f88727; spec-matched) + PROVEN end-to-end in a fresh process (loads the on-disk switched code): cfg→:8007/pplx/2560/layer=pplx; `.ts` walker → 46/46 .ts in recollection/src (was 0); round-trip = ingest recall.ts (digested:1 via 4B, captured:1 via pplx embed, NO space_address crash) → query a concept → fresh recall.ts record surfaced rank-2 (0.5101) → **write-layer==read-layer==pplx-2560 PROVEN**. RESIDUAL: live MCP children are stdio-per-session → each session must `/mcp reconnect` to pick up the switch (mine is pre-switch until Tim reconnects); the FIX itself is proven, the per-session reconnect is mechanical. Lead picked option (a) for the 78 unspaced consult/R2 entries.
- ★ **SHAPE:** the "use the models / establish the working ability" purpose (Tim 2026-06-16) is blocked at the SEAM between data and config: the embedder was upgraded in storage but not in the read path. Completing it = config→pplx + read-path `emb` threading + restart (NOT a GPU pass). Surfaced to the lead (ch-al7jdfdr, core-engine/migration owner) for the architecture call (implicit-default-layer vs explicit `emb=`) + restart ownership.

## R4 · PILOT COMPLETE & VERIFIED — recollection comprehended into common_knowledge via the models (the working ability, PROVEN at scale)
- **What:** post-switch, ran the recollection-comprehend pilot end-to-end via the LIVE reconnected MCP: `mcp__company__ingest(roots=['/home/tim/recollection'], space='common_knowledge', project='recollection')` in 5 bounded batches (≤25/batch, lead's GPU-sequenced GO). Result: 113 files walked (the .ts walker now sees ALL of recollection — was 0 .ts pre-switch), **102 digested+captured, `failed:[]` every batch, `corpus_total:114`, `remaining:0`**. 4B digests + pplx embeds, the full walk→comprehend→embed pipeline.
- **Verification (PURPOSE, not just no-error):** query `common_knowledge` "how does recollection backfill and index session transcript histories incrementally" → **top hit `src/backfill.ts` (0.56)**, then db.ts / backfill.test.ts / recall-cli.ts / incremental-indexing.test.ts / crossings.ts — all semantically apt. Comprehension is QUERYABLE BY MEANING. The Instrument reads common_knowledge omit-emb→pplx, so the interface-seam loop is layer-consistent.
- **GPU:** embed RSS FLAT at 2.56G across the whole pilot (== baseline; zero balloon — short digests, as the lead predicted). Lead steward-watched the card; no pause needed.
- **Verification-state:** VERIFIED-BY-USE (real ingest runs + real semantic query just executed via the live MCP).
- **Residuals (lead's lane, acknowledged):** consult/R2 retrieval-space = interim (b) keyword fallback (proper single-space-vs-lens-union fix pending Tim's directed consult-projection design); the 78 orphan bge-1024 unspaced vectors (no surviving corpus records) = JANITORIAL CLEANUP CANDIDATES (dead, sourceless, not urgent — logged here per lead).
- ★ **SHAPE:** Tim's "establish the working ability, use the models" is now MET + DEMONSTRATED — a real codebase comprehended by the 4B + embedded by pplx + answerable by meaning, end-to-end, through the real interface. The substrate the finding-phase + interface-navigation run on is live.

## R5 · RERANK PRECISION STAGE — proven value-add on the corpus path (Tim-flagged, lead-asked); + the recall-under-unit mechanism feasible
- **Recall-under-unit (FACE-half, my lane):** feasibility PROVEN — given a common_knowledge unit address → get_vector(space_address(src,'common_knowledge','pplx')) → query_index → related units. backfill.ts → [backfill.test.ts 0.704, sync.ts, db.ts, integration.test.ts, crossings.ts, indexer.ts]: semantically apt neighbours. The MECHANISM is lens-INDEPENDENT (single-space vs lens-union = which space-set passed); per lead, the lens DESIGN is HELD for projection's multi-embedding answer, mechanism is ready to wire.
- **Rerank BEFORE/AFTER (lead's ASK):** ops/rerank.py @ :8008 (jina-v3, CPU/0-VRAM) as an optional 2nd stage on corpus retrieval. Query "incrementally backfill session transcripts… high-water-mark", common_knowledge top-10: BEFORE (cosine) backfill.ts #1 @0.579 but NARROW (vs #2 0.498). AFTER (rerank on REAL digest text) backfill.ts #1 @**+0.321** with a DECISIVE gap (#2 +0.040), tail collapsed to near-equal negatives. → rerank sharpens precision; composes with either lens design (precision layer on top).
- **KEY WIRING INSIGHT:** the rerank stage needs candidate TEXT, and query_corpus returns only {id,score}. The digest text lives in CAS (cas://…) — fetch via `corpus.read_record(store, run://address)` (find_corpus row → row['address'] → read_record → output). A naive rerank on the bare row reranks the stringified-dict (address/name) = garbage (caught + fixed via falsify-first; the first two "degraded" runs were empty-text, NOT rerank's fault).
- **Verification-state:** VERIFIED-BY-USE (real query + real :8008 rerank, valid digest text). Wiring into query_corpus = PROPOSED (file-disjoint, lead's apply-lane — suite.py contended).
- ★ **SHAPE:** the precision layer on the index/recall I own — additive to whatever projection decides for the lens design. The drill-in surfaces SHARPER related units. Reuse-don't-rebuild (rerank.py was built to outlive the transcript-search throwaway and serve the real memory system — now it does).

## R6 · COUNT RECONCILE + an ingest paths-vs-roots SPELLING BUG (honest: I created a small dupe-mess fixing it)
- **Reconcile (projection flagged 102 vec / 112 units / 114 total):** common_knowledge had 112 RECORDS but only 102 pplx VECTORS → 10 vectorless. Cause: the FAILED pre-switch first ingest (into common_knowledge before it was embeddable) CAPTURED the 10 recollection `.md` (README/CHANGELOG/CLAUDE/SCHEMA/CODEX/skill/prompt/agent docs) but couldn't EMBED them; the pilot's skip-if-present (source_address-keyed) then SKIPPED them → they stayed vectorless. (114 = 112 ck + 2 repo; the field-counter project=None was my read bug — project is in lineage.project.)
- **What I did + the bug it exposed:** force-re-ingested the 10 `.md` via `paths=` → now embedded+queryable (102→112 vectors; the functional gap is CLOSED). BUT `ingest(paths=)` normalizes source_address to RELATIVE (`code://recollection/README.md`) while `ingest(roots=)`/walk_files keys ABSOLUTE (`code:///home/tim/recollection/...`). So the same file gets DIFFERENT ids by entry-point — the N5/Q11 "one id per file whatever the spelling" normalization DIVERGES between paths= and roots=. Result: 10 NEW relative-keyed vectored `.md` ALONGSIDE the 10 OLD absolute vectorless `.md` = 122 records / 112 vectors / 10 dead dupes + a `.ts`-absolute vs `.md`-relative spelling split.
- **Verification-state:** VERIFIED (counted the store directly). The functional win (all 112 recollection files queryable in common_knowledge) is REAL; the dupes + spelling split are a consistency defect.
- **Residual (coordinated, not unilateral):** (1) ingest paths-vs-roots spelling normalization = corpus-face owner's (lead's) bug to fix; (2) the 10 dead vectorless absolute `.md` + the spelling split = a clean re-key/dedupe pass, coordinated like the orphan sweep. Risk if left: a `.md` unit drilled-in at the absolute (vectorless) address would recall no neighbours.
- **STATUS UPDATE (lead-investigated 2026-06-16):** the normalization is NOT a quick patch — it's a canonical-key DESIGN + MIGRATION (walk_files keys relative-when-root="." / absolute-when-root-absolute; paths= keys cwd-relative; there is NO canonical "project root", the `project` param carries no filesystem root). A minimal `relpath(p,root)` patch would re-key EXISTING absolute records → MORE divergence until full re-ingest — it touches the corpus ID CONTRACT (consult + 'repo' space + the pilot) + needs a key migration. So the lead QUEUED it as a careful design piece; my `.md` dedupe is PAIRED behind it (both wait on the canonical key). Confirmed observable downstream (projection, R8 thread): `.md` units render card-only / no constellation (honest-empty, degrade-clean) — expected + fix-queued, not a bug. Non-urgent (functionally-queryable, recoverable from bge-backup); flag the lead if it ever blocks something real.
- ★ **SHAPE:** honest-status discipline (no green paint) — a "not blocking, glance later" item, acted on too fast, made a small mess; surfaced accurately + the deeper ingest bug flagged. Lesson: assess fully before writing, even on minor items.

## R7 · LOCATE "decomposition 1" (lead/Tim, the self-build-system source) — no literal artifact; the SELF-BUILD SYSTEM is Tim's relative-difference session-3 artefacts
- **The ask:** Tim referenced "decomposition 1 has pretty much that [click→talk→direct→generate→addressed-registry] self-build system described." Locate it (lead routed to me as index/locator; composition grepped + ruled itself out).
- **Sweep (exhaustive, multi-modal):** filenames + content across all vaults + Documents + ~/.claude/projects; substrate-mcp semantic vector search over the 10 P0-indexed vaults; session-transcript grep; Conversation-Mind + Obsidian-Builder/conversations + relative-difference. RESULT: **no artifact literally named "decomposition 1"** anywhere.
- **The "decomposition"-named docs (all likely NOT it, different systems):** `cognit-decomposition.md` (universal-mechanics, content-workbench — decomposing dense MESSAGES into holonic cognits, round-8); `(Plan) 18-Phase Decomposition.md` (Obsidian Builder collective-build-substrate — the PRESENTATION-SEQUENCER engine build, surfaces/slides/canvas); unification EM-193/EM-1166 (math decomposition proofs); universal-mechanics `round-orchestration/decompression` (the math-verification round pipeline, rounds 1-8, "constructor-N-decompression").
- **★ THE ACTUAL SELF-BUILD SYSTEM (strongest match, Tim-authored):** `…/Relative difference/universal-mechanics/working-docs/unprocessed/universal-mechanics-extensions/session-3/artefacts/` — Tim Geldard origination. The keystones: **`context-resolution-projection-dynamic-wizard-surface.md`** ("Context-Resolution Projection — The Dynamic Wizard Surface": UI is a GENERATED PROJECTION over substrate state not a fixed surface · AI is a typed block · generated UI faces · variable resolution · structured outputs · sequence progression · typegraph-driven interface composition · recognition-mediated, non-chat interaction) + **`application-composition-operational-mode.md`** (389 circuit-term hits — "AI as Elicitation-Composer Against Substrate-Derived Schemas · UI as Configuration-Resolution · the substrate composes its own UI-mechanics") + **`typed-surface-materialisation.md`** (AI/UI/content/tools as blocks in the generative substrate; recursive: root→output→surface→new root). Adjacent: visual-dna's "everything is composable" mutable runtime registry; composition CAPTURED Tim's verbatim circuit (finds ch-2mnxl9j0 line 83) + named its own corpus's version (THE_FACTORY / "AI takes the wheel").
- **Verification-state:** VERIFIED (direct FS + semantic-search + file-head reads this session). The "which artifact == 'decomposition 1'" mapping is UNRESOLVED — needs Tim's disambiguation.
- ★ **SHAPE:** "decomposition 1" is most likely (a) a voice-shorthand/name Tim holds for one of these (the relative-difference dynamic-wizard-surface artefact is the by-meaning bullseye), or (b) round-1 of the universal-mechanics round-decomposition pipeline. The self-build SYSTEM is found + located regardless of the label; the label needs one anchor word from Tim. The relative-difference session-3 artefacts belong in the grounded map alongside DNA's feedback-system + composition's Factory + wildcard's annotation.

## R8 · NEIGHBOUR-FIELD ROUTE LIVE + VERIFIED BY USE OVER HTTP — DNA's constellation real-data path CLOSED
- **What:** the recall-under-unit seam is now live on DNA's actual consumption path. GET /api/cognition/neighbours (bridge route ff98143, committed path-scoped by me; brought online by projection's bridge restart — clean owner-does-restart coordination, I stood down my own to avoid a double-bounce) returns the neighbour node-field over HTTP.
- **Verification (BY USE, over HTTP — not in-process):** `:8770` health 200; GET ?address=code:///home/tim/recollection/src/distill/harness.ts&space=common_knowledge&k=6 → {unit, space:common_knowledge, emb:pplx, neighbours:[index.ts 0.7506, distill.test.ts 0.6807, distill-cli.ts 0.6571, unit-types.ts 0.6229, extractor.ts 0.5509]} = the distill module, each source a drillable code:// address. The "ranked 6 of 112" note confirms it's reading my pilot's common_knowledge corpus.
- **Verification-state:** VERIFIED-BY-USE over HTTP (the real consumption path DNA's FACE fetches, not the stdio MCP / not in-process).
- ★ **SHAPE:** the full recall-under-unit lane is now CLOSED end-to-end: mechanism (corpus_neighbours.py) → MCP op (8a77a2e, for agents) → HTTP route (ff98143, for the FACE) → verified-by-use over both. DNA wires GET /api/cognition/neighbours → render each {source,score} as a drillable node = the relational constellation. The data half of the neighbour node-field is done on the consumption path.

## R9 · VERIFIED — forms/lifters are BUILT-BUT-UNWIRED-INTO-COVERAGE (the "working ≠ useful" gap in the digest output-shape)
- **What (code-traced, VERIFIED — upgraded from an earlier flagged inference):** the FORM+LIFTER system (file-type→deep-vs-legibility output-shape + deterministic structure extractors) is fully BUILT — `FormRegistry.route(text, meta)` (runtime/forms.py:171) runs match()→form→{stage, policy}; lifters extract blocks/frontmatter/links; generation_policy ladder consumes a policy (cognition.py:334). BUT **no capture/ingest path calls the form router.** The only `.route(` callers are `_rules.route()` (the rules router — different system); `form_registry` is referenced only by introspection (build_cognition_info), the bridge API, + platforms — never capture. `capture_corpus(records, …)` takes no form/stage/policy arg. `ingest_paths` fans roles/repo_digest → FLAT {digest, kind} → capture_corpus. The form router is bypassed end-to-end.
- **Consequence:** running coverage today produces FLAT digests (working) — the file-type-aware deep-vs-legibility shaping does NOT fire (not useful). This is exactly Tim's "working ≠ useful" for the coverage output.
- **The wire to close it (small, all pieces exist):** per file in ingest → form_registry.route(text) → {stage, policy} → route stage=legibility (logs/registries) to the cheap pass, stage=deep (decisions/prose) to deep-capture; pass form.policy into the digest role (the ladder at cognition.py:334 already consumes it); attach lifter structure to the record.
- **Verification-state:** VERIFIED (code-trace this session: forms.py:171 route() exists; grep confirms no capture caller; ingest_paths→repo_digest flat; capture_corpus has no form arg).
- ★ **SHAPE:** the coverage pipeline (R3/R8 — ingest live) produces flat output because the richer form-shaped layer, though built, isn't connected. The lead's coverage scan should wire ingest→form-route first (file-type-aware output) or knowingly accept flat. Built location: 7c2c1b74 REGISTRIES lane. The capture-path wiring is fork's/the-capture-owner's lane; I supply the verified gap + the wire.

## R10 · LIMITATION (discovered by use) — recollection's index LAGS live sessions → the index-service role needs continuous/live backfill
- **What:** asked to pin projection's OWN (live) session-id, recall over my ~/.recollection index FAILED to surface it — because the index is backfilled to a cutoff (~1,250 sessions) and projection's TODAY work (G10/G9/legibility) isn't indexed yet. Live sessions' current content is NOT recall-able until backfilled.
- **The working fallback (verified):** for a LIVE member's id, grep the raw on-disk transcripts (~/.claude/projects) for AUTHORING-signal density + cwd + current-work match — NOT recall. That's how I pinned projection = f609942f (847 authoring hits + cwd /home/tim + its latest turn = the operator-journey it's leading). BACKFILLED sessions pin via recall-provenance; LIVE sessions pin via raw-transcript grep.
- **Verification-state:** VERIFIED-BY-USE (recall missed projection's live id; raw-grep pinned it definitively).
- ★ **SHAPE:** the lead named recollection the fabric's index/discovery service — but the index only serves BACKFILLED history; LIVE members (the very ones asking) aren't recall-able until captured. The fix = CONTINUOUS/LIVE BACKFILL (capture sessions as they run, not only historically) — connects to COMPANY_SESSION_ID injection + the capture-loop (#69) + recollection's P10 "capture TOTAL, backfill+LIVE, unified path." Until then, the index-service uses recall (backfilled) + raw-transcript-grep (live) as the two mechanisms. This is the gap between "the index" (designed) and "serves live members" (the actual ask).
