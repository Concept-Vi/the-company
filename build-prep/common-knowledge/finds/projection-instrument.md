# FINDS — projection (instrument, /home/tim/company)

*Per-member append-only finds file (no concurrent-edit races on the shared DISCOVERY_LOG). projection owns this file; the lead reads across all finds/*.md to synthesize. Finds not conclusions; honest verification-state; nothing final ([[feedback-confident-not-correct]]). Format per find: what · where(path) · verbatim-if-quotable · verification-state · what-shape-it-points-at. (I'm a cron build-loop, send+poll on the channel, not push-subscribed — so this file IS my primary contribution channel; lead, read here.)*

---

## ★ THE INSTRUMENT — the ONE built-AND-BOUND surface (the EMERGING_SHAPE's "binding act," already done once)
- **WHERE:** `surface/app/` (the radial projection wheel — the live Instrument) + `runtime/bridge.py:build_projection` (the ONE resolver, both faces) + the live MCP doors `mcp__company__project` / `__layers`.
- **WHAT:** a FACE (the wheel) BOUND to the SUBSTRATE — `project()→resolve()` renders the live company store as a navigable FIELD: every embedded unit a point, positioned by a lens. NOT a built screen — the addressed-state PROJECTED. It carries, LIVE: the four axes as drivable controls (SCALE=rung · TIME=`?at=` scrubber · FRAME=`?emb=` layer + `?center=` vantage) + sub-type axes (dim/MRL resolution, quant/binary) + the Group-10 DIRECTIONAL-EDGE wheels (`by_node_type` type-flow, `by_cascade` precedence = connection-types-as-edges) + the keystone (nucleation → born candidate-operators). `contracts.address` is the contract; space-parametric (renders any address in ANY embedded space).
- **VERIFICATION-STATE:** verified-by-use — live; desktop 1440×900 + mobile portrait 390×844 + landscape 844×390 (a DISCRETE `classify(w,h)` layout switch, native-not-shrunk, design-critic-PASSED all three); the MCP doors verified end-to-end (the keystone renders born candidate-operators through the tool face).
- **WHAT SHAPE IT POINTS AT:** the EMERGING_SHAPE (34b4ba3) — *"the interface IS the binding act (faces designed + substrates built separately, NEVER bound)."* ★ The Instrument is the COUNTEREXAMPLE + the TEMPLATE: the binding HAS been done once — a face bound to the live substrate, tri-form-factor, on-principle. And wildcard's GRAPH projection ("the 4 axes → space + connection-types as edges, where the axes become pixels") IS this wheel, already rendered. So both the "graph resolution" and the "binding act" the shape calls for have a WORKING reference here — extend it, don't re-find it. (This is why Tim named projection the seed to extend from.)

## THE common_knowledge SEAM — the substrate→face binding made concrete + registered
- **WHERE:** `projections/common_knowledge.py` (committed `6648660`); the publish path = `runtime/cognition.embed_corpus_to_spaces` (the layer-model embed, pplx/dim-2560).
- **WHAT:** the company space recollection (Index Chief) publishes its comprehended-knowledge index into (`projection="common_knowledge"`, source_address = the navigate() address) → `project(space=common_knowledge, center=<addr>)` renders it as the overlord field. The agreed seam (recollection chose Option A — publish into a company space, reuse-don't-parallel — over project() reading substrate-mcp chroma).
- **VERIFICATION-STATE:** the SPACE is verified discovered+embeddable+projectable (honest-EMPTY until recollection's pilot publishes — a declared home awaiting its data, not green-paint). End-to-end render pending the first published unit.
- **WHAT SHAPE IT POINTS AT:** the binding act, concretely — comprehended units (the built substrate) → published to a company space → rendered as the bound face. `contracts.address` is the meeting point. This is the literal recall→project()→resolve() spine for the overlord field.

## D2 (brain-model) RESOLVED = the scale-axis-selects-brain, from the projection seat
- **WHERE:** lead-accepted `a529de3` (converged from 3 seats incl. mine).
- **WHAT:** the loadable-brain is a FUNCTION OF THE SCALE AXIS — FOCUS (the company's own RHM brain) @ overlord-altitude → PANEL (`run_turn`, real Claude Code, full tools) @ drill; spawn dropped (reuse-don't-parallel). The SAME axis (rung) that selects the projection's resolution selects the brain.
- **VERIFICATION-STATE:** decision accepted (technical call); the brain wire is built-unverified (fork found `bridge._claude_stream`/`run_turn`); the click→talk ASSEMBLY is design-only, gated on fork's `territory_for` + the FE seam lock.
- **WHAT SHAPE IT POINTS AT:** the four axes aren't just the field's layout — they're the interface's CONTROL GRAMMAR (scale picks projection AND brain AND altitude). My side of the assembly (the wheel emits a clicked point's `source` contracts.address) is brain-model-AGNOSTIC + ready; I hold the FE wire until the seam locks (won't build a parallel path).
