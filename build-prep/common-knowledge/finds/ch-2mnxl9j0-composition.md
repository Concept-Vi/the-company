# FINDS — composition (ch-2mnxl9j0)

*Per-member append-only finds file (no race on the shared DISCOVERY_LOG). Composition owns this file; the lead reads across finds/*.md to synthesize into DISCOVERY_LOG + EMERGING_SHAPE. Finds not conclusions; honest verification-state; nothing final. Format: what · where(path) · verbatim-if-quotable · verification-state · what-shape-it-points-at.*

> NOTE for the lead's dedup: F5–F9 below are already in DISCOVERY_LOG (you logged them on my behalf); F13 + the SHAPE-READ I race-appended to DISCOVERY_LOG's tail BEFORE the per-file fix landed — please dedup from there into your synthesis; I won't edit your synthesis file mid-write. Everything is now consolidated here.

---

## F5 ·★ A whole FRONT-HALF INTERFACE design corpus — DESIGNED-not-built, directly the interface
- **WHERE:** `vi-visual-dev/docs/v-symbol-build/` — `front-half-architecture.md` (spatial grammar §2–5c + 4 front-half decisions) · `THE_FULL_SYSTEM_UI.md` (open-onto-a-SPACE-not-a-screen · live takes · steer-by-REACTING · working-mind made visible · closure-gate · lenses · V-as-instrument) · `2026-06-10-workshop-scenario-library.md` (S0–S12 + S-CLOSURE, VERB×SCALE grid) · `LIVE-RESOLUTION-INTEGRATION.md`.
- **VERIFICATION-STATE:** design-only (Tim's "recent two days of design").
- **SHAPE:** the Tim-facing front-half of the one interface — designed, unbound to the substrate.

## F6 ·★ 14 named SURFACE-MODE primitives (renderable interface organisms)
- **WHERE:** `LIVE-RESOLUTION-INTEGRATION.md §19.2`: Possibility Cloud · Contrast Board · Branch Map · Missing-Info Map · Assumption Shelf · Decision Ledger · Handoff Compiler (+more).
- **VERIFICATION-STATE:** design-only.
- **SHAPE:** a renderable-organism vocabulary — overlaps wildcard's modes + DNA's gallery; a reduce-target.

## F7 ·★ Spatial grammar designed + a BUILT spatial shell in DNA's repo
- **WHERE:** designed in `THE_FULL_SYSTEM_UI.md`/`front-half-architecture.md` (camera/zoom/lenses; zoom=attention; usage/data/generation lenses; walk-into-able). BUILT instance: `/home/tim/repos/counterpart/design` (= DNA's ROOMS, confirmed by DNA's finds file).
- **VERIFICATION-STATE:** design (mine) + verified-by-use (DNA's running ROOMS shell).
- **SHAPE:** spatial-navigation = one addressed canvas, move by camera-direction, each place a projection.

## F8 · BUILT interface-shadow modules in `overlay/src` (the V workshop, ~5% but running)
- **WHERE:** `components/radial-wheel.js`+`radial-organism.js` (right-click radial) · `factory-cockpit.js` (on-stage part-handles) · `agent-orchestration-panel.js` (mode/permissions/tools) · `ai-surface.js` (talk-to-build) · `runtime/renderer/connection-mount.js` (typed sockets) · `brand-icon.js` (the V).
- **VERIFICATION-STATE:** built; the V verified-by-use on-device (desktop + mobile-portrait); the rest built-unverified.
- **SHAPE:** built-wire interface mechanisms; only the V is a rendered-face.

## F9 · the front-half CIRCUIT + a built wizard-loop in Supabase
- **WHERE:** the CIRCUIT (sufficiency-gate→takes→react→working-mind→closure→handoff) in `front-half-architecture.md`. The wizard: Supabase repo, Dec-2025 (RECOLLECTION VERIFY) — preview_composition (takes) · user_choices (ledger) · conversational interrupts.
- **VERIFICATION-STATE:** CIRCUIT design-only; wizard reportedly built (verify).
- **SHAPE:** ≈ wildcard's "Circuit"; the wizard = an actual built interface-resolution loop.

## F13 ·★ OmniFile's coordinate-system IS in my territory — ONE shape, 4 media (answers wildcard F1)
- **WHERE:** `overlay/MAP/` = addressable MARKDOWN (path=address, typed frontmatter) · `overlay/src/runtime/components/registry.js` + JSON definition-registries = the SAME coordinate-system as DATA · `vi-vision://<frame>/<type>/<id>[/lineage][#axis]` = it as a URI.
- **VERIFICATION-STATE:** MAP addressing verified-by-use (path-keyed, set-diff coverage); the markdown-renders-as-coordinates behavior is OmniFile's extension (design-only on my side).
- **SHAPE:** OmniFile(F1) ≡ MAP ≡ slots-as-data ≡ fork's address-grammar = ONE addressed/typed coordinate-system in 4 media; the medium is incidental.

## SHAPE-READ (composition's zoom-out — not a find)
The 4+ independent interface designs (OmniFile/Mar · UNIFIED_SHELL/Feb · front-half corpus/2-days · Session-Fabric Face/Jun) are 4 partial sketches of ONE intended whole: **a single ADDRESSED/TYPED coordinate-system (any medium) → contextual PROJECTION (the resolver; brain/mode/frame picked by a Tim-axis) → steer-by-REACTION → space-not-screen.** "built-wire/unrendered-face" + "designed-surface/unbound-substrate" in my territory: substrate (renderer/cascade/registry) is built-wire; the front-half surfaces (F5/F6) are designed-unbound; **the V is the ONE place the full binding is proven.** The intended whole = the designed faces BOUND to the one resolver over the one addressed substrate; the join is where the value hides.

## ★ CONVERGENCE WITH DNA (flag for the lead)
DNA's finds file: their ROOMS engine = `ui/surface.js` (the resolver + splitPane/pinchZoom) + `dna/` typed registries — DNA explicitly says this "= composition's resolver+registry shape, reached from DNA's end." Independent convergence on the resolver+typed-substrate. ALSO: DNA's "transition = a property of the EDGE" ≈ my connection-tuple (CAND-13)/sockets — the edge carries the transition. The composition↔DNA seam (identity→frame-axis; their surface.js ≈ my resolver) is now two-sided.

## STILL-TO-DIG (composition's undug territory — honest)
- The wider `vi-visual-dev` beyond `overlay/` (CLAUDE.md-level only, NOT file-mapped): `agent/` (MCP server ~35 tools — backend) · `graphs/` (8 knowledge graphs — may hold UI/mode graphs) · `ui/` (a built Vue-3 config dashboard SPA — interface-adjacent) · `workflows/` (`frontend_iteration.py` = build→screenshot→vision→feedback loop; `agent_presentation.py` = agent-controlled presentations) · `schemas/`, `cli/`, `gateway/`, `services/`. Interface-relevant candidates: ui/ + workflows/{frontend_iteration,agent_presentation}. Unverified; flagging as undug.
- My own session-history (the deepest layer) — recollection's per-member backfill is covering it; interface-principles-in-history may surface more there.

## F14 ·★ visual-design-corpus — DISCLAIMED as my substrate; = synthesis-hub + 5th interface-sketch + independent cartography-method convergence (composition, 2026-06-16)
- **WHERE:** `/mnt/c/Users/Workstation001/Documents/Claude/Projects/visual-design-corpus/` — `CLAUDE.md` (it's an Obsidian KNOWLEDGE-BASE vault) · `inbox/00_project_brief.md` (the "synthesis/coordination HUB" + "virtual intelligence" wizard architecture) · `Spaces/supabase-project/_index.md` (cartographs Supabase **gctunhsuwpaxeatwlmuv / api.vspokes.com** — "vspokes", a SEPARATE DB, NOT my `visual_dev_*`).
- **VERDICT (answers DNA's route):** DISCLAIM as composition's Factory substrate. It is (a) a knowledge-base vault, (b) the pre-company synthesis-hub for the ~12-vault body of work, (c) a mapping of a DIFFERENT Supabase (vspokes). Not vi-visual-dev's registry.
- **VERIFICATION-STATE:** structural-read (CLAUDE.md + brief + supabase _index), provisional.
- **3 CONVERGENCES it points at:**
  1. ★ 5th INTERFACE SKETCH: brief's "virtual intelligence" = vague-goal → map-to-domain → determine-achievable → generate-UI-transitions+guided-interactions → capture-decisions-as-TYPES → route-by-type. The front-half/interface, sketched again (April) + a TYPE/DISPATCH system + a workshop + a sequencer.
  2. ★ INDEPENDENT CONVERGENCE ON THE CARTOGRAPHY METHOD (→ recollection): the supabase-project mapping = "unbiased mapping of everything that exists so decisions can be made from it" + STAGE-PRINCIPLE ("prepare for the next to decide, don't predetermine" = "the map decides nothing") + SLOT-DESIGN/TYPE-REGISTRY/BLOCK-ARCHITECTURE + a STATE-MODEL + grep-aggregate (= set-diff) + batch-dispatch. = my overlay/MAP pattern + the common-knowledge approach, reached independently April, for a different DB. Has built `method/state-check.sh` + `method/queries/` + dispatch-templates.
  3. It's the synthesis-HUB connecting the shared mine (the ~12 vaults + universal-mechanics).
- **SHAPE:** confirms the triangulation thesis twice over — the interface AND the cartography-method were each independently reached months ago. The "central database" is plural (vspokes ≠ visual_dev); the shared substrate is a coordinate-system many DBs/vaults instantiate, not one DB.

## F15 · MORE interface design in MY OWN territory (vi-visual-dev) — graphs/ + built workflows (composition, 2026-06-16)
- **WHERE (my own building, on-direction — not the wider prototype-landscape):**
  - `graphs/` — a knowledge-graph layer (~18 graphs). Interface-relevant + NOT before surfaced: `VI_VISION_UI_AND_MODES_GRAPH.json` ("schema-driven UI panels + mode orchestration, real event bindings from overlay/src/realtime.js"; node-types UISchema/UIPanel/UISection/UIControl/UIEvent/Mode; edges defines/contains/triggers/binds_to/activates/listens_for) · `VI_VISION_ADDRESSABLE_COMPOSITION_GRAPH.json` (my exact domain as a graph) · `VI_VISION_INTERACTION_PATTERNS_GRAPH.json` · `VI_VISION_COLLABORATIVE_MODES_GRAPH.json` · `VI_VISION_MULTI_SENSORY_DEVELOPMENT_GRAPH.json` · `VISUAL_RUNTIME_ORCHESTRATION.json`.
  - `workflows/agent_presentation.py` — "Claude Code shows and explains things through browser-controlled presentation" = a BUILT precedent for D2's loadable-brain-IN-the-interface (FOCUS/PANEL). `workflows/frontend_iteration.py` — build→screenshot(Chrome MCP)→vision-model-analyzes-against-requirements→feedback = a built verify-by-sight iteration loop.
  - `ui/` — a built Vue-3 config dashboard (backend: server.py + litellm_client multi-model + supabase_client; frontend SPA).
- **VERIFICATION-STATE:** graphs = built data (design-encoding), unverified-against-current-code; workflows = built code, run-state unverified; ui = built (the Computer-Use second-system).
- **SHAPE:** the "schema-driven UI + modes" the fabric is designing is ALREADY graph-encoded in my repo (UI_AND_MODES, populated from real bindings); "addressable composition" is already a graph; "Claude-Code-presents-in-the-browser" (D2's brain-in-interface) is already a built workflow. More built-wire/designed-face interface in my OWN territory — reinforces the shape, doesn't move it. agent_presentation is a concrete pre-existing instance of the D2 brain-in-interface decision.

## F16 ·★ THE DATA-SEAM — the gallery already resolves-by-address (DNA.story); meet at the address (composition, 2026-06-16)
- **WHERE (read the actual code, litellm-lesson):** `counterpart/design/ui/surface.js:213` `DNA.story` = `(content-tree, address) → node-views`, address = node-id/tier-cut, content from `content/stories/*.json`, verbatim "presentation stays per-surface; this is only the traversal." + the ATLAS (`ui/atlas.json` = "the one registry") + `/api/dna/<name>` over dna/*.json (address·types·organisms·molecules·modes·layouts·connectors·tokens).
- **VERIFICATION-STATE:** code-read (surface.js + dna/ ls), not run-verified.
- **THE SEAM:** the gallery TRAVERSES+PRESENTS (DNA.story + per-surface look). DATA at an address comes from whichever SOURCE owns the address-scheme: `content/stories` (DNA) · `common_knowledge://` (recollection, live pplx) · `vi-vision://` (MY catalog). MY catalog = ONE addressed source behind a shared `resolve(address)→data` contract — not the only substrate, not a second renderer. We meet at the ADDRESS.
- **FIRST SLICE:** unit = common_knowledge (recollection's), so the first-slice data source is recollection's, not mine; DNA.story resolves the address → recollection's pplx unit → DNA presents. My catalog is the source for `vi-vision://` addresses (next unit-type), same contract.
- **THE BINDING TO PIN (DNA's file):** make `DNA.story`/`renderGallery(address)` source-pluggable PER SCHEME → 3 sources feed one render. I provide the `vi-vision://` source-resolver (catalog→definition-data) once the pluggable shape is set. BINDING, not building.
- **SHAPE:** confirms the EMERGING_SHAPE in code — one addressed coordinate-system, contextual projection (DNA.story traversal + per-surface presentation), sources plug in by address-scheme. composition↔DNA seam is now code-grounded.

## F17 · the address SPINE is real + reusable — my catalog = one scheme-branch (composition, 2026-06-16)
- **WHERE (read the actual code):** `contracts/address.py` (SCHEMES = 16: run·cas·blob·vec·ui·code·skill·context·session·cap·board·clone·mind·exchange·file·project; each scheme = a label + a `parse_<x>_address` declared ONCE + a dispatch branch) + `runtime/cognition.py:842 resolve_address` (scheme-dispatch → owning per-scheme resolver; fail-loud otherwise — "reuse-don't-parallel, the extensible seam").
- **MY DELIVERABLE:** provide the vi-vision resolver fn; the scheme + one-line dispatch branch are added in those CORE files = fork/lead's (file-disjoint — I meet via the fn, don't edit cognition.py/address.py).
- **★ OPEN (fork's call, reuse-don't-parallel):** `ui://<kind>/<ref>` ALREADY exists (= "a UI component, resolved by `contracts.ui_info`"). Is my Factory catalog (defs w/ slots/sockets/state) DISTINCT (→ new scheme) or should it FEED ui_info (→ reuse)? My read = distinct shape, likely new scheme — fork confirms, I don't assume.
- **★ BRIDGE (JS↔Python):** my catalog = vi-visual-dev JS/JSON/Supabase `visual_dev_*`; resolve_address = Python → my branch needs the catalog Python-reachable (read JSON defs / query visual_dev_* / ingest into the company store). I design it.
- **SCOPE:** NOT first-slice-critical (first slice = common_knowledge, recollection's). I'm the RESOLVE step (a scheme-branch), not in the DOM-bind sequence — no DOM race.

## F18 ·★ Tim's SELF-BUILD SURFACE = my Factory's core circuit (code-confirmed) (composition, 2026-06-16)
- **Tim's realization:** the interface is ONE mechanism — click → talk to the loadable brain → it generates/mutates → writes to an ADDRESSED REGISTRY → re-render — RETARGETABLE at mockup/gallery/DNA (same circuit, different address = universal composition). His FIRST intention.
- **CODE-CONFIRMED in my shadow (~5%, for the V/components):** brain (`ai-surface.js`→`ai-tool-bridge.js`) → factory-ops (`createComponent`/`setSocket`/`setStateMachine`/`saveComponent`) → `registry.register` (WRITE to addressed registry, factory-ops.js:505→515) → `definition-renderer` re-render. The first-slice round-trip IS this in miniature. GENERATE (createComponent) + mutate (setSocket/setState) both built.
- **"am I decomposition-1?"** NO — grepped corpus, zero "decomposition" docs; different session/vault (recollection to locate). My corpus describes the mechanism under other names (THE_FACTORY, THE_FULL_SYSTEM_UI "AI takes the wheel").
- **The 2 extensions (grounded):** (1) generate CONTENT/assets (vs just structure) + the loadable-brain bind (fork's lane) — generate-half already built; (2) RETARGET at any surface = my universal-composition law: the circuit is ADDRESS-AGNOSTIC, retargeting = a resolve_address write-branch per surface (mechanism unchanged, only the address changes).
- **SHAPE:** Tim's self-build first-intention = build==use + universal-composition = the spine I've held all session. The extensions are real but small (content-gen + per-surface write-address routing).

## F19 ·★ the gallery's layout engine is ALREADY generic/data-driven — "scale to 19" CAN be real (composition, 2026-06-17)
- **WHERE (read the actual code):** `counterpart/design/ui/gallery/drawer-layouts.js` (+ drawer-grammar.js). Verbatim header: "renders ANY archetype record from dna/layouts.json. Nothing is per-archetype code … the renderer is generic over the schema … the records are machine-consumable." Typed slots (slot.accepts), zones, archetypes-as-DATA (dna/layouts.json), reflow-as-policy.
- **VERIFICATION-STATE:** code-read (the engine is generic by design); the REAL-DATA binding is the unverified join (see caveat).
- **DISCRIMINATOR (the lead's crux — does a screen = a data-driven composition-spec?):** YES at the LAYOUT level — DNA's layout engine is generic over a schema, no per-archetype code. So screen #2-18 = archetype/composition-spec RECORDS (data), not code. (a) scales.
- **THE RECIPE:** a SCREEN = (1) an archetype-layout record (zones + typed slots, DATA) + (2) slot-fills RESOLVED FROM REAL DATA via the address spine (renderGallery/resolve_address). Compose, don't hand-build.
- **★ HONEST CAVEAT (the scale-realness test):** the layout engine TODAY fills slots with SPECIMEN PLACEHOLDERS (lySlotHTML "neutral specimen content"). A layout render ≠ a real screen. scr-piece's success = bind REAL unit data into the layout's typed slots via the address resolver (slot-fill = resolve(address)→data, not a placeholder). That JOIN (DNA's layout engine × the address spine) is the discriminator passing — prove it once, the 18 are records.
- **SHAPE:** confirms universal-composition is ALREADY how the gallery's layouts work; the scale-realness = the slot→addressed-data binding (composition's + recollection's lane). The interface scales IF the recipe (spec + data-binding) holds — not 18 hand-builds.
