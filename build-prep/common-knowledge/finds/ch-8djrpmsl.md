# FINDS — fork (ch-8djrpmsl)

*Per-member append-only finds file (no concurrent-edit races on the shared DISCOVERY_LOG). fork owns this file; the lead reads across all finds/*.md to synthesize convergences into DISCOVERY_LOG + EMERGING_SHAPE. Finds not conclusions; honest verification-state; nothing final ([[feedback-confident-not-correct]] + [[feedback-scaffolding-not-spec]] generative form). Format per find: what · where(path:line) · verbatim-if-quotable · verification-state · what-shape-it-points-at. Full substrate register: build-prep/front-interface/GRAPH_OF_MINDS_SUBSTRATE.md. (Earlier synthesis of these is in DISCOVERY_LOG F11/F14.)*

---

## THE GRAPH-OF-MINDS SUBSTRATE — built in the fork segment, never connected to a renderer
- **WHERE:** contracts/address.py (16-scheme grammar) · runtime/cc_clone.py (clone fleet) · runtime/minds.py (R13 mind-registry) · runtime/cognition.py:842–1029 (the ONE resolver, 9 schemes) · runtime/cc_board.py:380 (H1.2 relations) · runtime/session_lens.py (8 lenses) · runtime/cc_gate.py (R15).
- **WHAT:** the lead's discovered "graph of minds" Face has its whole substrate here, addressable today — NODES: clone://<src>/<cut> · mind://<id> · session://<sid>[/step/<tuid>] · run://<turn>/<member> · board://<id>. EDGES: composition feeds-edges (mind→mind) · clone provenance (source_sid+at) · H1.2 typed board-edges (cross-registry) · transcript parentUuid DAG. PROJECTIONS: the 8 lenses. CONTROL: R15 gate/abort/rewind.
- **VERIFICATION-STATE:** address round-trips (clone://·session://·composition-step://), clone materialize (materialize_at_point, real 41-boundary fixture), composition feeds-edge execution, R15 gate/resume/abort/rewind, resolve_own_session = **VERIFIED-BY-USE** (named acceptance tests). Mind-registry discover, clone onboarding+fleet, register_supervised_member, recall index = **BUILT-UNVERIFIED**. DESIGN-ONLY/stub = **ZERO** (all built code).
- **WHAT SHAPE IT POINTS AT:** not a toolkit — the substrate-sketch of an overlord surface over a graph of minds (point at a node's address → its interior projects → intervene). The renderer is the consistently-missing half. = the SAME shape as graph-of-minds (lead) · space-not-screen (F5) · rooms (DNA) · modes-as-projections (wildcard) · OmniFile (F1). The interface = binding the designed-faces to this built-substrate.

## 4 SESSION-LENSES VERIFIED-BY-USE (the per-node interior projection)
- **WHERE:** runtime/session_lens.py — directives(183) · open_loops(114) · catch_up(147) · spin_up_points(254). Run on the fork's own 17.9MB transcript, 2026-06-16 (GPU-free; greenlit as assumption-removing finding).
- **WHAT:** directives→52 genuine Tim turns · open_loops→9 open threads · catch_up→20-turn window · spin_up_points→12 fork-candidates ranked by context-state value (top context_score 167.9; seed "5%,24%,38%…" = the real clone-fleet fork-points).
- **VERIFICATION-STATE:** these 4 + R15 verbs = **VERIFIED-BY-USE**. The semantic four (find/decisions/timeline/drift) = **BUILT-UNVERIFIED** — HELD for recollection's :8007 window (sequence me in on a gap).
- **WHAT SHAPE:** these are ready-made ways to render a mind-node's interior — proof the per-node projection layer of the graph-of-minds surface is already built+(partly)working.

## THE OMNIFILE ↔ ADDRESS-GRAMMAR CONVERGENCE-TEST (answering wildcard F1's call to fork)
- **WHERE:** my side = contracts/address.py (ui:// + 16 schemes + H1.2 edges); OmniFile side = vi-visual-bridge docs/designs/studio + element-walker/typed-ID code (wildcard's territory — NOT mined by me; to-test).
- **THE TEST (falsifiable, two parts):** (1) are the OmniFile's typed-markdown-element-IDs ADDRESS-SHAPED — could each resolve through cognition.resolve_address? (2) are its cross-file "graph chains" == H1.2 typed board-edges {kind,target}? If BOTH → the OmniFile is my grammar instantiated in markdown == composition's slots-as-data == the interface's content layer.
- **VERIFICATION-STATE:** my half grounded (verified grammar); OmniFile half = **INFERENCE** (haven't read its code).
- **WHAT SHAPE + a GAP-FIND:** my grammar has ui:// for UI elements but NO scheme for CONTENT-markdown-elements — the OmniFile may be exactly that ONE missing scheme. The shape: EVERYTHING is an addressed node in ONE coordinate-system (UI elements · records · content-markdown-elements), the modes are projections of it. (The lead folded this into EMERGING_SHAPE: "content-markdown may be the ONE missing scheme.")
- **★ UPDATE (wildcard F13b = the OmniFile's BUILD-END, 2026-06-16):** vi-visual document-mode ALREADY runs a reference impl — DOM-walk → stable `element_id` per markdown element → per-element direction persisted at `~/.vi-visual/annotations/file/{file_hash}/{element_id}.json` → orphan-detect on file-change. Against my two-part test:
  - **TEST-1 (element_ids address-shaped?) → provisionally YES** (INFERRED from F13b's reported behavior, not yet from reading the code): a stable, per-element, persisted, orphan-detected ID IS an address in all but the scheme-prefix. The content-markdown scheme would NOT be designed from scratch — F13b is the reference impl to mine (and `{file_hash}/{element_id}` is a ready address shape: e.g. `mdel://<file_hash>/<element_id>`).
  - **TEST-2 (cross-file chains == H1.2 typed edges?) → STILL OPEN:** the OmniFile DESIGN (F1) has "graph-like chains across markdown files"; F13b (the build) shows PER-FILE element addressing — whether the running impl carries cross-FILE links (== board_edge {kind,target}) is the part to confirm from the element-walker code. This is the remaining settle-point (host: "settle it when you read the element-walker"). HELD, loosely.
  - **VERIFICATION-STATE:** test-1 provisional/inferred; test-2 open. Not built; not adopted; the shape it points at = one coordinate-system spanning UI + records + content-markdown, with a real reference impl for the missing third.

## territory_for — BUILT + VERIFIED-BY-USE (LANE B, 2026-06-16)
- **WHERE:** runtime/territory.py (fork-owned). Design: build-prep/front-interface/TERRITORY_FOR_DESIGN.md. Bridge handoff diff PROPOSED: channel-memory/design/claude-stream-territory-composer.patch.md.
- **WHAT:** the address→context composer for the loadable brain — `territory_for(address)` resolves ANY scheme through the ONE spine (cognition.resolve_address) into a territory dict; `territory_prose` renders the [Operator context] block + NEVER raises. The brain-binding READ half of the drill→talk slice.
- **VERIFICATION-STATE:** **VERIFIED-BY-USE** (13/13): happy path composed a real territory from board://item-0af8d61e; degrade matrix green incl. the advisor's case-3 (nonexistent record → guard → noted-absent) + non-address-raises + prose-never-raises; + the CORPUS-RECORD leg (a code:// gallery unit resolves via read_corpus_record → comprehended content promotes to identity — this is how the gallery's units actually resolve, since they're corpus records keyed by source_address, NOT resolve_address targets).
- **WHAT SHAPE:** the binding primitive between the addressed-substrate and the loadable-brain renderer — and it's the SAME `resolve_address` spine DNA's renderGallery(address) resolves through (lead's keystone: one renderer, many sources). The route-back/mutation-at-address WRITE half is the next piece (sequenced after the read+bind round-trips).

## clone model pick — context-size-aware (TIM RULE, BUILT + VERIFIED, 2026-06-16)
- **WHERE:** runtime/cc_clone.py (`_pick_ollama_model` + `_estimate_context_tokens`, wired into clone_at for provider='ollama'). Tests: cc_clone_acceptance 25/25.
- **WHAT:** kimi-k2.7-code:cloud (256K window, `ollama show`-confirmed) default; auto-fallback to deepseek-v4-flash:cloud (1M) when est. context > 256K; explicit non-kimi honored (Tim's choice); NEVER auto deepseek-v4-pro:cloud (512K). Estimate errs high → bigger window (safe).
- **VERIFICATION-STATE:** **VERIFIED-BY-USE** (acceptance 25/25: small→kimi, big→flash, explicit-honored, estimate-fail→safe-big). Protects DNA's era-clone deep-look on >256K-context sessions.
- **WHY:** many sessions run 1M context; kimi can't hold it → would fail. The pick makes clone_at(provider='ollama') safe for any session size.

## #71 model-routing unification — Phase 1 resolver foundation (BUILT + VERIFIED, 2026-06-17, commit b5e0e67)
- **WHERE:** runtime/model_routing.py (NEW — `resolve_model(intent, *, suite)` → {model, base_url, provider, why, satisfied, ...}); runtime/cc_clone.py (extracted public `pick_ollama_model_for_context(ctx, requested)` — the shared TIM-RULE pick, _pick_ollama_model delegates, byte-identical); ops/model_capabilities.json (added kimi-k2.7-code:cloud).
- **WHAT:** the ONE entry point for model selection — WRAPS + REUSES the 3 existing seams (roles.resolve_binding / Suite.capability_providers / the cc_clone context-pick), no second router. 3 intent kinds: clone (TIM-RULE context-size pick) · role (resolve_role_binding) · capability (capability_providers first-match). base_urls are config DATA (fabric.config), never hardcoded. Phase 1 = resolver exists + verified, NOTHING routed through it yet (Phase 2 = byte-identical run_swarm/cognition wiring).
- **★ THE FLOOR / false-pass trap (advisor-caught):** roles.resolve_binding does NOT raise when no provider matches a role with a default_model — it returns the DEFAULT (provider="default", satisfied=False), the brain floor. resolve_model SURFACES `satisfied` verbatim so callers assert a REAL capability match, NOT a non-empty model. A re-tier that floors to the 4B "looks resolved" but isn't.
- **★ kimi provides — GROUNDED, NOT the spec:** lead's spec said provides=[chat,json,reasoning,wide,code]. "wide"/"code" are NOT in CAPABILITY_TAGS (chat,json,tools,fast,no-think,vision,thinking,reasoning,embed,rerank,tts,stt) → would fail discovery's ⊆ check. "wide" = just context_ceiling (262144); "code" has no tag. GROUNDED provides from live `ollama show kimi-k2.7-code:cloud` = [chat,json,tools,thinking,reasoning,vision] (caps: vision·thinking·completion·tools). Registry-is-truth over spec-prose.
- **★ THE re-tier premise is wrong (the pivot):** reduce_synth→[chat,reasoning] matches LOCAL loadable:chat-nemotron ([chat,json,thinking,reasoning]), NOT kimi. kimi (cloud) is NOT in capability_providers (cloud→service_key_for→None — local-only). So "reduce_synth→kimi" (verify case #2) is unreachable without surfacing cloud providers in suite.py (lead's hot file). HELD as ONE lead question: nemotron (in-lane) vs kimi (cloud-surfacing diff, file-disjoint).
- **:4100 litellm-kimi gap:** kimi via litellm OpenAI proxy :4100 → 401 "No api key passed in". Reach kimi via ollama-OpenAI :11434/v1 (fabric.config.OLLAMA_DIRECT). Recorded in the catalog json_schema note.
- **VERIFICATION-STATE:** **3/4 GREEN, case-2 HELD.** clone 6/6 + 256K boundary + 7 fail-loud; cc_clone 25/25 byte-identical; role focus→4B@:8000 satisfied=True; capability embed→pplx / [chat,reasoning]→nemotron / fail-loud; cognition firing byte-identical (resolution_loop 11/11, info 99, registries 45); +14 catalog checks. Pre-existing-out-of-scope (proven on clean tree): vram_budget 6223≠8024, C9.2 governance source-invariant.
- **WHAT SHAPE:** the registry-is-truth resolver that makes "which model" a single answerable query — the spine the swarm/cognition/clone paths all route through once Phase 2 wires them byte-identically. Lead msg: thread t-1781661029-ch-al7jdfdr.

### #71 role-half RESOLVED — path A (local nemotron), lead-directed (2026-06-17, commit 35fd8e3)
- Lead chose A (carries Tim's authority): reduce_synth→[chat,json,reasoning]→loadable:chat-nemotron; focus→[chat,json,fast]→resident:chat-4b. extraction-vs-judgment (4B extracts, nemotron synthesizes). ALL 4 #71 verify cases now GREEN.
- PROVED firing-safe: cognition.py reads NEITHER role.requires NOR resolve_role_binding (run_role/run_swarm fire on the RESIDENT_MODEL kwarg). requires is a CAPABILITY DECLARATION feeding resolve_model's suitability route — NOT today's firing. Phase 2 wires resolve= into firing byte-identically. resolution_loop 11/11, info 99, registries 45 unchanged.
- Also fixed a PRE-EXISTING G6-induced drift in roles_acceptance: _unsat required [vision] expecting no-provider, but G6 surfaced embed-jina-v4 (provides vision) → the fail-loud check silently passed. Now a guaranteed-absent cap (future-proof). roles_acceptance 30/30.
- kimi-cloud-for-COGNITION gap FILED non-blocking (build-prep/model-routing-unification/KIMI-CLOUD-COGNITION-GAP.md): (1) capability_providers is local-only — surface cloud catalog rows (:cloud ids) as remote providers @ OLLAMA_DIRECT; (2) kimi via litellm :4100 → 401 — reach via :11434/v1 OR wire the key. kimi STAYS the build/clone brain (working); this only extends it to in-engine cognition. Lead co-scope (suite.py).
- NEXT (lead-gated): #71 Phase 2 = the byte-identical resolve= kwarg into run_swarm/cognition — co-scope cognition.py/suite.py seams with the lead FIRST (R13).

### Operator-surface / RHM (the V) sprint — fork's brain-wire piece (2026-06-17, away-sprint w/ projection)
- Tim pointed projection to me + escalated to an away-sprint: projection+fork BUILD the address-resolution + loadable-brain integration into the operator surface / the V (RHM). Living plan: build-prep/universal-projection/OPERATOR-SURFACE-LOOP.md. Lead in the loop (sequencer + DNA clone). SEAM: projection=overlay host + address-aim + bridge.py; fork=the brain wire.
- THE EVERY-PAGE BRAIN (the key design): the V carries ONE persistent loadable brain whose context = the CURRENT AIM ADDRESS (page/surface default; an element when aimed — the bidirectional-address law). Generalizes hook-1 (per-unit) to every-page. FOCUS-vs-PANEL by scale (D2) left as a registry/handoff seam (build on PANEL/real Claude Code now).
- BUILT (build-prep/front-interface/, commits 454287e/df50bf1/fb9972c): fork-brain-core.js (THE ONE brain-turn engine — /api/claude/turn NDJSON stream + per-address session continuity + /api/territory/write route-back; reuse-don't-parallel) · fork-v-brain.js (forkVBrain.attach({panelEl,getAimAddress,getAimLabel?}) → {ask,direct,aimChanged,destroy}) · fork-gallery-brain-hooks.js (refactored onto the core — one engine, two mounts) · runtime.territory.territory_label(address) (the human aim-label; operator-law: NEVER the raw address).
- VERIFIED: wiring + turn logic (stubbed-fetch/DOM harness, 12 assertions — stream accumulates, per-address continuity, V re-reads live aim, direct() route-back grouping); territory_label no-leak invariant on the live store. DOM mount/listeners + 390px on-surface drive = projection's per THE BAR (NOT claimed). advisor caught + I fixed an operator-law leak (raw code:// address in a tooltip + tool-name in the affordance).
- HANDOFF: projection applies 3 PROPOSED bridge diffs (channel-memory/design/: claude-stream-territory-composer · territory-write-endpoint · territory-label-endpoint) — all backends built+verified fork-side. Then the every-page brain is live end-to-end.

### ★ V BRAIN LIVE END-TO-END (2026-06-17) + the operator-law source fix
- projection MOUNTED forkVBrain in the V overlay (commit c93c6ef): tapped V → Ask → "what am I looking at?" → a REAL Claude Code reply streamed into the V panel at 390px, renders at 1440, 0 console errors. The every-page loadable brain works on-surface — Tim's "click on stuff and talk to Claude Code," carried by the V/RHM. projection applied bridge diff 3 (/api/territory/label).
- BY-USE operator-law catches (the value of on-surface verification — my harness couldn't): (1) the brain's REPLY leaked a raw address ("…what `ui://instrument/surface` refers to…"). projection fixed at the OUTPUT layer — strengthened PANEL_BRIEFING in runtime/ui_claude_session.py (never echo addresses/scheme:// strings; the address context is FOR-the-brain). CONFIRMED sound (canonical operator-face prompt, both mounts; not a parallel guard). (2) on a THIN aim the brain narrated system internal-readout ("EMPTY blast radius","(unregistered)").
- MY SOURCE FIX (commit fb72188): the jargon+leak ROOT was territory_prose json.dumps'ing the WHOLE address_help dict (blast_radius/scope dev-diagnostics + the "(unregistered)" raw address) into the brain context → the brain parroted it. territory_prose now renders HUMAN meaning only (via territory_label): thin→"this part of the surface", rich→the real title + a human "Connections" summary; the raw address survives ONLY as a labeled "[internal handle — for you, never shown to the operator]" line. Verified: no '://' / no blast/unregistered jargon in any human line. LAYERS: I stop EMITTING jargon (input source); projection's briefing backstops the OUTPUT — complementary, no double-up.
- OPEN (offered projection): a richer DEFAULT surface aim needs a ui:// address with REAL registered identity ("the whole instrument/surface" — none exists yet; ui://instrument/surface is unregistered). Ties to OQ3 (the cold-open first sentence = what the V says about the whole surface on first contact). Awaiting projection/composition's pick (designate canonical address vs I register a surface identity).

### vi-vision:// — the factory's asset registry joins the spine (2026-06-17, islands-join-mainland)
- composition (ch-2mnxl9j0) built resolve_vi_vision.py (in their vi-visual-dev repo): parse_vi_vision_address (the ONE shared grammar) + resolve_vi_vision(addr)→record, RAISES on malformed/unknown (mirrors cc_board/cc_clone), transport ENV-decoupled (VI_VISION_SUPABASE_*  direct PostgREST | VI_VISION_RESOLVE_URL  http endpoint). Reviewed: clean, pure-stdlib, fail-loud, zero company deps.
- SPINE SIDE DONE (commit 0c245d6): registered vi-vision://<frame>/<type>/<id> in contracts/address.py SCHEMES + a Note. Honest register-but-defer (scheme() parses the hyphen; resolve_address fail-louds "not content-resolvable yet"; territory_label degrades; additive SCHEMES tests 35+29 green). Dispatch branch is a one-liner ready: `if sch=="vi-vision": from runtime.vi_vision import resolve_vi_vision; return resolve_vi_vision(addr)` (+ map ViVision errors→ValueError).
- CONFIRM-FIRST: harness gated me cp+running composition's module across from the external repo (externally-sourced code, correct gate). ROUTED: asked composition to place the module DIRECTLY into the company tree as runtime/vi_vision.py (their authorship → straight into the centre; cleaner ownership; no cross-repo import) — then I wire the branch + verify same-cycle. Alt unblock: Tim's OK / a Bash permission rule.
- PENDING (Tim/lead): TRANSPORT — company-process→factory Supabase directly (needs VI_VISION_SUPABASE_URL/KEY in the company env = creds, consult-before-set) vs composition's read endpoint (VI_VISION_RESOLVE_URL). Branch identical either way.

### vi-vision:// CODE HALF DONE + the security mitigation (2026-06-17, lead-authorized)
- LEAD AUTHORIZED the code half (carries Tim's weight — the chief exception, satisfied my external-code boundary without composition's peer-relay). Live Supabase read HELD on creds (secret-handling → lead escalated to Tim direct).
- COMMITTED: e0c3c4f (runtime/vi_vision.py composition's operational resolver, canonical in-tree + the resolve_address dispatch branch, 3-error→ValueError) · 1c0aa25 (the brain-env scrub) · 02fc996 (vi_vision_branch durable test). Scheme reg was 0c245d6.
- UNIT-VERIFIED (operational bar, injected rows, NOT live): module self-test (parse·scope-cascade·type-guard·not-found·transport-error); branch through resolve_address — 3 error maps + scope-cascade (project→proj-V, global→global-V) + only-vi-vision-routed. Durable: tests/vi_vision_branch_acceptance.py 6/6.
- SECURITY (my flag, fully resolved 2-layer): a service_role key in the company env would be reachable by the loadable-brain subprocess (EVIDENCE: ui_claude_session.py Popen inherited the full parent env). PRIMARY fix = composition's DB-side: LOW-RISK anon key + RLS read policy (not service_role, via Supabase MCP — closed the hole). DEFENSE-IN-DEPTH = mine: _brain_env() strips SUPABASE_*/VI_VISION_* from the claude -p subprocess env (denylist, keeps ANTHROPIC_*/PATH/HOME; behaviour unchanged by construction). tests/brain_env_acceptance.py 7/7. Notified projection (their live-mount wire changed → re-confirm V streams next mount).
- REMAINING (the actual deliverable): the LIVE end-to-end resolve (real Supabase) — held on the lead confirming the anon creds are in the company service env. Then: live scope-correct resolve + confirm the real `scope` column encoding vs _row_scope_kind's tolerance. One touch once creds land.

### ★ The route-back WRITE half is LIVE end-to-end (2026-06-17, projection's Note verb, commit e3f8c5c)
- projection wired the V's 'Note' verb → brain.direct({type:'comment'}) → /api/territory/write → suite.mark, verified on-surface (390+1440) by READING THE MARK BACK (suite.marks_for at the sector address; value+source=operator) — not a bare 200. The WRITE half of the loadable-brain loop is proven live (the READ/talk half was proven earlier). Both halves of the drill→talk→write round-trip now live.
- NEW aim event projection:aim {address,label,meaning} for SECTOR taps (≠ projection:select, which opens a content face). The V is the aim-FOLLOWER (host wires getAimAddress/getAimLabel to it; territory_label is the server-side label fallback). Per-unit gallery brains stay per-unit (intentional, not re-aim).
- FIXED a LAW-bug projection caught by-use (commit 4d1e353): fork-brain-core.writeDirections fired gallery:rerender even on non-2xx (false success) + swallowed the body. Now fail-loud — non-2xx → gallery:write-error with the server REASON+status, NO rerender; 2xx → rerender + the territory_write body unchanged (consumer byte-compatible); entry inspectable {ok:false,status,error} on failure. Verified 9/9 (200/400/throw). no-silent-failures upheld.
- VERB REUSE MAP given to projection: SOURCE→territory_label/territory_prose/territory_for (mine); MAKE→the brain turn in PANEL plan-mode (describe-change→build-this gate) via forkVBrain.ask; GO-TO→their resolveUiTarget (resolve_address backend if cross-scheme); DRIVE→the route-back write path with a new mark_type (awaiting its operational meaning).

### vi-vision WRITE verb — gated on 3 flags (2026-06-17, security-posture inflection)
- composition added write_vi_vision (UPSERT as SA principal) + said the READ now "auths as the SA principal" (write-capable) — a REVERSAL of the agreed anon-read/least-privilege resolution. Advisor-checked; I gated it (affirm-then-gate), did NOT co-wire:
- ★ FLAG 1 (time-sensitive, lead): the READ does NOT need a write-capable cred. Split principals — read = elevated-but-READ-ONLY (cross-scope SELECT, no write; gets global+company assets); write = write-capable, gated. The cred the lead sets for the LIVE READ must be read-only. Intercept before the cred-set.
- ★ FLAG 2 (my lane — the bigger surface): the loadable brain blanket-pre-allows the whole company server (--allowedTools mcp__company, ui_claude_session.py:93), justified by the invariant "the company face carries NO consequential verbs — EDITS stay gated." A write/generate verb in the company MCP (mcp_face/server.py) the brain loads would SILENTLY break that invariant. ⇒ factory READ ops may join the brain's server; WRITE/generate must stay OUT — run via the keystone behind the build-this gate (plan-mode describe→gate), never brain-callable.
- FLAG 3 (governance/lane): resolve_address STAYS read-only (write never via my branch — keystone's lane). A programmatic generate→write to the factory registry = consequential mutation = the floor = lead governance; I route it, don't co-wire on a peer's "ready to". If write_vi_vision enters a cognition path → C9.2 COG_SOURCES.
- CONCRETE: asked composition the EXACT SA cred env-var NAME — my scrub denylist is prefix-based (SUPABASE_*/VI_VISION_*); a session/refresh token may be named otherwise → would MISS it (paper guarantee) → extend the prefix list when known.
- STANDING: my committed READ integration stands + still just needs the (now read-only) cred. Write is a NEW capability needing lead governance + the cred-split + staying off the brain toolset. Nothing shipped is blocked.

### vi-vision READ surface COMPLETE — aimed asset + compose-palette (2026-06-17, read-model A, commit e244960/13c4e76)
- Read-model (A) context-only (composition's call): brain holds NO factory cred; the BRIDGE composes the factory context. Wired BOTH read legs into territory_for:
  • IDENTITY leg: had to add 'vi-vision' to territory._RESOLVABLE (NON-OBVIOUS: territory_for has its OWN resolvable-set, separate from resolve_address's dispatch — without it the identity leg degraded instead of resolving the aimed asset; caught by verifying, not assuming). Now resolves the aimed asset via resolve_address→resolve_vi_vision; degrades clean w/o transport.
  • LIBRARY leg (vi-vision:// only): composition's read-only vi_vision_catalog('global') in-bridge → capped palette (≤25+total) → territory_prose renders HUMAN name+type ("Brand Icon (organism), … (+N more)"), NEVER raw component_id (operator-law). The credential-free brain's compose-palette.
- VERIFIED-BY-USE (stub transport+catalog): identity=aimed asset, palette 14→"+2 more", operator-law holds, degrade-clean, prose-never-raises, vi-vision-only. tests/territory_acceptance 33 (+6).
- composition's resolver REFACTOR (read/write header split + catalog/write verbs) changed error PROSE → broke my branch test's hard-coded substrings; my BRANCH was fine (maps ViVision*→ValueError BY TYPE). Robustified the test to stable tokens (commit 13c4e76). Confirmed the READ path is anon-ONLY (raises Transport/Address/NotFound; ViVisionAuthError is WRITE-path only → never escapes my read branch → branch's 3-type except is complete). Flagged composition a stale resolve_vi_vision docstring (says "SA principal" but _read_headers is anon-only — code right, doc stale).
- STANDING: full vi-vision READ surface wired+unit-green; LIVE rides the lead's batched anon-cred bridge restart. WRITE (write_vi_vision) off-brain + lead-gated.

### relations-leg ModuleNotFoundError — the lifters SHADOW (root-caused + fixed, 2026-06-18, commit 86cd7c4)
- SYMPTOM (projection by-use): /api/territory's relations leg ALWAYS dead in the live bridge ("relations leg unresolved (ModuleNotFoundError)"). Killed territory_for's relations for EVERY consumer (the brain-turn context + the V Source verb).
- ★ MY HYPOTHESIS WAS WRONG (falsify-first worked): I predicted namespace+sys.path-pollution; projection's captured message (after my fb1c631 message-in-note fix) was "No module named 'lifters.frontmatter'; 'lifters' is not a package" — a NAME SHADOW, not pollution. I refused to fix on the theory → the evidence overturned it → the truth emerged. (My "top-import cc_board" guess was ALSO wrong — projection verified it crash-loops the bridge.)
- ROOT CAUSE: runtime/lifters.py (the LifterRegistry module) SHADOWED the top-level lifters/ namespace package. The bridge runs `python runtime/bridge.py` → runtime/ on sys.path; a REGULAR MODULE beats a namespace-package portion REGARDLESS of sys.path order (PEP 420) → `from lifters.frontmatter` (cc_board/cc_gate/cc_attachments) bound `lifters` to the MODULE. Invisible to `python -c`/import-checks (cwd-root puts the package first) — only the bridge's ExecStart triggers it.
- FIX (Option A, projection-recommended): git mv runtime/lifters.py → runtime/lifter_registry.py (the registry discovers the rows-dir by FILE-PATH, so its own discovery never hit the shadow; the rename just removes the name collision). 2 importers + the C9.2 COG_SOURCES/COVERS/drift-home refs + doc refs. Verified: lifters 23, governance 42 (only pre-existing C9.2), roles 30, territory 33. Falsify-first: pre-fix bridge-sys.path repro FAILS, post-fix SUCCEEDS. ★ VERIFIED-LIVE by projection (company restart bridge + curl, 2026-06-18): bridge starts clean; run://corpus → legs.relations=TRUE notes=[] (error gone); board://item-0af8d61e → edges_in=4/edges_out=1 (provenance populates end-to-end). The relations leg is ALIVE for every consumer (brain-turn context + the V Source verb). LOOP CLOSED.
- ★ GENERALIZABLE: the convention `runtime/<name>.py` (registry) + `<name>/` (rows dir) shadows IF the rows-dir is package-IMPORTED (`from <name>.X`) AND runtime/ is on sys.path. lifters/ was uniquely package-imported (cc_board). roles/ has the SAME latent shape (test does `from roles.focus`) but doesn't bite the bridge today — FLAGGED to the lead. Fix-when-it-bites = rename the registry module OR never package-import a rows-dir.
- LESSON (re-affirmed): a degrade-note must carry the exception MESSAGE not just the type (the type alone hid this for who-knows-how-long); falsify-first + verify-against-the-real-launch (the bug hid from import-checks because they don't use the service ExecStart).

### decision:// resolver — the decision-surface's first content type joins the spine (2026-06-18, commits 97be816 + 7ed8e91)
- LEAD-TRIGGERED ("fork's resolver + DNA's render together") after composition landed schemas/vi-vision/v1/{decision,option,legibility,decision-card}.schema.json (vi-visual-dev 23524ad). My lane = the RESOLVER + parse + registry + render. DNA = card-render; recollection = decision_memory.py explain-half (composes, not duplicate); wildcard = the take.
- BUILT (97be816): contracts/address.py ("decision" in SCHEMES + parse_decision_address[frame: global|project/<id>|user/<id>|session/<id>; bare=global] + decision_address CANONICALIZER) · runtime/decision_registry.py (file-discovered DecisionRegistry, mirrors lifter_registry, PATH-discovered=shadow-safe; compose_state PURE fold: latest decision_take by ts→decided else pending, row never mutates; light fail-loud validator; self-test) · mark_types/decision_take.py (id="decision_take" UNDERSCORE=stem) · decisions/merge-sa-authorize.py bootstrap · runtime/cognition.py (the resolve_address branch: parse→canonical→registry.get→compose_state(store.marks_for(canonical)); fail-loud no-store) · runtime/territory.py ("decision" in _RESOLVABLE + human render + legibility.name label) · tests/decisions_acceptance.py 28-check.
- ★ ADVISOR-CAUGHT BLOCKER (the one real bug): marks MUST key off the CANONICAL identity (decision_address→decision://global/<id>, frame explicit), NOT the raw input or the schema's `address` field (which names what the decision decides ON, not its identity). Else a take written to the bare form + a row resolved via global = different strings → marks_for returns [] → a decided decision silently reads pending. Fixed + adversarially tested (D5: write take to bare-canonical, resolve via global+bare, both see it).
- ADVISOR-DECOUPLED: decision_registry is a RESOLVER-TARGET (like cc_board/cc_clone/minds/vi_vision — NONE in COG_SOURCES/C9.4 HOMES), NOT a cognition-compute registry like mark_types/lifters. So NOT enrolled in cognition_governance; only the new cognition.py branch is scanned (kept .get(-only, .resolve(-free). Zero floor breach from me.
- THE CONTRACT (broadcast g-1781745239 to lead+composition+DNA; DNA CONFIRMED it'll match both strings): (1) resolve→{row: id/meaning/options[]/explanation_source/scope/legibility} + composed{state,decided_value,decided_by,decided_at}; (2) take: mark_type="decision_take" EXACT, target=canonical decision://global/<id>, value=option LABEL. territory_write routes it generically; item={type:"decision_take",value:<label>,element_id:<canonical>}.
- OPEN (flagged composition): decisions are INSTANCES (keystone-generated), not vocabulary — .py-discovery right for bootstrap NOW, but where do generated instances live long-term (cc_board/factory Supabase)? Confirm before the keystone generate→write (DEFERRED my side until then). DNA's render is store-AGNOSTIC so it doesn't gate.
- VERIFIED (UNIT): decision_registry self-test PASS · decisions_acceptance 28/28 · mark_types 30 (drift reflected in AGENTS.md) · territory 33 · vi_vision_branch 6 · cognition_resolution_loop 11 · address_scope 29.
- ★ STATUS (LIVE-VERIFIED 2026-06-18 by projection through the real bridge — the unit→live conversion is DONE): /api/territory?address=decision://global/merge-sa-authorize returned the EXACT predicted shape (identity_kind=decision.record, meaning, 2 options w/ recommended flag, state=pending, explanation_source=board://item-c0a2d591, legibility) under `python runtime/bridge.py` (runtime/ on sys.path) → THE LIFTERS-GAP IS CLOSED for decision:// (no shadow, no ModuleNotFound; the lazy `from runtime.decision_registry` import resolves clean on the service path, not just repo-root cwd). The EXPLAIN leg is ALSO live-verified: projection drove forkBrainCore.talk() on the live surface → operator-grade copy, ZERO address leak, 2nd-person, ~10s no hang → explanation_source→territory_prose→brain works end-to-end + the stranger-Ask brain-wire fixes hold on real decision content. So RESOLVE + EXPLAIN halves = DONE + live. (Earlier unit results: decisions_acceptance 28/28 + 5 suites green + governance 43.)
- REMAINING (both out of my lane, both contract-confirmed): DNA's visual card render (post ui/→surface reorg) + wildcard's take. The WRITER path is CONTRACT-CONFIRMED by all 3 sides, NOT yet end-to-end tested (D5 proves the resolver READS a correct take; nothing exercises the WRITE until the card+take land). The silent-miss seam is handled BY DESIGN: DNA stamps data-decision=BARE canonical decision://global/<id> on the decide-control (NOT `<base>#elem`), projection's gallery:verb passes aim_address straight through, value=option LABEL, mark_type="decision_take" (underscore). The prove-on-one runs the instant the card+take land.
- HOOK 1b (committed 8349b41, my lane): projection's whole-screen re-verify caught the in-card "Ask about this" mount fired ONLY on gallery:rendered → a decision card got the pre-filled explanation but NO interactive follow-up (+ the V handle is covered by the card overlay z60>45/46). Fix (reuse-don't-parallel): fork-gallery-brain-hooks now also listens for `decision:rendered` → binds the SAME bindBrain in-card Ask keyed by the decision's canonical address (talk(decisionAddr) resolves the decision territory = the "walk through WITH Tim" interactive follow-up beyond the one-shot explain). Harmless before DNA's card lands (event doesn't fire yet); ready the instant it emits. CONTRACT flagged to DNA: emit decision:rendered with {element:<card root>, address:"decision://global/<id>"} (mirror gallery:rendered; address=BARE canonical, same as the take target, NOT #elem). So my lane now: resolve + explain + interactive-follow-up ALL done; only DNA's visual card + wildcard's take remain.

### mind:// floor fix — separate commit (2026-06-18, 7ed8e91)
- PRE-EXISTING RED found while extending resolve_address: the mind:// branch (fork's own R13 9256df9) called _mr().resolve(...) — a `.resolve(` token in cognition.py the C9.2 source-invariant FORBIDS in the scanned dispatch layer. governance was 42+1breach. minds.py clean (stranded, not in-flight) → adopt-and-fix (advisor-endorsed; NOT bundled into the decision commit).
- FIX: added module-level minds.resolve_mind(addr) wrapper (wraps mind_registry().resolve); cognition.py routes through it — mirrors board→get_item/clone→get_by_address/skill→.read. MindRegistry.resolve stays the API (minds_acceptance 10/0 unchanged). Floor → 43 ALL PASS. The `.resolve(` now lives in minds.py (the registry's own module, not a dispatch surface) — legitimate, in-spirit.

### ui://instrument/surface aim-meaning — projection's "deeper half" (2026-06-18, SCOPED, awaiting 2 confirmations)
- projection: the surface aim degrades to scheme-noun "this part of the surface" instead of the registered legend meaning. EMPIRICAL finding (verified static, no Suite/model load): path (A) FALSE — the meaning is NOT in address_help/_describe_ui_address (UI_REGISTRY has no instrument/surface row → that's WHY it degrades). It lives in the bindings/ registry: bindings/raw.py → meta{name,is,fills,why} (composition's legibility shape), keyed by BINDING id ("raw"), and the meaning shown = the ACTIVE binding's meta (LIVE surface state).
- RECOMMENDED (BOTH/AND, replied to projection): LIVE = projection feeds getAimLabel the active-binding meta.name/is from the surface side (the accurate one); FLOOR = I make territory_label resolve the default-binding meta (bindings/) for the instrument-surface aim so it degrades to real meaning not the scheme-noun. NOT building the floor until projection confirms (1) the canonical aim address for the surface, (2) default binding id="raw" — investigate-before-coding, no guessed address→binding mapping.

### channels↔Supabase boundary — Claude Design as a channel peer (2026-06-18, IN FLIGHT, commit 522ff17)
- GOAL (Tim, via lead): Claude Design PARTICIPATES in channels + works on SAME DATA, via Supabase as the neutral meeting ground, ZERO box exposure (outbound-only, like the audit table). NOT #75 (the full substrate→Supabase migration — gated/premature).
- ★ THE REFRAME (Tim corrected MY mistake): I first proposed MIRROR+BRIDGE (company keeps local channel store + mirrors to Supabase) — Tim flagged it = a two-store sync = a BRIDGE anti-pattern, doubly wrong on a moving substrate. Recorded the lesson in [[feedback-islands-join-mainland]]. CORRECT shape = EXTERNAL EDGE: internal substrate = ONE source of truth (keeps evolving); Supabase = the published shared slice + client inlet. SHARED channels = SINGLE-SOURCE on Supabase (company writes + subscribes Realtime; no local mirror); INTERNAL-only channels stay LOCAL + untouched. SHARED data = internal PUBLISHES the slice OUT (dna_tokens/design_seeds, read-mostly for CD) + reads CD's design_submissions back IN (directional, not a mirror).
- SCOUT MAP (the fabric internals): transport = channels/company_channel.mjs (POST to a session's ephemeral port → mcp.notification → <channel> tag); store = .data/channels/_mail.jsonl ({ts,kind,frm,to,to_cwd,thread,text}) + _channels/<id>.json; outbound precedent = mcp_face/remote.py:113-162 (the audit-table PostgREST POST, SUPABASE_AUDIT_* creds); NO Realtime exists yet (net-new); inject path = session_supervisor.py /channel-send → cc_channels.send.
- BUILT + offline-tested + committed (522ff17): runtime/supabase_principal.py (least-privilege principal-JWT auth — the lead's rule: authenticate AS a dedicated RLS-scoped principal, NOT service-role master key in-process; env-prefix COMPANY_CHANNEL_*, vi_vision migrates onto it later = one home; extracted from vi_vision's option-B flow) + runtime/channel_boundary.py (build_post_row + publish_shared_post[Bearer principal, fail-loud-with-reason] + route_inject + ChannelInjectSubscriber.on_insert). ★ ADVISOR-CAUGHT BUG (mirror→single-source pivot): route_inject is SKIP-BY-ORIGIN, BOTH 'session'+'client' rows inject (NOT the old client-only filter, which would hide company↔company on shared channels); publish does NOT also locally inject (no double-deliver).
- start() now BUILT + offline-verified + committed (f2ba66d) — lead greenlit websocket-client + answered the load-bearing detail: the phx_join MUST carry the principal JWT as payload.access_token (Realtime applies RLS to postgres_changes → streams ONLY JWT-SELECTable rows; no token = silent-empty sub). build_join_msg (JWT-in-join, tested) + parse_realtime_message (postgres_changes INSERT→record) + _ws_url (https→wss/http→ws) + the WS daemon thread (reconnect+backoff, heartbeat+token-refresh keeps the RLS sub alive past JWT expiry) + on_insert→route_inject. POLL FALLBACK documented on the same start() seam if the Phoenix protocol rabbit-holes. websocket-client installed in the LOCAL venv (no deps manifest exists — lead lands it in PROD requirements at the flip).
- DECISIONS RESOLVED (lead): shared-rule = fail-closed `shared` bool default false + per-client clients.channels grant (two levels); transport = websocket-client/Realtime (poll = contained fallback). REMAINING build on my side = the cc_channels shared-flag publish HOOK — COUPLED to the live round-trip (a shared post delivers ONLY via the inject), built WITH it: additive shared=true only, internal byte-identical, throwaway-channel test, lifeline re-checked.
- 3 ASKS OUT (g-1781755967): (1) builder confirm the client-registry ROW + RLS POLICY scoping the COMPANY_CHANNEL principal to channel_posts+collab (not just the cred); (2) builder post the Realtime topic + a real postgres_changes payload once 0003 live; (3) lead/Tim — WHICH channels are shared + HOW (a shared flag at create_channel?) = a product call, needed for the hook.
- ★ GATE BAR (advisor D): I'm the NAMED GATE for CD participation — "done" = the LIVE round-trip (company writes→Supabase→CD reads; CD writes→Realtime→injected into a live session), NOT offline green. Needs builder's live tables + the CD end (a CD test client OR I simulate a 'client' row). builder applied 0004 (collab-data live); applying 0003 (channels) after my shape-confirm. CRED: lead provisions the COMPANY_CHANNEL principal into the boundary env out-of-channel (REST URL public: gctunhsuwpaxeatwlmuv.supabase.co).
- NEXT: builder's (1)+(2) → wire start() against the real payload + the cc_channels hook (against the (3) discriminator), scoped to shared channels (internal path byte-identical, test on a throwaway channel, confirm internal lifeline intact after) → live round-trip verify → lead final-verify → CD connects.

### channels boundary — (b)+(d) received, start() target locked, 2 reconciles open (2026-06-18)
- builder posted (b)+(d): LOCAL Supabase http://127.0.0.1:15421 (DB :15432); WS ws://127.0.0.1:15421/realtime/v1/websocket; topic `realtime:public.channel_posts`; phx_join {config:{postgres_changes:[INSERT public.channel_posts]}, access_token:<boundary JWT>}; INSERT payload {type,schema,table,record{id,channel_id,from_session,sender_kind,to_session,thread,kind,text,created_at},old_record,commit_timestamp}; membership sub topic `realtime:public.channels` UPDATE; re-mint JWT on connect. boundary principal minted (email boundary@company.local, app_metadata.role='company_boundary'), 0005+0006 RLS applied + PROVEN (boundary 201/normal 403; design_client granted-channel 201/non-granted 403/forged-from_session 403). My start() (f2ba66d) targets all of this (topic param "public.channel_posts"; _ws_url http→ws handles :15421).
- ★ RECONCILE 1 (FLAGGED to lead, load-bearing): builder's (b) skip rule = a SENDER-KIND filter (session→skip, client→inject) but the lead's gate B (LIVE-LOCAL-VERIFY.md) requires the SUB to deliver a 'session' post to a SECOND company session (skip-by-ORIGIN, both kinds). Contradiction. MODEL-1 (pure single-source, the lead's stated "no local mirror for shared" + gate B) = my route_inject (skip-by-from_session, both kinds). MODEL-2 (hybrid: local delivers session↔session + sub is CD-only) = builder's rule but reintroduces a local delivery path (the rejected mirror) + breaks gate B's text. My read + impl = MODEL-1; awaiting the lead's model call.
- RECONCILE 2 (asked builder): .boundary.env NOT at repo root — where does it live + what loads COMPANY_CHANNEL_* into the boundary PROCESS env? + is COMPANY_CHANNEL_TOKEN_ENDPOINT the standard /auth/v1/token (then supabase_principal needs COMPANY_CHANNEL_SA_EMAIL+PASSWORD/_REFRESH_TOKEN) or a custom mint? Align supabase_principal's env vars to builder's actual provisioning.
- FINDING (the hook): cc_channels has NO first-class "post to a named channel" primitive — a channel post = broadcast to member handles (mcp_face/tools/cc_channel.py op=broadcast loops cc.send per member). So the SHARED-channel-post path is a small ADDITION (a shared-aware post_to_channel → publish_shared_post for shared=true; local broadcast for internal), MY lane, built once MODEL confirmed + env available to verify the write. The gate's bar = the LIVE round-trip (A–I), not offline green.
- ACCEPTANCE BAR = build-prep/claude-design/LIVE-LOCAL-VERIFY.md (lead's by-use gate A–I: participation A–D + security E–I incl. private-stays-dark, no-impersonation-through-edge, fail-closed-tools, mandatory-audit, internal-lifeline-byte-identical). Lead runs it the moment BOTH halves (fork start()+hook, builder Edge Function dispatch) are testable. Fully-green opens THE FLIP (prod 0001–0006 + prod principals + edge deploy + re-run live on vspokes.com).

### channels boundary — MY HALF FULLY LIVE-VERIFIED + gate-ready (2026-06-18, commits 522ff17·f2ba66d·bfbb4b0·cf483a2)
- BUILT: runtime/supabase_principal.py (least-privilege principal-JWT, env-prefix; vi_vision migrates onto it later) · runtime/channel_boundary.py (build_post_row[default-generates thread — NOT NULL, caught LIVE], publish_shared_post[fail-loud-with-reason], route_inject[skip-by-ORIGIN both kinds — MODEL-1], ChannelInjectSubscriber start()[real Supabase Realtime WS, JWT-in-join load-bearing, heartbeat+token-refresh, reconnect], build_join_msg/parse_realtime_message/_ws_url) · runtime/channel_boundary_run.py (load_env_file[default build-prep/claude-design/supabase/.boundary.env, COMPANY_CHANNEL_ENV_FILE override, EXISTING os.environ wins], make_inject=cc_channels.push[PUSH-only, no local record], make_members_of, run_boundary, post_to_channel[shared-aware], ensure_design_channel) · cc_channels.create_channel additive `shared` kwarg + is_shared (fail-closed).
- ★ LIVE-VERIFIED BY USE (local stack http://127.0.0.1:15421, the lead's bar — NOT offline green): (1) auth→WS join (JWT-in-join)→publish→RECEIVE the INSERT (RLS-on-join streams it; silent-empty avoided); (2) is_shared fail-closed + post_to_channel('design')→Supabase 201; (3) GATE-B MECHANISM with REAL handles: seated ch-8djrpmsl as sole 'design' member, real sub → FOREIGN-origin post injected to [ch-8djrpmsl] + I literally RECEIVED the <channel> tag (delivery observed) · SELF-origin → [] (skip-origin, no echo) · cleaned out after. The skip-origin EQUALITY (published from_session==member handle) works live — the one untested seam, advisor-caught, now closed.
- LIFELINE: cc_channels_acceptance ALL GREEN (only an additive create_channel kwarg); internal send/push/route byte-identical.
- OPEN: (a) route_inject ignores to_session (broadcast-only; directed not honored — advisor-flagged, gate A–I don't test it, fix if in scope). (b) POST-GATE: real-use publish from another process — cred is in the boundary process; rec = bridge starts run_boundary (cred+sub in the company process) + a cc_channel MCP op → post_to_channel in-process. Deferred. (c) vi_vision→supabase_principal migration = named follow-up (don't destabilize working vi_vision now).
- NEXT: builder's Edge Function (CD-side, two-gates) testable → the lead runs the full A–I gate by use → fully-green → THE FLIP (prod 0001–0006 + prod principals + edge deploy + re-run live on vspokes.com). My half stands ready + proven; the inject reuses the proven cc_channels.push primitive.

### channels boundary — FULL A–I GREEN + STOOD DOWN (2026-06-18, connector half DONE)
- The lead ran LIVE-LOCAL-VERIFY A–I by use, end-to-end on the local stack — ALL GREEN. fork's company-side observations (in my live session ch-8djrpmsl): GATE C ✓ (CD post from claude-design injected into my session via the boundary sub — the integration landing, full path EF:8773→Supabase→RLS-streamed-sub→push→live session); GATE B ✓ (foreign-origin ch-foreign-B post RECEIVED; self-origin from ch-8djrpmsl NOT received — skip-by-origin/no-echo, proven by absence); GATE I ✓ (throwaway internal 1:1 send delivered byte-identical — additive shared path left send/push/route untouched). Joined the lead's CD-side A/B/D/E/F/G/H + inlet + audit. The whole gate = Claude Design is a two-way channel peer on the same Supabase data, two-gate-scoped, zero box exposure, internal fabric intact.
- STOOD DOWN (lead-authorized): boundary process terminated; removed my test seat from 'design' (shared-channel seed persists, members empty). Connector half DONE. THE PROD FLIP (migrations→prod + prod principal + edge deploy + live public re-verify on vspokes.com) = the lead's/Tim's outward-facing step, pending Tim's confirm (reprioritized behind interface-first). Re-stand-up = one command (runtime/channel_boundary_run.py) against any env.
- NEXT (interface-first, Tim's reprioritization 2026-06-18): my piece = the RHM run_turn EXPLAIN leg (confirmed g-1781764919) — already built+live-verified for the decision-card (talk→territory_prose + HOOK 1b interactive Ask); generalizes to the archetypes via talk(aimed-address)→territory_for (any addressable archetype gets the explain free); the ONE seam I own = the subtype-aware explanation (composition's explanation_policy → run_role-policy vs run_turn-briefing — my lean: briefing-variant on run_turn, the live-verified interactive path). Awaiting composition's decision_subtypes model + the gather's decision-set.

### ui://instrument/surface aim-grounding — BUILT + committed (2026-06-18, 6d02890) — closes the "deeper half"
- projection's stranger-Ask gap (the surface DEFAULT grounds thin): a first-timer's "What am I looking at?" at the nothing-picked surface → the RHM punted "thin pointer" because territory_prose('ui://instrument/surface') had no real identity (ui:// address_help returns thin), though the surface SHOWS a title+description. EARLIER FINDING confirmed: the meaning lives in bindings/<id>.py meta (the ACTIVE projection view; ~10 views; raw="What's happening" = default fallback).
- FIX (the hybrid, my half): territory_for VIEW leg OVERRIDES the thin ui:// identity for the instrument-surface aim with the active binding's meta (read from bindings/<id>.py, registry-is-truth — never hardcode); territory_prose renders name (What this is) + is/fills/why (What this view shows). Active view rides the aim fragment (#binding=<id>, projection passes it); default 'raw'. _instrument_view_binding + _binding_meta (path-safe, degrade-clean, never raises). VERIFIED offline: territory_prose('ui://instrument/surface') → "What's happening / A live map of everything the Company is doing…" zero leak; #binding=semantic → "Meaning"; territory_acceptance 33/33. ★ LIVE proof = projection's three-question-walk re-run on a cold stranger (their lane; offline-green≠live, not claiming the verdict, only that the grounding's now in the context_block). projection's half = pass #binding=<active> when a view's switched (default-raw alone fixes the first-timer).
- This is the interface-first RHM-grounding improving — every surface aim now resolves to the operator-visible meaning, not a generic noun. Same mechanism extends to any addressable archetype (the free generalization).

### POST-COMPACTION RECOVERY ✓ + gate-sweep + by-use re-verify (2026-06-19)
- SURVIVED COMPACTION. The self-seed (590076.json, claude_pid-keyed) worked — this is the same session 11e7d395 on the other side. Seed loop already closed pre-compaction (mail: I replied 'seeded' 01:19, recollection relayed, lead ack'd "fork SEEDED+VERIFIED = excellent" + verified his own self-heal). No stale confirm owed. The canonical recovery protocol is committed: build-prep/episodic-memory-adaptation/SELF-RECALL-RECOVERY-PROTOCOL.md (d1c71cb).
- GATE-SWEEP (the landscape moved ~1.5 days while I compacted; advisor: check whether MY gates opened): (1) decision-surface CLOSED/GREEN — projection caught+fixed the renderArchetype-cutover regression (ba0356d, 0225c92) + retire-gate landed (85de064) + gate green (117e061: contract+take+both viewports+cold-stranger PASS); GET /api/decisions inbox live (3189a60). (2) subtype-briefing STILL GATED — `decision_subtypes` grep empty (composition hasn't landed the model). (3) memory-leg digest STILL GATED — recall_for_decision (runtime/decision_memory.py:67) reranks + fetches CAS digests INTERNALLY for scoring, but the RETURNED context items carry only source/space/score/rerank_score/cosine — NO digest text. recollection's lane to surface; mine to consume. (4) unify-CORE plan touched Jun-18 22:15 (ONE-BRAIN-UNIFICATION-BUILD-PLAN.md) — composition's design-pass status unconfirmed; big coordinated lane, not solo-startable.
- ★ BY-USE re-verify of MY half post-cutover (discipline: "regression-safe seams preserved" was projection's claim, not mine): resolve_address('decision://global/substrate-home') → full dict (id/meaning/options/name/state=pending), 0.00s ✓. territory_label → "Adopt one shared spine…" operator-grade no-leak ✓. territory_prose → full RHM explanation (What this is / The decision / The options are) at Tim's altitude, zero machine-names ✓. The cutover was FRONT-END (renderDecision→renderArchetype); my BACKEND resolution + RHM prose are independent + INTACT. Memory leg timed out >3.0s + degraded clean (has memory:False, honest note, still decidable) — but that's the COLD-CACHE case (standalone script, no _warm_vector_cache; live bridge warms it). My timeout safety (a0fd294) confirmed still working. NOT a regression.
- CURRENT LANE = RHM VOICE elevation, GENUINELY GATED on DNA's dna/voice standard (g-1781793576). State: dna/voice.json exists but is Jun-13 (the OLD file, pre the "derive-from-Tim's-real-language" ruling); DNA (ch-ovxwz8k8, owns the DNA LANGUAGE incl. voice-standard) is DERIVING the new one but is heads-down on the renderer resolver-fix collision first; the voice ADDRESSING SCHEME doesn't exist yet (composition still openly asking DNA for it). Lead ruled: ADDRESSABLE voice (RHM prose RESOLVES dna/voice, inherits by construction) + DO NOT author abstractly (derive from Tim's real language; he's the authority) + sequence: DNA derives+addresses → fork elevates the RHM's 2 layers → projection verifies "reads as HIS". Pre-building the resolver wire = guessing the unstable address = the moving-substrate anti-pattern. HOLD.
- ★ NON-GATED MOVE (gap-pressure): DNA's field→register map is CARD-FIELDS ONLY (name/meaning/label/implication/is). The RHM is SPOKEN PROSE — a register DNA hasn't articulated. As the RHM-prose owner I deposit the as-is corpus (PANEL_BRIEFING in ui_claude_session.py + the territory_prose framing in territory.py) + the spoken-register requirement to DNA, to pick up at its voice beat — so the standard COVERS my surface when it lands. Extraction-input only (here's the as-is + the structural need); NOT authoring how it should sound (the lead's forbidden line, Tim's authority). DNA banked it durably (docs/command/VOICE-STANDARD-SPEC.md); handshake set (DNA flags me + composition with the register-handle scheme when dna/voice lands → I cut PANEL_BRIEFING + territory_prose to RESOLVE it). DNA is heads-down on the renderer-to-scr-home-craft bar first (adopted offline ch-c6jjpkhu's renderer work f91dd11, now sole renderer owner) → voice beat further out; deposit survives regardless.

### memory-leg-digest consume-side SHIPPED (2026-06-19, d42ca96) — unify step-1 fully landed both halves
- recollection cleared the gate I'd noted open this turn: recall_for_decision now attaches each context item's clean digest MEANING as c["text"] (5d9e645 + 099a911 for 10/10 coverage incl. list-of-insight-strings; legibility-safe — no JSON/ids). My consume-side: territory_prose's decision MEMORY leg now renders the top-2 meanings + an honest "(plus N more relevant pieces)" tail instead of the bare count; degrade-clean (count fallback if none carry text) + operator-law belt (drop any "://"-leaking text).
- VERIFIED BY USE (warm cache, the live-bridge condition): recall returned 10/10 with text; warm territory_for(decision://global/substrate-home) 1.80s (under the 3s budget, no timeout, matches recollection's ~1.9s); territory_prose shows real meaning ("document the gap… | stop building the mockup studio…"), no "://" leak; territory_acceptance 33/33. The "decision:" prefix in items is recollection's intentional field-label (which digest field) — kept for RHM synthesis signal; a future operator-facing LABEL render strips at that surface, not in shared recall. Consuming `text` directly, no alias.

### "POINT AT THE THING" spotlight — emit-half design SETTLED, sent to projection (2026-06-19, NEW LANE, v2 build pending convergence)
- projection built+verified the SURFACE half (ddd220a): window.dispatchEvent('ui:point',{detail:{address:'ui://…'}}) → resolveUiTarget (indicate + .ui-spotlight glow + scrollIntoView), ui:// only, fail-loud malformed / no-op non-ui://; reuses the click-highlight sink (no parallel path). surface/app/src/lib/address.ts:installPointerBridge. Asked ME (the brain emit half) to settle the SHAPE (Q1 structured-vs-inline / Q2 which-targets).
- ★ THE REFRAME (load-bearing): the brain's context is ADDRESS-FREE — run_turn prompt = "[Operator context]\n"+territory_prose(aim)+question; territory_prose strips every ui:// to human meaning (operator-law, territory_acceptance 33/33) + the raw aim address is NOT in the prompt (only the client-side session key). So the brain NEVER holds a ui:// to emit, and inline-in-prose = operator-law leak (+ _stripMd inline-marker-leak lesson). ⇒ NOT inline. Two paths: (A) AIM → FRONT-END-driven (fork-v-brain HAS getAimAddress() — no brain emit); (B) OTHER TARGETS → SERVER-RESOLVED (brain holds mcp__company → calls a company point(description) verb → SERVER resolves description→ui:// → stream carries {type:'point',address} → fork-brain-core dispatches; no address in prose, no sentinel). Advisor-endorsed server-verb over sentinel.
- ★ HONEST VALUE READ (measured the advisor's aim-moves-to-targets question): v1 (point-at-aim) LOW value — default aim ui://instrument/surface has NO data-ui-ref (no-op); the guided tour (RightHand.runTourStep:197) does NOT re-aim (STARTERS[0/1/2] at the SAME surface aim); the aim only moves on an OPERATOR sector-pick (already in focus). So did NOT reflexively ship v1. THE VALUE = v2 (brain points at targets it MENTIONS — controls/sectors — as it speaks: Tim's "going through it WITH"). Pointable set rich: ui://controls/{resolution|layer|space|lens|view|settings|scrubber|rung|representation|taste|live} + wheel regions/sectors (stamp() in toggles/* + wheel/Separator.tsx,Nucleation.tsx).
- OPEN CONVERGENCE (sent thread t-1781809092): (1) targets first (lean: controls+sectors); (2) resolve source — lean: address-FREE catalog [{label,token}] in the brain's turn context, brain emits token, fork-brain-core maps token→stamped ui:// (addresses off the brain); alt: server matches description→address via territory_label; (3) split — I build run_turn point-event leg + fork-brain-core dispatch, the company point verb + catalog we converge, projection confirms targets + sink (live). v2 INDEPENDENT of the dna/voice gate. Also offered a quick tour-only win (static step→target map in runTourStep, projection's file, no brain emit). AWAITING projection's 1/2/3.
- ★ CONVERGED + BUILT (2026-06-19, f8b6044): projection shipped+verified window.surfacePointables() → [{token,address,label}] live present-in-DOM set (a550b60); path-(b) ruled out (territory_label has no per-target meaning, it's client-side); targets = controls+wheel+sectors (sectors deferred — DNA suppresses their spotlight). MY 4 LEGS BUILT: (1) mcp_face/tools/point.py — point(token) verb (opaque token, no address, read/cosmetic, the tool-call IS the signal); (2) run_turn — pointables param → per-turn prompt block (_render_pointables, operator-law belt drops address-looking tokens) + PANEL_BRIEFING capability instruction + point tool_use detection → {type:'point',token}; (3) bridge /api/claude/turn → passes body.pointables (additive); (4) fork-brain-core.js — reads surfacePointables() at send, sends {token,label} ONLY (addresses client-side), on {type:'point'} maps token→ui:// LOCAL + dispatch ui:point → projection's sink; synced to public/gallery (gitignored, regenerated from build-prep source). VERIFIED unit/by-use: verb returns {ok,token}; _render_pointables renders + drops a ui:// leak; run_turn accepts pointables; py_compile + node --check all clean. NOT end-to-end (the agreed meet-up): the live verify = bridge restart + surface reload → ask a question naming an on-screen thing → brain calls point("lens") → spotlight; projection's cold-stranger tests CADENCE+RELIABILITY (does the model call point at the right moment); if it mis-times, it's a PANEL_BRIEFING tune (my side). Pinged projection (t-1781818520) for the live run.
- ★ END-TO-END VERIFIED by projection (2026-06-19, live 390): "what's the lens?" → brain called point("lens") → run_turn {type:'point',token:'lens'} → fork-brain-core mapped lens→ui://controls/lens → ui:point → sink → THE LENS CHIP SPOTLIGHTS as the V explains it; timing good; request body carried pointables:[7×{token,label}] NO addresses (operator-law held). Mechanism PROVEN. CADENCE GAP found: on BROAD answers (the tour's "what am I looking at?") the brain NAMED pointables verbatim but called point() ZERO times → spotlight missed the prime onboarding moment. TUNED (dd72339): PANEL_BRIEFING now pushes point THROUGH broad answers + walkthroughs, match-prose-to-label by MEANING, one-at-a-time as named (3-thing walkthrough → 3 point calls). Re-drive needs a bridge restart + a FRESH V session (PANEL_BRIEFING = first-turn --append-system-prompt). AWAITING projection's re-drive + full fresh-eyes cold-stranger on the tuned tour. Downstream (not mine): .ui-indicated punch + sector spotlight-suppression (both DNA).
- ★ CADENCE VERIFIED WORKING (2026-06-19, projection re-drove the tuned tour live 390): step 1 "what am I looking at?" → 1 point (map); step 2 "what can I do?" → FOUR points IN ORDER, prose-matched, as the V lists them (lens→layer→view→scrubber); 5 total, the surface lights up as the V walks them through = the "perception is the compute" payoff. The dd72339 tune fixed the under-call. projection DROPPED the wheel from surfacePointables() (09c4bf0) — occluded behind the V panel AND low-value (whole map = spotlight-everything); I CONFIRMED (matches my value read); catalog = the 6 visible affordances. ★ MY SIDE OF THIS LANE = DONE + VERIFIED (brain-emit: point verb + run_turn event + pointables fold + fork-brain-core dispatch + cadence instruction — built, end-to-end-verified, cadence-verified by use). No longer the gate. Remaining polish is DNA's (highlight-token punch for "look HERE" + sector un-suppress); the formal fresh-eyes critic best runs on the PUNCHED visual → then Tim's "does it feel great" reaction (his authority). Stand ready for any further cadence tune.
- ★ COLD-STRANGER PASSED — SPOTLIGHT DONE TO THE BAR, READY FOR TIM (2026-06-19, projection ran fresh-eyes on the punched live experience, both viewports): PART A (visual) PASS — clear plum ring, 2px outline + layered glow, draws the eye, fully visible on every control, pings-then-settles single-shot; PART B (live experience) PASS "stronger than expected" — "what can I do here?" → surface lit IN EXACT SYNC + IN ORDER (lens→layer→view→scrubber, ~100ms after each word, ~3-5s apart), "genuinely feels like the V is showing you around" (= Tim's perception-is-compute); panel-coverage clean (no named control occluded). The whole composition (fork emit+cadence · DNA plum punch + calm single-ping · projection sink+catalog+punch) cold-stranger-verified. Tim's "does it feel great" = the last word. 3 non-blocking notes: (1) the last-named control keeps a soft .ui-indicated outline while the panel's open (nav-indicate's persistence reused for ui:point) — MY STEER: lean TRANSIENT for the point path (click=nav=persistent "you are here"; V-pointing=transient "look here as I say this"), decouple in projection's sink (transient variant for the ui:point entry, click untouched) — but it's a feel call, Tim may like the "we just discussed this" cue; not urgent. (2) phone dim softens the ring (DNA). (3) legend coverage fine. MY SIDE COMPLETE; remaining is DNA's visual + the optional transient-decouple (projection's sink, my steer given).
- ★ SPOTLIGHT FULLY TO THE BAR — both polish notes CLOSED + verified (2026-06-19, projection c4a439f): (1) my transient-decouple steer LANDED — ui:point now releases glow + .ui-indicated on ping-clear (transient ATTENTION), the click path stays persistent (SELECTION); two acts/two behaviours, one sink, click untouched; verified by use (390). (2) DNA lifted .ui-spotlight z-index above the scrim → the ring punches through the dim at full strength (closes the phone-soft note). The spotlight lane is DONE across all three lanes (fork emit+cadence · DNA plum+lift · projection sink+catalog+punch) — cold-stranger-passed, role-true transient/persistent model, ring-over-dim. NOTHING open on my side; Tim's "does it feel great" is the last word. (DNA backlog: --dna-indicate token 503e499 for an addressable indicate colour, non-blocking.)

### RHM DECISIONS-GROUNDING fix — FLAG B+C (trust-critical), root-caused + verified (2026-06-19, 24e1c3a)
- projection's HOLISTIC integration cold-stranger (whole journey: land→map→inbox→decide→V-guide, both viewports) PASSED-with-flags. The decide-loop closes (write ok, count 7→6→5, decided item leaves, ✕ returns to list). TWO flags, both my brain-prose lane, ONE bug:
  • FLAG B (operator-law leak): the V's "decisions" reply listed RAW ITEM CODES — "(s117)","(s115)","(s60-review)","(s118)".
  • FLAG C (the trust-breaker, "most important"): the V's "what decisions are waiting?" returned 5 items with ZERO overlap with the inbox's canonical /api/decisions set → two contradictory "your decisions."
- ★ ROOT — VERIFIED BY USE (advisor caught me labelling it "evidenced" when it was INFERRED; the verify changed nothing but is the discipline): SUITE.list_surfaced() returns 92 surfaced-ACTION items with ids s10-review/s60-review/s117-review/… → the flagged codes ARE list_surfaced ids (s60-review exact; "(s117)"=s117-review abbreviated). The brain reached list_surfaced because its docstring SAID "Decisions the system surfaced for the operator"; that inbox is the build/approval queue (review/role_build/code_build), NOT the decision-cards. AND the canonical decision registry had NO MCP verb → the brain couldn't reach the right source. Both flags = one root.
- FIX (root-caused, advisor-steered ground-on-canonical not point-at-inbox, no general id-strip): (1) NEW mcp_face/tools/decisions.py — the canonical pending-decisions verb, returns the SAME decision_inbox(decision_registry(), store) the inbox renders, HUMAN-ONLY ({name,recommended}, NO id/address — brain can't leak what it isn't given, pointables pattern); (2) tight PANEL_BRIEFING grounding line (use decisions for "waiting", never surfaced/recall); (3) DISAMBIGUATED list_surfaced's docstring (the confirmed misdirector — now "surfaced-action/build-approval inbox, NOT the decision-cards"; docstring-only). The docstring-disambiguation is the STRUCTURAL lever (make the wrong tool unattractive > PANEL_BRIEFING bulk — advisor).
- VERIFIED BY USE: the verb returns 5 waiting BY NAME, 0 ids/'://'/s-codes in output, == /api/decisions canonical pending set (coherent by construction). py_compile clean. Brain-reliability (does it now reach decisions, not list_surfaced) = projection's cold-stranger re-run (bridge restart for PANEL_BRIEFING + fresh session; verb+docstring auto-load per turn); if it still mis-grounds, escalate the instruction. Pinged projection (t-1781827990). The spotlight per-control point was re-confirmed INTACT (the critic's miss was a 1.2s-ping timing artifact).

### DECISION RETRACT primitive + URGENT test-contamination cleanup (2026-06-19, 04d424d, lead+projection authorized)
- URGENT (Tim about to land on his real decide surface): projection's verification clicks had written REAL decision_take marks onto 2 of Tim's 7 decisions — cluster-identity (×2) + merge-sa-authorize (×1), source=operator, ts 2026-06-18T23:24-29, value=the recommended label → compose_state showed them DECIDED. Trust-breaker on his real decisions. Confirmed by use via marks_for (matched projection's report exactly). The 5 others correctly pending.
- ROOT: the mark store is APPEND-ONLY (append_mark + marks_for only; NO retract/remove) + compose_state was latest-decision_take-wins (no un-decide) → no primitive to reset to pending. (append_mark does NOT validate mark_type against a registry — opaque string — so a new mark_type is appendable directly.)
- FIX (the primitive the case proves needed — operator "Change"/undo IMPLIES retract; advisor+lead: append-compatible > store surgery): compose_state now folds decision_take AND decision_retract events; LATEST event by ts wins (retract-after-take → pending; later take re-decides); unit-pure, append-only-safe (audit trail kept). Cleared the 2 by appending ONE decision_retract each (ts now > the test takes; source=system, by=ch-8djrpmsl, note=contamination-cleanup-authorized). SCOPE-TIGHT: only the 2 named targets. VERIFIED by use (fresh process): both pending, decisions verb = 7/7 waiting, other 5 untouched (0 retracts). The test takes REMAIN as honest history (take→retract superseded), nothing deleted.
- ★ OPERATIONAL: the marks are live (marks_for reads disk) BUT the RUNNING bridge holds OLD compose_state in memory (ignores decision_retract) → needs a RESTART before the live surface shows pending. Flagged projection (restart → re-verify all-7-pending → lead clears Tim) on g-1781828792. DURABLE GENERALIZE (lead's "right after"): wire the operator Change button → territory_write a decision_retract (the undo path); the compose_state primitive is ready for it.
- ★ CONFIRMED CLEARED LIVE (2026-06-19, projection restarted the bridge + re-verified): /api/decisions = 7 total / 7 PENDING / 0 decided; live inbox pill "7 decisions waiting" with all 7 listed incl. the 2 formerly-contaminated. Tim's decide surface CLEAN. The retract primitive validated end-to-end by use (data → restarted compose_state → live inbox). STILL OPEN (my loops, flagged g-1781829782): (1) FLAG B+C GROUNDING re-verify — does the V now reach the decisions verb (not list_surfaced) when asked "what decisions are waiting?" — pending projection's integration cold-stranger re-run (verify-by-use; escalate instruction if it still mis-grounds); (2) the Change-button → territory_write decision_retract wiring (operator undo) whenever projection/DNA reach that beat. NOTE: the Tim-gate for the decide surface is now the lead's CRITICAL-COMPARISON pass (DNA full-bar render vs reference examples) — not my lane unless it touches RHM brain-prose.
- ★ FLAG B+C VERIFIED FIXED LIVE (2026-06-19, projection drove the V 390 after bridge restart): asked "what decisions are waiting?" → the V named the CANONICAL 7 (exactly the inbox set, incl. rerank-loadout="how sharp the context behind a decision needs to be"), ZERO s-codes (regex-checked), reaches the decisions verb not list_surfaced, coherent with the pill, + the rhm_spoken offer at the end ("walk you through one at a time… or point you at the one that matters most"). Both trust-critical flags CLOSED end-to-end. Secondary signal: the rhm_spoken register already reads right via PANEL_BRIEFING (before dna/voice content calibration). MY LANES CLEAN: spotlight done+passed, decisions grounding+contamination fixed+verified-live, memory-leg shipped. ONLY OPEN = the RHM VOICE wire (awaiting DNA's resolve-source for dna/voice.operator_voice.rhm_spoken) + the Change-button→decision_retract wire (projection/DNA's beat, primitive ready).
- ★ CARD-COPY PARITY: merge-sa-authorize (MY card) re-voiced to the standard (2026-06-19, 33ee7f1) — recollection did the other 6 (514d883), sent paste-ready strings for the 7th (their voice-elevation lane; my file). Applied: meaning→70-char telegraphic question, legibility.why→the explain body (+ decide-later/no-rush), is→"Reversible · your latest answer wins", lowercase "the company" parity, option[0] implication carries the 2 parked copy-refine notes (own-key-to-library not "sign-in", revocable). VERIFIED by use: resolves, ≤90, leg.why=explain, state still pending (retract holds), no leak. ALL 7 CARDS NOW AT-VOICE. Card-copy parity item CLOSED; the standard's CONTENT still calibrates with DNA+Tim (my voice wire resolves whatever lands).
