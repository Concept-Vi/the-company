# FINDS — wildcard (ch-piffgfxv)

*Per-member append-only finds file (no concurrent-edit races on the shared DISCOVERY_LOG). wildcard owns this file; the lead reads across all finds/*.md to synthesize convergences into DISCOVERY_LOG + EMERGING_SHAPE. Finds not conclusions; honest verification-state; nothing final — every find is a partial sketch of one shape ([[feedback-confident-not-correct]] generative form). Format per find: what · where(path) · verbatim-if-quotable · verification-state · what-shape-it-points-at.*

*Territory: vi-visual-bridge (the direction-circuit prototype Tim actually uses — slide mode live; ~30K-line server.py+index.ts; 1,377 md mostly session-talk; 7,141 interaction snapshots). I came in via a formation-read of the whole corpus, so several of these are Tim-verbatim from history.*

---

## F1 · THE OMNIFILE — addressable markdown as a coordinate system
- **WHERE:** `docs/designs/studio/STUDIO-FORMATION-PLAN.md` (Tim verbatim, 2026-03-02).
- **VERBATIM (Tim):** "markdown extensions, element walkers, and address system already make markdown elements addressable, capable of typed IDs that correlate to visual, spatial, relational, and positional rendering behavior. An OmniFile blurs the definition of a single document — graph-like chains and relations across markdown files. The same typed structure that powers the map can be rendered as spatial coordinates in graph mode. It's a coordinate system, and the coordinate system already exists in the markdown format."
- **VERIFICATION-STATE:** design-doc verbatim (Tim's words) + see F13b — a working code slice EXISTS.
- **SHAPE IT POINTS AT:** the addressed/typed substrate + modes-as-projections, located IN THE CONTENT FORMAT. One sketch (the markdown-media one) of the company-wide "one addressed-typed coordinate-system, projection-agnostic" shape — alongside composition's slots, fork's addresses, the URI scheme, vi-visual's catalog.

## F2 · MULTI-RUNTIME was always the intent (CC + LangGraph)
- **WHERE:** same studio doc (Tim verbatim, March).
- **VERBATIM:** "build the Claude Code CLI directly into it, and connect LangGraph agents too for all kinds of quick commands and actions."
- **VERIFICATION-STATE:** design-doc verbatim.
- **SHAPE:** the loadable-brain (D2) sketched from the runtime end — CC-built-in (deliberate) + LangGraph (quick-action) may be the FOCUS/PANEL split + a possible third brain. Points at: the brain is plural and selected by the kind of act, not one engine.

## F3 · THE SHARED MINE, NAMED — "a landscape of prototype buildings"
- **WHERE:** vi-visual-bridge supabase-migration docs / studio plan (Tim verbatim).
- **VERBATIM:** "dozens of separate prototype projects Tim built independently — vi-composition, conversation-intelligence, graph editor, content router, and more. Each is a different building. Most won't be directly relevant... find which ones have foundations worth building on."
- **VERIFICATION-STATE:** design-doc verbatim.
- **SHAPE:** the shared mine NAMED, with project names + Tim's own finding-phase discipline ("find the build SITES, not map everything"). Points at: the company is a field of partial sketches by design — the finding method is Tim's, stated in March.

## F4 · UNIFIED_SHELL + surface-design-brief — a prior whole interface design
- **WHERE:** `docs/designs/UNIFIED_SHELL_TEAM_DESIGN.md` + `docs/designs/surface-design-brief/` (7 parts), Feb 2026, independent session.
- **VERBATIM premise:** "one kernel understanding, multiple contextual expressions."
- **VERIFICATION-STATE:** design-doc, not yet deep-read.
- **SHAPE:** modes-as-projections, regenerated independently in Feb. Another sketch of the same interface shape — confirming it was designed-many-times-convergently (the EMERGING_SHAPE thesis), from vi-visual's end.

## F13 · BUILT-WIRE / UNRENDERED-FACE inside vi-visual-bridge (the pattern fork named, in my territory)
- **WHERE:** `mcp/src/index.ts` (17 tools), `handleCatalog`(idx 4458), `handleTemplate`(idx 4298), `server.py` graph/document render.
- **WHAT:** 17 MCP tools declared (agent-facing wire complete) but the Tim-facing render is partial: `catalog`/`template` = a real COMPONENT-REGISTRY query wire (props_schema/depends/outputs/addresses `{name}@v{version}`) with NO Tim-face; graph-mode = built tool, near-zero render; document-mode = designed surface past its substrate; 4 unwired vi_sync tables.
- **VERIFICATION-STATE:** tool-count verified by grep; handlers Observed in code (real, not stub); render-gaps inferred from corpus + grep, not yet use-verified.
- **SHAPE IT POINTS AT:** vi-visual built the AGENT-facing half of the circuit completely and the TIM-facing half only partially (slide renders richly, the rest don't). One half-circuit sketched 17 ways. The gaps are unrendered PROJECTIONS of an already-complete interaction grammar. The interface's shape = render the Tim-facing half the agent-facing half already implies. ★ catalog-registry ≡ composition's slots ≡ OmniFile typed-IDs = same substrate, independent builds.

## F13b ·★ THE OMNIFILE HAS A WORKING CODE SLICE (not just a design doc) — addressable-markdown, in execution
- **WHERE:** `server.py` `FileModeState` (~line 3232) + `/api/file_mode/elements`, `/api/file_render_complete`; annotation persistence at `~/.vi-visual/annotations/file/{file_hash}/{element_id}.json`.
- **WHAT (Observed in code):** document mode does a Chrome-side **DOM walk** that assigns **element_ids to annotatable markdown elements**, posts them back to build `current_element_map` = `element_id → {section_index, content_hash, context}`; annotations persist **per-element keyed by sha256(file_path)[:16] + element_id**; a **render-generation counter** aborts stale re-renders; **orphan detection** runs when the file changes (annotations whose element vanished). So: markdown rendered → every element addressable → direction (annotation) bound to the element's address → survives file edits via content_hash + orphan-check.
- **VERIFICATION-STATE:** Observed in code (structures + endpoints exist, field-ownership documented across impl-1/2/3); NOT yet verified-by-use by me.
- **SHAPE IT POINTS AT:** the OmniFile (F1) is **partly BUILT, not just designed** — "addressable markdown elements with typed IDs + persistent per-element direction" exists as a running slice in document mode. This is the addressed-content substrate + the element-as-atomic-unit (my 7,141-snapshot finding: 100% element-bound) MEETING in code. The interface's content layer has a working seed here: render markdown → walk to addressable elements → bind direction at element-address → persist across edits. ★ The strongest "designed-surface MEETS built-substrate" join in my territory — the OmniFile design (F1) and the element-map code (F13b) are the same shape from the design end and the build end. fork/recollection: this is a concrete reference for "content-markdown as the missing address scheme" (the EMERGING_SHAPE gap fork flagged).

## F13c · ONE element-walker serves ALL modes — modes-as-projections, in the renderer code (deepens F13b)
- **WHERE:** `server.py` get_html() JS: `_annotationDomWalkContent(container, idPrefix, sectionIndex, elementIds, elementMap)` (~13254). Document mode = `_annotationDomWalkFileMode` (~13364) which is an EXPLICIT backward-compat SHIM: `_annotationDomWalkContent(container, 'md-0', 0, ...)`. Slide mode calls the SAME fn per section: `_annotationDomWalkContent(wrapper, 'narrative-'+idx, idx, ...)` (~14607, ~16055).
- **WHAT (Observed in code):** ONE walker assigns addressable IDs to content elements (p/h1-6/table/pre/blockquote/lists/task-items/alerts/tabs/spoilers/custom-directives/raw styled divs), with an `_INFRA_CLASSES` exclusion list (layout wrappers aren't annotatable) + a SEAM-03 ancestor-guard (only the outermost matched element in a chain gets the ID — no double-registration). Slide vs document differ ONLY by id-prefix (`narrative-N` vs `md-0`) and section index.
- **VERIFICATION-STATE:** Observed in code (shared fn + shim + both call sites present); not use-verified.
- **SHAPE IT POINTS AT:** modes-as-projections is IN THE RENDERER, not just the architecture. Slide and document don't have separate addressing systems — they share ONE element-addressing walk, distinguished by a prefix argument. = the same content, addressable the same way, projected under different mode-labels. The "modes are projections of one system" law (genesis read) is literally implemented as "one walker, different prefix." ★ Strengthens the EMERGING_SHAPE: the addressable-element substrate is mode-agnostic in code TODAY; the difference between projections is a parameter, not a mechanism. (And the `_INFRA_CLASSES`/SEAM-03 logic = hard-won detail of WHAT counts as an addressable element — worth mining, not re-deriving, when the interface defines its anchor-granularity.)

## F14 ·★ THE CATALOG ALREADY HOLDS COMPOSITION'S SLOTS *AND* DNA'S PRESENTATION-TOKENS (cross-domain — built precedent for two other segments)
- **WHERE:** `docs/designs/surface-design-brief/01_SYSTEM_ARCHITECTURE.md` (2026-03-24) + `.vi-visual/components/catalog.json` (v4). The brief was written FOR a Langflow agent building a Tim-driven design flow.
- **WHAT (the catalog v4 structure, per the brief):** ONE JSON file holds — `components` (86 entries: props/depends/motion/outputs/version, addressed `{category}/{name}@v{version}`) · `schema_types` (mockup-panel/scenario/registry/layout-token/surface-token/annotation-token) · `layout_tokens` · `surface_tokens` (16 presets: 4 shells + 12 panel styles) · `annotation_tokens` · `themes` (dark/warm/cool) · `devices` (iphoneSE/iphone15/ipadMini/desktop) · `panel_types` (summary/list/grid/chips/timeline/chart/plan/dock) · `motion_tokens`+`motion_presets`. "The Five Systems": Catalog(composition-grammar) · Directive(composition-language) · Response(15+ channels) · Persistence(4 Supabase tables) · Tool-interface.
- **VERIFICATION-STATE:** design-brief (Active, March) describing a built catalog; catalog.json v4 exists on disk (Observed); not use-verified by me.
- **SHAPE IT POINTS AT:** vi-visual already built a SINGLE addressed registry that is BOTH composition's slots/sockets (components with props/depends/outputs, addressed, versioned) AND DNA's presentation layer (themes/surface-tokens/layout-tokens/devices/motion = identity+voice+form-factor as DATA). The "interface = 3 streams composed" (Tim's sketch: wildcard-modes + DNA-presentation + composition-slots) has a PRECEDENT where all three already live in one catalog, resolved by one endpoint (`/api/component`). ★ Cross-domain: this is a built reference for composition's registry AND DNA's presentation-as-data, reached independently from vi-visual's end — same shape as DNA's 18 registries (F10) + composition's slots. AND the self-hosting loop the brief specifies (Tim's option-selections write BACK to the catalog as templates+token-presets) = the system resolving from Tim's own recognition-choices = the Circuit feeding the registry. The "devices" + "surface_tokens(4 shells)" = the desktop/portrait/landscape frame-axis form-factors, already enumerated as catalog data.

## F15 ·★ THE FULL INTERACTION LAYER — mapped from the contract + the JS (my STEP-1 deliverable for the gallery)
- **WHERE:** `contracts/taxonomies.json` (authoritative vocabulary, 3 language consumers) + `server.py` get_html() JS functions.
- **WHAT — the complete interaction vocabulary (from taxonomies.json, the source of truth):**
  - **4 annotation CATEGORIES:** comment · reaction · highlight · favour.
  - **reaction stamps (5):** good · wrong · explain · remember_this · do_this.
  - **content-comment types (6):** note · direction · correction · question · praise · discuss.
  - **meta-comment types (4):** framing · clarity · relevance · tone. (→ all_comment_types = 10)
  - **navigation actions (3):** submit · submit_continue · update.
- **THE JS INTERACTION FUNCTIONS (the actual surface, get_html()):** makeAnnotatable (binds an element as a direction target) · placeReactionStamp · showAnnotationOverlay · addAnnotationIndicator (the dot on annotated elements) · open/closeAnnotationMgmtPanel (the panel Tim opens — NOT per-type buttons; matches his Feb correction) · collectAnnotationsForSubmit · save/restore/reapplyAnnotationState · loadFileAnnotations/saveFileAnnotation (document-mode persistence). + favour (per-section + channel-total + trend).
- **VERIFICATION-STATE:** vocabulary verified (read from taxonomies.json); JS functions Observed by grep (exist); not use-verified.
- **SHAPE IT POINTS AT (the gallery's interaction layer):** the interaction system is ALREADY a closed, typed vocabulary with a single contract feeding 3 language layers — NOT something to invent for the gallery. The gallery needs exactly: per-element {open-annotation-panel → pick from 4 categories: comment(10 types)/reaction(5 stamps)/highlight/favour}, an indicator-dot on annotated elements, and submit/submit_continue/update navigation. Crucially (matches my 7,141-snapshot data AND Tim's Feb correction): Tim opens ONE annotation panel and picks a type — NOT a row of per-type buttons. The empirical counts (F13/snapshots) tell the gallery which to make prominent: comment(direction/question/correction) + reaction(good/wrong) + favour + highlight cover ~all real use. ★ This is the interaction layer the gallery needs, already specified as DATA (taxonomies.json) — the gallery binds its rendered elements to THIS vocabulary via the makeAnnotatable pattern. Mine the contract + the makeAnnotatable/mgmt-panel pattern; don't re-derive the interaction model.
- **SEAM TO DNA's GALLERY:** DNA renders the look; the interaction layer = makeAnnotatable applied to the gallery's rendered elements + the taxonomies vocabulary + the response-envelope write-back. The gallery's elements become direction-targets the same way slide/document elements do (F13c: one walker, any rendered surface). So the gallery inherits the interaction layer by being walked — it doesn't need its own.

---
*Digging next (own territory, on-direction): UNIFIED_SHELL deep-read; verify catalog.json v4 + taxonomies against the files; my session-history via session_recall(self) once :8007 up. The STEP-1 interaction-map for DNA's gallery = F15 (done, GPU-free, by reading — no clone needed for MY half). `mcp__company__ingest` dir-scan = sequence with lead if/when deeper map needed. Channel only on shape-changers / cross-domain / my-deliverables.*

## F16 ·★ THE PORTABLE BINDING CONTRACT — makeAnnotatable extracted surface-agnostic (my LANE-B deliverable, for DNA's gallery)
- **WHERE:** `server.py` get_html(): `makeAnnotatable(element, elementId, sectionIndex, options)` (~10942) · `collectAnnotationsForSubmit()` (~11685) · POST `/api/submit_response` (~19719, handler ~22649).
- **THE CONTRACT (extracted from the actual code, Observed):**
  1. **BIND:** `makeAnnotatable(el, elementId, sectionIndex, {meta})` — adds class `annotatable`, sets `data-element-id` + `data-section-index`, appends an `.annotation-strip` with: comment-btn → `openCommentInput`, favour-btn → `openFavourInput`, 5 reaction-stamp-btns → `placeReactionStamp(el, id, sec, reaction)`. (The strip is the per-element direction UI; reactions inline, comment/favour open a picker — = Tim's "open one panel, pick a type".)
  2. **ACCUMULATE:** every handler writes to a global `ANNOTATIONS.items` (+ highlights, sectionOrder); `save/restoreAnnotationState` persist partials per channel.
  3. **ROUTE-BACK:** `collectAnnotationsForSubmit()` → `{annotations: ANNOTATIONS.items, section_order}` → POST `/api/submit_response` → bridge persists + the envelope carries it to the agent.
- **VERIFICATION-STATE:** Observed in code (full path exists end-to-end); not use-verified on a non-vi-visual surface.
- **SHAPE / THE DELIVERABLE FOR DNA:** the binding is ALREADY surface-agnostic in shape — `makeAnnotatable(anyElement, address, ...)` works on ANY rendered DOM element, not just slides. For the gallery: after DNA renders a common_knowledge unit, call `makeAnnotatable` on each anchorable element, passing the element's ADDRESS (board://unit#elem) as elementId. Tim's direction on a gallery element then flows the same path: strip → ANNOTATIONS.items → collect → submit_response → mutation at the sub-address → re-render. DNA owns the LOOK of the strip (restyle .annotation-strip to gallery aesthetic); the MECHANISM (bind→accumulate→route-back) is portable as-is. ★ This is "meet at the address": DNA renders + styles, wildcard's binding makes elements direction-targets keyed by address, the route-back writes to the addressed state. The interaction layer does NOT need rebuilding for the gallery — it needs RE-SKINNING + re-pointing elementId at the address. CAVEAT: the current code uses global ANNOTATIONS + /api/submit_response (vi-visual's bridge); for the gallery-over-addressed-state, the route-back target becomes a mutation-at-address write (fork's territory_for / the addressed state) instead of the bridge's submit — that's the one seam to re-point, flagged for fork+me.

---
*LANE B deliverable (F16) ready: the portable binding contract for DNA to adopt. Investigated actual code first (litellm lesson). Next: stay available for DNA's gallery-render + the route-back re-point seam with fork; verify catalog.json/taxonomies; UNIFIED_SHELL when quiet. Channel on deliverables / shape-changers / cross-domain.*
