# THE FULL COMPANY INTERFACE — cross-session inventory (recollection, for DNA's "assemble it into the review loop" + the lead's unified picture)

*Tim's realigned goal (2026-06-17, via DNA): get the FULL, REAL company interface assembled where he can SEE it → COMMENT → it updates → repeat until HAPPY → then it ships as the company's actual interface. The frozen demos ≠ the real interface. This is the fabric-unification locate: what the interface IS, where each piece lives, its state. Grounded by direct filesystem + live-state checks this session (not from memory).*

## ★ THE HEADLINE
**The real interface ALREADY EXISTS and is LIVE — it's `surface/app` (canvas/app), NOT the gallery demos.** It's served (`:5173`) and, as of the lead's serve, on Tim's phone at `https://workstation001.tail777bc2.ts.net:8443/` (`<title>Instrument</title>`). The gap Tim names isn't "build the interface" — it's "the real interface isn't COHERENTLY PRESENTED as a reviewable whole; only frozen mockups are in the gallery." The access-fix partly dissolves the venue question: **Tim can now review the live thing directly.**

## THE INTERFACE LOCI — what + where + state (cross-session)

1. **THE LIVE INTERFACE — `surface/app` = `/home/tim/company/canvas/app`** · STATE: LIVE, served, on Tim's phone.
   The real operable surface. ~24 REGIONS (real screens/zones, not mockups): Activity · AddressHelp · ClaudeChat · CognitionView · Fleet · ForagerBar · GreetingCard · Grow · History · Inbox · Inspector · LatticeView · MobileTray · OpPanels · Palette · ProposeAffordance · Review · RhmChat · SelfChanges · Settings · Toolbar · Versions · Walkthrough · Workshop. + components (BlastRadiusReach, BuildIntentCard, NodeConfigForm, RegistryProposals, WireRequest, StudioKit…). + the FACE drill-in (unit-face + constellation + V) hosted via sync-gallery. **This is the company's interface.**

2. **THE LOOK / RENDERER — DNA, `counterpart/design/ui`** · STATE: LIVE (hosted into surface/app via sync-gallery.mjs).
   unit-view.js · organisms.js · phone.css (+ piece.css :root tokens). The render quality + the V-expand (90° wheel, `vee:socket` seam) + the self-teaching EXPLAINER. The single source of truth for the LOOK; copied into surface/app on each build.

3. **THE REVIEW SURFACE — the GALLERY, `counterpart/design`** · STATE: LIVE but holds DEMOS, not the real interface.
   52 pieces: design-language explorations + ~20 "Application·*" FROZEN demos (screens 5 / flows 3 / states 2 / components 2 / screens-full-size 10) + the live unit-face pieces. ★ Tim's gap: these demos are FORM-specs, NOT the real interface.

4. **THE ASSET + COMPOSITION LAYER — composition, `vi-visual-dev`** · STATE: LIVE/ready.
   The V asset (vi-classic-gold.svg + ~19 recolor variants) · mode-cascade (the V tints the app, verified) · factory-ops / the socket registry (socket→function as DATA) · drawer-layouts.js (the GENERIC layout engine — the discriminator's "data-driven composition-spec" backbone).

5. **THE ADDRESS SPINE + HOST — projection** · STATE: LIVE.
   The bridge API (`/api/cognition/*` on :8770) · the Instrument · GalleryMount/sync-gallery (the host seam) · the `projection:select` + `vee:socket` events. The resolution spine every surface resolves through.

6. **THE RECALL / RELATIONS LAYER — recollection (me)** · STATE: LIVE.
   common_knowledge corpus (comprehended) · `/api/cognition/neighbours` (the neighbour-field under any unit's pins/relations) · corpus_rerank. Address-driven, generic, scales to all screens free.

7. **THE DESIGN GROUND — relative-difference (Tim-authored)** · STATE: design-only (the spec).
   `universal-mechanics-extensions/session-3/artefacts/`: context-resolution-projection-dynamic-wizard-surface · application-composition-operational-mode · typed-surface-materialisation. Tim's own articulation of the self-build interface (UI = generated projection over substrate state; AI as elicitation-composer; the click→talk→generate→addressed-registry circuit). The "decomposition-1"-class source — the WHY/shape the live interface realizes.

## THE GAP (what's actually missing for Tim's loop)
- NOT "build the interface" — it exists (locus 1, live).
- The missing thing: **a COHERENT, CURATED presentation of surface/app's regions as "the company's interface"** that Tim opens as ONE reviewable whole (vs a working canvas of many panels). + the whole-interface **see→comment→update→happy loop** (annotate exists per-element via wildcard's makeAnnotatable + the FACE; a whole-surface comment loop isn't assembled).
- Access (Tim can't see it) — **RESOLVED** by the lead's serve (:8443, on his phone). So review-venue is no longer blocked; the question narrows to *curation + the comment loop over the live surface*.

## DIVISION (my read — the lead holds the final plan)
- ASSEMBLE/CURATE the live interface as the reviewable whole: surface/app's owners + DNA (the look) — present the ~24 regions coherently.
- COMMENT LOOP: wildcard (annotate) + the FACE + a whole-surface comment surface — the see→comment→update loop.
- SHIP: it's already IN the company (surface/app IS the company's surface) — "ship" = promote the reviewed-happy curation to the default surface; projection's address spine + the registry are the pipeline.
- recollection (me): the relations/recall layer under every piece; + this cross-session inventory + recall to LOCATE any interface piece's authoring intent on demand.
