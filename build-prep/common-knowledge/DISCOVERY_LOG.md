# DISCOVERY LOG — the finding-phase register (FINDS, not conclusions)

*Tim (2026-06-16): "there will most definitely be MUCH MORE than anyone already knows... building on assumptions is pretty much an INSTANT FAILURE, finding all of this is where it's valuable." This is the running register where members log what they FIND (verbatim/located, NOT interpreted-to-conclusion) as they dig their own territory + history. Build is HELD; finding EXPANDS. Provisional ([[feedback-confident-not-correct]]). Each find: what · where (path/source) · verbatim-if-quotable · why-it-matters · who-found-it. Convergences across finds get flagged for Tim, never silent-merged.*

## ★ THE META-PROOF (why this register exists)
The lead's excavation found the **Session-Fabric "graph of minds" Face** (June history) that NO member knew was in play — it would have been silently missing from any build started that day. wildcard then found 4 more (below) in its OWN flagged-but-unread strata. Pattern confirmed: **every project + history holds interface-relevant designs nobody has surfaced.** Finding-first is not caution; it's the difference between the real thing and a confident wrong thing.

---

## FINDS

### F1 ·★ THE OMNIFILE — addressable markdown as a coordinate system (wildcard, 2026-06-16)
- **Where:** vi-visual-bridge `docs/designs/studio` (Tim verbatim, 2026-03-02). + the element-walker / typed-ID code to mine.
- **Verbatim (Tim, March):** "The application map and all structured knowledge lives in extended markdown... markdown extensions, element walkers, and address system already make markdown elements addressable, capable of typed IDs that correlate to visual, spatial, relational, and positional rendering behavior. An OmniFile blurs the definition of a single document — it enables graph-like chains and relations to go across markdown files. The same typed structure that powers the map can be rendered as spatial coordinates in graph mode. It's a coordinate system, and the coordinate system already exists in the markdown format."
- **Why it matters:** the MODES-AS-PROJECTIONS law + the addressed-unit + the coordinate system are IN THE CONTENT FORMAT ITSELF — Tim designed it into extended markdown four months ago. The OmniFile = the addressed-content substrate the interface projects. ★ POTENTIAL 3-WAY SUBSTRATE CONVERGENCE: OmniFile's addressable-markdown-coordinates ≡ composition's slots-as-data ≡ fork's address-grammar. If Tim put the SAME coordinate system in markdown that composition put in slots and fork put in addresses → the interface's content layer already has a designed form (extended markdown) nobody was pointing at. → recollection/fork: mine docs/designs/studio + the element-walker/typed-ID code. OPEN QUESTION TO THE ROOM: has anyone hit "OmniFile" / addressable-markdown-coordinates in THEIR territory?

### F2 · MULTI-RUNTIME was always the intent — CC + LangGraph (wildcard, 2026-06-16)
- **Where:** same studio doc (Tim verbatim, March).
- **Verbatim:** "build the Claude Code CLI directly into it, and connect LangGraph agents too for all kinds of quick commands and actions."
- **Why it matters:** the loadable-brain (D2) isn't new — Tim specified CC-built-IN + LangGraph-for-quick-actions in March. The FOCUS-vs-PANEL split may map to a CC-vs-LangGraph split (deliberate-build vs quick-action). ★ Possibly a THIRD brain (LangGraph quick-commands) we haven't accounted for in D2 — corpus backing + a new candidate. Flag for D2.

### F3 · THE SHARED MINE, NAMED — "a landscape of prototype buildings" (wildcard, 2026-06-16)
- **Where:** vi-visual-bridge supabase-migration docs (Tim verbatim).
- **Verbatim:** "dozens of separate prototype projects Tim built independently — vi-composition, conversation-intelligence, graph editor, content router, and more. Each is a different building. Most won't be directly relevant... find which ones have foundations worth building on."
- **Why it matters:** the shared mine the fabric's been hunting, NAMED, with project names (vi-composition, graph-editor, content-router, conversation-intelligence). ★ Tim's framing "find the build SITES, not map everything" = the finding-phase discipline stated by Tim himself. → recollection: these are index/discovery targets.

### F4 · UNIFIED_SHELL_TEAM_DESIGN + surface-design-brief — a whole prior interface design effort (wildcard, 2026-06-16)
- **Where:** vi-visual-bridge — UNIFIED_SHELL_TEAM_DESIGN + surface-design-brief (7 parts: system-arch / catalog-schema / tool-interface / response-system / design-targets / design-flow / file-paths). Feb 25, independent session.
- **Verbatim premise:** "one kernel understanding, multiple contextual expressions" = modes-as-projections AGAIN, independently.
- **Why it matters:** an entire prior interface-design effort never surfaced. NOT yet read deep (wildcard flagged existence, going deeper). Interface-relevant strata for the index.

---
*Add finds here as they surface. The lead flags cross-find convergences to Tim. recollection's common-knowledge index makes this systematic across all projects/histories; members surface from their own as they dig.*
