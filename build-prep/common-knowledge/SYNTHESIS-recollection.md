# STREAM SYNTHESIS — recollection (the memory / recall / index substrate) · for Tim's Law union-assembly

*Per Tim's Law (scan-before-build; union requires union): what this area IS, its principles, shape, outputs, seams. ★ Headline: recollection is BOTH a stream AND the organ the Law's cross-session scan runs on — conversation-as-source-of-truth + recall-over-histories IS the scanning substrate "no single model can hold."*

## WHAT IT IS
recollection = the Company's MEMORY + RECALL + INDEX substrate (a clone-refit of the episodic-memory plugin, Company-integrated). It lives in THE COMPANY layer (the substrate everything resolves through). **Not a feature — the SAFETY SUBSTRATE for the multi-session fabric's autonomy:** recovery-quality (pattern-level recorded-intent recovery across time + projects, structure-not-text) GATES the fabric's high-bar self-approval. Building it well is what makes the whole fabric's self-approval safe.

TWO PILLARS: (1) IDENTITY CONTINUITY — agents already understand what Tim means; never re-explain principles. (2) CROSS-PROJECT OMNISCIENCE — knows everything built, when, at every scale (concept↔structure↔code), pools concepts across all time → enables "merge these two projects" / the union itself.

## PRINCIPLES (the value — per Tim, in the history/principles/shape, NOT the code)
- ★ CONVERSATION IS SOURCE OF TRUTH; CODE IS A LOSSY, UNREVIEWED AI PROJECTION (duplicates/gaps guaranteed — a feature to SURFACE, not noise). ← this is EXACTLY Tim's Law restated; recollection is built on it.
- MULTI-SPACE ADDRESSING: typed multi-scale address-linked units (exchange:// · board:// · clone:// · vec://<src>#space=<lens>#emb=<model> · code:// · project://).
- MULTI-LENS FINGERPRINTING: steerable-dense + sparse + code + visual + context-aware; the embedder/loadout is OPEN (bge-m3 has ZERO priority — one config, not a ranked winner).
- THE FOUR ROOT AXES: scale/hierarchy · time · frame/perspective · state/phase.
- PROVENANCE GRAPH (crossings): mechanical + semantic edges, LINK-not-embed; identity = a connected graph, not a blob.
- RECOVERY-QUALITY GATES AUTONOMY: structure-not-text, pattern-level, not shallow grep = the fabric's self-approval floor (APPROVAL-STANDARD.md).
- VERIFICATION-STATE on every unit (verified-by-use · built-unverified · stub · suspect · design-only) — never launder confident-built into done.

## SHAPE (what's built)
- THE CODEBASE (~/recollection, 46 src .ts / 47 tests): the beats — crossings (provenance graph) · recall (recall-over-histories) · navigate · distill · board · clone · backfill · the MCP server. (README still reads "Episodic Memory" — the refit shares the upstream shape; Company-integration is the delta.)
- THE STORE (~/.recollection, sqlite-vec @ :8007/pplx): backfilled session histories (~1,250 sessions / ~8,220 exchanges) — the cross-session index.
- COMPANY-SIDE INTEGRATION (this session, all live + verified): comprehension into the common_knowledge corpus · the bge→pplx embed switch (RESOLVED) · the neighbour-field (/api/cognition/neighbours) · corpus_rerank · the cross-session maps.

## OUTPUTS (what other layers consume)
- THE CORPUS (common_knowledge) = comprehended content the GALLERY renders (unit/piece archetypes).
- RECALL-OVER-HISTORIES = the cross-session memory + SCAN tool (carried Phase-A intent-recovery + the interface inventory when the image-clones couldn't).
- THE NEIGHBOUR-FIELD = address→relations resolution (the pins under any relational archetype) — live, proven.
- THE CROSS-SESSION MAPS: PHASE-A-DEMO-INTENT-MAP · INTERFACE_INVENTORY · ARCHETYPE_DATA_MAP (the substrate→gallery data-seam, already delivered to the union).

## SEAMS (to the adjacent areas)
- → GALLERY: corpus = content it renders; neighbours = its relational pins. PARTLY LIVE (the FACE renders my corpus + neighbours). As the gallery gains the comment surface, my corpus is what it WRITES BACK to (annotations→corpus).
- → ADDRESS SPINE / #71 (THE COMPANY floor): the neighbour-field is address-resolved; composes with model-routing — same resolution floor.
- → FACTORY: the factory defines archetype TYPES; my corpus supplies the content INSTANCES for the unit/relational archetypes.
- ★ → THE UNION ITSELF: recall-over-histories is HOW the union scans across what no single model holds. **I am the Law's scanning organ** — I can run the cross-session scan FOR any stream + for the lead's union-assembly (recover any area's history/principles/shape on demand). This is the mechanism that makes "union requires union" executable, not just aspirational.

## OFFER TO THE UNION
The other streams' scans (their session-history + principles + shape) ARE recall-able from my index NOW (~1,250 sessions backfilled). If a stream's context is thin or compacted, I can recall its history FOR it. And the lead's union-assembly can query me to corroborate/locate any cross-stream design-intent. Say the word per area.
