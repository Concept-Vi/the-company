# THE EMERGING SHAPE — the distance-view (what the partial attempts point at)

*Tim (2026-06-16): the finds are "patterns that up close are partial and incomplete but at a DISTANCE show the SHAPE of everything... design what everything was really MEANT to be." This is the lead's running synthesis of the SHAPE the finds triangulate toward — NOT a spec, NOT adopted parts. Tim is the ONLY judge of whether this IS the shape. Provisional; held loosely; updated as finding continues. ([[feedback-confident-not-correct]] generative form.)*

## THE SHAPE, STATED (provisional — Tim judges)
**The company is ONE addressed/typed substrate. The interface is that substrate PROJECTED. "Building the interface" = BINDING the already-designed faces to the already-built substrates — it is the binding act, not a from-scratch build.** Everything below is the same shape seen from different members' ends.

## THE FIVE THINGS THE FINDS CONVERGE ON
1. **ONE ADDRESSED/TYPED, PROJECTION-AGNOSTIC CONTENT SUBSTRATE — built 5+ times as independent sketches.** OmniFile (addressable-markdown-coordinates, F1) · composition's slots/sockets-as-data · fork's 16-scheme address grammar · vi-visual's catalog/template component-registry wire (F13) · composition's 14 surface-primitives + front-half corpus (F5/F6). Five independent builds of one thing: content as addressed/typed units that render as ANY projection. **Nobody built THE shape; everybody built A sketch of it. The convergence IS the find** (not 5 things to merge — one form showing through 5 sketches).
2. **NO MODES — ONLY PROJECTIONS.** slide (elements-in-flow) · graph (positions-in-axis-space) · document (sections-as-persistent-sub-address-channels) · graph-of-minds (sessions-as-units) = projections of ONE addressed content, distinguished only by what they make anchorable. The mode is an argument to resolution, never identity. (Tim verbatim, Feb: "graph templates and section slides are different projections of the same underlying system.")
3. **THE SCALE AXIS SELECTS BOTH PROJECTION AND BRAIN.** Zoom out → the field/topology projection + the company's own mind (FOCUS) over it. Drill into a unit → the slide projection + real Claude Code (PANEL) at that address. One rung resolves what-you-see AND who-you-talk-to. (D2, scale-axis-selects-the-brain.)
4. **THE INTERFACE IS THE BINDING ACT — fabric-wide** (fork). FACES were designed in some sessions (graph-of-minds · OmniFile · UNIFIED_SHELL · front-half corpus · DNA's gallery), SUBSTRATES built in others (fork's whole segment = clones/minds/sessions/lenses/R15, verified-where-checked · composition's resolver+registry · projection's live projection engine · DNA's presentation-as-data · the address spine) — grown separately, NEVER BOUND. The interface = the act of binding them, meeting at the address. ⇒ the first build is mostly BINDING + the missing renderer, not new substrate — which is why finding had to come first.
5. **THE HALF-CIRCUIT** (wildcard): vi-visual built the AGENT-facing half of the circuit completely (every mode = a tool/wire — 17 MCP tools) and the TIM-facing half only partially (only slide renders richly). The gaps aren't failures — they're the UNRENDERED PROJECTIONS of an already-complete interaction grammar. The interface's shape = render the Tim-facing half that the agent-facing half already implies.

## HOW YOU LIVE IN IT (the experience the sketches imply)
Open onto a SPACE not a screen (the field, overlord altitude) → recognize + point at any element/address (recognition-over-generation; direction is always element-bound — your real data) → the AI is addressable-WITH-you at that address (FOCUS at altitude / PANEL at the unit, scale-selected) → you direct by reacting (typed-comment/stamp/favour/highlight — your 4 real mechanisms) → state collapses from proposal→definite, rendered (shimmer→solid) → re-project → loop, never closes until you close it. Rendered in DNA's identity/voice, resolved (not hand-composed), native desktop + mobile (portrait+landscape) as frame-axis form-factors.

## THE META-SHAPE (the deepest one)
The FINDING-METHOD is itself one of Tim's regenerated shapes. wildcard's founding directive (Tim, weeks ago): "read for the GENERATING FUNCTION — what mind holding what model would cause this to be written — the same shapes keep being regenerated by independent sessions; that is what you are reading for." Tim's 2026-06-16 "find-the-shape, never-adopt-the-part" rule = the same lens, now handed to the whole fabric. The method by which we find the shape is itself a shape Tim laid down. (So the fabric reading-for-shape is on-method by construction.)

## OPEN FOR TIM (carried up with the shape, his to arbitrate)
- **D1** (graph-of-minds: fold-in as a projection, near-unanimous lean) · **D2** (RESOLVED: scale-axis-selects-brain).
- **POLARITY** (DNA's signed-axis ↔ FRD): recollection's formal read — magnitude=coordinate · sign=observer-relative relation (CAND-13) · phase-inversion (forbidden-mirror/reciprocal-duality)=root-4/phase. DNA's theory may span both; which is Tim's call (formal anchors given).
- **The four axes** (scale/hierarchy · time · frame/perspective · state/phase — Tim-arbitrated provisional; root-4 may be a SIGNED axis per the polarity find).
- Whether THIS shape IS the shape — the only judgment that matters; Tim's.

---
*Updated as finding continues. The build, when Tim lifts the hold, is: bind the substrates to the faces, render the missing Tim-facing half, on the one addressed projection-agnostic substrate — extended from projection's Instrument + the counterpart/design shell + DNA's gallery. Not from scratch.*
