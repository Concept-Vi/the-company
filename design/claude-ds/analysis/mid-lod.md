# Analysis · `company-info` (18pp) + `presentation-15p` (15pp) — mid-LOD confirmation

> Status: ☑ analysed (strategic sample). **6th–7th folders — mid-LOD redundancy check.**
> Verdict: **fully confirms the model; no new grammar.** One valuable refinement to the
> diagram finding (state-morph is a first-class, animatable transform).

## Confirms (nothing contradicts; 7 folders now consistent)
- Same DNA at 16:9: zoning ladder, gold→bronze ramp, frame signature (V mark, hatch), triangle/→ bullets, blueprint ghost, soft panels, thin icons, number+label, octagon/hex Vi-Hub badge.
- **Mid-LOD sits exactly between terse and high-detail** as the LOD axis predicts: fuller than `deck1-2026`, lighter than `recent-pitches`. Same archetypes (split, network diagram, hub, flow, compare-pillars, metric).
- **Containment + per-container collapse** behave as modelled; multi-section slides at mid density.

## ★ Refinement: the diagram "state-morph" is a first-class, animatable transform
`presentation-15p` p-06 & p-11 and `company-info` show the **same hub diagram in two states side
by side with a ▶ between them**: (1) a **fully-connected tangle** (every node↔every node, ramp-coloured
edges) → (2) the **same nodes routed through a central hub** (Virtual Tours / Vi octagon), edges now
clean radial. This chaos→order **morph** recurs across multiple folders (the deck's "before/after"
visual rhyme) and is shown *explicitly* here as two frames + arrow.
→ This is a **rule, not a one-off drawing**: "same node-set, re-route edges through a hub" is a
deterministic transform on a `{nodes, edges}` graph. It is exactly what the **diagram generator**
(D4) should produce, and it is **inherently animatable** (edges retract from N×N to radial) — a
prime motion component. Confirms: diagrams are *generated from a spec + a state*, not authored.

## Other small confirmations
- **Octagon Vi-Hub / hexagon Property-Wizard** shape-coded badges recur (shape = semantic), often the morph's center.
- **Flow stepper** (Networks→Developers→User Portal→Courtesy Tours→Sales→Organic awareness) — the same horizontal flow/stepper atom, here with octagon nodes + `+` join.
- **Map locator + big-stat hatch footer** ("99.8% … fewer than 20 employees") reused.
- File-extension "data-exhaust" list + product URL + refresh-cycle glyph reused (the "we ingest any file" motif).

## Diff
- ✅ **7 folders consistent — model is comprehensively validated.** Mid-LOD adds no new structure (expected).
- ✏️ **Refines D4:** diagram = `{nodes, edges}` + **state** (tangle ↔ hub-routed); the chaos→order **morph is a deterministic, animatable transform** → generator + motion component.
- ➕ Minor: octagon/hex shape-coded badge confirmed as a reusable atom; flow-stepper variant with join node.
- ⚠️ No contradictions.

## → Corpus complete
All 12 source folders analysed (see PROGRESS.md). Model holds across 3 surfaces · 4 LOD rungs ·
3+ ratios · static+interactive, with zero contradictions. Ready to move analysis → synthesis
(SYNTHESIS-PLAN.md), starting with the 🟢 token recalibration.
