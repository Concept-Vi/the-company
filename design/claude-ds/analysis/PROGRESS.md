# Analysis progress

Resumable tracker. One row per folder. Update after each session.

| Folder | Pages | Ratio | Type | Status | Analysis doc |
|---|---|---|---|---|---|
| pitch-deck | 17 | 16:9 (1930×1085) | presentation (clean) | ☑ analysed | analysis/pitch-deck.md |
| deck1-2026 | 16 | 16:9 | presentation (canonical?) | ☑ analysed | analysis/deck1-2026.md |
| company-info | 18 | — | presentation | ☑ analysed | analysis/mid-lod.md |
| presentation-15p | 15 | — | presentation | ☑ analysed | analysis/mid-lod.md |
| capital-raise | 30 | 3:2 (1893×1287) | capital-raise pitch (deepest LOD) | ☑ analysed | analysis/capital-raise.md |
| recent-pitches | 49 | 3:2 (1893×1287) | presentation (dense) | ☑ analysed | analysis/recent-pitches.md |
| vt-architects | 4 | A4 portrait (910×1287) | print one-pager | ☑ analysed | analysis/vt-family.md |
| vt-gatehouse | 3 | portrait | print one-pager | ☑ analysed | analysis/vt-family.md |
| vt-property | 2 | portrait | print one-pager | ☑ analysed | analysis/vt-family.md |
| vt-residential | 2 | portrait | print one-pager | ☑ analysed | analysis/vt-family.md |
| landing-mockups | 3 | tall web (1223×4630+) | web surface | ☑ analysed | analysis/landing-mockups.md |
| vi-onepager | 1 | 16:9 (966×543) | summary | ☑ analysed | analysis/vt-family.md |

**Legend:** ☐ not started · ◐ in progress · ☑ analysed · ★ synthesised into system

## Session log
- *(s1)* — set up analysis scaffold; sampled 5 images across types; drafted method. Awaiting go-ahead on first folder.
- *(s2)* — **baseline complete: `pitch-deck` (17pp)**. Viewed all pages; sampled real pixel values (zoning ladder + gold ramp via hue clustering); measured structural ratios (12% margins, 7.5% title top, 76% band). Wrote full `pitch-deck.md` + logged confirmed gaps. Headline finding: zoning is a ~1–3% warm/neutral undertone ladder (NOT hue-coded) — recalibrate `--zone-*`; gold is a ramp (`#d6bf57`→`#c09d5d`→`#b98664`), soften `--accent-gold`. **Next:** test these against a 2nd folder (`deck1-2026` or `recent-pitches`) before synthesising.
- *(s2 deep pass)* — added `pitch-deck.md` §10–14: narrative architecture, reading order, constant-vs-variable DNA, **texture & depth** (3 background textures; warm-soft z-stack; frosted glass; translucent layered panels), and **unnamed findings**: status traffic-light set, embedded product-UI vocabulary, connector/"2-way" language, number+label convention, gold-underline, shape-coded badges, trust strip, photography art-direction.
- *(s2 third pass)* — added `pitch-deck.md` §15–21: **rules between elements** — modular type scale (~1.25 + display jump), balanced titles, hanging-indent bullets, number-formatting convention; **colour-role logic** (ink=content / gold=active / bronze=structural) + contrast-as-hierarchy; fine grid (padding/gutter ratios, invisible 4-corner frame); **latent motion** (directional-cue set, entrance=reading order); **restraint guardrails** (the ABSENT-list / anti-patterns); master-template + copy content-model; responsive-fragility list. `pitch-deck` now analysed to depth across **three passes** — strong baseline. **Next:** `deck1-2026` to confirm/extend, then `recent-pitches` (dense 3:2).
- *(s3)* — **2nd folder: `deck1-2026` (16pp)**. Confirms baseline: zoning ladder (`#fdfcf7` exact match) + gold ramp (`#d6bf57`/`#c09d5d`) **identical → promoted to CONFIRMED/synthesis-ready**. New dimension: **register/purpose** — `deck1-2026` is the *terse, motion-ready, presented* cut of the same content `pitch-deck` carries as *verbose, static, leave-behind* (GIF placeholders, headline bullets, 16 vs 17pp, 2× res). → templates need a register switch (content-model + density + motion-slot). No contradictions. **Next:** `recent-pitches` (dense 3:2) to stress-test the ladder at high density + a new ratio.
- *(s3 deep comparative)* — mined the *diff* between the two cuts (deck1-2026.md §A–I): **space↔time complexity trade-off** (density vs motion are one control); **pace-controller determines register**; **LOD is an axis orthogonal to surface size** (the mechanism for one-source→many-outputs); **invariant skeleton (numbers+diagrams) vs flexible prose**; **bullet={claim,support}**; **motion-placement rule**; **narrative-priority/auto-shorten**; conserved ink-budget; deck1-2026 = canonical master.
- *(s4)* — **3rd folder: `recent-pitches` (49pp, 3:2)** strategic sample. **Stress test passed:** zoning/gold/12%-margins all hold at 3:2 + high density → **ratio is independent of density/LOD** (resolves AXES open question). LOD ladder gains a 3rd rung (high-detail/long). **Proof slides are templates:** p-25 & p-49 = same "Our Entry Markets" layout, different data. New atoms: QR phygital card, `→` flow-bullet (glyph = semantic), quadrant/positioning map, team/org grid, geo locator, multi-section-per-slide, device-channel strip. No contradictions. **Next:** `vt-*` one-pagers (tight end of LOD ladder) or `landing-mockups` (web surface).
- *(s5)* — **4th folder: `landing-mockups` (3 tall web pages, 1223×4630+)**. **Web-surface stress test passed:** containment tree, zoning ladder, gold ramp, atoms, narrative arc all transfer with zero new grammar. Slide→section band; sequence→scroll; zoning→alternating band backgrounds (+ dark footer). **Surface axis now spans paged↔scrolling** (Slide = paged-or-stacked band). Audience-variant unified: variant slides (recent-pitches) = variant pages (p-2/p-3) = live toggle (p-1). New web-chrome atoms: persistent nav, segmented audience toggle, comparison-pricing table, repeated CTA band, scroll-cue, content-LOD placeholder boxes. No contradictions. **Next:** `vt-*` one-pagers (tight LOD end + A4 print) or mid-LOD `company-info`/`presentation-15p`.

- *(s6)* — **final corpus gap closed: `capital-raise` (30pp, 3:2)** — the deepest-LOD pitch (incl. a photographic Appendix). Re-confirmed the whole model a 4th time (ramp `#cdbf5a`/`#af8b4f`/`#a77146`, 12% margins, shape-coding, ratio⟂density⟂LOD, zoning=depth) with **zero contradictions**. Surfaced NEW grammar now logged: **running-sentence titles** (the title rail is one argument via leading connectives — strongest narrative rule yet), **product-surface bookends + frosted overlay**, **full-bleed photo divider**, **clean-white ground sub-mode** (#ffffff vs ivory), **sage-green communication accent** (#9fbc73, p5 legend), **5 new diagram sub-types** (orbital verb-ring · stacked/expandable pipeline node · 1-D spectrum axis · manifold/converging-summation · progressive-fidelity vertical stepper), **ramp-bordered card row**, + atom growth (logo wall, partner band, advisory icon row, circular-image grid, two-tone-icon stat row, bidirectional pointer bullets, inline wordmark, outline panel). Model + DIAGRAMS + DESIGN-LANGUAGE updated; build of the new sub-types into DiagramSolver/archetype-catalog queued. **The corpus is now fully analysed (12/12 folders).**

### Build sessions (analysis complete; corpus frozen — these BUILD on the model)
- *(build s1–s3)* token recalibration + atom tokens + the generative core (block & graph solvers on the shared node type). See `FINDINGS-LOG.md`.
- *(build s4a)* — **the generative archetype & template layer.** Grew the block solver into an extensible atom registry (+ hero-number / note / badge / qr / image roles); grew the graph solver (octagon shape + ramp-tinted chevron `stepper`); built `core/Slide.jsx` — the 13 corpus archetypes as pure `content→tree` builders (`design = f(content, axisPosition)`); shipped the first real template `templates/pitch-deck/PitchDeck.dc.html` + the `slide-archetypes` card. Audit found the app's `WS_LAYOUTS`/`WS_BLOCKS` is a parallel archetype system to unify. → `FINDINGS-LOG.md` slice 4a.
- *(build s4b — UNIFICATION W1–W2)* — **welded the two disconnected halves.** Found (via `grep`) the type system + Vi generator (`app/registry/*`) and the rule engine + solvers (`core/*`) had ZERO references between them, with THREE parallel archetype lists. Wrote `analysis/UNIFICATION.md` (the map + canonical decision + W1–W5). Built `core/RenderType.jsx` (the `typeToNode` bridge: any CV_REGISTRY Type → solver IR → one engine) + `core/archetype-catalog.js` (single-source catalogue read by both halves); folded `Slide` onto `RenderType`; the app seeds archetypes from the shared catalogue, `title/content/section` become `extends` aliases, `WS_LAYOUTS`/`WS_BLOCKS` annotated with their canonical Type/atom. Verified via harness; clean. Remaining welds W3–W5 documented. → `FINDINGS-LOG.md` unification pass.
