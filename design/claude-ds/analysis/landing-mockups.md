# Analysis · `landing-mockups`

> Source: `ingest/landing-mockups/` · 3 pages · `_ingest/landing-mockups/`
> Dims: 1223 × {4630, 6967, 7334} — fixed-width, **scrolling web** surface.
> Status: ☑ analysed · **4th folder — the WEB-surface stress test**
>
> Verdict: **the model transfers to a scrolling web surface intact.** A "slide" becomes a
> "section band"; the deck's vertical *sequence* becomes *scroll*. Containment tree, zoning,
> gold ramp, atoms, and the narrative arc all carry over with no new grammar.

## Stress-test result: does the containment tree survive scroll?
**Yes — scroll is just the Deck→Slide level re-rendered as stacked bands instead of paged frames.**
- **Slide → section band.** Each former "slide" becomes a full-width horizontal band; the page is a vertical stack of them. The **Section/Zone/Group/Atom** sub-tree inside each band is unchanged.
- **Sequence → scroll.** The narrative arc (hero → problem → solution → proof → pricing → CTA) is identical to the decks' arc, just traversed by scrolling rather than paging. *Motion = temporal traversal* becomes *scroll = spatial traversal of the same tree* — the same ordering.
- **Zoning = band backgrounds.** Alternating bands use the near-white ladder to separate sections (white ↔ warm-ivory ↔ neutral-grey), plus a **dark footer band** (the one strong-dark surface, consistent across all 3 + the decks' contact slides). Zoning-as-containment-depth holds: band undertone marks section boundaries without rules.
- **Atoms transfer 1:1.** Icon-tiles ("Visualise / Inform / Communicate…"), stat row (100%/80%/89%/100%), trust strip (logos), gold pills (Architects/Developers toggles), QR + virtual-tour tiles, the 4-stage timeline, a **pricing comparison table** (new for web), CTA buttons. All the deck atoms reappear.

## What's NEW on the web surface (genuinely additive)
- **Persistent top nav + segmented audience toggle** ("Architects | Developers" gold pills) — a web chrome the fixed decks lack; it's the interactive form of the deck's "audience-variant" idea (recall recent-pitches p-25/49 variant slides → here it's a live toggle).
- **Hero band** with logo + headline + audience pills + a **down-chevron scroll cue** (the deck's directional-cue set, now functional).
- **Placeholder/wireframe state:** grey boxes labelled "Guided tour here would be ideal", "Team picture (everyone together)", "Banner with 3-4 Steps" — these are **content-LOD placeholders** (the page is authored at a structural LOD before assets land). Direct evidence of the *template-with-typed-slots* model on web.
- **Pricing comparison table** — a denser data atom (feature × plan grid with ✓/✗, gold-outlined "recommended" column) not needed in the decks.
- **Sticky-ish CTA bands** ("Sounds good?", "Get in touch") — repeated conversion sections; the web equivalent of the deck's closing contact slide, used more than once.
- **Footer** — dark band, contact + legal; consistent across all three.

## Confirms / extends
- ✅✅✅✅ **Confirmed across 4 folders:** gold ramp, near-white zoning ladder, frame/footer signature (V mark, dark contact band), triangle bullets, thin icons, soft rounded panels, blueprint-free but same warm photographic hero, the narrative arc, generous negative space.
- ✏️ **Extends AXES.md:** **surface now spans paged (slide) ↔ scrolling (web)** — same containment tree, different traversal (page vs scroll). The "Slide" node generalises to a **"band / frame"** that is either paged or stacked. Width-fixed responsive (1223) → the mobile reflow rules (stack, pill-wrap, grid-fit) are exactly what a narrower web viewport needs.
- ✏️ **Audience-variant** is one mechanism across surfaces: variant *slides* (recent-pitches) = variant *page* (p-2 developers / p-3 architects) = live *toggle* (p-1). Same {template + audience param}.
- ➕ **New atoms:** persistent nav, segmented audience toggle, comparison-pricing table, repeated CTA band, scroll-cue, content-LOD placeholder boxes.
- ⚠️ **No contradictions.** The web surface needed zero new grammar — only web-chrome atoms.

## → Gaps / AXES updated
Surface axis spans paged↔scrolling (Slide = paged-or-stacked band); audience-variant unified across surfaces; web-chrome atoms logged.
