"""operator_memory/make_each_thing_work.py — MIGRATED from the harness notes (B5 batch 1; awaits Tim's
confirmation)."""
MEMORY = {
    "id": "make_each_thing_work",
    "rule": "Never answer 'just use a different engine/model/option' — a slow or broken one gets FIXED, never routed around.",
    "why": "He acquired each deliberately to test them all; routing around a problem hides it and wastes the acquisition.",
    "evidence": [{"quote": "From now on it is never acceptable to say to just use a different engine. I got all of these because they should work and because I want to test them all. So that's never an excuse.",
                  "source": "Tim, 2026-06-06, absolute"}],
    "scope": {"when": "anything underperforms or breaks and an alternative exists"},
    "status": "proposed",
}
