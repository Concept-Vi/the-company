# THE DATA-DRIVEN DEFAULT — no hardcode: sectors ARE the distinct kinds present in the store.
BINDING = {
    "id": "raw",
    "label": "Kinds — raw (data-driven)",
    "angle_from": "kind",
    "radius_from": "time",
    "order_by": "count",
    # THE HUMAN MEANING this view carries (registry-true — the instrument READS + renders it, stays empty of
    # meaning itself). SEED field-set name/is/fills/why (composition's legibility shape, ch-2mnxl9j0). TENTATIVE
    # draft copy for Tim/DNA to ratify — the field-set is journey-gated (OPERATOR-SURFACE-LOOP.md OQ1–4). The
    # operator NEVER sees machine names; this is the human "what am I looking at" for the Kinds view.
    "meta": {
        "name": "What's happening",
        "is": "A live map of everything the Company is doing.",
        "fills": "Each dot is one thing that happened — a note saved, a job run, a message. Its slice is what KIND it is; how far from the centre is how long ago (the centre is now).",
        "why": "To see, at a glance, what kinds of things are happening and how recently.",
    },
}
