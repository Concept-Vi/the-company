"""cloning — the point-in-time cloning capability bound to a channel."""
ATTACHMENT_TYPE = {
    "id": "cloning",
    "label": "Cloning",
    "target_kind": "scope",         # target = the source session/scope clones are minted from
    "multi": True,
    "desc": "the cloning capability bound to a channel — era-clones minted from the target source.",
}
