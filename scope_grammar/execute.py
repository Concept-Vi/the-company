"""scope_grammar/execute.py — the EXECUTE verb (A's base five). Run/act, e.g. execute:send_quote."""
SCOPE_VERB = {"verb": "execute", "means": "run/act on a resource (dealership: execute:send_quote)", "reversible": False}
