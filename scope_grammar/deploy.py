"""scope_grammar/deploy.py — the DEPLOY verb (A's L5 delegation scope: deploy:langgraph)."""
SCOPE_VERB = {"verb": "deploy", "means": "deploy a workflow/service (deploy:langgraph)", "reversible": False}
