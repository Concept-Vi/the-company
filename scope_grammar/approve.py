"""scope_grammar/approve.py — the APPROVE verb (A's base five). Resolve a surfaced decision, e.g. approve:financing_proposals."""
SCOPE_VERB = {"verb": "approve", "means": "resolve/approve a surfaced decision (operator-gated)", "reversible": False}
