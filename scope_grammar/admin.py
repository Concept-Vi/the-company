"""scope_grammar/admin.py — the ADMIN verb (A's base five). Manage the container's own settings/members."""
SCOPE_VERB = {"verb": "admin", "means": "administer a container (members, settings, structure)", "reversible": False}
