# projection/ — THE SECTOR REGISTRY (Tim Geldard's universal projection; types = angular divisions)
One row per sector (projection/<id>.py, file-discovered, dial-pattern). n = number of rows; each
sector spans 2π/n — ADDING A ROW REDISTRIBUTES ALL SECTORS EVENLY (the water law; never a bolt-on
slice). Row shape: SECTOR = {id, label, gathers: [event-kind patterns], meaning}. Order = sorted
row ids (deterministic). The angular geometry of the screen IS this registry. Pure data (the floor).
