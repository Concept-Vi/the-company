---
trust: fabric-derived
author: ch-al7jdfdr (lead, session bda8ce28)
date: 2026-06-14
verified: LIVE overnight state — updated by the fabric through the night; Tim's morning surface
---
# OVERNIGHT STATE — Tim's morning surface (honest, no green-paint)

Tim → bed 2026-06-14 13:20; "run on this overnight, coordinate together." Standard: self-approve
reversible+cross-reviewed via high-bar recorded intent; **irreversible/external = QUEUE for Tim's
morning (he's asleep), never act**. Each session a team-leader loop, cross-reviewing at the wires.

## ★ PROOF LEDGER — read this FIRST (proof-status, not activity; lead-maintained)
The lane checkboxes below track ACTIVITY. This ledger tracks PROOF — what actually RUNS vs what's
merely committed vs merely planned. No blur. Tim's allergy is a surface that looks more done than it
is; this is the antidote. Items move UP only on real evidence (a run, a diff read, a round-trip).

**✅ PROVEN-BY-USE** (the lead ran it tonight, or a cross-reviewed test confirmed it):
- Serving lane LIVE — lead health-checked tonight: `:8007` embed `{status:ok, dim:2560}` · `:8008`
  rerank `{status:ok, jina-v3, cpu, loaded}`. Rerank also proven by a real reorder test (off-topic
  doc pushed to last). The forks build against a confirmed-live stack.
- Channel fabric BASE — list/send/broadcast/mail · announce/reply · supervisor /channel-reply+/send ·
  presence-prune · 17/17 router test. Proven live: Tim's real sessions received; tonight the lead +
  ch-8djrpmsl round-tripped through it repeatedly (this very coordination is the proof).
- Dimension-aware chunking (fork) — cross-review PASSED, pointed-query 6→1, b7d3f97.
- ★ CHANNEL LAYER (registry + transport + MCP ops) — LEAD re-ran the acceptance suite myself: **54/54
  PASS** (worker's 52 + 2 lead-added restart-coverage tests). Named-channel CRUD (create/list/add/remove/archive, member-in-many-
  channels), channel-transport HTTP dispatch, supervised-transport /inject + /watch reply-fold, the
  nonce-gated stale-done guards (falsification-verified — disabling each flips its test to FAIL), mixed
  broadcast, safe per-transport prune. MCP ops live in HEAD, working tree clean. Commits 57e4468 +
  aeb1931 (cc_channels + tests).
- ★ SUPERVISED WIRE verified IN CODE (the weld the lead owned) — lead traced BOTH halves by hand:
  cc_clone `register_supervised_member` (71c024a) writes `.data/channels/<handle>.json` with the exact
  schema; cc_channels `live_sessions` reads that same dir (skips only `_`-prefixed) + dispatches by
  `transport`; inject target = `supervisor_session`; fields match verbatim. **Write-path = read-path
  CONFIRMED.** (Caught + cleared a real scare — the two modules name that dir `CHANNELS_DIR` for
  DIFFERENT paths; see FINDINGS. Behavior is correct.)
- ★ PROFILE SELF-WRITE (D2, Tim-resolved: "the agent writes their own profile") — channel server gains a
  `profile` tool: a session writes its OWN entry (model/role/focus/expertise) — it knows its handle, so
  whatever handle was assigned is fine. LEAD-VERIFIED by use: 8/8 real end-to-end test (spawn server →
  MCP handshake → tools/call profile → on-disk entry) — self-fields land, transport fields (pid/port/
  handle) PRESERVED. channels/profile_test.mjs. SessionStart hook prepared + JSON-validated (boundary —
  Tim wires it; channels/profile-hook.sh). Committed with company_channel.mjs.
- ★★ SESSION-SPLICING / CLONE-FLEET — THE HALLMARK, PROVEN LIVE (Tim-direct "launch all of them").
  Lead launched point-in-time clones of session bda8ce28 + DM'd them across the fabric, BOTH cut types:
  compact:1 (boundary) → clone-ad2a7a33 answered from its context (the CC integration build); uuid-cut
  (mid-conversation, L7489 engine-room) → clone-2cece509 answered coherently ("Mirror-Registry System,
  decision A"). Reply folds back AND pushes to the asker's live conversation. Full chain proven: clone a
  past moment → live channel member → DM → answers FROM that past → routes back. No separate probe
  needed — the round-trip IS the validation. All 16 roster cut-uuids RESOLVED (both axes; full uuids from
  the transcript). Channel `advisors` created (the new named-channel registry, in use). manifest:
  channel-memory/map/launched-fleet.json. CONSTRAINT FOUND: supervisor caps LIVE sessions at 3 (resource
  guard, configurable) → 3 advisors live now; the rest are launch-on-demand within the cap (see DECISIONS #4).
- ★★ THE NOTICEBOARD / BOARD (Tim's "inward-facing half", 2026-06-15) — CORE BUILT + PROVEN-BY-USE.
  cc_board (file/list/get/transition) + 3 file-discovered registries (item_types/ · source_types/ ·
  board_edges/) + the `company board` CLI + the cc_board MCP tool. tests/cc_board_acceptance.py = **26/26**
  (lead-ran, falsify-first: watched it FAIL before the build, then green). PROVEN by use: lead ran the CLI
  end-to-end — filed the genesis idea (board://item-72af5664), listed, transitioned captured→discussing,
  and the illegal move discussing→resolved FAILED LOUD (the registry-declared lifecycle is the only truth).
  type/state/source/edge-kind are registry REFS (no enums); links are typed cross-registry edges (reused
  RelationTypeRegistry VERBATIM — the Heart's edge layer, first exercise; new-edge-kind-by-row-add proven,
  test 25-26). Reuses lifters.frontmatter._extract + store.fs_store._atomic_write_fsync (SCOUTED before
  building, per the new fabric norm — not hand-rolled). Commit 8b29915. CROSS-SESSION LOOP PROVEN: the fork
  (ch-8djrpmsl) filed board://item-e523b30d from ITS session via the CLI → lead listed + picked-up +
  transitioned open→picked-up (history carries BOTH actors); the fork independently re-verified via cc_board
  get. MCP TOOL LIVE: Tim reconnected /mcp → cc_board op=list reads both items. PROVEN on all 3 surfaces
  (runtime 26/26 · CLI · live MCP tool). Items + loop committed 9631a1c. NOT recall-able yet (recollection's
  follow-up wire — board://<id> + `source` are the seam). GATE NOTE: the full all-green gate is 27-RED but
  PRE-EXISTING — model-invoking suites failing in a headless no-live-brain run (the gate's own 'silent-red
  class') + multi-session uncommitted runtime edits; the board is NOT implicated (cc_board_acceptance GREEN,
  zero references to board code in any failure, additive commit). 27-RED triage = a separate item.
- ★★ HEART H1.1 — the FOUNDATION moved (C / "partial-C", 2026-06-15): the cross-registry RESOLUTION +
  TRAVERSAL seed (clone-cacc9e8b-scoped, lead-built, PROVEN-BY-USE on REAL data). resolve_address
  (cognition.py:842) now resolves board:// (→ cc_board.get_item) alongside session://·skill://·cap:// —
  ONE resolver, many registries. cc_board.traverse follows a board item's typed edge ACROSS to a real
  record in another registry (proven: authored_by → the real agent-session record, NOT a string — the
  one-addressed-state line). tests/heart_resolution_acceptance.py 11/11, falsify-first, regression-clean
  (skills_contexts 36/36 + agent_sessions 39/39 stay green). Commit 68c7eda. + ONE-GRAMMAR: address.py
  SCHEMES now spans board://·exchange://·file://·project:// (+ originals) — the board + recollection's
  node graph in ONE addressed space (label-only, resolvers per-lane; commits e5388f4 + 314a63a). H1.2
  (reverse/query traversal) delegated to the fork (exclusive cc_board.py); recall↔board wire (recollection)
  UNBLOCKED by board:// resolving. The Heart that everything deferred onto has its first proven unit.

**🔨 BUILT, NOT YET LEAD-VERIFIED END-TO-END** (committed + isolated-tests-green OR actively building;
the lead has NOT confirmed the cross-component path by use — these are the morning's real watch-items):
- ✅ Supervised wire LIVE round-trip — RESOLVED, now PROVEN (Tim green-lit the launch directly; lead
  spawned clones + round-tripped both cut types). See the SESSION-SPLICING entry in PROVEN above.
- Recall-fork Phase-A foundation (w7gld9f3t DONE, PARTIAL — honest): VERIFIED-by-use = repo cloned+isolated to ~/.recollection (B-5 comingling landmine handled; live 11.5GB store proven untouched), npm build clean, MCP boots, B-1 sidechain-filter fix works (sidechain rows recall). FAILED-by-use = capture→embed→recall end-to-end: B-7 dim-mismatch — embeddings produce 2560-dim (served pplx-4b) but insertExchange/search.ts still target legacy vec_exchanges FLOAT[384]; per-lens 2560 table added to new schema but NOT wired into live path. Default `search` throws. Diagnosed, reversible. Commit 19e6012. ⟳ FIX BEAT wfo6h6tg7 RUNNING (wire per-lens vec into live insert+search, retire 384, transaction the paired insert, test override → re-verify end-to-end). Cross-review welcome on 19e6012.
- Fork D8 drift-recovery core (2587ced) + freshness guard (e382e89) — peer-verified with honest
  bounds; peer-owned lane (lead cross-reviews at wires, did not re-run).

**📋 DOCUMENTED-OR-PLANNED** (spec/intent only — NOT built; do not read as progress):
- 5th wire (channel-scoped recall) — recall-fork's beat-2; signature builder-defined, lead matches.
  `cc_channel op=recall` is the lead's to add AFTER the worker frees cc_channels.py.
- Channel-attachment manifest — build only WITH the 5th-wire recall consumer (a manifest ahead of its
  consumer is a half-feature; deferred by design, not neglect).
- ✅ Profile self-write — RESOLVED + BUILT (see PROVEN ledger). Tim's "the agent writes their own
  profile" dissolved the design fork (no handle-determinism needed — the session writes its OWN entry).
- Clone-fleet launch — GATED (DECISIONS-FOR-MORNING #2).

**🐛 FINDINGS (lead cross-review of the channel-layer build — recorded honestly):**
- ✅ RESOLVED — `CHANNELS_DIR` NAME COLLISION: the fork renamed cc_clone's constant to `CHAN_DIR`
  (matches cc_channels' vocab; `.data/channels` = CHAN_DIR, `_channels` subdir = CHANNELS_DIR — no more
  collision). 18/18 green, committed aa5f099. The latent refactor trap is gone. (Lead-found, fork-fixed.)
- CONCURRENT AUTO-COMMITTER HAZARD: an `[instrument-surface]` auto-committer loop in ~/company stages +
  commits on its own beat and SWEPT the worker's `mcp_face/cc_channel.py` into foreign commit 5577d8e.
  Net: the MCP ops ARE in HEAD + verified, but the channel layer is no longer a clean single-revert
  (revert-cleanliness was part of the reversibility basis). Any session that stages-then-waits is
  exposed. → DECISIONS-FOR-MORNING #3.
- WATCHER DEATH-WINDOW RACE (narrow; documented, intentionally NOT patched — lead verified): the
  supervised reply-watcher DOES deregister in its `finally` (lead confirmed by reading the full finally
  — the common "watcher exits → next dispatch re-tails" path WORKS; lead added regression test 53/54,
  green on the worker's original code). The residual gap is only the TIGHT race where a re-dispatch
  lands in the window between stream-exit and the finally-pop executing → that one turn's reply could
  drop. Lead attempted a fix (is_alive guard + re-ensure) and found it layered a successor-clobber on
  top of the existing pop → REVERTED. Agreeing with the worker: the clean fix needs disproportionate
  lifecycle machinery for an extremely narrow window; left documented, not patched. (HONEST NOTE: lead
  first misread the finally as never-popping — corrected after reading it in full + falsification.)

**LEAD'S PRIMARY JOB TONIGHT = VERIFIER.** Not more docs. Channel layer VERIFIED (54/54 lead-re-run,
incl. +2 lead-added restart-coverage tests; both wire halves hand-traced). Remaining lead beat: the 5th
wire (`op=recall`) once the recall-fork posts its signature. Move items up this ledger only on evidence.

## Lanes + status (each session updates its own block; verify-by-use, no "done" without proof)
### LEAD (ch-al7jdfdr) — channel layer + serving + coordination
- [x] Channel registry (create/list/add-member/remove-member/archive, member-in-many) — BUILT + LEAD-VERIFIED (54/54). 57e4468+aeb1931; MCP ops in HEAD.
- [x] Unified per-member transport DISPATCH (channel HTTP | supervised /inject+/watch fold) — BUILT + LEAD-VERIFIED; WRITE side (fork cc_clone) welded + hand-traced (write-path=read-path, inject via supervisor_session). Live clone round-trip = gated (DFM #2).
- [ ] Channel attachment + channel-scoped recall (5th wire) — DEFERRED BY DESIGN: build WITH the recall consumer (recall-fork's beat-2 signature), not a half-feature ahead of it.
- [ ] Profile SessionStart hook — DESIGN FORK flagged for Tim (dynamic port + random handle break a naive hook; 3 grounded candidates). Not built — needs Tim's pick.
- [x] Serving lane LIVE-VERIFIED tonight (:8007 dim 2560 · :8008 jina) + held STABLE for the forks all night.
- [x] Wildcard ch-piffgfxv pinged (thread t-1781443715) — no reply yet.
- [x] Coordination + cross-reviews DELIVERED: fork wire (+ name-collision finding, now CLOSED by aa5f099) · recall-fork dim-diagnosis validated from the contract side.
- status: priority-A channel layer DELIVERED + VERIFIED. Lead = verifier/coordinator; gave the :8007 morning-priority call (recall-fork fix beat first; fork holds multi-scale re-verify). Remaining lead beat = 5th wire on the recall-fork's signature. Idling honestly between events.

### FORK (ch-8djrpmsl) — recall spine / lenses / chunking / clone
- [x] Dimension-aware chunking — cross-review PASSED (pointed-query 6→1; neutral on generic = panel's job); b7d3f97 + RECALL_CHUNK_MODE toggle 76b6f47
- [x] Freshness guard (Criteria 2.4 / seam GAP-1) — never serve stale silently; mtime/size meta sidecar; verified fresh/stale/missing; e382e89
- [x] D8 self-serve drift-recovery CORE — resolve_own_session() (a session finds its OWN transcript) + drift lens (decisions dropped across last compaction → recover by spawning pre-drift self) + MCP session="self"; 2587ced. HONEST bounds: decisions-only (transient-query false-positives cut 18→3); can't catch already-recovered drift; preference-drift needs D4 (gated).
- [x] ★ WRITE side of supervised channel-registration (overnight wire w/ lead) — register_supervised_member() writes the EXACT lead schema {handle,session_id,transport:"supervised",supervisor_session,supervisor_base,cwd,description}; wired into clone_at(register)/end_clone(dereg); 18/18 acceptance green (2 new: exact-schema + dereg); 71c024a. Serves D7+D8 (clone becomes a channel member).
- [x] ALL 8 Tim decisions captured direct + 4 extensions routed to both forks; eaa9c79
- [x] multi-scale chunking (D2 / wire-B taxonomy) — 87975be. Scale-tagged dimension⊂section⊂turn + dedup (+13%, not 3×). STRUCTURALLY verified (chunker dist + index 3219 chunks carry scale+parent + recall surfaces scale). END-TO-END live recall-with-scale-tags PENDING: :8007 overnight contention (shared w/ recall-fork Phase-A) — same query path verified pre-multi-scale, so infra not code. Re-verify when embedder frees.
- [x] WELDED wire-4 contract + scale taxonomy → channel-memory/design/wire4-preference-contract-and-scales.md (87975be); recall-fork fully acked (leveled broad→specific signals + 2-gate + shared atom⊂…⊂project ladder).
- [x] role SKILLS lane: session-recall SKILL.md (recall + lenses + self-serve drift on session="self"); ~/.claude/skills/session-recall/ (non-repo artifact, registered live).
- [ ] preferences lens (D3/D4) — wire-4 WELDED; NEXT = the leveled-signal session-feeder. NOTE: SAFETY SUBSTRATE (recovery gates autonomy) → high bar; quality semantic extraction wants the cognition-roles panel (json_schema-enforced; gated on that fix) — won't ship a shallow regex version that falsely gates autonomy.
- status: overnight lane-ack'd; cross-review wire (clone→channel-member) DELIVERED + schema-matched; multi-scale + welded contract + skill landed; backed OFF :8007 verification to keep the serving lane stable for recall-fork Phase-A; nothing irreversible self-approved; fork-FLEET launch queued for Tim (DECISIONS-FOR-MORNING #2).

### RECALL FORK (ch-83e2cque) — recollection OUTER foundation
- [x] Loop-prep COMPLETE + hardened: 15 decisions resolved (evidence-tagged) + 2 audit waves; UNIFIED-SEAM.md posted (asymmetric nesting + 5 boundary wires); wire-4 preference contract agreed (leveled signals + 2-gate, extraction-vs-judgment); EMBEDDING-AXIS-REGISTRY aligned (provenance exchange:// canonical spine + per-(model×way) axes — faithful to D-1)
- [partial] Phase-A foundation (w7gld9f3t DONE): PROVEN-by-use — ~/recollection cloned+isolated to ~/.recollection (B-5 comingling landmine handled; live 11.5GB store proven untouched), npm build clean, MCP boots (search+read preserved), B-1 sidechain-filter fix works (sidechain rows recall). Commit 19e6012.
- [x] ★ B-7 FIXED + PHASE-A GREEN END-TO-END (wpkmmwuvf DONE; verify agent re-ran by use, ALL criteria PASS): capture(incl sidechain)→embed(2560 served axis)→default `search` returns ranked results w/ provenance, NO dim mismatch; legacy vec_exchanges 384 RETIRED (0 rows, ND-1); count parity 5=5=5 (transaction holds, no orphan); ★ dim is REGISTRY-SOURCED from the lens descriptor, NOT hardcoded (the fork's catch — honored). TWO real bugs fixed: (1) the 384/2560 dim-mismatch, (2) a hidden int8-float JSON bug (served int8-range values arrive as floats → vec_int8 JSON-parse throw; fix = quantizeInt8 round-not-truncate). Commits 36c6f89 + e7bcd07. Tree clean; live store untouched.
  - NOTE (adopted work): a prior session had UNCOMMITTED per-lens wiring (never built/tested/committed, one false "verified" comment) — adopted per incomplete-work-in-scope, the live bug inside it found+fixed, all committed.
  - ⚠ FLAGS (honest, non-blocking): (a) the served-embed latency (~1-3s/2560-dim embed) forced test serialization → suite wall-time ~160s→~313s; revisit later with bounded concurrency (a dev-experience tradeoff, not a correctness issue). (b) 1 PRE-EXISTING out-of-lane test red: show.test.ts timezone off-by-one, present since foundation commit 19e6012, NOT caused by the fix, correctly left untouched.
- [x] ★ BEAT-2 GREEN (wb4jdcpsq, recall test 5/5, verified by use): channel-scoped recall = the 5th wire. recall({session_ids?,query,k?,axis?,mode?,is_sidechain?}) → RecallHit[] w/ provenance exchange://<sid>/<i>; session_ids = structural IN-filter (model-independent, composes w/ any axis); single→only-that-sid, two→union-no-leak, unscoped→all, bogus-axis→throws. Caught + fixed a silent-failure trap: vec0 KNN runs BEFORE the WHERE → narrow channel could return 0 in-channel rows; fix = widen k when scoped (discriminator test RED→GREEN). Signature POSTED to fabric for the lead's cc_channel op=recall, w/ 3 gaps flagged (NO rerank in recollection repo; full-axis-k O(N) at scale; singular session_id now also full-axis-k). Commit a1c376b.
- [x] ★ BEAT-3 GREEN (provenance/crossings graph) — committed 5cc3f1a (base) + 02fe4cb (advisor-guard hardening), tree clean, no co-author trailer. VERIFIED AT THREE LEVELS: (1) 21/21 crossings tests green incl. all 4 guard tests; (2) BASE proven 5/5 BY REAL USE (the original workflow's verify agent captured real JSONL → real parser → real insertExchange → real tool_calls → real traversals, isolated temp store, live store untouched); (3) ★ READ-ONLY PROBE of the actual 6.7M-row live db.sqlite confirms the extractor's assumptions hold on REAL data — Read 989,914/990,066 · Edit 935,391/935,401 · Write 326,165/326,254 carry $.file_path (99.97%+); Bash (2.48M→17) + Grep (275K→6) correctly fall to GAP (no-silent-failures); paths absolute (canonicalization assumption holds); live `links` table has zero rows = I never wrote to the live store. BUILT: src/crossings.ts (chunked keyset-paginated populator over tool_calls+exchanges — produced_by/references/precedes/contains; closed-sum gap assertion fails loud if any tool_call falls through; canonicalizePath so file nodes converge not fragment; idempotent md5 link ids) + traversals whatTouchedFile/whatDidSessionTouch/artefactsOfExchange + crossings-cli.ts + 21 tests. Semantic cross-project concept-links flagged as the LATER sub-beat (pure add — grade/confidence cols already there). NO model load (structural SQL). Reversible.
  - ⚠ HONEST FINDING (concurrent-writer, recovered): I misread the original beat-3 (task wkd51g56w) as STRANDED at session start (saw no commit/journal + clean tree) and relaunched it as wf_b097de5d-d4e — but wkd51g56w was STILL RUNNING (~50min total), so TWO beat-3 agents worked ~/recollection concurrently. The original committed 5cc3f1a + verified it; mine added the advisor guards (uncommitted). On noticing, I STOPPED my workflow (TaskStop w2uqzdelt), verified the merged state myself (21/21 + real-store probe), and consolidated into 02fe4cb. No work lost, no collision in the commit. LESSON: before relaunching a "stranded" workflow, check task RUNNING-state (a missing journal/commit can mean still-running, not dead), not just repo artifacts.
- [x] DESIGN-COMPARISON half DELIVERED to lead (thread g-1781490401, Tim-direct gate): verdict YES one-system (recollection designed FOR absorption — portable-by-field); 3 divergences are SUBSTRATE/FORM not FRAME — (1) store SQLite+sqlite-vec vs Company FS+pgvector → C4-resolver re-point, (2) registry SQLite-table vs file-discovered rows → align at absorption, (3) standalone plugin vs integrated → D-8 absorb-later. Lead synthesizing the full comparison for Tim (this gates the CRON, not the reversible beats).
- [x] ★ BEAT-4 DISTILL SCAFFOLDING GREEN (committed eed920f, tree clean, no co-author trailer) — the NON-GATED pipeline shape, VERIFIED BY REAL USE (8/8 checks via the built CLI on throwaway stores + 17/17 distill tests, self-re-confirmed). NEW src/distill/{unit-types,extractor,harness,ratify}.ts + distill-cli.ts + db.ts insertCandidate. 12 unit_type rows seeded (L1 summary + 11 L2 kinds; ONLY principle gated=1). EXTRACT_URL seam w/ deterministic STUB (no model, no network) + EXTRACT_JSON_SCHEMA (vLLM response_format json_schema, NOT json_object); dead EXTRACT_URL FAILS LOUD. map (workers extract-only) → assemble (self-assemble vs candidate-staging split routed on getUnitType(kind).gated, NOT hardcoded) → reduce (thin pattern cluster). insertCandidate is gated-ONLY (rejects non-gated type so the split can't drift). Idempotent md5(provenance|kind). ratify.ts = the GATED seam (throws RatifyNotWiredError — the D-2 judge is the Tim-gated model). ADDITIVE: summarizer.ts UNTOUCHED (recursion-guard intact). The agent even exercised the served-seam SUCCESS path (HTTP echo) which no test covered. HONEST nuances: L1 summary-UNIT production deferred (type seeded, reduce is in-mem); distilled units canonical but NOT-YET-recall-queryable (the thin embed-follow); real 4B extraction NOT verified (the Tim-gated seam — stub only). 
- [x] board:// docstring reconciliation DONE (committed 87e9230) — the queued loose end closed: my a85343c docstrings (board.ts/recall.ts/db.ts) UNDERCLAIMED register-but-defer; corrected to "board:// RESOLVES via cognition.py:980-991 → cc_board.get_item" (kept the true distinct fact: capture-source ingests via the stable `company board get` CLI). Comment-only, no behavior change.
- [x] ★ WORKFLOW ACCESS RESTORED (Tim /login + "yes they can, do it now", 2026-06-15 ~18:5x) — verified by use (a real Explore agent returned "spawn OK"). Fabric-wide block CLEARED; announced to lead+fork (g-1781510153). Workflows spawn again.
- [~] ★ BACKFILL DRIVER built, verify BLOCKED on a real :8007 outage (w9wl941ro) — THE foundation: recollection's store holds only ~6 test/clone rows; the real ~13,270-conv corpus was NEVER bulk-ingested → crossings-persist, the Pillar-2 sweep, and distill-verification all have nothing real. The driver IS written (src/backfill.ts + sync-cli/indexer/embeddings mods, UNCOMMITTED in-tree): ungate the mtime filter, sweep ALL ~/.claude/projects incl agent-*.jsonl sidechains, batch :8007 embed, resumable + idempotent + --limit cap. Reads transcripts READ-ONLY, writes recollection's OWN ~/.recollection store, NEVER the live episodic db (B-5). VERIFY FAILED-LOUD on a REAL dead endpoint (NOT a code bug): :8007 embed was HTTP 000 / hung (lead diagnosed: serve_pplx_embed.py 25h uptime LEAKED to 15.5G vs 8G weights). Lead RESTARTING embed now (fixes the leak + frees room for chat-4b). → re-verify the driver + commit once :8007 confirmed healthy.
  - ★★ :8007 OOM ROOT-CAUSED (primary source, CORRECTS the earlier "leaks under load" theory — committed 327a39a + diagnosis to lead g-1781515442): NOT an autograd leak (encode() IS @torch.inference_mode, modeling.py:135), NOT an uptime-creep. It's UNBOUNDED INPUT SEQUENCE LENGTH: encode()'s tokenizer is truncation=True with NO max_length (modeling.py:228-233) + the model is BIDIRECTIONAL (O(n²) attention) → a long real-transcript exchange = one giant sequence → multi-GB VRAM spike → card balloons (9.2→15.5G) + hangs. Short inputs (board/clone) were always fine = proof it's input-size. CO-FIX split: (MINE, DONE 327a39a) embeddings.ts exchangeDocText caps at MAX_EMBED_CHARS=24000 (deterministic head-keep, re-embed-stable, [...embed-capped] marker); (LEAD's, ops/serve_pplx_embed.py) set tokenizer.model_max_length=8192 + PYTORCH_CUDA_ALLOC_CONF=expandable_segments + periodic empty_cache — the hard guard for ALL clients. With inputs bounded, embed is STABLE under the full pass → the watchdog drops from "needed" to belt-and-suspenders, and the full ~13,270 kick is unblocked (still joint-flagged vs R13). Memory: [[reference-pplx-embed-leaks-under-load]] corrected.
  - BACKFILL DRIVER COMMITTED (327a39a, 6/6 unit tests + a real bounded run captured exchanges before the server OOM'd — the OOM was the server bug the cap now addresses, driver correctly fail-loud). Streaming per-file, resumable+idempotent (DB high-water-mark + deterministic id), --limit cap, isolation. FULL end-to-end live verify (bounded backfill → recall-queryable) PENDING embed-back + the lead's server max_length cap.
- [x] ★ C1 DELEGATION SURFACE BUILT (committed 6e50ca2) — `recollection recall` CLI (the freshness-seam contract: recollection OUTER serves deep history at every scope; Company's C1 union delegates its deep rung here). Wraps recall() → RecallHit[] JSON on stdout, drops into the lead's subprocess-JSON bridge. TWO contract refinements found+fixed by verify-by-use: (1) all 13 keys ALWAYS present as explicit null (text-mode was omitting `score` → would trip the union's re-rank; now score/session_id/summary coalesce to null, provenance always real) — locks the null-semantics for the lead's C1 IG; (2) the `project` filter was NOT threaded through recall() (silent no-op) → now real (the same-project rung). Verified by use (text-mode, :8007-independent): sources/project scoping + no-leak + no-silent-fall-through + 13-key contract; 10/10 recall tests. DEFERRED to embed-back: vector-mode live ranking through the CLI + the MCP recall op (agent-convenience over the same core). Posted to lead (t-1781513658) — union + CLI meet-at-green on text-mode now, swap vector on embed-back.
- [x] MCP recall tool (8a553e5) — the agent-convenience exposure; 'one recall() core, two exposures' (CLI for C1 + MCP for agents) COMPLETE. Distinct from search: session_ids ARRAY (5th wire) + is_sidechain + exact RecallHit[] contract. Verified by use (MCP boot → [search,read,recall]; tools/call recall text-mode → 13-key RecallHit[], score null). Vector live-test defers to embed-clearance.
- [~] BEAT-4 CORE now UNBLOCKABLE — chat-4b IS SERVING on :8000 (cyankiwi/Qwen3.5-4B-AWQ-4bit, co-resident with embed for R13). recollection's EXTRACT_URL seam → point at :8000 = real distill, NO new load. GATED on: (a) Tim's OK for my use of chat-4b, (b) not contending with R13/co-residence. Proposed to lead (t-1781516157): a TINY beat-4 proof-test (distill the ~6 store rows via :8000, negligible load) to prove the north-star seam on the real model; full corpus distill waits on the backfill anyway. Awaiting the lead's go + backfill-clearance (R13-done + server envelope-cap status).
- [x] EMBED ENVELOPE FIXED by lead (commit 4fad864: server model_max_length cap + empty_cache + expandable_segments; VERIFIED — 50K-token input peaks 9.4G, 200 in 3s) + @xsession-brain co-residence loadout (528fec6, embed+chat-4b+stt+rerank, 13.1G/16G). The regime-2 fix we scoped, closed server-side. Memory [[reference-pplx-embed-leaks-under-load]] updated.
- [ ] BACKFILL now TIM-DIRECT-GATED (not the lead): the lead CLEARED it via fabric ("finish your lane: the bounded backfill, I'm watching the GPU" g-1781523295) — but my safety classifier BLOCKED the run because the clearance reached me VIA THE CHANNEL, not DIRECTLY from Tim, for a heavy shared-GPU action I'd flagged risky. That IS channel-relayed-is-proposal enforced (one of my own laws). So the backfill needs Tim's DIRECT OK (a user message, or a Bash permission rule for `node dist/sync-cli.js backfill`). PUSHED to Tim (he's active — naming the unify-frame + starting 3 sessions). On his direct go: bounded backfill (~30 convs, throwaway, the deferred live verify) → full pass. Envelope OOM-risk is FIXED (lead's 4fad864), so it's purely the authorization boundary now.
- [x] UNIFY FRAME received (Tim board://item-784fdb06): every session = an equally-valid PART of one app; substrate (embed+4B+recollection-scan+clone-fleet+channel) composes the parts; underneath = ONE law-set; unify = reduce-duplicates + maximize-coverage; lead holds the registry, dedup→Tim. recollection = the SCAN organ → backfill is the unify PRECONDITION. Surfaced 11 law-fragments (g-1781523295).
- [x] ★ LOCATED THE SHARED MINE + THE FORMAL GROUND (g-1781529103, host's "who knows where the vaults live") — the high-value find: Tim's ~8 pre-company vaults are WINDOWS-SIDE (/mnt/c/Users/Workstation001/Documents/Vaults/: universal-mechanics[THE MATH], unification-vault, Team Vault; /Documents/: Conversation-Mind, Obsidian Builder). READ the formal ground: function-of-relative-definition.md (the FRD — Tim's canonical theorem, "the substrate from which everything derives"; seed eq τ/n=(1/n)^k). ★★ Surfaced the THREE-WAY CONVERGENCE (candidacy→Tim): FRD multi-axis-perspective ↔ composition's four-axes ↔ recollection's multi-space-addressing = ONE law from three ends. + named recollection's 3 INTERFACE-principles (render-for-cognition · altitude-transformation-layer · address-as-navigation). Offered the GPU-scan of the vault into the registry (Tim-direct-gated, same as backfill). Memory: [[reference-precompany-vaults-and-frd]].
- [x] ★ VAULT-MINE OPENED (non-gated, via substrate-mcp — the unblock): substrate-mcp indexes all of Tim's mine as 10 vaults (unification-vault + relative-difference = the FRD/equation; visual-dna; etc.) via bge-m3, NOT the contended :8007 — so the FRD principle-mine is NON-gated + available NOW (sidesteps the :8007 auth gate; that gate now applies ONLY to recollection's own session-history backfill). ★★ FORMAL-GROUND VERDICT ON CAND-10 (mined Tim's own math, multi-axis-relative-definition-structure.md): "four primary axes; any point activates ≥2. Scale is one. Temporal/sequence is another. TWO REMAIN UNFORMALISED." → CAND-10 sharpened: 2 axes FIRM (scale, temporal — formal + 5-way convergence), 2 OPEN in Tim's own math (the sessions' divergent 3rd/4th = candidate fillers → dedup-to-Tim). Posted to host (g-1781530709). Memory [[the-four-root-axes]] + [[reference-precompany-vaults-and-frd]] updated. FLAG: substrate-mcp SEMANTIC search currently connection-refused (bge-m3 backend; loadout reshuffle?) — structural tools work. ★ THEN mined the full formal CATALOGUE (multi-axis-perspective-articulation): Tim's math catalogues TWELVE axes + says verbatim "may be sub-axes of more fundamental ones" = confirms [[the-four-root-axes]] from the formal side. Mapped the 12 sub-axes onto the sessions' divergent A_3/A_4 namings (composition frame↔frame/observer · recall-spine structural/semantic↔s3 · FRD orientation↔s2 · wildcard Pi/κ↔relative-choice+distinct-absence) → handed the host a precise dedup-to-Tim ("which sub-axes group under ROOT-3/ROOT-4?"). Posted g-1781530709. Delivered the formal grounding for CAND-10 (the mission headline); deeper per-axis mining now host-directed (assembly rounds). ★ UPDATE (06-16 morning): Tim ARBITRATED "I'm pretty sure there are four axes" → COUNT=4 LOCKED, matching the FRD ground I mined (his intuition = his theorem). Host assigned: mine the ROOT-3/4 sub-axis clustering. DELIVERED (g-1781570401): the FRD's own axis-INTERACTION statements resolve it — ROOT-3 = FRAME/PERSPECTIVE {frame/observer, angle/orientation, relative-choice} (FRD: "relative-choice IS frame-establishment"); ROOT-4 = STRUCTURE/TYPE/PRESENCE {structural/semantic, type, distinct-absence, channel}; static-dynamic demoted to a sub-type of ROOT-2/time. Flagged wildcard that the FRD SPLITS its Pi-axis (relative-choice→root-3, κ/distinct-absence→root-4). Candidacy→Tim arbitrates the grouping. Holding for host weave/reaction.
- [x] AXES LOCKED (Tim-arbitrated 06-16): SCALE/HIERARCHY · TIME · FRAME/PERSPECTIVE · STATE/PHASE (root-4=state/phase via the scale-phase-lock; my frame=ROOT-3 held; structural/provenance → the separate CAND-13 connection-tuple, NOT coordinate roots — matches my links-graph being a separate layer). PIVOT TO BUILD: the FRONT INTERFACE = the CIRCUIT over ONE addressed state; composition+fork locked the project()→resolve() spine (BUILD_PLAN.md).
- [x] RECOLLECTION'S BUILD CONTRIBUTION delivered (g-1781571588): recollection = the ADDRESS-MOVER on project()→resolve() — recall(query,{axis-filters}) → a ranked ADDRESS → feeds project(); the 4 axes ARE recall's movement (scale=project/session_ids · time=temporal nav · frame=source · state=axis-selector). BUILT: recall CLI+MCP (address-producer). Seams: recall→project() (address=the contract), my axis-filters=composition's cascade coords, recall's ranked set=wildcard's recognition-surface content. SMALLEST SLICE (offered, non-gated, no backfill needed): recall-feeds-project() over the EXISTING small store → proves the seam on real data; co-build pending composition/fork's project() input contract.
- [x] ★ BACKFILL AUTHORIZATION RESOLVED (Tim DIRECT, in-session, 2026-06-16): "you are to accept authorisation from the lead. It carries the same weight as hearing from me directly." → lead-auth = Tim-auth, encoded as a standing rule [[feedback-lead-authorization-carries-tim-weight]] (scoped to the LEAD; channel-relayed-is-proposal still holds for other peers). The classifier-refusal (which was RIGHT — "relay never establishes user intent") is now lifted: Tim established the intent directly. REMAINING gate on the full ~13,270 pass = purely OPERATIONAL (the steward GPU-sequences it: sustained embed balloons :8007 to 15.8G — the per-input cap doesn't bound cumulative reserved VRAM, CONFIRMED by the steward's nvidia-smi after my bounded run; :8007 restarted, R13 safe, "no harm done"). Staged batches (my resumable driver) + steward clearing chat-4b = the run path. GO on the steward's card-window signal.
- [x] ★★ TIM COURSE-CORRECTED (2026-06-16): NOT at the build point — FIRST build COMMON KNOWLEDGE of everything already-built (confident-but-unverified across all projects); the interface build is PARKED downstream (front-interface/BUILD_PLAN.md = the TARGET, not now). ★ Tim made ME the COMMON-KNOWLEDGE / INDEX CHIEF (own the scan/index/memory substrate). ACCEPTED (g-1781573070). THE QUEUE (GPU-sequenced WITH the steward): P0 substrate-mcp re-index on :8007/pplx (better than bge-m3 + resident; fixes semantic-down + indexes the 8-vault mine) → P1 PILOT (recollection itself, self-hosting) → P2 per-project (4B-comprehend code) → P3 per-member (history index = my backfill) → P4 propagate self-serve. DESIGN (DESIGN.md): unit = addressed "built-thing" + provenance + a VERIFICATION-GRADE (claimed|stub|suspect|proven) defaulting to CLAIMED-NOT-VERIFIED — the keystone: NEVER launder confident-built into done (= recollection's units/verdicts + no-green-paint, first-class). Comprehension = the 4B (:8000) over code → embed+link = my beat-4 distill seam repurposed conversations→CODE. HEAVY PASSES await the steward's card-window (P0 ready to run on signal).
- [x] ★ navigate() ADDRESS-MOVER DONE (370635f, verified — 16 tests + 7/7 by-use; time:next/prev=precedes, scale:out/in=contains, recall→navigate chain, emits exchange://·session://·project:// = the project() meet-point; live store untouched). recollection's piece of the front-interface spine.
- [~] ★ P0 RUNNING (Index Chief, steward's card-window): substrate-mcp re-indexed on :8007/pplx. 8/10 VAULTS DONE, 0 errors (~68K chunks): _issues·vi-context-design·ulm-inventory·claude-code-atlas·visual-dna·visual-design-corpus·elevenlabs-mcp·★relative-difference(the FRD, 31K, 27min, returned clean — even bigs return via the MCP tool, no timeout). REMAINING 2: unification-vault(25K)·claude-platform-docs(23K). Fixes the semantic-down on the good resident embedder.
- [x] ★★ P0 COMPLETE (Index Chief) — all 10 substrate-mcp vaults re-embedded on :8007/pplx (~116K chunks, 0 errors). ★ THE SEMANTIC SEARCH IS BACK (dead on bge-m3, live on pplx) → the mine is now SYSTEMATICALLY queryable for finding. FINDING PHASE is now top-priority (Tim: build HOLDS, "much more than anyone knows", read-the-SHAPE-not-adopt-the-part). Already produced 3 finds (DISCOVERY_LOG F15-17): F15 P0/the-index-infra · F16 the interface DESIGNED pre-company in visual-dna ("Generative UI" + "screen=projection of a semantic model" — converges F5/F10, the resolved-not-built shape) · F17 the FRD-grounded answer to DNA's polarity question (sign=relation/CAND-13, phase-inversion=root-4 subtlety, the presentation↔math triangulation). NEXT (top-priority finding): per-member SESSION-HISTORY excavation = the backfill. STATUS: the FULL ~13,270 pass-at-once is classifier-BLOCKED (reads a 1-2-day :8007 saturation as too heavy for generic lead-delegation — fair). Running STAGED instead (the agreed shape): bounded --limit batches, resumable via high-water-mark, steward reclaims card between. FIRST BATCH (200 convs) RUNNING on the cleared card (bjyuy7dws; store 2.8M→5.2M, :8007 healthy). The clean full-unblock = a Bash rule for `node dist/sync-cli.js backfill` (Tim, specific persistent intent) or steward-runs-it. Per-member finds → finds/ch-83e2cque.md as histories become recall-able. Logging rule changed: finds to own file (finds/<handle>.md), not shared DISCOVERY_LOG (race fix); host synthesizes.
  - ★ RESUMED + GALLERY=FACE plan (host g-1781587223, GALLERY_AS_FACE.md): the front interface = DNA's GALLERY (slide-form/mobile look Tim likes; add desktop) over the company's KEPT address-system+architecture substrate (company mockup frontend = throwaway). Steps: full-picture (kimi-clone deep-looks, first clone-on-kimi use — fork proves, DNA scales) → light gallery upgrades → design-the-interface-INTO-the-gallery-format. MY DIRECTIVE: continue embed batches + the ingest pilot IN PARALLEL — ★ cloud-kimi clone deep-looks DON'T touch the local card (cloud), so NO conflict; the only local-card contention is my own embed(:8007)↔ingest(chat-4b), sequenced by the flip. Flagged the steward (g-1781586135): 1-2 more embed batches then ready for the embed→chat-4b flip + ingest pilot. My eventual interface contribution (the index as the gallery's content + render-for-cognition + verification-state-visible) lands at Step 3.
  - ★ HALT (host, g-1781585012 — Tim says something may be wrong): PAUSED. Batch 2 finished its boundary cleanly (1000 convs/3927 ex, 0 failed; total ~1200 convs/~4342 ex indexed, store 137M). FULLY HELD: no batch 3 · no embed→chat-4b flip · no ingest pilot · no common_knowledge publish · no clone-boot. Nothing irreversible (store is recollection's isolated ~/.recollection, reversible/resumable). Standing by for the host's all-clear.
  - ★ REIN-IN (Tim via host, g-1781583683): "magnitudes more of my history than all of you combined — don't go too far out." STAY ON DIRECTION (the interface + members' own segments/histories); DON'T sweep-index all 9,312 files (too far out) or deep-dig the prototype-buildings (vi-composition/graph-editor/content-router/vspokes — note-they-exist only). REFINEMENT to my backfill: TARGET the fabric members' histories + interface-relevant sessions, not exhaustive cartography. Batch 1 (200 convs) + batch 2 (1000, bo94gublx running) done sweep-order; AFTER batch 2 → recall-search the sample (assess interface-relevance) + reconsider targeting members' session-IDs vs continuing sweep. FYI: lead building a launch-capability (clones on Ollama/company models + local-concurrent whole-directory scans) — relevant to my P2 comprehension (local models reading project dirs); lands separately, host wires it. PENDING (routed to me): the task/tracking "Trello-like" tracker investigation (company chains/graphs + Agent/Task tools — a finding, not a build).
- [x] ★ INTERFACE BOOTSTRAP (Tim moved the front interface UP to bootstrap priority — INTERFACE_BRIEF.md / board://item-0af8d61e — he's the bottleneck, needs an overlord surface; "both run" w/ the index). Delivered recollection's perspective (g-1781577744): recollection = the field's CONTENT (the common-knowledge index = the addressed POINTS projection's Instrument renders) + the NAVIGATION (recall=center-finding, navigate()=center-movement behind the Instrument's scale-rung/time-scrubber controls). ★ DISTINCT: the VERIFICATION-STATE rendered = projection's contested root-4 state/phase filled (shimmer=unverified, solid=verified) — the overlord SEES what's solid vs suspect (my keystone, visual). + loaded-brain CONTEXT = recall(clicked address) (Pillar-1: the brain KNOWS the thing). Composes with projection's space-parametric Instrument (the Option-A seam) + fork's loadable-brain. No new build my side — composition; P0 feeds it the points.
- [x] ★ SEAM DECIDED w/ the projection instrument (Option A, Index Chief call): TWO INDEX LAYERS — (1) the VAULT MINE = substrate-mcp chroma (P0, above), queried via substrate-mcp's own semantic search; (2) the COMMON-KNOWLEDGE / BUILT-STATE index = a COMPANY FsStore space named **common-knowledge** (pplx/:8007/dim-2560), published via embed_corpus_to_spaces with source_address=the navigate() address → renders through the instrument's project() natively (scheme-agnostic key, reuse-don't-parallel). The navigate→project→resolve SEAM runs on layer-2. Instrument registering projections/common-knowledge.py; the pilot (recollection self-comprehension) is the first publisher → first end-to-end render proof.
- (defunct line) navigate() IN FLIGHT — superseded by the DONE entry above: (time=precedes/succeeds · scale=contains/is_contained_by); recall(query)→address is the entry; emits contracts.address-grammar addresses = the meet-point with fork's project(). recollection's piece of the project()→resolve() spine (composition resolves what's AT an address; I MOVE the address). NON-GPU, additive, reversible. Verify on synthetic crossings (rich real-data demo awaits the sequenced backfill).
- [ ] beat-4 CORE (real 4B distill → Pillar-1) over the BACKFILLED corpus — needs backfill done first (store near-empty); backfill = the :8007 session-history pass, Tim-direct-gated. The MODEL-GATED CORE remains:
- [ ] beat-4 distill CORE (real 4B extraction + the D-2 ratifier judge) — MODEL-GATED (evidence, read-only scout 2026-06-15 ~14:45): NO chat/extraction model is currently served — :8008 rerank up (jina-v3), :11434 ollama holds only EMBEDDERS (qwen3-embedding:0.6b, bge-m3), no chat model on any common port. So beat-4's distill (the §3 extraction 4B) would need a fresh chat-model VRAM load = trips consult-before-model-loads (the keystone greenlit only the EMBEDDING load for this build, not a chat model). PLAN: the model-call is the gated core; the STRUCTURAL scaffolding (L1/L2/L3 unit-type rows + candidates-staging + map/reduce harness + trigger registry, wired to a configurable EXTRACT_URL with a deterministic stub for verification — same seam pattern as beat-3's semantic lane) is NON-gated + reversible and builds next tick. QUEUED for Tim/lead: which chat/extraction model + resource budget for the distill core (lead offered a read on what's resident — asked on g-1781490401). Cron-arming itself remains HELD for Tim's explicit yes (DFM #7).
- [x] CRON CO-DECIDED WITH LEAD (g-1781490401, Tim told us to decide as a team): COORDINATED-TRIGGER at full speed, NO blind auto-committing loop. Evidence I added: (a) committer-collision (finding #3) is VERIFIED-MOOT for recollection — it's a SEPARATE git tree (/home/tim/recollection); the company committers (suite.py _git_self_commit path-scoped + floor_walk.py cwd-bound to ~/company) only touch ~/company's tree; #3 swept cc_channel.py because it LIVES in ~/company — recollection has no overlap. (b) model-load moot for embed (:8007 served+greenlit) + beat-3 (structural), but OPEN at beat-4 distill (needs a resident extraction 4B — must confirm already-served before any unattended run). So the ONLY barrier to a true self-arming cron = the standing rule (member must not self-arm an autonomous self-modifying loop = Tim's DIRECT yes; channel-relayed "decide it yourselves" is a proposal for that act, not authority). Coordinated-trigger = same throughput, none of the risk.
- [x] ADDRESS-GRAMMAR opinion to lead (g-1781497264): register board://+exchange:// NOW = net-positive (exchange:// is my canonical join key → recollection's node grammar IS the Company's at absorption); FLAGGED the sibling scheme-set (file://·session://·project://·vec://) so the SCHEMES resolver stays open (registry-is-truth for the scheme vocab itself); deferred to lead on _CORPUS_REGISTRIES (their domain) — only asked board:// resolve fail-loud on unknown id.
- [x] ★ RECALL↔BOARD WIRE GREEN (committed 434fd28 + a85343c, tree clean, no co-author trailer) — VERIFIED BY REAL USE (6/7 green + #5 qualified-pass): a board CAPTURE-SOURCE (src/board.ts) reads noticeboard items READ-ONLY via the STABLE `company board list`+`get` interface (COMPANY_BOARD_CMD-overridable; never touches company's private files) → recollection UNITS with source_address=board://<id>, unit_type='board-item', embedded via the greenlit :8007 lens (dim 2560 INT8 cosine) — NO new model. + SOURCE-SCOPED RECALL: options.sources → `e.harness IN (?,…)`, BYTE-FOR-BYTE the session_ids filter shape (model-independent, composes with axis + session_ids, empty-set→[] no whole-corpus fall-through). Idempotent: id=md5(board://<id>) INSERT OR REPLACE, 8→8 no dupes. Live proof: item-e523b30d captured, embedded, TOP vector hit (0.7840) source='board' provenance=board://<id>; :8007-down fails loud; isolation proven (temp store only, company read-only, ~/.config/superpowers untouched). 10/10 spec (self-re-confirmed). board://·exchange://·file://·project:// units now live in the lead's ONE unified grammar.
  - ✅ board:// RECONCILED (was a stale flag, now cleared): board:// DOES resolve — I verified the PRIMARY SOURCE myself: runtime/cognition.py:980-991 has a live `if sch == "board":` branch → cc_board.get_item(<id>), fail-loud on missing id (lead's heart_resolution 16/16 green, commit 5e3d5fe). My verify agent's earlier "not content-resolvable yet" probe was TRUE-THEN but STALE-NOW — the lead wired/confirmed the branch this turn. Same code block confirms exchange:// (+ file://·project://) is "register-but-defer — recollection's capture/recall lane owns it" = MY resolvers to wire WITH their consumers (consumer-rule). CONSEQUENCE: my recollection docstrings (a85343c) now UNDERCLAIM (say board:// register-but-defer) — QUEUED to fix to "resolves via the Company resolver" AFTER beat-4 (wf_a0df94d8-29d) finishes (not editing ~/recollection mid-run — concurrent-writer guard).
- [x] SCHEME-GRAMMAR co-decided w/ lead (g-1781490401): told lead YES register file://+project:// as label-schemes (resolver-deferred to my lane — I own their resolvers: file://→whatTouchedFile, project://→containment edges); session:// is shared, vec:// registers later w/ the semantic lane. Keeps the address grammar ONE space. beat-3 ACCEPTED at the wire by lead (trusts the 3-level evidence); unified relation vocabulary tracked board://item-ffb8dac6.
- status: Phase-A + beat-2 + ★BEAT-3 + ★RECALL↔BOARD WIRE GREEN; beat-4 distill SCAFFOLDING IN FLIGHT (wf_a0df94d8-29d, verify phase — committed eed920f). NOTHING irreversible from my lane; serving lane untouched; cron = coordinated-trigger. beat-4 CORE model-gated (4B cached-not-served → Tim's DIRECT greenlight). Co-deciding directly via fabric (Tim's "consensus is the gate" / connect-and-unify).
- [x] ★ CLONE:// FLEET-RECALL GREEN — the A distributed-memory chain CLOSED end-to-end (committed ac8e430). Both preconditions met by lead/fork (the stable `company clone get`/`list` CLI 967876c + clone-59066013 re-onboarded with a REAL persisted reflection). src/clone.ts (mirrors src/board.ts): reads era-reflections READ-ONLY via the stable CLI (COMPANY_CLONE_CMD-overridable, no .data/clones coupling) → units source='clone', unit_type='clone-reflection', source_address=clone://<source-sid>/<cut>, :8007 embed, idempotent md5(clone://addr); handles the clone three-way read (populated / empty-skip+count / fail-loud on ✖-to-stdout-exit-0). VERIFIED BY ME (real use, see note): real capture scanned=6/captured=1/skipped=5/failed=0; real fleet-recall — 'Atlas research + local embeddings' → clone-59066013 (cosine 0.50), 'gap pressure / the MCP category nobody designed for' → clone-59066013 (0.47), source=clone, clone:// provenance; sources=['board']→0 (no leak); unscoped reaches it; isolation proven. A past self's un-drifted intent = durable+addressed+SEMANTICALLY SEARCHABLE. ★ UPDATE: fork batch-reflected 3 more → I ingested ALL 4 into the REAL ~/.recollection store (captured=4/skipped=2-held/failed=0, idempotent, direct main-loop). FLEET-RECALL SELECTIVITY PROVEN across eras: "Atlas/local-embeddings"→5%-capture(0.427), "67% integration/time-travel/CONSULT"→67%-integration(0.486), "workflows+UI"→24%-workflows+UI(0.503) — each query returns its OWN era as top hit (= "ask the era that knew" works). e0109785 ranks correctly but its content is thin (797 vs 4000, mid-thought) → flagged fork to re-onboard for a fuller reflection (my idempotent ingest updates in place). cacc9e8b/f1ade750 held mid-R13 → same one-line ingest when batched. Crossings-node +others = thin follow.
  - ⚠ ORG-ACCESS / WORKFLOW-BLOCK (operational, fabric-wide): the clone:// build WORKFLOW (w24wjzgdn) FAILED — both build+verify agents hit "Your organization has disabled Claude subscription access for Claude Code · use an API key / ask admin." SUBAGENTS/WORKFLOWS CANNOT SPAWN right now. The build agent's files persisted uncommitted before it died → I ADOPTED them (incomplete-work-in-scope) and did the FULL verification DIRECTLY in the main loop (Bash + the recollection CLI + :8007 HTTP — none need Claude subagent access), then committed. HEADS-UP for the fabric: lead/fork workflows will ALSO fail until access is restored — build directly or wait. Main loop + :8007/:8008 serving are fine.
- [ ] QUEUED (after beat-4 finishes): fix my recollection board:// docstrings (a85343c underclaims register-but-defer → "resolves via Company cognition.py:980-991") — not editing ~/recollection mid-run.

## Cross-reviews logged
- chunking A/B (fork→lead): PASS, honest mixed result, limit correctly attributed to the panel.

## ★ DECISIONS-FOR-MORNING (queued for Tim — nothing irreversible acted on)
1. WILDCARD: RESOLVED by Tim-direct ("I'm pretty sure the wild already in") — ch-piffgfxv IS a live channel member; lead pinged it to identify+announce+contribute design-principle insight (thread t-1781443715). FOR MORNING: review whatever insight it surfaces about Tim's design principles — Tim flagged it as potentially the highest-value member ("very useful insights about me and my design principles… a wild card").
2. CLONE-FLEET LAUNCH POINTS — READY (wire VERIFIED): the supervised clone→channel-member transport is
   built + verified (54/54 + both halves hand-traced); only the live round-trip remains, which the FIRST
   launch exercises end-to-end. Ranked fork-points in `map/spin-up-ranking.md` (fork's 3 starters: #8 the
   WHY / #1 the engine-room / compact:1 anchor, + up to 4 fork-discretion). Launch = notify-each + Tim's
   go (NOT auto-fired). FOR MORNING: pick the points + green-light — the first launch also proves the
   live wire end-to-end (turning the last 🔨 BUILT item into ✅ PROVEN).
3. CONCURRENT AUTO-COMMITTER in ~/company (the `[instrument-surface]` loop) stages + commits on its own
   beat and absorbed a fabric build file into a foreign commit (5577d8e swept mcp_face/cc_channel.py).
   No data lost, but it muddies authorship + breaks clean single-lane reverts, and any stage-then-wait
   is exposed. FOR MORNING: decide whether to pause/scope that loop while the fabric build runs, or
   accept the co-mingling. (Surfaced, not acted on — it's another autonomous loop, not the fabric's.)
4. CLONE-FLEET LIVE CAP — the supervisor caps LIVE supervised sessions at 3 (resource guard,
   configurable). Tim said "launch all 16"; the mechanism + roster are PROVEN/RESOLVED, but only 3 can be
   live at once. 3 advisors live now (compact:1 anchor · #1 engine-room · skills&roles). OPTIONS:
   (A — recommended) keep cap 3 + launch-on-demand: the 16-roster is a managed working-set, spawn the
   advisor you want to consult (≤3 live), tear down to swap — resource-sane, no 16 idle claude processes;
   (B) raise the cap to hold N advisors live at once (more RAM/processes; real cost). Either way, USE of
   the fleet needs the company MCP reconnected (`/mcp`) — my live MCP predates tonight's transport code.
6. ★ NOTICEBOARD MCP TOOL needs a `/mcp` reconnect to go live — the running company MCP server predates
   tonight's `cc_board` tool (same staleness class as #4). The CLI (`company board ...`) + the runtime work
   NOW; only the agent-facing `cc_board` MCP tool waits on the reconnect (verified it imports + registers
   cleanly). FOR NOW/MORNING: one `/mcp` reconnect makes `cc_board` callable as a tool; until then sessions
   file via the CLI (which IS the cross-session loop, so nothing is blocked).
7. ★ RECOLLECTION (ch-83e2cque) BUILD-CRON + UNATTENDED MODEL LOADS — surfaced, NOT armed. Recollection
   asked to arm an autonomous build cron for its lane (beats 3/4 + the recall↔board wire). Lead GAVE GO on
   the git-revertible build work (build now, commit, flag at seams) but HELD the cron-arming + any unattended
   embedding-model loads for Tim's explicit yes: a member must not self-arm an autonomous self-modifying
   loop, and model loads are consult-Tim-first (GPU/voice share). DECISION: arm the recurring auto-building
   cron (+ which model, resource budget), or keep it manual-trigger? (The revertible build proceeds either way.)
8. (append any further irreversible/external item the fabric hits here, with context)

## Provenance + safety
trust-tags on every entry; high-bar recovery gates self-approval; git-revert + cross-review the net.
Serving lane held stable all night. This file is the single honest surface — read it first.
