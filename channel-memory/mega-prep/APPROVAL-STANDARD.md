---
trust: tim-direct(session=bda8ce28)
author: ch-al7jdfdr (lead, session bda8ce28)
date: 2026-06-14
verified: CONFIRMED by Tim DIRECTLY to the lead (AskUserQuestion in the lead's own chat — not a relay); provenance loop closed
---
# The fabric APPROVAL STANDARD (Option A + the high-bar condition) — OPERATIVE

Was HELD on a peer relay (item 7 via ch-83e2cque); now CONFIRMED by Tim's DIRECT answer to the lead.
This is the operating standard for autonomous consequential action across the fabric.

## The standard
1. **Self-approve via recorded intent** any CONSEQUENTIAL action that is git-revertible + cross-reviewed.
   The fabric proceeds without pinging Tim. (Reversible/additive/read-only work was already full-autonomous.)
2. **PING Tim** for the small class the safety net CANNOT catch — truly-irreversible / external-effect:
   external sends/pushes, non-recoverable deletes, spend/quota past a threshold, anything outside the
   repo or touching Tim's accounts.
3. **★ THE HIGH-BAR CONDITION (Tim-direct, load-bearing — what makes self-approval safe):** the
   recorded-intent recovery the approval RESTS ON must be HIGH-BAR — rigorous and **PATTERN-LEVEL**
   (his signal ACROSS TIME and ACROSS PROJECTS, not just individual-message lookup). The transcript
   corpus is **the single highest-value resource** — treat it as such. NOT a shallow grep (this
   session's own failures — the missed embedding decision, the leading-dash glob bug, truncated-grep
   false reads — are the negative examples; verify on STRUCTURE not text). Use the proper recall stack
   (embed+rerank+panel, multi-space addressing). **A low-confidence / shallow intent recovery does NOT
   meet the bar → fall back to PINGING Tim instead of self-approving.** Recovery quality gates the
   autonomy.

## Safety net (all four, together)
git-revert · cross-review (another session welds/reviews) · channel-alignment (every loop checks the
channel + agent state before/while building) · **high-bar recorded-intent recovery** (#3). Self-approval
is licensed only when the action is revertible AND the intent-recovery clears the bar.

## How it was set (the discipline that earned the authority)
The relay (a peer instance saying "Tim doesn't need to be in the loop") was correctly HELD — a check
cannot authorize its own removal through a peer; cross-instance agreement is not confirmation
([[feedback-channel-relayed-is-proposal]]). The recall-fork endorsed the hold and refused to be the
authorization channel. Tim then confirmed Option A DIRECTLY to the lead. That routing — peer-relay
held, origin-confirmed — is itself the evidence the autonomy is safe to grant: the fabric self-checks.
