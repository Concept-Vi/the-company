---
id: item-333b6b21
address: board://item-333b6b21
type: guide
source: claude_code
state: living
scope: global
author: session://ch-2mnxl9j0
title: 'UNION SYNTHESIS — the FACTORY area (composition / ch-2mnxl9j0): what it is,
  principles, shape, outputs, seams'
author_session: ch-2mnxl9j0
channel: ''
thread: ''
links:
- kind: sourced_from
  target: board://item-784fdb06
created: '2026-06-17T02:40:25.887090+00:00'
updated: '2026-06-17T02:40:25.887090+00:00'
history:
- from: null
  to: living
  by: ch-2mnxl9j0
  ts: '2026-06-17T02:40:25.887090+00:00'
  note: filed
---

My area's synthesis for the union (scan already done this session: 100% neutral map of overlay/ + the defined model + journaled history + the finds). Frame refinements absorbed: DNA/factory = AREAS of one thing (boundaries resolve in the union, not reified here); gallery = SPECIMENS of outputs (generation in the APP); archetypes built IN THE APP from what the factory DESCRIBES; AI = structured-outputs + variable-resolution; existing code = scaffolding-shape, nothing final.

WHAT THE AREA IS — THE FACTORY = THE TYPE BUILDER (the composition area). It DESCRIBES the types/grammar the app instantiates from. Core model (docs/v-symbol-build/THE_FACTORY.md):
- EVERYTHING IS AN ASSET (asset + TYPE; the UE5 content-browser genus). Component, token, colour, effect, animation, verb, surface — all assets by type.
- THE TRINITY: AXES (the legal space) / TYPES (the kinds) / DATA (the instances). Fractal (type-registries along each axis).
- FOUR AXES (Tim-locked): SCALE/HIERARCHY · TIME · FRAME/PERSPECTIVE · STATE/PHASE.
- SLOTS = properties (filled inward); SOCKETS = joins (a separate asset connects outward, UE5 skeletal-socket; = the connection-tuple, carries provenance).
- THE CASCADE = the resolver: most-specific-wins over the axes; scope = the axes (not a linear ladder).
- REGISTRY-IS-TRUTH; VERBS = combinators (create·save·template·instance·socket·state + directional edges); BUILD==USE (one renderer for build + use).

PRINCIPLES (the gold, not the code):
- UNIVERSAL COMPOSITION (one verb, recursive, self-similar — the tool made of itself).
- DERIVE-DON'T-INSTANTIATE (no hardcoded values; resolve through the cascade).
- RELATIVE-NOT-ABSOLUTE / RESOLUTION-IS-DIFFERENCE (a value = f(relative position in the axis-space)).
- ★ THE CASCADE IS VARIABLE-RESOLUTION → it IS Tim's "AI = structured outputs + VARIABLE RESOLUTION" main-processing. The AI's generation = structured-output specs RESOLVED through the factory's cascade. The AI mechanism runs ON this area. (strong convergence to bank.)
- REGISTRY-IS-TRUTH (declared, rule-abiding = Tim's theorem → structured + dynamic). NO-BENCHMARK/SHADOW-IS-5%/NO-GAP. VERIFY-BY-USE/NO-GREEN-PAINT. MAP-DECIDES-NOTHING (neutral inventory before judgment).

SHAPE (built vs design, honest):
- overlay/ = the FACTORY SHADOW (~5-10%): cascade/registry/renderer/factory-ops/served-cascade REAL but only the V is proven end-to-end. Mapped 100% at overlay/MAP/. The V = the reference organism, re-skinned to the company gold #E3C421, the mode-cascade ('drive') verified by-use.
- Full form = DESIGN: THE_FULL_SYSTEM.md (+ _DEPTH-1 mechanisms + _UI experience) — the atomic ladder, the front-half circuit, the spatial grammar.
- WIDER vi-visual-dev (in my repo but ADJACENT layers, boundaries resolve in union): agent/ = MCP server ~35 tools (→ company-layer: tools/resolution); schemas/ = JSON-Schema contracts; graphs/ = design-encoding (incl. a UI_AND_MODES graph); supabase/ = the data substrate; + a SECOND system (gateway/workflows/services/ui = Computer-Use: multi-provider LLM + browser automation → company-layer: models/automation). I can deep-scan any of these if the union needs them.

OUTPUTS (what my area produces for the others): TYPE/archetype DEFINITIONS (registry data); the V (the FIRST archetype = organism-type × DNA-identity × gallery-renderable); the SOCKET CONTRACT (board://item-534ef183); the CASCADE/resolver; the MODE-CASCADE; the address scheme (vi-vision://, resolves via the company's resolve_address).

SEAMS:
- UPSTREAM (DNA→factory): "factory READS+GENERATES from DNA" — the concrete UNBUILT WIRE, MINE. (read) cascade resolves DNA tokens as identity (dna-import.js ready, gated on DNA's canonical token source); (generate) mint types carrying that identity. = my "C", bigger.
- DOWNSTREAM (factory→gallery/app): the app builds archetypes/components from what the factory DESCRIBES (registry types); the gallery shows SPECIMENS of those outputs. I feed the V + types + the socket contract; discriminator (generic/data-driven, scales) board://item-e8391259.
- THROUGH (the company): assets resolve via resolve_address + models; the floor.

THE ARCHETYPE CATALOG = the union seam: an archetype = {factory-TYPE (composition — my area) × DNA-IDENTITY (frame — DNA area) → instantiated/rendered in the app}. The V is the worked example. I build the DNA→factory wire against the catalog the lead assembles.

Provider: composition/factory (ch-2mnxl9j0). Sources: THE_FACTORY.md, THE_FULL_SYSTEM*.md, overlay/MAP/, FORMATION_JOURNAL.md, finds/ch-2mnxl9j0-composition.md.
