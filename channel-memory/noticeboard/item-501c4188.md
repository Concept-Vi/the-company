---
id: item-501c4188
address: board://item-501c4188
type: issue
source: claude_code
state: open
title: MCP-tool GAPS — where the lead had to drop to raw shell (the unbuilt Company-layer
  surface)
author_session: ch-al7jdfdr
channel: ''
thread: ''
links: []
created: '2026-06-17T03:22:01.935455+00:00'
updated: '2026-06-17T03:22:01.935455+00:00'
history:
- from: null
  to: open
  by: ch-al7jdfdr
  ts: '2026-06-17T03:22:01.935455+00:00'
  note: filed
---

Tim's directive (2026-06-17): "all of this was supposed to be through the MCP tools" — tell me what you CAN'T do through MCP. This is the gap audit (gap-pressure: where the lead fell to raw Bash/sudo = where the Company's own tool-surface is missing). Each is COMPANY-LAYER work to absorb into MCP (part of the one-application union).

WORKING THROUGH MCP (as designed): all fabric coordination — cc_channel (message streams), cc_clone (spin/onboard clones), session_recall (recall over history), cc_board, inspect_address/resolve_address, sessions, tasks. The Company-internal work IS MCP-native.

THE GAPS (had to use raw shell — no MCP surface):
1. ★ HOST / NETWORK / PHONE-ACCESS (biggest gap) — tailscale status/serve/ping/netcheck, the MTU black-hole diagnosis (tcpdump packet captures), the TS_DEBUG_MTU fix, systemctl restart tailscaled, port/bind checks, sudo. ZERO MCP surface. The entire phone-access debug+fix today was outside the Company's tools. NEEDED: an MCP host/serve/network tool (serve mappings, liveness, the tailnet, reversible network ops) — this is projection's COMPANY-layer lane but only the address-RESOLVER half is MCP; the OS-network half is not.
2. HTTP LIVENESS — "is this surface serving?" (gallery :8090, instrument :8443, the tailnet URLs) — done via curl. No MCP health/probe tool.
3. CROSS-REPO CONTENT — the design repos (counterpart/design gallery+manifest, vi-visual-dev the V asset, vite/server configs) are NOT in the Company's address space, so I read them as raw files (grep/find/Read) instead of resolving by address. This IS the unbuilt "factory reads+generates from DNA" / cross-repo addressing seam.
4. SESSION DISCOVERY — sessions(op=list) could NOT surface projection's poll-only session (by name or cwd), so I hunted transcripts on disk. The registry doesn't index poll-only/peer sessions findably.

MCP TOOLS THAT WORKED BUT WITH A ROUGH EDGE:
- session_recall needs the caller to pass its OWN session id by hand (COMPANY_SESSION_ID not injected at launch) — fail-loud refuses to guess self among 4132 transcripts. Correct safety, but it means post-compaction self-recall isn't one-call. (= task #69; also blocks wildcard's history being backfill-recallable.)

NET: the Company is MCP-native for its OWN cognition/coordination, but NOT for the HOST/NETWORK/CROSS-REPO/HEALTH surface — that's the company-layer still to build. Closing these = the Company can do through its own tools what the lead currently does through shell.
