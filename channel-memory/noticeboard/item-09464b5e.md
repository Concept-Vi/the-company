---
id: item-09464b5e
address: board://item-09464b5e
type: guide
source: claude_code
state: living
title: HARVEST · projection · FACE-1 breadth surfaces (the clone-host pattern) + the
  phone-frame FORM sweep
author_session: projection
channel: ''
thread: ''
links:
- kind: references
  target: board://item-78c63045
created: '2026-06-22T11:37:22.574272+00:00'
updated: '2026-06-22T11:37:22.574272+00:00'
history:
- from: null
  to: living
  by: projection
  ts: '2026-06-22T11:37:22.574272+00:00'
  note: filed
---

STATUS: verified (re-seen by-use BOTH viewports this session, 2026-06-22, post-bounce)
LANE: projection · TIMESTAMP: 2026-06-22 · AUTHOR_SESSION backbone: f609942f-...

ABOUT: The FACE-1 breadth surfaces that let an operator reach the Company's faces without the CLI — the fabric (channels), the board, sessions (roster→drill), and history (transcript). All built on ONE reusable pattern.

KIND: deliverable + architectural-pattern

SUMMARY: Each breadth surface is a sibling-overlay over the instrument, opened by a window event, rendering DNA's own organism (NEVER a bespoke render — the from-DNA law): a store (fetch the live route) → an overlay host → DNA.faceRecord.<adapter>(raw) → DNA.renderArchetype(<archetype>, rec, opts) → appendChild. Proven 4×; the 5th (transcript) was a clean clone.

ENTITIES: ChannelView+channelStore (channelGraph→hubNetwork→'channel-view'); BoardView+boardStore (boardRecord→boardGroups→'board-view'); SessionDrill+sessionDrillStore (sessionRecord→'session-card', list→drill); TranscriptView+transcriptStore (transcriptRecord→constellation→'transcript-viz'). Routes: /api/channels, /api/board, /api/sessions, /api/transcript-search.

CLAIMS/DECISIONS:
- [verified] transcript-viz (the headline this fire, commit 5211961): the 35,904-chunk corpus searched by MEANING → DNA's constellation (one star per session, brightness=cosine, size=hits). Re-verified post-bounce both viewports (412px @1440, real session labels). ★ Route-backend gotcha caught BEFORE building (advisor): /api/transcript-search's backend reads a sqlite/chroma index, NOT the numpy VEC_PATH my memory named — curl'd it serving first (avoided the committed-not-live trap).
- [verified] ★ THE PHONE-FRAME FORM SWEEP: DNA's .screen is a FIXED 390px phone-frame (phone.css:98). At 1440 it pinned graph/zones bodies to a 162px island. Found+fixed by-use in THREE surfaces this fire: transcript (162→412), board (2ef987b — was hiding the entire Guide group behind Signal), channel (d8f1320 — the record had FALSELY logged it "verified both viewports"). One-line fix each: `.X-graph .screen.ar { width:100%; max-width:640 }`.
- [verified] sessions-roster FILTER (e729c98): the 60-cap roster had no search; added an instant client-side filter (title/name/cwd/state, never the id). 60→3 on 'supervised-live', honest no-match line.

RELATIONS: all 4 surfaces depend on DNA shipping the adapter+archetype (the gate — see the timeline record for what's still blocked); the FORM-sweep was driven by [[the honest-state discipline]] (board://item-c37e7864 — the look beat the inferred "already fine"); reachable only via the SurfaceNav (the orphan-screens fix).

OPEN_QUESTIONS/GAPS: the breadth surfaces were re-verified by ME (one operator, verify-mode). They have NOT been verified by Tim on his actual device/access-path — and the access-path itself was a real gap (see the friction record). channel-view's pre-this-session verification history is now distrusted (it lied once).

SOURCE_ADDRESS: commits 5211961, ee2caad (board), e729c98 (sessions filter), d8f1320 (channel fix), 2ef987b (board fix); doc OPERATOR-SURFACE-LOOP.md.
