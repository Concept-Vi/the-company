---
id: item-a43918e1
address: board://item-a43918e1
type: idea
source: claude_code
state: captured
title: TRIGGER + HOOK + CC-LAUNCH-NODE registries — a reusable event→action capability
  (Tim-directed build)
author_session: ch-al7jdfdr
channel: ''
thread: ''
links:
- kind: sourced_from
  target: board://item-501c4188
created: '2026-06-17T04:12:37.269442+00:00'
updated: '2026-06-17T04:12:37.269442+00:00'
history:
- from: null
  to: captured
  by: ch-al7jdfdr
  ts: '2026-06-17T04:12:37.269442+00:00'
  note: filed
---

Tim-directed 2026-06-17 (decompressed from a dense transmission). The board-auto-research idea generalizes into a REUSABLE capability. Build in alignment with the union + core theorems (everything variables, registry-declared, composable, no hardcoding, directory-as-graph).

THE DECOMPOSITION (3 composable pieces, likely 2-3 registries in the GRAPH/MCP layer, connected to routing):
1. CC-LAUNCH NODE — the atomic composable unit Tim named: "input variable → output variable for Claude Code." A typed node = {inputs: prompt, startup-flags, model/provider, context-address; output: structured result → written to an address}. Reuses what's built (cc_clone/supervisor launch, the loadable brain, structured-output). The launched model is near-frontier (deepseek-v4-flash/kimi) → keep the node SIMPLE for reliability. This is a NODE TYPE in the compositional dataflow system.
2. TRIGGER REGISTRY — declared event→action rules (data, registry-is-truth → setting a trigger = a data edit, zero code). A trigger row = {watch: event/address-pattern (e.g. "a board item of type=issue is filed"); action: a node to run (e.g. the CC-launch node with prompt P); output-target: where to write the result (reply/attach to the TRIGGERING item's address/position)}. Lives in the GRAPH/MCP tools, CONNECTED TO ROUTING. Agents can set up triggers via the MCP tools.
3. HOOK = the firing MECHANISM (the event-interception point in the lifecycle, e.g. on-board-file, SessionStart). TRIGGER = the registry-declared rule that RIDES a hook. (Answer to Tim's "is a trigger and a hook the same thing?": related but distinct — hook = plumbing/where it fires; trigger = the declared composable rule. A trigger registry is the new thing; it uses hooks.) NEEDS a trigger/hook registry — none exists today (to confirm in the scan).

FIRST USE (dogfood): board item filed → trigger fires → CC-launch node (kimi/flash) researches it → attaches findings as a linked board item (attached_to) under the original. = the introspective self-build loop ([[project-introspective-data-building]], [[project-ai-path-of-least-resistance]]).

PROCESS (Tim's instruction, = scan-before-build law): fan-out sub-agents scan ALL relevant current code → a RESOURCE PACK / directory-as-graph MAP (drift-risk is high; do it properly) → design round (reuse-vs-build, alternatives) → implement REUSABLE (usable from the MCP tools so agents set up triggers). Open: dynamic-Workflow vs fan-out sequencing — rec = fan-out scan FIRST (discovery), Workflow for the design-rounds+implementation. Scan launched (3 scouts → build-prep/trigger-system/SCAN-*.md).

RELATES/CONNECTS-TO-EVERYTHING: routing (#71), the graph MCP registries, the board, the channel/clone-launch. Status: SCOPING (scan running).
