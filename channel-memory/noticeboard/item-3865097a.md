---
id: item-3865097a
address: board://item-3865097a
type: block
source: claude_code
state: current
title: S15 · Parts-harvest map (the FUSE-TARGET)
author_session: lead
channel: operator-surface
thread: ''
links:
- kind: part_of
  target: board://item-ed91000e
created: '2026-06-28T14:05:16.343769+00:00'
updated: '2026-06-28T14:05:16.343769+00:00'
history:
- from: null
  to: current
  by: lead
  ts: '2026-06-28T14:05:16.343769+00:00'
  note: filed
---

S15 · Parts-harvest map (the FUSE-TARGET — neither app is a base; Tim: "BOTH AI-generated fronts, NOTHING canonical, both have high-quality PARTS")
KEY INSIGHT: canvas/app and surface/app are TWO ATTEMPTS AT THE SAME SHAPE — both ride the bridge's ui:// address grammar + SSE + a fail-loud /api client. So the fuse is "take the better-executed half of each", NOT a frankenstein patchwork.

HARVEST from canvas/app (the rich substrate):
- THE ADDRESS SPINE (Tim's praised 'backend address system', strongest asset): ui://<region>/<element>/@state grammar + UI_REGISTRY (suite.py:35-54, served /api/ui_info) + resolveUiTarget keystone (useAppController.ts:1703 — ONE sink drives the view to ANY address) + data-ui-ref capture click-routing (clickMode :836, mirrors backend route_click) + companion address-keyed reads (addressHelp/refVersions/annotations/presentationPref/staleAt/upTranslate, api.ts).
- SSE wiring (useAppController.ts): seq high-water dedup (:644), kind-dispatched refreshes (:646-696), gapless reconnect (:701), fail-loud on missed refresh (:658).
- api.ts whole client (67 methods, the `jr` json-or-error fail-loud normalizer :21).
- Decision/consent components: Inbox (lanes-by-tone), ProposeAffordance (B1/B2 consent), BlastRadiusReach, ContextBundle, WireRequest, BuildIntentCard; + ForagerBar, AddressHelp, CognitionView, MobileTray, kit.tsx primitives, registryStore (reflects-never-owns).
- Responsive layout engine + fitGraph measurement (lift the grid; DROP the tldraw shell).
LEAVE from canvas/app: the debug-console LOOK (dark amber-on-black dev-harness — but it's owned by design/design-system.css, FIXABLE CENTRALLY, not a canvas rewrite); tldraw-graph-as-primary-surface (WRONG operator altitude); RegistryProposals inline styles (FIX→token classes); dead live_clock_widget; sealed LatticeView geometry (clone-whole-or-not-at-all).

HARVEST from surface/app (the clean foundation):
- THIN-CLIENT SPINE: lib/api.ts (fail-loud getJSON, 3 failure classes, :113-162); the SSE live spine (throttled pulse, PAUSE-ON-SCRUB, App.tsx:451-508); the subscribe-store pattern (boardStore.ts:33-94 + mirrors, monotonic loadSeq stale-guard); the address layer (lib/address.ts — stamp/parseUiAddress/resolveUiTarget + the ui:point twin so the RHM can spotlight what it discusses).
- surface.css "FINE PAPER" token system (690 lines, semantic --ink/--pig/--ground/--elev, tabular-nums) + the 3-form-factor layout switch classify(w,h) → Desktop/Portrait/Landscape. THE NATIVE SURFACE DESIGN IS GOOD (not the ugly part).
- wheel geometry math (lib/seed.ts: wheelGeom/placePolar/wedgePath/chordPath/dyadicLevels).
- tools-form engine (op-conditional friendly forms, applyFormMeta snake→flat adapter, resolveEnumSources w/ abort-timeout).
- the gallery HOST-SEAM idea (a React-untouched <div> so vanilla render keeps refs).
FIX/LEAVE from surface/app: the DNA GALLERY LIVE-LOAD = the "slow as balls" (index.html:18-59 — NINE render-blocking /gallery/*.js scripts + 5 CDN font families + per-open self-fetch + a 250ms×20s deep-link poll) → bundle/lazy-load, self-host fonts, DON'T inherit DNA standalone CSS; the Wheel RENDER (600 pts × 3 framer-motion SVG layers = jank → render to canvas/instanced, KEEP the math); tools-invoke endpoint (GAP — don't guess); drive/generate gallery-verb stubs; inert resolve/operator-token seams.

THE FUSE-TARGET (the operator surface): surface/app's thin-client spine + fine-paper tokens + layout switch (the clean FOUNDATION) FUSED WITH canvas/app's address spine + SSE wiring + decision/consent components (the rich SUBSTRATE), riding the bridge. The LOOK is fixed CENTRALLY in design-system.css (ties to S12 token-axes merge), once, for both heritages. LEAVE behind: the tldraw-graph altitude + the DNA-gallery live-load. This is the convergence methodology applied to the two fronts — harvest parts, leave the rest, fuse into one.
