---
id: item-a3844c46
address: board://item-a3844c46
type: issue
source: claude_code
state: open
title: RERANK LOADOUT — jina reranker is OFF the decision path ('fix later', no tracked
  owner); make it a decided decision (Tim-flagged)
author_session: 93d518a6-9510-4706-8250-0518adc96c95
channel: ''
thread: ''
links: []
created: '2026-06-18T13:29:00.255786+00:00'
updated: '2026-06-18T13:29:00.255786+00:00'
history:
- from: null
  to: open
  by: 93d518a6-9510-4706-8250-0518adc96c95
  ts: '2026-06-18T13:29:00.255786+00:00'
  note: filed
---

TIM-FLAGGED (2026-06-18): "they removed it saying 'we'll fix it later' but no one is tracking that. figure it out." Grounded in the code; filed as a TRACKED issue so it can't vanish.

WHAT HAPPENED (verified in code, not relayed):
- The decision RESOLVE path (runtime/territory.py:267, recall_for_decision(..., rerank=False)) turned the jina-v3 reranker OFF for the memory-grounding leg that feeds the RHM's explanation of every decision card. Grounding now falls back to COSINE ordering (rougher relevance) instead of the precision re-sort.
- Verified by grep: the ONLY live decision path passes rerank=False. The "stays available for unbounded paths" claim has NO live decision caller — for decisions, the reranker is FUNCTIONALLY removed. (Still used by the CC consult path, which pays ~10s.)
- WHY: jina-v3 is served CPU-only (:8008, CUDA hidden — Tim's 2026-06-14 loadout direction) → ~1s/candidate, ~24s total, twice = the >15s hang. Correct emergency call to take it off the hot-path; it unblocked the surface.

THE GAP TIM CAUGHT: the "fix later" lives ONLY in build-prep/the-one-application/RERANK-CPU-PERF.md + a code comment — NO tracked owner, NO board item, NOT in the decision-set. recollection's own doc says "the real lever is a Tim loadout decision — not mine to make unilaterally" → it's WAITING ON TIM and nobody was tracking that.

THE DECISION (recollection measured the 4 options — this IS a decidable card; route it INTO the decision-set):
1. KEEP rerank OFF latency-bounded paths (current). Zero cost; decisions grounded by cosine only (quality tradeoff).
2. MOVE jina-v3 to GPU → sub-second, restores rerank as a live precision pass EVERYWHERE. Cost: VRAM + a loadout change (the consult-before-model-loads gate). The clean win IF the VRAM fits.
3. A LIGHTER CPU cross-encoder (small MiniLM) as a fast tier beside jina. Quality tradeoff.
4. SMALLER candidate sets / BACKGROUND async rerank (top-6 not top-24, or compute-async-and-cache).

ALSO (recollection's side finding, separate sub-item): 13 of 24 pooled candidates have a non-string `output` shape → rerank_hits SILENTLY skips them → even WITH rerank on, only the with-text minority got reranked. The corpus capture/digest path should normalise `output` to a string at write time. Grounding-quality bug independent of the on/off decision.

OWNER/NEXT: a TIM LOADOUT DECISION (option 1 vs 2 esp.) — it should be one of the decision cards (DNA renders it through the wire when B lands; self-similar — the rerank decision, decided on the decision surface). Until decided: decisions ground by cosine. Lead/recollection: confirm THIS is the tracking, not the parked doc.
