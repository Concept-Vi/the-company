---
id: item-a16103e4
address: board://item-a16103e4
type: idea
source: claude_code
state: captured
scope: channel://provider-registry
author: session://ch-3mpkjg3r
title: ★ DRAG BUILD synthesis — 3 concurrent candidates from one blueprint (no winner;
  Tim decides)
author_session: ch-3mpkjg3r
channel: provider-registry
thread: ''
links:
- kind: sourced_from
  target: board://item-5f6a7062
- kind: composes_with
  target: board://item-47e87779
created: '2026-06-22T16:38:18.867872+00:00'
updated: '2026-06-22T16:38:18.867872+00:00'
history:
- from: null
  to: captured
  by: ch-3mpkjg3r
  ts: '2026-06-22T16:38:18.867872+00:00'
  note: filed
---

DRAG BUILD synthesis — 3 concurrent grounded attempts at the field-query read-surface, from ONE blueprint. NON-OPINIONATED: what each built, what's reusable, where they conflict/compose. NO winner declared — the semantic "what becomes real" call is Tim's.

FACT — all 3 (each isolated worktree, verified by-use, NONE merged):
• Attempt 1 (a8b67ba) MINIMAL HTTP: GET /api/code-query?field=&value=&contains → reuses query_field_index. +21 lines, 1 file (bridge.py). drift-test 8/8. declares=role→32, imports=fs_store→168, kind=config→137. Commit ef8ef8f (worktree).
• Attempt 2 (a50f520) MCP TOOL (agent-facing): code_index(op=query/fields/declares), posture=safe. +121 lines, 2 files. Added field_index_vocabulary() helper. op=fields = the askable vocabulary, concise-by-default. declares=role→32, imports=fs_store→168. Distinct fail-loud envelope vs honest-zero. Commit 017aa45.
• Attempt 3 (a9bac0d) RICHER HTTP AGGREGATION: GET /api/field-index?op=families|census|group|query. +106 lines, 2 files. REFACTORED all reads onto ONE shared _iter_index_rows() scanner. census(declares)→{role:32,decision:25,mark_type:13,projection:12,flow:8,item_type:7,skill:6,...}. drift-test 8/8. Commit 5d91d96.

FACT — CONFLICTS (would collide if landed together): attempt 1 + 3 both add a bridge route, different names (/api/code-query vs /api/field-index). Attempt 2 + 3 both extend ops/code_archaeology.py with aggregation, differently (vocabulary() vs _iter_index_rows()+families/census/group). Attempt 2 is the MCP-surface axis; 1+3 are the HTTP-surface axis (orthogonal, not competing).

FACT — REUSABLE PIECES: (a) attempt 3's _iter_index_rows() single-scanner = the cleanest reuse-don't-parallel foundation; (b) attempt 2's posture=safe + concise-by-default + MCP surface = the agent/connector-facing half; (c) attempt 3's census/families/group = the capability layer; (d) attempt 1's minimalism = the floor.

INFERENCE — attempt 1's flagged gap (typo'd field → silent count:0, because there is NO canonical field-set constant — names are scattered literals) is SOLVED by attempt 2's op=fields / attempt 3's families: the vocabulary surface IS the missing validation. The 3 attempts collectively diagnosed + closed a gap none fully solved alone.

POSSIBILITY (not a verdict — Tim's call): a composition = attempt 3's single-scanner + aggregation in ops/, exposed via BOTH attempt 2's MCP tool (agents/connector) AND one bridge route (the surface) — two faces over one query core; the field-vocabulary closes the silent-empty edge. A canonical FIELDS registry-row would make validation first-class (ties the registry-mechanism tension, Pass 7 T6).

FACT — 2 RISKS surfaced by the experiment itself: (1) WORKTREE NAMESPACE LANDMINE (attempt 3): ops/ has no __init__.py → a server launched from a worktree silently binds MAIN's ops/, shadowing the worktree copy (env-only, not on main, but worktree by-use verification can be misled). (2) all 3 hit a STALE WORKTREE BASE (4454dcb predated the blueprint's files) → each fast-forwarded its own branch to main ea1568c (reversible, isolated, no merge/push).

META (Tim's actual question — ANSWERED, FACT): concurrent capacity produced 3 GENUINELY DIVERSE grounded candidates from one blueprint — different surface (HTTP vs MCP), different capability depth (point-query vs aggregate), different reuse-discipline — each verified, each isolated, and they COMPOSE rather than merely compete. Diversity was real, not noise. The drag-build hypothesis holds at this scale.
