.venv/
__pycache__/
*.pyc
node_modules/
.DS_Store
store-data/
.litellm-venv/
.voice-venv/
.secrets
canvas/app/node_modules/
canvas/app/dist/
voice/models/
# Obsidian: track shared settings, ignore per-user workspace/cache state
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.obsidian/graph.json

# stray runtime debug log written at repo root
mcp_registration_debug.log
# runtime store — written by the live system, never code
.data/
# build scratch
.build/

# CLI telemetry (generated runtime data)
ops/cli/telemetry.jsonl
.adv.log
.loop.log

# Connector boundary credentials — secret, live out-of-tree (chmod 600). NEVER commit.
.boundary.env
*.boundary.env

# local CD verify drive handle (local throwaway test creds)
.cd-drive.sh
*.cd-drive.sh
.cd-drive.env
*.cd-drive.env

.cd-history.sh
*.cd-history.sh

# Tailscale Funnel / connector TLS cert + private key — secret, live out-of-tree. NEVER commit.
# (the .key is a live EC private key; the .crt is its pair — managed on the box, not in git)
*.ts.net.key
*.ts.net.crt
workstation001.tail777bc2.ts.net.key
workstation001.tail777bc2.ts.net.crt
/recollection/
/.recollection/
